# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Documentation operations
#' @description datarobot.apicore.Documentation
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#' @export
DocumentationApi <- R6::R6Class(
  "DocumentationApi",
  public = list(
    apiClient = NULL,

    #' @param apiClient A configurable `ApiClient` instance. If none provided, a new client with default configuration will be created.
    initialize = function(apiClient) {
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      } else {
        self$apiClient <- ApiClient$new()
      }
    },
    #' @description List all available document types and locales.
    #' Produces: "application/json"
    #'
    #' @details Check which document types and locales are available for generation with your account.
    #' @details This method invokes `GET /automatedDocumentOptions/` in the DataRobot Public API.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{AutomatedDocOptionsResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Available document types and locales retrieved.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$AutomatedDocumentOptionsList()
    #' }
    AutomatedDocumentOptionsList = function(...) {
      apiResponse <- private$AutomatedDocumentOptionsListWithHttpInfo(...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Request generation of automated document
    #' Produces: NA
    #'
    #' @details Request generation of an automated document that's available for your account. Below is an example request body to generate Model Compliance documentation:  ```json {"documentType": "MODEL_COMPLIANCE", "entityId": "507f191e810c19729de860ea", "outputFormat": "docx" }```  For Autopilot Summary, set a corresponding document type, "AUTOPILOT_SUMMARY", and assign a needed project ID to the "entityId" value.  After the request is sent, the jobs needed for document generation are queued. You can see the status of the generation by polling the URL in the "Location" headers. After the generation is complete, the status URL will automatically redirect you to the resource location to download the document.    #' @details This method invokes `POST /automatedDocuments/` in the DataRobot Public API.
    #' @param automatedDocCreate \link{AutomatedDocCreate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`202`** Document generation request accepted.
    #' \itemize{
    #' \item **`Location`** URL to poll document generation status: :http:get:"/api/v2/status/(statusId)/"
    #' }
    #' \item **`422`** Unable to process document generation request.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' automatedDocCreate <- AutomatedDocCreate$new() # AutomatedDocCreate |
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$AutomatedDocumentsCreate(automatedDocCreate=automatedDocCreate)
    #' }
    AutomatedDocumentsCreate = function(automatedDocCreate = NULL, ...) {
      apiResponse <- private$AutomatedDocumentsCreateWithHttpInfo(automatedDocCreate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Delete automated document.
    #' Produces: NA
    #'
    #' @details Delete a document using its ID. Example request:  &gt; ``` text &gt; DELETE https://app.datarobot.com/api/v2/automatedDocuments/5ec4ea7e41054c158c5b002f/ HTTP/1.1 &gt; Authorization: Bearer DnwzBUSTOtKBO5Sp1hoUByG4YgZwCCw4 &gt; ```
    #' @details This method invokes `DELETE /automatedDocuments/{documentId}/` in the DataRobot Public API.
    #' @param documentId character. Unique identifier of the generated document.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`** Document successfully deleted.
    #' \itemize{
    #' }
    #' \item **`404`** Provided document ID not found.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' documentId <- 'documentId_example' # character | Unique identifier of the generated document.
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$AutomatedDocumentsDelete(documentId)
    #' }
    AutomatedDocumentsDelete = function(documentId, ...) {
      apiResponse <- private$AutomatedDocumentsDeleteWithHttpInfo(documentId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List all generated documents.
    #' Produces: "application/json"
    #'
    #' @details Get information about all previously generated documents available for your account. The information includes document ID and type, ID of the entity it was generated for, time of creation, and other information. If there are no matching documents, you will get a page with an empty data array.
    #' @details This method invokes `GET /automatedDocuments/` in the DataRobot Public API.
    #' @param offset integer. Number of items to skip. Defaults to 0 if not provided.
    #' @param limit integer. Number of items to return, defaults to 100 if not provided.
    #' @param documentType \link{OneOfstringarray}. Query for one or more document types.
    #' @param outputFormat \link{OneOfstringarray}. Query for one or more output formats.
    #' @param locale \link{OneOfstringarray}. Query generated documents by one or more locales.
    #' @param entityId \link{OneOfstringarray}. Query generated documents by one or more entity IDs. For Model Compliance docs, the entity ID is a model ID. For Autopilot Summary reports, query by project IDs.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{AutomatedDocListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** List of found documents retrieved.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' offset <- 0 # integer | Number of items to skip. Defaults to 0 if not provided.
    #' limit <- 100 # integer | Number of items to return, defaults to 100 if not provided.
    #' documentType <- list(c('documentType_example')) # OneOfstringarray | Query for one or more document types.
    #' outputFormat <- list(c('outputFormat_example')) # OneOfstringarray | Query for one or more output formats.
    #' locale <- list(c('locale_example')) # OneOfstringarray | Query generated documents by one or more locales.
    #' entityId <- list(c('entityId_example')) # OneOfstringarray | Query generated documents by one or more entity IDs. For Model Compliance docs, the entity ID is a model ID. For Autopilot Summary reports, query by project IDs.
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$AutomatedDocumentsList(offset, limit, documentType=documentType, outputFormat=outputFormat, locale=locale, entityId=entityId)
    #' }
    AutomatedDocumentsList = function(offset, limit, documentType = NULL, outputFormat = NULL, locale = NULL, entityId = NULL, ...) {
      apiResponse <- private$AutomatedDocumentsListWithHttpInfo(offset, limit, documentType, outputFormat, locale, entityId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Download generated document.
    #' Produces: NA
    #'
    #' @details Download a generated Automated Documentation file.  ``` text GET https://app.datarobot.com/api/v2/automatedDocuments/5ec4ea7e41054c158c5b002f/ HTTP/1.1 Authorization: Bearer DnwzBUSTOtKBO5Sp1hoUByG4YgZwCCw4 ```  In response, you will get a file containing the generated documentation.
    #' @details This method invokes `GET /automatedDocuments/{documentId}/` in the DataRobot Public API.
    #' @param documentId character. Unique identifier of the generated document.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Download request succeeded.
    #' \itemize{
    #' \item **`ContentMinusDisposition`** Autogenerated filename ("attachment;filename&#x3D;report_name.outputFormat").
    #' \item **`ContentMinusType`** MIME type corresponding to document file format
    #' }
    #' \item **`404`** Documentation record not found.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' documentId <- 'documentId_example' # character | Unique identifier of the generated document.
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$AutomatedDocumentsRetrieve(documentId)
    #' }
    AutomatedDocumentsRetrieve = function(documentId, ...) {
      apiResponse <- private$AutomatedDocumentsRetrieveWithHttpInfo(documentId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Create a new compliance documentation template
    #' Produces: "application/json"
    #'
    #' @details Create a new compliance documentation template. One can retrieve the default DataRobot template via "GET /api/v2/complianceDocTemplates/default/" endpoint.
    #' @details This method invokes `POST /complianceDocTemplates/` in the DataRobot Public API.
    #' @param complianceDocTemplateCreate \link{ComplianceDocTemplateCreate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ComplianceDocTemplateCreateResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`201`** Model compliance documentation template created successfully
    #' \itemize{
    #' \item **`Location`** URL location of the newly created template.
    #' }
    #' \item **`422`** Template cannot be created, e.g., invalid sections or name already exists
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' complianceDocTemplateCreate <- ComplianceDocTemplateCreate$new() # ComplianceDocTemplateCreate |
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ComplianceDocTemplatesCreate(complianceDocTemplateCreate=complianceDocTemplateCreate)
    #' }
    ComplianceDocTemplatesCreate = function(complianceDocTemplateCreate = NULL, ...) {
      apiResponse <- private$ComplianceDocTemplatesCreateWithHttpInfo(complianceDocTemplateCreate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve the default documentation template
    #' Produces: "application/json"
    #'
    #' @details Retrieve the default documentation template.
    #' @details This method invokes `GET /complianceDocTemplates/default/` in the DataRobot Public API.
    #' @param type Enum < [timeSeries, normal] > Specifies the type of the default template to retrieve, either ""normal"" or ""timeSeries"". The ""normal"" template is applicable for all AutoML projects that are not time series. The ""timeSeries"" template is only applicable to time series projects.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ComplianceDocTemplateDefaultRetrieveResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Successfully retrieved default template
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' type <- "normal" # character | Specifies the type of the default template to retrieve, either ``normal`` or ``timeSeries``. The ``normal`` template is applicable for all AutoML projects that are not time series. The ``timeSeries`` template is only applicable to time series projects.
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ComplianceDocTemplatesDefaultList(type)
    #' }
    ComplianceDocTemplatesDefaultList = function(type, ...) {
      apiResponse <- private$ComplianceDocTemplatesDefaultListWithHttpInfo(type, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Delete a compliance documentation template
    #' Produces: NA
    #'
    #' @details Delete a compliance documentation template. Documentation previously generated using this template will remain unchanged.
    #' @details This method invokes `DELETE /complianceDocTemplates/{templateId}/` in the DataRobot Public API.
    #' @param templateId character. The Id of a model compliance document template accessible by the user
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`** Template deleted successfully
    #' \itemize{
    #' }
    #' \item **`403`** Insufficient permissions to delete template
    #' \itemize{
    #' }
    #' \item **`404`** Template not found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' templateId <- 'templateId_example' # character | The Id of a model compliance document template accessible by the user
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ComplianceDocTemplatesDelete(templateId)
    #' }
    ComplianceDocTemplatesDelete = function(templateId, ...) {
      apiResponse <- private$ComplianceDocTemplatesDeleteWithHttpInfo(templateId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List compliance documentation templates
    #' Produces: "application/json"
    #'
    #' @details List user's custom-built compliance documentation templates.
    #' @details This method invokes `GET /complianceDocTemplates/` in the DataRobot Public API.
    #' @param offset integer. Number of results to skip.
    #' @param limit integer. At most this many results are returned. The default may change without notice.
    #' @param namePart character. When present, only return templates with names that contain the given substring.
    #' @param orderBy Enum < [id, -id] > Sort order to apply to the dataset list. Prefix the attribute name with a dash to sort in descending order (e.g., orderBy&#x3D;'-id').
    #' @param labels character. Name of labels to filter by.
    #' @param projectType Enum < [autoMl, timeSeries] > Type of project templates to search for. If not specified, returns all project templates types.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ComplianceDocTemplateListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Successfully returned list of templates
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' offset <- 0 # integer | Number of results to skip.
    #' limit <- 100 # integer | At most this many results are returned. The default may change without notice.
    #' namePart <- "" # character | When present, only return templates with names that contain the given substring.
    #' orderBy <- "id" # character | Sort order to apply to the dataset list. Prefix the attribute name with a dash to sort in descending order (e.g., orderBy='-id').
    #' labels <- 'labels_example' # character | Name of labels to filter by.
    #' projectType <- 'projectType_example' # character | Type of project templates to search for. If not specified, returns all project templates types.
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ComplianceDocTemplatesList(offset=offset, limit=limit, namePart=namePart, orderBy=orderBy, labels=labels, projectType=projectType)
    #' }
    ComplianceDocTemplatesList = function(offset = 0, limit = 100, namePart = "", orderBy = "id", labels = NULL, projectType = NULL, ...) {
      apiResponse <- private$ComplianceDocTemplatesListWithHttpInfo(offset, limit, namePart, orderBy, labels, projectType, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Update an existing model compliance documentation template
    #' Produces: NA
    #'
    #' @details Update an existing model compliance documentation template with the given "templateId". The template must be accessible by the user. If the "templateId" is not found for the user, the update cannot be performed. For a description of the template "sections" object options, see the sample "sections" on the schema below.
    #' @details This method invokes `PATCH /complianceDocTemplates/{templateId}/` in the DataRobot Public API.
    #' @param templateId character. The Id of a model compliance document template accessible by the user
    #' @param complianceDocTemplateUpdate \link{ComplianceDocTemplateUpdate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`** Model compliance documentation template updated successfully
    #' \itemize{
    #' }
    #' \item **`403`** Not enough permissions to edit template
    #' \itemize{
    #' }
    #' \item **`404`** Template not found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' templateId <- 'templateId_example' # character | The Id of a model compliance document template accessible by the user
    #' complianceDocTemplateUpdate <- ComplianceDocTemplateUpdate$new() # ComplianceDocTemplateUpdate |
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ComplianceDocTemplatesPatch(templateId, complianceDocTemplateUpdate=complianceDocTemplateUpdate)
    #' }
    ComplianceDocTemplatesPatch = function(templateId, complianceDocTemplateUpdate = NULL, ...) {
      apiResponse <- private$ComplianceDocTemplatesPatchWithHttpInfo(templateId, complianceDocTemplateUpdate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve a documentation template
    #' Produces: "application/json"
    #'
    #' @details Retrieve a JSON representation of a custom Compliance Documentation template.
    #' @details This method invokes `GET /complianceDocTemplates/{templateId}/` in the DataRobot Public API.
    #' @param templateId character. The Id of a model compliance document template accessible by the user
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{TemplateResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Successfully retrieved template
    #' \itemize{
    #' }
    #' \item **`404`** No matching template owned by this user was found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' templateId <- 'templateId_example' # character | The Id of a model compliance document template accessible by the user
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ComplianceDocTemplatesRetrieve(templateId)
    #' }
    ComplianceDocTemplatesRetrieve = function(templateId, ...) {
      apiResponse <- private$ComplianceDocTemplatesRetrieveWithHttpInfo(templateId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Get template's access control list
    #' Produces: "application/json"
    #'
    #' @details Get a list of users, groups and organizations who have access to this template and their roles on the template.
    #' @details This method invokes `GET /complianceDocTemplates/{templateId}/sharedRoles/` in the DataRobot Public API.
    #' @param offset integer. This many results will be skipped
    #' @param limit integer. At most this many results are returned
    #' @param templateId character. The template identifier
    #' @param id character. Only return roles for a user, group or organization with this identifier.
    #' @param name character. Only return roles for a user, group or organization with this name.
    #' @param shareRecipientType Enum < [user, group, organization] > List access controls for recipients with this type.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{SharingListV2Response}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** The template&#39;s access control list.
    #' \itemize{
    #' }
    #' \item **`400`** Both username and userId were specified
    #' \itemize{
    #' }
    #' \item **`404`** Either the template does not exist or the user does not have permissions to view the template.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' offset <- 0 # integer | This many results will be skipped
    #' limit <- 10 # integer | At most this many results are returned
    #' templateId <- 'templateId_example' # character | The template identifier
    #' id <- 'id_example' # character | Only return roles for a user, group or organization with this identifier.
    #' name <- 'name_example' # character | Only return roles for a user, group or organization with this name.
    #' shareRecipientType <- 'shareRecipientType_example' # character | List access controls for recipients with this type.
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ComplianceDocTemplatesSharedRolesList(offset, limit, templateId, id=id, name=name, shareRecipientType=shareRecipientType)
    #' }
    ComplianceDocTemplatesSharedRolesList = function(offset, limit, templateId, id = NULL, name = NULL, shareRecipientType = NULL, ...) {
      apiResponse <- private$ComplianceDocTemplatesSharedRolesListWithHttpInfo(offset, limit, templateId, id, name, shareRecipientType, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Update template's access controls
    #' Produces: NA
    #'
    #' @details Set roles for users on this template.
    #' @details This method invokes `PATCH /complianceDocTemplates/{templateId}/sharedRoles/` in the DataRobot Public API.
    #' @param templateId character. The template identifier
    #' @param sharedRolesUpdate \link{SharedRolesUpdate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`** Roles updated successfully.
    #' \itemize{
    #' }
    #' \item **`409`** The request would leave the template without an owner.
    #' \itemize{
    #' }
    #' \item **`422`** One of the users in the request does not exist, or the request is otherwise invalid
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' templateId <- 'templateId_example' # character | The template identifier
    #' sharedRolesUpdate <- SharedRolesUpdate$new() # SharedRolesUpdate |
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ComplianceDocTemplatesSharedRolesPatchMany(templateId, sharedRolesUpdate=sharedRolesUpdate)
    #' }
    ComplianceDocTemplatesSharedRolesPatchMany = function(templateId, sharedRolesUpdate = NULL, ...) {
      apiResponse <- private$ComplianceDocTemplatesSharedRolesPatchManyWithHttpInfo(templateId, sharedRolesUpdate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Initialize compliance documentation pre-processing for the model
    #' Produces: NA
    #'
    #' @details Initialize compliance documentation pre-processing for the current model. This route must be called before generating documentation for a custom model.
    #' @details This method invokes `POST /modelComplianceDocsInitializations/{entityId}/` in the DataRobot Public API.
    #' @param entityId character. The ID of the model or model package the document corresponds to.
    #' @param body object.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`202`** Job to initialize compliance documentation pre-processing successfully started.
    #' \itemize{
    #' \item **`Location`** URL to poll for getting a status of the job: :http:get:"/api/v2/status/(statusId)/"
    #' }
    #' \item **`404`** Model not found.
    #' \itemize{
    #' }
    #' \item **`422`** Cannot prepare model for compliance document generation.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' entityId <- 'entityId_example' # character | The ID of the model or model package the document corresponds to.
    #' body <- NULL # object |
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ModelComplianceDocsInitializationsCreateOne(entityId, body=body)
    #' }
    ModelComplianceDocsInitializationsCreateOne = function(entityId, body = NULL, ...) {
      apiResponse <- private$ModelComplianceDocsInitializationsCreateOneWithHttpInfo(entityId, body, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Check if compliance documentation pre-processing is initialized for the model
    #' Produces: "application/json"
    #'
    #' @details Check if compliance documentation pre-processing is initialized for the current model. This is only required for custom models.
    #' @details This method invokes `GET /modelComplianceDocsInitializations/{entityId}/` in the DataRobot Public API.
    #' @param entityId character. The ID of the model or model package the document corresponds to.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ModelComplianceDocsInitializationsResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Retrieve information about the status of the compliance documentation pre-preprocessing initialization.
    #' \itemize{
    #' }
    #' \item **`404`** Model not found.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' entityId <- 'entityId_example' # character | The ID of the model or model package the document corresponds to.
    #'
    #' api.instance <- DocumentationApi$new()
    #' result <- api.instance$ModelComplianceDocsInitializationsRetrieve(entityId)
    #' }
    ModelComplianceDocsInitializationsRetrieve = function(entityId, ...) {
      apiResponse <- private$ModelComplianceDocsInitializationsRetrieveWithHttpInfo(entityId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    }
  ),
  private = list(
    # A helper function to invoke the API operation `AutomatedDocumentOptionsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    AutomatedDocumentOptionsListWithHttpInfo = function(...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      body <- NULL
      urlPath <- "/automatedDocumentOptions/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "AutomatedDocOptionsResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `AutomatedDocumentsCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    AutomatedDocumentsCreateWithHttpInfo = function(automatedDocCreate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (!missing(`automatedDocCreate`) && isa(automatedDocCreate, c("AutomatedDocCreate", "R6"))) {
        body <- `automatedDocCreate`$toJSON()
      } else {
        stop("AutomatedDocumentsCreateWithHttpInfo requires parameter automatedDocCreate to be of type AutomatedDocCreate.")
      }

      urlPath <- "/automatedDocuments/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `AutomatedDocumentsDelete`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    AutomatedDocumentsDeleteWithHttpInfo = function(documentId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`documentId`)) {
        stop("Missing required parameter `documentId`.")
      }

      body <- NULL
      urlPath <- "/automatedDocuments/{documentId}/"
      if (!missing(`documentId`)) {
        urlPath <- gsub(paste0("\\{", "documentId", "\\}"), URLencode(as.character(`documentId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "DELETE",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `AutomatedDocumentsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    AutomatedDocumentsListWithHttpInfo = function(offset, limit, documentType = NULL, outputFormat = NULL, locale = NULL, entityId = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      if (!is.null(documentType)) {
        queryParams["documentType"] <- paste0(documentType, collapse = ",")
      }

      if (!is.null(outputFormat)) {
        queryParams["outputFormat"] <- paste0(outputFormat, collapse = ",")
      }

      if (!is.null(locale)) {
        queryParams["locale"] <- paste0(locale, collapse = ",")
      }

      if (!is.null(entityId)) {
        queryParams["entityId"] <- paste0(entityId, collapse = ",")
      }

      body <- NULL
      urlPath <- "/automatedDocuments/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "AutomatedDocListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `AutomatedDocumentsRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    AutomatedDocumentsRetrieveWithHttpInfo = function(documentId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`documentId`)) {
        stop("Missing required parameter `documentId`.")
      }

      body <- NULL
      urlPath <- "/automatedDocuments/{documentId}/"
      if (!missing(`documentId`)) {
        urlPath <- gsub(paste0("\\{", "documentId", "\\}"), URLencode(as.character(`documentId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ComplianceDocTemplatesCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ComplianceDocTemplatesCreateWithHttpInfo = function(complianceDocTemplateCreate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (!missing(`complianceDocTemplateCreate`) && isa(complianceDocTemplateCreate, c("ComplianceDocTemplateCreate", "R6"))) {
        body <- `complianceDocTemplateCreate`$toJSON()
      } else {
        stop("ComplianceDocTemplatesCreateWithHttpInfo requires parameter complianceDocTemplateCreate to be of type ComplianceDocTemplateCreate.")
      }

      urlPath <- "/complianceDocTemplates/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ComplianceDocTemplateCreateResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ComplianceDocTemplatesDefaultList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ComplianceDocTemplatesDefaultListWithHttpInfo = function(type, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }

      queryParams["type"] <- type

      body <- NULL
      urlPath <- "/complianceDocTemplates/default/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ComplianceDocTemplateDefaultRetrieveResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ComplianceDocTemplatesDelete`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ComplianceDocTemplatesDeleteWithHttpInfo = function(templateId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`templateId`)) {
        stop("Missing required parameter `templateId`.")
      }

      body <- NULL
      urlPath <- "/complianceDocTemplates/{templateId}/"
      if (!missing(`templateId`)) {
        urlPath <- gsub(paste0("\\{", "templateId", "\\}"), URLencode(as.character(`templateId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "DELETE",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ComplianceDocTemplatesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ComplianceDocTemplatesListWithHttpInfo = function(offset = 0, limit = 100, namePart = "", orderBy = "id", labels = NULL, projectType = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["namePart"] <- namePart

      queryParams["orderBy"] <- orderBy

      queryParams["labels"] <- labels

      queryParams["projectType"] <- projectType

      body <- NULL
      urlPath <- "/complianceDocTemplates/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ComplianceDocTemplateListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ComplianceDocTemplatesPatch`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ComplianceDocTemplatesPatchWithHttpInfo = function(templateId, complianceDocTemplateUpdate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`templateId`)) {
        stop("Missing required parameter `templateId`.")
      }

      if (!missing(`complianceDocTemplateUpdate`) && isa(complianceDocTemplateUpdate, c("ComplianceDocTemplateUpdate", "R6"))) {
        body <- `complianceDocTemplateUpdate`$toJSON()
      } else {
        stop("ComplianceDocTemplatesPatchWithHttpInfo requires parameter complianceDocTemplateUpdate to be of type ComplianceDocTemplateUpdate.")
      }

      urlPath <- "/complianceDocTemplates/{templateId}/"
      if (!missing(`templateId`)) {
        urlPath <- gsub(paste0("\\{", "templateId", "\\}"), URLencode(as.character(`templateId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ComplianceDocTemplatesRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ComplianceDocTemplatesRetrieveWithHttpInfo = function(templateId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`templateId`)) {
        stop("Missing required parameter `templateId`.")
      }

      body <- NULL
      urlPath <- "/complianceDocTemplates/{templateId}/"
      if (!missing(`templateId`)) {
        urlPath <- gsub(paste0("\\{", "templateId", "\\}"), URLencode(as.character(`templateId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "TemplateResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ComplianceDocTemplatesSharedRolesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ComplianceDocTemplatesSharedRolesListWithHttpInfo = function(offset, limit, templateId, id = NULL, name = NULL, shareRecipientType = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`templateId`)) {
        stop("Missing required parameter `templateId`.")
      }

      queryParams["id"] <- id

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["name"] <- name

      queryParams["shareRecipientType"] <- shareRecipientType

      body <- NULL
      urlPath <- "/complianceDocTemplates/{templateId}/sharedRoles/"
      if (!missing(`templateId`)) {
        urlPath <- gsub(paste0("\\{", "templateId", "\\}"), URLencode(as.character(`templateId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "SharingListV2Response", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ComplianceDocTemplatesSharedRolesPatchMany`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ComplianceDocTemplatesSharedRolesPatchManyWithHttpInfo = function(templateId, sharedRolesUpdate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`templateId`)) {
        stop("Missing required parameter `templateId`.")
      }

      if (!missing(`sharedRolesUpdate`) && isa(sharedRolesUpdate, c("SharedRolesUpdate", "R6"))) {
        body <- `sharedRolesUpdate`$toJSON()
      } else {
        stop("ComplianceDocTemplatesSharedRolesPatchManyWithHttpInfo requires parameter sharedRolesUpdate to be of type SharedRolesUpdate.")
      }

      urlPath <- "/complianceDocTemplates/{templateId}/sharedRoles/"
      if (!missing(`templateId`)) {
        urlPath <- gsub(paste0("\\{", "templateId", "\\}"), URLencode(as.character(`templateId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ModelComplianceDocsInitializationsCreateOne`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ModelComplianceDocsInitializationsCreateOneWithHttpInfo = function(entityId, body = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entityId`)) {
        stop("Missing required parameter `entityId`.")
      }

      if (!missing(`body`) && isa(body, c("object", "R6"))) {
        body <- `body`$toJSON()
      } else {
        stop("ModelComplianceDocsInitializationsCreateOneWithHttpInfo requires parameter body to be of type object.")
      }

      urlPath <- "/modelComplianceDocsInitializations/{entityId}/"
      if (!missing(`entityId`)) {
        urlPath <- gsub(paste0("\\{", "entityId", "\\}"), URLencode(as.character(`entityId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ModelComplianceDocsInitializationsRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ModelComplianceDocsInitializationsRetrieveWithHttpInfo = function(entityId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entityId`)) {
        stop("Missing required parameter `entityId`.")
      }

      body <- NULL
      urlPath <- "/modelComplianceDocsInitializations/{entityId}/"
      if (!missing(`entityId`)) {
        urlPath <- gsub(paste0("\\{", "entityId", "\\}"), URLencode(as.character(`entityId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ModelComplianceDocsInitializationsResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    }
  )
)
