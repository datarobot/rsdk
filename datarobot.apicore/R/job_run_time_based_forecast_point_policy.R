# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title JobRunTimeBasedForecastPointPolicy
#'
#' @description JobRunTimeBasedForecastPointPolicy Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field configuration  \link{JobRunTimeBasedForecastPointPolicySettings} [optional]
#'
#' @field type  character Type of the forecast point policy. Forecast point will be based on the scheduled run time of the job or the current moment in UTC if job was launched manually. Run time can be adjusted backwards or forwards.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
JobRunTimeBasedForecastPointPolicy <- R6::R6Class(
  "JobRunTimeBasedForecastPointPolicy",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(configuration = NULL, type = NULL) {
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`configuration`)) {
        stopifnot(R6::is.R6(`configuration`))
      }
    }
  ),
  public = list(
    `configuration` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param configuration
    #' @param type Type of the forecast point policy. Forecast point will be based on the scheduled run time of the job or the current moment in UTC if job was launched manually. Run time can be adjusted backwards or forwards.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`type` = NULL, `configuration` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(configuration, type)
      }
      self$`configuration` <- `configuration`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(configuration = self$`configuration`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`configuration`)) {
          sprintf(
            '"configuration":
          %s
        ',
            jsonlite::toJSON(self$`configuration`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param JobRunTimeBasedForecastPointPolicyJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(JobRunTimeBasedForecastPointPolicyJson, validateParams = FALSE) {
      JobRunTimeBasedForecastPointPolicyObject <- jsonlite::fromJSON(JobRunTimeBasedForecastPointPolicyJson)
      self$`configuration` <- JobRunTimeBasedForecastPointPolicySettings$new()$fromJSON(jsonlite::toJSON(JobRunTimeBasedForecastPointPolicyObject$configuration, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`type` <- JobRunTimeBasedForecastPointPolicyObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
