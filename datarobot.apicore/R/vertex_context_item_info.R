# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title VertexContextItemInfo
#'
#' @description VertexContextItemInfo Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field inputs  list( character ) A specification of requirements of the inputs of the vertex.
#'
#' @field outputs  list( character ) A specification of expectations of the output of the vertex.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
VertexContextItemInfo <- R6::R6Class(
  "VertexContextItemInfo",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`inputs` = NULL, `outputs` = NULL) {
      if (!is.null(`inputs`)) {
        stopifnot(is.vector(`inputs`))
      }
      if (!is.null(`outputs`)) {
        stopifnot(is.vector(`outputs`))
      }
    }
  ),
  public = list(
    `inputs` = NULL,
    `outputs` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param inputs A specification of requirements of the inputs of the vertex.
    #' @param outputs A specification of expectations of the output of the vertex.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`inputs` = NULL, `outputs` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`inputs`, `outputs`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(inputs, outputs)
      }
      sapply(`inputs`, function(x) stopifnot(is.character(x)))
      sapply(`outputs`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(inputs = self$`inputs`, outputs = self$`outputs`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`inputs`)) {
          sprintf(
            '"inputs":
            [%s]
                  ',
            paste(unlist(lapply(self$`inputs`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`outputs`)) {
          sprintf(
            '"outputs":
            [%s]
                  ',
            paste(unlist(lapply(self$`outputs`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param VertexContextItemInfoJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(VertexContextItemInfoJson, validateParams = FALSE) {
      VertexContextItemInfoObject <- jsonlite::fromJSON(VertexContextItemInfoJson)
      self$`inputs` <- ApiClient$new()$deserializeObj(VertexContextItemInfoObject$`inputs`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`outputs` <- ApiClient$new()$deserializeObj(VertexContextItemInfoObject$`outputs`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
