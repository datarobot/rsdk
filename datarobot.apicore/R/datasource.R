# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Datasource
#'
#' @description Datasource Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field categories  \link{OneOfstringarray} [optional] An array of strings describing the intended use of the dataset.
#'
#' @field credentialData  \link{OneOfBasicCredentialsS3CredentialsOAuthCredentials} [optional] The credentials to authenticate with the database, to be used instead of credential ID.
#'
#' @field credentialId  character [optional] The ID of the set of credentials to authenticate with the database.
#'
#' @field dataSourceId  character The identifier for the DataSource to use as the source of data.
#'
#' @field doSnapshot  character [optional] If true, create a snapshot dataset; if false, create a remote dataset. Creating snapshots from non-file sources requires an additional permission, &#x60;Enable Create Snapshot Data Source&#x60;.
#'
#' @field password  character [optional] The password (in cleartext) for database authentication. The password will be encrypted on the server side in scope of HTTP request and never saved or stored. DEPRECATED: please use credentialId or credentialData instead.
#'
#' @field persistDataAfterIngestion  character [optional] If true, will enforce saving all data (for download and sampling) and will allow a user to view extended data profile (which includes data statistics like min/max/median/mean, histogram, etc.). If false, will not enforce saving data. The data schema (feature names and types) still will be available. Specifying this parameter to false and &#x60;doSnapshot&#x60; to true will result in an error.
#'
#' @field sampleSize  \link{SampleSize} [optional]
#'
#' @field useKerberos  character [optional] If true, use kerberos authentication for database authentication.
#'
#' @field user  character [optional] The username for database authentication. DEPRECATED: please use credentialId or credentialData instead.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Datasource <- R6::R6Class(
  "Datasource",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`categories` = NULL, `credentialData` = NULL, `credentialId` = NULL, `dataSourceId` = NULL, `doSnapshot` = NULL, `password` = NULL, `persistDataAfterIngestion` = NULL, `sampleSize` = NULL, `useKerberos` = NULL, `user` = NULL) {
      if (!is.null(`dataSourceId`)) {
        stopifnot(is.character(`dataSourceId`), length(`dataSourceId`) == 1)
      }
      if (!is.null(`categories`)) {
        .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      }
      if (!is.null(`credentialData`)) {
        .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`doSnapshot`)) {
        stopifnot(is.logical(`doSnapshot`), length(`doSnapshot`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`persistDataAfterIngestion`)) {
        stopifnot(is.logical(`persistDataAfterIngestion`), length(`persistDataAfterIngestion`) == 1)
      }
      if (!is.null(`sampleSize`)) {
        stopifnot(R6::is.R6(`sampleSize`))
      }
      if (!is.null(`useKerberos`)) {
        stopifnot(is.logical(`useKerberos`), length(`useKerberos`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `categories` = NULL,
    `credentialData` = NULL,
    `credentialId` = NULL,
    `dataSourceId` = NULL,
    `doSnapshot` = NULL,
    `password` = NULL,
    `persistDataAfterIngestion` = NULL,
    `sampleSize` = NULL,
    `useKerberos` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param categories An array of strings describing the intended use of the dataset.
    #' @param credentialData The credentials to authenticate with the database, to be used instead of credential ID.
    #' @param credentialId The ID of the set of credentials to authenticate with the database.
    #' @param dataSourceId The identifier for the DataSource to use as the source of data.
    #' @param doSnapshot If true, create a snapshot dataset; if false, create a remote dataset. Creating snapshots from non-file sources requires an additional permission, &#x60;Enable Create Snapshot Data Source&#x60;.
    #' @param password The password (in cleartext) for database authentication. The password will be encrypted on the server side in scope of HTTP request and never saved or stored. DEPRECATED: please use credentialId or credentialData instead.
    #' @param persistDataAfterIngestion If true, will enforce saving all data (for download and sampling) and will allow a user to view extended data profile (which includes data statistics like min/max/median/mean, histogram, etc.). If false, will not enforce saving data. The data schema (feature names and types) still will be available. Specifying this parameter to false and &#x60;doSnapshot&#x60; to true will result in an error.
    #' @param sampleSize
    #' @param useKerberos If true, use kerberos authentication for database authentication.
    #' @param user The username for database authentication. DEPRECATED: please use credentialId or credentialData instead.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataSourceId` = NULL, `categories` = NULL, `credentialData` = NULL, `credentialId` = NULL, `doSnapshot` = TRUE, `password` = NULL, `persistDataAfterIngestion` = NULL, `sampleSize` = NULL, `useKerberos` = FALSE, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`dataSourceId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(categories, credentialData, credentialId, dataSourceId, doSnapshot, password, persistDataAfterIngestion, sampleSize, useKerberos, user)
      }
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      self$`credentialId` <- `credentialId`
      self$`dataSourceId` <- `dataSourceId`
      self$`doSnapshot` <- `doSnapshot`
      self$`password` <- `password`
      self$`persistDataAfterIngestion` <- `persistDataAfterIngestion`
      self$`sampleSize` <- `sampleSize`
      self$`useKerberos` <- `useKerberos`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(categories = self$`categories`, credentialData = self$`credentialData`, credentialId = self$`credentialId`, dataSourceId = self$`dataSourceId`, doSnapshot = self$`doSnapshot`, password = self$`password`, persistDataAfterIngestion = self$`persistDataAfterIngestion`, sampleSize = self$`sampleSize`, useKerberos = self$`useKerberos`, user = self$`user`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`categories`)) {
          sprintf(
            '"categories":
            %s
      ',
            self$`categories`
          )
        },
        if (!is.null(self$`credentialData`)) {
          sprintf(
            '"credentialData":
            %s
      ',
            jsonlite::toJSON(self$`credentialData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`dataSourceId`)) {
          sprintf(
            '"dataSourceId":
            "%s"
                  ',
            self$`dataSourceId`
          )
        },
        if (!is.null(self$`doSnapshot`)) {
          sprintf(
            '"doSnapshot":
            %s
                  ',
            tolower(self$`doSnapshot`)
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`persistDataAfterIngestion`)) {
          sprintf(
            '"persistDataAfterIngestion":
            %s
                  ',
            tolower(self$`persistDataAfterIngestion`)
          )
        },
        if (!is.null(self$`sampleSize`)) {
          sprintf(
            '"sampleSize":
            %s
      ',
            jsonlite::toJSON(self$`sampleSize`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`useKerberos`)) {
          sprintf(
            '"useKerberos":
            %s
                  ',
            tolower(self$`useKerberos`)
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasourceJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasourceJson, validateParams = FALSE) {
      DatasourceObject <- jsonlite::fromJSON(DatasourceJson)
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = DatasourceObject$categories)
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = DatasourceObject$credentialData)
      self$`credentialId` <- DatasourceObject$`credentialId`
      self$`dataSourceId` <- DatasourceObject$`dataSourceId`
      self$`doSnapshot` <- DatasourceObject$`doSnapshot`
      self$`password` <- DatasourceObject$`password`
      self$`persistDataAfterIngestion` <- DatasourceObject$`persistDataAfterIngestion`
      self$`sampleSize` <- SampleSize$new()$fromJSON(jsonlite::toJSON(DatasourceObject$sampleSize, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`useKerberos` <- DatasourceObject$`useKerberos`
      self$`user` <- DatasourceObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
