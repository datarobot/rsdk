# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataStoreSQLVerify
#'
#' @description DataStoreSQLVerify Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialId  character [optional] ID of the set of credentials to use instead of username and password.
#'
#' @field maxRows  integer Maximum number of rows of data to return if successful.
#'
#' @field password  character [optional] Password for data store authentication.
#'
#' @field query  character SQL query to verify.
#'
#' @field useKerberos  character [optional] Whether to use Kerberos for data store authentication.
#'
#' @field user  character [optional] Username for data store authentication.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataStoreSQLVerify <- R6::R6Class(
  "DataStoreSQLVerify",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(credentialId = NULL, maxRows = NULL, password = NULL, query = NULL, useKerberos = NULL, user = NULL) {
      if (!is.null(`maxRows`)) {
        stopifnot(is.numeric(`maxRows`), length(`maxRows`) == 1)
      }
      if (!is.null(`query`)) {
        stopifnot(is.character(`query`), length(`query`) == 1)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`useKerberos`)) {
        stopifnot(is.logical(`useKerberos`), length(`useKerberos`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `credentialId` = NULL,
    `maxRows` = NULL,
    `password` = NULL,
    `query` = NULL,
    `useKerberos` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialId ID of the set of credentials to use instead of username and password.
    #' @param maxRows Maximum number of rows of data to return if successful.
    #' @param password Password for data store authentication.
    #' @param query SQL query to verify.
    #' @param useKerberos Whether to use Kerberos for data store authentication.
    #' @param user Username for data store authentication.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`maxRows` = NULL, `query` = NULL, `credentialId` = NULL, `password` = NULL, `useKerberos` = FALSE, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`maxRows`, `query`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(credentialId, maxRows, password, query, useKerberos, user)
      }
      self$`credentialId` <- `credentialId`
      self$`maxRows` <- `maxRows`
      self$`password` <- `password`
      self$`query` <- `query`
      self$`useKerberos` <- `useKerberos`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(credentialId = self$`credentialId`, maxRows = self$`maxRows`, password = self$`password`, query = self$`query`, useKerberos = self$`useKerberos`, user = self$`user`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`maxRows`)) {
          sprintf(
            '"maxRows":
            %d
                  ',
            self$`maxRows`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`query`)) {
          sprintf(
            '"query":
            "%s"
                  ',
            self$`query`
          )
        },
        if (!is.null(self$`useKerberos`)) {
          sprintf(
            '"useKerberos":
            %s
                  ',
            tolower(self$`useKerberos`)
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataStoreSQLVerifyJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataStoreSQLVerifyJson, validateParams = FALSE) {
      DataStoreSQLVerifyObject <- jsonlite::fromJSON(DataStoreSQLVerifyJson)
      self$`credentialId` <- DataStoreSQLVerifyObject$`credentialId`
      self$`maxRows` <- DataStoreSQLVerifyObject$`maxRows`
      self$`password` <- DataStoreSQLVerifyObject$`password`
      self$`query` <- DataStoreSQLVerifyObject$`query`
      self$`useKerberos` <- DataStoreSQLVerifyObject$`useKerberos`
      self$`user` <- DataStoreSQLVerifyObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
