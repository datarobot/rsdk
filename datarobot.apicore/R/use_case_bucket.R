# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseBucket
#'
#' @description UseCaseBucket Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field period  \link{UseCaseBucketPeriod}
#'
#' @field predictionsCount  integer Total number of predictions that happened in this bucket
#'
#' @field realizedValue  integer [optional] Total amount of value realised during this bucket
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseBucket <- R6::R6Class(
  "UseCaseBucket",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(period = NULL, predictionsCount = NULL, realizedValue = NULL) {
      if (!is.null(`period`)) {
        stopifnot(R6::is.R6(`period`))
      }
      if (!is.null(`predictionsCount`)) {
        stopifnot(is.numeric(`predictionsCount`), length(`predictionsCount`) == 1)
      }
      if (!is.null(`realizedValue`)) {
        stopifnot(is.numeric(`realizedValue`), length(`realizedValue`) == 1)
      }
    }
  ),
  public = list(
    `period` = NULL,
    `predictionsCount` = NULL,
    `realizedValue` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param period
    #' @param predictionsCount Total number of predictions that happened in this bucket
    #' @param realizedValue Total amount of value realised during this bucket
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`period` = NULL, `predictionsCount` = NULL, `realizedValue` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`period`, `predictionsCount`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(period, predictionsCount, realizedValue)
      }
      self$`period` <- `period`
      self$`predictionsCount` <- `predictionsCount`
      self$`realizedValue` <- `realizedValue`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(period = self$`period`, predictionsCount = self$`predictionsCount`, realizedValue = self$`realizedValue`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`period`)) {
          sprintf(
            '"period":
          %s
        ',
            jsonlite::toJSON(self$`period`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`predictionsCount`)) {
          sprintf(
            '"predictionsCount":
            %d
                  ',
            self$`predictionsCount`
          )
        },
        if (!is.null(self$`realizedValue`)) {
          sprintf(
            '"realizedValue":
            %d
                  ',
            self$`realizedValue`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseBucketJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseBucketJson, validateParams = FALSE) {
      UseCaseBucketObject <- jsonlite::fromJSON(UseCaseBucketJson)
      self$`period` <- UseCaseBucketPeriod$new()$fromJSON(jsonlite::toJSON(UseCaseBucketObject$period, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`predictionsCount` <- UseCaseBucketObject$`predictionsCount`
      self$`realizedValue` <- UseCaseBucketObject$`realizedValue`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
