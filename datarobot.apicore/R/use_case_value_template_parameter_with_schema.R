# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseValueTemplateParameterWithSchema
#'
#' @description UseCaseValueTemplateParameterWithSchema Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field parameterName  character Name of the parameter
#'
#' @field schema  \link{UseCaseValueTemplateSchema}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseValueTemplateParameterWithSchema <- R6::R6Class(
  "UseCaseValueTemplateParameterWithSchema",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(parameterName = NULL, schema = NULL) {
      if (!is.null(`parameterName`)) {
        stopifnot(is.character(`parameterName`), length(`parameterName`) == 1)
      }
      if (!is.null(`schema`)) {
        stopifnot(R6::is.R6(`schema`))
      }
    }
  ),
  public = list(
    `parameterName` = NULL,
    `schema` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param parameterName Name of the parameter
    #' @param schema
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`parameterName` = NULL, `schema` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`parameterName`, `schema`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(parameterName, schema)
      }
      self$`parameterName` <- `parameterName`
      self$`schema` <- `schema`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(parameterName = self$`parameterName`, schema = self$`schema`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`parameterName`)) {
          sprintf(
            '"parameterName":
            "%s"
                  ',
            self$`parameterName`
          )
        },
        if (!is.null(self$`schema`)) {
          sprintf(
            '"schema":
          %s
        ',
            jsonlite::toJSON(self$`schema`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseValueTemplateParameterWithSchemaJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseValueTemplateParameterWithSchemaJson, validateParams = FALSE) {
      UseCaseValueTemplateParameterWithSchemaObject <- jsonlite::fromJSON(UseCaseValueTemplateParameterWithSchemaJson)
      self$`parameterName` <- UseCaseValueTemplateParameterWithSchemaObject$`parameterName`
      self$`schema` <- UseCaseValueTemplateSchema$new()$fromJSON(jsonlite::toJSON(UseCaseValueTemplateParameterWithSchemaObject$schema, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
