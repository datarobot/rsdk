# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ProjectCreate
#'
#' @description ProjectCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialData  \link{OneOfBasicCredentialsS3CredentialsOAuthCredentials} [optional] The credentials to authenticate with the database, to be used instead of credential ID. Can only be used along with datasetId or dataSourceId.
#'
#' @field credentialId  character [optional] The ID of the set of credentials to authenticate with the database. Can only be used along with datasetId or dataSourceId.
#'
#' @field dataSourceId  character [optional] Identifier for the data source to retrieve.
#'
#' @field datasetId  character [optional] The ID of the dataset entry to use for the project dataset.
#'
#' @field datasetVersionId  character [optional] Only used when also providing a datasetId, and specifies the the ID of the dataset version to use for the project dataset. If not specified, the latest version associated with the dataset ID is used.
#'
#' @field password  character [optional] The password (in cleartext) for database authentication. The password will be encrypted on the server side as part of the HTTP request and never saved or stored. Can only be used along with datasetId or dataSourceId. DEPRECATED: please use credentialId or credentialData instead.
#'
#' @field projectName  character [optional] The name of the project to be created. If not specified, &#39;Untitled Project&#39; will be used for database connections and file name will be used as the project name.
#'
#' @field url  character [optional] The URL to download the dataset used to create the project.
#'
#' @field useKerberos  character [optional] If true, use Kerberos authentication for database authentication. Default is false. Can only be used along with datasetId or dataSourceId.
#'
#' @field user  character [optional] The username for database authentication. Can only be used along with datasetId or dataSourceId. DEPRECATED: please use credentialId or credentialData instead.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ProjectCreate <- R6::R6Class(
  "ProjectCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`credentialData` = NULL, `credentialId` = NULL, `dataSourceId` = NULL, `datasetId` = NULL, `datasetVersionId` = NULL, `password` = NULL, `projectName` = NULL, `url` = NULL, `useKerberos` = NULL, `user` = NULL) {
      if (!is.null(`credentialData`)) {
        .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`dataSourceId`)) {
        stopifnot(is.character(`dataSourceId`), length(`dataSourceId`) == 1)
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`datasetVersionId`)) {
        stopifnot(is.character(`datasetVersionId`), length(`datasetVersionId`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`projectName`)) {
        stopifnot(is.character(`projectName`), length(`projectName`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
      if (!is.null(`useKerberos`)) {
        stopifnot(is.logical(`useKerberos`), length(`useKerberos`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `credentialData` = NULL,
    `credentialId` = NULL,
    `dataSourceId` = NULL,
    `datasetId` = NULL,
    `datasetVersionId` = NULL,
    `password` = NULL,
    `projectName` = NULL,
    `url` = NULL,
    `useKerberos` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialData The credentials to authenticate with the database, to be used instead of credential ID. Can only be used along with datasetId or dataSourceId.
    #' @param credentialId The ID of the set of credentials to authenticate with the database. Can only be used along with datasetId or dataSourceId.
    #' @param dataSourceId Identifier for the data source to retrieve.
    #' @param datasetId The ID of the dataset entry to use for the project dataset.
    #' @param datasetVersionId Only used when also providing a datasetId, and specifies the the ID of the dataset version to use for the project dataset. If not specified, the latest version associated with the dataset ID is used.
    #' @param password The password (in cleartext) for database authentication. The password will be encrypted on the server side as part of the HTTP request and never saved or stored. Can only be used along with datasetId or dataSourceId. DEPRECATED: please use credentialId or credentialData instead.
    #' @param projectName The name of the project to be created. If not specified, &#39;Untitled Project&#39; will be used for database connections and file name will be used as the project name.
    #' @param url The URL to download the dataset used to create the project.
    #' @param useKerberos If true, use Kerberos authentication for database authentication. Default is false. Can only be used along with datasetId or dataSourceId.
    #' @param user The username for database authentication. Can only be used along with datasetId or dataSourceId. DEPRECATED: please use credentialId or credentialData instead.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialData` = NULL, `credentialId` = NULL, `dataSourceId` = NULL, `datasetId` = NULL, `datasetVersionId` = NULL, `password` = NULL, `projectName` = NULL, `url` = NULL, `useKerberos` = NULL, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(credentialData, credentialId, dataSourceId, datasetId, datasetVersionId, password, projectName, url, useKerberos, user)
      }
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      self$`credentialId` <- `credentialId`
      self$`dataSourceId` <- `dataSourceId`
      self$`datasetId` <- `datasetId`
      self$`datasetVersionId` <- `datasetVersionId`
      self$`password` <- `password`
      self$`projectName` <- `projectName`
      self$`url` <- `url`
      self$`useKerberos` <- `useKerberos`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(credentialData = self$`credentialData`, credentialId = self$`credentialId`, dataSourceId = self$`dataSourceId`, datasetId = self$`datasetId`, datasetVersionId = self$`datasetVersionId`, password = self$`password`, projectName = self$`projectName`, url = self$`url`, useKerberos = self$`useKerberos`, user = self$`user`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialData`)) {
          sprintf(
            '"credentialData":
            %s
      ',
            jsonlite::toJSON(self$`credentialData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`dataSourceId`)) {
          sprintf(
            '"dataSourceId":
            "%s"
                  ',
            self$`dataSourceId`
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetVersionId`)) {
          sprintf(
            '"datasetVersionId":
            "%s"
                  ',
            self$`datasetVersionId`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`projectName`)) {
          sprintf(
            '"projectName":
            "%s"
                  ',
            self$`projectName`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        },
        if (!is.null(self$`useKerberos`)) {
          sprintf(
            '"useKerberos":
            %s
                  ',
            tolower(self$`useKerberos`)
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ProjectCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ProjectCreateJson, validateParams = FALSE) {
      ProjectCreateObject <- jsonlite::fromJSON(ProjectCreateJson)
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = ProjectCreateObject$credentialData)
      self$`credentialId` <- ProjectCreateObject$`credentialId`
      self$`dataSourceId` <- ProjectCreateObject$`dataSourceId`
      self$`datasetId` <- ProjectCreateObject$`datasetId`
      self$`datasetVersionId` <- ProjectCreateObject$`datasetVersionId`
      self$`password` <- ProjectCreateObject$`password`
      self$`projectName` <- ProjectCreateObject$`projectName`
      self$`url` <- ProjectCreateObject$`url`
      self$`useKerberos` <- ProjectCreateObject$`useKerberos`
      self$`user` <- ProjectCreateObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
