# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ProjectSecondaryDatasetConfigResponse
#'
#' @description ProjectSecondaryDatasetConfigResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field config  list( \link{SecondaryDatasetConfig} ) [optional] Graph-specific secondary datasets. Deprecated in version v2.23.
#'
#' @field created  character DR-formatted datetime, null for legacy (before DR 6.0) db records.
#'
#' @field creatorFullName  character Fullname or email of the user created this config. null for legacy (before DR 6.0) db records.
#'
#' @field creatorUserId  character ID of the user created this config, null for legacy (before DR 6.0) db records.
#'
#' @field credentialIds  list( \link{DatasetsCredential} ) [optional] List of credentials used by the secondary datasets if the datasets used in the configuration are from datasource.
#'
#' @field featurelistId  character [optional] Id of the feature list.
#'
#' @field id  character ID of the secondary datasets configuration.
#'
#' @field isDefault  character Secondary datasets config is default config or not.
#'
#' @field name  character Name of the secondary datasets config.
#'
#' @field projectId  character ID of the project.
#'
#' @field projectVersion  character [optional] DataRobot project version.
#'
#' @field secondaryDatasets  list( \link{SecondaryDatasetResponse} ) [optional] List of secondary datasets used in the config.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ProjectSecondaryDatasetConfigResponse <- R6::R6Class(
  "ProjectSecondaryDatasetConfigResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`config` = NULL, `created` = NULL, `creatorFullName` = NULL, `creatorUserId` = NULL, `credentialIds` = NULL, `featurelistId` = NULL, `id` = NULL, `isDefault` = NULL, `name` = NULL, `projectId` = NULL, `projectVersion` = NULL, `secondaryDatasets` = NULL) {
      if (!is.null(`created`)) {
        stopifnot(inherits(`created`, "POSIXt"))
      }
      if (!is.null(`creatorFullName`)) {
        stopifnot(is.character(`creatorFullName`), length(`creatorFullName`) == 1)
      }
      if (!is.null(`creatorUserId`)) {
        stopifnot(is.character(`creatorUserId`), length(`creatorUserId`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`isDefault`)) {
        stopifnot(is.logical(`isDefault`), length(`isDefault`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`config`) && length(`config`) > 0) {
        stopifnot(is.vector(`config`), sapply(`config`, R6::is.R6))
      }
      if (!is.null(`credentialIds`) && length(`credentialIds`) > 0) {
        stopifnot(is.vector(`credentialIds`), sapply(`credentialIds`, R6::is.R6))
      }
      if (!is.null(`featurelistId`)) {
        stopifnot(is.character(`featurelistId`), length(`featurelistId`) == 1)
      }
      if (!is.null(`projectVersion`)) {
        stopifnot(is.character(`projectVersion`), length(`projectVersion`) == 1)
      }
      if (!is.null(`secondaryDatasets`) && length(`secondaryDatasets`) > 0) {
        stopifnot(is.vector(`secondaryDatasets`), sapply(`secondaryDatasets`, R6::is.R6))
      }
    }
  ),
  public = list(
    `config` = NULL,
    `created` = NULL,
    `creatorFullName` = NULL,
    `creatorUserId` = NULL,
    `credentialIds` = NULL,
    `featurelistId` = NULL,
    `id` = NULL,
    `isDefault` = NULL,
    `name` = NULL,
    `projectId` = NULL,
    `projectVersion` = NULL,
    `secondaryDatasets` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param config Graph-specific secondary datasets. Deprecated in version v2.23.
    #' @param created DR-formatted datetime, null for legacy (before DR 6.0) db records.
    #' @param creatorFullName Fullname or email of the user created this config. null for legacy (before DR 6.0) db records.
    #' @param creatorUserId ID of the user created this config, null for legacy (before DR 6.0) db records.
    #' @param credentialIds List of credentials used by the secondary datasets if the datasets used in the configuration are from datasource.
    #' @param featurelistId Id of the feature list.
    #' @param id ID of the secondary datasets configuration.
    #' @param isDefault Secondary datasets config is default config or not.
    #' @param name Name of the secondary datasets config.
    #' @param projectId ID of the project.
    #' @param projectVersion DataRobot project version.
    #' @param secondaryDatasets List of secondary datasets used in the config.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`created` = NULL, `creatorFullName` = NULL, `creatorUserId` = NULL, `id` = NULL, `isDefault` = NULL, `name` = NULL, `projectId` = NULL, `config` = NULL, `credentialIds` = NULL, `featurelistId` = NULL, `projectVersion` = NULL, `secondaryDatasets` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`created`, `creatorFullName`, `creatorUserId`, `id`, `isDefault`, `name`, `projectId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(config, created, creatorFullName, creatorUserId, credentialIds, featurelistId, id, isDefault, name, projectId, projectVersion, secondaryDatasets)
      }
      self$`config` <- `config`
      self$`created` <- `created`
      self$`creatorFullName` <- `creatorFullName`
      self$`creatorUserId` <- `creatorUserId`
      self$`credentialIds` <- `credentialIds`
      self$`featurelistId` <- `featurelistId`
      self$`id` <- `id`
      self$`isDefault` <- `isDefault`
      self$`name` <- `name`
      self$`projectId` <- `projectId`
      self$`projectVersion` <- `projectVersion`
      self$`secondaryDatasets` <- `secondaryDatasets`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(config = self$`config`, created = self$`created`, creatorFullName = self$`creatorFullName`, creatorUserId = self$`creatorUserId`, credentialIds = self$`credentialIds`, featurelistId = self$`featurelistId`, id = self$`id`, isDefault = self$`isDefault`, name = self$`name`, projectId = self$`projectId`, projectVersion = self$`projectVersion`, secondaryDatasets = self$`secondaryDatasets`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`config`)) {
          sprintf(
            '"config":
            [%s]
      ',
            paste(sapply(self$`config`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`created`)) {
          sprintf(
            '"created":
            "%s"
                  ',
            format(self$`created`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`creatorFullName`)) {
          sprintf(
            '"creatorFullName":
            "%s"
                  ',
            self$`creatorFullName`
          )
        },
        if (!is.null(self$`creatorUserId`)) {
          sprintf(
            '"creatorUserId":
            "%s"
                  ',
            self$`creatorUserId`
          )
        },
        if (!is.null(self$`credentialIds`)) {
          sprintf(
            '"credentialIds":
            [%s]
      ',
            paste(sapply(self$`credentialIds`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`featurelistId`)) {
          sprintf(
            '"featurelistId":
            "%s"
                  ',
            self$`featurelistId`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`isDefault`)) {
          sprintf(
            '"isDefault":
            %s
                  ',
            tolower(self$`isDefault`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`projectVersion`)) {
          sprintf(
            '"projectVersion":
            "%s"
                  ',
            self$`projectVersion`
          )
        },
        if (!is.null(self$`secondaryDatasets`)) {
          sprintf(
            '"secondaryDatasets":
            [%s]
      ',
            paste(sapply(self$`secondaryDatasets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ProjectSecondaryDatasetConfigResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ProjectSecondaryDatasetConfigResponseJson, validateParams = FALSE) {
      ProjectSecondaryDatasetConfigResponseObject <- jsonlite::fromJSON(ProjectSecondaryDatasetConfigResponseJson)
      self$`config` <- ApiClient$new()$deserializeObj(ProjectSecondaryDatasetConfigResponseObject$`config`, "array[SecondaryDatasetConfig]", loadNamespace("datarobot.apicore"))
      self$`created` <- ParseRFC3339Timestamp(ProjectSecondaryDatasetConfigResponseObject$`created`)
      self$`creatorFullName` <- ProjectSecondaryDatasetConfigResponseObject$`creatorFullName`
      self$`creatorUserId` <- ProjectSecondaryDatasetConfigResponseObject$`creatorUserId`
      self$`credentialIds` <- ApiClient$new()$deserializeObj(ProjectSecondaryDatasetConfigResponseObject$`credentialIds`, "array[DatasetsCredential]", loadNamespace("datarobot.apicore"))
      self$`featurelistId` <- ProjectSecondaryDatasetConfigResponseObject$`featurelistId`
      self$`id` <- ProjectSecondaryDatasetConfigResponseObject$`id`
      self$`isDefault` <- ProjectSecondaryDatasetConfigResponseObject$`isDefault`
      self$`name` <- ProjectSecondaryDatasetConfigResponseObject$`name`
      self$`projectId` <- ProjectSecondaryDatasetConfigResponseObject$`projectId`
      self$`projectVersion` <- ProjectSecondaryDatasetConfigResponseObject$`projectVersion`
      self$`secondaryDatasets` <- ApiClient$new()$deserializeObj(ProjectSecondaryDatasetConfigResponseObject$`secondaryDatasets`, "array[SecondaryDatasetResponse]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
