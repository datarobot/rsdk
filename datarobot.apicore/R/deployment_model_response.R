# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentModelResponse
#'
#' @description DeploymentModelResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field buildEnvironmentType  character [optional] Build environment type of the current model.
#'
#' @field customModelImage  \link{DeploymentModelCustomModelImageResponse} [optional]
#'
#' @field decisionFlowId  character [optional] The ID of the decision flow if the deployment has one.
#'
#' @field decisionFlowVersionId  character [optional] The ID of the decision flow version if the deployment has one.
#'
#' @field deployedAt  character [optional] Timestamp of when current model was deployed.
#'
#' @field hasDecisionFlow  character [optional] True if this deployment contains a Decision Flow
#'
#' @field id  character ID of the current model.
#'
#' @field isDeprecated  character Whether the current model is deprecated model. eg. python2 based model.
#'
#' @field projectId  character [optional] Project ID of the current model.
#'
#' @field projectName  character Project name of the current model.
#'
#' @field targetName  character Target name of the current model.
#'
#' @field targetType  character Target type of the current model.
#'
#' @field type  character Type of the current model.
#'
#' @field unstructuredModelKind  character Whether the current model is an unstructured model.
#'
#' @field unsupervisedMode  character Whether the current model&#39;s project is unsupervised.
#'
#' @field unsupervisedType  character [optional] Only valid when unsupervisedMode is True. The type of unsupervised project, anomaly or clustering. If unsupervisedMode, defaults to &#39;anomaly&#39;.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentModelResponse <- R6::R6Class(
  "DeploymentModelResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`buildEnvironmentType` = NULL, `customModelImage` = NULL, `decisionFlowId` = NULL, `decisionFlowVersionId` = NULL, `deployedAt` = NULL, `hasDecisionFlow` = NULL, `id` = NULL, `isDeprecated` = NULL, `projectId` = NULL, `projectName` = NULL, `targetName` = NULL, `targetType` = NULL, `type` = NULL, `unstructuredModelKind` = NULL, `unsupervisedMode` = NULL, `unsupervisedType` = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`isDeprecated`)) {
        stopifnot(is.logical(`isDeprecated`), length(`isDeprecated`) == 1)
      }
      if (!is.null(`projectName`)) {
        stopifnot(is.character(`projectName`), length(`projectName`) == 1)
      }
      if (!is.null(`targetName`)) {
        stopifnot(is.character(`targetName`), length(`targetName`) == 1)
      }
      if (!is.null(`targetType`)) {
        stopifnot(is.character(`targetType`), length(`targetType`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`unstructuredModelKind`)) {
        stopifnot(is.logical(`unstructuredModelKind`), length(`unstructuredModelKind`) == 1)
      }
      if (!is.null(`unsupervisedMode`)) {
        stopifnot(is.logical(`unsupervisedMode`), length(`unsupervisedMode`) == 1)
      }
      if (!is.null(`buildEnvironmentType`)) {
        stopifnot(is.character(`buildEnvironmentType`), length(`buildEnvironmentType`) == 1)
      }
      if (!is.null(`customModelImage`)) {
        stopifnot(R6::is.R6(`customModelImage`))
      }
      if (!is.null(`decisionFlowId`)) {
        stopifnot(is.character(`decisionFlowId`), length(`decisionFlowId`) == 1)
      }
      if (!is.null(`decisionFlowVersionId`)) {
        stopifnot(is.character(`decisionFlowVersionId`), length(`decisionFlowVersionId`) == 1)
      }
      if (!is.null(`deployedAt`)) {
        stopifnot(inherits(`deployedAt`, "POSIXt"))
      }
      if (!is.null(`hasDecisionFlow`)) {
        stopifnot(is.logical(`hasDecisionFlow`), length(`hasDecisionFlow`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`unsupervisedType`)) {
        stopifnot(is.character(`unsupervisedType`), length(`unsupervisedType`) == 1)
      }
    }
  ),
  public = list(
    `buildEnvironmentType` = NULL,
    `customModelImage` = NULL,
    `decisionFlowId` = NULL,
    `decisionFlowVersionId` = NULL,
    `deployedAt` = NULL,
    `hasDecisionFlow` = NULL,
    `id` = NULL,
    `isDeprecated` = NULL,
    `projectId` = NULL,
    `projectName` = NULL,
    `targetName` = NULL,
    `targetType` = NULL,
    `type` = NULL,
    `unstructuredModelKind` = NULL,
    `unsupervisedMode` = NULL,
    `unsupervisedType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param buildEnvironmentType Build environment type of the current model.
    #' @param customModelImage
    #' @param decisionFlowId The ID of the decision flow if the deployment has one.
    #' @param decisionFlowVersionId The ID of the decision flow version if the deployment has one.
    #' @param deployedAt Timestamp of when current model was deployed.
    #' @param hasDecisionFlow True if this deployment contains a Decision Flow
    #' @param id ID of the current model.
    #' @param isDeprecated Whether the current model is deprecated model. eg. python2 based model.
    #' @param projectId Project ID of the current model.
    #' @param projectName Project name of the current model.
    #' @param targetName Target name of the current model.
    #' @param targetType Target type of the current model.
    #' @param type Type of the current model.
    #' @param unstructuredModelKind Whether the current model is an unstructured model.
    #' @param unsupervisedMode Whether the current model&#39;s project is unsupervised.
    #' @param unsupervisedType Only valid when unsupervisedMode is True. The type of unsupervised project, anomaly or clustering. If unsupervisedMode, defaults to &#39;anomaly&#39;.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `isDeprecated` = NULL, `projectName` = NULL, `targetName` = NULL, `targetType` = NULL, `type` = NULL, `unstructuredModelKind` = NULL, `unsupervisedMode` = NULL, `buildEnvironmentType` = NULL, `customModelImage` = NULL, `decisionFlowId` = NULL, `decisionFlowVersionId` = NULL, `deployedAt` = NULL, `hasDecisionFlow` = NULL, `projectId` = NULL, `unsupervisedType` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`id`, `isDeprecated`, `projectName`, `targetName`, `targetType`, `type`, `unstructuredModelKind`, `unsupervisedMode`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(buildEnvironmentType, customModelImage, decisionFlowId, decisionFlowVersionId, deployedAt, hasDecisionFlow, id, isDeprecated, projectId, projectName, targetName, targetType, type, unstructuredModelKind, unsupervisedMode, unsupervisedType)
      }
      self$`buildEnvironmentType` <- `buildEnvironmentType`
      self$`customModelImage` <- `customModelImage`
      self$`decisionFlowId` <- `decisionFlowId`
      self$`decisionFlowVersionId` <- `decisionFlowVersionId`
      self$`deployedAt` <- `deployedAt`
      self$`hasDecisionFlow` <- `hasDecisionFlow`
      self$`id` <- `id`
      self$`isDeprecated` <- `isDeprecated`
      self$`projectId` <- `projectId`
      self$`projectName` <- `projectName`
      self$`targetName` <- `targetName`
      self$`targetType` <- `targetType`
      self$`type` <- `type`
      self$`unstructuredModelKind` <- `unstructuredModelKind`
      self$`unsupervisedMode` <- `unsupervisedMode`
      self$`unsupervisedType` <- `unsupervisedType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(buildEnvironmentType = self$`buildEnvironmentType`, customModelImage = self$`customModelImage`, decisionFlowId = self$`decisionFlowId`, decisionFlowVersionId = self$`decisionFlowVersionId`, deployedAt = self$`deployedAt`, hasDecisionFlow = self$`hasDecisionFlow`, id = self$`id`, isDeprecated = self$`isDeprecated`, projectId = self$`projectId`, projectName = self$`projectName`, targetName = self$`targetName`, targetType = self$`targetType`, type = self$`type`, unstructuredModelKind = self$`unstructuredModelKind`, unsupervisedMode = self$`unsupervisedMode`, unsupervisedType = self$`unsupervisedType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`buildEnvironmentType`)) {
          sprintf(
            '"buildEnvironmentType":
            "%s"
                  ',
            self$`buildEnvironmentType`
          )
        },
        if (!is.null(self$`customModelImage`)) {
          sprintf(
            '"customModelImage":
            %s
      ',
            jsonlite::toJSON(self$`customModelImage`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`decisionFlowId`)) {
          sprintf(
            '"decisionFlowId":
            "%s"
                  ',
            self$`decisionFlowId`
          )
        },
        if (!is.null(self$`decisionFlowVersionId`)) {
          sprintf(
            '"decisionFlowVersionId":
            "%s"
                  ',
            self$`decisionFlowVersionId`
          )
        },
        if (!is.null(self$`deployedAt`)) {
          sprintf(
            '"deployedAt":
            "%s"
                  ',
            format(self$`deployedAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`hasDecisionFlow`)) {
          sprintf(
            '"hasDecisionFlow":
            %s
                  ',
            tolower(self$`hasDecisionFlow`)
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`isDeprecated`)) {
          sprintf(
            '"isDeprecated":
            %s
                  ',
            tolower(self$`isDeprecated`)
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`projectName`)) {
          sprintf(
            '"projectName":
            "%s"
                  ',
            self$`projectName`
          )
        },
        if (!is.null(self$`targetName`)) {
          sprintf(
            '"targetName":
            "%s"
                  ',
            self$`targetName`
          )
        },
        if (!is.null(self$`targetType`)) {
          sprintf(
            '"targetType":
            "%s"
                  ',
            self$`targetType`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        },
        if (!is.null(self$`unstructuredModelKind`)) {
          sprintf(
            '"unstructuredModelKind":
            %s
                  ',
            tolower(self$`unstructuredModelKind`)
          )
        },
        if (!is.null(self$`unsupervisedMode`)) {
          sprintf(
            '"unsupervisedMode":
            %s
                  ',
            tolower(self$`unsupervisedMode`)
          )
        },
        if (!is.null(self$`unsupervisedType`)) {
          sprintf(
            '"unsupervisedType":
            "%s"
                  ',
            self$`unsupervisedType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentModelResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentModelResponseJson, validateParams = FALSE) {
      DeploymentModelResponseObject <- jsonlite::fromJSON(DeploymentModelResponseJson)
      self$`buildEnvironmentType` <- DeploymentModelResponseObject$`buildEnvironmentType`
      self$`customModelImage` <- DeploymentModelCustomModelImageResponse$new()$fromJSON(jsonlite::toJSON(DeploymentModelResponseObject$customModelImage, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`decisionFlowId` <- DeploymentModelResponseObject$`decisionFlowId`
      self$`decisionFlowVersionId` <- DeploymentModelResponseObject$`decisionFlowVersionId`
      self$`deployedAt` <- ParseRFC3339Timestamp(DeploymentModelResponseObject$`deployedAt`)
      self$`hasDecisionFlow` <- DeploymentModelResponseObject$`hasDecisionFlow`
      self$`id` <- DeploymentModelResponseObject$`id`
      self$`isDeprecated` <- DeploymentModelResponseObject$`isDeprecated`
      self$`projectId` <- DeploymentModelResponseObject$`projectId`
      self$`projectName` <- DeploymentModelResponseObject$`projectName`
      self$`targetName` <- DeploymentModelResponseObject$`targetName`
      self$`targetType` <- DeploymentModelResponseObject$`targetType`
      self$`type` <- DeploymentModelResponseObject$`type`
      self$`unstructuredModelKind` <- DeploymentModelResponseObject$`unstructuredModelKind`
      self$`unsupervisedMode` <- DeploymentModelResponseObject$`unsupervisedMode`
      self$`unsupervisedType` <- DeploymentModelResponseObject$`unsupervisedType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
