# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MulticategoricalHistogramItem
#'
#' @description MulticategoricalHistogramItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field label  character Label name.
#'
#' @field plot  list( \link{LabelRelevancePlot} ) Relevance histogram for label.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MulticategoricalHistogramItem <- R6::R6Class(
  "MulticategoricalHistogramItem",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`label` = NULL, `plot` = NULL) {
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`plot`)) {
        stopifnot(is.vector(`plot`), sapply(`plot`, R6::is.R6))
      }
    }
  ),
  public = list(
    `label` = NULL,
    `plot` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param label Label name.
    #' @param plot Relevance histogram for label.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`label` = NULL, `plot` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`label`, `plot`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(label, plot)
      }
      self$`label` <- `label`
      self$`plot` <- `plot`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(label = self$`label`, plot = self$`plot`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`plot`)) {
          sprintf(
            '"plot":
            [%s]
      ',
            paste(sapply(self$`plot`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MulticategoricalHistogramItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MulticategoricalHistogramItemJson, validateParams = FALSE) {
      MulticategoricalHistogramItemObject <- jsonlite::fromJSON(MulticategoricalHistogramItemJson)
      self$`label` <- MulticategoricalHistogramItemObject$`label`
      self$`plot` <- ApiClient$new()$deserializeObj(MulticategoricalHistogramItemObject$`plot`, "array[LabelRelevancePlot]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
