# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetRefreshJobCreate
#'
#' @description DatasetRefreshJobCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field categories  \link{OneOfstringarray} [optional] An array of strings describing the intended use of the dataset. The supported options are &#x60;&#x60;TRAINING&#x60;&#x60;, and &#x60;&#x60;PREDICTION&#x60;&#x60;.
#'
#' @field credentialId  character [optional] The ID of the set of credentials to use to run the scheduled job when the Kerberos authentication service is utilized. Required when useKerberos is true.
#'
#' @field credentials  character [optional] A JSON string describing the data engine queries credentials to use when refreshing.
#'
#' @field enabled  character [optional] Boolean for whether the scheduled job is active (true) or inactive (false).
#'
#' @field name  character Scheduled job name.
#'
#' @field schedule  \link{DatasetRefresh}
#'
#' @field scheduleReferenceDate  character [optional] The UTC reference date in RFC-3339 format of when the schedule starts from. This value is returned in &#x60;/api/v2/datasets/(datasetId)/refreshJobs/(jobId)/&#x60; to help build a more intuitive schedule picker. The default is the current time.
#'
#' @field useKerberos  character [optional] If true, the Kerberos authentication system is used in conjunction with a credential ID.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetRefreshJobCreate <- R6::R6Class(
  "DatasetRefreshJobCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(categories = NULL, credentialId = NULL, credentials = NULL, enabled = NULL, name = NULL, schedule = NULL, scheduleReferenceDate = NULL, useKerberos = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`schedule`)) {
        stopifnot(R6::is.R6(`schedule`))
      }
      if (!is.null(`categories`)) {
        .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`credentials`)) {
        stopifnot(is.character(`credentials`), length(`credentials`) == 1)
      }
      if (!is.null(`enabled`)) {
        stopifnot(is.logical(`enabled`), length(`enabled`) == 1)
      }
      if (!is.null(`scheduleReferenceDate`)) {
        stopifnot(inherits(`scheduleReferenceDate`, "POSIXt"))
      }
      if (!is.null(`useKerberos`)) {
        stopifnot(is.logical(`useKerberos`), length(`useKerberos`) == 1)
      }
    }
  ),
  public = list(
    `categories` = NULL,
    `credentialId` = NULL,
    `credentials` = NULL,
    `enabled` = NULL,
    `name` = NULL,
    `schedule` = NULL,
    `scheduleReferenceDate` = NULL,
    `useKerberos` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param categories An array of strings describing the intended use of the dataset. The supported options are &#x60;&#x60;TRAINING&#x60;&#x60;, and &#x60;&#x60;PREDICTION&#x60;&#x60;.
    #' @param credentialId The ID of the set of credentials to use to run the scheduled job when the Kerberos authentication service is utilized. Required when useKerberos is true.
    #' @param credentials A JSON string describing the data engine queries credentials to use when refreshing.
    #' @param enabled Boolean for whether the scheduled job is active (true) or inactive (false).
    #' @param name Scheduled job name.
    #' @param schedule
    #' @param scheduleReferenceDate The UTC reference date in RFC-3339 format of when the schedule starts from. This value is returned in &#x60;/api/v2/datasets/(datasetId)/refreshJobs/(jobId)/&#x60; to help build a more intuitive schedule picker. The default is the current time.
    #' @param useKerberos If true, the Kerberos authentication system is used in conjunction with a credential ID.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `schedule` = NULL, `categories` = NULL, `credentialId` = "null", `credentials` = NULL, `enabled` = TRUE, `scheduleReferenceDate` = NULL, `useKerberos` = FALSE, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`name`, `schedule`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(categories, credentialId, credentials, enabled, name, schedule, scheduleReferenceDate, useKerberos)
      }
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      self$`credentialId` <- `credentialId`
      self$`credentials` <- `credentials`
      self$`enabled` <- `enabled`
      self$`name` <- `name`
      self$`schedule` <- `schedule`
      self$`scheduleReferenceDate` <- `scheduleReferenceDate`
      self$`useKerberos` <- `useKerberos`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(categories = self$`categories`, credentialId = self$`credentialId`, credentials = self$`credentials`, enabled = self$`enabled`, name = self$`name`, schedule = self$`schedule`, scheduleReferenceDate = self$`scheduleReferenceDate`, useKerberos = self$`useKerberos`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`categories`)) {
          sprintf(
            '"categories":
          %s
        ',
            self$`categories`
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`credentials`)) {
          sprintf(
            '"credentials":
            "%s"
                  ',
            self$`credentials`
          )
        },
        if (!is.null(self$`enabled`)) {
          sprintf(
            '"enabled":
            %s
                  ',
            tolower(self$`enabled`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`schedule`)) {
          sprintf(
            '"schedule":
          %s
        ',
            jsonlite::toJSON(self$`schedule`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`scheduleReferenceDate`)) {
          sprintf(
            '"scheduleReferenceDate":
            "%s"
                  ',
            format(self$`scheduleReferenceDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`useKerberos`)) {
          sprintf(
            '"useKerberos":
            %s
                  ',
            tolower(self$`useKerberos`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetRefreshJobCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetRefreshJobCreateJson, validateParams = FALSE) {
      DatasetRefreshJobCreateObject <- jsonlite::fromJSON(DatasetRefreshJobCreateJson)
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = DatasetRefreshJobCreateObject$categories)
      self$`credentialId` <- DatasetRefreshJobCreateObject$`credentialId`
      self$`credentials` <- DatasetRefreshJobCreateObject$`credentials`
      self$`enabled` <- DatasetRefreshJobCreateObject$`enabled`
      self$`name` <- DatasetRefreshJobCreateObject$`name`
      self$`schedule` <- DatasetRefresh$new()$fromJSON(jsonlite::toJSON(DatasetRefreshJobCreateObject$schedule, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`scheduleReferenceDate` <- ParseRFC3339Timestamp(DatasetRefreshJobCreateObject$`scheduleReferenceDate`)
      self$`useKerberos` <- DatasetRefreshJobCreateObject$`useKerberos`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
