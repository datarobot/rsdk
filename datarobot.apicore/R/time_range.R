# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TimeRange
#'
#' @description TimeRange Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field end  character [optional] RFC3339 datetime. End of time period to retrieve the features drift for. If not set, defaults to the next top of the hour from now.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
#'
#' @field start  character [optional] RFC3339 datetime. Start of time period to retrieve the features drift for. If not set, defaults to 1 week from the end of the period.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TimeRange <- R6::R6Class(
  "TimeRange",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(end = NULL, start = NULL) {
      if (!is.null(`end`)) {
        stopifnot(inherits(`end`, "POSIXt"))
      }
      if (!is.null(`start`)) {
        stopifnot(inherits(`start`, "POSIXt"))
      }
    }
  ),
  public = list(
    `end` = NULL,
    `start` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param end RFC3339 datetime. End of time period to retrieve the features drift for. If not set, defaults to the next top of the hour from now.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
    #' @param start RFC3339 datetime. Start of time period to retrieve the features drift for. If not set, defaults to 1 week from the end of the period.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`end` = NULL, `start` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(end, start)
      }
      self$`end` <- `end`
      self$`start` <- `start`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(end = self$`end`, start = self$`start`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`end`)) {
          sprintf(
            '"end":
            "%s"
                  ',
            format(self$`end`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`start`)) {
          sprintf(
            '"start":
            "%s"
                  ',
            format(self$`start`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TimeRangeJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TimeRangeJson, validateParams = FALSE) {
      TimeRangeObject <- jsonlite::fromJSON(TimeRangeJson)
      self$`end` <- ParseRFC3339Timestamp(TimeRangeObject$`end`)
      self$`start` <- ParseRFC3339Timestamp(TimeRangeObject$`start`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
