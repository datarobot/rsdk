# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionIntegrationJobCreatePayload
#'
#' @description PredictionIntegrationJobCreatePayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field deploymentId  character Id of the deployment used to run predictions
#'
#' @field integrationTypeId  character Type Id for this integration
#'
#' @field integrationTypeName  character Name of the integration
#'
#' @field name  character Scheduled Job name
#'
#' @field predictionOptions  \link{PredictionOptionsPayload} 
#'
#' @field schedule  \link{ScheduledJobPayload} 
#'
#' @field source  \link{OneOfUsernamePasswordCatalogDataSourcePayloadCredentialsCatalogDataSourcePayload} Object describing data source for prediction
#'
#' @field target  \link{OneOfCredentialsJdbcPredictionTargetPayloadUsernamePasswordJdbcPredictionTargetPayloadTableauServerPredictionTargetPayload} Settings for the predictions output destination
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionIntegrationJobCreatePayload <- R6::R6Class(
  "PredictionIntegrationJobCreatePayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(deploymentId = NULL, integrationTypeId = NULL, integrationTypeName = NULL, name = NULL, predictionOptions = NULL, schedule = NULL, source = NULL, target = NULL) {
      if (!is.null(`deploymentId`)) {
        stopifnot(is.character(`deploymentId`), length(`deploymentId`) == 1)
      }
      if (!is.null(`integrationTypeId`)) {
        stopifnot(is.character(`integrationTypeId`), length(`integrationTypeId`) == 1)
      }
      if (!is.null(`integrationTypeName`)) {
        stopifnot(is.character(`integrationTypeName`), length(`integrationTypeName`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`predictionOptions`)) {
        stopifnot(R6::is.R6(`predictionOptions`))
      }
      if (!is.null(`schedule`)) {
        stopifnot(R6::is.R6(`schedule`))
      }
      if (!is.null(`source`)) {
        .setComplexProperty(typeList = list(UsernamePasswordCatalogDataSourcePayload, CredentialsCatalogDataSourcePayload), propertyData = source)
      }
      if (!is.null(`target`)) {
        .setComplexProperty(typeList = list(CredentialsJdbcPredictionTargetPayload, UsernamePasswordJdbcPredictionTargetPayload, TableauServerPredictionTargetPayload), propertyData = target)
      }
    }
  ),
  public = list(
    `deploymentId` = NULL,
    `integrationTypeId` = NULL,
    `integrationTypeName` = NULL,
    `name` = NULL,
    `predictionOptions` = NULL,
    `schedule` = NULL,
    `source` = NULL,
    `target` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param deploymentId Id of the deployment used to run predictions
    #' @param integrationTypeId Type Id for this integration
    #' @param integrationTypeName Name of the integration
    #' @param name Scheduled Job name
    #' @param predictionOptions 
    #' @param schedule 
    #' @param source Object describing data source for prediction
    #' @param target Settings for the predictions output destination
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `deploymentId` = NULL, `integrationTypeId` = NULL, `integrationTypeName` = NULL, `name` = NULL, `predictionOptions` = NULL, `schedule` = NULL, `source` = NULL, `target` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`deploymentId`, `integrationTypeId`, `integrationTypeName`, `name`, `predictionOptions`, `schedule`, `source`, `target`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(deploymentId, integrationTypeId, integrationTypeName, name, predictionOptions, schedule, source, target)
      }
      self$`deploymentId` <- `deploymentId`
      self$`integrationTypeId` <- `integrationTypeId`
      self$`integrationTypeName` <- `integrationTypeName`
      self$`name` <- `name`
      self$`predictionOptions` <- `predictionOptions`
      self$`schedule` <- `schedule`
        self$`source` <- .setComplexProperty(typeList = list(UsernamePasswordCatalogDataSourcePayload, CredentialsCatalogDataSourcePayload), propertyData = source)
        self$`target` <- .setComplexProperty(typeList = list(CredentialsJdbcPredictionTargetPayload, UsernamePasswordJdbcPredictionTargetPayload, TableauServerPredictionTargetPayload), propertyData = target)
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(deploymentId = self$`deploymentId`, integrationTypeId = self$`integrationTypeId`, integrationTypeName = self$`integrationTypeName`, name = self$`name`, predictionOptions = self$`predictionOptions`, schedule = self$`schedule`, source = self$`source`, target = self$`target`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`deploymentId`)) {
        sprintf(
        '"deploymentId":
          "%s"
                ',
        self$`deploymentId`
        )},
        if (!is.null(self$`integrationTypeId`)) {
        sprintf(
        '"integrationTypeId":
          "%s"
                ',
        self$`integrationTypeId`
        )},
        if (!is.null(self$`integrationTypeName`)) {
        sprintf(
        '"integrationTypeName":
          "%s"
                ',
        self$`integrationTypeName`
        )},
        if (!is.null(self$`name`)) {
        sprintf(
        '"name":
          "%s"
                ',
        self$`name`
        )},
        if (!is.null(self$`predictionOptions`)) {
        sprintf(
        '"predictionOptions":
        %s
        ',
        jsonlite::toJSON(self$`predictionOptions`$toJSON(), auto_unbox = TRUE, digits = NA)
        )},
        if (!is.null(self$`schedule`)) {
        sprintf(
        '"schedule":
        %s
        ',
        jsonlite::toJSON(self$`schedule`$toJSON(), auto_unbox = TRUE, digits = NA)
        )},
        if (!is.null(self$`source`)) {
        sprintf(
        '"source":
        %s
        ',
        jsonlite::toJSON(self$`source`$toJSON(), auto_unbox = TRUE, digits = NA)
        )},
        if (!is.null(self$`target`)) {
        sprintf(
        '"target":
        %s
        ',
        jsonlite::toJSON(self$`target`$toJSON(), auto_unbox = TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionIntegrationJobCreatePayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionIntegrationJobCreatePayloadJson, validateParams = FALSE) {
      PredictionIntegrationJobCreatePayloadObject <- jsonlite::fromJSON(PredictionIntegrationJobCreatePayloadJson)
      self$`deploymentId` <- PredictionIntegrationJobCreatePayloadObject$`deploymentId`
      self$`integrationTypeId` <- PredictionIntegrationJobCreatePayloadObject$`integrationTypeId`
      self$`integrationTypeName` <- PredictionIntegrationJobCreatePayloadObject$`integrationTypeName`
      self$`name` <- PredictionIntegrationJobCreatePayloadObject$`name`
      self$`predictionOptions` <- PredictionOptionsPayload$new()$fromJSON(jsonlite::toJSON(PredictionIntegrationJobCreatePayloadObject$predictionOptions, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`schedule` <- ScheduledJobPayload$new()$fromJSON(jsonlite::toJSON(PredictionIntegrationJobCreatePayloadObject$schedule, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`source` <- .setComplexProperty(typeList = list(UsernamePasswordCatalogDataSourcePayload, CredentialsCatalogDataSourcePayload), propertyData = PredictionIntegrationJobCreatePayloadObject$source)
      self$`target` <- .setComplexProperty(typeList = list(CredentialsJdbcPredictionTargetPayload, UsernamePasswordJdbcPredictionTargetPayload, TableauServerPredictionTargetPayload), propertyData = PredictionIntegrationJobCreatePayloadObject$target)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
