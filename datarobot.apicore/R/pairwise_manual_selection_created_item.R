# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PairwiseManualSelectionCreatedItem
#'
#' @description PairwiseManualSelectionCreatedItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field columnLabels  list( character ) Manually selected column labels.
#'
#' @field id  character Id of the manually selected labels set
#'
#' @field name  character Name for the set of manually selected labels.
#'
#' @field rowLabels  list( character ) Manually selected row labels.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PairwiseManualSelectionCreatedItem <- R6::R6Class(
  "PairwiseManualSelectionCreatedItem",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`columnLabels` = NULL, `id` = NULL, `name` = NULL, `rowLabels` = NULL) {
      if (!is.null(`columnLabels`)) {
        stopifnot(is.vector(`columnLabels`))
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`rowLabels`)) {
        stopifnot(is.vector(`rowLabels`))
      }
    }
  ),
  public = list(
    `columnLabels` = NULL,
    `id` = NULL,
    `name` = NULL,
    `rowLabels` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param columnLabels Manually selected column labels.
    #' @param id Id of the manually selected labels set
    #' @param name Name for the set of manually selected labels.
    #' @param rowLabels Manually selected row labels.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`columnLabels` = NULL, `id` = NULL, `name` = NULL, `rowLabels` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`columnLabels`, `id`, `name`, `rowLabels`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(columnLabels, id, name, rowLabels)
      }
      sapply(`columnLabels`, function(x) stopifnot(is.character(x)))
      self$`id` <- `id`
      self$`name` <- `name`
      sapply(`rowLabels`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(columnLabels = self$`columnLabels`, id = self$`id`, name = self$`name`, rowLabels = self$`rowLabels`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`columnLabels`)) {
          sprintf(
            '"columnLabels":
            [%s]
                  ',
            paste(unlist(lapply(self$`columnLabels`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`rowLabels`)) {
          sprintf(
            '"rowLabels":
            [%s]
                  ',
            paste(unlist(lapply(self$`rowLabels`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PairwiseManualSelectionCreatedItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PairwiseManualSelectionCreatedItemJson, validateParams = FALSE) {
      PairwiseManualSelectionCreatedItemObject <- jsonlite::fromJSON(PairwiseManualSelectionCreatedItemJson)
      self$`columnLabels` <- ApiClient$new()$deserializeObj(PairwiseManualSelectionCreatedItemObject$`columnLabels`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`id` <- PairwiseManualSelectionCreatedItemObject$`id`
      self$`name` <- PairwiseManualSelectionCreatedItemObject$`name`
      self$`rowLabels` <- ApiClient$new()$deserializeObj(PairwiseManualSelectionCreatedItemObject$`rowLabels`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
