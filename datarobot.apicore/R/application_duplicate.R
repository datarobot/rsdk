# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApplicationDuplicate
#'
#' @description ApplicationDuplicate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field description  character [optional] The description of the application
#'
#' @field duplicatePredictions  character [optional] Import all predictions from the source application
#'
#' @field name  character [optional] The name of the app
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApplicationDuplicate <- R6::R6Class(
  "ApplicationDuplicate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`description` = NULL, `duplicatePredictions` = NULL, `name` = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`duplicatePredictions`)) {
        stopifnot(is.logical(`duplicatePredictions`), length(`duplicatePredictions`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
    }
  ),
  public = list(
    `description` = NULL,
    `duplicatePredictions` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param description The description of the application
    #' @param duplicatePredictions Import all predictions from the source application
    #' @param name The name of the app
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `duplicatePredictions` = FALSE, `name` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(description, duplicatePredictions, name)
      }
      self$`description` <- `description`
      self$`duplicatePredictions` <- `duplicatePredictions`
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(description = self$`description`, duplicatePredictions = self$`duplicatePredictions`, name = self$`name`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`duplicatePredictions`)) {
          sprintf(
            '"duplicatePredictions":
            %s
                  ',
            tolower(self$`duplicatePredictions`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApplicationDuplicateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApplicationDuplicateJson, validateParams = FALSE) {
      ApplicationDuplicateObject <- jsonlite::fromJSON(ApplicationDuplicateJson)
      self$`description` <- ApplicationDuplicateObject$`description`
      self$`duplicatePredictions` <- ApplicationDuplicateObject$`duplicatePredictions`
      self$`name` <- ApplicationDuplicateObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
