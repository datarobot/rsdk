# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionIntegrationJobLog
#'
#' @description PredictionIntegrationJobLog Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field message  character The message from that particular log entry. Can include a stacktrace
#'
#' @field time  character The time this log message occured at in UTC ISO format
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionIntegrationJobLog <- R6::R6Class(
  "PredictionIntegrationJobLog",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(message = NULL, time = NULL) {
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`time`)) {
        stopifnot(inherits(`time`, 'POSIXt'))
      }
    }
  ),
  public = list(
    `message` = NULL,
    `time` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param message The message from that particular log entry. Can include a stacktrace
    #' @param time The time this log message occured at in UTC ISO format
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `message` = NULL, `time` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`message`, `time`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(message, time)
      }
      self$`message` <- `message`
      self$`time` <- `time`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(message = self$`message`, time = self$`time`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`message`)) {
        sprintf(
        '"message":
          "%s"
                ',
        self$`message`
        )},
        if (!is.null(self$`time`)) {
        sprintf(
        '"time":
          "%s"
                ',
        format(self$`time`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionIntegrationJobLogJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionIntegrationJobLogJson, validateParams = FALSE) {
      PredictionIntegrationJobLogObject <- jsonlite::fromJSON(PredictionIntegrationJobLogJson)
      self$`message` <- PredictionIntegrationJobLogObject$`message`
      self$`time` <- ParseRFC3339Timestamp(PredictionIntegrationJobLogObject$`time`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
