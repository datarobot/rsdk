# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsBodyOneOf2
#'
#' @description CredentialsBodyOneOf2 Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field awsAccessKeyId  character [optional] AWS access key ID.
#'
#' @field awsSecretAccessKey  character [optional] AWS secret access key.
#'
#' @field awsSessionToken  character [optional] AWS session token.
#'
#' @field credentialType  character Credentials type.
#'
#' @field description  character [optional] Credentials description.
#'
#' @field name  character Credentials name.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsBodyOneOf2 <- R6::R6Class(
  "CredentialsBodyOneOf2",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(awsAccessKeyId = NULL, awsSecretAccessKey = NULL, awsSessionToken = NULL, credentialType = NULL, description = NULL, name = NULL) {
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`awsAccessKeyId`)) {
        stopifnot(is.character(`awsAccessKeyId`), length(`awsAccessKeyId`) == 1)
      }
      if (!is.null(`awsSecretAccessKey`)) {
        stopifnot(is.character(`awsSecretAccessKey`), length(`awsSecretAccessKey`) == 1)
      }
      if (!is.null(`awsSessionToken`)) {
        stopifnot(is.character(`awsSessionToken`), length(`awsSessionToken`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
    }
  ),
  public = list(
    `awsAccessKeyId` = NULL,
    `awsSecretAccessKey` = NULL,
    `awsSessionToken` = NULL,
    `credentialType` = NULL,
    `description` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param awsAccessKeyId AWS access key ID.
    #' @param awsSecretAccessKey AWS secret access key.
    #' @param awsSessionToken AWS session token.
    #' @param credentialType Credentials type.
    #' @param description Credentials description.
    #' @param name Credentials name.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialType` = NULL, `name` = NULL, `awsAccessKeyId` = NULL, `awsSecretAccessKey` = NULL, `awsSessionToken` = NULL, `description` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`credentialType`, `name`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(awsAccessKeyId, awsSecretAccessKey, awsSessionToken, credentialType, description, name)
      }
      self$`awsAccessKeyId` <- `awsAccessKeyId`
      self$`awsSecretAccessKey` <- `awsSecretAccessKey`
      self$`awsSessionToken` <- `awsSessionToken`
      self$`credentialType` <- `credentialType`
      self$`description` <- `description`
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(awsAccessKeyId = self$`awsAccessKeyId`, awsSecretAccessKey = self$`awsSecretAccessKey`, awsSessionToken = self$`awsSessionToken`, credentialType = self$`credentialType`, description = self$`description`, name = self$`name`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`awsAccessKeyId`)) {
          sprintf(
            '"awsAccessKeyId":
            "%s"
                  ',
            self$`awsAccessKeyId`
          )
        },
        if (!is.null(self$`awsSecretAccessKey`)) {
          sprintf(
            '"awsSecretAccessKey":
            "%s"
                  ',
            self$`awsSecretAccessKey`
          )
        },
        if (!is.null(self$`awsSessionToken`)) {
          sprintf(
            '"awsSessionToken":
            "%s"
                  ',
            self$`awsSessionToken`
          )
        },
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsBodyOneOf2Json A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsBodyOneOf2Json, validateParams = FALSE) {
      CredentialsBodyOneOf2Object <- jsonlite::fromJSON(CredentialsBodyOneOf2Json)
      self$`awsAccessKeyId` <- CredentialsBodyOneOf2Object$`awsAccessKeyId`
      self$`awsSecretAccessKey` <- CredentialsBodyOneOf2Object$`awsSecretAccessKey`
      self$`awsSessionToken` <- CredentialsBodyOneOf2Object$`awsSessionToken`
      self$`credentialType` <- CredentialsBodyOneOf2Object$`credentialType`
      self$`description` <- CredentialsBodyOneOf2Object$`description`
      self$`name` <- CredentialsBodyOneOf2Object$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
