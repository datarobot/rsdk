# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureDerivationWindow
#'
#' @description FeatureDerivationWindow Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field end  integer How many featureDerivationWindowUnits of each dataset&#39;s primary temporal key into the past relative to the datetimePartitionColumn the feature derivation window should end. Will be a non-positive integer, if present. If present, time-aware joins will be used. Only applicable when table1Identifier is not provided.
#'
#' @field start  integer How many featureDerivationWindowUnits of each dataset&#39;s primary temporal key into the past relative to the datetimePartitionColumn the feature derivation window should begin. Will be a negative integer, if present. If present, time-aware joins will be used. Only applicable when table1Identifier is not provided.
#'
#' @field unit  character Time unit of the feature derivation window. Supported values are MILLISECOND, SECOND, MINUTE, HOUR, DAY, WEEK, MONTH, QUARTER, YEAR. If present, time-aware joins will be used. Only applicable when table1Identifier is not provided.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureDerivationWindow <- R6::R6Class(
  "FeatureDerivationWindow",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`end` = NULL, `start` = NULL, `unit` = NULL) {
      if (!is.null(`end`)) {
        stopifnot(is.numeric(`end`), length(`end`) == 1)
      }
      if (!is.null(`start`)) {
        stopifnot(is.numeric(`start`), length(`start`) == 1)
      }
      if (!is.null(`unit`)) {
        stopifnot(is.character(`unit`), length(`unit`) == 1)
      }
    }
  ),
  public = list(
    `end` = NULL,
    `start` = NULL,
    `unit` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param end How many featureDerivationWindowUnits of each dataset&#39;s primary temporal key into the past relative to the datetimePartitionColumn the feature derivation window should end. Will be a non-positive integer, if present. If present, time-aware joins will be used. Only applicable when table1Identifier is not provided.
    #' @param start How many featureDerivationWindowUnits of each dataset&#39;s primary temporal key into the past relative to the datetimePartitionColumn the feature derivation window should begin. Will be a negative integer, if present. If present, time-aware joins will be used. Only applicable when table1Identifier is not provided.
    #' @param unit Time unit of the feature derivation window. Supported values are MILLISECOND, SECOND, MINUTE, HOUR, DAY, WEEK, MONTH, QUARTER, YEAR. If present, time-aware joins will be used. Only applicable when table1Identifier is not provided.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`end` = NULL, `start` = NULL, `unit` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`end`, `start`, `unit`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(end, start, unit)
      }
      self$`end` <- `end`
      self$`start` <- `start`
      self$`unit` <- `unit`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(end = self$`end`, start = self$`start`, unit = self$`unit`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`end`)) {
          sprintf(
            '"end":
            %d
                  ',
            self$`end`
          )
        },
        if (!is.null(self$`start`)) {
          sprintf(
            '"start":
            %d
                  ',
            self$`start`
          )
        },
        if (!is.null(self$`unit`)) {
          sprintf(
            '"unit":
            "%s"
                  ',
            self$`unit`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureDerivationWindowJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureDerivationWindowJson, validateParams = FALSE) {
      FeatureDerivationWindowObject <- jsonlite::fromJSON(FeatureDerivationWindowJson)
      self$`end` <- FeatureDerivationWindowObject$`end`
      self$`start` <- FeatureDerivationWindowObject$`start`
      self$`unit` <- FeatureDerivationWindowObject$`unit`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
