# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsBodyOneOf1
#'
#' @description CredentialsBodyOneOf1 Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialType  character Credentials type.
#'
#' @field description  character [optional] Credentials description.
#'
#' @field name  character Credentials name.
#'
#' @field refreshToken  character OAUTH token.
#'
#' @field token  character OAUTH token.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsBodyOneOf1 <- R6::R6Class(
  "CredentialsBodyOneOf1",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(credentialType = NULL, description = NULL, name = NULL, refreshToken = NULL, token = NULL) {
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`refreshToken`)) {
        stopifnot(is.character(`refreshToken`), length(`refreshToken`) == 1)
      }
      if (!is.null(`token`)) {
        stopifnot(is.character(`token`), length(`token`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
    }
  ),
  public = list(
    `credentialType` = NULL,
    `description` = NULL,
    `name` = NULL,
    `refreshToken` = NULL,
    `token` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialType Credentials type.
    #' @param description Credentials description.
    #' @param name Credentials name.
    #' @param refreshToken OAUTH token.
    #' @param token OAUTH token.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialType` = NULL, `name` = NULL, `refreshToken` = NULL, `token` = NULL, `description` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`credentialType`, `name`, `refreshToken`, `token`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(credentialType, description, name, refreshToken, token)
      }
      self$`credentialType` <- `credentialType`
      self$`description` <- `description`
      self$`name` <- `name`
      self$`refreshToken` <- `refreshToken`
      self$`token` <- `token`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(credentialType = self$`credentialType`, description = self$`description`, name = self$`name`, refreshToken = self$`refreshToken`, token = self$`token`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`refreshToken`)) {
          sprintf(
            '"refreshToken":
            "%s"
                  ',
            self$`refreshToken`
          )
        },
        if (!is.null(self$`token`)) {
          sprintf(
            '"token":
            "%s"
                  ',
            self$`token`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsBodyOneOf1Json A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsBodyOneOf1Json, validateParams = FALSE) {
      CredentialsBodyOneOf1Object <- jsonlite::fromJSON(CredentialsBodyOneOf1Json)
      self$`credentialType` <- CredentialsBodyOneOf1Object$`credentialType`
      self$`description` <- CredentialsBodyOneOf1Object$`description`
      self$`name` <- CredentialsBodyOneOf1Object$`name`
      self$`refreshToken` <- CredentialsBodyOneOf1Object$`refreshToken`
      self$`token` <- CredentialsBodyOneOf1Object$`token`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
