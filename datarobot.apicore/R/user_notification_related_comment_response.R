# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserNotificationRelatedCommentResponse
#'
#' @description UserNotificationRelatedCommentResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field commentId  character The ID of the comment
#'
#' @field commentLink  character [optional] The link to the comment.
#'
#' @field entityId  character The ID of the commented entity
#'
#' @field entityType  character [optional] The type of the commented entity.
#'
#' @field inactive  character [optional] True if the comment was deleted.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserNotificationRelatedCommentResponse <- R6::R6Class(
  "UserNotificationRelatedCommentResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(commentId = NULL, commentLink = NULL, entityId = NULL, entityType = NULL, inactive = NULL) {
      if (!is.null(`commentId`)) {
        stopifnot(is.character(`commentId`), length(`commentId`) == 1)
      }
      if (!is.null(`entityId`)) {
        stopifnot(is.character(`entityId`), length(`entityId`) == 1)
      }
      if (!is.null(`commentLink`)) {
        stopifnot(is.character(`commentLink`), length(`commentLink`) == 1)
      }
      if (!is.null(`entityType`)) {
        stopifnot(is.character(`entityType`), length(`entityType`) == 1)
      }
      if (!is.null(`inactive`)) {
        stopifnot(is.logical(`inactive`), length(`inactive`) == 1)
      }
    }
  ),
  public = list(
    `commentId` = NULL,
    `commentLink` = NULL,
    `entityId` = NULL,
    `entityType` = NULL,
    `inactive` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param commentId The ID of the comment
    #' @param commentLink The link to the comment.
    #' @param entityId The ID of the commented entity
    #' @param entityType The type of the commented entity.
    #' @param inactive True if the comment was deleted.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`commentId` = NULL, `entityId` = NULL, `commentLink` = NULL, `entityType` = NULL, `inactive` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`commentId`, `entityId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(commentId, commentLink, entityId, entityType, inactive)
      }
      self$`commentId` <- `commentId`
      self$`commentLink` <- `commentLink`
      self$`entityId` <- `entityId`
      self$`entityType` <- `entityType`
      self$`inactive` <- `inactive`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(commentId = self$`commentId`, commentLink = self$`commentLink`, entityId = self$`entityId`, entityType = self$`entityType`, inactive = self$`inactive`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`commentId`)) {
          sprintf(
            '"commentId":
            "%s"
                  ',
            self$`commentId`
          )
        },
        if (!is.null(self$`commentLink`)) {
          sprintf(
            '"commentLink":
            "%s"
                  ',
            self$`commentLink`
          )
        },
        if (!is.null(self$`entityId`)) {
          sprintf(
            '"entityId":
            "%s"
                  ',
            self$`entityId`
          )
        },
        if (!is.null(self$`entityType`)) {
          sprintf(
            '"entityType":
            "%s"
                  ',
            self$`entityType`
          )
        },
        if (!is.null(self$`inactive`)) {
          sprintf(
            '"inactive":
            %s
                  ',
            tolower(self$`inactive`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserNotificationRelatedCommentResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserNotificationRelatedCommentResponseJson, validateParams = FALSE) {
      UserNotificationRelatedCommentResponseObject <- jsonlite::fromJSON(UserNotificationRelatedCommentResponseJson)
      self$`commentId` <- UserNotificationRelatedCommentResponseObject$`commentId`
      self$`commentLink` <- UserNotificationRelatedCommentResponseObject$`commentLink`
      self$`entityId` <- UserNotificationRelatedCommentResponseObject$`entityId`
      self$`entityType` <- UserNotificationRelatedCommentResponseObject$`entityType`
      self$`inactive` <- UserNotificationRelatedCommentResponseObject$`inactive`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
