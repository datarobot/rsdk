# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintsBlueprintTaskData
#'
#' @description UserBlueprintsBlueprintTaskData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field inputs  list( character ) The ids or input data types which will be inputs to the task.
#'
#' @field outputMethod  character The method which the task will use to produce output.
#'
#' @field outputMethodParameters  list( \link{ParamValuePair} ) The parameters which further define how output will be produced.
#'
#' @field taskCode  character The unique code representing the python class which will be instantiated and executed.
#'
#' @field taskParameters  list( \link{ParamValuePair} ) The parameters which further define the behavior of the task.
#'
#' @field xTransformations  list( \link{ParamValuePair} ) Transformations to apply to the input data before fitting or predicting.
#'
#' @field yTransformations  list( \link{ParamValuePair} ) Transformations to apply to the input target before fitting or predicting.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintsBlueprintTaskData <- R6::R6Class(
  "UserBlueprintsBlueprintTaskData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`inputs` = NULL, `outputMethod` = NULL, `outputMethodParameters` = NULL, `taskCode` = NULL, `taskParameters` = NULL, `xTransformations` = NULL, `yTransformations` = NULL) {
      if (!is.null(`inputs`)) {
        stopifnot(is.vector(`inputs`), sapply(`inputs`, is.character))
      }
      if (!is.null(`outputMethod`)) {
        stopifnot(is.character(`outputMethod`), length(`outputMethod`) == 1)
      }
      if (!is.null(`outputMethodParameters`)) {
        stopifnot(is.vector(`outputMethodParameters`), sapply(`outputMethodParameters`, R6::is.R6))
      }
      if (!is.null(`taskCode`)) {
        stopifnot(is.character(`taskCode`), length(`taskCode`) == 1)
      }
      if (!is.null(`taskParameters`)) {
        stopifnot(is.vector(`taskParameters`), sapply(`taskParameters`, R6::is.R6))
      }
      if (!is.null(`xTransformations`)) {
        stopifnot(is.vector(`xTransformations`), sapply(`xTransformations`, R6::is.R6))
      }
      if (!is.null(`yTransformations`)) {
        stopifnot(is.vector(`yTransformations`), sapply(`yTransformations`, R6::is.R6))
      }
    }
  ),
  public = list(
    `inputs` = NULL,
    `outputMethod` = NULL,
    `outputMethodParameters` = NULL,
    `taskCode` = NULL,
    `taskParameters` = NULL,
    `xTransformations` = NULL,
    `yTransformations` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param inputs The ids or input data types which will be inputs to the task.
    #' @param outputMethod The method which the task will use to produce output.
    #' @param outputMethodParameters The parameters which further define how output will be produced.
    #' @param taskCode The unique code representing the python class which will be instantiated and executed.
    #' @param taskParameters The parameters which further define the behavior of the task.
    #' @param xTransformations Transformations to apply to the input data before fitting or predicting.
    #' @param yTransformations Transformations to apply to the input target before fitting or predicting.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`inputs` = NULL, `outputMethod` = NULL, `outputMethodParameters` = NULL, `taskCode` = NULL, `taskParameters` = NULL, `xTransformations` = NULL, `yTransformations` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`inputs`, `outputMethod`, `outputMethodParameters`, `taskCode`, `taskParameters`, `xTransformations`, `yTransformations`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(inputs, outputMethod, outputMethodParameters, taskCode, taskParameters, xTransformations, yTransformations)
      }
      self$`inputs` <- `inputs`
      self$`outputMethod` <- `outputMethod`
      self$`outputMethodParameters` <- `outputMethodParameters`
      self$`taskCode` <- `taskCode`
      self$`taskParameters` <- `taskParameters`
      self$`xTransformations` <- `xTransformations`
      self$`yTransformations` <- `yTransformations`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(inputs = self$`inputs`, outputMethod = self$`outputMethod`, outputMethodParameters = self$`outputMethodParameters`, taskCode = self$`taskCode`, taskParameters = self$`taskParameters`, xTransformations = self$`xTransformations`, yTransformations = self$`yTransformations`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`inputs`)) {
          sprintf(
            '"inputs":
            [%s]
                  ',
            paste(unlist(lapply(self$`inputs`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`outputMethod`)) {
          sprintf(
            '"outputMethod":
            "%s"
                  ',
            self$`outputMethod`
          )
        },
        if (!is.null(self$`outputMethodParameters`)) {
          sprintf(
            '"outputMethodParameters":
            [%s]
      ',
            paste(sapply(self$`outputMethodParameters`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`taskCode`)) {
          sprintf(
            '"taskCode":
            "%s"
                  ',
            self$`taskCode`
          )
        },
        if (!is.null(self$`taskParameters`)) {
          sprintf(
            '"taskParameters":
            [%s]
      ',
            paste(sapply(self$`taskParameters`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`xTransformations`)) {
          sprintf(
            '"xTransformations":
            [%s]
      ',
            paste(sapply(self$`xTransformations`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`yTransformations`)) {
          sprintf(
            '"yTransformations":
            [%s]
      ',
            paste(sapply(self$`yTransformations`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintsBlueprintTaskDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintsBlueprintTaskDataJson, validateParams = FALSE) {
      UserBlueprintsBlueprintTaskDataObject <- jsonlite::fromJSON(UserBlueprintsBlueprintTaskDataJson)
      self$`inputs` <- ApiClient$new()$deserializeObj(UserBlueprintsBlueprintTaskDataObject$`inputs`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`outputMethod` <- UserBlueprintsBlueprintTaskDataObject$`outputMethod`
      self$`outputMethodParameters` <- ApiClient$new()$deserializeObj(UserBlueprintsBlueprintTaskDataObject$`outputMethodParameters`, "array[ParamValuePair]", loadNamespace("datarobot.apicore"))
      self$`taskCode` <- UserBlueprintsBlueprintTaskDataObject$`taskCode`
      self$`taskParameters` <- ApiClient$new()$deserializeObj(UserBlueprintsBlueprintTaskDataObject$`taskParameters`, "array[ParamValuePair]", loadNamespace("datarobot.apicore"))
      self$`xTransformations` <- ApiClient$new()$deserializeObj(UserBlueprintsBlueprintTaskDataObject$`xTransformations`, "array[ParamValuePair]", loadNamespace("datarobot.apicore"))
      self$`yTransformations` <- ApiClient$new()$deserializeObj(UserBlueprintsBlueprintTaskDataObject$`yTransformations`, "array[ParamValuePair]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
