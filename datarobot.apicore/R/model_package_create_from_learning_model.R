# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelPackageCreateFromLearningModel
#'
#' @description ModelPackageCreateFromLearningModel Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field modelId  character ID of the DataRobot model.
#'
#' @field name  character [optional] Name of the model package.
#'
#' @field predictionThreshold  numeric [optional] Threshold used for binary classification in predictions
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelPackageCreateFromLearningModel <- R6::R6Class(
  "ModelPackageCreateFromLearningModel",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(modelId = NULL, name = NULL, predictionThreshold = NULL) {
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`predictionThreshold`)) {
        stopifnot(is.numeric(`predictionThreshold`), length(`predictionThreshold`) == 1)
      }
    }
  ),
  public = list(
    `modelId` = NULL,
    `name` = NULL,
    `predictionThreshold` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param modelId ID of the DataRobot model.
    #' @param name Name of the model package.
    #' @param predictionThreshold Threshold used for binary classification in predictions
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`modelId` = NULL, `name` = "null", `predictionThreshold` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`modelId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(modelId, name, predictionThreshold)
      }
      self$`modelId` <- `modelId`
      self$`name` <- `name`
      self$`predictionThreshold` <- `predictionThreshold`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(modelId = self$`modelId`, name = self$`name`, predictionThreshold = self$`predictionThreshold`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelPackageCreateFromLearningModelJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelPackageCreateFromLearningModelJson, validateParams = FALSE) {
      ModelPackageCreateFromLearningModelObject <- jsonlite::fromJSON(ModelPackageCreateFromLearningModelJson)
      self$`modelId` <- ModelPackageCreateFromLearningModelObject$`modelId`
      self$`name` <- ModelPackageCreateFromLearningModelObject$`name`
      self$`predictionThreshold` <- ModelPackageCreateFromLearningModelObject$`predictionThreshold`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
