# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CommentUpdate
#'
#' @description CommentUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field content  character Updated content of the comment, 10000 symbols max
#'
#' @field mentions  list( character ) [optional] A list of users IDs mentioned in the content
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CommentUpdate <- R6::R6Class(
  "CommentUpdate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`content` = NULL, `mentions` = NULL) {
      if (!is.null(`content`)) {
        stopifnot(is.character(`content`), length(`content`) == 1)
      }
      if (!is.null(`mentions`)) {
        stopifnot(is.vector(`mentions`))
      }
    }
  ),
  public = list(
    `content` = NULL,
    `mentions` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param content Updated content of the comment, 10000 symbols max
    #' @param mentions A list of users IDs mentioned in the content
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`content` = NULL, `mentions` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`content`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(content, mentions)
      }
      self$`content` <- `content`
      sapply(`mentions`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(content = self$`content`, mentions = self$`mentions`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`content`)) {
          sprintf(
            '"content":
            "%s"
                  ',
            self$`content`
          )
        },
        if (!is.null(self$`mentions`)) {
          sprintf(
            '"mentions":
            [%s]
                  ',
            paste(unlist(lapply(self$`mentions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CommentUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CommentUpdateJson, validateParams = FALSE) {
      CommentUpdateObject <- jsonlite::fromJSON(CommentUpdateJson)
      self$`content` <- CommentUpdateObject$`content`
      self$`mentions` <- ApiClient$new()$deserializeObj(CommentUpdateObject$`mentions`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
