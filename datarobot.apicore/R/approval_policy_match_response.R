# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApprovalPolicyMatchResponse
#'
#' @description ApprovalPolicyMatchResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field action  character Searched policy action.
#'
#' @field entityType  character Searched typed of the entity.
#'
#' @field fieldName  character [optional] Name of the entity field to filter policies by.
#'
#' @field fieldValue  character [optional] Value of the entity field to filter policies by.
#'
#' @field policyId  character ID of the matching approval policy. &#x60;&#x60;null&#x60;&#x60; if no matching policies found.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApprovalPolicyMatchResponse <- R6::R6Class(
  "ApprovalPolicyMatchResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(action = NULL, entityType = NULL, fieldName = NULL, fieldValue = NULL, policyId = NULL) {
      if (!is.null(`action`)) {
        stopifnot(is.character(`action`), length(`action`) == 1)
      }
      if (!is.null(`entityType`)) {
        stopifnot(is.character(`entityType`), length(`entityType`) == 1)
      }
      if (!is.null(`policyId`)) {
        stopifnot(is.character(`policyId`), length(`policyId`) == 1)
      }
      if (!is.null(`fieldName`)) {
        stopifnot(is.character(`fieldName`), length(`fieldName`) == 1)
      }
      if (!is.null(`fieldValue`)) {
        stopifnot(is.character(`fieldValue`), length(`fieldValue`) == 1)
      }
    }
  ),
  public = list(
    `action` = NULL,
    `entityType` = NULL,
    `fieldName` = NULL,
    `fieldValue` = NULL,
    `policyId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param action Searched policy action.
    #' @param entityType Searched typed of the entity.
    #' @param fieldName Name of the entity field to filter policies by.
    #' @param fieldValue Value of the entity field to filter policies by.
    #' @param policyId ID of the matching approval policy. &#x60;&#x60;null&#x60;&#x60; if no matching policies found.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`action` = NULL, `entityType` = NULL, `policyId` = NULL, `fieldName` = NULL, `fieldValue` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`action`, `entityType`, `policyId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(action, entityType, fieldName, fieldValue, policyId)
      }
      self$`action` <- `action`
      self$`entityType` <- `entityType`
      self$`fieldName` <- `fieldName`
      self$`fieldValue` <- `fieldValue`
      self$`policyId` <- `policyId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(action = self$`action`, entityType = self$`entityType`, fieldName = self$`fieldName`, fieldValue = self$`fieldValue`, policyId = self$`policyId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`action`)) {
          sprintf(
            '"action":
            "%s"
                  ',
            self$`action`
          )
        },
        if (!is.null(self$`entityType`)) {
          sprintf(
            '"entityType":
            "%s"
                  ',
            self$`entityType`
          )
        },
        if (!is.null(self$`fieldName`)) {
          sprintf(
            '"fieldName":
            "%s"
                  ',
            self$`fieldName`
          )
        },
        if (!is.null(self$`fieldValue`)) {
          sprintf(
            '"fieldValue":
            "%s"
                  ',
            self$`fieldValue`
          )
        },
        if (!is.null(self$`policyId`)) {
          sprintf(
            '"policyId":
            "%s"
                  ',
            self$`policyId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApprovalPolicyMatchResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApprovalPolicyMatchResponseJson, validateParams = FALSE) {
      ApprovalPolicyMatchResponseObject <- jsonlite::fromJSON(ApprovalPolicyMatchResponseJson)
      self$`action` <- ApprovalPolicyMatchResponseObject$`action`
      self$`entityType` <- ApprovalPolicyMatchResponseObject$`entityType`
      self$`fieldName` <- ApprovalPolicyMatchResponseObject$`fieldName`
      self$`fieldValue` <- ApprovalPolicyMatchResponseObject$`fieldValue`
      self$`policyId` <- ApprovalPolicyMatchResponseObject$`policyId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
