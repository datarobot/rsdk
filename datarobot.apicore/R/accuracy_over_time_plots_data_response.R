# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AccuracyOverTimePlotsDataResponse
#'
#' @description AccuracyOverTimePlotsDataResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field bins  list( \link{AccuracyOverTimePlotsBins} ) An array of bins for the retrieved plots.
#'
#' @field calendarEvents  list( \link{CalendarEvent} ) An array of calendar events for a retrieved plot.
#'
#' @field endDate  character The datetime of the end of the chart data (exclusive).
#'
#' @field resolution  character The resolution that is used for binning.
#'
#' @field startDate  character The datetime of the start of the chart data (inclusive).
#'
#' @field statistics  \link{AccuracyOverTimePlotsStatistics}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AccuracyOverTimePlotsDataResponse <- R6::R6Class(
  "AccuracyOverTimePlotsDataResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`bins` = NULL, `calendarEvents` = NULL, `endDate` = NULL, `resolution` = NULL, `startDate` = NULL, `statistics` = NULL) {
      if (!is.null(`bins`)) {
        stopifnot(is.vector(`bins`), sapply(`bins`, R6::is.R6))
      }
      if (!is.null(`calendarEvents`)) {
        stopifnot(is.vector(`calendarEvents`), sapply(`calendarEvents`, R6::is.R6))
      }
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`resolution`)) {
        stopifnot(is.character(`resolution`), length(`resolution`) == 1)
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
      if (!is.null(`statistics`)) {
        stopifnot(R6::is.R6(`statistics`))
      }
    }
  ),
  public = list(
    `bins` = NULL,
    `calendarEvents` = NULL,
    `endDate` = NULL,
    `resolution` = NULL,
    `startDate` = NULL,
    `statistics` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param bins An array of bins for the retrieved plots.
    #' @param calendarEvents An array of calendar events for a retrieved plot.
    #' @param endDate The datetime of the end of the chart data (exclusive).
    #' @param resolution The resolution that is used for binning.
    #' @param startDate The datetime of the start of the chart data (inclusive).
    #' @param statistics
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`bins` = NULL, `calendarEvents` = NULL, `endDate` = NULL, `resolution` = NULL, `startDate` = NULL, `statistics` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`bins`, `calendarEvents`, `endDate`, `resolution`, `startDate`, `statistics`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(bins, calendarEvents, endDate, resolution, startDate, statistics)
      }
      self$`bins` <- `bins`
      self$`calendarEvents` <- `calendarEvents`
      self$`endDate` <- `endDate`
      self$`resolution` <- `resolution`
      self$`startDate` <- `startDate`
      self$`statistics` <- `statistics`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(bins = self$`bins`, calendarEvents = self$`calendarEvents`, endDate = self$`endDate`, resolution = self$`resolution`, startDate = self$`startDate`, statistics = self$`statistics`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`bins`)) {
          sprintf(
            '"bins":
            [%s]
      ',
            paste(sapply(self$`bins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`calendarEvents`)) {
          sprintf(
            '"calendarEvents":
            [%s]
      ',
            paste(sapply(self$`calendarEvents`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`resolution`)) {
          sprintf(
            '"resolution":
            "%s"
                  ',
            self$`resolution`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`statistics`)) {
          sprintf(
            '"statistics":
            %s
      ',
            jsonlite::toJSON(self$`statistics`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AccuracyOverTimePlotsDataResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AccuracyOverTimePlotsDataResponseJson, validateParams = FALSE) {
      AccuracyOverTimePlotsDataResponseObject <- jsonlite::fromJSON(AccuracyOverTimePlotsDataResponseJson)
      self$`bins` <- ApiClient$new()$deserializeObj(AccuracyOverTimePlotsDataResponseObject$`bins`, "array[AccuracyOverTimePlotsBins]", loadNamespace("datarobot.apicore"))
      self$`calendarEvents` <- ApiClient$new()$deserializeObj(AccuracyOverTimePlotsDataResponseObject$`calendarEvents`, "array[CalendarEvent]", loadNamespace("datarobot.apicore"))
      self$`endDate` <- ParseRFC3339Timestamp(AccuracyOverTimePlotsDataResponseObject$`endDate`)
      self$`resolution` <- AccuracyOverTimePlotsDataResponseObject$`resolution`
      self$`startDate` <- ParseRFC3339Timestamp(AccuracyOverTimePlotsDataResponseObject$`startDate`)
      self$`statistics` <- AccuracyOverTimePlotsStatistics$new()$fromJSON(jsonlite::toJSON(AccuracyOverTimePlotsDataResponseObject$statistics, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
