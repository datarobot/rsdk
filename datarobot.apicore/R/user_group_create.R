# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserGroupCreate
#'
#' @description UserGroupCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accessRoleId  character [optional] The identifier of the access role assigned to the group.
#'
#' @field description  character [optional] The description of this user group.
#'
#' @field email  character [optional] The email that can be used to contact this user group.
#'
#' @field name  character The name of the new user group. Must be unique.
#'
#' @field orgId  character [optional] The organization identifier of this user group.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserGroupCreate <- R6::R6Class(
  "UserGroupCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`accessRoleId` = NULL, `description` = NULL, `email` = NULL, `name` = NULL, `orgId` = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`accessRoleId`)) {
        stopifnot(is.character(`accessRoleId`), length(`accessRoleId`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`email`)) {
        stopifnot(is.character(`email`), length(`email`) == 1)
      }
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
    }
  ),
  public = list(
    `accessRoleId` = NULL,
    `description` = NULL,
    `email` = NULL,
    `name` = NULL,
    `orgId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accessRoleId The identifier of the access role assigned to the group.
    #' @param description The description of this user group.
    #' @param email The email that can be used to contact this user group.
    #' @param name The name of the new user group. Must be unique.
    #' @param orgId The organization identifier of this user group.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `accessRoleId` = NULL, `description` = NULL, `email` = NULL, `orgId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`name`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(accessRoleId, description, email, name, orgId)
      }
      self$`accessRoleId` <- `accessRoleId`
      self$`description` <- `description`
      self$`email` <- `email`
      self$`name` <- `name`
      self$`orgId` <- `orgId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(accessRoleId = self$`accessRoleId`, description = self$`description`, email = self$`email`, name = self$`name`, orgId = self$`orgId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accessRoleId`)) {
          sprintf(
            '"accessRoleId":
            "%s"
                  ',
            self$`accessRoleId`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`email`)) {
          sprintf(
            '"email":
            "%s"
                  ',
            self$`email`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserGroupCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserGroupCreateJson, validateParams = FALSE) {
      UserGroupCreateObject <- jsonlite::fromJSON(UserGroupCreateJson)
      self$`accessRoleId` <- UserGroupCreateObject$`accessRoleId`
      self$`description` <- UserGroupCreateObject$`description`
      self$`email` <- UserGroupCreateObject$`email`
      self$`name` <- UserGroupCreateObject$`name`
      self$`orgId` <- UserGroupCreateObject$`orgId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
