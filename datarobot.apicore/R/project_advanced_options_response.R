# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ProjectAdvancedOptionsResponse
#'
#' @description ProjectAdvancedOptionsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field allowedPairwiseInteractionGroups  list( \link{array[character]} ) [optional] For GAM models - specify groups of columns for which pairwise interactions will be allowed. E.g. if set to [[\&quot;A\&quot;, \&quot;B\&quot;, \&quot;C\&quot;], [\&quot;C\&quot;, \&quot;D\&quot;] ] then GAM models will allow interactions between columns AxB, BxC, AxC, CxD. All others (AxD, BxD) will not be considered. If not specified - all possible interactions will be considered by model.
#'
#' @field blendBestModels  character blend best models during Autopilot run
#'
#' @field blueprintThreshold  integer an upper bound on running time (in hours), such that models exceeding the bound will be excluded in subsequent autopilot runs
#'
#' @field considerBlendersInRecommendation  character [optional] Include blenders when selecting a model to prepare for deployment in an Autopilot Run.
#'
#' @field defaultMonotonicDecreasingFeaturelistId  character null or str, the ID of the featurelist specifying a set of features with a monotonically decreasing relationship to the target. All blueprints generated in the project use this as their default monotonic constraint, but it can be overriden at model submission time.
#'
#' @field defaultMonotonicIncreasingFeaturelistId  character null or str, the ID of the featurelist specifying a set of features with a monotonically increasing relationship to the target. All blueprints generated in the project use this as their default monotonic constraint, but it can be overriden at model submission time.
#'
#' @field downsampledMajorityRows  integer the total number of the majority rows available for modeling, or null for projects without smart downsampling
#'
#' @field downsampledMinorityRows  integer the total number of the minority rows available for modeling, or null for projects without smart downsampling
#'
#' @field eventsCount  character [optional] the name of the event count column, if specified, otherwise null.
#'
#' @field exposure  character [optional] the name of the exposure column, if specified.
#'
#' @field majorityDownsamplingRate  numeric the percentage between 0 and 100 of the majority rows that are kept, or null for projects without smart downsampling
#'
#' @field minSecondaryValidationModelCount  character [optional] Compute \&quot;All backtest\&quot; scores (datetime models) or cross validation scores for the specified number of highest ranking models on the Leaderboard, if over the Autopilot default.
#'
#' @field offset  list( character ) [optional] the list of names of the offset columns, if specified, otherwise null.
#'
#' @field onlyIncludeMonotonicBlueprints  character whether the project only includes blueprints support enforcing monotonic constraints
#'
#' @field prepareModelForDeployment  character Prepare model for deployment during Autopilot run. The preparation includes creating reduced feature list models, retraining best model on higher sample size, computing insights and assigning \&quot;RECOMMENDED FOR DEPLOYMENT\&quot; label.
#'
#' @field responseCap  \link{OneOfbooleannumber} defaults to False, if specified used to cap the maximum response of a model
#'
#' @field runLeakageRemovedFeatureList  character [optional] Run Autopilot on Leakage Removed feature list (if exists).
#'
#' @field scoringCodeOnly  character Keep only models that can be converted to scorable java code during Autopilot run.
#'
#' @field seed  character defaults to null, the random seed to be used if specified
#'
#' @field shapOnlyMode  character Keep only models that support SHAP values during Autopilot run. Use SHAP-based insights wherever possible. For pre SHAP-only mode projects this is always &#x60;&#x60;null&#x60;&#x60;.
#'
#' @field smartDownsampled  character whether the project uses smart downsampling to throw away excess rows of the majority class.  Smart downsampled projects express all sample percents in terms of percent of minority rows (as opposed to percent of all rows).
#'
#' @field weights  character the name of the weight column, if specified, otherwise null.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ProjectAdvancedOptionsResponse <- R6::R6Class(
  "ProjectAdvancedOptionsResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(allowedPairwiseInteractionGroups = NULL, blendBestModels = NULL, blueprintThreshold = NULL, considerBlendersInRecommendation = NULL, defaultMonotonicDecreasingFeaturelistId = NULL, defaultMonotonicIncreasingFeaturelistId = NULL, downsampledMajorityRows = NULL, downsampledMinorityRows = NULL, eventsCount = NULL, exposure = NULL, majorityDownsamplingRate = NULL, minSecondaryValidationModelCount = NULL, offset = NULL, onlyIncludeMonotonicBlueprints = NULL, prepareModelForDeployment = NULL, responseCap = NULL, runLeakageRemovedFeatureList = NULL, scoringCodeOnly = NULL, seed = NULL, shapOnlyMode = NULL, smartDownsampled = NULL, weights = NULL) {
      if (!is.null(`blendBestModels`)) {
        stopifnot(is.logical(`blendBestModels`), length(`blendBestModels`) == 1)
      }
      if (!is.null(`blueprintThreshold`)) {
        stopifnot(is.numeric(`blueprintThreshold`), length(`blueprintThreshold`) == 1)
      }
      if (!is.null(`defaultMonotonicDecreasingFeaturelistId`)) {
        stopifnot(is.character(`defaultMonotonicDecreasingFeaturelistId`), length(`defaultMonotonicDecreasingFeaturelistId`) == 1)
      }
      if (!is.null(`defaultMonotonicIncreasingFeaturelistId`)) {
        stopifnot(is.character(`defaultMonotonicIncreasingFeaturelistId`), length(`defaultMonotonicIncreasingFeaturelistId`) == 1)
      }
      if (!is.null(`downsampledMajorityRows`)) {
        stopifnot(is.numeric(`downsampledMajorityRows`), length(`downsampledMajorityRows`) == 1)
      }
      if (!is.null(`downsampledMinorityRows`)) {
        stopifnot(is.numeric(`downsampledMinorityRows`), length(`downsampledMinorityRows`) == 1)
      }
      if (!is.null(`majorityDownsamplingRate`)) {
      }
      if (!is.null(`onlyIncludeMonotonicBlueprints`)) {
        stopifnot(is.logical(`onlyIncludeMonotonicBlueprints`), length(`onlyIncludeMonotonicBlueprints`) == 1)
      }
      if (!is.null(`prepareModelForDeployment`)) {
        stopifnot(is.logical(`prepareModelForDeployment`), length(`prepareModelForDeployment`) == 1)
      }
      if (!is.null(`responseCap`)) {
        .setPrimitiveProperty(typeList = list("boolean", "numeric"), propertyData = responseCap)
      }
      if (!is.null(`scoringCodeOnly`)) {
        stopifnot(is.logical(`scoringCodeOnly`), length(`scoringCodeOnly`) == 1)
      }
      if (!is.null(`seed`)) {
        stopifnot(is.character(`seed`), length(`seed`) == 1)
      }
      if (!is.null(`shapOnlyMode`)) {
        stopifnot(is.logical(`shapOnlyMode`), length(`shapOnlyMode`) == 1)
      }
      if (!is.null(`smartDownsampled`)) {
        stopifnot(is.logical(`smartDownsampled`), length(`smartDownsampled`) == 1)
      }
      if (!is.null(`weights`)) {
        stopifnot(is.character(`weights`), length(`weights`) == 1)
      }
      if (!is.null(`allowedPairwiseInteractionGroups`)) {
        stopifnot(is.vector(`allowedPairwiseInteractionGroups`))
      }
      if (!is.null(`considerBlendersInRecommendation`)) {
        stopifnot(is.logical(`considerBlendersInRecommendation`), length(`considerBlendersInRecommendation`) == 1)
      }
      if (!is.null(`eventsCount`)) {
        stopifnot(is.character(`eventsCount`), length(`eventsCount`) == 1)
      }
      if (!is.null(`exposure`)) {
        stopifnot(is.character(`exposure`), length(`exposure`) == 1)
      }
      if (!is.null(`minSecondaryValidationModelCount`)) {
        stopifnot(is.logical(`minSecondaryValidationModelCount`), length(`minSecondaryValidationModelCount`) == 1)
      }
      if (!is.null(`offset`)) {
        stopifnot(is.vector(`offset`))
      }
      if (!is.null(`runLeakageRemovedFeatureList`)) {
        stopifnot(is.logical(`runLeakageRemovedFeatureList`), length(`runLeakageRemovedFeatureList`) == 1)
      }
    }
  ),
  public = list(
    `allowedPairwiseInteractionGroups` = NULL,
    `blendBestModels` = NULL,
    `blueprintThreshold` = NULL,
    `considerBlendersInRecommendation` = NULL,
    `defaultMonotonicDecreasingFeaturelistId` = NULL,
    `defaultMonotonicIncreasingFeaturelistId` = NULL,
    `downsampledMajorityRows` = NULL,
    `downsampledMinorityRows` = NULL,
    `eventsCount` = NULL,
    `exposure` = NULL,
    `majorityDownsamplingRate` = NULL,
    `minSecondaryValidationModelCount` = NULL,
    `offset` = NULL,
    `onlyIncludeMonotonicBlueprints` = NULL,
    `prepareModelForDeployment` = NULL,
    `responseCap` = NULL,
    `runLeakageRemovedFeatureList` = NULL,
    `scoringCodeOnly` = NULL,
    `seed` = NULL,
    `shapOnlyMode` = NULL,
    `smartDownsampled` = NULL,
    `weights` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param allowedPairwiseInteractionGroups For GAM models - specify groups of columns for which pairwise interactions will be allowed. E.g. if set to [[\&quot;A\&quot;, \&quot;B\&quot;, \&quot;C\&quot;], [\&quot;C\&quot;, \&quot;D\&quot;] ] then GAM models will allow interactions between columns AxB, BxC, AxC, CxD. All others (AxD, BxD) will not be considered. If not specified - all possible interactions will be considered by model.
    #' @param blendBestModels blend best models during Autopilot run
    #' @param blueprintThreshold an upper bound on running time (in hours), such that models exceeding the bound will be excluded in subsequent autopilot runs
    #' @param considerBlendersInRecommendation Include blenders when selecting a model to prepare for deployment in an Autopilot Run.
    #' @param defaultMonotonicDecreasingFeaturelistId null or str, the ID of the featurelist specifying a set of features with a monotonically decreasing relationship to the target. All blueprints generated in the project use this as their default monotonic constraint, but it can be overriden at model submission time.
    #' @param defaultMonotonicIncreasingFeaturelistId null or str, the ID of the featurelist specifying a set of features with a monotonically increasing relationship to the target. All blueprints generated in the project use this as their default monotonic constraint, but it can be overriden at model submission time.
    #' @param downsampledMajorityRows the total number of the majority rows available for modeling, or null for projects without smart downsampling
    #' @param downsampledMinorityRows the total number of the minority rows available for modeling, or null for projects without smart downsampling
    #' @param eventsCount the name of the event count column, if specified, otherwise null.
    #' @param exposure the name of the exposure column, if specified.
    #' @param majorityDownsamplingRate the percentage between 0 and 100 of the majority rows that are kept, or null for projects without smart downsampling
    #' @param minSecondaryValidationModelCount Compute \&quot;All backtest\&quot; scores (datetime models) or cross validation scores for the specified number of highest ranking models on the Leaderboard, if over the Autopilot default.
    #' @param offset the list of names of the offset columns, if specified, otherwise null.
    #' @param onlyIncludeMonotonicBlueprints whether the project only includes blueprints support enforcing monotonic constraints
    #' @param prepareModelForDeployment Prepare model for deployment during Autopilot run. The preparation includes creating reduced feature list models, retraining best model on higher sample size, computing insights and assigning \&quot;RECOMMENDED FOR DEPLOYMENT\&quot; label.
    #' @param responseCap defaults to False, if specified used to cap the maximum response of a model
    #' @param runLeakageRemovedFeatureList Run Autopilot on Leakage Removed feature list (if exists).
    #' @param scoringCodeOnly Keep only models that can be converted to scorable java code during Autopilot run.
    #' @param seed defaults to null, the random seed to be used if specified
    #' @param shapOnlyMode Keep only models that support SHAP values during Autopilot run. Use SHAP-based insights wherever possible. For pre SHAP-only mode projects this is always &#x60;&#x60;null&#x60;&#x60;.
    #' @param smartDownsampled whether the project uses smart downsampling to throw away excess rows of the majority class.  Smart downsampled projects express all sample percents in terms of percent of minority rows (as opposed to percent of all rows).
    #' @param weights the name of the weight column, if specified, otherwise null.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blendBestModels` = NULL, `blueprintThreshold` = NULL, `defaultMonotonicDecreasingFeaturelistId` = NULL, `defaultMonotonicIncreasingFeaturelistId` = NULL, `downsampledMajorityRows` = NULL, `downsampledMinorityRows` = NULL, `majorityDownsamplingRate` = NULL, `onlyIncludeMonotonicBlueprints` = NULL, `prepareModelForDeployment` = NULL, `responseCap` = NULL, `scoringCodeOnly` = NULL, `seed` = NULL, `shapOnlyMode` = NULL, `smartDownsampled` = NULL, `weights` = NULL, `allowedPairwiseInteractionGroups` = NULL, `considerBlendersInRecommendation` = NULL, `eventsCount` = NULL, `exposure` = NULL, `minSecondaryValidationModelCount` = NULL, `offset` = NULL, `runLeakageRemovedFeatureList` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`blendBestModels`, `blueprintThreshold`, `defaultMonotonicDecreasingFeaturelistId`, `defaultMonotonicIncreasingFeaturelistId`, `downsampledMajorityRows`, `downsampledMinorityRows`, `majorityDownsamplingRate`, `onlyIncludeMonotonicBlueprints`, `prepareModelForDeployment`, `responseCap`, `scoringCodeOnly`, `seed`, `shapOnlyMode`, `smartDownsampled`, `weights`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(allowedPairwiseInteractionGroups, blendBestModels, blueprintThreshold, considerBlendersInRecommendation, defaultMonotonicDecreasingFeaturelistId, defaultMonotonicIncreasingFeaturelistId, downsampledMajorityRows, downsampledMinorityRows, eventsCount, exposure, majorityDownsamplingRate, minSecondaryValidationModelCount, offset, onlyIncludeMonotonicBlueprints, prepareModelForDeployment, responseCap, runLeakageRemovedFeatureList, scoringCodeOnly, seed, shapOnlyMode, smartDownsampled, weights)
      }
      sapply(`allowedPairwiseInteractionGroups`, function(x) stopifnot(R6::is.R6(x)))
      self$`blendBestModels` <- `blendBestModels`
      self$`blueprintThreshold` <- `blueprintThreshold`
      self$`considerBlendersInRecommendation` <- `considerBlendersInRecommendation`
      self$`defaultMonotonicDecreasingFeaturelistId` <- `defaultMonotonicDecreasingFeaturelistId`
      self$`defaultMonotonicIncreasingFeaturelistId` <- `defaultMonotonicIncreasingFeaturelistId`
      self$`downsampledMajorityRows` <- `downsampledMajorityRows`
      self$`downsampledMinorityRows` <- `downsampledMinorityRows`
      self$`eventsCount` <- `eventsCount`
      self$`exposure` <- `exposure`
      self$`majorityDownsamplingRate` <- `majorityDownsamplingRate`
      self$`minSecondaryValidationModelCount` <- `minSecondaryValidationModelCount`
      sapply(`offset`, function(x) stopifnot(is.character(x)))
      self$`onlyIncludeMonotonicBlueprints` <- `onlyIncludeMonotonicBlueprints`
      self$`prepareModelForDeployment` <- `prepareModelForDeployment`
      self$`responseCap` <- .setPrimitiveProperty(typeList = list("boolean", "numeric"), propertyData = responseCap)
      self$`runLeakageRemovedFeatureList` <- `runLeakageRemovedFeatureList`
      self$`scoringCodeOnly` <- `scoringCodeOnly`
      self$`seed` <- `seed`
      self$`shapOnlyMode` <- `shapOnlyMode`
      self$`smartDownsampled` <- `smartDownsampled`
      self$`weights` <- `weights`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(allowedPairwiseInteractionGroups = self$`allowedPairwiseInteractionGroups`, blendBestModels = self$`blendBestModels`, blueprintThreshold = self$`blueprintThreshold`, considerBlendersInRecommendation = self$`considerBlendersInRecommendation`, defaultMonotonicDecreasingFeaturelistId = self$`defaultMonotonicDecreasingFeaturelistId`, defaultMonotonicIncreasingFeaturelistId = self$`defaultMonotonicIncreasingFeaturelistId`, downsampledMajorityRows = self$`downsampledMajorityRows`, downsampledMinorityRows = self$`downsampledMinorityRows`, eventsCount = self$`eventsCount`, exposure = self$`exposure`, majorityDownsamplingRate = self$`majorityDownsamplingRate`, minSecondaryValidationModelCount = self$`minSecondaryValidationModelCount`, offset = self$`offset`, onlyIncludeMonotonicBlueprints = self$`onlyIncludeMonotonicBlueprints`, prepareModelForDeployment = self$`prepareModelForDeployment`, responseCap = self$`responseCap`, runLeakageRemovedFeatureList = self$`runLeakageRemovedFeatureList`, scoringCodeOnly = self$`scoringCodeOnly`, seed = self$`seed`, shapOnlyMode = self$`shapOnlyMode`, smartDownsampled = self$`smartDownsampled`, weights = self$`weights`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`allowedPairwiseInteractionGroups`)) {
          sprintf(
            '"allowedPairwiseInteractionGroups":
            [%s]
',
            paste(sapply(self$`allowedPairwiseInteractionGroups`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`blendBestModels`)) {
          sprintf(
            '"blendBestModels":
            %s
                  ',
            tolower(self$`blendBestModels`)
          )
        },
        if (!is.null(self$`blueprintThreshold`)) {
          sprintf(
            '"blueprintThreshold":
            %d
                  ',
            self$`blueprintThreshold`
          )
        },
        if (!is.null(self$`considerBlendersInRecommendation`)) {
          sprintf(
            '"considerBlendersInRecommendation":
            %s
                  ',
            tolower(self$`considerBlendersInRecommendation`)
          )
        },
        if (!is.null(self$`defaultMonotonicDecreasingFeaturelistId`)) {
          sprintf(
            '"defaultMonotonicDecreasingFeaturelistId":
            "%s"
                  ',
            self$`defaultMonotonicDecreasingFeaturelistId`
          )
        },
        if (!is.null(self$`defaultMonotonicIncreasingFeaturelistId`)) {
          sprintf(
            '"defaultMonotonicIncreasingFeaturelistId":
            "%s"
                  ',
            self$`defaultMonotonicIncreasingFeaturelistId`
          )
        },
        if (!is.null(self$`downsampledMajorityRows`)) {
          sprintf(
            '"downsampledMajorityRows":
            %d
                  ',
            self$`downsampledMajorityRows`
          )
        },
        if (!is.null(self$`downsampledMinorityRows`)) {
          sprintf(
            '"downsampledMinorityRows":
            %d
                  ',
            self$`downsampledMinorityRows`
          )
        },
        if (!is.null(self$`eventsCount`)) {
          sprintf(
            '"eventsCount":
            "%s"
                  ',
            self$`eventsCount`
          )
        },
        if (!is.null(self$`exposure`)) {
          sprintf(
            '"exposure":
            "%s"
                  ',
            self$`exposure`
          )
        },
        if (!is.null(self$`majorityDownsamplingRate`)) {
          sprintf(
            '"majorityDownsamplingRate":
            %d
                  ',
            self$`majorityDownsamplingRate`
          )
        },
        if (!is.null(self$`minSecondaryValidationModelCount`)) {
          sprintf(
            '"minSecondaryValidationModelCount":
            %s
                  ',
            tolower(self$`minSecondaryValidationModelCount`)
          )
        },
        if (!is.null(self$`offset`)) {
          sprintf(
            '"offset":
              [%s]
            ',
            paste(unlist(lapply(self$`offset`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`onlyIncludeMonotonicBlueprints`)) {
          sprintf(
            '"onlyIncludeMonotonicBlueprints":
            %s
                  ',
            tolower(self$`onlyIncludeMonotonicBlueprints`)
          )
        },
        if (!is.null(self$`prepareModelForDeployment`)) {
          sprintf(
            '"prepareModelForDeployment":
            %s
                  ',
            tolower(self$`prepareModelForDeployment`)
          )
        },
        if (!is.null(self$`responseCap`)) {
          sprintf(
            '"responseCap":
          %s
        ',
            self$`responseCap`
          )
        },
        if (!is.null(self$`runLeakageRemovedFeatureList`)) {
          sprintf(
            '"runLeakageRemovedFeatureList":
            %s
                  ',
            tolower(self$`runLeakageRemovedFeatureList`)
          )
        },
        if (!is.null(self$`scoringCodeOnly`)) {
          sprintf(
            '"scoringCodeOnly":
            %s
                  ',
            tolower(self$`scoringCodeOnly`)
          )
        },
        if (!is.null(self$`seed`)) {
          sprintf(
            '"seed":
            "%s"
                  ',
            self$`seed`
          )
        },
        if (!is.null(self$`shapOnlyMode`)) {
          sprintf(
            '"shapOnlyMode":
            %s
                  ',
            tolower(self$`shapOnlyMode`)
          )
        },
        if (!is.null(self$`smartDownsampled`)) {
          sprintf(
            '"smartDownsampled":
            %s
                  ',
            tolower(self$`smartDownsampled`)
          )
        },
        if (!is.null(self$`weights`)) {
          sprintf(
            '"weights":
            "%s"
                  ',
            self$`weights`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ProjectAdvancedOptionsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ProjectAdvancedOptionsResponseJson, validateParams = FALSE) {
      ProjectAdvancedOptionsResponseObject <- jsonlite::fromJSON(ProjectAdvancedOptionsResponseJson)
      self$`allowedPairwiseInteractionGroups` <- ApiClient$new()$deserializeObj(ProjectAdvancedOptionsResponseObject$`allowedPairwiseInteractionGroups`, "array[array[character]]", loadNamespace("datarobot.apicore"))
      self$`blendBestModels` <- ProjectAdvancedOptionsResponseObject$`blendBestModels`
      self$`blueprintThreshold` <- ProjectAdvancedOptionsResponseObject$`blueprintThreshold`
      self$`considerBlendersInRecommendation` <- ProjectAdvancedOptionsResponseObject$`considerBlendersInRecommendation`
      self$`defaultMonotonicDecreasingFeaturelistId` <- ProjectAdvancedOptionsResponseObject$`defaultMonotonicDecreasingFeaturelistId`
      self$`defaultMonotonicIncreasingFeaturelistId` <- ProjectAdvancedOptionsResponseObject$`defaultMonotonicIncreasingFeaturelistId`
      self$`downsampledMajorityRows` <- ProjectAdvancedOptionsResponseObject$`downsampledMajorityRows`
      self$`downsampledMinorityRows` <- ProjectAdvancedOptionsResponseObject$`downsampledMinorityRows`
      self$`eventsCount` <- ProjectAdvancedOptionsResponseObject$`eventsCount`
      self$`exposure` <- ProjectAdvancedOptionsResponseObject$`exposure`
      self$`majorityDownsamplingRate` <- ProjectAdvancedOptionsResponseObject$`majorityDownsamplingRate`
      self$`minSecondaryValidationModelCount` <- ProjectAdvancedOptionsResponseObject$`minSecondaryValidationModelCount`
      self$`offset` <- ApiClient$new()$deserializeObj(ProjectAdvancedOptionsResponseObject$`offset`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`onlyIncludeMonotonicBlueprints` <- ProjectAdvancedOptionsResponseObject$`onlyIncludeMonotonicBlueprints`
      self$`prepareModelForDeployment` <- ProjectAdvancedOptionsResponseObject$`prepareModelForDeployment`
      self$`responseCap` <- .setPrimitiveProperty(typeList = list("boolean", "numeric"), propertyData = ProjectAdvancedOptionsResponseObject$responseCap)
      self$`runLeakageRemovedFeatureList` <- ProjectAdvancedOptionsResponseObject$`runLeakageRemovedFeatureList`
      self$`scoringCodeOnly` <- ProjectAdvancedOptionsResponseObject$`scoringCodeOnly`
      self$`seed` <- ProjectAdvancedOptionsResponseObject$`seed`
      self$`shapOnlyMode` <- ProjectAdvancedOptionsResponseObject$`shapOnlyMode`
      self$`smartDownsampled` <- ProjectAdvancedOptionsResponseObject$`smartDownsampled`
      self$`weights` <- ProjectAdvancedOptionsResponseObject$`weights`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
