# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataStoreDescribePermissionsResponse
#'
#' @description DataStoreDescribePermissionsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field canCreateDataSource  character True if the user can create data source from this data store.
#'
#' @field canDelete  character True if the user can delete the data store.
#'
#' @field canEdit  character True if the user can edit data store info.
#'
#' @field canScanDatabase  character True if the user can scan data store database.
#'
#' @field canSetRoles  list( character ) Roles the user can grant or revoke from other users, groups, or organizations.
#'
#' @field canShare  character True if the user can share the data store.
#'
#' @field canTestConnection  character True if the user can test data store database connection.
#'
#' @field canView  character True if the user can view data store info.
#'
#' @field dataStoreId  character The ID of the data store.
#'
#' @field userId  character The ID of the user identified by username.
#'
#' @field username  character &#x60;username&#x60; of a user with access to this data store.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataStoreDescribePermissionsResponse <- R6::R6Class(
  "DataStoreDescribePermissionsResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`canCreateDataSource` = NULL, `canDelete` = NULL, `canEdit` = NULL, `canScanDatabase` = NULL, `canSetRoles` = NULL, `canShare` = NULL, `canTestConnection` = NULL, `canView` = NULL, `dataStoreId` = NULL, `userId` = NULL, `username` = NULL) {
      if (!is.null(`canCreateDataSource`)) {
        stopifnot(is.logical(`canCreateDataSource`), length(`canCreateDataSource`) == 1)
      }
      if (!is.null(`canDelete`)) {
        stopifnot(is.logical(`canDelete`), length(`canDelete`) == 1)
      }
      if (!is.null(`canEdit`)) {
        stopifnot(is.logical(`canEdit`), length(`canEdit`) == 1)
      }
      if (!is.null(`canScanDatabase`)) {
        stopifnot(is.logical(`canScanDatabase`), length(`canScanDatabase`) == 1)
      }
      if (!is.null(`canSetRoles`)) {
        stopifnot(is.vector(`canSetRoles`))
      }
      if (!is.null(`canShare`)) {
        stopifnot(is.logical(`canShare`), length(`canShare`) == 1)
      }
      if (!is.null(`canTestConnection`)) {
        stopifnot(is.logical(`canTestConnection`), length(`canTestConnection`) == 1)
      }
      if (!is.null(`canView`)) {
        stopifnot(is.logical(`canView`), length(`canView`) == 1)
      }
      if (!is.null(`dataStoreId`)) {
        stopifnot(is.character(`dataStoreId`), length(`dataStoreId`) == 1)
      }
      if (!is.null(`userId`)) {
        stopifnot(is.character(`userId`), length(`userId`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
    }
  ),
  public = list(
    `canCreateDataSource` = NULL,
    `canDelete` = NULL,
    `canEdit` = NULL,
    `canScanDatabase` = NULL,
    `canSetRoles` = NULL,
    `canShare` = NULL,
    `canTestConnection` = NULL,
    `canView` = NULL,
    `dataStoreId` = NULL,
    `userId` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param canCreateDataSource True if the user can create data source from this data store.
    #' @param canDelete True if the user can delete the data store.
    #' @param canEdit True if the user can edit data store info.
    #' @param canScanDatabase True if the user can scan data store database.
    #' @param canSetRoles Roles the user can grant or revoke from other users, groups, or organizations.
    #' @param canShare True if the user can share the data store.
    #' @param canTestConnection True if the user can test data store database connection.
    #' @param canView True if the user can view data store info.
    #' @param dataStoreId The ID of the data store.
    #' @param userId The ID of the user identified by username.
    #' @param username &#x60;username&#x60; of a user with access to this data store.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`canCreateDataSource` = NULL, `canDelete` = NULL, `canEdit` = NULL, `canScanDatabase` = NULL, `canSetRoles` = NULL, `canShare` = NULL, `canTestConnection` = NULL, `canView` = NULL, `dataStoreId` = NULL, `userId` = NULL, `username` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`canCreateDataSource`, `canDelete`, `canEdit`, `canScanDatabase`, `canSetRoles`, `canShare`, `canTestConnection`, `canView`, `dataStoreId`, `userId`, `username`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(canCreateDataSource, canDelete, canEdit, canScanDatabase, canSetRoles, canShare, canTestConnection, canView, dataStoreId, userId, username)
      }
      self$`canCreateDataSource` <- `canCreateDataSource`
      self$`canDelete` <- `canDelete`
      self$`canEdit` <- `canEdit`
      self$`canScanDatabase` <- `canScanDatabase`
      sapply(`canSetRoles`, function(x) stopifnot(is.character(x)))
      self$`canShare` <- `canShare`
      self$`canTestConnection` <- `canTestConnection`
      self$`canView` <- `canView`
      self$`dataStoreId` <- `dataStoreId`
      self$`userId` <- `userId`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(canCreateDataSource = self$`canCreateDataSource`, canDelete = self$`canDelete`, canEdit = self$`canEdit`, canScanDatabase = self$`canScanDatabase`, canSetRoles = self$`canSetRoles`, canShare = self$`canShare`, canTestConnection = self$`canTestConnection`, canView = self$`canView`, dataStoreId = self$`dataStoreId`, userId = self$`userId`, username = self$`username`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`canCreateDataSource`)) {
          sprintf(
            '"canCreateDataSource":
            %s
                  ',
            tolower(self$`canCreateDataSource`)
          )
        },
        if (!is.null(self$`canDelete`)) {
          sprintf(
            '"canDelete":
            %s
                  ',
            tolower(self$`canDelete`)
          )
        },
        if (!is.null(self$`canEdit`)) {
          sprintf(
            '"canEdit":
            %s
                  ',
            tolower(self$`canEdit`)
          )
        },
        if (!is.null(self$`canScanDatabase`)) {
          sprintf(
            '"canScanDatabase":
            %s
                  ',
            tolower(self$`canScanDatabase`)
          )
        },
        if (!is.null(self$`canSetRoles`)) {
          sprintf(
            '"canSetRoles":
            [%s]
                  ',
            paste(unlist(lapply(self$`canSetRoles`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`canShare`)) {
          sprintf(
            '"canShare":
            %s
                  ',
            tolower(self$`canShare`)
          )
        },
        if (!is.null(self$`canTestConnection`)) {
          sprintf(
            '"canTestConnection":
            %s
                  ',
            tolower(self$`canTestConnection`)
          )
        },
        if (!is.null(self$`canView`)) {
          sprintf(
            '"canView":
            %s
                  ',
            tolower(self$`canView`)
          )
        },
        if (!is.null(self$`dataStoreId`)) {
          sprintf(
            '"dataStoreId":
            "%s"
                  ',
            self$`dataStoreId`
          )
        },
        if (!is.null(self$`userId`)) {
          sprintf(
            '"userId":
            "%s"
                  ',
            self$`userId`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataStoreDescribePermissionsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataStoreDescribePermissionsResponseJson, validateParams = FALSE) {
      DataStoreDescribePermissionsResponseObject <- jsonlite::fromJSON(DataStoreDescribePermissionsResponseJson)
      self$`canCreateDataSource` <- DataStoreDescribePermissionsResponseObject$`canCreateDataSource`
      self$`canDelete` <- DataStoreDescribePermissionsResponseObject$`canDelete`
      self$`canEdit` <- DataStoreDescribePermissionsResponseObject$`canEdit`
      self$`canScanDatabase` <- DataStoreDescribePermissionsResponseObject$`canScanDatabase`
      self$`canSetRoles` <- ApiClient$new()$deserializeObj(DataStoreDescribePermissionsResponseObject$`canSetRoles`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`canShare` <- DataStoreDescribePermissionsResponseObject$`canShare`
      self$`canTestConnection` <- DataStoreDescribePermissionsResponseObject$`canTestConnection`
      self$`canView` <- DataStoreDescribePermissionsResponseObject$`canView`
      self$`dataStoreId` <- DataStoreDescribePermissionsResponseObject$`dataStoreId`
      self$`userId` <- DataStoreDescribePermissionsResponseObject$`userId`
      self$`username` <- DataStoreDescribePermissionsResponseObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
