# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title JDBCFieldSchemas
#'
#' @description JDBCFieldSchemas Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field choices  list( character ) If non-empty, a list of all possible values for this parameter.
#'
#' @field default  character Default value of the JDBC parameter.
#'
#' @field description  character Description of this parameter.
#'
#' @field index  integer [optional] Sort order within one &#x60;kind&#x60;.
#'
#' @field kind  character Use of this parameter in constructing the JDBC URL.
#'
#' @field name  character Name of the JDBC parameter.
#'
#' @field required  character Whether or not the parameter is required for a connection.
#'
#' @field visibleByDefault  character Whether or not the parameter should be shown in the UI by default.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
JDBCFieldSchemas <- R6::R6Class(
  "JDBCFieldSchemas",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(choices = NULL, default = NULL, description = NULL, index = NULL, kind = NULL, name = NULL, required = NULL, visibleByDefault = NULL) {
      if (!is.null(`choices`)) {
        stopifnot(is.vector(`choices`))
      }
      if (!is.null(`default`)) {
        stopifnot(is.character(`default`), length(`default`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`kind`)) {
        stopifnot(is.character(`kind`), length(`kind`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`required`)) {
        stopifnot(is.logical(`required`), length(`required`) == 1)
      }
      if (!is.null(`visibleByDefault`)) {
        stopifnot(is.logical(`visibleByDefault`), length(`visibleByDefault`) == 1)
      }
      if (!is.null(`index`)) {
        stopifnot(is.numeric(`index`), length(`index`) == 1)
      }
    }
  ),
  public = list(
    `choices` = NULL,
    `default` = NULL,
    `description` = NULL,
    `index` = NULL,
    `kind` = NULL,
    `name` = NULL,
    `required` = NULL,
    `visibleByDefault` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param choices If non-empty, a list of all possible values for this parameter.
    #' @param default Default value of the JDBC parameter.
    #' @param description Description of this parameter.
    #' @param index Sort order within one &#x60;kind&#x60;.
    #' @param kind Use of this parameter in constructing the JDBC URL.
    #' @param name Name of the JDBC parameter.
    #' @param required Whether or not the parameter is required for a connection.
    #' @param visibleByDefault Whether or not the parameter should be shown in the UI by default.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`choices` = NULL, `default` = NULL, `description` = NULL, `kind` = NULL, `name` = NULL, `required` = NULL, `visibleByDefault` = NULL, `index` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`choices`, `default`, `description`, `kind`, `name`, `required`, `visibleByDefault`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(choices, default, description, index, kind, name, required, visibleByDefault)
      }
      sapply(`choices`, function(x) stopifnot(is.character(x)))
      self$`default` <- `default`
      self$`description` <- `description`
      self$`index` <- `index`
      self$`kind` <- `kind`
      self$`name` <- `name`
      self$`required` <- `required`
      self$`visibleByDefault` <- `visibleByDefault`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(choices = self$`choices`, default = self$`default`, description = self$`description`, index = self$`index`, kind = self$`kind`, name = self$`name`, required = self$`required`, visibleByDefault = self$`visibleByDefault`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`choices`)) {
          sprintf(
            '"choices":
              [%s]
            ',
            paste(unlist(lapply(self$`choices`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`default`)) {
          sprintf(
            '"default":
            "%s"
                  ',
            self$`default`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`index`)) {
          sprintf(
            '"index":
            %d
                  ',
            self$`index`
          )
        },
        if (!is.null(self$`kind`)) {
          sprintf(
            '"kind":
            "%s"
                  ',
            self$`kind`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`required`)) {
          sprintf(
            '"required":
            %s
                  ',
            tolower(self$`required`)
          )
        },
        if (!is.null(self$`visibleByDefault`)) {
          sprintf(
            '"visibleByDefault":
            %s
                  ',
            tolower(self$`visibleByDefault`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param JDBCFieldSchemasJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(JDBCFieldSchemasJson, validateParams = FALSE) {
      JDBCFieldSchemasObject <- jsonlite::fromJSON(JDBCFieldSchemasJson)
      self$`choices` <- ApiClient$new()$deserializeObj(JDBCFieldSchemasObject$`choices`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`default` <- JDBCFieldSchemasObject$`default`
      self$`description` <- JDBCFieldSchemasObject$`description`
      self$`index` <- JDBCFieldSchemasObject$`index`
      self$`kind` <- JDBCFieldSchemasObject$`kind`
      self$`name` <- JDBCFieldSchemasObject$`name`
      self$`required` <- JDBCFieldSchemasObject$`required`
      self$`visibleByDefault` <- JDBCFieldSchemasObject$`visibleByDefault`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
