# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BatchFeatureTransform
#'
#' @description BatchFeatureTransform Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field parentNames  list( character ) List of feature names that will be transformed into a new variable type.
#'
#' @field prefix  character [optional] The string that will preface all feature names. Optional if suffix is present. (One or both are required.)
#'
#' @field suffix  character [optional] The string that will be appended at the end to all feature names. Optional if prefix is present. (One or both are required.)
#'
#' @field variableType  character The type of the new feature. Must be one of &#x60;text&#x60;, &#x60;categorical&#x60; (Deprecated in version v2.21), &#x60;numeric&#x60;, or &#x60;categoricalInt&#x60;.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BatchFeatureTransform <- R6::R6Class(
  "BatchFeatureTransform",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(parentNames = NULL, prefix = NULL, suffix = NULL, variableType = NULL) {
      if (!is.null(`parentNames`)) {
        stopifnot(is.vector(`parentNames`))
      }
      if (!is.null(`variableType`)) {
        stopifnot(is.character(`variableType`), length(`variableType`) == 1)
      }
      if (!is.null(`prefix`)) {
        stopifnot(is.character(`prefix`), length(`prefix`) == 1)
      }
      if (!is.null(`suffix`)) {
        stopifnot(is.character(`suffix`), length(`suffix`) == 1)
      }
    }
  ),
  public = list(
    `parentNames` = NULL,
    `prefix` = NULL,
    `suffix` = NULL,
    `variableType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param parentNames List of feature names that will be transformed into a new variable type.
    #' @param prefix The string that will preface all feature names. Optional if suffix is present. (One or both are required.)
    #' @param suffix The string that will be appended at the end to all feature names. Optional if prefix is present. (One or both are required.)
    #' @param variableType The type of the new feature. Must be one of &#x60;text&#x60;, &#x60;categorical&#x60; (Deprecated in version v2.21), &#x60;numeric&#x60;, or &#x60;categoricalInt&#x60;.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`parentNames` = NULL, `variableType` = NULL, `prefix` = NULL, `suffix` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`parentNames`, `variableType`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(parentNames, prefix, suffix, variableType)
      }
      sapply(`parentNames`, function(x) stopifnot(is.character(x)))
      self$`prefix` <- `prefix`
      self$`suffix` <- `suffix`
      self$`variableType` <- `variableType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(parentNames = self$`parentNames`, prefix = self$`prefix`, suffix = self$`suffix`, variableType = self$`variableType`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`parentNames`)) {
          sprintf(
            '"parentNames":
              [%s]
            ',
            paste(unlist(lapply(self$`parentNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`prefix`)) {
          sprintf(
            '"prefix":
            "%s"
                  ',
            self$`prefix`
          )
        },
        if (!is.null(self$`suffix`)) {
          sprintf(
            '"suffix":
            "%s"
                  ',
            self$`suffix`
          )
        },
        if (!is.null(self$`variableType`)) {
          sprintf(
            '"variableType":
            "%s"
                  ',
            self$`variableType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BatchFeatureTransformJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BatchFeatureTransformJson, validateParams = FALSE) {
      BatchFeatureTransformObject <- jsonlite::fromJSON(BatchFeatureTransformJson)
      self$`parentNames` <- ApiClient$new()$deserializeObj(BatchFeatureTransformObject$`parentNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`prefix` <- BatchFeatureTransformObject$`prefix`
      self$`suffix` <- BatchFeatureTransformObject$`suffix`
      self$`variableType` <- BatchFeatureTransformObject$`variableType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
