# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PrimeInfoRetrieveResponse
#'
#' @description PrimeInfoRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field canMakePrime  character Indicating whether the requested model is a valid input for creating a Prime mode
#'
#' @field message  character May contain details about why a model is not eligible for DataRobotPrime
#'
#' @field messageId  integer An error code representing the reason the model cannot be approximated with DataRobot Prime; 0 for eligible models
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PrimeInfoRetrieveResponse <- R6::R6Class(
  "PrimeInfoRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`canMakePrime` = NULL, `message` = NULL, `messageId` = NULL) {
      if (!is.null(`canMakePrime`)) {
        stopifnot(is.logical(`canMakePrime`), length(`canMakePrime`) == 1)
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`messageId`)) {
        stopifnot(is.numeric(`messageId`), length(`messageId`) == 1)
      }
    }
  ),
  public = list(
    `canMakePrime` = NULL,
    `message` = NULL,
    `messageId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param canMakePrime Indicating whether the requested model is a valid input for creating a Prime mode
    #' @param message May contain details about why a model is not eligible for DataRobotPrime
    #' @param messageId An error code representing the reason the model cannot be approximated with DataRobot Prime; 0 for eligible models
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`canMakePrime` = NULL, `message` = NULL, `messageId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`canMakePrime`, `message`, `messageId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(canMakePrime, message, messageId)
      }
      self$`canMakePrime` <- `canMakePrime`
      self$`message` <- `message`
      self$`messageId` <- `messageId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(canMakePrime = self$`canMakePrime`, message = self$`message`, messageId = self$`messageId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`canMakePrime`)) {
          sprintf(
            '"canMakePrime":
            %s
                  ',
            tolower(self$`canMakePrime`)
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`messageId`)) {
          sprintf(
            '"messageId":
            %d
                  ',
            self$`messageId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PrimeInfoRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PrimeInfoRetrieveResponseJson, validateParams = FALSE) {
      PrimeInfoRetrieveResponseObject <- jsonlite::fromJSON(PrimeInfoRetrieveResponseJson)
      self$`canMakePrime` <- PrimeInfoRetrieveResponseObject$`canMakePrime`
      self$`message` <- PrimeInfoRetrieveResponseObject$`message`
      self$`messageId` <- PrimeInfoRetrieveResponseObject$`messageId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
