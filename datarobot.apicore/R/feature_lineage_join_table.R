# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureLineageJoinTable
#'
#' @description FeatureLineageJoinTable Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field columns  list( character ) List of columns which datasets were joined by.
#'
#' @field datasteps  list( integer ) List of *data* steps id which brought the *columns* into the current step dataset.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureLineageJoinTable <- R6::R6Class(
  "FeatureLineageJoinTable",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(columns = NULL, datasteps = NULL) {
      if (!is.null(`columns`)) {
        stopifnot(is.vector(`columns`))
      }
      if (!is.null(`datasteps`)) {
        stopifnot(is.vector(`datasteps`))
      }
    }
  ),
  public = list(
    `columns` = NULL,
    `datasteps` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param columns List of columns which datasets were joined by.
    #' @param datasteps List of *data* steps id which brought the *columns* into the current step dataset.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`columns` = NULL, `datasteps` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`columns`, `datasteps`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(columns, datasteps)
      }
      sapply(`columns`, function(x) stopifnot(is.character(x)))
      sapply(`datasteps`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(columns = self$`columns`, datasteps = self$`datasteps`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`columns`)) {
          sprintf(
            '"columns":
              [%s]
            ',
            paste(unlist(lapply(self$`columns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`datasteps`)) {
          sprintf(
            '"datasteps":
              [%s]
            ',
            paste(unlist(lapply(self$`datasteps`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureLineageJoinTableJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureLineageJoinTableJson, validateParams = FALSE) {
      FeatureLineageJoinTableObject <- jsonlite::fromJSON(FeatureLineageJoinTableJson)
      self$`columns` <- ApiClient$new()$deserializeObj(FeatureLineageJoinTableObject$`columns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`datasteps` <- ApiClient$new()$deserializeObj(FeatureLineageJoinTableObject$`datasteps`, "array[integer]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
