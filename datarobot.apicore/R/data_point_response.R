# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataPointResponse
#'
#' @description DataPointResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field prediction  numeric The output of the model for this row.
#'
#' @field shapExplanation  list( \link{ShapExplanationResponse} ) Either &#x60;&#x60;null&#x60;&#x60; or an array of up to 10 &#x60;ShapleyFeatureContribution&#x60; objects. Only rows with the highest anomaly scores have Shapley explanations calculated.
#'
#' @field timestamp  character ISO-formatted timestamp for the row.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataPointResponse <- R6::R6Class(
  "DataPointResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`prediction` = NULL, `shapExplanation` = NULL, `timestamp` = NULL) {
      if (!is.null(`prediction`)) {
      }
      if (!is.null(`shapExplanation`)) {
        stopifnot(is.vector(`shapExplanation`), sapply(`shapExplanation`, R6::is.R6))
      }
      if (!is.null(`timestamp`)) {
        stopifnot(inherits(`timestamp`, "POSIXt"))
      }
    }
  ),
  public = list(
    `prediction` = NULL,
    `shapExplanation` = NULL,
    `timestamp` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param prediction The output of the model for this row.
    #' @param shapExplanation Either &#x60;&#x60;null&#x60;&#x60; or an array of up to 10 &#x60;ShapleyFeatureContribution&#x60; objects. Only rows with the highest anomaly scores have Shapley explanations calculated.
    #' @param timestamp ISO-formatted timestamp for the row.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`prediction` = NULL, `shapExplanation` = NULL, `timestamp` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`prediction`, `shapExplanation`, `timestamp`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(prediction, shapExplanation, timestamp)
      }
      self$`prediction` <- `prediction`
      self$`shapExplanation` <- `shapExplanation`
      self$`timestamp` <- `timestamp`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(prediction = self$`prediction`, shapExplanation = self$`shapExplanation`, timestamp = self$`timestamp`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`prediction`)) {
          sprintf(
            '"prediction":
            %d
                  ',
            self$`prediction`
          )
        },
        if (!is.null(self$`shapExplanation`)) {
          sprintf(
            '"shapExplanation":
            [%s]
      ',
            paste(sapply(self$`shapExplanation`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`timestamp`)) {
          sprintf(
            '"timestamp":
            "%s"
                  ',
            format(self$`timestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataPointResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataPointResponseJson, validateParams = FALSE) {
      DataPointResponseObject <- jsonlite::fromJSON(DataPointResponseJson)
      self$`prediction` <- DataPointResponseObject$`prediction`
      self$`shapExplanation` <- ApiClient$new()$deserializeObj(DataPointResponseObject$`shapExplanation`, "array[ShapExplanationResponse]", loadNamespace("datarobot.apicore"))
      self$`timestamp` <- ParseRFC3339Timestamp(DataPointResponseObject$`timestamp`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
