# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CommentUser
#'
#' @description CommentUser Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field firstName  character First name of the commenter
#'
#' @field id  character User ID of the commenter
#'
#' @field lastName  character Last name of the commenter
#'
#' @field username  character Username of the commenter
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CommentUser <- R6::R6Class(
  "CommentUser",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(firstName = NULL, id = NULL, lastName = NULL, username = NULL) {
      if (!is.null(`firstName`)) {
        stopifnot(is.character(`firstName`), length(`firstName`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`lastName`)) {
        stopifnot(is.character(`lastName`), length(`lastName`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
    }
  ),
  public = list(
    `firstName` = NULL,
    `id` = NULL,
    `lastName` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param firstName First name of the commenter
    #' @param id User ID of the commenter
    #' @param lastName Last name of the commenter
    #' @param username Username of the commenter
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`firstName` = NULL, `id` = NULL, `lastName` = NULL, `username` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`firstName`, `id`, `lastName`, `username`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(firstName, id, lastName, username)
      }
      self$`firstName` <- `firstName`
      self$`id` <- `id`
      self$`lastName` <- `lastName`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(firstName = self$`firstName`, id = self$`id`, lastName = self$`lastName`, username = self$`username`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`firstName`)) {
          sprintf(
            '"firstName":
            "%s"
                  ',
            self$`firstName`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`lastName`)) {
          sprintf(
            '"lastName":
            "%s"
                  ',
            self$`lastName`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CommentUserJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CommentUserJson, validateParams = FALSE) {
      CommentUserObject <- jsonlite::fromJSON(CommentUserJson)
      self$`firstName` <- CommentUserObject$`firstName`
      self$`id` <- CommentUserObject$`id`
      self$`lastName` <- CommentUserObject$`lastName`
      self$`username` <- CommentUserObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
