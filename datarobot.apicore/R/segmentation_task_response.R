# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SegmentationTaskResponse
#'
#' @description SegmentationTaskResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field created  character Date and time when the segmentation task was originally created
#'
#' @field data  \link{SegmentationTaskDataResponse}
#'
#' @field metadata  \link{SegmentationTaskMetadataResponse}
#'
#' @field name  character Name of the segmentation task
#'
#' @field projectId  character Id of the parent project associated with the segmentation task
#'
#' @field segmentationTaskId  character Id of the segmentation task
#'
#' @field segments  list( character ) Names of the unique segments generated by the segmentation task
#'
#' @field segmentsCount  integer Number of segments generated by the segmentation task
#'
#' @field segmentsEda  list( \link{SegmentationEDACompletedResponse} ) Array of segments EDA information.
#'
#' @field type  character Type of segmentation task (e.g. AutoML, AutoTS)
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SegmentationTaskResponse <- R6::R6Class(
  "SegmentationTaskResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`created` = NULL, `data` = NULL, `metadata` = NULL, `name` = NULL, `projectId` = NULL, `segmentationTaskId` = NULL, `segments` = NULL, `segmentsCount` = NULL, `segmentsEda` = NULL, `type` = NULL) {
      if (!is.null(`created`)) {
        stopifnot(inherits(`created`, "POSIXt"))
      }
      if (!is.null(`data`)) {
        stopifnot(R6::is.R6(`data`))
      }
      if (!is.null(`metadata`)) {
        stopifnot(R6::is.R6(`metadata`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`segmentationTaskId`)) {
        stopifnot(is.character(`segmentationTaskId`), length(`segmentationTaskId`) == 1)
      }
      if (!is.null(`segments`)) {
        stopifnot(is.vector(`segments`), sapply(`segments`, is.character))
      }
      if (!is.null(`segmentsCount`)) {
        stopifnot(is.numeric(`segmentsCount`), length(`segmentsCount`) == 1)
      }
      if (!is.null(`segmentsEda`)) {
        stopifnot(is.vector(`segmentsEda`), sapply(`segmentsEda`, R6::is.R6))
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
    }
  ),
  public = list(
    `created` = NULL,
    `data` = NULL,
    `metadata` = NULL,
    `name` = NULL,
    `projectId` = NULL,
    `segmentationTaskId` = NULL,
    `segments` = NULL,
    `segmentsCount` = NULL,
    `segmentsEda` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param created Date and time when the segmentation task was originally created
    #' @param data
    #' @param metadata
    #' @param name Name of the segmentation task
    #' @param projectId Id of the parent project associated with the segmentation task
    #' @param segmentationTaskId Id of the segmentation task
    #' @param segments Names of the unique segments generated by the segmentation task
    #' @param segmentsCount Number of segments generated by the segmentation task
    #' @param segmentsEda Array of segments EDA information.
    #' @param type Type of segmentation task (e.g. AutoML, AutoTS)
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`created` = NULL, `data` = NULL, `metadata` = NULL, `name` = NULL, `projectId` = NULL, `segmentationTaskId` = NULL, `segments` = NULL, `segmentsCount` = NULL, `segmentsEda` = NULL, `type` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`created`, `data`, `metadata`, `name`, `projectId`, `segmentationTaskId`, `segments`, `segmentsCount`, `segmentsEda`, `type`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(created, data, metadata, name, projectId, segmentationTaskId, segments, segmentsCount, segmentsEda, type)
      }
      self$`created` <- `created`
      self$`data` <- `data`
      self$`metadata` <- `metadata`
      self$`name` <- `name`
      self$`projectId` <- `projectId`
      self$`segmentationTaskId` <- `segmentationTaskId`
      self$`segments` <- `segments`
      self$`segmentsCount` <- `segmentsCount`
      self$`segmentsEda` <- `segmentsEda`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(created = self$`created`, data = self$`data`, metadata = self$`metadata`, name = self$`name`, projectId = self$`projectId`, segmentationTaskId = self$`segmentationTaskId`, segments = self$`segments`, segmentsCount = self$`segmentsCount`, segmentsEda = self$`segmentsEda`, type = self$`type`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`created`)) {
          sprintf(
            '"created":
            "%s"
                  ',
            format(self$`created`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            %s
      ',
            jsonlite::toJSON(self$`data`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`metadata`)) {
          sprintf(
            '"metadata":
            %s
      ',
            jsonlite::toJSON(self$`metadata`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`segmentationTaskId`)) {
          sprintf(
            '"segmentationTaskId":
            "%s"
                  ',
            self$`segmentationTaskId`
          )
        },
        if (!is.null(self$`segments`)) {
          sprintf(
            '"segments":
            [%s]
                  ',
            paste(unlist(lapply(self$`segments`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`segmentsCount`)) {
          sprintf(
            '"segmentsCount":
            %d
                  ',
            self$`segmentsCount`
          )
        },
        if (!is.null(self$`segmentsEda`)) {
          sprintf(
            '"segmentsEda":
            [%s]
      ',
            paste(sapply(self$`segmentsEda`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SegmentationTaskResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SegmentationTaskResponseJson, validateParams = FALSE) {
      SegmentationTaskResponseObject <- jsonlite::fromJSON(SegmentationTaskResponseJson)
      self$`created` <- ParseRFC3339Timestamp(SegmentationTaskResponseObject$`created`)
      self$`data` <- SegmentationTaskDataResponse$new()$fromJSON(jsonlite::toJSON(SegmentationTaskResponseObject$data, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`metadata` <- SegmentationTaskMetadataResponse$new()$fromJSON(jsonlite::toJSON(SegmentationTaskResponseObject$metadata, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`name` <- SegmentationTaskResponseObject$`name`
      self$`projectId` <- SegmentationTaskResponseObject$`projectId`
      self$`segmentationTaskId` <- SegmentationTaskResponseObject$`segmentationTaskId`
      self$`segments` <- ApiClient$new()$deserializeObj(SegmentationTaskResponseObject$`segments`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`segmentsCount` <- SegmentationTaskResponseObject$`segmentsCount`
      self$`segmentsEda` <- ApiClient$new()$deserializeObj(SegmentationTaskResponseObject$`segmentsEda`, "array[SegmentationEDACompletedResponse]", loadNamespace("datarobot.apicore"))
      self$`type` <- SegmentationTaskResponseObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
