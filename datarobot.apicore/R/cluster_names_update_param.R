# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ClusterNamesUpdateParam
#'
#' @description ClusterNamesUpdateParam Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field clusterNameMappings  list( \link{ClusterNamesMappingValidation} )              A list of the mappings from a cluster&#39;s current name to its new name.             After update, value passed as a new name will become cluster&#39;s current name.             All cluster names should be unique and should identify one and only one cluster.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ClusterNamesUpdateParam <- R6::R6Class(
  "ClusterNamesUpdateParam",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`clusterNameMappings` = NULL) {
      if (!is.null(`clusterNameMappings`)) {
        stopifnot(is.vector(`clusterNameMappings`), sapply(`clusterNameMappings`, R6::is.R6))
      }
    }
  ),
  public = list(
    `clusterNameMappings` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param clusterNameMappings              A list of the mappings from a cluster&#39;s current name to its new name.             After update, value passed as a new name will become cluster&#39;s current name.             All cluster names should be unique and should identify one and only one cluster.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`clusterNameMappings` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`clusterNameMappings`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(clusterNameMappings)
      }
      self$`clusterNameMappings` <- `clusterNameMappings`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(clusterNameMappings = self$`clusterNameMappings`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`clusterNameMappings`)) {
          sprintf(
            '"clusterNameMappings":
            [%s]
      ',
            paste(sapply(self$`clusterNameMappings`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ClusterNamesUpdateParamJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ClusterNamesUpdateParamJson, validateParams = FALSE) {
      ClusterNamesUpdateParamObject <- jsonlite::fromJSON(ClusterNamesUpdateParamJson)
      self$`clusterNameMappings` <- ApiClient$new()$deserializeObj(ClusterNamesUpdateParamObject$`clusterNameMappings`, "array[ClusterNamesMappingValidation]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
