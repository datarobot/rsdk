# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintTaskCategoryItem
#'
#' @description UserBlueprintTaskCategoryItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field name  character The name of the category.
#'
#' @field subcategories  list( object ) [optional] A list of the available task category items.
#'
#' @field taskCodes  list( character ) A list of task codes representing the tasks in this category.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintTaskCategoryItem <- R6::R6Class(
  "UserBlueprintTaskCategoryItem",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(name = NULL, subcategories = NULL, taskCodes = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`taskCodes`)) {
        stopifnot(is.vector(`taskCodes`))
      }
      if (!is.null(`subcategories`)) {
        stopifnot(is.vector(`subcategories`))
      }
    }
  ),
  public = list(
    `name` = NULL,
    `subcategories` = NULL,
    `taskCodes` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param name The name of the category.
    #' @param subcategories A list of the available task category items.
    #' @param taskCodes A list of task codes representing the tasks in this category.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `taskCodes` = NULL, `subcategories` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`name`, `taskCodes`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(name, subcategories, taskCodes)
      }
      self$`name` <- `name`
      sapply(`subcategories`, function(x) stopifnot(is.character(x)))
      sapply(`taskCodes`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(name = self$`name`, subcategories = self$`subcategories`, taskCodes = self$`taskCodes`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`subcategories`)) {
          sprintf(
            '"subcategories":
              [%s]
            ',
            paste(unlist(lapply(self$`subcategories`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`taskCodes`)) {
          sprintf(
            '"taskCodes":
              [%s]
            ',
            paste(unlist(lapply(self$`taskCodes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintTaskCategoryItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintTaskCategoryItemJson, validateParams = FALSE) {
      UserBlueprintTaskCategoryItemObject <- jsonlite::fromJSON(UserBlueprintTaskCategoryItemJson)
      self$`name` <- UserBlueprintTaskCategoryItemObject$`name`
      self$`subcategories` <- ApiClient$new()$deserializeObj(UserBlueprintTaskCategoryItemObject$`subcategories`, "array[object]", loadNamespace("datarobot.apicore"))
      self$`taskCodes` <- ApiClient$new()$deserializeObj(UserBlueprintTaskCategoryItemObject$`taskCodes`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
