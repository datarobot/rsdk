# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TableauServerPredictionTargetResponse
#'
#' @description TableauServerPredictionTargetResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field contentUrl  character Site name of the Tableau online or Tableau server you wish to use. Required for Tableau online users.
#'
#' @field credentialId  character Id of the stored credentials
#'
#' @field datasourceId  character Id of the DSS data source object
#'
#' @field typeId  character For Tableau should be set to &#x60;tableauServer&#x60;
#'
#' @field url  character Url of the Tableau online or Tableau server instance
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TableauServerPredictionTargetResponse <- R6::R6Class(
  "TableauServerPredictionTargetResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(contentUrl = NULL, credentialId = NULL, datasourceId = NULL, typeId = NULL, url = NULL) {
      if (!is.null(`contentUrl`)) {
        stopifnot(is.character(`contentUrl`), length(`contentUrl`) == 1)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`datasourceId`)) {
        stopifnot(is.character(`datasourceId`), length(`datasourceId`) == 1)
      }
      if (!is.null(`typeId`)) {
        stopifnot(is.character(`typeId`), length(`typeId`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
    }
  ),
  public = list(
    `contentUrl` = NULL,
    `credentialId` = NULL,
    `datasourceId` = NULL,
    `typeId` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param contentUrl Site name of the Tableau online or Tableau server you wish to use. Required for Tableau online users.
    #' @param credentialId Id of the stored credentials
    #' @param datasourceId Id of the DSS data source object
    #' @param typeId For Tableau should be set to &#x60;tableauServer&#x60;
    #' @param url Url of the Tableau online or Tableau server instance
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `contentUrl` = NULL, `credentialId` = NULL, `datasourceId` = NULL, `typeId` = NULL, `url` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`contentUrl`, `credentialId`, `datasourceId`, `typeId`, `url`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(contentUrl, credentialId, datasourceId, typeId, url)
      }
      self$`contentUrl` <- `contentUrl`
      self$`credentialId` <- `credentialId`
      self$`datasourceId` <- `datasourceId`
      self$`typeId` <- `typeId`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(contentUrl = self$`contentUrl`, credentialId = self$`credentialId`, datasourceId = self$`datasourceId`, typeId = self$`typeId`, url = self$`url`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`contentUrl`)) {
        sprintf(
        '"contentUrl":
          "%s"
                ',
        self$`contentUrl`
        )},
        if (!is.null(self$`credentialId`)) {
        sprintf(
        '"credentialId":
          "%s"
                ',
        self$`credentialId`
        )},
        if (!is.null(self$`datasourceId`)) {
        sprintf(
        '"datasourceId":
          "%s"
                ',
        self$`datasourceId`
        )},
        if (!is.null(self$`typeId`)) {
        sprintf(
        '"typeId":
          "%s"
                ',
        self$`typeId`
        )},
        if (!is.null(self$`url`)) {
        sprintf(
        '"url":
          "%s"
                ',
        self$`url`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TableauServerPredictionTargetResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TableauServerPredictionTargetResponseJson, validateParams = FALSE) {
      TableauServerPredictionTargetResponseObject <- jsonlite::fromJSON(TableauServerPredictionTargetResponseJson)
      self$`contentUrl` <- TableauServerPredictionTargetResponseObject$`contentUrl`
      self$`credentialId` <- TableauServerPredictionTargetResponseObject$`credentialId`
      self$`datasourceId` <- TableauServerPredictionTargetResponseObject$`datasourceId`
      self$`typeId` <- TableauServerPredictionTargetResponseObject$`typeId`
      self$`url` <- TableauServerPredictionTargetResponseObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
