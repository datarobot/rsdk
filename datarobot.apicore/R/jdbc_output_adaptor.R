# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title JdbcOutputAdaptor
#'
#' @description JdbcOutputAdaptor Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalog  character [optional] The name of the specified database catalog to write output data to.
#'
#' @field commitInterval  integer [optional] Defines a time interval in seconds between each commit is done to the JDBC source. If set to 0, the batch prediction operation will write the entire job before committing.
#'
#' @field createTableIfNotExists  character [optional] Attempt to create the table first if no existing one is detected, before writing data with the strategy defined in the &#x60;statementType&#x60; parameter.
#'
#' @field credentialId  \link{OneOfstringstring} [optional] Either the populated value of the field or [redacted] due to permission settings
#'
#' @field dataStoreId  \link{OneOfstringstring} Either the populated value of the field or [redacted] due to permission settings
#'
#' @field schema  character [optional] The name of the specified database schema to write the results to.
#'
#' @field statementType  character The statement type to use when writing the results. Deprecation Warning: Use of &#x60;create_table&#x60; is now discouraged. Use one of the other possibilities along with the parameter &#x60;createTableIfNotExists&#x60; set to &#x60;true&#x60;.
#'
#' @field table  character The name of the specified database table to write the results to.If this job is executed with a job definition, then template variables are available which will be substituted for timestamps: {{ current_run_timestamp }}, {{ last_completed_run_time }}, {{ last_scheduled_run_time }}, {{ next_scheduled_run_time }}, {{ current_run_time }}
#'
#' @field type  character Type name for this intake type
#'
#' @field updateColumns  list( character ) [optional] The column names to be updated if statementType is set to either update or upsert.
#'
#' @field whereColumns  list( character ) [optional] The column names to be used in the where clause if statementType is set to update or upsert.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
JdbcOutputAdaptor <- R6::R6Class(
  "JdbcOutputAdaptor",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`catalog` = NULL, `commitInterval` = NULL, `createTableIfNotExists` = NULL, `credentialId` = NULL, `dataStoreId` = NULL, `schema` = NULL, `statementType` = NULL, `table` = NULL, `type` = NULL, `updateColumns` = NULL, `whereColumns` = NULL) {
      if (!is.null(`dataStoreId`)) {
        .setPrimitiveProperty(typeList = list("character"), propertyData = dataStoreId)
      }
      if (!is.null(`statementType`)) {
        stopifnot(is.character(`statementType`), length(`statementType`) == 1)
      }
      if (!is.null(`table`)) {
        stopifnot(is.character(`table`), length(`table`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`catalog`)) {
        stopifnot(is.character(`catalog`), length(`catalog`) == 1)
      }
      if (!is.null(`commitInterval`)) {
        stopifnot(is.numeric(`commitInterval`), length(`commitInterval`) == 1)
      }
      if (!is.null(`createTableIfNotExists`)) {
        stopifnot(is.logical(`createTableIfNotExists`), length(`createTableIfNotExists`) == 1)
      }
      if (!is.null(`credentialId`)) {
        .setPrimitiveProperty(typeList = list("character"), propertyData = credentialId)
      }
      if (!is.null(`schema`)) {
        stopifnot(is.character(`schema`), length(`schema`) == 1)
      }
      if (!is.null(`updateColumns`) && length(`updateColumns`) > 0) {
        stopifnot(is.vector(`updateColumns`), sapply(`updateColumns`, is.character))
      }
      if (!is.null(`whereColumns`) && length(`whereColumns`) > 0) {
        stopifnot(is.vector(`whereColumns`), sapply(`whereColumns`, is.character))
      }
    }
  ),
  public = list(
    `catalog` = NULL,
    `commitInterval` = NULL,
    `createTableIfNotExists` = NULL,
    `credentialId` = NULL,
    `dataStoreId` = NULL,
    `schema` = NULL,
    `statementType` = NULL,
    `table` = NULL,
    `type` = NULL,
    `updateColumns` = NULL,
    `whereColumns` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalog The name of the specified database catalog to write output data to.
    #' @param commitInterval Defines a time interval in seconds between each commit is done to the JDBC source. If set to 0, the batch prediction operation will write the entire job before committing.
    #' @param createTableIfNotExists Attempt to create the table first if no existing one is detected, before writing data with the strategy defined in the &#x60;statementType&#x60; parameter.
    #' @param credentialId Either the populated value of the field or [redacted] due to permission settings
    #' @param dataStoreId Either the populated value of the field or [redacted] due to permission settings
    #' @param schema The name of the specified database schema to write the results to.
    #' @param statementType The statement type to use when writing the results. Deprecation Warning: Use of &#x60;create_table&#x60; is now discouraged. Use one of the other possibilities along with the parameter &#x60;createTableIfNotExists&#x60; set to &#x60;true&#x60;.
    #' @param table The name of the specified database table to write the results to.If this job is executed with a job definition, then template variables are available which will be substituted for timestamps: {{ current_run_timestamp }}, {{ last_completed_run_time }}, {{ last_scheduled_run_time }}, {{ next_scheduled_run_time }}, {{ current_run_time }}
    #' @param type Type name for this intake type
    #' @param updateColumns The column names to be updated if statementType is set to either update or upsert.
    #' @param whereColumns The column names to be used in the where clause if statementType is set to update or upsert.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataStoreId` = NULL, `statementType` = NULL, `table` = NULL, `type` = NULL, `catalog` = NULL, `commitInterval` = 600, `createTableIfNotExists` = FALSE, `credentialId` = NULL, `schema` = NULL, `updateColumns` = NULL, `whereColumns` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`dataStoreId`, `statementType`, `table`, `type`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(catalog, commitInterval, createTableIfNotExists, credentialId, dataStoreId, schema, statementType, table, type, updateColumns, whereColumns)
      }
      self$`catalog` <- `catalog`
      self$`commitInterval` <- `commitInterval`
      self$`createTableIfNotExists` <- `createTableIfNotExists`
      self$`credentialId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = credentialId)
      self$`dataStoreId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = dataStoreId)
      self$`schema` <- `schema`
      self$`statementType` <- `statementType`
      self$`table` <- `table`
      self$`type` <- `type`
      self$`updateColumns` <- `updateColumns`
      self$`whereColumns` <- `whereColumns`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(catalog = self$`catalog`, commitInterval = self$`commitInterval`, createTableIfNotExists = self$`createTableIfNotExists`, credentialId = self$`credentialId`, dataStoreId = self$`dataStoreId`, schema = self$`schema`, statementType = self$`statementType`, table = self$`table`, type = self$`type`, updateColumns = self$`updateColumns`, whereColumns = self$`whereColumns`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalog`)) {
          sprintf(
            '"catalog":
            "%s"
                  ',
            self$`catalog`
          )
        },
        if (!is.null(self$`commitInterval`)) {
          sprintf(
            '"commitInterval":
            %d
                  ',
            self$`commitInterval`
          )
        },
        if (!is.null(self$`createTableIfNotExists`)) {
          sprintf(
            '"createTableIfNotExists":
            %s
                  ',
            tolower(self$`createTableIfNotExists`)
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            %s
      ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`dataStoreId`)) {
          sprintf(
            '"dataStoreId":
            %s
      ',
            self$`dataStoreId`
          )
        },
        if (!is.null(self$`schema`)) {
          sprintf(
            '"schema":
            "%s"
                  ',
            self$`schema`
          )
        },
        if (!is.null(self$`statementType`)) {
          sprintf(
            '"statementType":
            "%s"
                  ',
            self$`statementType`
          )
        },
        if (!is.null(self$`table`)) {
          sprintf(
            '"table":
            "%s"
                  ',
            self$`table`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        },
        if (!is.null(self$`updateColumns`)) {
          sprintf(
            '"updateColumns":
            [%s]
                  ',
            paste(unlist(lapply(self$`updateColumns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`whereColumns`)) {
          sprintf(
            '"whereColumns":
            [%s]
                  ',
            paste(unlist(lapply(self$`whereColumns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param JdbcOutputAdaptorJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(JdbcOutputAdaptorJson, validateParams = FALSE) {
      JdbcOutputAdaptorObject <- jsonlite::fromJSON(JdbcOutputAdaptorJson)
      self$`catalog` <- JdbcOutputAdaptorObject$`catalog`
      self$`commitInterval` <- JdbcOutputAdaptorObject$`commitInterval`
      self$`createTableIfNotExists` <- JdbcOutputAdaptorObject$`createTableIfNotExists`
      self$`credentialId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = JdbcOutputAdaptorObject$credentialId)
      self$`dataStoreId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = JdbcOutputAdaptorObject$dataStoreId)
      self$`schema` <- JdbcOutputAdaptorObject$`schema`
      self$`statementType` <- JdbcOutputAdaptorObject$`statementType`
      self$`table` <- JdbcOutputAdaptorObject$`table`
      self$`type` <- JdbcOutputAdaptorObject$`type`
      self$`updateColumns` <- ApiClient$new()$deserializeObj(JdbcOutputAdaptorObject$`updateColumns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`whereColumns` <- ApiClient$new()$deserializeObj(JdbcOutputAdaptorObject$`whereColumns`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
