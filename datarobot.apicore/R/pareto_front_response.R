# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ParetoFrontResponse
#'
#' @description ParetoFrontResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field errorMetric  character The Eureqa error metric identifier used to compute error metrics for this search. Note that Eureqa error metrics do not correspond 1:1 with DataRobot error metrics - the available metrics are not the same, and even equivalent metrics may be computed slightly differently.
#'
#' @field hyperparameters  \link{OneOfHyperparametersResponse} The hyperparameters used by this run of the Eureqa blueprint.
#'
#' @field projectId  character The project ID of the Eureqa model.
#'
#' @field solutions  list( \link{SolutionResponse} ) The Eureqa model solutions.
#'
#' @field targetType  character The type of the target variable.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ParetoFrontResponse <- R6::R6Class(
  "ParetoFrontResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`errorMetric` = NULL, `hyperparameters` = NULL, `projectId` = NULL, `solutions` = NULL, `targetType` = NULL) {
      if (!is.null(`errorMetric`)) {
        stopifnot(is.character(`errorMetric`), length(`errorMetric`) == 1)
      }
      if (!is.null(`hyperparameters`)) {
        .setComplexProperty(typeList = list(HyperparametersResponse), propertyData = hyperparameters)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`solutions`)) {
        stopifnot(is.vector(`solutions`))
      }
      if (!is.null(`targetType`)) {
        stopifnot(is.character(`targetType`), length(`targetType`) == 1)
      }
    }
  ),
  public = list(
    `errorMetric` = NULL,
    `hyperparameters` = NULL,
    `projectId` = NULL,
    `solutions` = NULL,
    `targetType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param errorMetric The Eureqa error metric identifier used to compute error metrics for this search. Note that Eureqa error metrics do not correspond 1:1 with DataRobot error metrics - the available metrics are not the same, and even equivalent metrics may be computed slightly differently.
    #' @param hyperparameters The hyperparameters used by this run of the Eureqa blueprint.
    #' @param projectId The project ID of the Eureqa model.
    #' @param solutions The Eureqa model solutions.
    #' @param targetType The type of the target variable.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`errorMetric` = NULL, `hyperparameters` = NULL, `projectId` = NULL, `solutions` = NULL, `targetType` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`errorMetric`, `hyperparameters`, `projectId`, `solutions`, `targetType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(errorMetric, hyperparameters, projectId, solutions, targetType)
      }
      self$`errorMetric` <- `errorMetric`
      self$`hyperparameters` <- .setComplexProperty(typeList = list(HyperparametersResponse), propertyData = hyperparameters)
      self$`projectId` <- `projectId`
      sapply(`solutions`, function(x) stopifnot(R6::is.R6(x)))
      self$`targetType` <- `targetType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(errorMetric = self$`errorMetric`, hyperparameters = self$`hyperparameters`, projectId = self$`projectId`, solutions = self$`solutions`, targetType = self$`targetType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`errorMetric`)) {
          sprintf(
            '"errorMetric":
            "%s"
                  ',
            self$`errorMetric`
          )
        },
        if (!is.null(self$`hyperparameters`)) {
          sprintf(
            '"hyperparameters":
            %s
      ',
            jsonlite::toJSON(self$`hyperparameters`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`solutions`)) {
          sprintf(
            '"solutions":
            [%s]
      ',
            paste(sapply(self$`solutions`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`targetType`)) {
          sprintf(
            '"targetType":
            "%s"
                  ',
            self$`targetType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ParetoFrontResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ParetoFrontResponseJson, validateParams = FALSE) {
      ParetoFrontResponseObject <- jsonlite::fromJSON(ParetoFrontResponseJson)
      self$`errorMetric` <- ParetoFrontResponseObject$`errorMetric`
      self$`hyperparameters` <- .setComplexProperty(typeList = list(HyperparametersResponse), propertyData = ParetoFrontResponseObject$hyperparameters)
      self$`projectId` <- ParetoFrontResponseObject$`projectId`
      self$`solutions` <- ApiClient$new()$deserializeObj(ParetoFrontResponseObject$`solutions`, "array[SolutionResponse]", loadNamespace("datarobot.apicore"))
      self$`targetType` <- ParetoFrontResponseObject$`targetType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
