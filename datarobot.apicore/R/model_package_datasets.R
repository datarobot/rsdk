# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelPackageDatasets
#'
#' @description ModelPackageDatasets Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field baselineSegmentedBy  list( character ) Names of categorical features by which the training baseline was segmented. This allows for deployment prediction requests to be segmented by those same features. Segmenting the training baseline by these features allows for users to perform segmented analysis of Data Drift and Accuracy, and to compare the same subset of training and scoring data based on the selected segment attribute and segment value.
#'
#' @field datasetName  character Name of dataset used to train the model
#'
#' @field holdoutDataCatalogId  character ID for holdout data (returned from uploading a data set)
#'
#' @field holdoutDatasetName  character Name of dataset used for model holdout
#'
#' @field targetHistogramBaseline  character [optional] Values used to establish the training baseline
#'
#' @field trainingDataCatalogId  character ID for training data (returned from uploading a data set)
#'
#' @field trainingDataSize  integer [optional] Number of rows in training data (used by DR models)
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelPackageDatasets <- R6::R6Class(
  "ModelPackageDatasets",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`baselineSegmentedBy` = NULL, `datasetName` = NULL, `holdoutDataCatalogId` = NULL, `holdoutDatasetName` = NULL, `targetHistogramBaseline` = NULL, `trainingDataCatalogId` = NULL, `trainingDataSize` = NULL) {
      if (!is.null(`baselineSegmentedBy`)) {
        stopifnot(is.vector(`baselineSegmentedBy`), sapply(`baselineSegmentedBy`, is.character))
      }
      if (!is.null(`datasetName`)) {
        stopifnot(is.character(`datasetName`), length(`datasetName`) == 1)
      }
      if (!is.null(`holdoutDataCatalogId`)) {
        stopifnot(is.character(`holdoutDataCatalogId`), length(`holdoutDataCatalogId`) == 1)
      }
      if (!is.null(`holdoutDatasetName`)) {
        stopifnot(is.character(`holdoutDatasetName`), length(`holdoutDatasetName`) == 1)
      }
      if (!is.null(`trainingDataCatalogId`)) {
        stopifnot(is.character(`trainingDataCatalogId`), length(`trainingDataCatalogId`) == 1)
      }
      if (!is.null(`targetHistogramBaseline`)) {
        stopifnot(is.character(`targetHistogramBaseline`), length(`targetHistogramBaseline`) == 1)
      }
      if (!is.null(`trainingDataSize`)) {
        stopifnot(is.numeric(`trainingDataSize`), length(`trainingDataSize`) == 1)
      }
    }
  ),
  public = list(
    `baselineSegmentedBy` = NULL,
    `datasetName` = NULL,
    `holdoutDataCatalogId` = NULL,
    `holdoutDatasetName` = NULL,
    `targetHistogramBaseline` = NULL,
    `trainingDataCatalogId` = NULL,
    `trainingDataSize` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param baselineSegmentedBy Names of categorical features by which the training baseline was segmented. This allows for deployment prediction requests to be segmented by those same features. Segmenting the training baseline by these features allows for users to perform segmented analysis of Data Drift and Accuracy, and to compare the same subset of training and scoring data based on the selected segment attribute and segment value.
    #' @param datasetName Name of dataset used to train the model
    #' @param holdoutDataCatalogId ID for holdout data (returned from uploading a data set)
    #' @param holdoutDatasetName Name of dataset used for model holdout
    #' @param targetHistogramBaseline Values used to establish the training baseline
    #' @param trainingDataCatalogId ID for training data (returned from uploading a data set)
    #' @param trainingDataSize Number of rows in training data (used by DR models)
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`baselineSegmentedBy` = NULL, `datasetName` = NULL, `holdoutDataCatalogId` = NULL, `holdoutDatasetName` = NULL, `trainingDataCatalogId` = NULL, `targetHistogramBaseline` = NULL, `trainingDataSize` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`baselineSegmentedBy`, `datasetName`, `holdoutDataCatalogId`, `holdoutDatasetName`, `trainingDataCatalogId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(baselineSegmentedBy, datasetName, holdoutDataCatalogId, holdoutDatasetName, targetHistogramBaseline, trainingDataCatalogId, trainingDataSize)
      }
      self$`baselineSegmentedBy` <- `baselineSegmentedBy`
      self$`datasetName` <- `datasetName`
      self$`holdoutDataCatalogId` <- `holdoutDataCatalogId`
      self$`holdoutDatasetName` <- `holdoutDatasetName`
      self$`targetHistogramBaseline` <- `targetHistogramBaseline`
      self$`trainingDataCatalogId` <- `trainingDataCatalogId`
      self$`trainingDataSize` <- `trainingDataSize`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(baselineSegmentedBy = self$`baselineSegmentedBy`, datasetName = self$`datasetName`, holdoutDataCatalogId = self$`holdoutDataCatalogId`, holdoutDatasetName = self$`holdoutDatasetName`, targetHistogramBaseline = self$`targetHistogramBaseline`, trainingDataCatalogId = self$`trainingDataCatalogId`, trainingDataSize = self$`trainingDataSize`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`baselineSegmentedBy`)) {
          sprintf(
            '"baselineSegmentedBy":
            [%s]
                  ',
            paste(unlist(lapply(self$`baselineSegmentedBy`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`datasetName`)) {
          sprintf(
            '"datasetName":
            "%s"
                  ',
            self$`datasetName`
          )
        },
        if (!is.null(self$`holdoutDataCatalogId`)) {
          sprintf(
            '"holdoutDataCatalogId":
            "%s"
                  ',
            self$`holdoutDataCatalogId`
          )
        },
        if (!is.null(self$`holdoutDatasetName`)) {
          sprintf(
            '"holdoutDatasetName":
            "%s"
                  ',
            self$`holdoutDatasetName`
          )
        },
        if (!is.null(self$`targetHistogramBaseline`)) {
          sprintf(
            '"targetHistogramBaseline":
            "%s"
                  ',
            self$`targetHistogramBaseline`
          )
        },
        if (!is.null(self$`trainingDataCatalogId`)) {
          sprintf(
            '"trainingDataCatalogId":
            "%s"
                  ',
            self$`trainingDataCatalogId`
          )
        },
        if (!is.null(self$`trainingDataSize`)) {
          sprintf(
            '"trainingDataSize":
            %d
                  ',
            self$`trainingDataSize`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelPackageDatasetsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelPackageDatasetsJson, validateParams = FALSE) {
      ModelPackageDatasetsObject <- jsonlite::fromJSON(ModelPackageDatasetsJson)
      self$`baselineSegmentedBy` <- ApiClient$new()$deserializeObj(ModelPackageDatasetsObject$`baselineSegmentedBy`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`datasetName` <- ModelPackageDatasetsObject$`datasetName`
      self$`holdoutDataCatalogId` <- ModelPackageDatasetsObject$`holdoutDataCatalogId`
      self$`holdoutDatasetName` <- ModelPackageDatasetsObject$`holdoutDatasetName`
      self$`targetHistogramBaseline` <- ModelPackageDatasetsObject$`targetHistogramBaseline`
      self$`trainingDataCatalogId` <- ModelPackageDatasetsObject$`trainingDataCatalogId`
      self$`trainingDataSize` <- ModelPackageDatasetsObject$`trainingDataSize`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
