# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CreateDriverRequest
#'
#' @description CreateDriverRequest Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field baseNames  list( character ) Original file name(s) of the uploaded JAR file(s). If there are multiple JAR files required for the driver, each of the original file names should be present in the list in the same order as found in &#39;localJarUrls&#39;
#'
#' @field canonicalName  character [optional] User-friendly driver name.
#'
#' @field className  character [optional] Driver class name. For example &#39;com.amazon.redshift.jdbc.Driver&#39;)
#'
#' @field configurationId  character [optional] Driver configuration ID if it was provided during driver upload.
#'
#' @field localJarUrls  list( character ) File path(s) for the driver files.This path is returned in the response as &#39;local_url&#39; by the driverUpload route.If there are multiple JAR files required for the driver, each uploaded JAR must be present in this list. If specified, values will replace any previous settings.
#'
#' @field version  character [optional] Driver version, which is used to construct the canonical name if driver configuration ID was provided during driver upload.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CreateDriverRequest <- R6::R6Class(
  "CreateDriverRequest",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(baseNames = NULL, canonicalName = NULL, className = NULL, configurationId = NULL, localJarUrls = NULL, version = NULL) {
      if (!is.null(`baseNames`)) {
        stopifnot(is.vector(`baseNames`))
      }
      if (!is.null(`localJarUrls`)) {
        stopifnot(is.vector(`localJarUrls`))
      }
      if (!is.null(`canonicalName`)) {
        stopifnot(is.character(`canonicalName`), length(`canonicalName`) == 1)
      }
      if (!is.null(`className`)) {
        stopifnot(is.character(`className`), length(`className`) == 1)
      }
      if (!is.null(`configurationId`)) {
        stopifnot(is.character(`configurationId`), length(`configurationId`) == 1)
      }
      if (!is.null(`version`)) {
        stopifnot(is.character(`version`), length(`version`) == 1)
      }
    }
  ),
  public = list(
    `baseNames` = NULL,
    `canonicalName` = NULL,
    `className` = NULL,
    `configurationId` = NULL,
    `localJarUrls` = NULL,
    `version` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param baseNames Original file name(s) of the uploaded JAR file(s). If there are multiple JAR files required for the driver, each of the original file names should be present in the list in the same order as found in &#39;localJarUrls&#39;
    #' @param canonicalName User-friendly driver name.
    #' @param className Driver class name. For example &#39;com.amazon.redshift.jdbc.Driver&#39;)
    #' @param configurationId Driver configuration ID if it was provided during driver upload.
    #' @param localJarUrls File path(s) for the driver files.This path is returned in the response as &#39;local_url&#39; by the driverUpload route.If there are multiple JAR files required for the driver, each uploaded JAR must be present in this list. If specified, values will replace any previous settings.
    #' @param version Driver version, which is used to construct the canonical name if driver configuration ID was provided during driver upload.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`baseNames` = NULL, `localJarUrls` = NULL, `canonicalName` = NULL, `className` = NULL, `configurationId` = NULL, `version` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`baseNames`, `localJarUrls`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(baseNames, canonicalName, className, configurationId, localJarUrls, version)
      }
      sapply(`baseNames`, function(x) stopifnot(is.character(x)))
      self$`canonicalName` <- `canonicalName`
      self$`className` <- `className`
      self$`configurationId` <- `configurationId`
      sapply(`localJarUrls`, function(x) stopifnot(is.character(x)))
      self$`version` <- `version`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(baseNames = self$`baseNames`, canonicalName = self$`canonicalName`, className = self$`className`, configurationId = self$`configurationId`, localJarUrls = self$`localJarUrls`, version = self$`version`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`baseNames`)) {
          sprintf(
            '"baseNames":
              [%s]
            ',
            paste(unlist(lapply(self$`baseNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`canonicalName`)) {
          sprintf(
            '"canonicalName":
            "%s"
                  ',
            self$`canonicalName`
          )
        },
        if (!is.null(self$`className`)) {
          sprintf(
            '"className":
            "%s"
                  ',
            self$`className`
          )
        },
        if (!is.null(self$`configurationId`)) {
          sprintf(
            '"configurationId":
            "%s"
                  ',
            self$`configurationId`
          )
        },
        if (!is.null(self$`localJarUrls`)) {
          sprintf(
            '"localJarUrls":
              [%s]
            ',
            paste(unlist(lapply(self$`localJarUrls`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`version`)) {
          sprintf(
            '"version":
            "%s"
                  ',
            self$`version`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CreateDriverRequestJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CreateDriverRequestJson, validateParams = FALSE) {
      CreateDriverRequestObject <- jsonlite::fromJSON(CreateDriverRequestJson)
      self$`baseNames` <- ApiClient$new()$deserializeObj(CreateDriverRequestObject$`baseNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`canonicalName` <- CreateDriverRequestObject$`canonicalName`
      self$`className` <- CreateDriverRequestObject$`className`
      self$`configurationId` <- CreateDriverRequestObject$`configurationId`
      self$`localJarUrls` <- ApiClient$new()$deserializeObj(CreateDriverRequestObject$`localJarUrls`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`version` <- CreateDriverRequestObject$`version`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
