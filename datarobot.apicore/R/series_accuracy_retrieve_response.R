# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SeriesAccuracyRetrieveResponse
#'
#' @description SeriesAccuracyRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field data  list( \link{SeriesAccuracyRetrieveDataResponse} ) An array of available multiseries identifiers and column values.
#'
#' @field next_  character [optional] A URL pointing to the next page (if null, there is no next page).
#'
#' @field previous  character [optional] A URL pointing to the previous page (if null, there is no previous page).
#'
#' @field querySeriesCount  integer The total number of series after filtering is applied.
#'
#' @field totalSeriesCount  integer The total number of series in the project dataset.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SeriesAccuracyRetrieveResponse <- R6::R6Class(
  "SeriesAccuracyRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`data` = NULL, `next_` = NULL, `previous` = NULL, `querySeriesCount` = NULL, `totalSeriesCount` = NULL) {
      if (!is.null(`data`)) {
        stopifnot(is.vector(`data`), sapply(`data`, R6::is.R6))
      }
      if (!is.null(`querySeriesCount`)) {
        stopifnot(is.numeric(`querySeriesCount`), length(`querySeriesCount`) == 1)
      }
      if (!is.null(`totalSeriesCount`)) {
        stopifnot(is.numeric(`totalSeriesCount`), length(`totalSeriesCount`) == 1)
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
    }
  ),
  public = list(
    `data` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    `querySeriesCount` = NULL,
    `totalSeriesCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param data An array of available multiseries identifiers and column values.
    #' @param next_ A URL pointing to the next page (if null, there is no next page).
    #' @param previous A URL pointing to the previous page (if null, there is no previous page).
    #' @param querySeriesCount The total number of series after filtering is applied.
    #' @param totalSeriesCount The total number of series in the project dataset.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`data` = NULL, `querySeriesCount` = NULL, `totalSeriesCount` = NULL, `next_` = NULL, `previous` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`data`, `querySeriesCount`, `totalSeriesCount`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(data, next_, previous, querySeriesCount, totalSeriesCount)
      }
      self$`data` <- `data`
      self$`next_` <- `next_`
      self$`previous` <- `previous`
      self$`querySeriesCount` <- `querySeriesCount`
      self$`totalSeriesCount` <- `totalSeriesCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(data = self$`data`, next_ = self$`next_`, previous = self$`previous`, querySeriesCount = self$`querySeriesCount`, totalSeriesCount = self$`totalSeriesCount`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
      ',
            paste(sapply(self$`data`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        },
        if (!is.null(self$`querySeriesCount`)) {
          sprintf(
            '"querySeriesCount":
            %d
                  ',
            self$`querySeriesCount`
          )
        },
        if (!is.null(self$`totalSeriesCount`)) {
          sprintf(
            '"totalSeriesCount":
            %d
                  ',
            self$`totalSeriesCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SeriesAccuracyRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SeriesAccuracyRetrieveResponseJson, validateParams = FALSE) {
      SeriesAccuracyRetrieveResponseObject <- jsonlite::fromJSON(SeriesAccuracyRetrieveResponseJson)
      self$`data` <- ApiClient$new()$deserializeObj(SeriesAccuracyRetrieveResponseObject$`data`, "array[SeriesAccuracyRetrieveDataResponse]", loadNamespace("datarobot.apicore"))
      self$`next_` <- SeriesAccuracyRetrieveResponseObject$`next`
      self$`previous` <- SeriesAccuracyRetrieveResponseObject$`previous`
      self$`querySeriesCount` <- SeriesAccuracyRetrieveResponseObject$`querySeriesCount`
      self$`totalSeriesCount` <- SeriesAccuracyRetrieveResponseObject$`totalSeriesCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
