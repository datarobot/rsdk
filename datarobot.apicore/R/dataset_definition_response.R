# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetDefinitionResponse
#'
#' @description DatasetDefinitionResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalogId  character ID of the catalog item.
#'
#' @field catalogVersionId  character ID of the catalog item version.
#'
#' @field dataSource  \link{DataSource} [optional]
#'
#' @field dataSources  list( \link{DataSource} ) [optional] Data source details for a JDBC dataset
#'
#' @field featureListId  character ID of the feature list. This decides which columns in the dataset are used for feature generation.
#'
#' @field featureLists  list( character ) [optional] List of available feature list ids for the dataset
#'
#' @field identifier  character Short name of the dataset (used directly as part of the generated feature names).
#'
#' @field isDeleted  character [optional] Is this dataset deleted?
#'
#' @field originalIdentifier  character [optional] Original identifier of the dataset if it was updated to resolve name conflicts.
#'
#' @field primaryTemporalKey  character [optional] Name of the column indicating time of record creation.
#'
#' @field snapshotPolicy  character Policy for using dataset snapshots when creating a project or making predictions. Must be one of the following values: &#39;specified&#39;: Use specific snapshot specified by catalogVersionId. &#39;latest&#39;: Use latest snapshot from the same catalog item. &#39;dynamic&#39;: Get data from the source (only applicable for JDBC datasets).
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetDefinitionResponse <- R6::R6Class(
  "DatasetDefinitionResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`catalogId` = NULL, `catalogVersionId` = NULL, `dataSource` = NULL, `dataSources` = NULL, `featureListId` = NULL, `featureLists` = NULL, `identifier` = NULL, `isDeleted` = NULL, `originalIdentifier` = NULL, `primaryTemporalKey` = NULL, `snapshotPolicy` = NULL) {
      if (!is.null(`catalogId`)) {
        stopifnot(is.character(`catalogId`), length(`catalogId`) == 1)
      }
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`featureListId`)) {
        stopifnot(is.character(`featureListId`), length(`featureListId`) == 1)
      }
      if (!is.null(`identifier`)) {
        stopifnot(is.character(`identifier`), length(`identifier`) == 1)
      }
      if (!is.null(`snapshotPolicy`)) {
        stopifnot(is.character(`snapshotPolicy`), length(`snapshotPolicy`) == 1)
      }
      if (!is.null(`dataSource`)) {
        stopifnot(R6::is.R6(`dataSource`))
      }
      if (!is.null(`dataSources`)) {
        stopifnot(is.vector(`dataSources`))
      }
      if (!is.null(`featureLists`)) {
        stopifnot(is.vector(`featureLists`))
      }
      if (!is.null(`isDeleted`)) {
        stopifnot(is.logical(`isDeleted`), length(`isDeleted`) == 1)
      }
      if (!is.null(`originalIdentifier`)) {
        stopifnot(is.character(`originalIdentifier`), length(`originalIdentifier`) == 1)
      }
      if (!is.null(`primaryTemporalKey`)) {
        stopifnot(is.character(`primaryTemporalKey`), length(`primaryTemporalKey`) == 1)
      }
    }
  ),
  public = list(
    `catalogId` = NULL,
    `catalogVersionId` = NULL,
    `dataSource` = NULL,
    `dataSources` = NULL,
    `featureListId` = NULL,
    `featureLists` = NULL,
    `identifier` = NULL,
    `isDeleted` = NULL,
    `originalIdentifier` = NULL,
    `primaryTemporalKey` = NULL,
    `snapshotPolicy` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalogId ID of the catalog item.
    #' @param catalogVersionId ID of the catalog item version.
    #' @param dataSource
    #' @param dataSources Data source details for a JDBC dataset
    #' @param featureListId ID of the feature list. This decides which columns in the dataset are used for feature generation.
    #' @param featureLists List of available feature list ids for the dataset
    #' @param identifier Short name of the dataset (used directly as part of the generated feature names).
    #' @param isDeleted Is this dataset deleted?
    #' @param originalIdentifier Original identifier of the dataset if it was updated to resolve name conflicts.
    #' @param primaryTemporalKey Name of the column indicating time of record creation.
    #' @param snapshotPolicy Policy for using dataset snapshots when creating a project or making predictions. Must be one of the following values: &#39;specified&#39;: Use specific snapshot specified by catalogVersionId. &#39;latest&#39;: Use latest snapshot from the same catalog item. &#39;dynamic&#39;: Get data from the source (only applicable for JDBC datasets).
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`catalogId` = NULL, `catalogVersionId` = NULL, `featureListId` = NULL, `identifier` = NULL, `snapshotPolicy` = NULL, `dataSource` = NULL, `dataSources` = NULL, `featureLists` = NULL, `isDeleted` = NULL, `originalIdentifier` = NULL, `primaryTemporalKey` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`catalogId`, `catalogVersionId`, `featureListId`, `identifier`, `snapshotPolicy`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(catalogId, catalogVersionId, dataSource, dataSources, featureListId, featureLists, identifier, isDeleted, originalIdentifier, primaryTemporalKey, snapshotPolicy)
      }
      self$`catalogId` <- `catalogId`
      self$`catalogVersionId` <- `catalogVersionId`
      self$`dataSource` <- `dataSource`
      sapply(`dataSources`, function(x) stopifnot(R6::is.R6(x)))
      self$`featureListId` <- `featureListId`
      sapply(`featureLists`, function(x) stopifnot(is.character(x)))
      self$`identifier` <- `identifier`
      self$`isDeleted` <- `isDeleted`
      self$`originalIdentifier` <- `originalIdentifier`
      self$`primaryTemporalKey` <- `primaryTemporalKey`
      self$`snapshotPolicy` <- `snapshotPolicy`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(catalogId = self$`catalogId`, catalogVersionId = self$`catalogVersionId`, dataSource = self$`dataSource`, dataSources = self$`dataSources`, featureListId = self$`featureListId`, featureLists = self$`featureLists`, identifier = self$`identifier`, isDeleted = self$`isDeleted`, originalIdentifier = self$`originalIdentifier`, primaryTemporalKey = self$`primaryTemporalKey`, snapshotPolicy = self$`snapshotPolicy`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalogId`)) {
          sprintf(
            '"catalogId":
            "%s"
                  ',
            self$`catalogId`
          )
        },
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`dataSource`)) {
          sprintf(
            '"dataSource":
            %s
      ',
            jsonlite::toJSON(self$`dataSource`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`dataSources`)) {
          sprintf(
            '"dataSources":
            [%s]
      ',
            paste(sapply(self$`dataSources`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`featureListId`)) {
          sprintf(
            '"featureListId":
            "%s"
                  ',
            self$`featureListId`
          )
        },
        if (!is.null(self$`featureLists`)) {
          sprintf(
            '"featureLists":
            [%s]
                  ',
            paste(unlist(lapply(self$`featureLists`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`identifier`)) {
          sprintf(
            '"identifier":
            "%s"
                  ',
            self$`identifier`
          )
        },
        if (!is.null(self$`isDeleted`)) {
          sprintf(
            '"isDeleted":
            %s
                  ',
            tolower(self$`isDeleted`)
          )
        },
        if (!is.null(self$`originalIdentifier`)) {
          sprintf(
            '"originalIdentifier":
            "%s"
                  ',
            self$`originalIdentifier`
          )
        },
        if (!is.null(self$`primaryTemporalKey`)) {
          sprintf(
            '"primaryTemporalKey":
            "%s"
                  ',
            self$`primaryTemporalKey`
          )
        },
        if (!is.null(self$`snapshotPolicy`)) {
          sprintf(
            '"snapshotPolicy":
            "%s"
                  ',
            self$`snapshotPolicy`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetDefinitionResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetDefinitionResponseJson, validateParams = FALSE) {
      DatasetDefinitionResponseObject <- jsonlite::fromJSON(DatasetDefinitionResponseJson)
      self$`catalogId` <- DatasetDefinitionResponseObject$`catalogId`
      self$`catalogVersionId` <- DatasetDefinitionResponseObject$`catalogVersionId`
      self$`dataSource` <- DataSource$new()$fromJSON(jsonlite::toJSON(DatasetDefinitionResponseObject$dataSource, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`dataSources` <- ApiClient$new()$deserializeObj(DatasetDefinitionResponseObject$`dataSources`, "array[DataSource]", loadNamespace("datarobot.apicore"))
      self$`featureListId` <- DatasetDefinitionResponseObject$`featureListId`
      self$`featureLists` <- ApiClient$new()$deserializeObj(DatasetDefinitionResponseObject$`featureLists`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`identifier` <- DatasetDefinitionResponseObject$`identifier`
      self$`isDeleted` <- DatasetDefinitionResponseObject$`isDeleted`
      self$`originalIdentifier` <- DatasetDefinitionResponseObject$`originalIdentifier`
      self$`primaryTemporalKey` <- DatasetDefinitionResponseObject$`primaryTemporalKey`
      self$`snapshotPolicy` <- DatasetDefinitionResponseObject$`snapshotPolicy`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
