# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PrimeFileResponse
#'
#' @description PrimeFileResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field id  character the ID of the file
#'
#' @field isValid  character whether the code passed basic validation checks
#'
#' @field language  character the language the code is written in (e.g. Python
#'
#' @field modelId  character the Id of the Prime model
#'
#' @field parentModelId  character the ID of the model this code approximates
#'
#' @field projectId  character the ID of the project the file belongs to
#'
#' @field rulesetId  integer the ID of the ruleset this code uses to approximate the parent model
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PrimeFileResponse <- R6::R6Class(
  "PrimeFileResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(id = NULL, isValid = NULL, language = NULL, modelId = NULL, parentModelId = NULL, projectId = NULL, rulesetId = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`isValid`)) {
        stopifnot(is.logical(`isValid`), length(`isValid`) == 1)
      }
      if (!is.null(`language`)) {
        stopifnot(is.character(`language`), length(`language`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`parentModelId`)) {
        stopifnot(is.character(`parentModelId`), length(`parentModelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`rulesetId`)) {
        stopifnot(is.numeric(`rulesetId`), length(`rulesetId`) == 1)
      }
    }
  ),
  public = list(
    `id` = NULL,
    `isValid` = NULL,
    `language` = NULL,
    `modelId` = NULL,
    `parentModelId` = NULL,
    `projectId` = NULL,
    `rulesetId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param id the ID of the file
    #' @param isValid whether the code passed basic validation checks
    #' @param language the language the code is written in (e.g. Python
    #' @param modelId the Id of the Prime model
    #' @param parentModelId the ID of the model this code approximates
    #' @param projectId the ID of the project the file belongs to
    #' @param rulesetId the ID of the ruleset this code uses to approximate the parent model
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `isValid` = NULL, `language` = NULL, `modelId` = NULL, `parentModelId` = NULL, `projectId` = NULL, `rulesetId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`id`, `isValid`, `language`, `modelId`, `parentModelId`, `projectId`, `rulesetId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(id, isValid, language, modelId, parentModelId, projectId, rulesetId)
      }
      self$`id` <- `id`
      self$`isValid` <- `isValid`
      self$`language` <- `language`
      self$`modelId` <- `modelId`
      self$`parentModelId` <- `parentModelId`
      self$`projectId` <- `projectId`
      self$`rulesetId` <- `rulesetId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(id = self$`id`, isValid = self$`isValid`, language = self$`language`, modelId = self$`modelId`, parentModelId = self$`parentModelId`, projectId = self$`projectId`, rulesetId = self$`rulesetId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`isValid`)) {
          sprintf(
            '"isValid":
            %s
                  ',
            tolower(self$`isValid`)
          )
        },
        if (!is.null(self$`language`)) {
          sprintf(
            '"language":
            "%s"
                  ',
            self$`language`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`parentModelId`)) {
          sprintf(
            '"parentModelId":
            "%s"
                  ',
            self$`parentModelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`rulesetId`)) {
          sprintf(
            '"rulesetId":
            %d
                  ',
            self$`rulesetId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PrimeFileResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PrimeFileResponseJson, validateParams = FALSE) {
      PrimeFileResponseObject <- jsonlite::fromJSON(PrimeFileResponseJson)
      self$`id` <- PrimeFileResponseObject$`id`
      self$`isValid` <- PrimeFileResponseObject$`isValid`
      self$`language` <- PrimeFileResponseObject$`language`
      self$`modelId` <- PrimeFileResponseObject$`modelId`
      self$`parentModelId` <- PrimeFileResponseObject$`parentModelId`
      self$`projectId` <- PrimeFileResponseObject$`projectId`
      self$`rulesetId` <- PrimeFileResponseObject$`rulesetId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
