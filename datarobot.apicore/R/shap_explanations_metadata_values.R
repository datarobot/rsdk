# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ShapExplanationsMetadataValues
#'
#' @description ShapExplanationsMetadataValues Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field baseValue  numeric Will be present only if&#x60;explanationAlgorithm&#x60; &#x3D; \&quot;shap\&quot;. The model&#39;s average prediction over the training data. SHAP values are deviations from the base value.
#'
#' @field remainingTotal  numeric Will be present only if &#x60;explanationAlgorithm&#x60; &#x3D; \&quot;shap\&quot; and &#x60;maxExplanations&#x60; is nonzero. The total of SHAP values for features beyond the &#x60;maxExplanations&#x60;. This can be identically 0 in all rows, if &#x60;maxExplanations&#x60; is greater than the number of features and thus all features are returned.
#'
#' @field warnings  list( \link{ShapExplanationsWarningsValues} ) Will be present if &#x60;explanationAlgorithm&#x60; was set to \&quot;shap\&quot; and there were additivity failures during SHAP values calculation.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShapExplanationsMetadataValues <- R6::R6Class(
  "ShapExplanationsMetadataValues",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`baseValue` = NULL, `remainingTotal` = NULL, `warnings` = NULL) {
      if (!is.null(`baseValue`)) {
      }
      if (!is.null(`remainingTotal`)) {
      }
      if (!is.null(`warnings`)) {
        stopifnot(is.vector(`warnings`))
      }
    }
  ),
  public = list(
    `baseValue` = NULL,
    `remainingTotal` = NULL,
    `warnings` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param baseValue Will be present only if&#x60;explanationAlgorithm&#x60; &#x3D; \&quot;shap\&quot;. The model&#39;s average prediction over the training data. SHAP values are deviations from the base value.
    #' @param remainingTotal Will be present only if &#x60;explanationAlgorithm&#x60; &#x3D; \&quot;shap\&quot; and &#x60;maxExplanations&#x60; is nonzero. The total of SHAP values for features beyond the &#x60;maxExplanations&#x60;. This can be identically 0 in all rows, if &#x60;maxExplanations&#x60; is greater than the number of features and thus all features are returned.
    #' @param warnings Will be present if &#x60;explanationAlgorithm&#x60; was set to \&quot;shap\&quot; and there were additivity failures during SHAP values calculation.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`baseValue` = NULL, `remainingTotal` = NULL, `warnings` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`baseValue`, `remainingTotal`, `warnings`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(baseValue, remainingTotal, warnings)
      }
      self$`baseValue` <- `baseValue`
      self$`remainingTotal` <- `remainingTotal`
      sapply(`warnings`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(baseValue = self$`baseValue`, remainingTotal = self$`remainingTotal`, warnings = self$`warnings`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`baseValue`)) {
          sprintf(
            '"baseValue":
            %d
                  ',
            self$`baseValue`
          )
        },
        if (!is.null(self$`remainingTotal`)) {
          sprintf(
            '"remainingTotal":
            %d
                  ',
            self$`remainingTotal`
          )
        },
        if (!is.null(self$`warnings`)) {
          sprintf(
            '"warnings":
            [%s]
      ',
            paste(sapply(self$`warnings`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ShapExplanationsMetadataValuesJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ShapExplanationsMetadataValuesJson, validateParams = FALSE) {
      ShapExplanationsMetadataValuesObject <- jsonlite::fromJSON(ShapExplanationsMetadataValuesJson)
      self$`baseValue` <- ShapExplanationsMetadataValuesObject$`baseValue`
      self$`remainingTotal` <- ShapExplanationsMetadataValuesObject$`remainingTotal`
      self$`warnings` <- ApiClient$new()$deserializeObj(ShapExplanationsMetadataValuesObject$`warnings`, "array[ShapExplanationsWarningsValues]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
