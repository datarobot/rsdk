# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsUpdate
#'
#' @description CredentialsUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field awsAccessKeyId  character [optional] AWS access key ID (applicable for credentialType &#x60;s3&#x60; only).
#'
#' @field awsSecretAccessKey  character [optional] The AWS secret access key (applicable for credentialType &#x60;s3&#x60; only).
#'
#' @field awsSessionToken  character [optional] The AWS session token (applicable for credentialType &#x60;s3&#x60; only).
#'
#' @field azureConnectionString  character [optional] Azure connection string (applicable for credentialType &#x60;azure&#x60; only).
#'
#' @field clientId  character [optional] Snowflake OAUTH client ID (applicable for credentialType &#x60;snowflake_oauth_user_account&#x60; only).
#'
#' @field clientSecret  character [optional] Snowflake OAUTH client secret (applicable for credentialType &#x60;snowflake_oauth_user_account&#x60; only).
#'
#' @field description  character [optional] Description of credentials. If omitted, and name is not omitted, clears any previous description for that name.
#'
#' @field gcpKey  \link{GCPKey} [optional]
#'
#' @field name  character [optional] Name of credentials.
#'
#' @field oauthIssuerType  character [optional] Snowflake IDP issuer type (applicable for credentialType &#x60;snowflake_oauth_user_account&#x60; only).
#'
#' @field oauthIssuerUrl  character [optional] Snowflake External IDP issuer URL (applicable for Snowflake External OAUTH connections only).
#'
#' @field oauthScopes  list( character ) [optional] Snowflake External OAUTH scopes (applicable for Snowflake External OAUTH connections only).
#'
#' @field password  character [optional] Password to update for this set of credentials (applicable for credentialType &#x60;basic&#x60; only).
#'
#' @field refreshToken  character [optional] OAUTH refresh token (applicable for credentialType &#x60;oauth&#x60; only).
#'
#' @field snowflakeAccountName  character [optional] Snowflake account name (applicable for &#x60;snowflake_oauth_user_account&#x60; only).
#'
#' @field token  character [optional] OAUTH token (applicable for credentialType &#x60;oauth&#x60; only).
#'
#' @field user  character [optional] Username to update for this set of credentials (applicable for credentialType &#x60;basic&#x60; only).
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsUpdate <- R6::R6Class(
  "CredentialsUpdate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(awsAccessKeyId = NULL, awsSecretAccessKey = NULL, awsSessionToken = NULL, azureConnectionString = NULL, clientId = NULL, clientSecret = NULL, description = NULL, gcpKey = NULL, name = NULL, oauthIssuerType = NULL, oauthIssuerUrl = NULL, oauthScopes = NULL, password = NULL, refreshToken = NULL, snowflakeAccountName = NULL, token = NULL, user = NULL) {
      if (!is.null(`awsAccessKeyId`)) {
        stopifnot(is.character(`awsAccessKeyId`), length(`awsAccessKeyId`) == 1)
      }
      if (!is.null(`awsSecretAccessKey`)) {
        stopifnot(is.character(`awsSecretAccessKey`), length(`awsSecretAccessKey`) == 1)
      }
      if (!is.null(`awsSessionToken`)) {
        stopifnot(is.character(`awsSessionToken`), length(`awsSessionToken`) == 1)
      }
      if (!is.null(`azureConnectionString`)) {
        stopifnot(is.character(`azureConnectionString`), length(`azureConnectionString`) == 1)
      }
      if (!is.null(`clientId`)) {
        stopifnot(is.character(`clientId`), length(`clientId`) == 1)
      }
      if (!is.null(`clientSecret`)) {
        stopifnot(is.character(`clientSecret`), length(`clientSecret`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`gcpKey`)) {
        stopifnot(R6::is.R6(`gcpKey`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`oauthIssuerType`)) {
        stopifnot(is.character(`oauthIssuerType`), length(`oauthIssuerType`) == 1)
      }
      if (!is.null(`oauthIssuerUrl`)) {
        stopifnot(is.character(`oauthIssuerUrl`), length(`oauthIssuerUrl`) == 1)
      }
      if (!is.null(`oauthScopes`)) {
        stopifnot(is.vector(`oauthScopes`))
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`refreshToken`)) {
        stopifnot(is.character(`refreshToken`), length(`refreshToken`) == 1)
      }
      if (!is.null(`snowflakeAccountName`)) {
        stopifnot(is.character(`snowflakeAccountName`), length(`snowflakeAccountName`) == 1)
      }
      if (!is.null(`token`)) {
        stopifnot(is.character(`token`), length(`token`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `awsAccessKeyId` = NULL,
    `awsSecretAccessKey` = NULL,
    `awsSessionToken` = NULL,
    `azureConnectionString` = NULL,
    `clientId` = NULL,
    `clientSecret` = NULL,
    `description` = NULL,
    `gcpKey` = NULL,
    `name` = NULL,
    `oauthIssuerType` = NULL,
    `oauthIssuerUrl` = NULL,
    `oauthScopes` = NULL,
    `password` = NULL,
    `refreshToken` = NULL,
    `snowflakeAccountName` = NULL,
    `token` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param awsAccessKeyId AWS access key ID (applicable for credentialType &#x60;s3&#x60; only).
    #' @param awsSecretAccessKey The AWS secret access key (applicable for credentialType &#x60;s3&#x60; only).
    #' @param awsSessionToken The AWS session token (applicable for credentialType &#x60;s3&#x60; only).
    #' @param azureConnectionString Azure connection string (applicable for credentialType &#x60;azure&#x60; only).
    #' @param clientId Snowflake OAUTH client ID (applicable for credentialType &#x60;snowflake_oauth_user_account&#x60; only).
    #' @param clientSecret Snowflake OAUTH client secret (applicable for credentialType &#x60;snowflake_oauth_user_account&#x60; only).
    #' @param description Description of credentials. If omitted, and name is not omitted, clears any previous description for that name.
    #' @param gcpKey
    #' @param name Name of credentials.
    #' @param oauthIssuerType Snowflake IDP issuer type (applicable for credentialType &#x60;snowflake_oauth_user_account&#x60; only).
    #' @param oauthIssuerUrl Snowflake External IDP issuer URL (applicable for Snowflake External OAUTH connections only).
    #' @param oauthScopes Snowflake External OAUTH scopes (applicable for Snowflake External OAUTH connections only).
    #' @param password Password to update for this set of credentials (applicable for credentialType &#x60;basic&#x60; only).
    #' @param refreshToken OAUTH refresh token (applicable for credentialType &#x60;oauth&#x60; only).
    #' @param snowflakeAccountName Snowflake account name (applicable for &#x60;snowflake_oauth_user_account&#x60; only).
    #' @param token OAUTH token (applicable for credentialType &#x60;oauth&#x60; only).
    #' @param user Username to update for this set of credentials (applicable for credentialType &#x60;basic&#x60; only).
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`awsAccessKeyId` = NULL, `awsSecretAccessKey` = NULL, `awsSessionToken` = NULL, `azureConnectionString` = NULL, `clientId` = NULL, `clientSecret` = NULL, `description` = NULL, `gcpKey` = NULL, `name` = NULL, `oauthIssuerType` = NULL, `oauthIssuerUrl` = NULL, `oauthScopes` = NULL, `password` = NULL, `refreshToken` = NULL, `snowflakeAccountName` = NULL, `token` = NULL, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(awsAccessKeyId, awsSecretAccessKey, awsSessionToken, azureConnectionString, clientId, clientSecret, description, gcpKey, name, oauthIssuerType, oauthIssuerUrl, oauthScopes, password, refreshToken, snowflakeAccountName, token, user)
      }
      self$`awsAccessKeyId` <- `awsAccessKeyId`
      self$`awsSecretAccessKey` <- `awsSecretAccessKey`
      self$`awsSessionToken` <- `awsSessionToken`
      self$`azureConnectionString` <- `azureConnectionString`
      self$`clientId` <- `clientId`
      self$`clientSecret` <- `clientSecret`
      self$`description` <- `description`
      self$`gcpKey` <- `gcpKey`
      self$`name` <- `name`
      self$`oauthIssuerType` <- `oauthIssuerType`
      self$`oauthIssuerUrl` <- `oauthIssuerUrl`
      sapply(`oauthScopes`, function(x) stopifnot(is.character(x)))
      self$`password` <- `password`
      self$`refreshToken` <- `refreshToken`
      self$`snowflakeAccountName` <- `snowflakeAccountName`
      self$`token` <- `token`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(awsAccessKeyId = self$`awsAccessKeyId`, awsSecretAccessKey = self$`awsSecretAccessKey`, awsSessionToken = self$`awsSessionToken`, azureConnectionString = self$`azureConnectionString`, clientId = self$`clientId`, clientSecret = self$`clientSecret`, description = self$`description`, gcpKey = self$`gcpKey`, name = self$`name`, oauthIssuerType = self$`oauthIssuerType`, oauthIssuerUrl = self$`oauthIssuerUrl`, oauthScopes = self$`oauthScopes`, password = self$`password`, refreshToken = self$`refreshToken`, snowflakeAccountName = self$`snowflakeAccountName`, token = self$`token`, user = self$`user`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`awsAccessKeyId`)) {
          sprintf(
            '"awsAccessKeyId":
            "%s"
                  ',
            self$`awsAccessKeyId`
          )
        },
        if (!is.null(self$`awsSecretAccessKey`)) {
          sprintf(
            '"awsSecretAccessKey":
            "%s"
                  ',
            self$`awsSecretAccessKey`
          )
        },
        if (!is.null(self$`awsSessionToken`)) {
          sprintf(
            '"awsSessionToken":
            "%s"
                  ',
            self$`awsSessionToken`
          )
        },
        if (!is.null(self$`azureConnectionString`)) {
          sprintf(
            '"azureConnectionString":
            "%s"
                  ',
            self$`azureConnectionString`
          )
        },
        if (!is.null(self$`clientId`)) {
          sprintf(
            '"clientId":
            "%s"
                  ',
            self$`clientId`
          )
        },
        if (!is.null(self$`clientSecret`)) {
          sprintf(
            '"clientSecret":
            "%s"
                  ',
            self$`clientSecret`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`gcpKey`)) {
          sprintf(
            '"gcpKey":
          %s
        ',
            jsonlite::toJSON(self$`gcpKey`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`oauthIssuerType`)) {
          sprintf(
            '"oauthIssuerType":
            "%s"
                  ',
            self$`oauthIssuerType`
          )
        },
        if (!is.null(self$`oauthIssuerUrl`)) {
          sprintf(
            '"oauthIssuerUrl":
            "%s"
                  ',
            self$`oauthIssuerUrl`
          )
        },
        if (!is.null(self$`oauthScopes`)) {
          sprintf(
            '"oauthScopes":
              [%s]
            ',
            paste(unlist(lapply(self$`oauthScopes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`refreshToken`)) {
          sprintf(
            '"refreshToken":
            "%s"
                  ',
            self$`refreshToken`
          )
        },
        if (!is.null(self$`snowflakeAccountName`)) {
          sprintf(
            '"snowflakeAccountName":
            "%s"
                  ',
            self$`snowflakeAccountName`
          )
        },
        if (!is.null(self$`token`)) {
          sprintf(
            '"token":
            "%s"
                  ',
            self$`token`
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsUpdateJson, validateParams = FALSE) {
      CredentialsUpdateObject <- jsonlite::fromJSON(CredentialsUpdateJson)
      self$`awsAccessKeyId` <- CredentialsUpdateObject$`awsAccessKeyId`
      self$`awsSecretAccessKey` <- CredentialsUpdateObject$`awsSecretAccessKey`
      self$`awsSessionToken` <- CredentialsUpdateObject$`awsSessionToken`
      self$`azureConnectionString` <- CredentialsUpdateObject$`azureConnectionString`
      self$`clientId` <- CredentialsUpdateObject$`clientId`
      self$`clientSecret` <- CredentialsUpdateObject$`clientSecret`
      self$`description` <- CredentialsUpdateObject$`description`
      self$`gcpKey` <- GCPKey$new()$fromJSON(jsonlite::toJSON(CredentialsUpdateObject$gcpKey, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`name` <- CredentialsUpdateObject$`name`
      self$`oauthIssuerType` <- CredentialsUpdateObject$`oauthIssuerType`
      self$`oauthIssuerUrl` <- CredentialsUpdateObject$`oauthIssuerUrl`
      self$`oauthScopes` <- ApiClient$new()$deserializeObj(CredentialsUpdateObject$`oauthScopes`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`password` <- CredentialsUpdateObject$`password`
      self$`refreshToken` <- CredentialsUpdateObject$`refreshToken`
      self$`snowflakeAccountName` <- CredentialsUpdateObject$`snowflakeAccountName`
      self$`token` <- CredentialsUpdateObject$`token`
      self$`user` <- CredentialsUpdateObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
