# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MultiseriesNamesControllerResponse
#'
#' @description MultiseriesNamesControllerResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer Total number of series items in the response
#'
#' @field data  \link{MultiseriesNamesControllerDataRecord}
#'
#' @field next_  character A URL pointing to the next page (if &#x60;null&#x60;, there is no next page).
#'
#' @field previous  character A URL pointing to the previous page (if &#x60;null&#x60;, there is no previous page).
#'
#' @field totalSeriesCount  integer Total number of series items
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MultiseriesNamesControllerResponse <- R6::R6Class(
  "MultiseriesNamesControllerResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(count = NULL, data = NULL, next_ = NULL, previous = NULL, totalSeriesCount = NULL) {
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`data`)) {
        stopifnot(R6::is.R6(`data`))
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
      if (!is.null(`totalSeriesCount`)) {
        stopifnot(is.numeric(`totalSeriesCount`), length(`totalSeriesCount`) == 1)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `data` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    `totalSeriesCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count Total number of series items in the response
    #' @param data
    #' @param next_ A URL pointing to the next page (if &#x60;null&#x60;, there is no next page).
    #' @param previous A URL pointing to the previous page (if &#x60;null&#x60;, there is no previous page).
    #' @param totalSeriesCount Total number of series items
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `data` = NULL, `next_` = NULL, `previous` = NULL, `totalSeriesCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`count`, `data`, `next_`, `previous`, `totalSeriesCount`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(count, data, next_, previous, totalSeriesCount)
      }
      self$`count` <- `count`
      self$`data` <- `data`
      self$`next_` <- `next_`
      self$`previous` <- `previous`
      self$`totalSeriesCount` <- `totalSeriesCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(count = self$`count`, data = self$`data`, next_ = self$`next_`, previous = self$`previous`, totalSeriesCount = self$`totalSeriesCount`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
          %s
        ',
            jsonlite::toJSON(self$`data`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        },
        if (!is.null(self$`totalSeriesCount`)) {
          sprintf(
            '"totalSeriesCount":
            %d
                  ',
            self$`totalSeriesCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MultiseriesNamesControllerResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MultiseriesNamesControllerResponseJson, validateParams = FALSE) {
      MultiseriesNamesControllerResponseObject <- jsonlite::fromJSON(MultiseriesNamesControllerResponseJson)
      self$`count` <- MultiseriesNamesControllerResponseObject$`count`
      self$`data` <- MultiseriesNamesControllerDataRecord$new()$fromJSON(jsonlite::toJSON(MultiseriesNamesControllerResponseObject$data, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`next_` <- MultiseriesNamesControllerResponseObject$`next`
      self$`previous` <- MultiseriesNamesControllerResponseObject$`previous`
      self$`totalSeriesCount` <- MultiseriesNamesControllerResponseObject$`totalSeriesCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
