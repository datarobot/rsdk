# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MonetaryValue
#'
#' @description MonetaryValue Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field currency  character ISO code of currency
#'
#' @field details  character [optional] Optional user notes
#'
#' @field value  numeric Amount of value
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MonetaryValue <- R6::R6Class(
  "MonetaryValue",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`currency` = NULL, `details` = NULL, `value` = NULL) {
      if (!is.null(`currency`)) {
        stopifnot(is.character(`currency`), length(`currency`) == 1)
      }
      if (!is.null(`value`)) {
      }
      if (!is.null(`details`)) {
        stopifnot(is.character(`details`), length(`details`) == 1)
      }
    }
  ),
  public = list(
    `currency` = NULL,
    `details` = NULL,
    `value` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param currency ISO code of currency
    #' @param details Optional user notes
    #' @param value Amount of value
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`currency` = NULL, `value` = NULL, `details` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`currency`, `value`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(currency, details, value)
      }
      self$`currency` <- `currency`
      self$`details` <- `details`
      self$`value` <- `value`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(currency = self$`currency`, details = self$`details`, value = self$`value`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`currency`)) {
          sprintf(
            '"currency":
            "%s"
                  ',
            self$`currency`
          )
        },
        if (!is.null(self$`details`)) {
          sprintf(
            '"details":
            "%s"
                  ',
            self$`details`
          )
        },
        if (!is.null(self$`value`)) {
          sprintf(
            '"value":
            %d
                  ',
            self$`value`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MonetaryValueJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MonetaryValueJson, validateParams = FALSE) {
      MonetaryValueObject <- jsonlite::fromJSON(MonetaryValueJson)
      self$`currency` <- MonetaryValueObject$`currency`
      self$`details` <- MonetaryValueObject$`details`
      self$`value` <- MonetaryValueObject$`value`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
