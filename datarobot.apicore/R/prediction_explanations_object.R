# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanationsObject
#'
#' @description PredictionExplanationsObject Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field feature  character The name of the feature contributing to the prediction.
#'
#' @field featureValue  \link{OneOfintegerbooleanstringnumber} The value the feature took on for this row. The type corresponds to the feature (bool, int, float, str, etc.).
#'
#' @field label  \link{OneOfstringnumber} Describes what output was driven by this prediction explanation. For regression projects, it is the name of the target feature. For classification projects, it is the class whose probability increasing would correspond to a positive strength of this prediction explanation. For predictions made using anomaly detection models, it is the &#x60;Anomaly Score&#x60;.
#'
#' @field strength  numeric [optional] Algorithm-specific explanation value attributed to &#x60;feature&#x60; in this row. If &#x60;explanationAlgorithm&#x60; &#x3D; &#x60;shap&#x60;, this is the SHAP value.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanationsObject <- R6::R6Class(
  "PredictionExplanationsObject",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(feature = NULL, featureValue = NULL, label = NULL, strength = NULL) {
      if (!is.null(`feature`)) {
        stopifnot(is.character(`feature`), length(`feature`) == 1)
      }
      if (!is.null(`featureValue`)) {
        .setPrimitiveProperty(typeList = list("numeric", "boolean", "character"), propertyData = featureValue)
      }
      if (!is.null(`label`)) {
        .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = label)
      }
      if (!is.null(`strength`)) {
        stopifnot(is.numeric(`strength`), length(`strength`) == 1)
      }
    }
  ),
  public = list(
    `feature` = NULL,
    `featureValue` = NULL,
    `label` = NULL,
    `strength` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param feature The name of the feature contributing to the prediction.
    #' @param featureValue The value the feature took on for this row. The type corresponds to the feature (bool, int, float, str, etc.).
    #' @param label Describes what output was driven by this prediction explanation. For regression projects, it is the name of the target feature. For classification projects, it is the class whose probability increasing would correspond to a positive strength of this prediction explanation. For predictions made using anomaly detection models, it is the &#x60;Anomaly Score&#x60;.
    #' @param strength Algorithm-specific explanation value attributed to &#x60;feature&#x60; in this row. If &#x60;explanationAlgorithm&#x60; &#x3D; &#x60;shap&#x60;, this is the SHAP value.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`feature` = NULL, `featureValue` = NULL, `label` = NULL, `strength` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`feature`, `featureValue`, `label`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(feature, featureValue, label, strength)
      }
      self$`feature` <- `feature`
      self$`featureValue` <- .setPrimitiveProperty(typeList = list("numeric", "boolean", "character"), propertyData = featureValue)
      self$`label` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = label)
      self$`strength` <- `strength`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(feature = self$`feature`, featureValue = self$`featureValue`, label = self$`label`, strength = self$`strength`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`feature`)) {
          sprintf(
            '"feature":
            "%s"
                  ',
            self$`feature`
          )
        },
        if (!is.null(self$`featureValue`)) {
          sprintf(
            '"featureValue":
          %s
        ',
            self$`featureValue`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
          %s
        ',
            self$`label`
          )
        },
        if (!is.null(self$`strength`)) {
          sprintf(
            '"strength":
            %d
                  ',
            self$`strength`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationsObjectJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationsObjectJson, validateParams = FALSE) {
      PredictionExplanationsObjectObject <- jsonlite::fromJSON(PredictionExplanationsObjectJson)
      self$`feature` <- PredictionExplanationsObjectObject$`feature`
      self$`featureValue` <- .setPrimitiveProperty(typeList = list("numeric", "boolean", "character"), propertyData = PredictionExplanationsObjectObject$featureValue)
      self$`label` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = PredictionExplanationsObjectObject$label)
      self$`strength` <- PredictionExplanationsObjectObject$`strength`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
