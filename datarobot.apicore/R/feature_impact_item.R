# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureImpactItem
#'
#' @description FeatureImpactItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featureName  character The name of the feature.
#'
#' @field impactNormalized  numeric The same as &#x60;impactUnnormalized&#x60;, but normalized such that the highest value is &#x60;1&#x60;.
#'
#' @field impactUnnormalized  numeric How much worse the error metric score is when making predictions on modified data.
#'
#' @field redundantWith  character Name of feature that has the highest correlation with this feature.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureImpactItem <- R6::R6Class(
  "FeatureImpactItem",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(featureName = NULL, impactNormalized = NULL, impactUnnormalized = NULL, redundantWith = NULL) {
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`impactNormalized`)) {
      }
      if (!is.null(`impactUnnormalized`)) {
      }
      if (!is.null(`redundantWith`)) {
        stopifnot(is.character(`redundantWith`), length(`redundantWith`) == 1)
      }
    }
  ),
  public = list(
    `featureName` = NULL,
    `impactNormalized` = NULL,
    `impactUnnormalized` = NULL,
    `redundantWith` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featureName The name of the feature.
    #' @param impactNormalized The same as &#x60;impactUnnormalized&#x60;, but normalized such that the highest value is &#x60;1&#x60;.
    #' @param impactUnnormalized How much worse the error metric score is when making predictions on modified data.
    #' @param redundantWith Name of feature that has the highest correlation with this feature.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featureName` = NULL, `impactNormalized` = NULL, `impactUnnormalized` = NULL, `redundantWith` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`featureName`, `impactNormalized`, `impactUnnormalized`, `redundantWith`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(featureName, impactNormalized, impactUnnormalized, redundantWith)
      }
      self$`featureName` <- `featureName`
      self$`impactNormalized` <- `impactNormalized`
      self$`impactUnnormalized` <- `impactUnnormalized`
      self$`redundantWith` <- `redundantWith`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(featureName = self$`featureName`, impactNormalized = self$`impactNormalized`, impactUnnormalized = self$`impactUnnormalized`, redundantWith = self$`redundantWith`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`impactNormalized`)) {
          sprintf(
            '"impactNormalized":
            %d
                  ',
            self$`impactNormalized`
          )
        },
        if (!is.null(self$`impactUnnormalized`)) {
          sprintf(
            '"impactUnnormalized":
            %d
                  ',
            self$`impactUnnormalized`
          )
        },
        if (!is.null(self$`redundantWith`)) {
          sprintf(
            '"redundantWith":
            "%s"
                  ',
            self$`redundantWith`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureImpactItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureImpactItemJson, validateParams = FALSE) {
      FeatureImpactItemObject <- jsonlite::fromJSON(FeatureImpactItemJson)
      self$`featureName` <- FeatureImpactItemObject$`featureName`
      self$`impactNormalized` <- FeatureImpactItemObject$`impactNormalized`
      self$`impactUnnormalized` <- FeatureImpactItemObject$`impactUnnormalized`
      self$`redundantWith` <- FeatureImpactItemObject$`redundantWith`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
