# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SnowflakeDataStreamer
#'
#' @description SnowflakeDataStreamer Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalog  character [optional] The name of the specified database catalog to read input data from.
#'
#' @field cloudStorageCredentialId  \link{OneOfstringstring} [optional] Either the populated value of the field or [redacted] due to permission settings
#'
#' @field cloudStorageType  character [optional] Type name for cloud storage
#'
#' @field credentialId  \link{OneOfstringstring} [optional] Either the populated value of the field or [redacted] due to permission settings
#'
#' @field dataStoreId  \link{OneOfstringstring} Either the populated value of the field or [redacted] due to permission settings
#'
#' @field externalStage  character External storage
#'
#' @field query  character [optional] A self-supplied SELECT statement of the dataset you wish to score. Helpful for supplying a more fine-grained selection of data not achievable through specification of \&quot;table\&quot; and/or \&quot;schema\&quot; parameters exclusively.If this job is executed with a job definition, then template variables are available which will be substituted for timestamps: {{ current_run_timestamp }}, {{ last_completed_run_time }}, {{ last_scheduled_run_time }}, {{ next_scheduled_run_time }}, {{ current_run_time }}
#'
#' @field schema  character [optional] The name of the specified database schema to read input data from.
#'
#' @field table  character [optional] The name of the specified database table to read input data from.
#'
#' @field type  character Type name for this intake type
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SnowflakeDataStreamer <- R6::R6Class(
  "SnowflakeDataStreamer",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(catalog = NULL, cloudStorageCredentialId = NULL, cloudStorageType = NULL, credentialId = NULL, dataStoreId = NULL, externalStage = NULL, query = NULL, schema = NULL, table = NULL, type = NULL) {
      if (!is.null(`dataStoreId`)) {
        .setPrimitiveProperty(typeList = list("character"), propertyData = dataStoreId)
      }
      if (!is.null(`externalStage`)) {
        stopifnot(is.character(`externalStage`), length(`externalStage`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`catalog`)) {
        stopifnot(is.character(`catalog`), length(`catalog`) == 1)
      }
      if (!is.null(`cloudStorageCredentialId`)) {
        .setPrimitiveProperty(typeList = list("character"), propertyData = cloudStorageCredentialId)
      }
      if (!is.null(`cloudStorageType`)) {
        stopifnot(is.character(`cloudStorageType`), length(`cloudStorageType`) == 1)
      }
      if (!is.null(`credentialId`)) {
        .setPrimitiveProperty(typeList = list("character"), propertyData = credentialId)
      }
      if (!is.null(`query`)) {
        stopifnot(is.character(`query`), length(`query`) == 1)
      }
      if (!is.null(`schema`)) {
        stopifnot(is.character(`schema`), length(`schema`) == 1)
      }
      if (!is.null(`table`)) {
        stopifnot(is.character(`table`), length(`table`) == 1)
      }
    }
  ),
  public = list(
    `catalog` = NULL,
    `cloudStorageCredentialId` = NULL,
    `cloudStorageType` = NULL,
    `credentialId` = NULL,
    `dataStoreId` = NULL,
    `externalStage` = NULL,
    `query` = NULL,
    `schema` = NULL,
    `table` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalog The name of the specified database catalog to read input data from.
    #' @param cloudStorageCredentialId Either the populated value of the field or [redacted] due to permission settings
    #' @param cloudStorageType Type name for cloud storage
    #' @param credentialId Either the populated value of the field or [redacted] due to permission settings
    #' @param dataStoreId Either the populated value of the field or [redacted] due to permission settings
    #' @param externalStage External storage
    #' @param query A self-supplied SELECT statement of the dataset you wish to score. Helpful for supplying a more fine-grained selection of data not achievable through specification of \&quot;table\&quot; and/or \&quot;schema\&quot; parameters exclusively.If this job is executed with a job definition, then template variables are available which will be substituted for timestamps: {{ current_run_timestamp }}, {{ last_completed_run_time }}, {{ last_scheduled_run_time }}, {{ next_scheduled_run_time }}, {{ current_run_time }}
    #' @param schema The name of the specified database schema to read input data from.
    #' @param table The name of the specified database table to read input data from.
    #' @param type Type name for this intake type
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataStoreId` = NULL, `externalStage` = NULL, `type` = NULL, `catalog` = NULL, `cloudStorageCredentialId` = NULL, `cloudStorageType` = "s3", `credentialId` = NULL, `query` = NULL, `schema` = NULL, `table` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`dataStoreId`, `externalStage`, `type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(catalog, cloudStorageCredentialId, cloudStorageType, credentialId, dataStoreId, externalStage, query, schema, table, type)
      }
      self$`catalog` <- `catalog`
      self$`cloudStorageCredentialId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = cloudStorageCredentialId)
      self$`cloudStorageType` <- `cloudStorageType`
      self$`credentialId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = credentialId)
      self$`dataStoreId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = dataStoreId)
      self$`externalStage` <- `externalStage`
      self$`query` <- `query`
      self$`schema` <- `schema`
      self$`table` <- `table`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(catalog = self$`catalog`, cloudStorageCredentialId = self$`cloudStorageCredentialId`, cloudStorageType = self$`cloudStorageType`, credentialId = self$`credentialId`, dataStoreId = self$`dataStoreId`, externalStage = self$`externalStage`, query = self$`query`, schema = self$`schema`, table = self$`table`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalog`)) {
          sprintf(
            '"catalog":
            "%s"
                  ',
            self$`catalog`
          )
        },
        if (!is.null(self$`cloudStorageCredentialId`)) {
          sprintf(
            '"cloudStorageCredentialId":
          %s
        ',
            self$`cloudStorageCredentialId`
          )
        },
        if (!is.null(self$`cloudStorageType`)) {
          sprintf(
            '"cloudStorageType":
            "%s"
                  ',
            self$`cloudStorageType`
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
          %s
        ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`dataStoreId`)) {
          sprintf(
            '"dataStoreId":
          %s
        ',
            self$`dataStoreId`
          )
        },
        if (!is.null(self$`externalStage`)) {
          sprintf(
            '"externalStage":
            "%s"
                  ',
            self$`externalStage`
          )
        },
        if (!is.null(self$`query`)) {
          sprintf(
            '"query":
            "%s"
                  ',
            self$`query`
          )
        },
        if (!is.null(self$`schema`)) {
          sprintf(
            '"schema":
            "%s"
                  ',
            self$`schema`
          )
        },
        if (!is.null(self$`table`)) {
          sprintf(
            '"table":
            "%s"
                  ',
            self$`table`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SnowflakeDataStreamerJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SnowflakeDataStreamerJson, validateParams = FALSE) {
      SnowflakeDataStreamerObject <- jsonlite::fromJSON(SnowflakeDataStreamerJson)
      self$`catalog` <- SnowflakeDataStreamerObject$`catalog`
      self$`cloudStorageCredentialId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = SnowflakeDataStreamerObject$cloudStorageCredentialId)
      self$`cloudStorageType` <- SnowflakeDataStreamerObject$`cloudStorageType`
      self$`credentialId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = SnowflakeDataStreamerObject$credentialId)
      self$`dataStoreId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = SnowflakeDataStreamerObject$dataStoreId)
      self$`externalStage` <- SnowflakeDataStreamerObject$`externalStage`
      self$`query` <- SnowflakeDataStreamerObject$`query`
      self$`schema` <- SnowflakeDataStreamerObject$`schema`
      self$`table` <- SnowflakeDataStreamerObject$`table`
      self$`type` <- SnowflakeDataStreamerObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
