# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RetrainingSettingsRetrieve
#'
#' @description RetrainingSettingsRetrieve Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dataset  \link{Dataset}
#'
#' @field predictionEnvironment  \link{PredictionEnvironment}
#'
#' @field retrainingUser  \link{RetrainingUser}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RetrainingSettingsRetrieve <- R6::R6Class(
  "RetrainingSettingsRetrieve",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`dataset` = NULL, `predictionEnvironment` = NULL, `retrainingUser` = NULL) {
      if (!is.null(`dataset`)) {
        stopifnot(R6::is.R6(`dataset`))
      }
      if (!is.null(`predictionEnvironment`)) {
        stopifnot(R6::is.R6(`predictionEnvironment`))
      }
      if (!is.null(`retrainingUser`)) {
        stopifnot(R6::is.R6(`retrainingUser`))
      }
    }
  ),
  public = list(
    `dataset` = NULL,
    `predictionEnvironment` = NULL,
    `retrainingUser` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dataset
    #' @param predictionEnvironment
    #' @param retrainingUser
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataset` = NULL, `predictionEnvironment` = NULL, `retrainingUser` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`dataset`, `predictionEnvironment`, `retrainingUser`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(dataset, predictionEnvironment, retrainingUser)
      }
      self$`dataset` <- `dataset`
      self$`predictionEnvironment` <- `predictionEnvironment`
      self$`retrainingUser` <- `retrainingUser`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(dataset = self$`dataset`, predictionEnvironment = self$`predictionEnvironment`, retrainingUser = self$`retrainingUser`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dataset`)) {
          sprintf(
            '"dataset":
            %s
      ',
            jsonlite::toJSON(self$`dataset`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`predictionEnvironment`)) {
          sprintf(
            '"predictionEnvironment":
            %s
      ',
            jsonlite::toJSON(self$`predictionEnvironment`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`retrainingUser`)) {
          sprintf(
            '"retrainingUser":
            %s
      ',
            jsonlite::toJSON(self$`retrainingUser`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RetrainingSettingsRetrieveJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RetrainingSettingsRetrieveJson, validateParams = FALSE) {
      RetrainingSettingsRetrieveObject <- jsonlite::fromJSON(RetrainingSettingsRetrieveJson)
      self$`dataset` <- Dataset$new()$fromJSON(jsonlite::toJSON(RetrainingSettingsRetrieveObject$dataset, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`predictionEnvironment` <- PredictionEnvironment$new()$fromJSON(jsonlite::toJSON(RetrainingSettingsRetrieveObject$predictionEnvironment, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`retrainingUser` <- RetrainingUser$new()$fromJSON(jsonlite::toJSON(RetrainingSettingsRetrieveObject$retrainingUser, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
