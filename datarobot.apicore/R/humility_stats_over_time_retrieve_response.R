# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title HumilityStatsOverTimeRetrieveResponse
#'
#' @description HumilityStatsOverTimeRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field buckets  list( \link{HumilityStatsBucket} ) An array of &#x60;bucket&#x60; objects, representing service health stats of the deployment over time
#'
#' @field modelId  character [optional] The id of the model for which metrics are being retrieved.
#'
#' @field segmentAttribute  character [optional] The name of the segment on which segment analysis is being performed.
#'
#' @field segmentValue  character [optional] The value of the &#x60;segmentAttribute&#x60; to segment on.
#'
#' @field summary  \link{HumilityStatsBucket}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
HumilityStatsOverTimeRetrieveResponse <- R6::R6Class(
  "HumilityStatsOverTimeRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(buckets = NULL, modelId = NULL, segmentAttribute = NULL, segmentValue = NULL, summary = NULL) {
      if (!is.null(`buckets`)) {
        stopifnot(is.vector(`buckets`))
      }
      if (!is.null(`summary`)) {
        stopifnot(R6::is.R6(`summary`))
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`segmentAttribute`)) {
        stopifnot(is.character(`segmentAttribute`), length(`segmentAttribute`) == 1)
      }
      if (!is.null(`segmentValue`)) {
        stopifnot(is.character(`segmentValue`), length(`segmentValue`) == 1)
      }
    }
  ),
  public = list(
    `buckets` = NULL,
    `modelId` = NULL,
    `segmentAttribute` = NULL,
    `segmentValue` = NULL,
    `summary` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param buckets An array of &#x60;bucket&#x60; objects, representing service health stats of the deployment over time
    #' @param modelId The id of the model for which metrics are being retrieved.
    #' @param segmentAttribute The name of the segment on which segment analysis is being performed.
    #' @param segmentValue The value of the &#x60;segmentAttribute&#x60; to segment on.
    #' @param summary
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`buckets` = NULL, `summary` = NULL, `modelId` = NULL, `segmentAttribute` = NULL, `segmentValue` = "", validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`buckets`, `summary`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(buckets, modelId, segmentAttribute, segmentValue, summary)
      }
      sapply(`buckets`, function(x) stopifnot(R6::is.R6(x)))
      self$`modelId` <- `modelId`
      self$`segmentAttribute` <- `segmentAttribute`
      self$`segmentValue` <- `segmentValue`
      self$`summary` <- `summary`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(buckets = self$`buckets`, modelId = self$`modelId`, segmentAttribute = self$`segmentAttribute`, segmentValue = self$`segmentValue`, summary = self$`summary`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`buckets`)) {
          sprintf(
            '"buckets":
            [%s]
',
            paste(sapply(self$`buckets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`segmentAttribute`)) {
          sprintf(
            '"segmentAttribute":
            "%s"
                  ',
            self$`segmentAttribute`
          )
        },
        if (!is.null(self$`segmentValue`)) {
          sprintf(
            '"segmentValue":
            "%s"
                  ',
            self$`segmentValue`
          )
        },
        if (!is.null(self$`summary`)) {
          sprintf(
            '"summary":
          %s
        ',
            jsonlite::toJSON(self$`summary`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param HumilityStatsOverTimeRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(HumilityStatsOverTimeRetrieveResponseJson, validateParams = FALSE) {
      HumilityStatsOverTimeRetrieveResponseObject <- jsonlite::fromJSON(HumilityStatsOverTimeRetrieveResponseJson)
      self$`buckets` <- ApiClient$new()$deserializeObj(HumilityStatsOverTimeRetrieveResponseObject$`buckets`, "array[HumilityStatsBucket]", loadNamespace("datarobot.apicore"))
      self$`modelId` <- HumilityStatsOverTimeRetrieveResponseObject$`modelId`
      self$`segmentAttribute` <- HumilityStatsOverTimeRetrieveResponseObject$`segmentAttribute`
      self$`segmentValue` <- HumilityStatsOverTimeRetrieveResponseObject$`segmentValue`
      self$`summary` <- HumilityStatsBucket$new()$fromJSON(jsonlite::toJSON(HumilityStatsOverTimeRetrieveResponseObject$summary, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
