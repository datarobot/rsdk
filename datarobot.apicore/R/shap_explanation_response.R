# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ShapExplanationResponse
#'
#' @description ShapExplanationResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field feature  character Feature name
#'
#' @field featureValue  character Feature value for this row. First 50 characters are returned.
#'
#' @field strength  numeric Shapley value for this feature and row.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShapExplanationResponse <- R6::R6Class(
  "ShapExplanationResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`feature` = NULL, `featureValue` = NULL, `strength` = NULL) {
      if (!is.null(`feature`)) {
        stopifnot(is.character(`feature`), length(`feature`) == 1)
      }
      if (!is.null(`featureValue`)) {
        stopifnot(is.character(`featureValue`), length(`featureValue`) == 1)
      }
      if (!is.null(`strength`)) {
      }
    }
  ),
  public = list(
    `feature` = NULL,
    `featureValue` = NULL,
    `strength` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param feature Feature name
    #' @param featureValue Feature value for this row. First 50 characters are returned.
    #' @param strength Shapley value for this feature and row.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`feature` = NULL, `featureValue` = NULL, `strength` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`feature`, `featureValue`, `strength`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(feature, featureValue, strength)
      }
      self$`feature` <- `feature`
      self$`featureValue` <- `featureValue`
      self$`strength` <- `strength`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(feature = self$`feature`, featureValue = self$`featureValue`, strength = self$`strength`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`feature`)) {
          sprintf(
            '"feature":
            "%s"
                  ',
            self$`feature`
          )
        },
        if (!is.null(self$`featureValue`)) {
          sprintf(
            '"featureValue":
            "%s"
                  ',
            self$`featureValue`
          )
        },
        if (!is.null(self$`strength`)) {
          sprintf(
            '"strength":
            %d
                  ',
            self$`strength`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ShapExplanationResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ShapExplanationResponseJson, validateParams = FALSE) {
      ShapExplanationResponseObject <- jsonlite::fromJSON(ShapExplanationResponseJson)
      self$`feature` <- ShapExplanationResponseObject$`feature`
      self$`featureValue` <- ShapExplanationResponseObject$`featureValue`
      self$`strength` <- ShapExplanationResponseObject$`strength`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
