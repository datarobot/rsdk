# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseSummary
#'
#' @description UseCaseSummary Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field averagePredictionsCount  integer Average number of predictions per bucket during this time period
#'
#' @field averageRealizedValue  integer [optional] Average amount of value per bucket realised during this time period
#'
#' @field period  \link{UseCaseBucketPeriod}
#'
#' @field totalPredictionsCount  integer [optional] Total number of predictions that happened during this time period
#'
#' @field totalRealizedValue  integer Total amount of value realised during this time period
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseSummary <- R6::R6Class(
  "UseCaseSummary",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(averagePredictionsCount = NULL, averageRealizedValue = NULL, period = NULL, totalPredictionsCount = NULL, totalRealizedValue = NULL) {
      if (!is.null(`averagePredictionsCount`)) {
        stopifnot(is.numeric(`averagePredictionsCount`), length(`averagePredictionsCount`) == 1)
      }
      if (!is.null(`period`)) {
        stopifnot(R6::is.R6(`period`))
      }
      if (!is.null(`totalRealizedValue`)) {
        stopifnot(is.numeric(`totalRealizedValue`), length(`totalRealizedValue`) == 1)
      }
      if (!is.null(`averageRealizedValue`)) {
        stopifnot(is.numeric(`averageRealizedValue`), length(`averageRealizedValue`) == 1)
      }
      if (!is.null(`totalPredictionsCount`)) {
        stopifnot(is.numeric(`totalPredictionsCount`), length(`totalPredictionsCount`) == 1)
      }
    }
  ),
  public = list(
    `averagePredictionsCount` = NULL,
    `averageRealizedValue` = NULL,
    `period` = NULL,
    `totalPredictionsCount` = NULL,
    `totalRealizedValue` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param averagePredictionsCount Average number of predictions per bucket during this time period
    #' @param averageRealizedValue Average amount of value per bucket realised during this time period
    #' @param period
    #' @param totalPredictionsCount Total number of predictions that happened during this time period
    #' @param totalRealizedValue Total amount of value realised during this time period
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`averagePredictionsCount` = NULL, `period` = NULL, `totalRealizedValue` = NULL, `averageRealizedValue` = NULL, `totalPredictionsCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`averagePredictionsCount`, `period`, `totalRealizedValue`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(averagePredictionsCount, averageRealizedValue, period, totalPredictionsCount, totalRealizedValue)
      }
      self$`averagePredictionsCount` <- `averagePredictionsCount`
      self$`averageRealizedValue` <- `averageRealizedValue`
      self$`period` <- `period`
      self$`totalPredictionsCount` <- `totalPredictionsCount`
      self$`totalRealizedValue` <- `totalRealizedValue`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(averagePredictionsCount = self$`averagePredictionsCount`, averageRealizedValue = self$`averageRealizedValue`, period = self$`period`, totalPredictionsCount = self$`totalPredictionsCount`, totalRealizedValue = self$`totalRealizedValue`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`averagePredictionsCount`)) {
          sprintf(
            '"averagePredictionsCount":
            %d
                  ',
            self$`averagePredictionsCount`
          )
        },
        if (!is.null(self$`averageRealizedValue`)) {
          sprintf(
            '"averageRealizedValue":
            %d
                  ',
            self$`averageRealizedValue`
          )
        },
        if (!is.null(self$`period`)) {
          sprintf(
            '"period":
          %s
        ',
            jsonlite::toJSON(self$`period`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`totalPredictionsCount`)) {
          sprintf(
            '"totalPredictionsCount":
            %d
                  ',
            self$`totalPredictionsCount`
          )
        },
        if (!is.null(self$`totalRealizedValue`)) {
          sprintf(
            '"totalRealizedValue":
            %d
                  ',
            self$`totalRealizedValue`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseSummaryJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseSummaryJson, validateParams = FALSE) {
      UseCaseSummaryObject <- jsonlite::fromJSON(UseCaseSummaryJson)
      self$`averagePredictionsCount` <- UseCaseSummaryObject$`averagePredictionsCount`
      self$`averageRealizedValue` <- UseCaseSummaryObject$`averageRealizedValue`
      self$`period` <- UseCaseBucketPeriod$new()$fromJSON(jsonlite::toJSON(UseCaseSummaryObject$period, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`totalPredictionsCount` <- UseCaseSummaryObject$`totalPredictionsCount`
      self$`totalRealizedValue` <- UseCaseSummaryObject$`totalRealizedValue`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
