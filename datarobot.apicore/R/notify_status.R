# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotifyStatus
#'
#' @description NotifyStatus Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field inviteLink  character [optional] The link the user can follow to complete their DR account.
#'
#' @field sentStatus  character Boolean value whether an invite has been sent to the user or not.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotifyStatus <- R6::R6Class(
  "NotifyStatus",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(inviteLink = NULL, sentStatus = NULL) {
      if (!is.null(`sentStatus`)) {
        stopifnot(is.logical(`sentStatus`), length(`sentStatus`) == 1)
      }
      if (!is.null(`inviteLink`)) {
        stopifnot(is.character(`inviteLink`), length(`inviteLink`) == 1)
      }
    }
  ),
  public = list(
    `inviteLink` = NULL,
    `sentStatus` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param inviteLink The link the user can follow to complete their DR account.
    #' @param sentStatus Boolean value whether an invite has been sent to the user or not.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`sentStatus` = NULL, `inviteLink` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`sentStatus`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(inviteLink, sentStatus)
      }
      self$`inviteLink` <- `inviteLink`
      self$`sentStatus` <- `sentStatus`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(inviteLink = self$`inviteLink`, sentStatus = self$`sentStatus`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`inviteLink`)) {
          sprintf(
            '"inviteLink":
            "%s"
                  ',
            self$`inviteLink`
          )
        },
        if (!is.null(self$`sentStatus`)) {
          sprintf(
            '"sentStatus":
            %s
                  ',
            tolower(self$`sentStatus`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotifyStatusJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotifyStatusJson, validateParams = FALSE) {
      NotifyStatusObject <- jsonlite::fromJSON(NotifyStatusJson)
      self$`inviteLink` <- NotifyStatusObject$`inviteLink`
      self$`sentStatus` <- NotifyStatusObject$`sentStatus`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
