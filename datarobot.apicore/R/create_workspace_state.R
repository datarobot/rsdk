# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CreateWorkspaceState
#'
#' @description CreateWorkspaceState Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasets  list( \link{DataEngineDataset} ) [optional] Source datasets in the Data Engine workspace.
#'
#' @field language  character Language of the Data Engine query.
#'
#' @field query  character Actual body of the Data Engine query.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CreateWorkspaceState <- R6::R6Class(
  "CreateWorkspaceState",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datasets` = NULL, `language` = NULL, `query` = NULL) {
      if (!is.null(`language`)) {
        stopifnot(is.character(`language`), length(`language`) == 1)
      }
      if (!is.null(`query`)) {
        stopifnot(is.character(`query`), length(`query`) == 1)
      }
      if (!is.null(`datasets`)) {
        stopifnot(is.vector(`datasets`))
      }
    }
  ),
  public = list(
    `datasets` = NULL,
    `language` = NULL,
    `query` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasets Source datasets in the Data Engine workspace.
    #' @param language Language of the Data Engine query.
    #' @param query Actual body of the Data Engine query.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`language` = NULL, `query` = NULL, `datasets` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`language`, `query`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datasets, language, query)
      }
      sapply(`datasets`, function(x) stopifnot(R6::is.R6(x)))
      self$`language` <- `language`
      self$`query` <- `query`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datasets = self$`datasets`, language = self$`language`, query = self$`query`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasets`)) {
          sprintf(
            '"datasets":
            [%s]
      ',
            paste(sapply(self$`datasets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`language`)) {
          sprintf(
            '"language":
            "%s"
                  ',
            self$`language`
          )
        },
        if (!is.null(self$`query`)) {
          sprintf(
            '"query":
            "%s"
                  ',
            self$`query`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CreateWorkspaceStateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CreateWorkspaceStateJson, validateParams = FALSE) {
      CreateWorkspaceStateObject <- jsonlite::fromJSON(CreateWorkspaceStateJson)
      self$`datasets` <- ApiClient$new()$deserializeObj(CreateWorkspaceStateObject$`datasets`, "array[DataEngineDataset]", loadNamespace("datarobot.apicore"))
      self$`language` <- CreateWorkspaceStateObject$`language`
      self$`query` <- CreateWorkspaceStateObject$`query`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
