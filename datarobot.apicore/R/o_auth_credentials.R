# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title OAuthCredentials
#'
#' @description OAuthCredentials Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialType  character The type of these credentials, &#39;oauth&#39; here.
#'
#' @field oauthAccessToken  character [optional] The oauth access token.
#'
#' @field oauthClientId  character [optional] The oauth client ID.
#'
#' @field oauthClientSecret  character [optional] The oauth client secret.
#'
#' @field oauthRefreshToken  character The oauth refresh token.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
OAuthCredentials <- R6::R6Class(
  "OAuthCredentials",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`credentialType` = NULL, `oauthAccessToken` = NULL, `oauthClientId` = NULL, `oauthClientSecret` = NULL, `oauthRefreshToken` = NULL) {
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`oauthRefreshToken`)) {
        stopifnot(is.character(`oauthRefreshToken`), length(`oauthRefreshToken`) == 1)
      }
      if (!is.null(`oauthAccessToken`)) {
        stopifnot(is.character(`oauthAccessToken`), length(`oauthAccessToken`) == 1)
      }
      if (!is.null(`oauthClientId`)) {
        stopifnot(is.character(`oauthClientId`), length(`oauthClientId`) == 1)
      }
      if (!is.null(`oauthClientSecret`)) {
        stopifnot(is.character(`oauthClientSecret`), length(`oauthClientSecret`) == 1)
      }
    }
  ),
  public = list(
    `credentialType` = NULL,
    `oauthAccessToken` = NULL,
    `oauthClientId` = NULL,
    `oauthClientSecret` = NULL,
    `oauthRefreshToken` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialType The type of these credentials, &#39;oauth&#39; here.
    #' @param oauthAccessToken The oauth access token.
    #' @param oauthClientId The oauth client ID.
    #' @param oauthClientSecret The oauth client secret.
    #' @param oauthRefreshToken The oauth refresh token.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialType` = NULL, `oauthRefreshToken` = NULL, `oauthAccessToken` = NULL, `oauthClientId` = NULL, `oauthClientSecret` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`credentialType`, `oauthRefreshToken`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(credentialType, oauthAccessToken, oauthClientId, oauthClientSecret, oauthRefreshToken)
      }
      self$`credentialType` <- `credentialType`
      self$`oauthAccessToken` <- `oauthAccessToken`
      self$`oauthClientId` <- `oauthClientId`
      self$`oauthClientSecret` <- `oauthClientSecret`
      self$`oauthRefreshToken` <- `oauthRefreshToken`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(credentialType = self$`credentialType`, oauthAccessToken = self$`oauthAccessToken`, oauthClientId = self$`oauthClientId`, oauthClientSecret = self$`oauthClientSecret`, oauthRefreshToken = self$`oauthRefreshToken`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        },
        if (!is.null(self$`oauthAccessToken`)) {
          sprintf(
            '"oauthAccessToken":
            "%s"
                  ',
            self$`oauthAccessToken`
          )
        },
        if (!is.null(self$`oauthClientId`)) {
          sprintf(
            '"oauthClientId":
            "%s"
                  ',
            self$`oauthClientId`
          )
        },
        if (!is.null(self$`oauthClientSecret`)) {
          sprintf(
            '"oauthClientSecret":
            "%s"
                  ',
            self$`oauthClientSecret`
          )
        },
        if (!is.null(self$`oauthRefreshToken`)) {
          sprintf(
            '"oauthRefreshToken":
            "%s"
                  ',
            self$`oauthRefreshToken`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param OAuthCredentialsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(OAuthCredentialsJson, validateParams = FALSE) {
      OAuthCredentialsObject <- jsonlite::fromJSON(OAuthCredentialsJson)
      self$`credentialType` <- OAuthCredentialsObject$`credentialType`
      self$`oauthAccessToken` <- OAuthCredentialsObject$`oauthAccessToken`
      self$`oauthClientId` <- OAuthCredentialsObject$`oauthClientId`
      self$`oauthClientSecret` <- OAuthCredentialsObject$`oauthClientSecret`
      self$`oauthRefreshToken` <- OAuthCredentialsObject$`oauthRefreshToken`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
