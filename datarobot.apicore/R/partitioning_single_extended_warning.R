# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PartitioningSingleExtendedWarning
#'
#' @description PartitioningSingleExtendedWarning Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field message  character Warning message.
#'
#' @field title  character Warning short title.
#'
#' @field type  character Warning severity type.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PartitioningSingleExtendedWarning <- R6::R6Class(
  "PartitioningSingleExtendedWarning",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`message` = NULL, `title` = NULL, `type` = NULL) {
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`title`)) {
        stopifnot(is.character(`title`), length(`title`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
    }
  ),
  public = list(
    `message` = NULL,
    `title` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param message Warning message.
    #' @param title Warning short title.
    #' @param type Warning severity type.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`message` = NULL, `title` = NULL, `type` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`message`, `title`, `type`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(message, title, type)
      }
      self$`message` <- `message`
      self$`title` <- `title`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(message = self$`message`, title = self$`title`, type = self$`type`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`title`)) {
          sprintf(
            '"title":
            "%s"
                  ',
            self$`title`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PartitioningSingleExtendedWarningJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PartitioningSingleExtendedWarningJson, validateParams = FALSE) {
      PartitioningSingleExtendedWarningObject <- jsonlite::fromJSON(PartitioningSingleExtendedWarningJson)
      self$`message` <- PartitioningSingleExtendedWarningObject$`message`
      self$`title` <- PartitioningSingleExtendedWarningObject$`title`
      self$`type` <- PartitioningSingleExtendedWarningObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
