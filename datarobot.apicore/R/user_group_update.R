# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserGroupUpdate
#'
#' @description UserGroupUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accessRoleId  character [optional] The identifier of the access role to assign to the group.
#'
#' @field accountPermissions  named list( character ) [optional] Account permissions to set for this user group. Each key is a permission name.
#'
#' @field description  character [optional] The description of this user group.
#'
#' @field email  character [optional] The email of this user group.
#'
#' @field maxAppCount  integer [optional] The upper limit for the allowed applications count for the user group.
#'
#' @field maxCustomDeployments  integer [optional] Maximum allowed value for maxCustomDeployment for the user group.
#'
#' @field maxEdaWorkers  integer [optional] The upper limit for a number of EDA workers to be run for users of this user group.
#'
#' @field maxRam  integer [optional] The upper limit for amount of RAM available to users of this user group.
#'
#' @field maxUploadSize  integer [optional] The upper limit for the allowed upload size for the user group.
#'
#' @field maxUploadSizeCatalog  integer [optional] The upper limit for the allowed upload size in the AI catalog for this user group.
#'
#' @field maxWorkers  integer [optional] The upper limit for a number of workers to be run for users of this user group.
#'
#' @field name  character [optional] The new name for the user group.
#'
#' @field orgId  character [optional] The id of the organization to assign the user group to.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserGroupUpdate <- R6::R6Class(
  "UserGroupUpdate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`accessRoleId` = NULL, `accountPermissions` = NULL, `description` = NULL, `email` = NULL, `maxAppCount` = NULL, `maxCustomDeployments` = NULL, `maxEdaWorkers` = NULL, `maxRam` = NULL, `maxUploadSize` = NULL, `maxUploadSizeCatalog` = NULL, `maxWorkers` = NULL, `name` = NULL, `orgId` = NULL) {
      if (!is.null(`accessRoleId`)) {
        stopifnot(is.character(`accessRoleId`), length(`accessRoleId`) == 1)
      }
      if (!is.null(`accountPermissions`)) {
        stopifnot(is.vector(`accountPermissions`))
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`email`)) {
        stopifnot(is.character(`email`), length(`email`) == 1)
      }
      if (!is.null(`maxAppCount`)) {
        stopifnot(is.numeric(`maxAppCount`), length(`maxAppCount`) == 1)
      }
      if (!is.null(`maxCustomDeployments`)) {
        stopifnot(is.numeric(`maxCustomDeployments`), length(`maxCustomDeployments`) == 1)
      }
      if (!is.null(`maxEdaWorkers`)) {
        stopifnot(is.numeric(`maxEdaWorkers`), length(`maxEdaWorkers`) == 1)
      }
      if (!is.null(`maxRam`)) {
        stopifnot(is.numeric(`maxRam`), length(`maxRam`) == 1)
      }
      if (!is.null(`maxUploadSize`)) {
        stopifnot(is.numeric(`maxUploadSize`), length(`maxUploadSize`) == 1)
      }
      if (!is.null(`maxUploadSizeCatalog`)) {
        stopifnot(is.numeric(`maxUploadSizeCatalog`), length(`maxUploadSizeCatalog`) == 1)
      }
      if (!is.null(`maxWorkers`)) {
        stopifnot(is.numeric(`maxWorkers`), length(`maxWorkers`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
    }
  ),
  public = list(
    `accessRoleId` = NULL,
    `accountPermissions` = NULL,
    `description` = NULL,
    `email` = NULL,
    `maxAppCount` = NULL,
    `maxCustomDeployments` = NULL,
    `maxEdaWorkers` = NULL,
    `maxRam` = NULL,
    `maxUploadSize` = NULL,
    `maxUploadSizeCatalog` = NULL,
    `maxWorkers` = NULL,
    `name` = NULL,
    `orgId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accessRoleId The identifier of the access role to assign to the group.
    #' @param accountPermissions Account permissions to set for this user group. Each key is a permission name.
    #' @param description The description of this user group.
    #' @param email The email of this user group.
    #' @param maxAppCount The upper limit for the allowed applications count for the user group.
    #' @param maxCustomDeployments Maximum allowed value for maxCustomDeployment for the user group.
    #' @param maxEdaWorkers The upper limit for a number of EDA workers to be run for users of this user group.
    #' @param maxRam The upper limit for amount of RAM available to users of this user group.
    #' @param maxUploadSize The upper limit for the allowed upload size for the user group.
    #' @param maxUploadSizeCatalog The upper limit for the allowed upload size in the AI catalog for this user group.
    #' @param maxWorkers The upper limit for a number of workers to be run for users of this user group.
    #' @param name The new name for the user group.
    #' @param orgId The id of the organization to assign the user group to.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`accessRoleId` = NULL, `accountPermissions` = NULL, `description` = NULL, `email` = NULL, `maxAppCount` = NULL, `maxCustomDeployments` = NULL, `maxEdaWorkers` = NULL, `maxRam` = NULL, `maxUploadSize` = NULL, `maxUploadSizeCatalog` = NULL, `maxWorkers` = NULL, `name` = NULL, `orgId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(accessRoleId, accountPermissions, description, email, maxAppCount, maxCustomDeployments, maxEdaWorkers, maxRam, maxUploadSize, maxUploadSizeCatalog, maxWorkers, name, orgId)
      }
      self$`accessRoleId` <- `accessRoleId`
      sapply(`accountPermissions`, function(x) stopifnot(is.character(x)))
      self$`description` <- `description`
      self$`email` <- `email`
      self$`maxAppCount` <- `maxAppCount`
      self$`maxCustomDeployments` <- `maxCustomDeployments`
      self$`maxEdaWorkers` <- `maxEdaWorkers`
      self$`maxRam` <- `maxRam`
      self$`maxUploadSize` <- `maxUploadSize`
      self$`maxUploadSizeCatalog` <- `maxUploadSizeCatalog`
      self$`maxWorkers` <- `maxWorkers`
      self$`name` <- `name`
      self$`orgId` <- `orgId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(accessRoleId = self$`accessRoleId`, accountPermissions = self$`accountPermissions`, description = self$`description`, email = self$`email`, maxAppCount = self$`maxAppCount`, maxCustomDeployments = self$`maxCustomDeployments`, maxEdaWorkers = self$`maxEdaWorkers`, maxRam = self$`maxRam`, maxUploadSize = self$`maxUploadSize`, maxUploadSizeCatalog = self$`maxUploadSizeCatalog`, maxWorkers = self$`maxWorkers`, name = self$`name`, orgId = self$`orgId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accessRoleId`)) {
          sprintf(
            '"accessRoleId":
            "%s"
                  ',
            self$`accessRoleId`
          )
        },
        if (!is.null(self$`accountPermissions`)) {
          sprintf(
            '"accountPermissions":
            "%s"
                  ',
            jsonlite::toJSON(lapply(self$`accountPermissions`, function(x) {
              x
            }), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`email`)) {
          sprintf(
            '"email":
            "%s"
                  ',
            self$`email`
          )
        },
        if (!is.null(self$`maxAppCount`)) {
          sprintf(
            '"maxAppCount":
            %d
                  ',
            self$`maxAppCount`
          )
        },
        if (!is.null(self$`maxCustomDeployments`)) {
          sprintf(
            '"maxCustomDeployments":
            %d
                  ',
            self$`maxCustomDeployments`
          )
        },
        if (!is.null(self$`maxEdaWorkers`)) {
          sprintf(
            '"maxEdaWorkers":
            %d
                  ',
            self$`maxEdaWorkers`
          )
        },
        if (!is.null(self$`maxRam`)) {
          sprintf(
            '"maxRam":
            %d
                  ',
            self$`maxRam`
          )
        },
        if (!is.null(self$`maxUploadSize`)) {
          sprintf(
            '"maxUploadSize":
            %d
                  ',
            self$`maxUploadSize`
          )
        },
        if (!is.null(self$`maxUploadSizeCatalog`)) {
          sprintf(
            '"maxUploadSizeCatalog":
            %d
                  ',
            self$`maxUploadSizeCatalog`
          )
        },
        if (!is.null(self$`maxWorkers`)) {
          sprintf(
            '"maxWorkers":
            %d
                  ',
            self$`maxWorkers`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserGroupUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserGroupUpdateJson, validateParams = FALSE) {
      UserGroupUpdateObject <- jsonlite::fromJSON(UserGroupUpdateJson)
      self$`accessRoleId` <- UserGroupUpdateObject$`accessRoleId`
      self$`accountPermissions` <- ApiClient$new()$deserializeObj(UserGroupUpdateObject$`accountPermissions`, "map(character)", loadNamespace("datarobot.apicore"))
      self$`description` <- UserGroupUpdateObject$`description`
      self$`email` <- UserGroupUpdateObject$`email`
      self$`maxAppCount` <- UserGroupUpdateObject$`maxAppCount`
      self$`maxCustomDeployments` <- UserGroupUpdateObject$`maxCustomDeployments`
      self$`maxEdaWorkers` <- UserGroupUpdateObject$`maxEdaWorkers`
      self$`maxRam` <- UserGroupUpdateObject$`maxRam`
      self$`maxUploadSize` <- UserGroupUpdateObject$`maxUploadSize`
      self$`maxUploadSizeCatalog` <- UserGroupUpdateObject$`maxUploadSizeCatalog`
      self$`maxWorkers` <- UserGroupUpdateObject$`maxWorkers`
      self$`name` <- UserGroupUpdateObject$`name`
      self$`orgId` <- UserGroupUpdateObject$`orgId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
