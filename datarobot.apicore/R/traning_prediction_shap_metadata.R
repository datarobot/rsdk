# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TraningPredictionShapMetadata
#'
#' @description TraningPredictionShapMetadata Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field shapBaseValue  numeric The model&#39;s average prediction over the training data. SHAP values are deviations from the base value.
#'
#' @field shapRemainingTotal  integer The total of SHAP values for features beyond the maxExplanations. This can be identically 0 in all rows, if maxExplanations is greater than the number of features and thus all features are returned.
#'
#' @field warnings  list( \link{ShapWarningItems} ) SHAP values calculation warnings
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TraningPredictionShapMetadata <- R6::R6Class(
  "TraningPredictionShapMetadata",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`shapBaseValue` = NULL, `shapRemainingTotal` = NULL, `warnings` = NULL) {
      if (!is.null(`shapBaseValue`)) {
      }
      if (!is.null(`shapRemainingTotal`)) {
        stopifnot(is.numeric(`shapRemainingTotal`), length(`shapRemainingTotal`) == 1)
      }
      if (!is.null(`warnings`)) {
        stopifnot(is.vector(`warnings`), sapply(`warnings`, R6::is.R6))
      }
    }
  ),
  public = list(
    `shapBaseValue` = NULL,
    `shapRemainingTotal` = NULL,
    `warnings` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param shapBaseValue The model&#39;s average prediction over the training data. SHAP values are deviations from the base value.
    #' @param shapRemainingTotal The total of SHAP values for features beyond the maxExplanations. This can be identically 0 in all rows, if maxExplanations is greater than the number of features and thus all features are returned.
    #' @param warnings SHAP values calculation warnings
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`shapBaseValue` = NULL, `shapRemainingTotal` = NULL, `warnings` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`shapBaseValue`, `shapRemainingTotal`, `warnings`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(shapBaseValue, shapRemainingTotal, warnings)
      }
      self$`shapBaseValue` <- `shapBaseValue`
      self$`shapRemainingTotal` <- `shapRemainingTotal`
      self$`warnings` <- `warnings`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(shapBaseValue = self$`shapBaseValue`, shapRemainingTotal = self$`shapRemainingTotal`, warnings = self$`warnings`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`shapBaseValue`)) {
          sprintf(
            '"shapBaseValue":
            %d
                  ',
            self$`shapBaseValue`
          )
        },
        if (!is.null(self$`shapRemainingTotal`)) {
          sprintf(
            '"shapRemainingTotal":
            %d
                  ',
            self$`shapRemainingTotal`
          )
        },
        if (!is.null(self$`warnings`)) {
          sprintf(
            '"warnings":
            [%s]
      ',
            paste(sapply(self$`warnings`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TraningPredictionShapMetadataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TraningPredictionShapMetadataJson, validateParams = FALSE) {
      TraningPredictionShapMetadataObject <- jsonlite::fromJSON(TraningPredictionShapMetadataJson)
      self$`shapBaseValue` <- TraningPredictionShapMetadataObject$`shapBaseValue`
      self$`shapRemainingTotal` <- TraningPredictionShapMetadataObject$`shapRemainingTotal`
      self$`warnings` <- ApiClient$new()$deserializeObj(TraningPredictionShapMetadataObject$`warnings`, "array[ShapWarningItems]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
