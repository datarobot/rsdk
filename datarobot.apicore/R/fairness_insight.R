# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FairnessInsight
#'
#' @description FairnessInsight Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field fairnessMetric  character The fairness metric used to calculate the fairness scores.
#'
#' @field fairnessThreshold  numeric Value of the fairness threshold, defined in project options.
#'
#' @field modelId  character ID of the model fairness was measured for.
#'
#' @field perClassFairness  list( \link{PerClassFairness} ) An array of calculated fairness scores for each protected feature class.
#'
#' @field predictionThreshold  numeric [optional] Model&#39;s prediction threshold used when insight was calculated. &#x60;&#x60;null&#x60;&#x60; if prediction threshold is not required for the fairness metric calculations.
#'
#' @field protectedFeature  character Name of the protected feature the fairness calculation is made for.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FairnessInsight <- R6::R6Class(
  "FairnessInsight",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(fairnessMetric = NULL, fairnessThreshold = NULL, modelId = NULL, perClassFairness = NULL, predictionThreshold = NULL, protectedFeature = NULL) {
      if (!is.null(`fairnessMetric`)) {
        stopifnot(is.character(`fairnessMetric`), length(`fairnessMetric`) == 1)
      }
      if (!is.null(`fairnessThreshold`)) {
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`perClassFairness`)) {
        stopifnot(is.vector(`perClassFairness`))
      }
      if (!is.null(`protectedFeature`)) {
        stopifnot(is.character(`protectedFeature`), length(`protectedFeature`) == 1)
      }
      if (!is.null(`predictionThreshold`)) {
        stopifnot(is.numeric(`predictionThreshold`), length(`predictionThreshold`) == 1)
      }
    }
  ),
  public = list(
    `fairnessMetric` = NULL,
    `fairnessThreshold` = NULL,
    `modelId` = NULL,
    `perClassFairness` = NULL,
    `predictionThreshold` = NULL,
    `protectedFeature` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param fairnessMetric The fairness metric used to calculate the fairness scores.
    #' @param fairnessThreshold Value of the fairness threshold, defined in project options.
    #' @param modelId ID of the model fairness was measured for.
    #' @param perClassFairness An array of calculated fairness scores for each protected feature class.
    #' @param predictionThreshold Model&#39;s prediction threshold used when insight was calculated. &#x60;&#x60;null&#x60;&#x60; if prediction threshold is not required for the fairness metric calculations.
    #' @param protectedFeature Name of the protected feature the fairness calculation is made for.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`fairnessMetric` = NULL, `fairnessThreshold` = NULL, `modelId` = NULL, `perClassFairness` = NULL, `protectedFeature` = NULL, `predictionThreshold` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`fairnessMetric`, `fairnessThreshold`, `modelId`, `perClassFairness`, `protectedFeature`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(fairnessMetric, fairnessThreshold, modelId, perClassFairness, predictionThreshold, protectedFeature)
      }
      self$`fairnessMetric` <- `fairnessMetric`
      self$`fairnessThreshold` <- `fairnessThreshold`
      self$`modelId` <- `modelId`
      sapply(`perClassFairness`, function(x) stopifnot(R6::is.R6(x)))
      self$`predictionThreshold` <- `predictionThreshold`
      self$`protectedFeature` <- `protectedFeature`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(fairnessMetric = self$`fairnessMetric`, fairnessThreshold = self$`fairnessThreshold`, modelId = self$`modelId`, perClassFairness = self$`perClassFairness`, predictionThreshold = self$`predictionThreshold`, protectedFeature = self$`protectedFeature`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`fairnessMetric`)) {
          sprintf(
            '"fairnessMetric":
            "%s"
                  ',
            self$`fairnessMetric`
          )
        },
        if (!is.null(self$`fairnessThreshold`)) {
          sprintf(
            '"fairnessThreshold":
            %d
                  ',
            self$`fairnessThreshold`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`perClassFairness`)) {
          sprintf(
            '"perClassFairness":
            [%s]
',
            paste(sapply(self$`perClassFairness`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        },
        if (!is.null(self$`protectedFeature`)) {
          sprintf(
            '"protectedFeature":
            "%s"
                  ',
            self$`protectedFeature`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FairnessInsightJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FairnessInsightJson, validateParams = FALSE) {
      FairnessInsightObject <- jsonlite::fromJSON(FairnessInsightJson)
      self$`fairnessMetric` <- FairnessInsightObject$`fairnessMetric`
      self$`fairnessThreshold` <- FairnessInsightObject$`fairnessThreshold`
      self$`modelId` <- FairnessInsightObject$`modelId`
      self$`perClassFairness` <- ApiClient$new()$deserializeObj(FairnessInsightObject$`perClassFairness`, "array[PerClassFairness]", loadNamespace("datarobot.apicore"))
      self$`predictionThreshold` <- FairnessInsightObject$`predictionThreshold`
      self$`protectedFeature` <- FairnessInsightObject$`protectedFeature`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
