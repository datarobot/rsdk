# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DRConnectorV1Details
#'
#' @description DRConnectorV1Details Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field connectorId  character ID of the connector.
#'
#' @field fieldSchemas  list( \link{ConnectorFieldSchema} ) [optional] Connector field schemas.
#'
#' @field fields  list( \link{DRConnectorV1Field} ) Connector fields.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DRConnectorV1Details <- R6::R6Class(
  "DRConnectorV1Details",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`connectorId` = NULL, `fieldSchemas` = NULL, `fields` = NULL) {
      if (!is.null(`connectorId`)) {
        stopifnot(is.character(`connectorId`), length(`connectorId`) == 1)
      }
      if (!is.null(`fields`)) {
        stopifnot(is.vector(`fields`), sapply(`fields`, R6::is.R6))
      }
      if (!is.null(`fieldSchemas`) && length(`fieldSchemas`) > 0) {
        stopifnot(is.vector(`fieldSchemas`), sapply(`fieldSchemas`, R6::is.R6))
      }
    }
  ),
  public = list(
    `connectorId` = NULL,
    `fieldSchemas` = NULL,
    `fields` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param connectorId ID of the connector.
    #' @param fieldSchemas Connector field schemas.
    #' @param fields Connector fields.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`connectorId` = NULL, `fields` = NULL, `fieldSchemas` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`connectorId`, `fields`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(connectorId, fieldSchemas, fields)
      }
      self$`connectorId` <- `connectorId`
      self$`fieldSchemas` <- `fieldSchemas`
      self$`fields` <- `fields`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(connectorId = self$`connectorId`, fieldSchemas = self$`fieldSchemas`, fields = self$`fields`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`connectorId`)) {
          sprintf(
            '"connectorId":
            "%s"
                  ',
            self$`connectorId`
          )
        },
        if (!is.null(self$`fieldSchemas`)) {
          sprintf(
            '"fieldSchemas":
            [%s]
      ',
            paste(sapply(self$`fieldSchemas`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`fields`)) {
          sprintf(
            '"fields":
            [%s]
      ',
            paste(sapply(self$`fields`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DRConnectorV1DetailsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DRConnectorV1DetailsJson, validateParams = FALSE) {
      DRConnectorV1DetailsObject <- jsonlite::fromJSON(DRConnectorV1DetailsJson)
      self$`connectorId` <- DRConnectorV1DetailsObject$`connectorId`
      self$`fieldSchemas` <- ApiClient$new()$deserializeObj(DRConnectorV1DetailsObject$`fieldSchemas`, "array[ConnectorFieldSchema]", loadNamespace("datarobot.apicore"))
      self$`fields` <- ApiClient$new()$deserializeObj(DRConnectorV1DetailsObject$`fields`, "array[DRConnectorV1Field]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
