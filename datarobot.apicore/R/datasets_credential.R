# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetsCredential
#'
#' @description DatasetsCredential Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalogVersionId  character ID of the catalog version
#'
#' @field credentialId  character ID of the credential store to be used for the given catalog version
#'
#' @field url  character [optional] The URL of the datasource
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetsCredential <- R6::R6Class(
  "DatasetsCredential",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(catalogVersionId = NULL, credentialId = NULL, url = NULL) {
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
    }
  ),
  public = list(
    `catalogVersionId` = NULL,
    `credentialId` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalogVersionId ID of the catalog version
    #' @param credentialId ID of the credential store to be used for the given catalog version
    #' @param url The URL of the datasource
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`catalogVersionId` = NULL, `credentialId` = NULL, `url` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`catalogVersionId`, `credentialId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(catalogVersionId, credentialId, url)
      }
      self$`catalogVersionId` <- `catalogVersionId`
      self$`credentialId` <- `credentialId`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(catalogVersionId = self$`catalogVersionId`, credentialId = self$`credentialId`, url = self$`url`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetsCredentialJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetsCredentialJson, validateParams = FALSE) {
      DatasetsCredentialObject <- jsonlite::fromJSON(DatasetsCredentialJson)
      self$`catalogVersionId` <- DatasetsCredentialObject$`catalogVersionId`
      self$`credentialId` <- DatasetsCredentialObject$`credentialId`
      self$`url` <- DatasetsCredentialObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
