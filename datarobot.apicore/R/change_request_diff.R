# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ChangeRequestDiff
#'
#' @description ChangeRequestDiff Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field changesFrom  list( character ) List of human readable messages describing the state of the entity before changes are applied.
#'
#' @field changesTo  list( character ) List of human readable messages describing the state of the entity after changes are applied.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ChangeRequestDiff <- R6::R6Class(
  "ChangeRequestDiff",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`changesFrom` = NULL, `changesTo` = NULL) {
      if (!is.null(`changesFrom`)) {
        stopifnot(is.vector(`changesFrom`))
      }
      if (!is.null(`changesTo`)) {
        stopifnot(is.vector(`changesTo`))
      }
    }
  ),
  public = list(
    `changesFrom` = NULL,
    `changesTo` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param changesFrom List of human readable messages describing the state of the entity before changes are applied.
    #' @param changesTo List of human readable messages describing the state of the entity after changes are applied.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`changesFrom` = NULL, `changesTo` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`changesFrom`, `changesTo`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(changesFrom, changesTo)
      }
      sapply(`changesFrom`, function(x) stopifnot(is.character(x)))
      sapply(`changesTo`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(changesFrom = self$`changesFrom`, changesTo = self$`changesTo`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`changesFrom`)) {
          sprintf(
            '"changesFrom":
            [%s]
                  ',
            paste(unlist(lapply(self$`changesFrom`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`changesTo`)) {
          sprintf(
            '"changesTo":
            [%s]
                  ',
            paste(unlist(lapply(self$`changesTo`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ChangeRequestDiffJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ChangeRequestDiffJson, validateParams = FALSE) {
      ChangeRequestDiffObject <- jsonlite::fromJSON(ChangeRequestDiffJson)
      self$`changesFrom` <- ApiClient$new()$deserializeObj(ChangeRequestDiffObject$`changesFrom`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`changesTo` <- ApiClient$new()$deserializeObj(ChangeRequestDiffObject$`changesTo`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
