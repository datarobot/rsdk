# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionFileUpload
#'
#' @description PredictionFileUpload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualValueColumn  character [optional] Actual value column name, valid for the prediction files if the project is unsupervised and the dataset is considered as bulk predictions dataset.
#'
#' @field credentials  character [optional] A list of credentials for the secondary datasets used in feature discovery project
#'
#' @field file  data.frame The dataset file to upload for prediction.
#'
#' @field forecastPoint  character [optional] For time series projects only. The time in the dataset relative to which predictions are generated. If not specified the default value is the value in the row with the latest specified timestamp. Specifying this value for a project that is not a time series project will result in an error.
#'
#' @field predictionsEndDate  character [optional] Used for time series projects only. The end date for bulk predictions. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsStartDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
#'
#' @field predictionsStartDate  character [optional] Used for time series projects only. The start date for bulk predictions. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsEndDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
#'
#' @field relaxKnownInAdvanceFeaturesCheck  character [optional] A boolean flag. If true, missing values in the known in advance features are allowed in the forecast window at the prediction time. If omitted or false, missing values are not allowed. For time series projects only.
#'
#' @field secondaryDatasetsConfigId  character [optional] Optional, for feature discovery projects only. The Id of the alternative secondary dataset config to use during prediction.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionFileUpload <- R6::R6Class(
  "PredictionFileUpload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(actualValueColumn = NULL, credentials = NULL, file = NULL, forecastPoint = NULL, predictionsEndDate = NULL, predictionsStartDate = NULL, relaxKnownInAdvanceFeaturesCheck = NULL, secondaryDatasetsConfigId = NULL) {
      if (!is.null(`file`)) {
      }
      if (!is.null(`actualValueColumn`)) {
        stopifnot(is.character(`actualValueColumn`), length(`actualValueColumn`) == 1)
      }
      if (!is.null(`credentials`)) {
        stopifnot(is.character(`credentials`), length(`credentials`) == 1)
      }
      if (!is.null(`forecastPoint`)) {
        stopifnot(inherits(`forecastPoint`, "POSIXt"))
      }
      if (!is.null(`predictionsEndDate`)) {
        stopifnot(inherits(`predictionsEndDate`, "POSIXt"))
      }
      if (!is.null(`predictionsStartDate`)) {
        stopifnot(inherits(`predictionsStartDate`, "POSIXt"))
      }
      if (!is.null(`relaxKnownInAdvanceFeaturesCheck`)) {
        stopifnot(is.character(`relaxKnownInAdvanceFeaturesCheck`), length(`relaxKnownInAdvanceFeaturesCheck`) == 1)
      }
      if (!is.null(`secondaryDatasetsConfigId`)) {
        stopifnot(is.character(`secondaryDatasetsConfigId`), length(`secondaryDatasetsConfigId`) == 1)
      }
    }
  ),
  public = list(
    `actualValueColumn` = NULL,
    `credentials` = NULL,
    `file` = NULL,
    `forecastPoint` = NULL,
    `predictionsEndDate` = NULL,
    `predictionsStartDate` = NULL,
    `relaxKnownInAdvanceFeaturesCheck` = NULL,
    `secondaryDatasetsConfigId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualValueColumn Actual value column name, valid for the prediction files if the project is unsupervised and the dataset is considered as bulk predictions dataset.
    #' @param credentials A list of credentials for the secondary datasets used in feature discovery project
    #' @param file The dataset file to upload for prediction.
    #' @param forecastPoint For time series projects only. The time in the dataset relative to which predictions are generated. If not specified the default value is the value in the row with the latest specified timestamp. Specifying this value for a project that is not a time series project will result in an error.
    #' @param predictionsEndDate Used for time series projects only. The end date for bulk predictions. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsStartDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
    #' @param predictionsStartDate Used for time series projects only. The start date for bulk predictions. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsEndDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
    #' @param relaxKnownInAdvanceFeaturesCheck A boolean flag. If true, missing values in the known in advance features are allowed in the forecast window at the prediction time. If omitted or false, missing values are not allowed. For time series projects only.
    #' @param secondaryDatasetsConfigId Optional, for feature discovery projects only. The Id of the alternative secondary dataset config to use during prediction.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`file` = NULL, `actualValueColumn` = NULL, `credentials` = NULL, `forecastPoint` = NULL, `predictionsEndDate` = NULL, `predictionsStartDate` = NULL, `relaxKnownInAdvanceFeaturesCheck` = NULL, `secondaryDatasetsConfigId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`file`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(actualValueColumn, credentials, file, forecastPoint, predictionsEndDate, predictionsStartDate, relaxKnownInAdvanceFeaturesCheck, secondaryDatasetsConfigId)
      }
      self$`actualValueColumn` <- `actualValueColumn`
      self$`credentials` <- `credentials`
      self$`file` <- `file`
      self$`forecastPoint` <- `forecastPoint`
      self$`predictionsEndDate` <- `predictionsEndDate`
      self$`predictionsStartDate` <- `predictionsStartDate`
      self$`relaxKnownInAdvanceFeaturesCheck` <- `relaxKnownInAdvanceFeaturesCheck`
      self$`secondaryDatasetsConfigId` <- `secondaryDatasetsConfigId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(actualValueColumn = self$`actualValueColumn`, credentials = self$`credentials`, file = self$`file`, forecastPoint = self$`forecastPoint`, predictionsEndDate = self$`predictionsEndDate`, predictionsStartDate = self$`predictionsStartDate`, relaxKnownInAdvanceFeaturesCheck = self$`relaxKnownInAdvanceFeaturesCheck`, secondaryDatasetsConfigId = self$`secondaryDatasetsConfigId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualValueColumn`)) {
          sprintf(
            '"actualValueColumn":
            "%s"
                  ',
            self$`actualValueColumn`
          )
        },
        if (!is.null(self$`credentials`)) {
          sprintf(
            '"credentials":
            "%s"
                  ',
            self$`credentials`
          )
        },
        if (!is.null(self$`file`)) {
          sprintf(
            '"file":
            "%s"
                  ',
            self$`file`
          )
        },
        if (!is.null(self$`forecastPoint`)) {
          sprintf(
            '"forecastPoint":
            "%s"
                  ',
            format(self$`forecastPoint`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`predictionsEndDate`)) {
          sprintf(
            '"predictionsEndDate":
            "%s"
                  ',
            format(self$`predictionsEndDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`predictionsStartDate`)) {
          sprintf(
            '"predictionsStartDate":
            "%s"
                  ',
            format(self$`predictionsStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`relaxKnownInAdvanceFeaturesCheck`)) {
          sprintf(
            '"relaxKnownInAdvanceFeaturesCheck":
            "%s"
                  ',
            self$`relaxKnownInAdvanceFeaturesCheck`
          )
        },
        if (!is.null(self$`secondaryDatasetsConfigId`)) {
          sprintf(
            '"secondaryDatasetsConfigId":
            "%s"
                  ',
            self$`secondaryDatasetsConfigId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionFileUploadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionFileUploadJson, validateParams = FALSE) {
      PredictionFileUploadObject <- jsonlite::fromJSON(PredictionFileUploadJson)
      self$`actualValueColumn` <- PredictionFileUploadObject$`actualValueColumn`
      self$`credentials` <- PredictionFileUploadObject$`credentials`
      self$`file` <- PredictionFileUploadObject$`file`
      self$`forecastPoint` <- ParseRFC3339Timestamp(PredictionFileUploadObject$`forecastPoint`)
      self$`predictionsEndDate` <- ParseRFC3339Timestamp(PredictionFileUploadObject$`predictionsEndDate`)
      self$`predictionsStartDate` <- ParseRFC3339Timestamp(PredictionFileUploadObject$`predictionsStartDate`)
      self$`relaxKnownInAdvanceFeaturesCheck` <- PredictionFileUploadObject$`relaxKnownInAdvanceFeaturesCheck`
      self$`secondaryDatasetsConfigId` <- PredictionFileUploadObject$`secondaryDatasetsConfigId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
