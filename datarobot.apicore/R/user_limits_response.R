# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserLimitsResponse
#'
#' @description UserLimitsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accountLimits  \link{AccountLimits}
#'
#' @field rateLimits  list( \link{RateLimits} ) The rate limits for the user that are available to be updated
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserLimitsResponse <- R6::R6Class(
  "UserLimitsResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(accountLimits = NULL, rateLimits = NULL) {
      if (!is.null(`accountLimits`)) {
        stopifnot(R6::is.R6(`accountLimits`))
      }
      if (!is.null(`rateLimits`)) {
        stopifnot(is.vector(`rateLimits`))
      }
    }
  ),
  public = list(
    `accountLimits` = NULL,
    `rateLimits` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accountLimits
    #' @param rateLimits The rate limits for the user that are available to be updated
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`accountLimits` = NULL, `rateLimits` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`accountLimits`, `rateLimits`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(accountLimits, rateLimits)
      }
      self$`accountLimits` <- `accountLimits`
      sapply(`rateLimits`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(accountLimits = self$`accountLimits`, rateLimits = self$`rateLimits`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accountLimits`)) {
          sprintf(
            '"accountLimits":
          %s
        ',
            jsonlite::toJSON(self$`accountLimits`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`rateLimits`)) {
          sprintf(
            '"rateLimits":
            [%s]
',
            paste(sapply(self$`rateLimits`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserLimitsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserLimitsResponseJson, validateParams = FALSE) {
      UserLimitsResponseObject <- jsonlite::fromJSON(UserLimitsResponseJson)
      self$`accountLimits` <- AccountLimits$new()$fromJSON(jsonlite::toJSON(UserLimitsResponseObject$accountLimits, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`rateLimits` <- ApiClient$new()$deserializeObj(UserLimitsResponseObject$`rateLimits`, "array[RateLimits]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
