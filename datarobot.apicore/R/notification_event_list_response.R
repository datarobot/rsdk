# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotificationEventListResponse
#'
#' @description NotificationEventListResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field eventGroups  list( \link{EventGroupResponse} ) Selectable event groups
#'
#' @field events  list( \link{EventResponse} ) Selectable individual events
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotificationEventListResponse <- R6::R6Class(
  "NotificationEventListResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`eventGroups` = NULL, `events` = NULL) {
      if (!is.null(`eventGroups`)) {
        stopifnot(is.vector(`eventGroups`), sapply(`eventGroups`, R6::is.R6))
      }
      if (!is.null(`events`)) {
        stopifnot(is.vector(`events`), sapply(`events`, R6::is.R6))
      }
    }
  ),
  public = list(
    `eventGroups` = NULL,
    `events` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param eventGroups Selectable event groups
    #' @param events Selectable individual events
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`eventGroups` = NULL, `events` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`eventGroups`, `events`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(eventGroups, events)
      }
      self$`eventGroups` <- `eventGroups`
      self$`events` <- `events`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(eventGroups = self$`eventGroups`, events = self$`events`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`eventGroups`)) {
          sprintf(
            '"eventGroups":
            [%s]
      ',
            paste(sapply(self$`eventGroups`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`events`)) {
          sprintf(
            '"events":
            [%s]
      ',
            paste(sapply(self$`events`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotificationEventListResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotificationEventListResponseJson, validateParams = FALSE) {
      NotificationEventListResponseObject <- jsonlite::fromJSON(NotificationEventListResponseJson)
      self$`eventGroups` <- ApiClient$new()$deserializeObj(NotificationEventListResponseObject$`eventGroups`, "array[EventGroupResponse]", loadNamespace("datarobot.apicore"))
      self$`events` <- ApiClient$new()$deserializeObj(NotificationEventListResponseObject$`events`, "array[EventResponse]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
