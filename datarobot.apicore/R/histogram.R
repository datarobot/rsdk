# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Histogram
#'
#' @description Histogram Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field intervalEnd  numeric The end of the interval.
#'
#' @field intervalStart  numeric The start of the interval.
#'
#' @field occurrences  integer The number of times the predicted value fits within that interval.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Histogram <- R6::R6Class(
  "Histogram",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`intervalEnd` = NULL, `intervalStart` = NULL, `occurrences` = NULL) {
      if (!is.null(`intervalEnd`)) {
      }
      if (!is.null(`intervalStart`)) {
      }
      if (!is.null(`occurrences`)) {
        stopifnot(is.numeric(`occurrences`), length(`occurrences`) == 1)
      }
    }
  ),
  public = list(
    `intervalEnd` = NULL,
    `intervalStart` = NULL,
    `occurrences` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param intervalEnd The end of the interval.
    #' @param intervalStart The start of the interval.
    #' @param occurrences The number of times the predicted value fits within that interval.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`intervalEnd` = NULL, `intervalStart` = NULL, `occurrences` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`intervalEnd`, `intervalStart`, `occurrences`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(intervalEnd, intervalStart, occurrences)
      }
      self$`intervalEnd` <- `intervalEnd`
      self$`intervalStart` <- `intervalStart`
      self$`occurrences` <- `occurrences`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(intervalEnd = self$`intervalEnd`, intervalStart = self$`intervalStart`, occurrences = self$`occurrences`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`intervalEnd`)) {
          sprintf(
            '"intervalEnd":
            %d
                  ',
            self$`intervalEnd`
          )
        },
        if (!is.null(self$`intervalStart`)) {
          sprintf(
            '"intervalStart":
            %d
                  ',
            self$`intervalStart`
          )
        },
        if (!is.null(self$`occurrences`)) {
          sprintf(
            '"occurrences":
            %d
                  ',
            self$`occurrences`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param HistogramJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(HistogramJson, validateParams = FALSE) {
      HistogramObject <- jsonlite::fromJSON(HistogramJson)
      self$`intervalEnd` <- HistogramObject$`intervalEnd`
      self$`intervalStart` <- HistogramObject$`intervalStart`
      self$`occurrences` <- HistogramObject$`occurrences`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
