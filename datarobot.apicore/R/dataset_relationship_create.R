# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetRelationshipCreate
#'
#' @description DatasetRelationshipCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field linkedDatasetId  character The ID of another dataset with which to create relationships.
#'
#' @field linkedFeatures  list( character ) List of features belonging to the linked dataset.
#'
#' @field sourceFeatures  list( character ) List of features belonging to the source dataset.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetRelationshipCreate <- R6::R6Class(
  "DatasetRelationshipCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`linkedDatasetId` = NULL, `linkedFeatures` = NULL, `sourceFeatures` = NULL) {
      if (!is.null(`linkedDatasetId`)) {
        stopifnot(is.character(`linkedDatasetId`), length(`linkedDatasetId`) == 1)
      }
      if (!is.null(`linkedFeatures`)) {
        stopifnot(is.vector(`linkedFeatures`), sapply(`linkedFeatures`, is.character))
      }
      if (!is.null(`sourceFeatures`)) {
        stopifnot(is.vector(`sourceFeatures`), sapply(`sourceFeatures`, is.character))
      }
    }
  ),
  public = list(
    `linkedDatasetId` = NULL,
    `linkedFeatures` = NULL,
    `sourceFeatures` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param linkedDatasetId The ID of another dataset with which to create relationships.
    #' @param linkedFeatures List of features belonging to the linked dataset.
    #' @param sourceFeatures List of features belonging to the source dataset.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`linkedDatasetId` = NULL, `linkedFeatures` = NULL, `sourceFeatures` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`linkedDatasetId`, `linkedFeatures`, `sourceFeatures`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(linkedDatasetId, linkedFeatures, sourceFeatures)
      }
      self$`linkedDatasetId` <- `linkedDatasetId`
      self$`linkedFeatures` <- `linkedFeatures`
      self$`sourceFeatures` <- `sourceFeatures`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(linkedDatasetId = self$`linkedDatasetId`, linkedFeatures = self$`linkedFeatures`, sourceFeatures = self$`sourceFeatures`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`linkedDatasetId`)) {
          sprintf(
            '"linkedDatasetId":
            "%s"
                  ',
            self$`linkedDatasetId`
          )
        },
        if (!is.null(self$`linkedFeatures`)) {
          sprintf(
            '"linkedFeatures":
            [%s]
                  ',
            paste(unlist(lapply(self$`linkedFeatures`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`sourceFeatures`)) {
          sprintf(
            '"sourceFeatures":
            [%s]
                  ',
            paste(unlist(lapply(self$`sourceFeatures`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetRelationshipCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetRelationshipCreateJson, validateParams = FALSE) {
      DatasetRelationshipCreateObject <- jsonlite::fromJSON(DatasetRelationshipCreateJson)
      self$`linkedDatasetId` <- DatasetRelationshipCreateObject$`linkedDatasetId`
      self$`linkedFeatures` <- ApiClient$new()$deserializeObj(DatasetRelationshipCreateObject$`linkedFeatures`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`sourceFeatures` <- ApiClient$new()$deserializeObj(DatasetRelationshipCreateObject$`sourceFeatures`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
