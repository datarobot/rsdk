# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title OrganizationJob
#'
#' @description OrganizationJob Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field jobId  character The identifier of the submitted job (unique within a project)
#'
#' @field projectId  character The project identifier associated with this job
#'
#' @field projectOwnerUserId  character Identifier the user that owns the project associated with the job
#'
#' @field projectOwnerUsername  character The username of the user that owns the project associated with the job
#'
#' @field userId  character Identifies the user that submitted the job
#'
#' @field username  character The username of the user that submitted the job
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
OrganizationJob <- R6::R6Class(
  "OrganizationJob",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`jobId` = NULL, `projectId` = NULL, `projectOwnerUserId` = NULL, `projectOwnerUsername` = NULL, `userId` = NULL, `username` = NULL) {
      if (!is.null(`jobId`)) {
        stopifnot(is.character(`jobId`), length(`jobId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`projectOwnerUserId`)) {
        stopifnot(is.character(`projectOwnerUserId`), length(`projectOwnerUserId`) == 1)
      }
      if (!is.null(`projectOwnerUsername`)) {
        stopifnot(is.character(`projectOwnerUsername`), length(`projectOwnerUsername`) == 1)
      }
      if (!is.null(`userId`)) {
        stopifnot(is.character(`userId`), length(`userId`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
    }
  ),
  public = list(
    `jobId` = NULL,
    `projectId` = NULL,
    `projectOwnerUserId` = NULL,
    `projectOwnerUsername` = NULL,
    `userId` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param jobId The identifier of the submitted job (unique within a project)
    #' @param projectId The project identifier associated with this job
    #' @param projectOwnerUserId Identifier the user that owns the project associated with the job
    #' @param projectOwnerUsername The username of the user that owns the project associated with the job
    #' @param userId Identifies the user that submitted the job
    #' @param username The username of the user that submitted the job
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`jobId` = NULL, `projectId` = NULL, `projectOwnerUserId` = NULL, `projectOwnerUsername` = NULL, `userId` = NULL, `username` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`jobId`, `projectId`, `projectOwnerUserId`, `projectOwnerUsername`, `userId`, `username`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(jobId, projectId, projectOwnerUserId, projectOwnerUsername, userId, username)
      }
      self$`jobId` <- `jobId`
      self$`projectId` <- `projectId`
      self$`projectOwnerUserId` <- `projectOwnerUserId`
      self$`projectOwnerUsername` <- `projectOwnerUsername`
      self$`userId` <- `userId`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(jobId = self$`jobId`, projectId = self$`projectId`, projectOwnerUserId = self$`projectOwnerUserId`, projectOwnerUsername = self$`projectOwnerUsername`, userId = self$`userId`, username = self$`username`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`jobId`)) {
          sprintf(
            '"jobId":
            "%s"
                  ',
            self$`jobId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`projectOwnerUserId`)) {
          sprintf(
            '"projectOwnerUserId":
            "%s"
                  ',
            self$`projectOwnerUserId`
          )
        },
        if (!is.null(self$`projectOwnerUsername`)) {
          sprintf(
            '"projectOwnerUsername":
            "%s"
                  ',
            self$`projectOwnerUsername`
          )
        },
        if (!is.null(self$`userId`)) {
          sprintf(
            '"userId":
            "%s"
                  ',
            self$`userId`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param OrganizationJobJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(OrganizationJobJson, validateParams = FALSE) {
      OrganizationJobObject <- jsonlite::fromJSON(OrganizationJobJson)
      self$`jobId` <- OrganizationJobObject$`jobId`
      self$`projectId` <- OrganizationJobObject$`projectId`
      self$`projectOwnerUserId` <- OrganizationJobObject$`projectOwnerUserId`
      self$`projectOwnerUsername` <- OrganizationJobObject$`projectOwnerUsername`
      self$`userId` <- OrganizationJobObject$`userId`
      self$`username` <- OrganizationJobObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
