# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictedFrequency
#'
#' @description PredictedFrequency Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field otherClassName  character The name of the class.
#'
#' @field percentage  numeric the percentage of the times this class was actual when &#x60;classMetrics.className&#x60; is predicted (from 0 to 100)
#'
#' @field value  integer The count of the times this class was actual &#x60;classMetrics.className&#x60; when is was predicted
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictedFrequency <- R6::R6Class(
  "PredictedFrequency",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`otherClassName` = NULL, `percentage` = NULL, `value` = NULL) {
      if (!is.null(`otherClassName`)) {
        stopifnot(is.character(`otherClassName`), length(`otherClassName`) == 1)
      }
      if (!is.null(`percentage`)) {
      }
      if (!is.null(`value`)) {
        stopifnot(is.numeric(`value`), length(`value`) == 1)
      }
    }
  ),
  public = list(
    `otherClassName` = NULL,
    `percentage` = NULL,
    `value` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param otherClassName The name of the class.
    #' @param percentage the percentage of the times this class was actual when &#x60;classMetrics.className&#x60; is predicted (from 0 to 100)
    #' @param value The count of the times this class was actual &#x60;classMetrics.className&#x60; when is was predicted
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`otherClassName` = NULL, `percentage` = NULL, `value` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`otherClassName`, `percentage`, `value`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(otherClassName, percentage, value)
      }
      self$`otherClassName` <- `otherClassName`
      self$`percentage` <- `percentage`
      self$`value` <- `value`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(otherClassName = self$`otherClassName`, percentage = self$`percentage`, value = self$`value`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`otherClassName`)) {
          sprintf(
            '"otherClassName":
            "%s"
                  ',
            self$`otherClassName`
          )
        },
        if (!is.null(self$`percentage`)) {
          sprintf(
            '"percentage":
            %d
                  ',
            self$`percentage`
          )
        },
        if (!is.null(self$`value`)) {
          sprintf(
            '"value":
            %d
                  ',
            self$`value`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictedFrequencyJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictedFrequencyJson, validateParams = FALSE) {
      PredictedFrequencyObject <- jsonlite::fromJSON(PredictedFrequencyJson)
      self$`otherClassName` <- PredictedFrequencyObject$`otherClassName`
      self$`percentage` <- PredictedFrequencyObject$`percentage`
      self$`value` <- PredictedFrequencyObject$`value`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
