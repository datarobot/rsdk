# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseAttachmentCustomModelOverview
#'
#' @description UseCaseAttachmentCustomModelOverview Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field createdAt  character Time when custom model was created
#'
#' @field modifiedAt  character Time of the last modification
#'
#' @field name  character Name of the custom model
#'
#' @field owner  character Name of the custom model owner
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseAttachmentCustomModelOverview <- R6::R6Class(
  "UseCaseAttachmentCustomModelOverview",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`createdAt` = NULL, `modifiedAt` = NULL, `name` = NULL, `owner` = NULL) {
      if (!is.null(`createdAt`)) {
        stopifnot(inherits(`createdAt`, "POSIXt"))
      }
      if (!is.null(`modifiedAt`)) {
        stopifnot(inherits(`modifiedAt`, "POSIXt"))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`owner`)) {
        stopifnot(is.character(`owner`), length(`owner`) == 1)
      }
    }
  ),
  public = list(
    `createdAt` = NULL,
    `modifiedAt` = NULL,
    `name` = NULL,
    `owner` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param createdAt Time when custom model was created
    #' @param modifiedAt Time of the last modification
    #' @param name Name of the custom model
    #' @param owner Name of the custom model owner
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`createdAt` = NULL, `modifiedAt` = NULL, `name` = NULL, `owner` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`createdAt`, `modifiedAt`, `name`, `owner`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(createdAt, modifiedAt, name, owner)
      }
      self$`createdAt` <- `createdAt`
      self$`modifiedAt` <- `modifiedAt`
      self$`name` <- `name`
      self$`owner` <- `owner`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(createdAt = self$`createdAt`, modifiedAt = self$`modifiedAt`, name = self$`name`, owner = self$`owner`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`createdAt`)) {
          sprintf(
            '"createdAt":
            "%s"
                  ',
            format(self$`createdAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`modifiedAt`)) {
          sprintf(
            '"modifiedAt":
            "%s"
                  ',
            format(self$`modifiedAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`owner`)) {
          sprintf(
            '"owner":
            "%s"
                  ',
            self$`owner`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseAttachmentCustomModelOverviewJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseAttachmentCustomModelOverviewJson, validateParams = FALSE) {
      UseCaseAttachmentCustomModelOverviewObject <- jsonlite::fromJSON(UseCaseAttachmentCustomModelOverviewJson)
      self$`createdAt` <- ParseRFC3339Timestamp(UseCaseAttachmentCustomModelOverviewObject$`createdAt`)
      self$`modifiedAt` <- ParseRFC3339Timestamp(UseCaseAttachmentCustomModelOverviewObject$`modifiedAt`)
      self$`name` <- UseCaseAttachmentCustomModelOverviewObject$`name`
      self$`owner` <- UseCaseAttachmentCustomModelOverviewObject$`owner`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
