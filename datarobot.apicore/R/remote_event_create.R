# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RemoteEventCreate
#'
#' @description RemoteEventCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field data  \link{RemoteEventData} [optional]
#'
#' @field deploymentId  character [optional] The identifier of the deployment associated with the event.
#'
#' @field eventType  character The type of the event. Labels in all_lower_case are deprecated.
#'
#' @field message  character [optional] Descriptive message for health events.
#'
#' @field orgId  character [optional] The identifier of the organization associated with the event.
#'
#' @field predictionEnvironmentId  character [optional] The identifier of the prediction environment associated with the event.
#'
#' @field predictionRequestData  \link{PredictionRemoteEventData} [optional]
#'
#' @field spoolerChannelData  \link{SpoolerChannelEventData} [optional]
#'
#' @field timestamp  character The time when the event occurred.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RemoteEventCreate <- R6::R6Class(
  "RemoteEventCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`data` = NULL, `deploymentId` = NULL, `eventType` = NULL, `message` = NULL, `orgId` = NULL, `predictionEnvironmentId` = NULL, `predictionRequestData` = NULL, `spoolerChannelData` = NULL, `timestamp` = NULL) {
      if (!is.null(`eventType`)) {
        stopifnot(is.character(`eventType`), length(`eventType`) == 1)
      }
      if (!is.null(`timestamp`)) {
        stopifnot(inherits(`timestamp`, "POSIXt"))
      }
      if (!is.null(`data`)) {
        stopifnot(R6::is.R6(`data`))
      }
      if (!is.null(`deploymentId`)) {
        stopifnot(is.character(`deploymentId`), length(`deploymentId`) == 1)
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
      if (!is.null(`predictionEnvironmentId`)) {
        stopifnot(is.character(`predictionEnvironmentId`), length(`predictionEnvironmentId`) == 1)
      }
      if (!is.null(`predictionRequestData`)) {
        stopifnot(R6::is.R6(`predictionRequestData`))
      }
      if (!is.null(`spoolerChannelData`)) {
        stopifnot(R6::is.R6(`spoolerChannelData`))
      }
    }
  ),
  public = list(
    `data` = NULL,
    `deploymentId` = NULL,
    `eventType` = NULL,
    `message` = NULL,
    `orgId` = NULL,
    `predictionEnvironmentId` = NULL,
    `predictionRequestData` = NULL,
    `spoolerChannelData` = NULL,
    `timestamp` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param data
    #' @param deploymentId The identifier of the deployment associated with the event.
    #' @param eventType The type of the event. Labels in all_lower_case are deprecated.
    #' @param message Descriptive message for health events.
    #' @param orgId The identifier of the organization associated with the event.
    #' @param predictionEnvironmentId The identifier of the prediction environment associated with the event.
    #' @param predictionRequestData
    #' @param spoolerChannelData
    #' @param timestamp The time when the event occurred.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`eventType` = NULL, `timestamp` = NULL, `data` = NULL, `deploymentId` = NULL, `message` = NULL, `orgId` = NULL, `predictionEnvironmentId` = NULL, `predictionRequestData` = NULL, `spoolerChannelData` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`eventType`, `timestamp`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(data, deploymentId, eventType, message, orgId, predictionEnvironmentId, predictionRequestData, spoolerChannelData, timestamp)
      }
      self$`data` <- `data`
      self$`deploymentId` <- `deploymentId`
      self$`eventType` <- `eventType`
      self$`message` <- `message`
      self$`orgId` <- `orgId`
      self$`predictionEnvironmentId` <- `predictionEnvironmentId`
      self$`predictionRequestData` <- `predictionRequestData`
      self$`spoolerChannelData` <- `spoolerChannelData`
      self$`timestamp` <- `timestamp`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(data = self$`data`, deploymentId = self$`deploymentId`, eventType = self$`eventType`, message = self$`message`, orgId = self$`orgId`, predictionEnvironmentId = self$`predictionEnvironmentId`, predictionRequestData = self$`predictionRequestData`, spoolerChannelData = self$`spoolerChannelData`, timestamp = self$`timestamp`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            %s
      ',
            jsonlite::toJSON(self$`data`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`deploymentId`)) {
          sprintf(
            '"deploymentId":
            "%s"
                  ',
            self$`deploymentId`
          )
        },
        if (!is.null(self$`eventType`)) {
          sprintf(
            '"eventType":
            "%s"
                  ',
            self$`eventType`
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        },
        if (!is.null(self$`predictionEnvironmentId`)) {
          sprintf(
            '"predictionEnvironmentId":
            "%s"
                  ',
            self$`predictionEnvironmentId`
          )
        },
        if (!is.null(self$`predictionRequestData`)) {
          sprintf(
            '"predictionRequestData":
            %s
      ',
            jsonlite::toJSON(self$`predictionRequestData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`spoolerChannelData`)) {
          sprintf(
            '"spoolerChannelData":
            %s
      ',
            jsonlite::toJSON(self$`spoolerChannelData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`timestamp`)) {
          sprintf(
            '"timestamp":
            "%s"
                  ',
            format(self$`timestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RemoteEventCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RemoteEventCreateJson, validateParams = FALSE) {
      RemoteEventCreateObject <- jsonlite::fromJSON(RemoteEventCreateJson)
      self$`data` <- RemoteEventData$new()$fromJSON(jsonlite::toJSON(RemoteEventCreateObject$data, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`deploymentId` <- RemoteEventCreateObject$`deploymentId`
      self$`eventType` <- RemoteEventCreateObject$`eventType`
      self$`message` <- RemoteEventCreateObject$`message`
      self$`orgId` <- RemoteEventCreateObject$`orgId`
      self$`predictionEnvironmentId` <- RemoteEventCreateObject$`predictionEnvironmentId`
      self$`predictionRequestData` <- PredictionRemoteEventData$new()$fromJSON(jsonlite::toJSON(RemoteEventCreateObject$predictionRequestData, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`spoolerChannelData` <- SpoolerChannelEventData$new()$fromJSON(jsonlite::toJSON(RemoteEventCreateObject$spoolerChannelData, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`timestamp` <- ParseRFC3339Timestamp(RemoteEventCreateObject$`timestamp`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
