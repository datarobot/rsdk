# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AssociationID
#'
#' @description AssociationID Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field columnNames  list( character ) Indicates the columns that will be used to associate actuals with predictions.
#'
#' @field requiredInPredictionRequests  character Indicates whether the association ID is required in prediction requests.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AssociationID <- R6::R6Class(
  "AssociationID",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(columnNames = NULL, requiredInPredictionRequests = NULL) {
      if (!is.null(`columnNames`)) {
        stopifnot(is.vector(`columnNames`))
      }
      if (!is.null(`requiredInPredictionRequests`)) {
        stopifnot(is.logical(`requiredInPredictionRequests`), length(`requiredInPredictionRequests`) == 1)
      }
    }
  ),
  public = list(
    `columnNames` = NULL,
    `requiredInPredictionRequests` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param columnNames Indicates the columns that will be used to associate actuals with predictions.
    #' @param requiredInPredictionRequests Indicates whether the association ID is required in prediction requests.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`columnNames` = NULL, `requiredInPredictionRequests` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`columnNames`, `requiredInPredictionRequests`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(columnNames, requiredInPredictionRequests)
      }
      sapply(`columnNames`, function(x) stopifnot(is.character(x)))
      self$`requiredInPredictionRequests` <- `requiredInPredictionRequests`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(columnNames = self$`columnNames`, requiredInPredictionRequests = self$`requiredInPredictionRequests`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`columnNames`)) {
          sprintf(
            '"columnNames":
              [%s]
            ',
            paste(unlist(lapply(self$`columnNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`requiredInPredictionRequests`)) {
          sprintf(
            '"requiredInPredictionRequests":
            %s
                  ',
            tolower(self$`requiredInPredictionRequests`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AssociationIDJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AssociationIDJson, validateParams = FALSE) {
      AssociationIDObject <- jsonlite::fromJSON(AssociationIDJson)
      self$`columnNames` <- ApiClient$new()$deserializeObj(AssociationIDObject$`columnNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`requiredInPredictionRequests` <- AssociationIDObject$`requiredInPredictionRequests`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
