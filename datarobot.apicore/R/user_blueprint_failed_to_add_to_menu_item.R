# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintFailedToAddToMenuItem
#'
#' @description UserBlueprintFailedToAddToMenuItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field error  character The error message representing why the blueprint was not added to the project repository.
#'
#' @field userBlueprintId  character The userBlueprintId associated with the blueprint which was not added to the project repository.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintFailedToAddToMenuItem <- R6::R6Class(
  "UserBlueprintFailedToAddToMenuItem",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(error = NULL, userBlueprintId = NULL) {
      if (!is.null(`error`)) {
        stopifnot(is.character(`error`), length(`error`) == 1)
      }
      if (!is.null(`userBlueprintId`)) {
        stopifnot(is.character(`userBlueprintId`), length(`userBlueprintId`) == 1)
      }
    }
  ),
  public = list(
    `error` = NULL,
    `userBlueprintId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param error The error message representing why the blueprint was not added to the project repository.
    #' @param userBlueprintId The userBlueprintId associated with the blueprint which was not added to the project repository.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`error` = NULL, `userBlueprintId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`error`, `userBlueprintId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(error, userBlueprintId)
      }
      self$`error` <- `error`
      self$`userBlueprintId` <- `userBlueprintId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(error = self$`error`, userBlueprintId = self$`userBlueprintId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`error`)) {
          sprintf(
            '"error":
            "%s"
                  ',
            self$`error`
          )
        },
        if (!is.null(self$`userBlueprintId`)) {
          sprintf(
            '"userBlueprintId":
            "%s"
                  ',
            self$`userBlueprintId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintFailedToAddToMenuItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintFailedToAddToMenuItemJson, validateParams = FALSE) {
      UserBlueprintFailedToAddToMenuItemObject <- jsonlite::fromJSON(UserBlueprintFailedToAddToMenuItemJson)
      self$`error` <- UserBlueprintFailedToAddToMenuItemObject$`error`
      self$`userBlueprintId` <- UserBlueprintFailedToAddToMenuItemObject$`userBlueprintId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
