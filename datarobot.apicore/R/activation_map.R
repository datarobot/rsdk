# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ActivationMap
#'
#' @description ActivationMap Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field activationValues  list( \link{array[integer]} ) A 2D matrix of values (row-major) representing the activation strengths for particular image regions.
#'
#' @field actualTargetValue  \link{OneOfstringinteger} Actual target value of the dataset row.
#'
#' @field featureName  character The name of the column containing the image value the activation map is based upon.
#'
#' @field imageHeight  integer The height of the original image (in pixels) this activation map has been computed for.
#'
#' @field imageId  character ID of the original image this activation map has been computed for.
#'
#' @field imageWidth  integer The width of the original image (in pixels) this activation map has been computed for.
#'
#' @field links  \link{ActivationMapLinks}
#'
#' @field overlayImageId  character ID of the image containing the original image overlaid by the activation heatmap.
#'
#' @field predictedTargetValue  \link{OneOfstringinteger} predicted target value of the dataset row containing this image.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ActivationMap <- R6::R6Class(
  "ActivationMap",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`activationValues` = NULL, `actualTargetValue` = NULL, `featureName` = NULL, `imageHeight` = NULL, `imageId` = NULL, `imageWidth` = NULL, `links` = NULL, `overlayImageId` = NULL, `predictedTargetValue` = NULL) {
      if (!is.null(`activationValues`)) {
        stopifnot(is.vector(`activationValues`), sapply(`activationValues`, R6::is.R6))
      }
      if (!is.null(`actualTargetValue`)) {
        .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = actualTargetValue)
      }
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`imageHeight`)) {
        stopifnot(is.numeric(`imageHeight`), length(`imageHeight`) == 1)
      }
      if (!is.null(`imageId`)) {
        stopifnot(is.character(`imageId`), length(`imageId`) == 1)
      }
      if (!is.null(`imageWidth`)) {
        stopifnot(is.numeric(`imageWidth`), length(`imageWidth`) == 1)
      }
      if (!is.null(`links`)) {
        stopifnot(R6::is.R6(`links`))
      }
      if (!is.null(`overlayImageId`)) {
        stopifnot(is.character(`overlayImageId`), length(`overlayImageId`) == 1)
      }
      if (!is.null(`predictedTargetValue`)) {
        .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = predictedTargetValue)
      }
    }
  ),
  public = list(
    `activationValues` = NULL,
    `actualTargetValue` = NULL,
    `featureName` = NULL,
    `imageHeight` = NULL,
    `imageId` = NULL,
    `imageWidth` = NULL,
    `links` = NULL,
    `overlayImageId` = NULL,
    `predictedTargetValue` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param activationValues A 2D matrix of values (row-major) representing the activation strengths for particular image regions.
    #' @param actualTargetValue Actual target value of the dataset row.
    #' @param featureName The name of the column containing the image value the activation map is based upon.
    #' @param imageHeight The height of the original image (in pixels) this activation map has been computed for.
    #' @param imageId ID of the original image this activation map has been computed for.
    #' @param imageWidth The width of the original image (in pixels) this activation map has been computed for.
    #' @param links
    #' @param overlayImageId ID of the image containing the original image overlaid by the activation heatmap.
    #' @param predictedTargetValue predicted target value of the dataset row containing this image.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`activationValues` = NULL, `actualTargetValue` = NULL, `featureName` = NULL, `imageHeight` = NULL, `imageId` = NULL, `imageWidth` = NULL, `links` = NULL, `overlayImageId` = NULL, `predictedTargetValue` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`activationValues`, `actualTargetValue`, `featureName`, `imageHeight`, `imageId`, `imageWidth`, `links`, `overlayImageId`, `predictedTargetValue`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(activationValues, actualTargetValue, featureName, imageHeight, imageId, imageWidth, links, overlayImageId, predictedTargetValue)
      }
      self$`activationValues` <- `activationValues`
      self$`actualTargetValue` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = actualTargetValue)
      self$`featureName` <- `featureName`
      self$`imageHeight` <- `imageHeight`
      self$`imageId` <- `imageId`
      self$`imageWidth` <- `imageWidth`
      self$`links` <- `links`
      self$`overlayImageId` <- `overlayImageId`
      self$`predictedTargetValue` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = predictedTargetValue)
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(activationValues = self$`activationValues`, actualTargetValue = self$`actualTargetValue`, featureName = self$`featureName`, imageHeight = self$`imageHeight`, imageId = self$`imageId`, imageWidth = self$`imageWidth`, links = self$`links`, overlayImageId = self$`overlayImageId`, predictedTargetValue = self$`predictedTargetValue`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`activationValues`)) {
          sprintf(
            '"activationValues":
            [%s]
      ',
            paste(sapply(self$`activationValues`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`actualTargetValue`)) {
          sprintf(
            '"actualTargetValue":
            %s
      ',
            self$`actualTargetValue`
          )
        },
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`imageHeight`)) {
          sprintf(
            '"imageHeight":
            %d
                  ',
            self$`imageHeight`
          )
        },
        if (!is.null(self$`imageId`)) {
          sprintf(
            '"imageId":
            "%s"
                  ',
            self$`imageId`
          )
        },
        if (!is.null(self$`imageWidth`)) {
          sprintf(
            '"imageWidth":
            %d
                  ',
            self$`imageWidth`
          )
        },
        if (!is.null(self$`links`)) {
          sprintf(
            '"links":
            %s
      ',
            jsonlite::toJSON(self$`links`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`overlayImageId`)) {
          sprintf(
            '"overlayImageId":
            "%s"
                  ',
            self$`overlayImageId`
          )
        },
        if (!is.null(self$`predictedTargetValue`)) {
          sprintf(
            '"predictedTargetValue":
            %s
      ',
            self$`predictedTargetValue`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ActivationMapJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ActivationMapJson, validateParams = FALSE) {
      ActivationMapObject <- jsonlite::fromJSON(ActivationMapJson)
      self$`activationValues` <- ApiClient$new()$deserializeObj(ActivationMapObject$`activationValues`, "array[array[integer]]", loadNamespace("datarobot.apicore"))
      self$`actualTargetValue` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = ActivationMapObject$actualTargetValue)
      self$`featureName` <- ActivationMapObject$`featureName`
      self$`imageHeight` <- ActivationMapObject$`imageHeight`
      self$`imageId` <- ActivationMapObject$`imageId`
      self$`imageWidth` <- ActivationMapObject$`imageWidth`
      self$`links` <- ActivationMapLinks$new()$fromJSON(jsonlite::toJSON(ActivationMapObject$links, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`overlayImageId` <- ActivationMapObject$`overlayImageId`
      self$`predictedTargetValue` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = ActivationMapObject$predictedTargetValue)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
