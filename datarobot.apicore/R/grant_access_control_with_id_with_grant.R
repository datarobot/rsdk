# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title GrantAccessControlWithIdWithGrant
#'
#' @description GrantAccessControlWithIdWithGrant Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field canShare  character [optional] Whether the org/group/user should be able to share with others.If true, the org/group/user will be able to grant any role up to and includingtheir own to other orgs/groups/user. If &#x60;role&#x60; is &#x60;NO_ROLE&#x60; &#x60;canShare&#x60; is ignored.
#'
#' @field id  character The ID of the recipient.
#'
#' @field role  character The role of the recipient on this entity. One of OWNER, USER, OBSERVER.
#'
#' @field shareRecipientType  character Describes the recipient type, either user, group, or organization.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GrantAccessControlWithIdWithGrant <- R6::R6Class(
  "GrantAccessControlWithIdWithGrant",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(canShare = NULL, id = NULL, role = NULL, shareRecipientType = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`role`)) {
        stopifnot(is.character(`role`), length(`role`) == 1)
      }
      if (!is.null(`shareRecipientType`)) {
        stopifnot(is.character(`shareRecipientType`), length(`shareRecipientType`) == 1)
      }
      if (!is.null(`canShare`)) {
        stopifnot(is.logical(`canShare`), length(`canShare`) == 1)
      }
    }
  ),
  public = list(
    `canShare` = NULL,
    `id` = NULL,
    `role` = NULL,
    `shareRecipientType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param canShare Whether the org/group/user should be able to share with others.If true, the org/group/user will be able to grant any role up to and includingtheir own to other orgs/groups/user. If &#x60;role&#x60; is &#x60;NO_ROLE&#x60; &#x60;canShare&#x60; is ignored.
    #' @param id The ID of the recipient.
    #' @param role The role of the recipient on this entity. One of OWNER, USER, OBSERVER.
    #' @param shareRecipientType Describes the recipient type, either user, group, or organization.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `role` = NULL, `shareRecipientType` = NULL, `canShare` = TRUE, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`id`, `role`, `shareRecipientType`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(canShare, id, role, shareRecipientType)
      }
      self$`canShare` <- `canShare`
      self$`id` <- `id`
      self$`role` <- `role`
      self$`shareRecipientType` <- `shareRecipientType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(canShare = self$`canShare`, id = self$`id`, role = self$`role`, shareRecipientType = self$`shareRecipientType`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`canShare`)) {
          sprintf(
            '"canShare":
            %s
                  ',
            tolower(self$`canShare`)
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`role`)) {
          sprintf(
            '"role":
            "%s"
                  ',
            self$`role`
          )
        },
        if (!is.null(self$`shareRecipientType`)) {
          sprintf(
            '"shareRecipientType":
            "%s"
                  ',
            self$`shareRecipientType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param GrantAccessControlWithIdWithGrantJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(GrantAccessControlWithIdWithGrantJson, validateParams = FALSE) {
      GrantAccessControlWithIdWithGrantObject <- jsonlite::fromJSON(GrantAccessControlWithIdWithGrantJson)
      self$`canShare` <- GrantAccessControlWithIdWithGrantObject$`canShare`
      self$`id` <- GrantAccessControlWithIdWithGrantObject$`id`
      self$`role` <- GrantAccessControlWithIdWithGrantObject$`role`
      self$`shareRecipientType` <- GrantAccessControlWithIdWithGrantObject$`shareRecipientType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
