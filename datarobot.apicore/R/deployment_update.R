# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentUpdate
#'
#' @description DeploymentUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field description  character [optional] A description for the deployment.
#'
#' @field importance  character [optional] Shows how important this deployment is.
#'
#' @field label  character [optional] A human-readable name for the deployment.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentUpdate <- R6::R6Class(
  "DeploymentUpdate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(description = NULL, importance = NULL, label = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`importance`)) {
        stopifnot(is.character(`importance`), length(`importance`) == 1)
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
    }
  ),
  public = list(
    `description` = NULL,
    `importance` = NULL,
    `label` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param description A description for the deployment.
    #' @param importance Shows how important this deployment is.
    #' @param label A human-readable name for the deployment.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `importance` = NULL, `label` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(description, importance, label)
      }
      self$`description` <- `description`
      self$`importance` <- `importance`
      self$`label` <- `label`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(description = self$`description`, importance = self$`importance`, label = self$`label`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`importance`)) {
          sprintf(
            '"importance":
            "%s"
                  ',
            self$`importance`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentUpdateJson, validateParams = FALSE) {
      DeploymentUpdateObject <- jsonlite::fromJSON(DeploymentUpdateJson)
      self$`description` <- DeploymentUpdateObject$`description`
      self$`importance` <- DeploymentUpdateObject$`importance`
      self$`label` <- DeploymentUpdateObject$`label`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
