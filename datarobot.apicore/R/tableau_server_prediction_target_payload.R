# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TableauServerPredictionTargetPayload
#'
#' @description TableauServerPredictionTargetPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field contentUrl  character Site name of the Tableau online or Tableau server you wish to use. Required for Tableau online users.
#'
#' @field datasourceId  character Id of the DSS data source object
#'
#' @field password  character User password to connect to the data location
#'
#' @field typeId  character For Tableau should be set to &#x60;tableauServer&#x60;
#'
#' @field url  character Url of the Tableau online or Tableau server instance
#'
#' @field userName  character User name to connect to the data location
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TableauServerPredictionTargetPayload <- R6::R6Class(
  "TableauServerPredictionTargetPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(contentUrl = NULL, datasourceId = NULL, password = NULL, typeId = NULL, url = NULL, userName = NULL) {
      if (!is.null(`contentUrl`)) {
        stopifnot(is.character(`contentUrl`), length(`contentUrl`) == 1)
      }
      if (!is.null(`datasourceId`)) {
        stopifnot(is.character(`datasourceId`), length(`datasourceId`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`typeId`)) {
        stopifnot(is.character(`typeId`), length(`typeId`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
      if (!is.null(`userName`)) {
        stopifnot(is.character(`userName`), length(`userName`) == 1)
      }
    }
  ),
  public = list(
    `contentUrl` = NULL,
    `datasourceId` = NULL,
    `password` = NULL,
    `typeId` = NULL,
    `url` = NULL,
    `userName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param contentUrl Site name of the Tableau online or Tableau server you wish to use. Required for Tableau online users.
    #' @param datasourceId Id of the DSS data source object
    #' @param password User password to connect to the data location
    #' @param typeId For Tableau should be set to &#x60;tableauServer&#x60;
    #' @param url Url of the Tableau online or Tableau server instance
    #' @param userName User name to connect to the data location
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `contentUrl` = NULL, `datasourceId` = NULL, `password` = NULL, `typeId` = NULL, `url` = NULL, `userName` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`contentUrl`, `datasourceId`, `password`, `typeId`, `url`, `userName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(contentUrl, datasourceId, password, typeId, url, userName)
      }
      self$`contentUrl` <- `contentUrl`
      self$`datasourceId` <- `datasourceId`
      self$`password` <- `password`
      self$`typeId` <- `typeId`
      self$`url` <- `url`
      self$`userName` <- `userName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(contentUrl = self$`contentUrl`, datasourceId = self$`datasourceId`, password = self$`password`, typeId = self$`typeId`, url = self$`url`, userName = self$`userName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`contentUrl`)) {
        sprintf(
        '"contentUrl":
          "%s"
                ',
        self$`contentUrl`
        )},
        if (!is.null(self$`datasourceId`)) {
        sprintf(
        '"datasourceId":
          "%s"
                ',
        self$`datasourceId`
        )},
        if (!is.null(self$`password`)) {
        sprintf(
        '"password":
          "%s"
                ',
        self$`password`
        )},
        if (!is.null(self$`typeId`)) {
        sprintf(
        '"typeId":
          "%s"
                ',
        self$`typeId`
        )},
        if (!is.null(self$`url`)) {
        sprintf(
        '"url":
          "%s"
                ',
        self$`url`
        )},
        if (!is.null(self$`userName`)) {
        sprintf(
        '"userName":
          "%s"
                ',
        self$`userName`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TableauServerPredictionTargetPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TableauServerPredictionTargetPayloadJson, validateParams = FALSE) {
      TableauServerPredictionTargetPayloadObject <- jsonlite::fromJSON(TableauServerPredictionTargetPayloadJson)
      self$`contentUrl` <- TableauServerPredictionTargetPayloadObject$`contentUrl`
      self$`datasourceId` <- TableauServerPredictionTargetPayloadObject$`datasourceId`
      self$`password` <- TableauServerPredictionTargetPayloadObject$`password`
      self$`typeId` <- TableauServerPredictionTargetPayloadObject$`typeId`
      self$`url` <- TableauServerPredictionTargetPayloadObject$`url`
      self$`userName` <- TableauServerPredictionTargetPayloadObject$`userName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
