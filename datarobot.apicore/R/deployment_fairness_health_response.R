# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentFairnessHealthResponse
#'
#' @description DeploymentFairnessHealthResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field endDate  character End date of fairness health period.
#'
#' @field influentialFairnessMetric  character Fairness metric with the most failing classes
#'
#' @field messages  list( character ) List of fairness status explanations
#'
#' @field protectedFeaturesCount  integer Count of the configured protected features for the deployment.
#'
#' @field startDate  character Start date of fairness health period.
#'
#' @field status  character              Overall status of the deployment health based of calculated fairness scores.                * &#x60;&#x60;unknown&#x60;&#x60; - used when the deployment fairness status was never calculated.               * &#x60;&#x60;passing&#x60;&#x60; - used when all fairness criteria are met.               * &#x60;&#x60;warning&#x60;&#x60; - used when certain criteria are not met                               but those are not critical for the user.                               It is an intermediate status between &#x60;&#x60;passing&#x60;&#x60; and &#x60;&#x60;failing&#x60;&#x60;.               * &#x60;&#x60;failing&#x60;&#x60; - used when critical criteria are not met,                               indicating that the deployment is failing fairness checks.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentFairnessHealthResponse <- R6::R6Class(
  "DeploymentFairnessHealthResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(endDate = NULL, influentialFairnessMetric = NULL, messages = NULL, protectedFeaturesCount = NULL, startDate = NULL, status = NULL) {
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`influentialFairnessMetric`)) {
        stopifnot(is.character(`influentialFairnessMetric`), length(`influentialFairnessMetric`) == 1)
      }
      if (!is.null(`messages`)) {
        stopifnot(is.vector(`messages`))
      }
      if (!is.null(`protectedFeaturesCount`)) {
        stopifnot(is.numeric(`protectedFeaturesCount`), length(`protectedFeaturesCount`) == 1)
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
    }
  ),
  public = list(
    `endDate` = NULL,
    `influentialFairnessMetric` = NULL,
    `messages` = NULL,
    `protectedFeaturesCount` = NULL,
    `startDate` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param endDate End date of fairness health period.
    #' @param influentialFairnessMetric Fairness metric with the most failing classes
    #' @param messages List of fairness status explanations
    #' @param protectedFeaturesCount Count of the configured protected features for the deployment.
    #' @param startDate Start date of fairness health period.
    #' @param status              Overall status of the deployment health based of calculated fairness scores.                * &#x60;&#x60;unknown&#x60;&#x60; - used when the deployment fairness status was never calculated.               * &#x60;&#x60;passing&#x60;&#x60; - used when all fairness criteria are met.               * &#x60;&#x60;warning&#x60;&#x60; - used when certain criteria are not met                               but those are not critical for the user.                               It is an intermediate status between &#x60;&#x60;passing&#x60;&#x60; and &#x60;&#x60;failing&#x60;&#x60;.               * &#x60;&#x60;failing&#x60;&#x60; - used when critical criteria are not met,                               indicating that the deployment is failing fairness checks.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`endDate` = NULL, `influentialFairnessMetric` = NULL, `messages` = NULL, `protectedFeaturesCount` = NULL, `startDate` = NULL, `status` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`endDate`, `influentialFairnessMetric`, `messages`, `protectedFeaturesCount`, `startDate`, `status`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(endDate, influentialFairnessMetric, messages, protectedFeaturesCount, startDate, status)
      }
      self$`endDate` <- `endDate`
      self$`influentialFairnessMetric` <- `influentialFairnessMetric`
      sapply(`messages`, function(x) stopifnot(is.character(x)))
      self$`protectedFeaturesCount` <- `protectedFeaturesCount`
      self$`startDate` <- `startDate`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(endDate = self$`endDate`, influentialFairnessMetric = self$`influentialFairnessMetric`, messages = self$`messages`, protectedFeaturesCount = self$`protectedFeaturesCount`, startDate = self$`startDate`, status = self$`status`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`influentialFairnessMetric`)) {
          sprintf(
            '"influentialFairnessMetric":
            "%s"
                  ',
            self$`influentialFairnessMetric`
          )
        },
        if (!is.null(self$`messages`)) {
          sprintf(
            '"messages":
              [%s]
            ',
            paste(unlist(lapply(self$`messages`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`protectedFeaturesCount`)) {
          sprintf(
            '"protectedFeaturesCount":
            %d
                  ',
            self$`protectedFeaturesCount`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentFairnessHealthResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentFairnessHealthResponseJson, validateParams = FALSE) {
      DeploymentFairnessHealthResponseObject <- jsonlite::fromJSON(DeploymentFairnessHealthResponseJson)
      self$`endDate` <- ParseRFC3339Timestamp(DeploymentFairnessHealthResponseObject$`endDate`)
      self$`influentialFairnessMetric` <- DeploymentFairnessHealthResponseObject$`influentialFairnessMetric`
      self$`messages` <- ApiClient$new()$deserializeObj(DeploymentFairnessHealthResponseObject$`messages`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`protectedFeaturesCount` <- DeploymentFairnessHealthResponseObject$`protectedFeaturesCount`
      self$`startDate` <- ParseRFC3339Timestamp(DeploymentFairnessHealthResponseObject$`startDate`)
      self$`status` <- DeploymentFairnessHealthResponseObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
