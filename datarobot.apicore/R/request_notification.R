# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RequestNotification
#'
#' @description RequestNotification Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field body  character The body of the request
#'
#' @field headers  character The headers of the request
#'
#' @field url  character The URL of the request
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RequestNotification <- R6::R6Class(
  "RequestNotification",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(body = NULL, headers = NULL, url = NULL) {
      if (!is.null(`body`)) {
        stopifnot(is.character(`body`), length(`body`) == 1)
      }
      if (!is.null(`headers`)) {
        stopifnot(is.character(`headers`), length(`headers`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
    }
  ),
  public = list(
    `body` = NULL,
    `headers` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param body The body of the request
    #' @param headers The headers of the request
    #' @param url The URL of the request
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`body` = NULL, `headers` = NULL, `url` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`body`, `headers`, `url`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(body, headers, url)
      }
      self$`body` <- `body`
      self$`headers` <- `headers`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(body = self$`body`, headers = self$`headers`, url = self$`url`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`body`)) {
          sprintf(
            '"body":
            "%s"
                  ',
            self$`body`
          )
        },
        if (!is.null(self$`headers`)) {
          sprintf(
            '"headers":
            "%s"
                  ',
            self$`headers`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RequestNotificationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RequestNotificationJson, validateParams = FALSE) {
      RequestNotificationObject <- jsonlite::fromJSON(RequestNotificationJson)
      self$`body` <- RequestNotificationObject$`body`
      self$`headers` <- RequestNotificationObject$`headers`
      self$`url` <- RequestNotificationObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
