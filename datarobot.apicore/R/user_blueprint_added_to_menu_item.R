# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintAddedToMenuItem
#'
#' @description UserBlueprintAddedToMenuItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blueprintId  character The blueprintId representing the blueprint which was added to the project repository.
#'
#' @field userBlueprintId  character The userBlueprintId associated with the blueprintId added to the project repository.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintAddedToMenuItem <- R6::R6Class(
  "UserBlueprintAddedToMenuItem",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(blueprintId = NULL, userBlueprintId = NULL) {
      if (!is.null(`blueprintId`)) {
        stopifnot(is.character(`blueprintId`), length(`blueprintId`) == 1)
      }
      if (!is.null(`userBlueprintId`)) {
        stopifnot(is.character(`userBlueprintId`), length(`userBlueprintId`) == 1)
      }
    }
  ),
  public = list(
    `blueprintId` = NULL,
    `userBlueprintId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blueprintId The blueprintId representing the blueprint which was added to the project repository.
    #' @param userBlueprintId The userBlueprintId associated with the blueprintId added to the project repository.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blueprintId` = NULL, `userBlueprintId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`blueprintId`, `userBlueprintId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(blueprintId, userBlueprintId)
      }
      self$`blueprintId` <- `blueprintId`
      self$`userBlueprintId` <- `userBlueprintId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(blueprintId = self$`blueprintId`, userBlueprintId = self$`userBlueprintId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blueprintId`)) {
          sprintf(
            '"blueprintId":
            "%s"
                  ',
            self$`blueprintId`
          )
        },
        if (!is.null(self$`userBlueprintId`)) {
          sprintf(
            '"userBlueprintId":
            "%s"
                  ',
            self$`userBlueprintId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintAddedToMenuItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintAddedToMenuItemJson, validateParams = FALSE) {
      UserBlueprintAddedToMenuItemObject <- jsonlite::fromJSON(UserBlueprintAddedToMenuItemJson)
      self$`blueprintId` <- UserBlueprintAddedToMenuItemObject$`blueprintId`
      self$`userBlueprintId` <- UserBlueprintAddedToMenuItemObject$`userBlueprintId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
