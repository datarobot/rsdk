# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Hdfs
#'
#' @description Hdfs Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field categories  \link{OneOfstringarray} [optional] An array of strings describing the intended use of the dataset.
#'
#' @field doSnapshot  character [optional] If true, create a snapshot dataset; if false, create a remote dataset. Creating snapshots from non-file sources requires an additional permission, &#x60;Enable Create Snapshot Data Source&#x60;.
#'
#' @field namenodeWebhdfsPort  integer [optional] The port of HDFS name node.
#'
#' @field password  character [optional] The password (in cleartext) for authenticating to HDFS using Kerberos. The password will be encrypted on the server side in scope of HTTP request and never saved or stored.
#'
#' @field persistDataAfterIngestion  character [optional] If true, will enforce saving all data (for download and sampling) and will allow a user to view extended data profile (which includes data statistics like min/max/median/mean, histogram, etc.). If false, will not enforce saving data. The data schema (feature names and types) still will be available. Specifying this parameter to false and &#x60;doSnapshot&#x60; to true will result in an error
#'
#' @field url  character The HDFS url to use as the source of data for the dataset being created.
#'
#' @field user  character [optional] The username for authenticating to HDFS using Kerberos.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Hdfs <- R6::R6Class(
  "Hdfs",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(categories = NULL, doSnapshot = NULL, namenodeWebhdfsPort = NULL, password = NULL, persistDataAfterIngestion = NULL, url = NULL, user = NULL) {
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
      if (!is.null(`categories`)) {
        .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      }
      if (!is.null(`doSnapshot`)) {
        stopifnot(is.logical(`doSnapshot`), length(`doSnapshot`) == 1)
      }
      if (!is.null(`namenodeWebhdfsPort`)) {
        stopifnot(is.numeric(`namenodeWebhdfsPort`), length(`namenodeWebhdfsPort`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`persistDataAfterIngestion`)) {
        stopifnot(is.logical(`persistDataAfterIngestion`), length(`persistDataAfterIngestion`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `categories` = NULL,
    `doSnapshot` = NULL,
    `namenodeWebhdfsPort` = NULL,
    `password` = NULL,
    `persistDataAfterIngestion` = NULL,
    `url` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param categories An array of strings describing the intended use of the dataset.
    #' @param doSnapshot If true, create a snapshot dataset; if false, create a remote dataset. Creating snapshots from non-file sources requires an additional permission, &#x60;Enable Create Snapshot Data Source&#x60;.
    #' @param namenodeWebhdfsPort The port of HDFS name node.
    #' @param password The password (in cleartext) for authenticating to HDFS using Kerberos. The password will be encrypted on the server side in scope of HTTP request and never saved or stored.
    #' @param persistDataAfterIngestion If true, will enforce saving all data (for download and sampling) and will allow a user to view extended data profile (which includes data statistics like min/max/median/mean, histogram, etc.). If false, will not enforce saving data. The data schema (feature names and types) still will be available. Specifying this parameter to false and &#x60;doSnapshot&#x60; to true will result in an error
    #' @param url The HDFS url to use as the source of data for the dataset being created.
    #' @param user The username for authenticating to HDFS using Kerberos.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`url` = NULL, `categories` = NULL, `doSnapshot` = TRUE, `namenodeWebhdfsPort` = NULL, `password` = NULL, `persistDataAfterIngestion` = NULL, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`url`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(categories, doSnapshot, namenodeWebhdfsPort, password, persistDataAfterIngestion, url, user)
      }
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      self$`doSnapshot` <- `doSnapshot`
      self$`namenodeWebhdfsPort` <- `namenodeWebhdfsPort`
      self$`password` <- `password`
      self$`persistDataAfterIngestion` <- `persistDataAfterIngestion`
      self$`url` <- `url`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(categories = self$`categories`, doSnapshot = self$`doSnapshot`, namenodeWebhdfsPort = self$`namenodeWebhdfsPort`, password = self$`password`, persistDataAfterIngestion = self$`persistDataAfterIngestion`, url = self$`url`, user = self$`user`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`categories`)) {
          sprintf(
            '"categories":
          %s
        ',
            self$`categories`
          )
        },
        if (!is.null(self$`doSnapshot`)) {
          sprintf(
            '"doSnapshot":
            %s
                  ',
            tolower(self$`doSnapshot`)
          )
        },
        if (!is.null(self$`namenodeWebhdfsPort`)) {
          sprintf(
            '"namenodeWebhdfsPort":
            %d
                  ',
            self$`namenodeWebhdfsPort`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`persistDataAfterIngestion`)) {
          sprintf(
            '"persistDataAfterIngestion":
            %s
                  ',
            tolower(self$`persistDataAfterIngestion`)
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param HdfsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(HdfsJson, validateParams = FALSE) {
      HdfsObject <- jsonlite::fromJSON(HdfsJson)
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = HdfsObject$categories)
      self$`doSnapshot` <- HdfsObject$`doSnapshot`
      self$`namenodeWebhdfsPort` <- HdfsObject$`namenodeWebhdfsPort`
      self$`password` <- HdfsObject$`password`
      self$`persistDataAfterIngestion` <- HdfsObject$`persistDataAfterIngestion`
      self$`url` <- HdfsObject$`url`
      self$`user` <- HdfsObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
