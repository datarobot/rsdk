# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ProjectUpdate
#'
#' @description ProjectUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field gpuWorkerCount  integer [optional] The desired new number of gpu workers if the             number of gpu workers should be changed.             Must not exceed the number of gpu workers available to the user. &#x60;0&#x60; is allowed.             &#x60;-1&#x60; requests the maximum number available to the user.
#'
#' @field holdoutUnlocked  character [optional] If specified, the holdout will be unlocked;             note that the holdout cannot be relocked after unlocking
#'
#' @field projectDescription  character [optional] The new description of the project, if the description should be updated.
#'
#' @field projectName  character [optional] The new name of the project, if it should be renamed.
#'
#' @field workerCount  integer [optional] The desired new number of workers if the             number of workers should be changed.             Must not exceed the number of workers available to the user. &#x60;0&#x60; is allowed.             (New in version v2.14) &#x60;-1&#x60; requests the maximum number available to the user.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ProjectUpdate <- R6::R6Class(
  "ProjectUpdate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`gpuWorkerCount` = NULL, `holdoutUnlocked` = NULL, `projectDescription` = NULL, `projectName` = NULL, `workerCount` = NULL) {
      if (!is.null(`gpuWorkerCount`)) {
        stopifnot(is.numeric(`gpuWorkerCount`), length(`gpuWorkerCount`) == 1)
      }
      if (!is.null(`holdoutUnlocked`)) {
        stopifnot(is.character(`holdoutUnlocked`), length(`holdoutUnlocked`) == 1)
      }
      if (!is.null(`projectDescription`)) {
        stopifnot(is.character(`projectDescription`), length(`projectDescription`) == 1)
      }
      if (!is.null(`projectName`)) {
        stopifnot(is.character(`projectName`), length(`projectName`) == 1)
      }
      if (!is.null(`workerCount`)) {
        stopifnot(is.numeric(`workerCount`), length(`workerCount`) == 1)
      }
    }
  ),
  public = list(
    `gpuWorkerCount` = NULL,
    `holdoutUnlocked` = NULL,
    `projectDescription` = NULL,
    `projectName` = NULL,
    `workerCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param gpuWorkerCount The desired new number of gpu workers if the             number of gpu workers should be changed.             Must not exceed the number of gpu workers available to the user. &#x60;0&#x60; is allowed.             &#x60;-1&#x60; requests the maximum number available to the user.
    #' @param holdoutUnlocked If specified, the holdout will be unlocked;             note that the holdout cannot be relocked after unlocking
    #' @param projectDescription The new description of the project, if the description should be updated.
    #' @param projectName The new name of the project, if it should be renamed.
    #' @param workerCount The desired new number of workers if the             number of workers should be changed.             Must not exceed the number of workers available to the user. &#x60;0&#x60; is allowed.             (New in version v2.14) &#x60;-1&#x60; requests the maximum number available to the user.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`gpuWorkerCount` = NULL, `holdoutUnlocked` = NULL, `projectDescription` = NULL, `projectName` = NULL, `workerCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(gpuWorkerCount, holdoutUnlocked, projectDescription, projectName, workerCount)
      }
      self$`gpuWorkerCount` <- `gpuWorkerCount`
      self$`holdoutUnlocked` <- `holdoutUnlocked`
      self$`projectDescription` <- `projectDescription`
      self$`projectName` <- `projectName`
      self$`workerCount` <- `workerCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(gpuWorkerCount = self$`gpuWorkerCount`, holdoutUnlocked = self$`holdoutUnlocked`, projectDescription = self$`projectDescription`, projectName = self$`projectName`, workerCount = self$`workerCount`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`gpuWorkerCount`)) {
          sprintf(
            '"gpuWorkerCount":
            %d
                  ',
            self$`gpuWorkerCount`
          )
        },
        if (!is.null(self$`holdoutUnlocked`)) {
          sprintf(
            '"holdoutUnlocked":
            "%s"
                  ',
            self$`holdoutUnlocked`
          )
        },
        if (!is.null(self$`projectDescription`)) {
          sprintf(
            '"projectDescription":
            "%s"
                  ',
            self$`projectDescription`
          )
        },
        if (!is.null(self$`projectName`)) {
          sprintf(
            '"projectName":
            "%s"
                  ',
            self$`projectName`
          )
        },
        if (!is.null(self$`workerCount`)) {
          sprintf(
            '"workerCount":
            %d
                  ',
            self$`workerCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ProjectUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ProjectUpdateJson, validateParams = FALSE) {
      ProjectUpdateObject <- jsonlite::fromJSON(ProjectUpdateJson)
      self$`gpuWorkerCount` <- ProjectUpdateObject$`gpuWorkerCount`
      self$`holdoutUnlocked` <- ProjectUpdateObject$`holdoutUnlocked`
      self$`projectDescription` <- ProjectUpdateObject$`projectDescription`
      self$`projectName` <- ProjectUpdateObject$`projectName`
      self$`workerCount` <- ProjectUpdateObject$`workerCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
