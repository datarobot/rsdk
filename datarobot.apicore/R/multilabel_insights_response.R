# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MultilabelInsightsResponse
#'
#' @description MultilabelInsightsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field multilabelInsightsKey  character Key for multilabel insights, unique per project, feature, and EDA stage. The response will contain the key for the most recent, finished EDA stage.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MultilabelInsightsResponse <- R6::R6Class(
  "MultilabelInsightsResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`multilabelInsightsKey` = NULL) {
      if (!is.null(`multilabelInsightsKey`)) {
        stopifnot(is.character(`multilabelInsightsKey`), length(`multilabelInsightsKey`) == 1)
      }
    }
  ),
  public = list(
    `multilabelInsightsKey` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param multilabelInsightsKey Key for multilabel insights, unique per project, feature, and EDA stage. The response will contain the key for the most recent, finished EDA stage.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`multilabelInsightsKey` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`multilabelInsightsKey`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(multilabelInsightsKey)
      }
      self$`multilabelInsightsKey` <- `multilabelInsightsKey`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(multilabelInsightsKey = self$`multilabelInsightsKey`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`multilabelInsightsKey`)) {
          sprintf(
            '"multilabelInsightsKey":
            "%s"
                  ',
            self$`multilabelInsightsKey`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MultilabelInsightsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MultilabelInsightsResponseJson, validateParams = FALSE) {
      MultilabelInsightsResponseObject <- jsonlite::fromJSON(MultilabelInsightsResponseJson)
      self$`multilabelInsightsKey` <- MultilabelInsightsResponseObject$`multilabelInsightsKey`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
