# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AOTChartPreviewResponse
#'
#' @description AOTChartPreviewResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field bins  list( \link{AOTChartBins} ) The datetime chart data for that source.
#'
#' @field datasetId  character The dataset id which was used to compute the AOT chart.
#'
#' @field modelId  character The model id which was used to compute the AOT chart.
#'
#' @field projectId  character The project id which was used to compute the AOT chart.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AOTChartPreviewResponse <- R6::R6Class(
  "AOTChartPreviewResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`bins` = NULL, `datasetId` = NULL, `modelId` = NULL, `projectId` = NULL) {
      if (!is.null(`bins`)) {
        stopifnot(is.vector(`bins`), sapply(`bins`, R6::is.R6))
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `bins` = NULL,
    `datasetId` = NULL,
    `modelId` = NULL,
    `projectId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param bins The datetime chart data for that source.
    #' @param datasetId The dataset id which was used to compute the AOT chart.
    #' @param modelId The model id which was used to compute the AOT chart.
    #' @param projectId The project id which was used to compute the AOT chart.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`bins` = NULL, `datasetId` = NULL, `modelId` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`bins`, `datasetId`, `modelId`, `projectId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(bins, datasetId, modelId, projectId)
      }
      self$`bins` <- `bins`
      self$`datasetId` <- `datasetId`
      self$`modelId` <- `modelId`
      self$`projectId` <- `projectId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(bins = self$`bins`, datasetId = self$`datasetId`, modelId = self$`modelId`, projectId = self$`projectId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`bins`)) {
          sprintf(
            '"bins":
            [%s]
      ',
            paste(sapply(self$`bins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AOTChartPreviewResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AOTChartPreviewResponseJson, validateParams = FALSE) {
      AOTChartPreviewResponseObject <- jsonlite::fromJSON(AOTChartPreviewResponseJson)
      self$`bins` <- ApiClient$new()$deserializeObj(AOTChartPreviewResponseObject$`bins`, "array[AOTChartBins]", loadNamespace("datarobot.apicore"))
      self$`datasetId` <- AOTChartPreviewResponseObject$`datasetId`
      self$`modelId` <- AOTChartPreviewResponseObject$`modelId`
      self$`projectId` <- AOTChartPreviewResponseObject$`projectId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
