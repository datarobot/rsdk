# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanationsValues
#'
#' @description PredictionExplanationsValues Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field feature  character The name of the feature contributing to the prediction.
#'
#' @field featureValue  \link{OneOfstringnumber} The value the feature took on for this row.
#'
#' @field label  \link{OneOfstringnumber} Describes what output was driven by this prediction explanation. For regression projects, it is the name of the target feature. For classification projects, it is the class whose probability increasing would correspond to a positive strength of this prediction explanation.
#'
#' @field qualitativeStrength  character The feature strength as a string, a plus or minus indicator from &#x60;+++&#x60; to &#x60;---&#x60;.
#'
#' @field strength  numeric [optional] The feature strength as a float.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanationsValues <- R6::R6Class(
  "PredictionExplanationsValues",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`feature` = NULL, `featureValue` = NULL, `label` = NULL, `qualitativeStrength` = NULL, `strength` = NULL) {
      if (!is.null(`feature`)) {
        stopifnot(is.character(`feature`), length(`feature`) == 1)
      }
      if (!is.null(`featureValue`)) {
        .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = featureValue)
      }
      if (!is.null(`label`)) {
        .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = label)
      }
      if (!is.null(`qualitativeStrength`)) {
        stopifnot(is.character(`qualitativeStrength`), length(`qualitativeStrength`) == 1)
      }
      if (!is.null(`strength`)) {
        stopifnot(is.numeric(`strength`), length(`strength`) == 1)
      }
    }
  ),
  public = list(
    `feature` = NULL,
    `featureValue` = NULL,
    `label` = NULL,
    `qualitativeStrength` = NULL,
    `strength` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param feature The name of the feature contributing to the prediction.
    #' @param featureValue The value the feature took on for this row.
    #' @param label Describes what output was driven by this prediction explanation. For regression projects, it is the name of the target feature. For classification projects, it is the class whose probability increasing would correspond to a positive strength of this prediction explanation.
    #' @param qualitativeStrength The feature strength as a string, a plus or minus indicator from &#x60;+++&#x60; to &#x60;---&#x60;.
    #' @param strength The feature strength as a float.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`feature` = NULL, `featureValue` = NULL, `label` = NULL, `qualitativeStrength` = NULL, `strength` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`feature`, `featureValue`, `label`, `qualitativeStrength`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(feature, featureValue, label, qualitativeStrength, strength)
      }
      self$`feature` <- `feature`
      self$`featureValue` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = featureValue)
      self$`label` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = label)
      self$`qualitativeStrength` <- `qualitativeStrength`
      self$`strength` <- `strength`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(feature = self$`feature`, featureValue = self$`featureValue`, label = self$`label`, qualitativeStrength = self$`qualitativeStrength`, strength = self$`strength`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`feature`)) {
          sprintf(
            '"feature":
            "%s"
                  ',
            self$`feature`
          )
        },
        if (!is.null(self$`featureValue`)) {
          sprintf(
            '"featureValue":
            %s
      ',
            self$`featureValue`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            %s
      ',
            self$`label`
          )
        },
        if (!is.null(self$`qualitativeStrength`)) {
          sprintf(
            '"qualitativeStrength":
            "%s"
                  ',
            self$`qualitativeStrength`
          )
        },
        if (!is.null(self$`strength`)) {
          sprintf(
            '"strength":
            %d
                  ',
            self$`strength`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationsValuesJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationsValuesJson, validateParams = FALSE) {
      PredictionExplanationsValuesObject <- jsonlite::fromJSON(PredictionExplanationsValuesJson)
      self$`feature` <- PredictionExplanationsValuesObject$`feature`
      self$`featureValue` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = PredictionExplanationsValuesObject$featureValue)
      self$`label` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = PredictionExplanationsValuesObject$label)
      self$`qualitativeStrength` <- PredictionExplanationsValuesObject$`qualitativeStrength`
      self$`strength` <- PredictionExplanationsValuesObject$`strength`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
