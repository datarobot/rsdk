# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ProjectSegmentationInfoResponse
#'
#' @description ProjectSegmentationInfoResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field parentProjectId  character [optional] ID of the original project.
#'
#' @field segment  character [optional] Segment value.
#'
#' @field segmentationTaskId  character ID of the Segmentation Task.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ProjectSegmentationInfoResponse <- R6::R6Class(
  "ProjectSegmentationInfoResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(parentProjectId = NULL, segment = NULL, segmentationTaskId = NULL) {
      if (!is.null(`segmentationTaskId`)) {
        stopifnot(is.character(`segmentationTaskId`), length(`segmentationTaskId`) == 1)
      }
      if (!is.null(`parentProjectId`)) {
        stopifnot(is.character(`parentProjectId`), length(`parentProjectId`) == 1)
      }
      if (!is.null(`segment`)) {
        stopifnot(is.character(`segment`), length(`segment`) == 1)
      }
    }
  ),
  public = list(
    `parentProjectId` = NULL,
    `segment` = NULL,
    `segmentationTaskId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param parentProjectId ID of the original project.
    #' @param segment Segment value.
    #' @param segmentationTaskId ID of the Segmentation Task.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`segmentationTaskId` = NULL, `parentProjectId` = NULL, `segment` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`segmentationTaskId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(parentProjectId, segment, segmentationTaskId)
      }
      self$`parentProjectId` <- `parentProjectId`
      self$`segment` <- `segment`
      self$`segmentationTaskId` <- `segmentationTaskId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(parentProjectId = self$`parentProjectId`, segment = self$`segment`, segmentationTaskId = self$`segmentationTaskId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`parentProjectId`)) {
          sprintf(
            '"parentProjectId":
            "%s"
                  ',
            self$`parentProjectId`
          )
        },
        if (!is.null(self$`segment`)) {
          sprintf(
            '"segment":
            "%s"
                  ',
            self$`segment`
          )
        },
        if (!is.null(self$`segmentationTaskId`)) {
          sprintf(
            '"segmentationTaskId":
            "%s"
                  ',
            self$`segmentationTaskId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ProjectSegmentationInfoResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ProjectSegmentationInfoResponseJson, validateParams = FALSE) {
      ProjectSegmentationInfoResponseObject <- jsonlite::fromJSON(ProjectSegmentationInfoResponseJson)
      self$`parentProjectId` <- ProjectSegmentationInfoResponseObject$`parentProjectId`
      self$`segment` <- ProjectSegmentationInfoResponseObject$`segment`
      self$`segmentationTaskId` <- ProjectSegmentationInfoResponseObject$`segmentationTaskId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
