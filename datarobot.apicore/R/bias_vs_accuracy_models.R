# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BiasVsAccuracyModels
#'
#' @description BiasVsAccuracyModels Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accuracyValue  numeric The model&#39;s accuracy score.
#'
#' @field bp  integer The blueprint number of the model from the leaderboard.
#'
#' @field dsName  character The name of the feature list used for model training.
#'
#' @field fairnessValue  numeric The model&#39;s relative fairness score for the class with the lowest fairness score. In other words, the fairness score of the least privileged class.
#'
#' @field modelId  character ID of the model.
#'
#' @field modelNumber  integer The model number from the leaderboard.
#'
#' @field modelType  character The type/name of the model.
#'
#' @field prime  character Flag to indicate whether the model is a prime model.
#'
#' @field samplepct  numeric The sample size percentage of the feature list data the model was trained on.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BiasVsAccuracyModels <- R6::R6Class(
  "BiasVsAccuracyModels",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`accuracyValue` = NULL, `bp` = NULL, `dsName` = NULL, `fairnessValue` = NULL, `modelId` = NULL, `modelNumber` = NULL, `modelType` = NULL, `prime` = NULL, `samplepct` = NULL) {
      if (!is.null(`accuracyValue`)) {
      }
      if (!is.null(`bp`)) {
        stopifnot(is.numeric(`bp`), length(`bp`) == 1)
      }
      if (!is.null(`dsName`)) {
        stopifnot(is.character(`dsName`), length(`dsName`) == 1)
      }
      if (!is.null(`fairnessValue`)) {
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`modelNumber`)) {
        stopifnot(is.numeric(`modelNumber`), length(`modelNumber`) == 1)
      }
      if (!is.null(`modelType`)) {
        stopifnot(is.character(`modelType`), length(`modelType`) == 1)
      }
      if (!is.null(`prime`)) {
        stopifnot(is.logical(`prime`), length(`prime`) == 1)
      }
      if (!is.null(`samplepct`)) {
      }
    }
  ),
  public = list(
    `accuracyValue` = NULL,
    `bp` = NULL,
    `dsName` = NULL,
    `fairnessValue` = NULL,
    `modelId` = NULL,
    `modelNumber` = NULL,
    `modelType` = NULL,
    `prime` = NULL,
    `samplepct` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accuracyValue The model&#39;s accuracy score.
    #' @param bp The blueprint number of the model from the leaderboard.
    #' @param dsName The name of the feature list used for model training.
    #' @param fairnessValue The model&#39;s relative fairness score for the class with the lowest fairness score. In other words, the fairness score of the least privileged class.
    #' @param modelId ID of the model.
    #' @param modelNumber The model number from the leaderboard.
    #' @param modelType The type/name of the model.
    #' @param prime Flag to indicate whether the model is a prime model.
    #' @param samplepct The sample size percentage of the feature list data the model was trained on.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`accuracyValue` = NULL, `bp` = NULL, `dsName` = NULL, `fairnessValue` = NULL, `modelId` = NULL, `modelNumber` = NULL, `modelType` = NULL, `prime` = NULL, `samplepct` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`accuracyValue`, `bp`, `dsName`, `fairnessValue`, `modelId`, `modelNumber`, `modelType`, `prime`, `samplepct`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(accuracyValue, bp, dsName, fairnessValue, modelId, modelNumber, modelType, prime, samplepct)
      }
      self$`accuracyValue` <- `accuracyValue`
      self$`bp` <- `bp`
      self$`dsName` <- `dsName`
      self$`fairnessValue` <- `fairnessValue`
      self$`modelId` <- `modelId`
      self$`modelNumber` <- `modelNumber`
      self$`modelType` <- `modelType`
      self$`prime` <- `prime`
      self$`samplepct` <- `samplepct`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(accuracyValue = self$`accuracyValue`, bp = self$`bp`, dsName = self$`dsName`, fairnessValue = self$`fairnessValue`, modelId = self$`modelId`, modelNumber = self$`modelNumber`, modelType = self$`modelType`, prime = self$`prime`, samplepct = self$`samplepct`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accuracyValue`)) {
          sprintf(
            '"accuracyValue":
            %d
                  ',
            self$`accuracyValue`
          )
        },
        if (!is.null(self$`bp`)) {
          sprintf(
            '"bp":
            %d
                  ',
            self$`bp`
          )
        },
        if (!is.null(self$`dsName`)) {
          sprintf(
            '"dsName":
            "%s"
                  ',
            self$`dsName`
          )
        },
        if (!is.null(self$`fairnessValue`)) {
          sprintf(
            '"fairnessValue":
            %d
                  ',
            self$`fairnessValue`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`modelNumber`)) {
          sprintf(
            '"modelNumber":
            %d
                  ',
            self$`modelNumber`
          )
        },
        if (!is.null(self$`modelType`)) {
          sprintf(
            '"modelType":
            "%s"
                  ',
            self$`modelType`
          )
        },
        if (!is.null(self$`prime`)) {
          sprintf(
            '"prime":
            %s
                  ',
            tolower(self$`prime`)
          )
        },
        if (!is.null(self$`samplepct`)) {
          sprintf(
            '"samplepct":
            %d
                  ',
            self$`samplepct`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BiasVsAccuracyModelsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BiasVsAccuracyModelsJson, validateParams = FALSE) {
      BiasVsAccuracyModelsObject <- jsonlite::fromJSON(BiasVsAccuracyModelsJson)
      self$`accuracyValue` <- BiasVsAccuracyModelsObject$`accuracyValue`
      self$`bp` <- BiasVsAccuracyModelsObject$`bp`
      self$`dsName` <- BiasVsAccuracyModelsObject$`dsName`
      self$`fairnessValue` <- BiasVsAccuracyModelsObject$`fairnessValue`
      self$`modelId` <- BiasVsAccuracyModelsObject$`modelId`
      self$`modelNumber` <- BiasVsAccuracyModelsObject$`modelNumber`
      self$`modelType` <- BiasVsAccuracyModelsObject$`modelType`
      self$`prime` <- BiasVsAccuracyModelsObject$`prime`
      self$`samplepct` <- BiasVsAccuracyModelsObject$`samplepct`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
