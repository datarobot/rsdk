# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SecondaryDatasetsConfig
#'
#' @description SecondaryDatasetsConfig Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field configId  character ID of the secondary datasets configuration
#'
#' @field configName  character Name of the secondary datasets config
#'
#' @field updated  character Timestamp when configuration was updated on the given deployment
#'
#' @field username  character Name of the user who made the update
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SecondaryDatasetsConfig <- R6::R6Class(
  "SecondaryDatasetsConfig",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`configId` = NULL, `configName` = NULL, `updated` = NULL, `username` = NULL) {
      if (!is.null(`configId`)) {
        stopifnot(is.character(`configId`), length(`configId`) == 1)
      }
      if (!is.null(`configName`)) {
        stopifnot(is.character(`configName`), length(`configName`) == 1)
      }
      if (!is.null(`updated`)) {
        stopifnot(is.character(`updated`), length(`updated`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
    }
  ),
  public = list(
    `configId` = NULL,
    `configName` = NULL,
    `updated` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param configId ID of the secondary datasets configuration
    #' @param configName Name of the secondary datasets config
    #' @param updated Timestamp when configuration was updated on the given deployment
    #' @param username Name of the user who made the update
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`configId` = NULL, `configName` = NULL, `updated` = NULL, `username` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`configId`, `configName`, `updated`, `username`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(configId, configName, updated, username)
      }
      self$`configId` <- `configId`
      self$`configName` <- `configName`
      self$`updated` <- `updated`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(configId = self$`configId`, configName = self$`configName`, updated = self$`updated`, username = self$`username`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`configId`)) {
          sprintf(
            '"configId":
            "%s"
                  ',
            self$`configId`
          )
        },
        if (!is.null(self$`configName`)) {
          sprintf(
            '"configName":
            "%s"
                  ',
            self$`configName`
          )
        },
        if (!is.null(self$`updated`)) {
          sprintf(
            '"updated":
            "%s"
                  ',
            self$`updated`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SecondaryDatasetsConfigJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SecondaryDatasetsConfigJson, validateParams = FALSE) {
      SecondaryDatasetsConfigObject <- jsonlite::fromJSON(SecondaryDatasetsConfigJson)
      self$`configId` <- SecondaryDatasetsConfigObject$`configId`
      self$`configName` <- SecondaryDatasetsConfigObject$`configName`
      self$`updated` <- SecondaryDatasetsConfigObject$`updated`
      self$`username` <- SecondaryDatasetsConfigObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
