# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BiasMitigationModelCreate
#'
#' @description BiasMitigationModelCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field biasMitigationFeature  character The name of the protected feature used to mitigate bias on models.
#'
#' @field biasMitigationParentLid  character The ID of the model to modify with a bias-mitigation task.
#'
#' @field biasMitigationTechnique  character Method applied to perform bias mitigation.
#'
#' @field includeBiasMitigationFeatureAsPredictorVariable  character Specifies whether the mitigation feature will be used as a predictor variable (i.e., treated like other categorical features in the input to train the modeler), in addition to being used for bias mitigation. If false, the mitigation feature will be used only for bias mitigation, and not for training the modeler task.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BiasMitigationModelCreate <- R6::R6Class(
  "BiasMitigationModelCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`biasMitigationFeature` = NULL, `biasMitigationParentLid` = NULL, `biasMitigationTechnique` = NULL, `includeBiasMitigationFeatureAsPredictorVariable` = NULL) {
      if (!is.null(`biasMitigationFeature`)) {
        stopifnot(is.character(`biasMitigationFeature`), length(`biasMitigationFeature`) == 1)
      }
      if (!is.null(`biasMitigationParentLid`)) {
        stopifnot(is.character(`biasMitigationParentLid`), length(`biasMitigationParentLid`) == 1)
      }
      if (!is.null(`biasMitigationTechnique`)) {
        stopifnot(is.character(`biasMitigationTechnique`), length(`biasMitigationTechnique`) == 1)
      }
      if (!is.null(`includeBiasMitigationFeatureAsPredictorVariable`)) {
        stopifnot(is.logical(`includeBiasMitigationFeatureAsPredictorVariable`), length(`includeBiasMitigationFeatureAsPredictorVariable`) == 1)
      }
    }
  ),
  public = list(
    `biasMitigationFeature` = NULL,
    `biasMitigationParentLid` = NULL,
    `biasMitigationTechnique` = NULL,
    `includeBiasMitigationFeatureAsPredictorVariable` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param biasMitigationFeature The name of the protected feature used to mitigate bias on models.
    #' @param biasMitigationParentLid The ID of the model to modify with a bias-mitigation task.
    #' @param biasMitigationTechnique Method applied to perform bias mitigation.
    #' @param includeBiasMitigationFeatureAsPredictorVariable Specifies whether the mitigation feature will be used as a predictor variable (i.e., treated like other categorical features in the input to train the modeler), in addition to being used for bias mitigation. If false, the mitigation feature will be used only for bias mitigation, and not for training the modeler task.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`biasMitigationFeature` = NULL, `biasMitigationParentLid` = NULL, `biasMitigationTechnique` = NULL, `includeBiasMitigationFeatureAsPredictorVariable` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`biasMitigationFeature`, `biasMitigationParentLid`, `biasMitigationTechnique`, `includeBiasMitigationFeatureAsPredictorVariable`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(biasMitigationFeature, biasMitigationParentLid, biasMitigationTechnique, includeBiasMitigationFeatureAsPredictorVariable)
      }
      self$`biasMitigationFeature` <- `biasMitigationFeature`
      self$`biasMitigationParentLid` <- `biasMitigationParentLid`
      self$`biasMitigationTechnique` <- `biasMitigationTechnique`
      self$`includeBiasMitigationFeatureAsPredictorVariable` <- `includeBiasMitigationFeatureAsPredictorVariable`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(biasMitigationFeature = self$`biasMitigationFeature`, biasMitigationParentLid = self$`biasMitigationParentLid`, biasMitigationTechnique = self$`biasMitigationTechnique`, includeBiasMitigationFeatureAsPredictorVariable = self$`includeBiasMitigationFeatureAsPredictorVariable`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`biasMitigationFeature`)) {
          sprintf(
            '"biasMitigationFeature":
            "%s"
                  ',
            self$`biasMitigationFeature`
          )
        },
        if (!is.null(self$`biasMitigationParentLid`)) {
          sprintf(
            '"biasMitigationParentLid":
            "%s"
                  ',
            self$`biasMitigationParentLid`
          )
        },
        if (!is.null(self$`biasMitigationTechnique`)) {
          sprintf(
            '"biasMitigationTechnique":
            "%s"
                  ',
            self$`biasMitigationTechnique`
          )
        },
        if (!is.null(self$`includeBiasMitigationFeatureAsPredictorVariable`)) {
          sprintf(
            '"includeBiasMitigationFeatureAsPredictorVariable":
            %s
                  ',
            tolower(self$`includeBiasMitigationFeatureAsPredictorVariable`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BiasMitigationModelCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BiasMitigationModelCreateJson, validateParams = FALSE) {
      BiasMitigationModelCreateObject <- jsonlite::fromJSON(BiasMitigationModelCreateJson)
      self$`biasMitigationFeature` <- BiasMitigationModelCreateObject$`biasMitigationFeature`
      self$`biasMitigationParentLid` <- BiasMitigationModelCreateObject$`biasMitigationParentLid`
      self$`biasMitigationTechnique` <- BiasMitigationModelCreateObject$`biasMitigationTechnique`
      self$`includeBiasMitigationFeatureAsPredictorVariable` <- BiasMitigationModelCreateObject$`includeBiasMitigationFeatureAsPredictorVariable`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
