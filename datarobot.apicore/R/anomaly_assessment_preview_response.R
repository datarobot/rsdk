# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AnomalyAssessmentPreviewResponse
#'
#' @description AnomalyAssessmentPreviewResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtest  \link{OneOfintegerstring} The backtest of the record.
#'
#' @field endDate  character ISO-formatted last timestamp in the subset. For example: &#x60;&#x60;2019-08-30T00:00:00.000000Z&#x60;&#x60;.
#'
#' @field modelId  character The model ID of the record.
#'
#' @field previewBins  list( \link{BinResponse} ) Aggregated predictions for the subset. Bins boundaries may differ from actual start/end dates because this is an aggregation.
#'
#' @field projectId  character The project ID of the record.
#'
#' @field recordId  character The ID of the anomaly assessment record.
#'
#' @field seriesId  character The series id of the record. Applicable in multiseries projects
#'
#' @field source  character The source of the record
#'
#' @field startDate  character ISO-formatted first timestamp in the subset. For example: &#x60;&#x60;2019-08-01T00:00:00.000000Z&#x60;&#x60;.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AnomalyAssessmentPreviewResponse <- R6::R6Class(
  "AnomalyAssessmentPreviewResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(backtest = NULL, endDate = NULL, modelId = NULL, previewBins = NULL, projectId = NULL, recordId = NULL, seriesId = NULL, source = NULL, startDate = NULL) {
      if (!is.null(`backtest`)) {
        .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = backtest)
      }
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`previewBins`)) {
        stopifnot(is.vector(`previewBins`))
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`recordId`)) {
        stopifnot(is.character(`recordId`), length(`recordId`) == 1)
      }
      if (!is.null(`seriesId`)) {
        stopifnot(is.character(`seriesId`), length(`seriesId`) == 1)
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `backtest` = NULL,
    `endDate` = NULL,
    `modelId` = NULL,
    `previewBins` = NULL,
    `projectId` = NULL,
    `recordId` = NULL,
    `seriesId` = NULL,
    `source` = NULL,
    `startDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtest The backtest of the record.
    #' @param endDate ISO-formatted last timestamp in the subset. For example: &#x60;&#x60;2019-08-30T00:00:00.000000Z&#x60;&#x60;.
    #' @param modelId The model ID of the record.
    #' @param previewBins Aggregated predictions for the subset. Bins boundaries may differ from actual start/end dates because this is an aggregation.
    #' @param projectId The project ID of the record.
    #' @param recordId The ID of the anomaly assessment record.
    #' @param seriesId The series id of the record. Applicable in multiseries projects
    #' @param source The source of the record
    #' @param startDate ISO-formatted first timestamp in the subset. For example: &#x60;&#x60;2019-08-01T00:00:00.000000Z&#x60;&#x60;.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`backtest` = NULL, `endDate` = NULL, `modelId` = NULL, `previewBins` = NULL, `projectId` = NULL, `recordId` = NULL, `seriesId` = NULL, `source` = NULL, `startDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`backtest`, `endDate`, `modelId`, `previewBins`, `projectId`, `recordId`, `seriesId`, `source`, `startDate`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(backtest, endDate, modelId, previewBins, projectId, recordId, seriesId, source, startDate)
      }
      self$`backtest` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = backtest)
      self$`endDate` <- `endDate`
      self$`modelId` <- `modelId`
      sapply(`previewBins`, function(x) stopifnot(R6::is.R6(x)))
      self$`projectId` <- `projectId`
      self$`recordId` <- `recordId`
      self$`seriesId` <- `seriesId`
      self$`source` <- `source`
      self$`startDate` <- `startDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(backtest = self$`backtest`, endDate = self$`endDate`, modelId = self$`modelId`, previewBins = self$`previewBins`, projectId = self$`projectId`, recordId = self$`recordId`, seriesId = self$`seriesId`, source = self$`source`, startDate = self$`startDate`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtest`)) {
          sprintf(
            '"backtest":
          %s
        ',
            self$`backtest`
          )
        },
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`previewBins`)) {
          sprintf(
            '"previewBins":
            [%s]
',
            paste(sapply(self$`previewBins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`recordId`)) {
          sprintf(
            '"recordId":
            "%s"
                  ',
            self$`recordId`
          )
        },
        if (!is.null(self$`seriesId`)) {
          sprintf(
            '"seriesId":
            "%s"
                  ',
            self$`seriesId`
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AnomalyAssessmentPreviewResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AnomalyAssessmentPreviewResponseJson, validateParams = FALSE) {
      AnomalyAssessmentPreviewResponseObject <- jsonlite::fromJSON(AnomalyAssessmentPreviewResponseJson)
      self$`backtest` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = AnomalyAssessmentPreviewResponseObject$backtest)
      self$`endDate` <- ParseRFC3339Timestamp(AnomalyAssessmentPreviewResponseObject$`endDate`)
      self$`modelId` <- AnomalyAssessmentPreviewResponseObject$`modelId`
      self$`previewBins` <- ApiClient$new()$deserializeObj(AnomalyAssessmentPreviewResponseObject$`previewBins`, "array[BinResponse]", loadNamespace("datarobot.apicore"))
      self$`projectId` <- AnomalyAssessmentPreviewResponseObject$`projectId`
      self$`recordId` <- AnomalyAssessmentPreviewResponseObject$`recordId`
      self$`seriesId` <- AnomalyAssessmentPreviewResponseObject$`seriesId`
      self$`source` <- AnomalyAssessmentPreviewResponseObject$`source`
      self$`startDate` <- ParseRFC3339Timestamp(AnomalyAssessmentPreviewResponseObject$`startDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
