# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title WorkspaceStateCreatedFromQueryGeneratorResponse
#'
#' @description WorkspaceStateCreatedFromQueryGeneratorResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasets  list( \link{DataEngineDataset} ) Source datasets in the Data Engine workspace.
#'
#' @field language  character Language of the Data Engine query.
#'
#' @field query  character Actual body of the Data Engine query.
#'
#' @field queryGeneratorId  character Query generator id.
#'
#' @field workspaceStateId  character Data Engine workspace state ID.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
WorkspaceStateCreatedFromQueryGeneratorResponse <- R6::R6Class(
  "WorkspaceStateCreatedFromQueryGeneratorResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datasets` = NULL, `language` = NULL, `query` = NULL, `queryGeneratorId` = NULL, `workspaceStateId` = NULL) {
      if (!is.null(`datasets`)) {
        stopifnot(is.vector(`datasets`))
      }
      if (!is.null(`language`)) {
        stopifnot(is.character(`language`), length(`language`) == 1)
      }
      if (!is.null(`query`)) {
        stopifnot(is.character(`query`), length(`query`) == 1)
      }
      if (!is.null(`queryGeneratorId`)) {
        stopifnot(is.character(`queryGeneratorId`), length(`queryGeneratorId`) == 1)
      }
      if (!is.null(`workspaceStateId`)) {
        stopifnot(is.character(`workspaceStateId`), length(`workspaceStateId`) == 1)
      }
    }
  ),
  public = list(
    `datasets` = NULL,
    `language` = NULL,
    `query` = NULL,
    `queryGeneratorId` = NULL,
    `workspaceStateId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasets Source datasets in the Data Engine workspace.
    #' @param language Language of the Data Engine query.
    #' @param query Actual body of the Data Engine query.
    #' @param queryGeneratorId Query generator id.
    #' @param workspaceStateId Data Engine workspace state ID.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasets` = NULL, `language` = NULL, `query` = NULL, `queryGeneratorId` = NULL, `workspaceStateId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datasets`, `language`, `query`, `queryGeneratorId`, `workspaceStateId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datasets, language, query, queryGeneratorId, workspaceStateId)
      }
      sapply(`datasets`, function(x) stopifnot(R6::is.R6(x)))
      self$`language` <- `language`
      self$`query` <- `query`
      self$`queryGeneratorId` <- `queryGeneratorId`
      self$`workspaceStateId` <- `workspaceStateId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datasets = self$`datasets`, language = self$`language`, query = self$`query`, queryGeneratorId = self$`queryGeneratorId`, workspaceStateId = self$`workspaceStateId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasets`)) {
          sprintf(
            '"datasets":
            [%s]
      ',
            paste(sapply(self$`datasets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`language`)) {
          sprintf(
            '"language":
            "%s"
                  ',
            self$`language`
          )
        },
        if (!is.null(self$`query`)) {
          sprintf(
            '"query":
            "%s"
                  ',
            self$`query`
          )
        },
        if (!is.null(self$`queryGeneratorId`)) {
          sprintf(
            '"queryGeneratorId":
            "%s"
                  ',
            self$`queryGeneratorId`
          )
        },
        if (!is.null(self$`workspaceStateId`)) {
          sprintf(
            '"workspaceStateId":
            "%s"
                  ',
            self$`workspaceStateId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param WorkspaceStateCreatedFromQueryGeneratorResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(WorkspaceStateCreatedFromQueryGeneratorResponseJson, validateParams = FALSE) {
      WorkspaceStateCreatedFromQueryGeneratorResponseObject <- jsonlite::fromJSON(WorkspaceStateCreatedFromQueryGeneratorResponseJson)
      self$`datasets` <- ApiClient$new()$deserializeObj(WorkspaceStateCreatedFromQueryGeneratorResponseObject$`datasets`, "array[DataEngineDataset]", loadNamespace("datarobot.apicore"))
      self$`language` <- WorkspaceStateCreatedFromQueryGeneratorResponseObject$`language`
      self$`query` <- WorkspaceStateCreatedFromQueryGeneratorResponseObject$`query`
      self$`queryGeneratorId` <- WorkspaceStateCreatedFromQueryGeneratorResponseObject$`queryGeneratorId`
      self$`workspaceStateId` <- WorkspaceStateCreatedFromQueryGeneratorResponseObject$`workspaceStateId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
