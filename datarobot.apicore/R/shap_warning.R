# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ShapWarning
#'
#' @description ShapWarning Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field partitionName  character The partition used for the prediction record.
#'
#' @field value  \link{ShapWarningItems}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShapWarning <- R6::R6Class(
  "ShapWarning",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(partitionName = NULL, value = NULL) {
      if (!is.null(`partitionName`)) {
        stopifnot(is.character(`partitionName`), length(`partitionName`) == 1)
      }
      if (!is.null(`value`)) {
        stopifnot(R6::is.R6(`value`))
      }
    }
  ),
  public = list(
    `partitionName` = NULL,
    `value` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param partitionName The partition used for the prediction record.
    #' @param value
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`partitionName` = NULL, `value` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`partitionName`, `value`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(partitionName, value)
      }
      self$`partitionName` <- `partitionName`
      self$`value` <- `value`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(partitionName = self$`partitionName`, value = self$`value`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`partitionName`)) {
          sprintf(
            '"partitionName":
            "%s"
                  ',
            self$`partitionName`
          )
        },
        if (!is.null(self$`value`)) {
          sprintf(
            '"value":
          %s
        ',
            jsonlite::toJSON(self$`value`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ShapWarningJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ShapWarningJson, validateParams = FALSE) {
      ShapWarningObject <- jsonlite::fromJSON(ShapWarningJson)
      self$`partitionName` <- ShapWarningObject$`partitionName`
      self$`value` <- ShapWarningItems$new()$fromJSON(jsonlite::toJSON(ShapWarningObject$value, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
