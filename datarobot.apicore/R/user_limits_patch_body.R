# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserLimitsPatchBody
#'
#' @description UserLimitsPatchBody Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accountLimits  \link{AccountLimits} [optional]
#'
#' @field rateLimits  list( \link{RateLimits} ) [optional] The rate limits for the user that are available to be updated
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserLimitsPatchBody <- R6::R6Class(
  "UserLimitsPatchBody",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`accountLimits` = NULL, `rateLimits` = NULL) {
      if (!is.null(`accountLimits`)) {
        stopifnot(R6::is.R6(`accountLimits`))
      }
      if (!is.null(`rateLimits`)) {
        stopifnot(is.vector(`rateLimits`))
      }
    }
  ),
  public = list(
    `accountLimits` = NULL,
    `rateLimits` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accountLimits
    #' @param rateLimits The rate limits for the user that are available to be updated
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`accountLimits` = NULL, `rateLimits` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(accountLimits, rateLimits)
      }
      self$`accountLimits` <- `accountLimits`
      sapply(`rateLimits`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(accountLimits = self$`accountLimits`, rateLimits = self$`rateLimits`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accountLimits`)) {
          sprintf(
            '"accountLimits":
            %s
      ',
            jsonlite::toJSON(self$`accountLimits`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`rateLimits`)) {
          sprintf(
            '"rateLimits":
            [%s]
      ',
            paste(sapply(self$`rateLimits`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserLimitsPatchBodyJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserLimitsPatchBodyJson, validateParams = FALSE) {
      UserLimitsPatchBodyObject <- jsonlite::fromJSON(UserLimitsPatchBodyJson)
      self$`accountLimits` <- AccountLimits$new()$fromJSON(jsonlite::toJSON(UserLimitsPatchBodyObject$accountLimits, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`rateLimits` <- ApiClient$new()$deserializeObj(UserLimitsPatchBodyObject$`rateLimits`, "array[RateLimits]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
