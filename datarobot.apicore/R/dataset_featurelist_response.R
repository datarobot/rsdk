# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetFeaturelistResponse
#'
#' @description DatasetFeaturelistResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field createdBy  character &#x60;username&#x60; of the user who created the dataset.
#'
#' @field creationDate  character the ISO 8601 formatted date and time when the dataset was created.
#'
#' @field datasetId  character The ID of the dataset.
#'
#' @field datasetVersionId  character The ID of the dataset version if the featurelist is associated with a specific dataset version, for example Informative Features, or null otherwise.
#'
#' @field features  list( character ) Features in the featurelist.
#'
#' @field id  character The ID of the featurelist
#'
#' @field name  character The name of the featurelist
#'
#' @field userCreated  character True if the featurelist was created by a user vs the system.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetFeaturelistResponse <- R6::R6Class(
  "DatasetFeaturelistResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`createdBy` = NULL, `creationDate` = NULL, `datasetId` = NULL, `datasetVersionId` = NULL, `features` = NULL, `id` = NULL, `name` = NULL, `userCreated` = NULL) {
      if (!is.null(`createdBy`)) {
        stopifnot(is.character(`createdBy`), length(`createdBy`) == 1)
      }
      if (!is.null(`creationDate`)) {
        stopifnot(inherits(`creationDate`, "POSIXt"))
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`datasetVersionId`)) {
        stopifnot(is.character(`datasetVersionId`), length(`datasetVersionId`) == 1)
      }
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`), sapply(`features`, is.character))
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`userCreated`)) {
        stopifnot(is.logical(`userCreated`), length(`userCreated`) == 1)
      }
    }
  ),
  public = list(
    `createdBy` = NULL,
    `creationDate` = NULL,
    `datasetId` = NULL,
    `datasetVersionId` = NULL,
    `features` = NULL,
    `id` = NULL,
    `name` = NULL,
    `userCreated` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param createdBy &#x60;username&#x60; of the user who created the dataset.
    #' @param creationDate the ISO 8601 formatted date and time when the dataset was created.
    #' @param datasetId The ID of the dataset.
    #' @param datasetVersionId The ID of the dataset version if the featurelist is associated with a specific dataset version, for example Informative Features, or null otherwise.
    #' @param features Features in the featurelist.
    #' @param id The ID of the featurelist
    #' @param name The name of the featurelist
    #' @param userCreated True if the featurelist was created by a user vs the system.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`createdBy` = NULL, `creationDate` = NULL, `datasetId` = NULL, `datasetVersionId` = NULL, `features` = NULL, `id` = NULL, `name` = NULL, `userCreated` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`createdBy`, `creationDate`, `datasetId`, `datasetVersionId`, `features`, `id`, `name`, `userCreated`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(createdBy, creationDate, datasetId, datasetVersionId, features, id, name, userCreated)
      }
      self$`createdBy` <- `createdBy`
      self$`creationDate` <- `creationDate`
      self$`datasetId` <- `datasetId`
      self$`datasetVersionId` <- `datasetVersionId`
      self$`features` <- `features`
      self$`id` <- `id`
      self$`name` <- `name`
      self$`userCreated` <- `userCreated`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(createdBy = self$`createdBy`, creationDate = self$`creationDate`, datasetId = self$`datasetId`, datasetVersionId = self$`datasetVersionId`, features = self$`features`, id = self$`id`, name = self$`name`, userCreated = self$`userCreated`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`createdBy`)) {
          sprintf(
            '"createdBy":
            "%s"
                  ',
            self$`createdBy`
          )
        },
        if (!is.null(self$`creationDate`)) {
          sprintf(
            '"creationDate":
            "%s"
                  ',
            format(self$`creationDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetVersionId`)) {
          sprintf(
            '"datasetVersionId":
            "%s"
                  ',
            self$`datasetVersionId`
          )
        },
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
                  ',
            paste(unlist(lapply(self$`features`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`userCreated`)) {
          sprintf(
            '"userCreated":
            %s
                  ',
            tolower(self$`userCreated`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetFeaturelistResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetFeaturelistResponseJson, validateParams = FALSE) {
      DatasetFeaturelistResponseObject <- jsonlite::fromJSON(DatasetFeaturelistResponseJson)
      self$`createdBy` <- DatasetFeaturelistResponseObject$`createdBy`
      self$`creationDate` <- ParseRFC3339Timestamp(DatasetFeaturelistResponseObject$`creationDate`)
      self$`datasetId` <- DatasetFeaturelistResponseObject$`datasetId`
      self$`datasetVersionId` <- DatasetFeaturelistResponseObject$`datasetVersionId`
      self$`features` <- ApiClient$new()$deserializeObj(DatasetFeaturelistResponseObject$`features`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`id` <- DatasetFeaturelistResponseObject$`id`
      self$`name` <- DatasetFeaturelistResponseObject$`name`
      self$`userCreated` <- DatasetFeaturelistResponseObject$`userCreated`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
