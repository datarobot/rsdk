# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureLineageJoin
#'
#' @description FeatureLineageJoin Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field joinType  character Kind of SQL JOIN applied.
#'
#' @field leftTable  \link{FeatureLineageJoinTable}
#'
#' @field rightTable  \link{FeatureLineageJoinTable}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureLineageJoin <- R6::R6Class(
  "FeatureLineageJoin",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`joinType` = NULL, `leftTable` = NULL, `rightTable` = NULL) {
      if (!is.null(`joinType`)) {
        stopifnot(is.character(`joinType`), length(`joinType`) == 1)
      }
      if (!is.null(`leftTable`)) {
        stopifnot(R6::is.R6(`leftTable`))
      }
      if (!is.null(`rightTable`)) {
        stopifnot(R6::is.R6(`rightTable`))
      }
    }
  ),
  public = list(
    `joinType` = NULL,
    `leftTable` = NULL,
    `rightTable` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param joinType Kind of SQL JOIN applied.
    #' @param leftTable
    #' @param rightTable
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`joinType` = NULL, `leftTable` = NULL, `rightTable` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`joinType`, `leftTable`, `rightTable`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(joinType, leftTable, rightTable)
      }
      self$`joinType` <- `joinType`
      self$`leftTable` <- `leftTable`
      self$`rightTable` <- `rightTable`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(joinType = self$`joinType`, leftTable = self$`leftTable`, rightTable = self$`rightTable`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`joinType`)) {
          sprintf(
            '"joinType":
            "%s"
                  ',
            self$`joinType`
          )
        },
        if (!is.null(self$`leftTable`)) {
          sprintf(
            '"leftTable":
            %s
      ',
            jsonlite::toJSON(self$`leftTable`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`rightTable`)) {
          sprintf(
            '"rightTable":
            %s
      ',
            jsonlite::toJSON(self$`rightTable`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureLineageJoinJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureLineageJoinJson, validateParams = FALSE) {
      FeatureLineageJoinObject <- jsonlite::fromJSON(FeatureLineageJoinJson)
      self$`joinType` <- FeatureLineageJoinObject$`joinType`
      self$`leftTable` <- FeatureLineageJoinTable$new()$fromJSON(jsonlite::toJSON(FeatureLineageJoinObject$leftTable, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`rightTable` <- FeatureLineageJoinTable$new()$fromJSON(jsonlite::toJSON(FeatureLineageJoinObject$rightTable, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
