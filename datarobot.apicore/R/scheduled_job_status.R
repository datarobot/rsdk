# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ScheduledJobStatus
#'
#' @description ScheduledJobStatus Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field lastFailedRun  character [optional] Date and time of the last failed run.
#'
#' @field lastSuccessfulRun  character [optional] Date and time of the last successful run.
#'
#' @field nextRunTime  character [optional] Date and time of the next run.
#'
#' @field queuePosition  integer [optional] Position of the job in the queue Job. The value will show 0 if the job is about to run, otherwise, the number will be greater than 0 if currently queued, or None if the job is not currently running.
#'
#' @field running  character &#x60;true&#x60; or &#x60;false&#x60; depending on whether the job is currently running.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ScheduledJobStatus <- R6::R6Class(
  "ScheduledJobStatus",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(lastFailedRun = NULL, lastSuccessfulRun = NULL, nextRunTime = NULL, queuePosition = NULL, running = NULL) {
      if (!is.null(`running`)) {
        stopifnot(is.logical(`running`), length(`running`) == 1)
      }
      if (!is.null(`lastFailedRun`)) {
        stopifnot(inherits(`lastFailedRun`, "POSIXt"))
      }
      if (!is.null(`lastSuccessfulRun`)) {
        stopifnot(inherits(`lastSuccessfulRun`, "POSIXt"))
      }
      if (!is.null(`nextRunTime`)) {
        stopifnot(inherits(`nextRunTime`, "POSIXt"))
      }
      if (!is.null(`queuePosition`)) {
        stopifnot(is.numeric(`queuePosition`), length(`queuePosition`) == 1)
      }
    }
  ),
  public = list(
    `lastFailedRun` = NULL,
    `lastSuccessfulRun` = NULL,
    `nextRunTime` = NULL,
    `queuePosition` = NULL,
    `running` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param lastFailedRun Date and time of the last failed run.
    #' @param lastSuccessfulRun Date and time of the last successful run.
    #' @param nextRunTime Date and time of the next run.
    #' @param queuePosition Position of the job in the queue Job. The value will show 0 if the job is about to run, otherwise, the number will be greater than 0 if currently queued, or None if the job is not currently running.
    #' @param running &#x60;true&#x60; or &#x60;false&#x60; depending on whether the job is currently running.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`running` = NULL, `lastFailedRun` = NULL, `lastSuccessfulRun` = NULL, `nextRunTime` = NULL, `queuePosition` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`running`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(lastFailedRun, lastSuccessfulRun, nextRunTime, queuePosition, running)
      }
      self$`lastFailedRun` <- `lastFailedRun`
      self$`lastSuccessfulRun` <- `lastSuccessfulRun`
      self$`nextRunTime` <- `nextRunTime`
      self$`queuePosition` <- `queuePosition`
      self$`running` <- `running`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(lastFailedRun = self$`lastFailedRun`, lastSuccessfulRun = self$`lastSuccessfulRun`, nextRunTime = self$`nextRunTime`, queuePosition = self$`queuePosition`, running = self$`running`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`lastFailedRun`)) {
          sprintf(
            '"lastFailedRun":
            "%s"
                  ',
            format(self$`lastFailedRun`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`lastSuccessfulRun`)) {
          sprintf(
            '"lastSuccessfulRun":
            "%s"
                  ',
            format(self$`lastSuccessfulRun`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`nextRunTime`)) {
          sprintf(
            '"nextRunTime":
            "%s"
                  ',
            format(self$`nextRunTime`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`queuePosition`)) {
          sprintf(
            '"queuePosition":
            %d
                  ',
            self$`queuePosition`
          )
        },
        if (!is.null(self$`running`)) {
          sprintf(
            '"running":
            %s
                  ',
            tolower(self$`running`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ScheduledJobStatusJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ScheduledJobStatusJson, validateParams = FALSE) {
      ScheduledJobStatusObject <- jsonlite::fromJSON(ScheduledJobStatusJson)
      self$`lastFailedRun` <- ParseRFC3339Timestamp(ScheduledJobStatusObject$`lastFailedRun`)
      self$`lastSuccessfulRun` <- ParseRFC3339Timestamp(ScheduledJobStatusObject$`lastSuccessfulRun`)
      self$`nextRunTime` <- ParseRFC3339Timestamp(ScheduledJobStatusObject$`nextRunTime`)
      self$`queuePosition` <- ScheduledJobStatusObject$`queuePosition`
      self$`running` <- ScheduledJobStatusObject$`running`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
