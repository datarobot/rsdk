# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelFeatureListResponse
#'
#' @description ModelFeatureListResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field aPrioriFeatureNames  list( character ) (Deprecated in version v2.11) Renamed to &#x60;knownInAdvanceFeatureNames&#x60;. This parameter always has the same value as &#x60;knownInAdvanceFeatureNames&#x60; and will be removed in a future release.
#'
#' @field featureNames  list( character ) An array of the names of all features used by the specified model.
#'
#' @field knownInAdvanceFeatureNames  list( character ) An array of the names of time series known-in-advance features used by the specified model.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelFeatureListResponse <- R6::R6Class(
  "ModelFeatureListResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`aPrioriFeatureNames` = NULL, `featureNames` = NULL, `knownInAdvanceFeatureNames` = NULL) {
      if (!is.null(`aPrioriFeatureNames`)) {
        stopifnot(is.vector(`aPrioriFeatureNames`))
      }
      if (!is.null(`featureNames`)) {
        stopifnot(is.vector(`featureNames`))
      }
      if (!is.null(`knownInAdvanceFeatureNames`)) {
        stopifnot(is.vector(`knownInAdvanceFeatureNames`))
      }
    }
  ),
  public = list(
    `aPrioriFeatureNames` = NULL,
    `featureNames` = NULL,
    `knownInAdvanceFeatureNames` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param aPrioriFeatureNames (Deprecated in version v2.11) Renamed to &#x60;knownInAdvanceFeatureNames&#x60;. This parameter always has the same value as &#x60;knownInAdvanceFeatureNames&#x60; and will be removed in a future release.
    #' @param featureNames An array of the names of all features used by the specified model.
    #' @param knownInAdvanceFeatureNames An array of the names of time series known-in-advance features used by the specified model.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`aPrioriFeatureNames` = NULL, `featureNames` = NULL, `knownInAdvanceFeatureNames` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`aPrioriFeatureNames`, `featureNames`, `knownInAdvanceFeatureNames`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(aPrioriFeatureNames, featureNames, knownInAdvanceFeatureNames)
      }
      sapply(`aPrioriFeatureNames`, function(x) stopifnot(is.character(x)))
      sapply(`featureNames`, function(x) stopifnot(is.character(x)))
      sapply(`knownInAdvanceFeatureNames`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(aPrioriFeatureNames = self$`aPrioriFeatureNames`, featureNames = self$`featureNames`, knownInAdvanceFeatureNames = self$`knownInAdvanceFeatureNames`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`aPrioriFeatureNames`)) {
          sprintf(
            '"aPrioriFeatureNames":
            [%s]
                  ',
            paste(unlist(lapply(self$`aPrioriFeatureNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`featureNames`)) {
          sprintf(
            '"featureNames":
            [%s]
                  ',
            paste(unlist(lapply(self$`featureNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`knownInAdvanceFeatureNames`)) {
          sprintf(
            '"knownInAdvanceFeatureNames":
            [%s]
                  ',
            paste(unlist(lapply(self$`knownInAdvanceFeatureNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelFeatureListResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelFeatureListResponseJson, validateParams = FALSE) {
      ModelFeatureListResponseObject <- jsonlite::fromJSON(ModelFeatureListResponseJson)
      self$`aPrioriFeatureNames` <- ApiClient$new()$deserializeObj(ModelFeatureListResponseObject$`aPrioriFeatureNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`featureNames` <- ApiClient$new()$deserializeObj(ModelFeatureListResponseObject$`featureNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`knownInAdvanceFeatureNames` <- ApiClient$new()$deserializeObj(ModelFeatureListResponseObject$`knownInAdvanceFeatureNames`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
