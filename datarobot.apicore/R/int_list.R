# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title IntList
#'
#' @description IntList Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field maxLength  integer Maximum permitted length of the list.
#'
#' @field maxVal  integer Maximum permitted value.
#'
#' @field minLength  integer Minimum permitted length of the list.
#'
#' @field minVal  integer Minimum permitted value.
#'
#' @field supportsGridSearch  character When True, Grid Search is supported for this parameter.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
IntList <- R6::R6Class(
  "IntList",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(maxLength = NULL, maxVal = NULL, minLength = NULL, minVal = NULL, supportsGridSearch = NULL) {
      if (!is.null(`maxLength`)) {
        stopifnot(is.numeric(`maxLength`), length(`maxLength`) == 1)
      }
      if (!is.null(`maxVal`)) {
        stopifnot(is.numeric(`maxVal`), length(`maxVal`) == 1)
      }
      if (!is.null(`minLength`)) {
        stopifnot(is.numeric(`minLength`), length(`minLength`) == 1)
      }
      if (!is.null(`minVal`)) {
        stopifnot(is.numeric(`minVal`), length(`minVal`) == 1)
      }
      if (!is.null(`supportsGridSearch`)) {
        stopifnot(is.logical(`supportsGridSearch`), length(`supportsGridSearch`) == 1)
      }
    }
  ),
  public = list(
    `maxLength` = NULL,
    `maxVal` = NULL,
    `minLength` = NULL,
    `minVal` = NULL,
    `supportsGridSearch` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param maxLength Maximum permitted length of the list.
    #' @param maxVal Maximum permitted value.
    #' @param minLength Minimum permitted length of the list.
    #' @param minVal Minimum permitted value.
    #' @param supportsGridSearch When True, Grid Search is supported for this parameter.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`maxLength` = NULL, `maxVal` = NULL, `minLength` = NULL, `minVal` = NULL, `supportsGridSearch` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`maxLength`, `maxVal`, `minLength`, `minVal`, `supportsGridSearch`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(maxLength, maxVal, minLength, minVal, supportsGridSearch)
      }
      self$`maxLength` <- `maxLength`
      self$`maxVal` <- `maxVal`
      self$`minLength` <- `minLength`
      self$`minVal` <- `minVal`
      self$`supportsGridSearch` <- `supportsGridSearch`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(maxLength = self$`maxLength`, maxVal = self$`maxVal`, minLength = self$`minLength`, minVal = self$`minVal`, supportsGridSearch = self$`supportsGridSearch`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`maxLength`)) {
          sprintf(
            '"maxLength":
            %d
                  ',
            self$`maxLength`
          )
        },
        if (!is.null(self$`maxVal`)) {
          sprintf(
            '"maxVal":
            %d
                  ',
            self$`maxVal`
          )
        },
        if (!is.null(self$`minLength`)) {
          sprintf(
            '"minLength":
            %d
                  ',
            self$`minLength`
          )
        },
        if (!is.null(self$`minVal`)) {
          sprintf(
            '"minVal":
            %d
                  ',
            self$`minVal`
          )
        },
        if (!is.null(self$`supportsGridSearch`)) {
          sprintf(
            '"supportsGridSearch":
            %s
                  ',
            tolower(self$`supportsGridSearch`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param IntListJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(IntListJson, validateParams = FALSE) {
      IntListObject <- jsonlite::fromJSON(IntListJson)
      self$`maxLength` <- IntListObject$`maxLength`
      self$`maxVal` <- IntListObject$`maxVal`
      self$`minLength` <- IntListObject$`minLength`
      self$`minVal` <- IntListObject$`minVal`
      self$`supportsGridSearch` <- IntListObject$`supportsGridSearch`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
