# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanation
#'
#' @description PredictionExplanation Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field feature  character The name of the feature contributing to the prediction.
#'
#' @field featureValue  character The value the feature took on for this row. For image features, this value is the URL of the input image (New in v2.21).
#'
#' @field imageExplanationUrl  character For image features, the URL of the image containing the input image overlaid by the activation heatmap. For non-image features, this field is null.
#'
#' @field label  character Describes what this model output corresponds to. For regression projects, it is the name of the target feature. For classification projects, it is a level from the target feature. For Anomaly Detection models it is an Anomaly Score.
#'
#' @field perNgramTextExplanations  list( \link{PerNgramTextExplanations} ) [optional] For text features, an array of JSON object containing the per ngram based text prediction explanations.
#'
#' @field qualitativateStrength  character A human-readable description of how strongly the feature affected the prediction (e.g. &#39;+++&#39;, &#39;--&#39;, &#39;+&#39;, &#39;&lt;+&#39;, &#39;&lt;-&#39;).
#'
#' @field strength  numeric The amount this feature&#39;s value affected the prediction.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanation <- R6::R6Class(
  "PredictionExplanation",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(feature = NULL, featureValue = NULL, imageExplanationUrl = NULL, label = NULL, perNgramTextExplanations = NULL, qualitativateStrength = NULL, strength = NULL) {
      if (!is.null(`feature`)) {
        stopifnot(is.character(`feature`), length(`feature`) == 1)
      }
      if (!is.null(`featureValue`)) {
        stopifnot(is.character(`featureValue`), length(`featureValue`) == 1)
      }
      if (!is.null(`imageExplanationUrl`)) {
        stopifnot(is.character(`imageExplanationUrl`), length(`imageExplanationUrl`) == 1)
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`qualitativateStrength`)) {
        stopifnot(is.character(`qualitativateStrength`), length(`qualitativateStrength`) == 1)
      }
      if (!is.null(`strength`)) {
      }
      if (!is.null(`perNgramTextExplanations`)) {
        stopifnot(is.vector(`perNgramTextExplanations`))
      }
    }
  ),
  public = list(
    `feature` = NULL,
    `featureValue` = NULL,
    `imageExplanationUrl` = NULL,
    `label` = NULL,
    `perNgramTextExplanations` = NULL,
    `qualitativateStrength` = NULL,
    `strength` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param feature The name of the feature contributing to the prediction.
    #' @param featureValue The value the feature took on for this row. For image features, this value is the URL of the input image (New in v2.21).
    #' @param imageExplanationUrl For image features, the URL of the image containing the input image overlaid by the activation heatmap. For non-image features, this field is null.
    #' @param label Describes what this model output corresponds to. For regression projects, it is the name of the target feature. For classification projects, it is a level from the target feature. For Anomaly Detection models it is an Anomaly Score.
    #' @param perNgramTextExplanations For text features, an array of JSON object containing the per ngram based text prediction explanations.
    #' @param qualitativateStrength A human-readable description of how strongly the feature affected the prediction (e.g. &#39;+++&#39;, &#39;--&#39;, &#39;+&#39;, &#39;&lt;+&#39;, &#39;&lt;-&#39;).
    #' @param strength The amount this feature&#39;s value affected the prediction.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`feature` = NULL, `featureValue` = NULL, `imageExplanationUrl` = NULL, `label` = NULL, `qualitativateStrength` = NULL, `strength` = NULL, `perNgramTextExplanations` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`feature`, `featureValue`, `imageExplanationUrl`, `label`, `qualitativateStrength`, `strength`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(feature, featureValue, imageExplanationUrl, label, perNgramTextExplanations, qualitativateStrength, strength)
      }
      self$`feature` <- `feature`
      self$`featureValue` <- `featureValue`
      self$`imageExplanationUrl` <- `imageExplanationUrl`
      self$`label` <- `label`
      sapply(`perNgramTextExplanations`, function(x) stopifnot(R6::is.R6(x)))
      self$`qualitativateStrength` <- `qualitativateStrength`
      self$`strength` <- `strength`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(feature = self$`feature`, featureValue = self$`featureValue`, imageExplanationUrl = self$`imageExplanationUrl`, label = self$`label`, perNgramTextExplanations = self$`perNgramTextExplanations`, qualitativateStrength = self$`qualitativateStrength`, strength = self$`strength`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`feature`)) {
          sprintf(
            '"feature":
            "%s"
                  ',
            self$`feature`
          )
        },
        if (!is.null(self$`featureValue`)) {
          sprintf(
            '"featureValue":
            "%s"
                  ',
            self$`featureValue`
          )
        },
        if (!is.null(self$`imageExplanationUrl`)) {
          sprintf(
            '"imageExplanationUrl":
            "%s"
                  ',
            self$`imageExplanationUrl`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`perNgramTextExplanations`)) {
          sprintf(
            '"perNgramTextExplanations":
            [%s]
',
            paste(sapply(self$`perNgramTextExplanations`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`qualitativateStrength`)) {
          sprintf(
            '"qualitativateStrength":
            "%s"
                  ',
            self$`qualitativateStrength`
          )
        },
        if (!is.null(self$`strength`)) {
          sprintf(
            '"strength":
            %d
                  ',
            self$`strength`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationJson, validateParams = FALSE) {
      PredictionExplanationObject <- jsonlite::fromJSON(PredictionExplanationJson)
      self$`feature` <- PredictionExplanationObject$`feature`
      self$`featureValue` <- PredictionExplanationObject$`featureValue`
      self$`imageExplanationUrl` <- PredictionExplanationObject$`imageExplanationUrl`
      self$`label` <- PredictionExplanationObject$`label`
      self$`perNgramTextExplanations` <- ApiClient$new()$deserializeObj(PredictionExplanationObject$`perNgramTextExplanations`, "array[PerNgramTextExplanations]", loadNamespace("datarobot.apicore"))
      self$`qualitativateStrength` <- PredictionExplanationObject$`qualitativateStrength`
      self$`strength` <- PredictionExplanationObject$`strength`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
