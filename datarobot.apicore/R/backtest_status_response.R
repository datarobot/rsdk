# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BacktestStatusResponse
#'
#' @description BacktestStatusResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field index  integer the index of the fold
#'
#' @field score  numeric the score of the model for this backtesting fold, if computed
#'
#' @field status  character the status of the current backtest model job
#'
#' @field trainingDuration  character the duration of the data used to train the model for this backtesting fold
#'
#' @field trainingEndDate  character the end date of the training for this backtesting fold
#'
#' @field trainingRowCount  integer the number of rows used to train the model for this backtesting fold
#'
#' @field trainingStartDate  character the start date of the training for this backtesting fold
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BacktestStatusResponse <- R6::R6Class(
  "BacktestStatusResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`index` = NULL, `score` = NULL, `status` = NULL, `trainingDuration` = NULL, `trainingEndDate` = NULL, `trainingRowCount` = NULL, `trainingStartDate` = NULL) {
      if (!is.null(`index`)) {
        stopifnot(is.numeric(`index`), length(`index`) == 1)
      }
      if (!is.null(`score`)) {
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
      if (!is.null(`trainingDuration`)) {
        stopifnot(is.character(`trainingDuration`), length(`trainingDuration`) == 1)
      }
      if (!is.null(`trainingEndDate`)) {
        stopifnot(inherits(`trainingEndDate`, "POSIXt"))
      }
      if (!is.null(`trainingRowCount`)) {
        stopifnot(is.numeric(`trainingRowCount`), length(`trainingRowCount`) == 1)
      }
      if (!is.null(`trainingStartDate`)) {
        stopifnot(inherits(`trainingStartDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `index` = NULL,
    `score` = NULL,
    `status` = NULL,
    `trainingDuration` = NULL,
    `trainingEndDate` = NULL,
    `trainingRowCount` = NULL,
    `trainingStartDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param index the index of the fold
    #' @param score the score of the model for this backtesting fold, if computed
    #' @param status the status of the current backtest model job
    #' @param trainingDuration the duration of the data used to train the model for this backtesting fold
    #' @param trainingEndDate the end date of the training for this backtesting fold
    #' @param trainingRowCount the number of rows used to train the model for this backtesting fold
    #' @param trainingStartDate the start date of the training for this backtesting fold
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`index` = NULL, `score` = NULL, `status` = NULL, `trainingDuration` = NULL, `trainingEndDate` = NULL, `trainingRowCount` = NULL, `trainingStartDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`index`, `score`, `status`, `trainingDuration`, `trainingEndDate`, `trainingRowCount`, `trainingStartDate`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(index, score, status, trainingDuration, trainingEndDate, trainingRowCount, trainingStartDate)
      }
      self$`index` <- `index`
      self$`score` <- `score`
      self$`status` <- `status`
      self$`trainingDuration` <- `trainingDuration`
      self$`trainingEndDate` <- `trainingEndDate`
      self$`trainingRowCount` <- `trainingRowCount`
      self$`trainingStartDate` <- `trainingStartDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(index = self$`index`, score = self$`score`, status = self$`status`, trainingDuration = self$`trainingDuration`, trainingEndDate = self$`trainingEndDate`, trainingRowCount = self$`trainingRowCount`, trainingStartDate = self$`trainingStartDate`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`index`)) {
          sprintf(
            '"index":
            %d
                  ',
            self$`index`
          )
        },
        if (!is.null(self$`score`)) {
          sprintf(
            '"score":
            %d
                  ',
            self$`score`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        },
        if (!is.null(self$`trainingDuration`)) {
          sprintf(
            '"trainingDuration":
            "%s"
                  ',
            self$`trainingDuration`
          )
        },
        if (!is.null(self$`trainingEndDate`)) {
          sprintf(
            '"trainingEndDate":
            "%s"
                  ',
            format(self$`trainingEndDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`trainingRowCount`)) {
          sprintf(
            '"trainingRowCount":
            %d
                  ',
            self$`trainingRowCount`
          )
        },
        if (!is.null(self$`trainingStartDate`)) {
          sprintf(
            '"trainingStartDate":
            "%s"
                  ',
            format(self$`trainingStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BacktestStatusResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BacktestStatusResponseJson, validateParams = FALSE) {
      BacktestStatusResponseObject <- jsonlite::fromJSON(BacktestStatusResponseJson)
      self$`index` <- BacktestStatusResponseObject$`index`
      self$`score` <- BacktestStatusResponseObject$`score`
      self$`status` <- BacktestStatusResponseObject$`status`
      self$`trainingDuration` <- BacktestStatusResponseObject$`trainingDuration`
      self$`trainingEndDate` <- ParseRFC3339Timestamp(BacktestStatusResponseObject$`trainingEndDate`)
      self$`trainingRowCount` <- BacktestStatusResponseObject$`trainingRowCount`
      self$`trainingStartDate` <- ParseRFC3339Timestamp(BacktestStatusResponseObject$`trainingStartDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
