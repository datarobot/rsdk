# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanationsMetadataValues
#'
#' @description PredictionExplanationsMetadataValues Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field shapRemainingTotal  integer [optional] Will be present only if &#x60;explanationAlgorithm&#x60; &#x3D; &#39;shap&#39; and &#x60;maxExplanations&#x60; is nonzero. The total of SHAP values for features beyond the &#x60;maxExplanations&#x60;. This can be identically 0 in all rows, if &#x60;maxExplanations&#x60; is greater than the number of features and thus all features are returned.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanationsMetadataValues <- R6::R6Class(
  "PredictionExplanationsMetadataValues",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`shapRemainingTotal` = NULL) {
      if (!is.null(`shapRemainingTotal`)) {
        stopifnot(is.numeric(`shapRemainingTotal`), length(`shapRemainingTotal`) == 1)
      }
    }
  ),
  public = list(
    `shapRemainingTotal` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param shapRemainingTotal Will be present only if &#x60;explanationAlgorithm&#x60; &#x3D; &#39;shap&#39; and &#x60;maxExplanations&#x60; is nonzero. The total of SHAP values for features beyond the &#x60;maxExplanations&#x60;. This can be identically 0 in all rows, if &#x60;maxExplanations&#x60; is greater than the number of features and thus all features are returned.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`shapRemainingTotal` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(shapRemainingTotal)
      }
      self$`shapRemainingTotal` <- `shapRemainingTotal`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(shapRemainingTotal = self$`shapRemainingTotal`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`shapRemainingTotal`)) {
          sprintf(
            '"shapRemainingTotal":
            %d
                  ',
            self$`shapRemainingTotal`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationsMetadataValuesJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationsMetadataValuesJson, validateParams = FALSE) {
      PredictionExplanationsMetadataValuesObject <- jsonlite::fromJSON(PredictionExplanationsMetadataValuesJson)
      self$`shapRemainingTotal` <- PredictionExplanationsMetadataValuesObject$`shapRemainingTotal`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
