# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureAssociationDetailsRetrieveControllerResponse
#'
#' @description FeatureAssociationDetailsRetrieveControllerResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field chartType  character Which type of plotting the pair of features gets in the UI, e.g. &#x60;SCATTER&#x60;
#'
#' @field features  list( character ) The name of &#x60;feature1&#x60; and &#x60;feature2&#x60;.
#'
#' @field types  list( character ) The type of &#x60;feature1&#x60; and &#x60;feature2&#x60;. Possible values: &#x60;CATEGORICAL&#x60;, &#x60;NUMERIC&#x60;.
#'
#' @field values  list( \link{array[AnyOfstringnumber]} ) The data triplets for pairwise plotting.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureAssociationDetailsRetrieveControllerResponse <- R6::R6Class(
  "FeatureAssociationDetailsRetrieveControllerResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`chartType` = NULL, `features` = NULL, `types` = NULL, `values` = NULL) {
      if (!is.null(`chartType`)) {
        stopifnot(is.character(`chartType`), length(`chartType`) == 1)
      }
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`))
      }
      if (!is.null(`types`)) {
        stopifnot(is.vector(`types`))
      }
      if (!is.null(`values`)) {
        stopifnot(is.vector(`values`))
      }
    }
  ),
  public = list(
    `chartType` = NULL,
    `features` = NULL,
    `types` = NULL,
    `values` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param chartType Which type of plotting the pair of features gets in the UI, e.g. &#x60;SCATTER&#x60;
    #' @param features The name of &#x60;feature1&#x60; and &#x60;feature2&#x60;.
    #' @param types The type of &#x60;feature1&#x60; and &#x60;feature2&#x60;. Possible values: &#x60;CATEGORICAL&#x60;, &#x60;NUMERIC&#x60;.
    #' @param values The data triplets for pairwise plotting.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`chartType` = NULL, `features` = NULL, `types` = NULL, `values` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`chartType`, `features`, `types`, `values`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(chartType, features, types, values)
      }
      self$`chartType` <- `chartType`
      sapply(`features`, function(x) stopifnot(is.character(x)))
      sapply(`types`, function(x) stopifnot(is.character(x)))
      sapply(`values`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(chartType = self$`chartType`, features = self$`features`, types = self$`types`, values = self$`values`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`chartType`)) {
          sprintf(
            '"chartType":
            "%s"
                  ',
            self$`chartType`
          )
        },
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
                  ',
            paste(unlist(lapply(self$`features`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`types`)) {
          sprintf(
            '"types":
            [%s]
                  ',
            paste(unlist(lapply(self$`types`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`values`)) {
          sprintf(
            '"values":
            [%s]
      ',
            paste(sapply(self$`values`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureAssociationDetailsRetrieveControllerResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureAssociationDetailsRetrieveControllerResponseJson, validateParams = FALSE) {
      FeatureAssociationDetailsRetrieveControllerResponseObject <- jsonlite::fromJSON(FeatureAssociationDetailsRetrieveControllerResponseJson)
      self$`chartType` <- FeatureAssociationDetailsRetrieveControllerResponseObject$`chartType`
      self$`features` <- ApiClient$new()$deserializeObj(FeatureAssociationDetailsRetrieveControllerResponseObject$`features`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`types` <- ApiClient$new()$deserializeObj(FeatureAssociationDetailsRetrieveControllerResponseObject$`types`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`values` <- ApiClient$new()$deserializeObj(FeatureAssociationDetailsRetrieveControllerResponseObject$`values`, "array[array[AnyOfstringnumber]]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
