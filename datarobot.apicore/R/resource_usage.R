# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ResourceUsage
#'
#' @description ResourceUsage Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field currentUsage  integer The usage accumulated within the current window.
#'
#' @field maxUsage  integer The maximum value currentUsage can take before requests using this resource are denied.
#'
#' @field resource  character The rate limit resource this usage applies to.
#'
#' @field timeToExpire  integer The number of seconds until this usage resets to zero.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ResourceUsage <- R6::R6Class(
  "ResourceUsage",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`currentUsage` = NULL, `maxUsage` = NULL, `resource` = NULL, `timeToExpire` = NULL) {
      if (!is.null(`currentUsage`)) {
        stopifnot(is.numeric(`currentUsage`), length(`currentUsage`) == 1)
      }
      if (!is.null(`maxUsage`)) {
        stopifnot(is.numeric(`maxUsage`), length(`maxUsage`) == 1)
      }
      if (!is.null(`resource`)) {
        stopifnot(is.character(`resource`), length(`resource`) == 1)
      }
      if (!is.null(`timeToExpire`)) {
        stopifnot(is.numeric(`timeToExpire`), length(`timeToExpire`) == 1)
      }
    }
  ),
  public = list(
    `currentUsage` = NULL,
    `maxUsage` = NULL,
    `resource` = NULL,
    `timeToExpire` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param currentUsage The usage accumulated within the current window.
    #' @param maxUsage The maximum value currentUsage can take before requests using this resource are denied.
    #' @param resource The rate limit resource this usage applies to.
    #' @param timeToExpire The number of seconds until this usage resets to zero.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`currentUsage` = NULL, `maxUsage` = NULL, `resource` = NULL, `timeToExpire` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`currentUsage`, `maxUsage`, `resource`, `timeToExpire`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(currentUsage, maxUsage, resource, timeToExpire)
      }
      self$`currentUsage` <- `currentUsage`
      self$`maxUsage` <- `maxUsage`
      self$`resource` <- `resource`
      self$`timeToExpire` <- `timeToExpire`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(currentUsage = self$`currentUsage`, maxUsage = self$`maxUsage`, resource = self$`resource`, timeToExpire = self$`timeToExpire`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`currentUsage`)) {
          sprintf(
            '"currentUsage":
            %d
                  ',
            self$`currentUsage`
          )
        },
        if (!is.null(self$`maxUsage`)) {
          sprintf(
            '"maxUsage":
            %d
                  ',
            self$`maxUsage`
          )
        },
        if (!is.null(self$`resource`)) {
          sprintf(
            '"resource":
            "%s"
                  ',
            self$`resource`
          )
        },
        if (!is.null(self$`timeToExpire`)) {
          sprintf(
            '"timeToExpire":
            %d
                  ',
            self$`timeToExpire`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ResourceUsageJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ResourceUsageJson, validateParams = FALSE) {
      ResourceUsageObject <- jsonlite::fromJSON(ResourceUsageJson)
      self$`currentUsage` <- ResourceUsageObject$`currentUsage`
      self$`maxUsage` <- ResourceUsageObject$`maxUsage`
      self$`resource` <- ResourceUsageObject$`resource`
      self$`timeToExpire` <- ResourceUsageObject$`timeToExpire`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
