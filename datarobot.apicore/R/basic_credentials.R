# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BasicCredentials
#'
#' @description BasicCredentials Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialType  character The type of these credentials, &#39;basic&#39; here.
#'
#' @field password  character The password for database authentication. The password is encrypted at rest and never saved / stored.
#'
#' @field user  character The username for database authentication.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BasicCredentials <- R6::R6Class(
  "BasicCredentials",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`credentialType` = NULL, `password` = NULL, `user` = NULL) {
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `credentialType` = NULL,
    `password` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialType The type of these credentials, &#39;basic&#39; here.
    #' @param password The password for database authentication. The password is encrypted at rest and never saved / stored.
    #' @param user The username for database authentication.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialType` = NULL, `password` = NULL, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`credentialType`, `password`, `user`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(credentialType, password, user)
      }
      self$`credentialType` <- `credentialType`
      self$`password` <- `password`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(credentialType = self$`credentialType`, password = self$`password`, user = self$`user`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BasicCredentialsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BasicCredentialsJson, validateParams = FALSE) {
      BasicCredentialsObject <- jsonlite::fromJSON(BasicCredentialsJson)
      self$`credentialType` <- BasicCredentialsObject$`credentialType`
      self$`password` <- BasicCredentialsObject$`password`
      self$`user` <- BasicCredentialsObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
