# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ImageAugmentationOptionsTransformation
#'
#' @description ImageAugmentationOptionsTransformation Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field affectedByTransformationProbability  character If true, whenever this transformation is included in an augmentation list, this transformation will be applied to each image with probability set by the Transformation Probability. If false, whenever this transformation is included in an augmentation list, this transformation will be applied to each image with probability described in the Platform Documentation here: https://app.datarobot.com/docs/modeling/special-workflows/visual-ai/tti-augment/ttia-lists.html#transformations
#'
#' @field enabledByDefault  character Determines if the parameter should be default selected in the UI
#'
#' @field name  character The name of the transformation
#'
#' @field params  list( \link{ImageAugmentationOptionsTransformationParam} ) The list of parameters that control the transformation
#'
#' @field translatedName  character Translated name of the transformation
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ImageAugmentationOptionsTransformation <- R6::R6Class(
  "ImageAugmentationOptionsTransformation",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`affectedByTransformationProbability` = NULL, `enabledByDefault` = NULL, `name` = NULL, `params` = NULL, `translatedName` = NULL) {
      if (!is.null(`affectedByTransformationProbability`)) {
        stopifnot(is.logical(`affectedByTransformationProbability`), length(`affectedByTransformationProbability`) == 1)
      }
      if (!is.null(`enabledByDefault`)) {
        stopifnot(is.logical(`enabledByDefault`), length(`enabledByDefault`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`params`)) {
        stopifnot(is.vector(`params`), sapply(`params`, R6::is.R6))
      }
      if (!is.null(`translatedName`)) {
        stopifnot(is.character(`translatedName`), length(`translatedName`) == 1)
      }
    }
  ),
  public = list(
    `affectedByTransformationProbability` = NULL,
    `enabledByDefault` = NULL,
    `name` = NULL,
    `params` = NULL,
    `translatedName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param affectedByTransformationProbability If true, whenever this transformation is included in an augmentation list, this transformation will be applied to each image with probability set by the Transformation Probability. If false, whenever this transformation is included in an augmentation list, this transformation will be applied to each image with probability described in the Platform Documentation here: https://app.datarobot.com/docs/modeling/special-workflows/visual-ai/tti-augment/ttia-lists.html#transformations
    #' @param enabledByDefault Determines if the parameter should be default selected in the UI
    #' @param name The name of the transformation
    #' @param params The list of parameters that control the transformation
    #' @param translatedName Translated name of the transformation
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`affectedByTransformationProbability` = NULL, `enabledByDefault` = NULL, `name` = NULL, `params` = NULL, `translatedName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`affectedByTransformationProbability`, `enabledByDefault`, `name`, `params`, `translatedName`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(affectedByTransformationProbability, enabledByDefault, name, params, translatedName)
      }
      self$`affectedByTransformationProbability` <- `affectedByTransformationProbability`
      self$`enabledByDefault` <- `enabledByDefault`
      self$`name` <- `name`
      self$`params` <- `params`
      self$`translatedName` <- `translatedName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(affectedByTransformationProbability = self$`affectedByTransformationProbability`, enabledByDefault = self$`enabledByDefault`, name = self$`name`, params = self$`params`, translatedName = self$`translatedName`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`affectedByTransformationProbability`)) {
          sprintf(
            '"affectedByTransformationProbability":
            %s
                  ',
            tolower(self$`affectedByTransformationProbability`)
          )
        },
        if (!is.null(self$`enabledByDefault`)) {
          sprintf(
            '"enabledByDefault":
            %s
                  ',
            tolower(self$`enabledByDefault`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`params`)) {
          sprintf(
            '"params":
            [%s]
      ',
            paste(sapply(self$`params`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`translatedName`)) {
          sprintf(
            '"translatedName":
            "%s"
                  ',
            self$`translatedName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ImageAugmentationOptionsTransformationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ImageAugmentationOptionsTransformationJson, validateParams = FALSE) {
      ImageAugmentationOptionsTransformationObject <- jsonlite::fromJSON(ImageAugmentationOptionsTransformationJson)
      self$`affectedByTransformationProbability` <- ImageAugmentationOptionsTransformationObject$`affectedByTransformationProbability`
      self$`enabledByDefault` <- ImageAugmentationOptionsTransformationObject$`enabledByDefault`
      self$`name` <- ImageAugmentationOptionsTransformationObject$`name`
      self$`params` <- ApiClient$new()$deserializeObj(ImageAugmentationOptionsTransformationObject$`params`, "array[ImageAugmentationOptionsTransformationParam]", loadNamespace("datarobot.apicore"))
      self$`translatedName` <- ImageAugmentationOptionsTransformationObject$`translatedName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
