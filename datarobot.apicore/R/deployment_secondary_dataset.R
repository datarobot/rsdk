# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentSecondaryDataset
#'
#' @description DeploymentSecondaryDataset Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalogId  character ID of the catalog item
#'
#' @field catalogVersionId  character ID of the catalog version
#'
#' @field identifier  character Short name of this table (used directly as part of generated feature names)
#'
#' @field snapshotPolicy  character Policy to use by a dataset while making prediction
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentSecondaryDataset <- R6::R6Class(
  "DeploymentSecondaryDataset",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(catalogId = NULL, catalogVersionId = NULL, identifier = NULL, snapshotPolicy = NULL) {
      if (!is.null(`catalogId`)) {
        stopifnot(is.character(`catalogId`), length(`catalogId`) == 1)
      }
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`identifier`)) {
        stopifnot(is.character(`identifier`), length(`identifier`) == 1)
      }
      if (!is.null(`snapshotPolicy`)) {
        stopifnot(is.character(`snapshotPolicy`), length(`snapshotPolicy`) == 1)
      }
    }
  ),
  public = list(
    `catalogId` = NULL,
    `catalogVersionId` = NULL,
    `identifier` = NULL,
    `snapshotPolicy` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalogId ID of the catalog item
    #' @param catalogVersionId ID of the catalog version
    #' @param identifier Short name of this table (used directly as part of generated feature names)
    #' @param snapshotPolicy Policy to use by a dataset while making prediction
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`catalogId` = NULL, `catalogVersionId` = NULL, `identifier` = NULL, `snapshotPolicy` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`catalogId`, `catalogVersionId`, `identifier`, `snapshotPolicy`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(catalogId, catalogVersionId, identifier, snapshotPolicy)
      }
      self$`catalogId` <- `catalogId`
      self$`catalogVersionId` <- `catalogVersionId`
      self$`identifier` <- `identifier`
      self$`snapshotPolicy` <- `snapshotPolicy`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(catalogId = self$`catalogId`, catalogVersionId = self$`catalogVersionId`, identifier = self$`identifier`, snapshotPolicy = self$`snapshotPolicy`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalogId`)) {
          sprintf(
            '"catalogId":
            "%s"
                  ',
            self$`catalogId`
          )
        },
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`identifier`)) {
          sprintf(
            '"identifier":
            "%s"
                  ',
            self$`identifier`
          )
        },
        if (!is.null(self$`snapshotPolicy`)) {
          sprintf(
            '"snapshotPolicy":
            "%s"
                  ',
            self$`snapshotPolicy`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentSecondaryDatasetJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentSecondaryDatasetJson, validateParams = FALSE) {
      DeploymentSecondaryDatasetObject <- jsonlite::fromJSON(DeploymentSecondaryDatasetJson)
      self$`catalogId` <- DeploymentSecondaryDatasetObject$`catalogId`
      self$`catalogVersionId` <- DeploymentSecondaryDatasetObject$`catalogVersionId`
      self$`identifier` <- DeploymentSecondaryDatasetObject$`identifier`
      self$`snapshotPolicy` <- DeploymentSecondaryDatasetObject$`snapshotPolicy`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
