# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ClusterInsightsPaginatedResponse
#'
#' @description ClusterInsightsPaginatedResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer [optional] Number of items returned on this page.
#'
#' @field data  list( \link{AnyOfImageFeatureGeospatialFeatureTextFeatureNumericFeatureCategoricalFeatureDocumentFeature} ) A list of features with clusters insights.
#'
#' @field isCurrentClusterInsightVersion  character If retrieved insights are current version.
#'
#' @field next_  character URL pointing to the next page (if null, there is no next page).
#'
#' @field previous  character URL pointing to the previous page (if null, there is no previous page).
#'
#' @field totalCount  integer The total number of items across all pages.
#'
#' @field version  integer Current version of the computed insight.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ClusterInsightsPaginatedResponse <- R6::R6Class(
  "ClusterInsightsPaginatedResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `data` = NULL, `isCurrentClusterInsightVersion` = NULL, `next_` = NULL, `previous` = NULL, `totalCount` = NULL, `version` = NULL) {
      if (!is.null(`data`)) {
        .setComplexProperty(typeList = list(ImageFeature, GeospatialFeature, TextFeature, NumericFeature, CategoricalFeature, DocumentFeature), propertyData = data)
      }
      if (!is.null(`isCurrentClusterInsightVersion`)) {
        stopifnot(is.logical(`isCurrentClusterInsightVersion`), length(`isCurrentClusterInsightVersion`) == 1)
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
      if (!is.null(`totalCount`)) {
        stopifnot(is.numeric(`totalCount`), length(`totalCount`) == 1)
      }
      if (!is.null(`version`)) {
        stopifnot(is.numeric(`version`), length(`version`) == 1)
      }
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `data` = NULL,
    `isCurrentClusterInsightVersion` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    `totalCount` = NULL,
    `version` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count Number of items returned on this page.
    #' @param data A list of features with clusters insights.
    #' @param isCurrentClusterInsightVersion If retrieved insights are current version.
    #' @param next_ URL pointing to the next page (if null, there is no next page).
    #' @param previous URL pointing to the previous page (if null, there is no previous page).
    #' @param totalCount The total number of items across all pages.
    #' @param version Current version of the computed insight.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`data` = NULL, `isCurrentClusterInsightVersion` = NULL, `next_` = NULL, `previous` = NULL, `totalCount` = NULL, `version` = NULL, `count` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`data`, `isCurrentClusterInsightVersion`, `next_`, `previous`, `totalCount`, `version`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, data, isCurrentClusterInsightVersion, next_, previous, totalCount, version)
      }
      self$`count` <- `count`
      self$`data` <- .setComplexProperty(typeList = list(ImageFeature, GeospatialFeature, TextFeature, NumericFeature, CategoricalFeature, DocumentFeature), propertyData = data)
      self$`isCurrentClusterInsightVersion` <- `isCurrentClusterInsightVersion`
      self$`next_` <- `next_`
      self$`previous` <- `previous`
      self$`totalCount` <- `totalCount`
      self$`version` <- `version`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, data = self$`data`, isCurrentClusterInsightVersion = self$`isCurrentClusterInsightVersion`, next_ = self$`next_`, previous = self$`previous`, totalCount = self$`totalCount`, version = self$`version`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
      ',
            paste(sapply(self$`data`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`isCurrentClusterInsightVersion`)) {
          sprintf(
            '"isCurrentClusterInsightVersion":
            %s
                  ',
            tolower(self$`isCurrentClusterInsightVersion`)
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        },
        if (!is.null(self$`totalCount`)) {
          sprintf(
            '"totalCount":
            %d
                  ',
            self$`totalCount`
          )
        },
        if (!is.null(self$`version`)) {
          sprintf(
            '"version":
            %d
                  ',
            self$`version`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ClusterInsightsPaginatedResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ClusterInsightsPaginatedResponseJson, validateParams = FALSE) {
      ClusterInsightsPaginatedResponseObject <- jsonlite::fromJSON(ClusterInsightsPaginatedResponseJson)
      self$`count` <- ClusterInsightsPaginatedResponseObject$`count`
      self$`data` <- ApiClient$new()$deserializeObj(ClusterInsightsPaginatedResponseObject$`data`, "array[AnyOfImageFeatureGeospatialFeatureTextFeatureNumericFeatureCategoricalFeatureDocumentFeature]", loadNamespace("datarobot.apicore"))
      self$`isCurrentClusterInsightVersion` <- ClusterInsightsPaginatedResponseObject$`isCurrentClusterInsightVersion`
      self$`next_` <- ClusterInsightsPaginatedResponseObject$`next`
      self$`previous` <- ClusterInsightsPaginatedResponseObject$`previous`
      self$`totalCount` <- ClusterInsightsPaginatedResponseObject$`totalCount`
      self$`version` <- ClusterInsightsPaginatedResponseObject$`version`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
