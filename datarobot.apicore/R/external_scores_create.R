# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ExternalScoresCreate
#'
#' @description ExternalScoresCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualValueColumn  character [optional] Actual value column name that contains actual values to be used for computing scores and insights for unsupervised projects only. This value can be set once for a dataset and cannot be changed.
#'
#' @field datasetId  character The dataset to compute predictions for - must have previously been uploaded.
#'
#' @field modelId  character The model to use to make predictions.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ExternalScoresCreate <- R6::R6Class(
  "ExternalScoresCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`actualValueColumn` = NULL, `datasetId` = NULL, `modelId` = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`actualValueColumn`)) {
        stopifnot(is.character(`actualValueColumn`), length(`actualValueColumn`) == 1)
      }
    }
  ),
  public = list(
    `actualValueColumn` = NULL,
    `datasetId` = NULL,
    `modelId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualValueColumn Actual value column name that contains actual values to be used for computing scores and insights for unsupervised projects only. This value can be set once for a dataset and cannot be changed.
    #' @param datasetId The dataset to compute predictions for - must have previously been uploaded.
    #' @param modelId The model to use to make predictions.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `modelId` = NULL, `actualValueColumn` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datasetId`, `modelId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(actualValueColumn, datasetId, modelId)
      }
      self$`actualValueColumn` <- `actualValueColumn`
      self$`datasetId` <- `datasetId`
      self$`modelId` <- `modelId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(actualValueColumn = self$`actualValueColumn`, datasetId = self$`datasetId`, modelId = self$`modelId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualValueColumn`)) {
          sprintf(
            '"actualValueColumn":
            "%s"
                  ',
            self$`actualValueColumn`
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ExternalScoresCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ExternalScoresCreateJson, validateParams = FALSE) {
      ExternalScoresCreateObject <- jsonlite::fromJSON(ExternalScoresCreateJson)
      self$`actualValueColumn` <- ExternalScoresCreateObject$`actualValueColumn`
      self$`datasetId` <- ExternalScoresCreateObject$`datasetId`
      self$`modelId` <- ExternalScoresCreateObject$`modelId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
