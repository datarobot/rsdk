# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataStoreTablesList
#'
#' @description DataStoreTablesList Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalog  character Catalog associated with schemas.
#'
#' @field tables  list( \link{TableDescription} ) List of tables in the data store.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataStoreTablesList <- R6::R6Class(
  "DataStoreTablesList",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`catalog` = NULL, `tables` = NULL) {
      if (!is.null(`catalog`)) {
        stopifnot(is.character(`catalog`), length(`catalog`) == 1)
      }
      if (!is.null(`tables`)) {
        stopifnot(is.vector(`tables`))
      }
    }
  ),
  public = list(
    `catalog` = NULL,
    `tables` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalog Catalog associated with schemas.
    #' @param tables List of tables in the data store.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`catalog` = NULL, `tables` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`catalog`, `tables`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(catalog, tables)
      }
      self$`catalog` <- `catalog`
      sapply(`tables`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(catalog = self$`catalog`, tables = self$`tables`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalog`)) {
          sprintf(
            '"catalog":
            "%s"
                  ',
            self$`catalog`
          )
        },
        if (!is.null(self$`tables`)) {
          sprintf(
            '"tables":
            [%s]
      ',
            paste(sapply(self$`tables`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataStoreTablesListJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataStoreTablesListJson, validateParams = FALSE) {
      DataStoreTablesListObject <- jsonlite::fromJSON(DataStoreTablesListJson)
      self$`catalog` <- DataStoreTablesListObject$`catalog`
      self$`tables` <- ApiClient$new()$deserializeObj(DataStoreTablesListObject$`tables`, "array[TableDescription]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
