# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SegmentationResultsFailedResponse
#'
#' @description SegmentationResultsFailedResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field message  character Response containing the error message from the segmentation task
#'
#' @field name  character Name of the segmentation task job
#'
#' @field parameters  object
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SegmentationResultsFailedResponse <- R6::R6Class(
  "SegmentationResultsFailedResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(message = NULL, name = NULL, parameters = NULL) {
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`parameters`)) {
      }
    }
  ),
  public = list(
    `message` = NULL,
    `name` = NULL,
    `parameters` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param message Response containing the error message from the segmentation task
    #' @param name Name of the segmentation task job
    #' @param parameters
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`message` = NULL, `name` = NULL, `parameters` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`message`, `name`, `parameters`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(message, name, parameters)
      }
      self$`message` <- `message`
      self$`name` <- `name`
      self$`parameters` <- `parameters`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(message = self$`message`, name = self$`name`, parameters = self$`parameters`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`parameters`)) {
          sprintf(
            '"parameters":
            "%s"
                  ',
            self$`parameters`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SegmentationResultsFailedResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SegmentationResultsFailedResponseJson, validateParams = FALSE) {
      SegmentationResultsFailedResponseObject <- jsonlite::fromJSON(SegmentationResultsFailedResponseJson)
      self$`message` <- SegmentationResultsFailedResponseObject$`message`
      self$`name` <- SegmentationResultsFailedResponseObject$`name`
      self$`parameters` <- SegmentationResultsFailedResponseObject$`parameters`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
