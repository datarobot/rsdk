# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title EureqaLeaderboardEntryPayload
#'
#' @description EureqaLeaderboardEntryPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field parentModelId  character [optional] The ID of the model to clone from. If omitted, will automatically search for and find the first leaderboard model created by the blueprint run that also created the solution associated with &#x60;solutionId&#x60;.
#'
#' @field solutionId  character the ID of the solution to be cloned.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
EureqaLeaderboardEntryPayload <- R6::R6Class(
  "EureqaLeaderboardEntryPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(parentModelId = NULL, solutionId = NULL) {
      if (!is.null(`solutionId`)) {
        stopifnot(is.character(`solutionId`), length(`solutionId`) == 1)
      }
      if (!is.null(`parentModelId`)) {
        stopifnot(is.character(`parentModelId`), length(`parentModelId`) == 1)
      }
    }
  ),
  public = list(
    `parentModelId` = NULL,
    `solutionId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param parentModelId The ID of the model to clone from. If omitted, will automatically search for and find the first leaderboard model created by the blueprint run that also created the solution associated with &#x60;solutionId&#x60;.
    #' @param solutionId the ID of the solution to be cloned.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`solutionId` = NULL, `parentModelId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`solutionId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(parentModelId, solutionId)
      }
      self$`parentModelId` <- `parentModelId`
      self$`solutionId` <- `solutionId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(parentModelId = self$`parentModelId`, solutionId = self$`solutionId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`parentModelId`)) {
          sprintf(
            '"parentModelId":
            "%s"
                  ',
            self$`parentModelId`
          )
        },
        if (!is.null(self$`solutionId`)) {
          sprintf(
            '"solutionId":
            "%s"
                  ',
            self$`solutionId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param EureqaLeaderboardEntryPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(EureqaLeaderboardEntryPayloadJson, validateParams = FALSE) {
      EureqaLeaderboardEntryPayloadObject <- jsonlite::fromJSON(EureqaLeaderboardEntryPayloadJson)
      self$`parentModelId` <- EureqaLeaderboardEntryPayloadObject$`parentModelId`
      self$`solutionId` <- EureqaLeaderboardEntryPayloadObject$`solutionId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
