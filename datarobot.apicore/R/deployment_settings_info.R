# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentSettingsInfo
#'
#' @description DeploymentSettingsInfo Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field humbleAiEnabled  character [optional] if humble ai is enabled.
#'
#' @field predictionIntervalsEnabled  character [optional] If  prediction intervals are enabled.
#'
#' @field predictionWarningEnabled  character [optional] If prediction warning is enabled.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentSettingsInfo <- R6::R6Class(
  "DeploymentSettingsInfo",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(humbleAiEnabled = NULL, predictionIntervalsEnabled = NULL, predictionWarningEnabled = NULL) {
      if (!is.null(`humbleAiEnabled`)) {
        stopifnot(is.logical(`humbleAiEnabled`), length(`humbleAiEnabled`) == 1)
      }
      if (!is.null(`predictionIntervalsEnabled`)) {
        stopifnot(is.logical(`predictionIntervalsEnabled`), length(`predictionIntervalsEnabled`) == 1)
      }
      if (!is.null(`predictionWarningEnabled`)) {
        stopifnot(is.logical(`predictionWarningEnabled`), length(`predictionWarningEnabled`) == 1)
      }
    }
  ),
  public = list(
    `humbleAiEnabled` = NULL,
    `predictionIntervalsEnabled` = NULL,
    `predictionWarningEnabled` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param humbleAiEnabled if humble ai is enabled.
    #' @param predictionIntervalsEnabled If  prediction intervals are enabled.
    #' @param predictionWarningEnabled If prediction warning is enabled.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`humbleAiEnabled` = NULL, `predictionIntervalsEnabled` = NULL, `predictionWarningEnabled` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(humbleAiEnabled, predictionIntervalsEnabled, predictionWarningEnabled)
      }
      self$`humbleAiEnabled` <- `humbleAiEnabled`
      self$`predictionIntervalsEnabled` <- `predictionIntervalsEnabled`
      self$`predictionWarningEnabled` <- `predictionWarningEnabled`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(humbleAiEnabled = self$`humbleAiEnabled`, predictionIntervalsEnabled = self$`predictionIntervalsEnabled`, predictionWarningEnabled = self$`predictionWarningEnabled`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`humbleAiEnabled`)) {
          sprintf(
            '"humbleAiEnabled":
            %s
                  ',
            tolower(self$`humbleAiEnabled`)
          )
        },
        if (!is.null(self$`predictionIntervalsEnabled`)) {
          sprintf(
            '"predictionIntervalsEnabled":
            %s
                  ',
            tolower(self$`predictionIntervalsEnabled`)
          )
        },
        if (!is.null(self$`predictionWarningEnabled`)) {
          sprintf(
            '"predictionWarningEnabled":
            %s
                  ',
            tolower(self$`predictionWarningEnabled`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentSettingsInfoJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentSettingsInfoJson, validateParams = FALSE) {
      DeploymentSettingsInfoObject <- jsonlite::fromJSON(DeploymentSettingsInfoJson)
      self$`humbleAiEnabled` <- DeploymentSettingsInfoObject$`humbleAiEnabled`
      self$`predictionIntervalsEnabled` <- DeploymentSettingsInfoObject$`predictionIntervalsEnabled`
      self$`predictionWarningEnabled` <- DeploymentSettingsInfoObject$`predictionWarningEnabled`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
