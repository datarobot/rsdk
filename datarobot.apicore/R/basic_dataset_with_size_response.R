# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BasicDatasetWithSizeResponse
#'
#' @description BasicDatasetWithSizeResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field categories  list( character ) An array of strings describing the intended use of the dataset.
#'
#' @field createdBy  character Username of the user who created the dataset.
#'
#' @field creationDate  character The date when the dataset was created.
#'
#' @field dataPersisted  character If true, user is allowed to view extended data profile (which includes data statistics like min/max/median/mean, histogram, etc.) and download data. If false, download is not allowed and only the data schema (feature names and types) will be available.
#'
#' @field datasetId  character The ID of this dataset.
#'
#' @field datasetSize  integer The size of the dataset as a CSV in bytes.
#'
#' @field isDataEngineEligible  character Whether this dataset can be a data source of a data engine query.
#'
#' @field isLatestVersion  character Whether this dataset version is the latest version of this dataset.
#'
#' @field isSnapshot  character Whether the dataset is an immutable snapshot of data which has previously been retrieved and saved to DataRobot.
#'
#' @field name  character The name of this dataset in the catalog.
#'
#' @field processingState  character Current ingestion process state of dataset.
#'
#' @field rowCount  integer The number of rows in the dataset.
#'
#' @field sampleSize  \link{SampleSize} [optional]
#'
#' @field timeSeriesProperties  \link{TimeSeriesProperties}
#'
#' @field versionId  character The object ID of the catalog_version the dataset belongs to.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BasicDatasetWithSizeResponse <- R6::R6Class(
  "BasicDatasetWithSizeResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`categories` = NULL, `createdBy` = NULL, `creationDate` = NULL, `dataPersisted` = NULL, `datasetId` = NULL, `datasetSize` = NULL, `isDataEngineEligible` = NULL, `isLatestVersion` = NULL, `isSnapshot` = NULL, `name` = NULL, `processingState` = NULL, `rowCount` = NULL, `sampleSize` = NULL, `timeSeriesProperties` = NULL, `versionId` = NULL) {
      if (!is.null(`categories`)) {
        stopifnot(is.vector(`categories`), sapply(`categories`, is.character))
      }
      if (!is.null(`createdBy`)) {
        stopifnot(is.character(`createdBy`), length(`createdBy`) == 1)
      }
      if (!is.null(`creationDate`)) {
        stopifnot(inherits(`creationDate`, "POSIXt"))
      }
      if (!is.null(`dataPersisted`)) {
        stopifnot(is.logical(`dataPersisted`), length(`dataPersisted`) == 1)
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`datasetSize`)) {
        stopifnot(is.numeric(`datasetSize`), length(`datasetSize`) == 1)
      }
      if (!is.null(`isDataEngineEligible`)) {
        stopifnot(is.logical(`isDataEngineEligible`), length(`isDataEngineEligible`) == 1)
      }
      if (!is.null(`isLatestVersion`)) {
        stopifnot(is.logical(`isLatestVersion`), length(`isLatestVersion`) == 1)
      }
      if (!is.null(`isSnapshot`)) {
        stopifnot(is.logical(`isSnapshot`), length(`isSnapshot`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`processingState`)) {
        stopifnot(is.character(`processingState`), length(`processingState`) == 1)
      }
      if (!is.null(`rowCount`)) {
        stopifnot(is.numeric(`rowCount`), length(`rowCount`) == 1)
      }
      if (!is.null(`timeSeriesProperties`)) {
        stopifnot(R6::is.R6(`timeSeriesProperties`))
      }
      if (!is.null(`versionId`)) {
        stopifnot(is.character(`versionId`), length(`versionId`) == 1)
      }
      if (!is.null(`sampleSize`)) {
        stopifnot(R6::is.R6(`sampleSize`))
      }
    }
  ),
  public = list(
    `categories` = NULL,
    `createdBy` = NULL,
    `creationDate` = NULL,
    `dataPersisted` = NULL,
    `datasetId` = NULL,
    `datasetSize` = NULL,
    `isDataEngineEligible` = NULL,
    `isLatestVersion` = NULL,
    `isSnapshot` = NULL,
    `name` = NULL,
    `processingState` = NULL,
    `rowCount` = NULL,
    `sampleSize` = NULL,
    `timeSeriesProperties` = NULL,
    `versionId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param categories An array of strings describing the intended use of the dataset.
    #' @param createdBy Username of the user who created the dataset.
    #' @param creationDate The date when the dataset was created.
    #' @param dataPersisted If true, user is allowed to view extended data profile (which includes data statistics like min/max/median/mean, histogram, etc.) and download data. If false, download is not allowed and only the data schema (feature names and types) will be available.
    #' @param datasetId The ID of this dataset.
    #' @param datasetSize The size of the dataset as a CSV in bytes.
    #' @param isDataEngineEligible Whether this dataset can be a data source of a data engine query.
    #' @param isLatestVersion Whether this dataset version is the latest version of this dataset.
    #' @param isSnapshot Whether the dataset is an immutable snapshot of data which has previously been retrieved and saved to DataRobot.
    #' @param name The name of this dataset in the catalog.
    #' @param processingState Current ingestion process state of dataset.
    #' @param rowCount The number of rows in the dataset.
    #' @param sampleSize
    #' @param timeSeriesProperties
    #' @param versionId The object ID of the catalog_version the dataset belongs to.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`categories` = NULL, `createdBy` = NULL, `creationDate` = NULL, `dataPersisted` = NULL, `datasetId` = NULL, `datasetSize` = NULL, `isDataEngineEligible` = NULL, `isLatestVersion` = NULL, `isSnapshot` = NULL, `name` = NULL, `processingState` = NULL, `rowCount` = NULL, `timeSeriesProperties` = NULL, `versionId` = NULL, `sampleSize` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`categories`, `createdBy`, `creationDate`, `dataPersisted`, `datasetId`, `datasetSize`, `isDataEngineEligible`, `isLatestVersion`, `isSnapshot`, `name`, `processingState`, `rowCount`, `timeSeriesProperties`, `versionId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(categories, createdBy, creationDate, dataPersisted, datasetId, datasetSize, isDataEngineEligible, isLatestVersion, isSnapshot, name, processingState, rowCount, sampleSize, timeSeriesProperties, versionId)
      }
      self$`categories` <- `categories`
      self$`createdBy` <- `createdBy`
      self$`creationDate` <- `creationDate`
      self$`dataPersisted` <- `dataPersisted`
      self$`datasetId` <- `datasetId`
      self$`datasetSize` <- `datasetSize`
      self$`isDataEngineEligible` <- `isDataEngineEligible`
      self$`isLatestVersion` <- `isLatestVersion`
      self$`isSnapshot` <- `isSnapshot`
      self$`name` <- `name`
      self$`processingState` <- `processingState`
      self$`rowCount` <- `rowCount`
      self$`sampleSize` <- `sampleSize`
      self$`timeSeriesProperties` <- `timeSeriesProperties`
      self$`versionId` <- `versionId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(categories = self$`categories`, createdBy = self$`createdBy`, creationDate = self$`creationDate`, dataPersisted = self$`dataPersisted`, datasetId = self$`datasetId`, datasetSize = self$`datasetSize`, isDataEngineEligible = self$`isDataEngineEligible`, isLatestVersion = self$`isLatestVersion`, isSnapshot = self$`isSnapshot`, name = self$`name`, processingState = self$`processingState`, rowCount = self$`rowCount`, sampleSize = self$`sampleSize`, timeSeriesProperties = self$`timeSeriesProperties`, versionId = self$`versionId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`categories`)) {
          sprintf(
            '"categories":
            [%s]
                  ',
            paste(unlist(lapply(self$`categories`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`createdBy`)) {
          sprintf(
            '"createdBy":
            "%s"
                  ',
            self$`createdBy`
          )
        },
        if (!is.null(self$`creationDate`)) {
          sprintf(
            '"creationDate":
            "%s"
                  ',
            format(self$`creationDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`dataPersisted`)) {
          sprintf(
            '"dataPersisted":
            %s
                  ',
            tolower(self$`dataPersisted`)
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetSize`)) {
          sprintf(
            '"datasetSize":
            %d
                  ',
            self$`datasetSize`
          )
        },
        if (!is.null(self$`isDataEngineEligible`)) {
          sprintf(
            '"isDataEngineEligible":
            %s
                  ',
            tolower(self$`isDataEngineEligible`)
          )
        },
        if (!is.null(self$`isLatestVersion`)) {
          sprintf(
            '"isLatestVersion":
            %s
                  ',
            tolower(self$`isLatestVersion`)
          )
        },
        if (!is.null(self$`isSnapshot`)) {
          sprintf(
            '"isSnapshot":
            %s
                  ',
            tolower(self$`isSnapshot`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`processingState`)) {
          sprintf(
            '"processingState":
            "%s"
                  ',
            self$`processingState`
          )
        },
        if (!is.null(self$`rowCount`)) {
          sprintf(
            '"rowCount":
            %d
                  ',
            self$`rowCount`
          )
        },
        if (!is.null(self$`sampleSize`)) {
          sprintf(
            '"sampleSize":
            %s
      ',
            jsonlite::toJSON(self$`sampleSize`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`timeSeriesProperties`)) {
          sprintf(
            '"timeSeriesProperties":
            %s
      ',
            jsonlite::toJSON(self$`timeSeriesProperties`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`versionId`)) {
          sprintf(
            '"versionId":
            "%s"
                  ',
            self$`versionId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BasicDatasetWithSizeResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BasicDatasetWithSizeResponseJson, validateParams = FALSE) {
      BasicDatasetWithSizeResponseObject <- jsonlite::fromJSON(BasicDatasetWithSizeResponseJson)
      self$`categories` <- ApiClient$new()$deserializeObj(BasicDatasetWithSizeResponseObject$`categories`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`createdBy` <- BasicDatasetWithSizeResponseObject$`createdBy`
      self$`creationDate` <- ParseRFC3339Timestamp(BasicDatasetWithSizeResponseObject$`creationDate`)
      self$`dataPersisted` <- BasicDatasetWithSizeResponseObject$`dataPersisted`
      self$`datasetId` <- BasicDatasetWithSizeResponseObject$`datasetId`
      self$`datasetSize` <- BasicDatasetWithSizeResponseObject$`datasetSize`
      self$`isDataEngineEligible` <- BasicDatasetWithSizeResponseObject$`isDataEngineEligible`
      self$`isLatestVersion` <- BasicDatasetWithSizeResponseObject$`isLatestVersion`
      self$`isSnapshot` <- BasicDatasetWithSizeResponseObject$`isSnapshot`
      self$`name` <- BasicDatasetWithSizeResponseObject$`name`
      self$`processingState` <- BasicDatasetWithSizeResponseObject$`processingState`
      self$`rowCount` <- BasicDatasetWithSizeResponseObject$`rowCount`
      self$`sampleSize` <- SampleSize$new()$fromJSON(jsonlite::toJSON(BasicDatasetWithSizeResponseObject$sampleSize, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`timeSeriesProperties` <- TimeSeriesProperties$new()$fromJSON(jsonlite::toJSON(BasicDatasetWithSizeResponseObject$timeSeriesProperties, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`versionId` <- BasicDatasetWithSizeResponseObject$`versionId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
