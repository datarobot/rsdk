# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotificationLogListDataResponse
#'
#' @description NotificationLogListDataResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field channelId  character The id of the channel that was used to send the notification
#'
#' @field emailSubject  character The email subject
#'
#' @field id  character The id of the notification log
#'
#' @field parentNotificationId  character The ID of the parent notification
#'
#' @field policyId  character the id of the policy that was used to send the notification
#'
#' @field request  \link{RequestNotification}
#'
#' @field response  \link{ResponseNotification}
#'
#' @field retryCount  integer The count of the retries while sending the notification
#'
#' @field status  character The status of the notification
#'
#' @field timestamp  character The date and time when the notification was sent
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotificationLogListDataResponse <- R6::R6Class(
  "NotificationLogListDataResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`channelId` = NULL, `emailSubject` = NULL, `id` = NULL, `parentNotificationId` = NULL, `policyId` = NULL, `request` = NULL, `response` = NULL, `retryCount` = NULL, `status` = NULL, `timestamp` = NULL) {
      if (!is.null(`channelId`)) {
        stopifnot(is.character(`channelId`), length(`channelId`) == 1)
      }
      if (!is.null(`emailSubject`)) {
        stopifnot(is.character(`emailSubject`), length(`emailSubject`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`parentNotificationId`)) {
        stopifnot(is.character(`parentNotificationId`), length(`parentNotificationId`) == 1)
      }
      if (!is.null(`policyId`)) {
        stopifnot(is.character(`policyId`), length(`policyId`) == 1)
      }
      if (!is.null(`request`)) {
        stopifnot(R6::is.R6(`request`))
      }
      if (!is.null(`response`)) {
        stopifnot(R6::is.R6(`response`))
      }
      if (!is.null(`retryCount`)) {
        stopifnot(is.numeric(`retryCount`), length(`retryCount`) == 1)
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
      if (!is.null(`timestamp`)) {
        stopifnot(inherits(`timestamp`, "POSIXt"))
      }
    }
  ),
  public = list(
    `channelId` = NULL,
    `emailSubject` = NULL,
    `id` = NULL,
    `parentNotificationId` = NULL,
    `policyId` = NULL,
    `request` = NULL,
    `response` = NULL,
    `retryCount` = NULL,
    `status` = NULL,
    `timestamp` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param channelId The id of the channel that was used to send the notification
    #' @param emailSubject The email subject
    #' @param id The id of the notification log
    #' @param parentNotificationId The ID of the parent notification
    #' @param policyId the id of the policy that was used to send the notification
    #' @param request
    #' @param response
    #' @param retryCount The count of the retries while sending the notification
    #' @param status The status of the notification
    #' @param timestamp The date and time when the notification was sent
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`channelId` = NULL, `emailSubject` = NULL, `id` = NULL, `parentNotificationId` = NULL, `policyId` = NULL, `request` = NULL, `response` = NULL, `retryCount` = NULL, `status` = NULL, `timestamp` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`channelId`, `emailSubject`, `id`, `parentNotificationId`, `policyId`, `request`, `response`, `retryCount`, `status`, `timestamp`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(channelId, emailSubject, id, parentNotificationId, policyId, request, response, retryCount, status, timestamp)
      }
      self$`channelId` <- `channelId`
      self$`emailSubject` <- `emailSubject`
      self$`id` <- `id`
      self$`parentNotificationId` <- `parentNotificationId`
      self$`policyId` <- `policyId`
      self$`request` <- `request`
      self$`response` <- `response`
      self$`retryCount` <- `retryCount`
      self$`status` <- `status`
      self$`timestamp` <- `timestamp`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(channelId = self$`channelId`, emailSubject = self$`emailSubject`, id = self$`id`, parentNotificationId = self$`parentNotificationId`, policyId = self$`policyId`, request = self$`request`, response = self$`response`, retryCount = self$`retryCount`, status = self$`status`, timestamp = self$`timestamp`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`channelId`)) {
          sprintf(
            '"channelId":
            "%s"
                  ',
            self$`channelId`
          )
        },
        if (!is.null(self$`emailSubject`)) {
          sprintf(
            '"emailSubject":
            "%s"
                  ',
            self$`emailSubject`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`parentNotificationId`)) {
          sprintf(
            '"parentNotificationId":
            "%s"
                  ',
            self$`parentNotificationId`
          )
        },
        if (!is.null(self$`policyId`)) {
          sprintf(
            '"policyId":
            "%s"
                  ',
            self$`policyId`
          )
        },
        if (!is.null(self$`request`)) {
          sprintf(
            '"request":
            %s
      ',
            jsonlite::toJSON(self$`request`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`response`)) {
          sprintf(
            '"response":
            %s
      ',
            jsonlite::toJSON(self$`response`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`retryCount`)) {
          sprintf(
            '"retryCount":
            %d
                  ',
            self$`retryCount`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        },
        if (!is.null(self$`timestamp`)) {
          sprintf(
            '"timestamp":
            "%s"
                  ',
            format(self$`timestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotificationLogListDataResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotificationLogListDataResponseJson, validateParams = FALSE) {
      NotificationLogListDataResponseObject <- jsonlite::fromJSON(NotificationLogListDataResponseJson)
      self$`channelId` <- NotificationLogListDataResponseObject$`channelId`
      self$`emailSubject` <- NotificationLogListDataResponseObject$`emailSubject`
      self$`id` <- NotificationLogListDataResponseObject$`id`
      self$`parentNotificationId` <- NotificationLogListDataResponseObject$`parentNotificationId`
      self$`policyId` <- NotificationLogListDataResponseObject$`policyId`
      self$`request` <- RequestNotification$new()$fromJSON(jsonlite::toJSON(NotificationLogListDataResponseObject$request, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`response` <- ResponseNotification$new()$fromJSON(jsonlite::toJSON(NotificationLogListDataResponseObject$response, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`retryCount` <- NotificationLogListDataResponseObject$`retryCount`
      self$`status` <- NotificationLogListDataResponseObject$`status`
      self$`timestamp` <- ParseRFC3339Timestamp(NotificationLogListDataResponseObject$`timestamp`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
