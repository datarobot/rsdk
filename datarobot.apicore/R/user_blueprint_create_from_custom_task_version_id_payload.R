# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintCreateFromCustomTaskVersionIdPayload
#'
#' @description UserBlueprintCreateFromCustomTaskVersionIdPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field customTaskVersionId  character The ID of a custom task version.
#'
#' @field decompressedBlueprint  character Whether to retrieve the blueprint in the decompressed format.
#'
#' @field description  character [optional] The description for the user blueprint that will be created from this CustomTaskVersion.
#'
#' @field saveToCatalog  character Whether to save the blueprint to the catalog.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintCreateFromCustomTaskVersionIdPayload <- R6::R6Class(
  "UserBlueprintCreateFromCustomTaskVersionIdPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(customTaskVersionId = NULL, decompressedBlueprint = NULL, description = NULL, saveToCatalog = NULL) {
      if (!is.null(`customTaskVersionId`)) {
        stopifnot(is.character(`customTaskVersionId`), length(`customTaskVersionId`) == 1)
      }
      if (!is.null(`decompressedBlueprint`)) {
        stopifnot(is.logical(`decompressedBlueprint`), length(`decompressedBlueprint`) == 1)
      }
      if (!is.null(`saveToCatalog`)) {
        stopifnot(is.logical(`saveToCatalog`), length(`saveToCatalog`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
    }
  ),
  public = list(
    `customTaskVersionId` = NULL,
    `decompressedBlueprint` = NULL,
    `description` = NULL,
    `saveToCatalog` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param customTaskVersionId The ID of a custom task version.
    #' @param decompressedBlueprint Whether to retrieve the blueprint in the decompressed format.
    #' @param description The description for the user blueprint that will be created from this CustomTaskVersion.
    #' @param saveToCatalog Whether to save the blueprint to the catalog.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`customTaskVersionId` = NULL, `decompressedBlueprint` = NULL, `saveToCatalog` = NULL, `description` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`customTaskVersionId`, `decompressedBlueprint`, `saveToCatalog`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(customTaskVersionId, decompressedBlueprint, description, saveToCatalog)
      }
      self$`customTaskVersionId` <- `customTaskVersionId`
      self$`decompressedBlueprint` <- `decompressedBlueprint`
      self$`description` <- `description`
      self$`saveToCatalog` <- `saveToCatalog`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(customTaskVersionId = self$`customTaskVersionId`, decompressedBlueprint = self$`decompressedBlueprint`, description = self$`description`, saveToCatalog = self$`saveToCatalog`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`customTaskVersionId`)) {
          sprintf(
            '"customTaskVersionId":
            "%s"
                  ',
            self$`customTaskVersionId`
          )
        },
        if (!is.null(self$`decompressedBlueprint`)) {
          sprintf(
            '"decompressedBlueprint":
            %s
                  ',
            tolower(self$`decompressedBlueprint`)
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`saveToCatalog`)) {
          sprintf(
            '"saveToCatalog":
            %s
                  ',
            tolower(self$`saveToCatalog`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintCreateFromCustomTaskVersionIdPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintCreateFromCustomTaskVersionIdPayloadJson, validateParams = FALSE) {
      UserBlueprintCreateFromCustomTaskVersionIdPayloadObject <- jsonlite::fromJSON(UserBlueprintCreateFromCustomTaskVersionIdPayloadJson)
      self$`customTaskVersionId` <- UserBlueprintCreateFromCustomTaskVersionIdPayloadObject$`customTaskVersionId`
      self$`decompressedBlueprint` <- UserBlueprintCreateFromCustomTaskVersionIdPayloadObject$`decompressedBlueprint`
      self$`description` <- UserBlueprintCreateFromCustomTaskVersionIdPayloadObject$`description`
      self$`saveToCatalog` <- UserBlueprintCreateFromCustomTaskVersionIdPayloadObject$`saveToCatalog`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
