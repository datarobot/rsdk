# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title S3Credentials
#'
#' @description S3Credentials Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field awsAccessKeyId  character [optional] The S3 AWS access key ID.
#'
#' @field awsSecretAccessKey  character [optional] The S3 AWS secret access key.
#'
#' @field awsSessionToken  character [optional] The S3 AWS session token.
#'
#' @field credentialType  character The type of these credentials, &#39;s3&#39; here.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
S3Credentials <- R6::R6Class(
  "S3Credentials",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(awsAccessKeyId = NULL, awsSecretAccessKey = NULL, awsSessionToken = NULL, credentialType = NULL) {
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`awsAccessKeyId`)) {
        stopifnot(is.character(`awsAccessKeyId`), length(`awsAccessKeyId`) == 1)
      }
      if (!is.null(`awsSecretAccessKey`)) {
        stopifnot(is.character(`awsSecretAccessKey`), length(`awsSecretAccessKey`) == 1)
      }
      if (!is.null(`awsSessionToken`)) {
        stopifnot(is.character(`awsSessionToken`), length(`awsSessionToken`) == 1)
      }
    }
  ),
  public = list(
    `awsAccessKeyId` = NULL,
    `awsSecretAccessKey` = NULL,
    `awsSessionToken` = NULL,
    `credentialType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param awsAccessKeyId The S3 AWS access key ID.
    #' @param awsSecretAccessKey The S3 AWS secret access key.
    #' @param awsSessionToken The S3 AWS session token.
    #' @param credentialType The type of these credentials, &#39;s3&#39; here.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialType` = NULL, `awsAccessKeyId` = "null", `awsSecretAccessKey` = "null", `awsSessionToken` = "null", validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`credentialType`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(awsAccessKeyId, awsSecretAccessKey, awsSessionToken, credentialType)
      }
      self$`awsAccessKeyId` <- `awsAccessKeyId`
      self$`awsSecretAccessKey` <- `awsSecretAccessKey`
      self$`awsSessionToken` <- `awsSessionToken`
      self$`credentialType` <- `credentialType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(awsAccessKeyId = self$`awsAccessKeyId`, awsSecretAccessKey = self$`awsSecretAccessKey`, awsSessionToken = self$`awsSessionToken`, credentialType = self$`credentialType`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`awsAccessKeyId`)) {
          sprintf(
            '"awsAccessKeyId":
            "%s"
                  ',
            self$`awsAccessKeyId`
          )
        },
        if (!is.null(self$`awsSecretAccessKey`)) {
          sprintf(
            '"awsSecretAccessKey":
            "%s"
                  ',
            self$`awsSecretAccessKey`
          )
        },
        if (!is.null(self$`awsSessionToken`)) {
          sprintf(
            '"awsSessionToken":
            "%s"
                  ',
            self$`awsSessionToken`
          )
        },
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param S3CredentialsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(S3CredentialsJson, validateParams = FALSE) {
      S3CredentialsObject <- jsonlite::fromJSON(S3CredentialsJson)
      self$`awsAccessKeyId` <- S3CredentialsObject$`awsAccessKeyId`
      self$`awsSecretAccessKey` <- S3CredentialsObject$`awsSecretAccessKey`
      self$`awsSessionToken` <- S3CredentialsObject$`awsSessionToken`
      self$`credentialType` <- S3CredentialsObject$`credentialType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
