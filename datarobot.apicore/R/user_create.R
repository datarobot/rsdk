# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserCreate
#'
#' @description UserCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field country  character [optional] Country where the user lives.
#'
#' @field defaultCatalogDatasetSampleSize  \link{AdminSampleSize} [optional]
#'
#' @field expirationDate  character [optional] Datetime, RFC3339, at which the user should expire.
#'
#' @field firstName  character First name of the user being created.
#'
#' @field language  character [optional] Sets the language in the app for the user, and the language of the invite email, if the user is being invited (i.e. the password is omitted). Value must be a valid ISO-639-1 language code. Available options are: &#x60;en&#x60;, &#x60;ja&#x60;, &#x60;fr&#x60;, &#x60;ko&#x60;. System&#39;s default language will be used if omitted.
#'
#' @field lastName  character [optional] Last name of the user being created.
#'
#' @field maxGpuWorkers  integer [optional] Amount of user&#39;s available GPU workers.
#'
#' @field maxIdleWorkers  integer [optional] Amount of org workers that the user can utilize when idle.
#'
#' @field maxUploadSize  integer [optional] The upper limit for the allowed upload size for this user.
#'
#' @field maxUploadSizeCatalog  integer [optional] The upper limit for the allowed upload size in the AI catalog for this user.
#'
#' @field maxWorkers  integer [optional] Amount of user&#39;s available workers.
#'
#' @field organizationId  character [optional] ID of the organization to add the user to.
#'
#' @field password  character [optional] Creates user with this password, if blank sends username invite.
#'
#' @field requireClickthroughAgreement  character [optional] Boolean to require the user to agree to a clickthrough.
#'
#' @field userType  character External name for UserType such as AcademicUser, BasicUser, ProUser.
#'
#' @field username  character Email address of new user.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserCreate <- R6::R6Class(
  "UserCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(country = NULL, defaultCatalogDatasetSampleSize = NULL, expirationDate = NULL, firstName = NULL, language = NULL, lastName = NULL, maxGpuWorkers = NULL, maxIdleWorkers = NULL, maxUploadSize = NULL, maxUploadSizeCatalog = NULL, maxWorkers = NULL, organizationId = NULL, password = NULL, requireClickthroughAgreement = NULL, userType = NULL, username = NULL) {
      if (!is.null(`firstName`)) {
        stopifnot(is.character(`firstName`), length(`firstName`) == 1)
      }
      if (!is.null(`userType`)) {
        stopifnot(is.character(`userType`), length(`userType`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
      if (!is.null(`country`)) {
        stopifnot(is.character(`country`), length(`country`) == 1)
      }
      if (!is.null(`defaultCatalogDatasetSampleSize`)) {
        stopifnot(R6::is.R6(`defaultCatalogDatasetSampleSize`))
      }
      if (!is.null(`expirationDate`)) {
        stopifnot(inherits(`expirationDate`, "POSIXt"))
      }
      if (!is.null(`language`)) {
        stopifnot(is.character(`language`), length(`language`) == 1)
      }
      if (!is.null(`lastName`)) {
        stopifnot(is.character(`lastName`), length(`lastName`) == 1)
      }
      if (!is.null(`maxGpuWorkers`)) {
        stopifnot(is.numeric(`maxGpuWorkers`), length(`maxGpuWorkers`) == 1)
      }
      if (!is.null(`maxIdleWorkers`)) {
        stopifnot(is.numeric(`maxIdleWorkers`), length(`maxIdleWorkers`) == 1)
      }
      if (!is.null(`maxUploadSize`)) {
        stopifnot(is.numeric(`maxUploadSize`), length(`maxUploadSize`) == 1)
      }
      if (!is.null(`maxUploadSizeCatalog`)) {
        stopifnot(is.numeric(`maxUploadSizeCatalog`), length(`maxUploadSizeCatalog`) == 1)
      }
      if (!is.null(`maxWorkers`)) {
        stopifnot(is.numeric(`maxWorkers`), length(`maxWorkers`) == 1)
      }
      if (!is.null(`organizationId`)) {
        stopifnot(is.character(`organizationId`), length(`organizationId`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`requireClickthroughAgreement`)) {
        stopifnot(is.logical(`requireClickthroughAgreement`), length(`requireClickthroughAgreement`) == 1)
      }
    }
  ),
  public = list(
    `country` = NULL,
    `defaultCatalogDatasetSampleSize` = NULL,
    `expirationDate` = NULL,
    `firstName` = NULL,
    `language` = NULL,
    `lastName` = NULL,
    `maxGpuWorkers` = NULL,
    `maxIdleWorkers` = NULL,
    `maxUploadSize` = NULL,
    `maxUploadSizeCatalog` = NULL,
    `maxWorkers` = NULL,
    `organizationId` = NULL,
    `password` = NULL,
    `requireClickthroughAgreement` = NULL,
    `userType` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param country Country where the user lives.
    #' @param defaultCatalogDatasetSampleSize
    #' @param expirationDate Datetime, RFC3339, at which the user should expire.
    #' @param firstName First name of the user being created.
    #' @param language Sets the language in the app for the user, and the language of the invite email, if the user is being invited (i.e. the password is omitted). Value must be a valid ISO-639-1 language code. Available options are: &#x60;en&#x60;, &#x60;ja&#x60;, &#x60;fr&#x60;, &#x60;ko&#x60;. System&#39;s default language will be used if omitted.
    #' @param lastName Last name of the user being created.
    #' @param maxGpuWorkers Amount of user&#39;s available GPU workers.
    #' @param maxIdleWorkers Amount of org workers that the user can utilize when idle.
    #' @param maxUploadSize The upper limit for the allowed upload size for this user.
    #' @param maxUploadSizeCatalog The upper limit for the allowed upload size in the AI catalog for this user.
    #' @param maxWorkers Amount of user&#39;s available workers.
    #' @param organizationId ID of the organization to add the user to.
    #' @param password Creates user with this password, if blank sends username invite.
    #' @param requireClickthroughAgreement Boolean to require the user to agree to a clickthrough.
    #' @param userType External name for UserType such as AcademicUser, BasicUser, ProUser.
    #' @param username Email address of new user.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`firstName` = NULL, `userType` = NULL, `username` = NULL, `country` = NULL, `defaultCatalogDatasetSampleSize` = NULL, `expirationDate` = NULL, `language` = NULL, `lastName` = NULL, `maxGpuWorkers` = NULL, `maxIdleWorkers` = NULL, `maxUploadSize` = NULL, `maxUploadSizeCatalog` = NULL, `maxWorkers` = NULL, `organizationId` = NULL, `password` = NULL, `requireClickthroughAgreement` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`firstName`, `userType`, `username`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(country, defaultCatalogDatasetSampleSize, expirationDate, firstName, language, lastName, maxGpuWorkers, maxIdleWorkers, maxUploadSize, maxUploadSizeCatalog, maxWorkers, organizationId, password, requireClickthroughAgreement, userType, username)
      }
      self$`country` <- `country`
      self$`defaultCatalogDatasetSampleSize` <- `defaultCatalogDatasetSampleSize`
      self$`expirationDate` <- `expirationDate`
      self$`firstName` <- `firstName`
      self$`language` <- `language`
      self$`lastName` <- `lastName`
      self$`maxGpuWorkers` <- `maxGpuWorkers`
      self$`maxIdleWorkers` <- `maxIdleWorkers`
      self$`maxUploadSize` <- `maxUploadSize`
      self$`maxUploadSizeCatalog` <- `maxUploadSizeCatalog`
      self$`maxWorkers` <- `maxWorkers`
      self$`organizationId` <- `organizationId`
      self$`password` <- `password`
      self$`requireClickthroughAgreement` <- `requireClickthroughAgreement`
      self$`userType` <- `userType`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(country = self$`country`, defaultCatalogDatasetSampleSize = self$`defaultCatalogDatasetSampleSize`, expirationDate = self$`expirationDate`, firstName = self$`firstName`, language = self$`language`, lastName = self$`lastName`, maxGpuWorkers = self$`maxGpuWorkers`, maxIdleWorkers = self$`maxIdleWorkers`, maxUploadSize = self$`maxUploadSize`, maxUploadSizeCatalog = self$`maxUploadSizeCatalog`, maxWorkers = self$`maxWorkers`, organizationId = self$`organizationId`, password = self$`password`, requireClickthroughAgreement = self$`requireClickthroughAgreement`, userType = self$`userType`, username = self$`username`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`country`)) {
          sprintf(
            '"country":
            "%s"
                  ',
            self$`country`
          )
        },
        if (!is.null(self$`defaultCatalogDatasetSampleSize`)) {
          sprintf(
            '"defaultCatalogDatasetSampleSize":
          %s
        ',
            jsonlite::toJSON(self$`defaultCatalogDatasetSampleSize`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`expirationDate`)) {
          sprintf(
            '"expirationDate":
            "%s"
                  ',
            format(self$`expirationDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`firstName`)) {
          sprintf(
            '"firstName":
            "%s"
                  ',
            self$`firstName`
          )
        },
        if (!is.null(self$`language`)) {
          sprintf(
            '"language":
            "%s"
                  ',
            self$`language`
          )
        },
        if (!is.null(self$`lastName`)) {
          sprintf(
            '"lastName":
            "%s"
                  ',
            self$`lastName`
          )
        },
        if (!is.null(self$`maxGpuWorkers`)) {
          sprintf(
            '"maxGpuWorkers":
            %d
                  ',
            self$`maxGpuWorkers`
          )
        },
        if (!is.null(self$`maxIdleWorkers`)) {
          sprintf(
            '"maxIdleWorkers":
            %d
                  ',
            self$`maxIdleWorkers`
          )
        },
        if (!is.null(self$`maxUploadSize`)) {
          sprintf(
            '"maxUploadSize":
            %d
                  ',
            self$`maxUploadSize`
          )
        },
        if (!is.null(self$`maxUploadSizeCatalog`)) {
          sprintf(
            '"maxUploadSizeCatalog":
            %d
                  ',
            self$`maxUploadSizeCatalog`
          )
        },
        if (!is.null(self$`maxWorkers`)) {
          sprintf(
            '"maxWorkers":
            %d
                  ',
            self$`maxWorkers`
          )
        },
        if (!is.null(self$`organizationId`)) {
          sprintf(
            '"organizationId":
            "%s"
                  ',
            self$`organizationId`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`requireClickthroughAgreement`)) {
          sprintf(
            '"requireClickthroughAgreement":
            %s
                  ',
            tolower(self$`requireClickthroughAgreement`)
          )
        },
        if (!is.null(self$`userType`)) {
          sprintf(
            '"userType":
            "%s"
                  ',
            self$`userType`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserCreateJson, validateParams = FALSE) {
      UserCreateObject <- jsonlite::fromJSON(UserCreateJson)
      self$`country` <- UserCreateObject$`country`
      self$`defaultCatalogDatasetSampleSize` <- AdminSampleSize$new()$fromJSON(jsonlite::toJSON(UserCreateObject$defaultCatalogDatasetSampleSize, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`expirationDate` <- ParseRFC3339Timestamp(UserCreateObject$`expirationDate`)
      self$`firstName` <- UserCreateObject$`firstName`
      self$`language` <- UserCreateObject$`language`
      self$`lastName` <- UserCreateObject$`lastName`
      self$`maxGpuWorkers` <- UserCreateObject$`maxGpuWorkers`
      self$`maxIdleWorkers` <- UserCreateObject$`maxIdleWorkers`
      self$`maxUploadSize` <- UserCreateObject$`maxUploadSize`
      self$`maxUploadSizeCatalog` <- UserCreateObject$`maxUploadSizeCatalog`
      self$`maxWorkers` <- UserCreateObject$`maxWorkers`
      self$`organizationId` <- UserCreateObject$`organizationId`
      self$`password` <- UserCreateObject$`password`
      self$`requireClickthroughAgreement` <- UserCreateObject$`requireClickthroughAgreement`
      self$`userType` <- UserCreateObject$`userType`
      self$`username` <- UserCreateObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
