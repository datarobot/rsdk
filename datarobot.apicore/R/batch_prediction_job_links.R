# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BatchPredictionJobLinks
#'
#' @description BatchPredictionJobLinks Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field csvUpload  character [optional] The URL used to upload the dataset for this job. Only available for localFile intake.
#'
#' @field download  character [optional] The URL used to download the results from this job. Only available for localFile outputs. Will be null if the download is not yet available.
#'
#' @field self_  character The URL used access this job.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BatchPredictionJobLinks <- R6::R6Class(
  "BatchPredictionJobLinks",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`csvUpload` = NULL, `download` = NULL, `self_` = NULL) {
      if (!is.null(`self_`)) {
        stopifnot(is.character(`self_`), length(`self_`) == 1)
      }
      if (!is.null(`csvUpload`)) {
        stopifnot(is.character(`csvUpload`), length(`csvUpload`) == 1)
      }
      if (!is.null(`download`)) {
        stopifnot(is.character(`download`), length(`download`) == 1)
      }
    }
  ),
  public = list(
    `csvUpload` = NULL,
    `download` = NULL,
    `self_` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param csvUpload The URL used to upload the dataset for this job. Only available for localFile intake.
    #' @param download The URL used to download the results from this job. Only available for localFile outputs. Will be null if the download is not yet available.
    #' @param self_ The URL used access this job.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`self_` = NULL, `csvUpload` = NULL, `download` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`self_`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(csvUpload, download, self_)
      }
      self$`csvUpload` <- `csvUpload`
      self$`download` <- `download`
      self$`self_` <- `self_`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(csvUpload = self$`csvUpload`, download = self$`download`, self_ = self$`self_`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`csvUpload`)) {
          sprintf(
            '"csvUpload":
            "%s"
                  ',
            self$`csvUpload`
          )
        },
        if (!is.null(self$`download`)) {
          sprintf(
            '"download":
            "%s"
                  ',
            self$`download`
          )
        },
        if (!is.null(self$`self_`)) {
          sprintf(
            '"self":
            "%s"
                  ',
            self$`self_`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BatchPredictionJobLinksJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BatchPredictionJobLinksJson, validateParams = FALSE) {
      BatchPredictionJobLinksObject <- jsonlite::fromJSON(BatchPredictionJobLinksJson)
      self$`csvUpload` <- BatchPredictionJobLinksObject$`csvUpload`
      self$`download` <- BatchPredictionJobLinksObject$`download`
      self$`self_` <- BatchPredictionJobLinksObject$`self`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
