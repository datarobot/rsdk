# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BatchPredictionJobCSVSettings
#'
#' @description BatchPredictionJobCSVSettings Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field delimiter  \link{OneOfstringstring} CSV fields are delimited by this character. Use the string \&quot;tab\&quot; to denote TSV (TAB separated values).
#'
#' @field encoding  character The encoding to be used for intake and output. For example (but not limited to): \&quot;shift_jis\&quot;, \&quot;latin_1\&quot; or \&quot;mskanji\&quot;.
#'
#' @field quotechar  character Fields containing the delimiter or newlines must be quoted using this character.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BatchPredictionJobCSVSettings <- R6::R6Class(
  "BatchPredictionJobCSVSettings",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(delimiter = NULL, encoding = NULL, quotechar = NULL) {
      if (!is.null(`delimiter`)) {
        .setPrimitiveProperty(typeList = list("character"), propertyData = delimiter)
      }
      if (!is.null(`encoding`)) {
        stopifnot(is.character(`encoding`), length(`encoding`) == 1)
      }
      if (!is.null(`quotechar`)) {
        stopifnot(is.character(`quotechar`), length(`quotechar`) == 1)
      }
    }
  ),
  public = list(
    `delimiter` = NULL,
    `encoding` = NULL,
    `quotechar` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param delimiter CSV fields are delimited by this character. Use the string \&quot;tab\&quot; to denote TSV (TAB separated values).
    #' @param encoding The encoding to be used for intake and output. For example (but not limited to): \&quot;shift_jis\&quot;, \&quot;latin_1\&quot; or \&quot;mskanji\&quot;.
    #' @param quotechar Fields containing the delimiter or newlines must be quoted using this character.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`delimiter` = NULL, `encoding` = NULL, `quotechar` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`delimiter`, `encoding`, `quotechar`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(delimiter, encoding, quotechar)
      }
      self$`delimiter` <- .setPrimitiveProperty(typeList = list("character"), propertyData = delimiter)
      self$`encoding` <- `encoding`
      self$`quotechar` <- `quotechar`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(delimiter = self$`delimiter`, encoding = self$`encoding`, quotechar = self$`quotechar`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`delimiter`)) {
          sprintf(
            '"delimiter":
          %s
        ',
            self$`delimiter`
          )
        },
        if (!is.null(self$`encoding`)) {
          sprintf(
            '"encoding":
            "%s"
                  ',
            self$`encoding`
          )
        },
        if (!is.null(self$`quotechar`)) {
          sprintf(
            '"quotechar":
            "%s"
                  ',
            self$`quotechar`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BatchPredictionJobCSVSettingsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BatchPredictionJobCSVSettingsJson, validateParams = FALSE) {
      BatchPredictionJobCSVSettingsObject <- jsonlite::fromJSON(BatchPredictionJobCSVSettingsJson)
      self$`delimiter` <- .setPrimitiveProperty(typeList = list("character"), propertyData = BatchPredictionJobCSVSettingsObject$delimiter)
      self$`encoding` <- BatchPredictionJobCSVSettingsObject$`encoding`
      self$`quotechar` <- BatchPredictionJobCSVSettingsObject$`quotechar`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
