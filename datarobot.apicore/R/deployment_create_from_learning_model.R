# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentCreateFromLearningModel
#'
#' @description DeploymentCreateFromLearningModel Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field defaultPredictionServerId  character [optional] ID of the default prediction server for deployment. Required for DataRobot Cloud, must be omitted for Enterprise installations.
#'
#' @field description  character A description for the deployment.
#'
#' @field importance  character [optional] Used only when &#x60;Model Deployment Approvals Workflow&#x60; is enabled. The parameter to show how important the deployment is. If &#x60;Model Deployment Approvals Workflow&#x60; is enabled and &#x60;&#x60;materiality&#x60;&#x60; is not specified defaults to &#x60;&#x60;MODERATE&#x60;&#x60;.
#'
#' @field label  character A human-readable name for the deployment.
#'
#' @field modelId  character ID of the model to be deployed.
#'
#' @field predictionThreshold  numeric [optional] Prediction threshold used for binary classification. If not specified, model default prediction threshold will be used.
#'
#' @field status  character [optional] Status of the deployment
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentCreateFromLearningModel <- R6::R6Class(
  "DeploymentCreateFromLearningModel",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(defaultPredictionServerId = NULL, description = NULL, importance = NULL, label = NULL, modelId = NULL, predictionThreshold = NULL, status = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`defaultPredictionServerId`)) {
        stopifnot(is.character(`defaultPredictionServerId`), length(`defaultPredictionServerId`) == 1)
      }
      if (!is.null(`importance`)) {
        stopifnot(is.character(`importance`), length(`importance`) == 1)
      }
      if (!is.null(`predictionThreshold`)) {
        stopifnot(is.numeric(`predictionThreshold`), length(`predictionThreshold`) == 1)
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
    }
  ),
  public = list(
    `defaultPredictionServerId` = NULL,
    `description` = NULL,
    `importance` = NULL,
    `label` = NULL,
    `modelId` = NULL,
    `predictionThreshold` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param defaultPredictionServerId ID of the default prediction server for deployment. Required for DataRobot Cloud, must be omitted for Enterprise installations.
    #' @param description A description for the deployment.
    #' @param importance Used only when &#x60;Model Deployment Approvals Workflow&#x60; is enabled. The parameter to show how important the deployment is. If &#x60;Model Deployment Approvals Workflow&#x60; is enabled and &#x60;&#x60;materiality&#x60;&#x60; is not specified defaults to &#x60;&#x60;MODERATE&#x60;&#x60;.
    #' @param label A human-readable name for the deployment.
    #' @param modelId ID of the model to be deployed.
    #' @param predictionThreshold Prediction threshold used for binary classification. If not specified, model default prediction threshold will be used.
    #' @param status Status of the deployment
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `label` = NULL, `modelId` = NULL, `defaultPredictionServerId` = NULL, `importance` = NULL, `predictionThreshold` = NULL, `status` = "active", validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`description`, `label`, `modelId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(defaultPredictionServerId, description, importance, label, modelId, predictionThreshold, status)
      }
      self$`defaultPredictionServerId` <- `defaultPredictionServerId`
      self$`description` <- `description`
      self$`importance` <- `importance`
      self$`label` <- `label`
      self$`modelId` <- `modelId`
      self$`predictionThreshold` <- `predictionThreshold`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(defaultPredictionServerId = self$`defaultPredictionServerId`, description = self$`description`, importance = self$`importance`, label = self$`label`, modelId = self$`modelId`, predictionThreshold = self$`predictionThreshold`, status = self$`status`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`defaultPredictionServerId`)) {
          sprintf(
            '"defaultPredictionServerId":
            "%s"
                  ',
            self$`defaultPredictionServerId`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`importance`)) {
          sprintf(
            '"importance":
            "%s"
                  ',
            self$`importance`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentCreateFromLearningModelJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentCreateFromLearningModelJson, validateParams = FALSE) {
      DeploymentCreateFromLearningModelObject <- jsonlite::fromJSON(DeploymentCreateFromLearningModelJson)
      self$`defaultPredictionServerId` <- DeploymentCreateFromLearningModelObject$`defaultPredictionServerId`
      self$`description` <- DeploymentCreateFromLearningModelObject$`description`
      self$`importance` <- DeploymentCreateFromLearningModelObject$`importance`
      self$`label` <- DeploymentCreateFromLearningModelObject$`label`
      self$`modelId` <- DeploymentCreateFromLearningModelObject$`modelId`
      self$`predictionThreshold` <- DeploymentCreateFromLearningModelObject$`predictionThreshold`
      self$`status` <- DeploymentCreateFromLearningModelObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
