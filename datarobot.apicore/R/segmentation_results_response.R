# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SegmentationResultsResponse
#'
#' @description SegmentationResultsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field completedJobs  list( \link{SegmentationResultsCompletedResponse} ) List of completed segmentation tasks
#'
#' @field failedJobs  list( \link{SegmentationResultsFailedResponse} ) List of failed segmentation tasks
#'
#' @field numberOfJobs  integer Total number of completed and failed jobs processed
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SegmentationResultsResponse <- R6::R6Class(
  "SegmentationResultsResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(completedJobs = NULL, failedJobs = NULL, numberOfJobs = NULL) {
      if (!is.null(`completedJobs`)) {
        stopifnot(is.vector(`completedJobs`))
      }
      if (!is.null(`failedJobs`)) {
        stopifnot(is.vector(`failedJobs`))
      }
      if (!is.null(`numberOfJobs`)) {
        stopifnot(is.numeric(`numberOfJobs`), length(`numberOfJobs`) == 1)
      }
    }
  ),
  public = list(
    `completedJobs` = NULL,
    `failedJobs` = NULL,
    `numberOfJobs` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param completedJobs List of completed segmentation tasks
    #' @param failedJobs List of failed segmentation tasks
    #' @param numberOfJobs Total number of completed and failed jobs processed
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`completedJobs` = NULL, `failedJobs` = NULL, `numberOfJobs` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`completedJobs`, `failedJobs`, `numberOfJobs`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(completedJobs, failedJobs, numberOfJobs)
      }
      sapply(`completedJobs`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`failedJobs`, function(x) stopifnot(R6::is.R6(x)))
      self$`numberOfJobs` <- `numberOfJobs`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(completedJobs = self$`completedJobs`, failedJobs = self$`failedJobs`, numberOfJobs = self$`numberOfJobs`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`completedJobs`)) {
          sprintf(
            '"completedJobs":
            [%s]
',
            paste(sapply(self$`completedJobs`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`failedJobs`)) {
          sprintf(
            '"failedJobs":
            [%s]
',
            paste(sapply(self$`failedJobs`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`numberOfJobs`)) {
          sprintf(
            '"numberOfJobs":
            %d
                  ',
            self$`numberOfJobs`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SegmentationResultsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SegmentationResultsResponseJson, validateParams = FALSE) {
      SegmentationResultsResponseObject <- jsonlite::fromJSON(SegmentationResultsResponseJson)
      self$`completedJobs` <- ApiClient$new()$deserializeObj(SegmentationResultsResponseObject$`completedJobs`, "array[SegmentationResultsCompletedResponse]", loadNamespace("datarobot.apicore"))
      self$`failedJobs` <- ApiClient$new()$deserializeObj(SegmentationResultsResponseObject$`failedJobs`, "array[SegmentationResultsFailedResponse]", loadNamespace("datarobot.apicore"))
      self$`numberOfJobs` <- SegmentationResultsResponseObject$`numberOfJobs`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
