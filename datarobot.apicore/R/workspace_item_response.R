# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title WorkspaceItemResponse
#'
#' @description WorkspaceItemResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field commitSha  character [optional] SHA1 hash pointing to the original file revision (set only for files pulled from Git-like repositories).
#'
#' @field created  character ISO-8601 timestamp of when the file item was created.
#'
#' @field fileName  character Name of the file item.
#'
#' @field filePath  character Path of the file item.
#'
#' @field fileSource  character Source of the file item.
#'
#' @field id  character ID of the file item.
#'
#' @field ref  character [optional] Remote reference (branch, commit, tag). Branch \&quot;master\&quot;, if not specified.
#'
#' @field repositoryFilePath  character [optional] Full path to the file in the remote repository.
#'
#' @field repositoryLocation  character [optional] URL to remote repository from which the file was pulled (e.g. Git server or S3 Bucket name).
#'
#' @field repositoryName  character [optional] Name of the repository from which the file was pulled.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
WorkspaceItemResponse <- R6::R6Class(
  "WorkspaceItemResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`commitSha` = NULL, `created` = NULL, `fileName` = NULL, `filePath` = NULL, `fileSource` = NULL, `id` = NULL, `ref` = NULL, `repositoryFilePath` = NULL, `repositoryLocation` = NULL, `repositoryName` = NULL) {
      if (!is.null(`created`)) {
        stopifnot(is.character(`created`), length(`created`) == 1)
      }
      if (!is.null(`fileName`)) {
        stopifnot(is.character(`fileName`), length(`fileName`) == 1)
      }
      if (!is.null(`filePath`)) {
        stopifnot(is.character(`filePath`), length(`filePath`) == 1)
      }
      if (!is.null(`fileSource`)) {
        stopifnot(is.character(`fileSource`), length(`fileSource`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`commitSha`)) {
        stopifnot(is.character(`commitSha`), length(`commitSha`) == 1)
      }
      if (!is.null(`ref`)) {
        stopifnot(is.character(`ref`), length(`ref`) == 1)
      }
      if (!is.null(`repositoryFilePath`)) {
        stopifnot(is.character(`repositoryFilePath`), length(`repositoryFilePath`) == 1)
      }
      if (!is.null(`repositoryLocation`)) {
        stopifnot(is.character(`repositoryLocation`), length(`repositoryLocation`) == 1)
      }
      if (!is.null(`repositoryName`)) {
        stopifnot(is.character(`repositoryName`), length(`repositoryName`) == 1)
      }
    }
  ),
  public = list(
    `commitSha` = NULL,
    `created` = NULL,
    `fileName` = NULL,
    `filePath` = NULL,
    `fileSource` = NULL,
    `id` = NULL,
    `ref` = NULL,
    `repositoryFilePath` = NULL,
    `repositoryLocation` = NULL,
    `repositoryName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param commitSha SHA1 hash pointing to the original file revision (set only for files pulled from Git-like repositories).
    #' @param created ISO-8601 timestamp of when the file item was created.
    #' @param fileName Name of the file item.
    #' @param filePath Path of the file item.
    #' @param fileSource Source of the file item.
    #' @param id ID of the file item.
    #' @param ref Remote reference (branch, commit, tag). Branch \&quot;master\&quot;, if not specified.
    #' @param repositoryFilePath Full path to the file in the remote repository.
    #' @param repositoryLocation URL to remote repository from which the file was pulled (e.g. Git server or S3 Bucket name).
    #' @param repositoryName Name of the repository from which the file was pulled.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`created` = NULL, `fileName` = NULL, `filePath` = NULL, `fileSource` = NULL, `id` = NULL, `commitSha` = NULL, `ref` = NULL, `repositoryFilePath` = NULL, `repositoryLocation` = NULL, `repositoryName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`created`, `fileName`, `filePath`, `fileSource`, `id`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(commitSha, created, fileName, filePath, fileSource, id, ref, repositoryFilePath, repositoryLocation, repositoryName)
      }
      self$`commitSha` <- `commitSha`
      self$`created` <- `created`
      self$`fileName` <- `fileName`
      self$`filePath` <- `filePath`
      self$`fileSource` <- `fileSource`
      self$`id` <- `id`
      self$`ref` <- `ref`
      self$`repositoryFilePath` <- `repositoryFilePath`
      self$`repositoryLocation` <- `repositoryLocation`
      self$`repositoryName` <- `repositoryName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(commitSha = self$`commitSha`, created = self$`created`, fileName = self$`fileName`, filePath = self$`filePath`, fileSource = self$`fileSource`, id = self$`id`, ref = self$`ref`, repositoryFilePath = self$`repositoryFilePath`, repositoryLocation = self$`repositoryLocation`, repositoryName = self$`repositoryName`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`commitSha`)) {
          sprintf(
            '"commitSha":
            "%s"
                  ',
            self$`commitSha`
          )
        },
        if (!is.null(self$`created`)) {
          sprintf(
            '"created":
            "%s"
                  ',
            self$`created`
          )
        },
        if (!is.null(self$`fileName`)) {
          sprintf(
            '"fileName":
            "%s"
                  ',
            self$`fileName`
          )
        },
        if (!is.null(self$`filePath`)) {
          sprintf(
            '"filePath":
            "%s"
                  ',
            self$`filePath`
          )
        },
        if (!is.null(self$`fileSource`)) {
          sprintf(
            '"fileSource":
            "%s"
                  ',
            self$`fileSource`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`ref`)) {
          sprintf(
            '"ref":
            "%s"
                  ',
            self$`ref`
          )
        },
        if (!is.null(self$`repositoryFilePath`)) {
          sprintf(
            '"repositoryFilePath":
            "%s"
                  ',
            self$`repositoryFilePath`
          )
        },
        if (!is.null(self$`repositoryLocation`)) {
          sprintf(
            '"repositoryLocation":
            "%s"
                  ',
            self$`repositoryLocation`
          )
        },
        if (!is.null(self$`repositoryName`)) {
          sprintf(
            '"repositoryName":
            "%s"
                  ',
            self$`repositoryName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param WorkspaceItemResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(WorkspaceItemResponseJson, validateParams = FALSE) {
      WorkspaceItemResponseObject <- jsonlite::fromJSON(WorkspaceItemResponseJson)
      self$`commitSha` <- WorkspaceItemResponseObject$`commitSha`
      self$`created` <- WorkspaceItemResponseObject$`created`
      self$`fileName` <- WorkspaceItemResponseObject$`fileName`
      self$`filePath` <- WorkspaceItemResponseObject$`filePath`
      self$`fileSource` <- WorkspaceItemResponseObject$`fileSource`
      self$`id` <- WorkspaceItemResponseObject$`id`
      self$`ref` <- WorkspaceItemResponseObject$`ref`
      self$`repositoryFilePath` <- WorkspaceItemResponseObject$`repositoryFilePath`
      self$`repositoryLocation` <- WorkspaceItemResponseObject$`repositoryLocation`
      self$`repositoryName` <- WorkspaceItemResponseObject$`repositoryName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
