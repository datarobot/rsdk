# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ProjectOptions
#'
#' @description ProjectOptions Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field cvMethod  character [optional] The partitioning method for projects used to build new models.
#'
#' @field holdoutPct  numeric [optional] The percentage of dataset to assign to holdout set in projects used to build new models.
#'
#' @field metric  character [optional] The model selection metric in projects used to build new models.
#'
#' @field reps  integer [optional] The number of cross validation folds to use for projects used to build new models.
#'
#' @field validationPct  numeric [optional] The percentage of dataset to assign to validation set in projects used to build new models.
#'
#' @field validationType  character [optional] The validation type for projects used to build new models.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ProjectOptions <- R6::R6Class(
  "ProjectOptions",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`cvMethod` = NULL, `holdoutPct` = NULL, `metric` = NULL, `reps` = NULL, `validationPct` = NULL, `validationType` = NULL) {
      if (!is.null(`cvMethod`)) {
        stopifnot(is.character(`cvMethod`), length(`cvMethod`) == 1)
      }
      if (!is.null(`holdoutPct`)) {
        stopifnot(is.numeric(`holdoutPct`), length(`holdoutPct`) == 1)
      }
      if (!is.null(`metric`)) {
        stopifnot(is.character(`metric`), length(`metric`) == 1)
      }
      if (!is.null(`reps`)) {
        stopifnot(is.numeric(`reps`), length(`reps`) == 1)
      }
      if (!is.null(`validationPct`)) {
        stopifnot(is.numeric(`validationPct`), length(`validationPct`) == 1)
      }
      if (!is.null(`validationType`)) {
        stopifnot(is.character(`validationType`), length(`validationType`) == 1)
      }
    }
  ),
  public = list(
    `cvMethod` = NULL,
    `holdoutPct` = NULL,
    `metric` = NULL,
    `reps` = NULL,
    `validationPct` = NULL,
    `validationType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param cvMethod The partitioning method for projects used to build new models.
    #' @param holdoutPct The percentage of dataset to assign to holdout set in projects used to build new models.
    #' @param metric The model selection metric in projects used to build new models.
    #' @param reps The number of cross validation folds to use for projects used to build new models.
    #' @param validationPct The percentage of dataset to assign to validation set in projects used to build new models.
    #' @param validationType The validation type for projects used to build new models.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`cvMethod` = NULL, `holdoutPct` = NULL, `metric` = NULL, `reps` = NULL, `validationPct` = NULL, `validationType` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(cvMethod, holdoutPct, metric, reps, validationPct, validationType)
      }
      self$`cvMethod` <- `cvMethod`
      self$`holdoutPct` <- `holdoutPct`
      self$`metric` <- `metric`
      self$`reps` <- `reps`
      self$`validationPct` <- `validationPct`
      self$`validationType` <- `validationType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(cvMethod = self$`cvMethod`, holdoutPct = self$`holdoutPct`, metric = self$`metric`, reps = self$`reps`, validationPct = self$`validationPct`, validationType = self$`validationType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`cvMethod`)) {
          sprintf(
            '"cvMethod":
            "%s"
                  ',
            self$`cvMethod`
          )
        },
        if (!is.null(self$`holdoutPct`)) {
          sprintf(
            '"holdoutPct":
            %d
                  ',
            self$`holdoutPct`
          )
        },
        if (!is.null(self$`metric`)) {
          sprintf(
            '"metric":
            "%s"
                  ',
            self$`metric`
          )
        },
        if (!is.null(self$`reps`)) {
          sprintf(
            '"reps":
            %d
                  ',
            self$`reps`
          )
        },
        if (!is.null(self$`validationPct`)) {
          sprintf(
            '"validationPct":
            %d
                  ',
            self$`validationPct`
          )
        },
        if (!is.null(self$`validationType`)) {
          sprintf(
            '"validationType":
            "%s"
                  ',
            self$`validationType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ProjectOptionsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ProjectOptionsJson, validateParams = FALSE) {
      ProjectOptionsObject <- jsonlite::fromJSON(ProjectOptionsJson)
      self$`cvMethod` <- ProjectOptionsObject$`cvMethod`
      self$`holdoutPct` <- ProjectOptionsObject$`holdoutPct`
      self$`metric` <- ProjectOptionsObject$`metric`
      self$`reps` <- ProjectOptionsObject$`reps`
      self$`validationPct` <- ProjectOptionsObject$`validationPct`
      self$`validationType` <- ProjectOptionsObject$`validationType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
