# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BatchPredictionCreatedBy
#'
#' @description BatchPredictionCreatedBy Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field fullName  character The full name of the user who created this job (if defined by the user)
#'
#' @field userId  character The User ID of the user who created this job
#'
#' @field username  character The username (e-mail address) of the user who created this job
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BatchPredictionCreatedBy <- R6::R6Class(
  "BatchPredictionCreatedBy",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(fullName = NULL, userId = NULL, username = NULL) {
      if (!is.null(`fullName`)) {
        stopifnot(is.character(`fullName`), length(`fullName`) == 1)
      }
      if (!is.null(`userId`)) {
        stopifnot(is.character(`userId`), length(`userId`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
    }
  ),
  public = list(
    `fullName` = NULL,
    `userId` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param fullName The full name of the user who created this job (if defined by the user)
    #' @param userId The User ID of the user who created this job
    #' @param username The username (e-mail address) of the user who created this job
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`fullName` = NULL, `userId` = NULL, `username` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`fullName`, `userId`, `username`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(fullName, userId, username)
      }
      self$`fullName` <- `fullName`
      self$`userId` <- `userId`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(fullName = self$`fullName`, userId = self$`userId`, username = self$`username`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`fullName`)) {
          sprintf(
            '"fullName":
            "%s"
                  ',
            self$`fullName`
          )
        },
        if (!is.null(self$`userId`)) {
          sprintf(
            '"userId":
            "%s"
                  ',
            self$`userId`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BatchPredictionCreatedByJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BatchPredictionCreatedByJson, validateParams = FALSE) {
      BatchPredictionCreatedByObject <- jsonlite::fromJSON(BatchPredictionCreatedByJson)
      self$`fullName` <- BatchPredictionCreatedByObject$`fullName`
      self$`userId` <- BatchPredictionCreatedByObject$`userId`
      self$`username` <- BatchPredictionCreatedByObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
