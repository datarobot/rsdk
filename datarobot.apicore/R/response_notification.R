# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ResponseNotification
#'
#' @description ResponseNotification Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field body  character The body of the response
#'
#' @field duration  integer The duration
#'
#' @field headers  character The headers of the response
#'
#' @field statusCode  character The status code of the response
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ResponseNotification <- R6::R6Class(
  "ResponseNotification",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`body` = NULL, `duration` = NULL, `headers` = NULL, `statusCode` = NULL) {
      if (!is.null(`body`)) {
        stopifnot(is.character(`body`), length(`body`) == 1)
      }
      if (!is.null(`duration`)) {
        stopifnot(is.numeric(`duration`), length(`duration`) == 1)
      }
      if (!is.null(`headers`)) {
        stopifnot(is.character(`headers`), length(`headers`) == 1)
      }
      if (!is.null(`statusCode`)) {
        stopifnot(is.character(`statusCode`), length(`statusCode`) == 1)
      }
    }
  ),
  public = list(
    `body` = NULL,
    `duration` = NULL,
    `headers` = NULL,
    `statusCode` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param body The body of the response
    #' @param duration The duration
    #' @param headers The headers of the response
    #' @param statusCode The status code of the response
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`body` = NULL, `duration` = NULL, `headers` = NULL, `statusCode` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`body`, `duration`, `headers`, `statusCode`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(body, duration, headers, statusCode)
      }
      self$`body` <- `body`
      self$`duration` <- `duration`
      self$`headers` <- `headers`
      self$`statusCode` <- `statusCode`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(body = self$`body`, duration = self$`duration`, headers = self$`headers`, statusCode = self$`statusCode`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`body`)) {
          sprintf(
            '"body":
            "%s"
                  ',
            self$`body`
          )
        },
        if (!is.null(self$`duration`)) {
          sprintf(
            '"duration":
            %d
                  ',
            self$`duration`
          )
        },
        if (!is.null(self$`headers`)) {
          sprintf(
            '"headers":
            "%s"
                  ',
            self$`headers`
          )
        },
        if (!is.null(self$`statusCode`)) {
          sprintf(
            '"statusCode":
            "%s"
                  ',
            self$`statusCode`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ResponseNotificationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ResponseNotificationJson, validateParams = FALSE) {
      ResponseNotificationObject <- jsonlite::fromJSON(ResponseNotificationJson)
      self$`body` <- ResponseNotificationObject$`body`
      self$`duration` <- ResponseNotificationObject$`duration`
      self$`headers` <- ResponseNotificationObject$`headers`
      self$`statusCode` <- ResponseNotificationObject$`statusCode`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
