# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TrainModel
#'
#' @description TrainModel Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blueprintId  character The ID of a blueprint to use to generate the model. Allowed blueprints can be retrieved using :http:get:&#x60;/api/v2/projects/(projectId)/blueprints/&#x60; or taken from existing models.
#'
#' @field featurelistId  character [optional] If specified, the model will be trained using this featurelist. If not specified, the recommended featurelist for the specified blueprint will be used. If there is no recommended featurelist, the project&#39;s default will be used.
#'
#' @field monotonicDecreasingFeaturelistId  character [optional] The ID of the featurelist that defines the set of features with a monotonically decreasing relationship to the target. If &#x60;&#x60;null&#x60;&#x60;, no constraints will be enforced. If omitted, the project default is used.
#'
#' @field monotonicIncreasingFeaturelistId  character [optional] The ID of the featurelist that defines the set of features with a monotonically increasing relationship to the target. If &#x60;&#x60;null&#x60;&#x60;, no constraints will be enforced. If omitted, the project default is used.
#'
#' @field nClusters  integer [optional] The number of clusters to use in the specified unsupervised clustering model. Only valid in unsupervised clustering projects.
#'
#' @field samplePct  numeric [optional] The percentage of the dataset to use with the model. Only one of &#x60;samplePct&#x60; and &#x60;trainingRowCount&#x60; should be specified. The specified percentage should be between 0 and 100.
#'
#' @field scoringType  character [optional] Validation is available for any partitioning. If the project uses cross validation, &#x60;crossValidation&#x60; may be used to indicate that all available training/validation combinations should be used.
#'
#' @field sourceProjectId  character [optional] The project the blueprint comes from. Required only if the &#x60;blueprintId&#x60; comes from a different project.
#'
#' @field trainingRowCount  integer [optional] An integer representing the number of rows of the dataset to use with the model. Only one of &#x60;samplePct&#x60; and &#x60;trainingRowCount&#x60; should be specified.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TrainModel <- R6::R6Class(
  "TrainModel",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`blueprintId` = NULL, `featurelistId` = NULL, `monotonicDecreasingFeaturelistId` = NULL, `monotonicIncreasingFeaturelistId` = NULL, `nClusters` = NULL, `samplePct` = NULL, `scoringType` = NULL, `sourceProjectId` = NULL, `trainingRowCount` = NULL) {
      if (!is.null(`blueprintId`)) {
        stopifnot(is.character(`blueprintId`), length(`blueprintId`) == 1)
      }
      if (!is.null(`featurelistId`)) {
        stopifnot(is.character(`featurelistId`), length(`featurelistId`) == 1)
      }
      if (!is.null(`monotonicDecreasingFeaturelistId`)) {
        stopifnot(is.character(`monotonicDecreasingFeaturelistId`), length(`monotonicDecreasingFeaturelistId`) == 1)
      }
      if (!is.null(`monotonicIncreasingFeaturelistId`)) {
        stopifnot(is.character(`monotonicIncreasingFeaturelistId`), length(`monotonicIncreasingFeaturelistId`) == 1)
      }
      if (!is.null(`nClusters`)) {
        stopifnot(is.numeric(`nClusters`), length(`nClusters`) == 1)
      }
      if (!is.null(`samplePct`)) {
        stopifnot(is.numeric(`samplePct`), length(`samplePct`) == 1)
      }
      if (!is.null(`scoringType`)) {
        stopifnot(is.character(`scoringType`), length(`scoringType`) == 1)
      }
      if (!is.null(`sourceProjectId`)) {
        stopifnot(is.character(`sourceProjectId`), length(`sourceProjectId`) == 1)
      }
      if (!is.null(`trainingRowCount`)) {
        stopifnot(is.numeric(`trainingRowCount`), length(`trainingRowCount`) == 1)
      }
    }
  ),
  public = list(
    `blueprintId` = NULL,
    `featurelistId` = NULL,
    `monotonicDecreasingFeaturelistId` = NULL,
    `monotonicIncreasingFeaturelistId` = NULL,
    `nClusters` = NULL,
    `samplePct` = NULL,
    `scoringType` = NULL,
    `sourceProjectId` = NULL,
    `trainingRowCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blueprintId The ID of a blueprint to use to generate the model. Allowed blueprints can be retrieved using :http:get:&#x60;/api/v2/projects/(projectId)/blueprints/&#x60; or taken from existing models.
    #' @param featurelistId If specified, the model will be trained using this featurelist. If not specified, the recommended featurelist for the specified blueprint will be used. If there is no recommended featurelist, the project&#39;s default will be used.
    #' @param monotonicDecreasingFeaturelistId The ID of the featurelist that defines the set of features with a monotonically decreasing relationship to the target. If &#x60;&#x60;null&#x60;&#x60;, no constraints will be enforced. If omitted, the project default is used.
    #' @param monotonicIncreasingFeaturelistId The ID of the featurelist that defines the set of features with a monotonically increasing relationship to the target. If &#x60;&#x60;null&#x60;&#x60;, no constraints will be enforced. If omitted, the project default is used.
    #' @param nClusters The number of clusters to use in the specified unsupervised clustering model. Only valid in unsupervised clustering projects.
    #' @param samplePct The percentage of the dataset to use with the model. Only one of &#x60;samplePct&#x60; and &#x60;trainingRowCount&#x60; should be specified. The specified percentage should be between 0 and 100.
    #' @param scoringType Validation is available for any partitioning. If the project uses cross validation, &#x60;crossValidation&#x60; may be used to indicate that all available training/validation combinations should be used.
    #' @param sourceProjectId The project the blueprint comes from. Required only if the &#x60;blueprintId&#x60; comes from a different project.
    #' @param trainingRowCount An integer representing the number of rows of the dataset to use with the model. Only one of &#x60;samplePct&#x60; and &#x60;trainingRowCount&#x60; should be specified.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blueprintId` = NULL, `featurelistId` = NULL, `monotonicDecreasingFeaturelistId` = NULL, `monotonicIncreasingFeaturelistId` = NULL, `nClusters` = NULL, `samplePct` = NULL, `scoringType` = NULL, `sourceProjectId` = NULL, `trainingRowCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`blueprintId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(blueprintId, featurelistId, monotonicDecreasingFeaturelistId, monotonicIncreasingFeaturelistId, nClusters, samplePct, scoringType, sourceProjectId, trainingRowCount)
      }
      self$`blueprintId` <- `blueprintId`
      self$`featurelistId` <- `featurelistId`
      self$`monotonicDecreasingFeaturelistId` <- `monotonicDecreasingFeaturelistId`
      self$`monotonicIncreasingFeaturelistId` <- `monotonicIncreasingFeaturelistId`
      self$`nClusters` <- `nClusters`
      self$`samplePct` <- `samplePct`
      self$`scoringType` <- `scoringType`
      self$`sourceProjectId` <- `sourceProjectId`
      self$`trainingRowCount` <- `trainingRowCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(blueprintId = self$`blueprintId`, featurelistId = self$`featurelistId`, monotonicDecreasingFeaturelistId = self$`monotonicDecreasingFeaturelistId`, monotonicIncreasingFeaturelistId = self$`monotonicIncreasingFeaturelistId`, nClusters = self$`nClusters`, samplePct = self$`samplePct`, scoringType = self$`scoringType`, sourceProjectId = self$`sourceProjectId`, trainingRowCount = self$`trainingRowCount`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blueprintId`)) {
          sprintf(
            '"blueprintId":
            "%s"
                  ',
            self$`blueprintId`
          )
        },
        if (!is.null(self$`featurelistId`)) {
          sprintf(
            '"featurelistId":
            "%s"
                  ',
            self$`featurelistId`
          )
        },
        if (!is.null(self$`monotonicDecreasingFeaturelistId`)) {
          sprintf(
            '"monotonicDecreasingFeaturelistId":
            "%s"
                  ',
            self$`monotonicDecreasingFeaturelistId`
          )
        },
        if (!is.null(self$`monotonicIncreasingFeaturelistId`)) {
          sprintf(
            '"monotonicIncreasingFeaturelistId":
            "%s"
                  ',
            self$`monotonicIncreasingFeaturelistId`
          )
        },
        if (!is.null(self$`nClusters`)) {
          sprintf(
            '"nClusters":
            %d
                  ',
            self$`nClusters`
          )
        },
        if (!is.null(self$`samplePct`)) {
          sprintf(
            '"samplePct":
            %d
                  ',
            self$`samplePct`
          )
        },
        if (!is.null(self$`scoringType`)) {
          sprintf(
            '"scoringType":
            "%s"
                  ',
            self$`scoringType`
          )
        },
        if (!is.null(self$`sourceProjectId`)) {
          sprintf(
            '"sourceProjectId":
            "%s"
                  ',
            self$`sourceProjectId`
          )
        },
        if (!is.null(self$`trainingRowCount`)) {
          sprintf(
            '"trainingRowCount":
            %d
                  ',
            self$`trainingRowCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TrainModelJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TrainModelJson, validateParams = FALSE) {
      TrainModelObject <- jsonlite::fromJSON(TrainModelJson)
      self$`blueprintId` <- TrainModelObject$`blueprintId`
      self$`featurelistId` <- TrainModelObject$`featurelistId`
      self$`monotonicDecreasingFeaturelistId` <- TrainModelObject$`monotonicDecreasingFeaturelistId`
      self$`monotonicIncreasingFeaturelistId` <- TrainModelObject$`monotonicIncreasingFeaturelistId`
      self$`nClusters` <- TrainModelObject$`nClusters`
      self$`samplePct` <- TrainModelObject$`samplePct`
      self$`scoringType` <- TrainModelObject$`scoringType`
      self$`sourceProjectId` <- TrainModelObject$`sourceProjectId`
      self$`trainingRowCount` <- TrainModelObject$`trainingRowCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
