# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SecondaryDatasetConfigUpdate
#'
#' @description SecondaryDatasetConfigUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialsIds  list( \link{DatasetsCredential} ) [optional] List of credentials used by the secondary datasets
#'
#' @field secondaryDatasetConfigId  character ID of the secondary datasets configuration to be used at the time of prediction
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SecondaryDatasetConfigUpdate <- R6::R6Class(
  "SecondaryDatasetConfigUpdate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(credentialsIds = NULL, secondaryDatasetConfigId = NULL) {
      if (!is.null(`secondaryDatasetConfigId`)) {
        stopifnot(is.character(`secondaryDatasetConfigId`), length(`secondaryDatasetConfigId`) == 1)
      }
      if (!is.null(`credentialsIds`)) {
        stopifnot(is.vector(`credentialsIds`))
      }
    }
  ),
  public = list(
    `credentialsIds` = NULL,
    `secondaryDatasetConfigId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialsIds List of credentials used by the secondary datasets
    #' @param secondaryDatasetConfigId ID of the secondary datasets configuration to be used at the time of prediction
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`secondaryDatasetConfigId` = NULL, `credentialsIds` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`secondaryDatasetConfigId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(credentialsIds, secondaryDatasetConfigId)
      }
      sapply(`credentialsIds`, function(x) stopifnot(R6::is.R6(x)))
      self$`secondaryDatasetConfigId` <- `secondaryDatasetConfigId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(credentialsIds = self$`credentialsIds`, secondaryDatasetConfigId = self$`secondaryDatasetConfigId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialsIds`)) {
          sprintf(
            '"credentialsIds":
            [%s]
',
            paste(sapply(self$`credentialsIds`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`secondaryDatasetConfigId`)) {
          sprintf(
            '"secondaryDatasetConfigId":
            "%s"
                  ',
            self$`secondaryDatasetConfigId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SecondaryDatasetConfigUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SecondaryDatasetConfigUpdateJson, validateParams = FALSE) {
      SecondaryDatasetConfigUpdateObject <- jsonlite::fromJSON(SecondaryDatasetConfigUpdateJson)
      self$`credentialsIds` <- ApiClient$new()$deserializeObj(SecondaryDatasetConfigUpdateObject$`credentialsIds`, "array[DatasetsCredential]", loadNamespace("datarobot.apicore"))
      self$`secondaryDatasetConfigId` <- SecondaryDatasetConfigUpdateObject$`secondaryDatasetConfigId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
