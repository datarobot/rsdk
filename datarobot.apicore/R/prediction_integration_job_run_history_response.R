# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionIntegrationJobRunHistoryResponse
#'
#' @description PredictionIntegrationJobRunHistoryResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field completed  character The time this job instance completed in ISO UTC format (e.g. &#39;2020-02-20T18:00:02.218000Z&#39;)
#'
#' @field created  character The time this job instance was created in ISO UTC format (e.g. &#39;2020-02-20T18:00:02.218000Z&#39;)
#'
#' @field duration  integer The number of seconds this job took to be completed
#'
#' @field failed  character The time this job instance failed in ISO UTC format (e.g. &#39;2020-02-20T18:00:02.218000Z&#39;)
#'
#' @field failedRows  integer The number of rows that failed to score correctly
#'
#' @field jobRunId  character The ID of the job run. Used for accessing log information
#'
#' @field jobStatus  character The current status of the job
#'
#' @field scoredRows  integer The number of rows that were successfully scored
#'
#' @field totalRows  integer The number of rows that were processed
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionIntegrationJobRunHistoryResponse <- R6::R6Class(
  "PredictionIntegrationJobRunHistoryResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(completed = NULL, created = NULL, duration = NULL, failed = NULL, failedRows = NULL, jobRunId = NULL, jobStatus = NULL, scoredRows = NULL, totalRows = NULL) {
      if (!is.null(`completed`)) {
        stopifnot(inherits(`completed`, 'POSIXt'))
      }
      if (!is.null(`created`)) {
        stopifnot(inherits(`created`, 'POSIXt'))
      }
      if (!is.null(`duration`)) {
        stopifnot(is.numeric(`duration`), length(`duration`) == 1)
      }
      if (!is.null(`failed`)) {
        stopifnot(inherits(`failed`, 'POSIXt'))
      }
      if (!is.null(`failedRows`)) {
        stopifnot(is.numeric(`failedRows`), length(`failedRows`) == 1)
      }
      if (!is.null(`jobRunId`)) {
        stopifnot(is.character(`jobRunId`), length(`jobRunId`) == 1)
      }
      if (!is.null(`jobStatus`)) {
        stopifnot(is.character(`jobStatus`), length(`jobStatus`) == 1)
      }
      if (!is.null(`scoredRows`)) {
        stopifnot(is.numeric(`scoredRows`), length(`scoredRows`) == 1)
      }
      if (!is.null(`totalRows`)) {
        stopifnot(is.numeric(`totalRows`), length(`totalRows`) == 1)
      }
    }
  ),
  public = list(
    `completed` = NULL,
    `created` = NULL,
    `duration` = NULL,
    `failed` = NULL,
    `failedRows` = NULL,
    `jobRunId` = NULL,
    `jobStatus` = NULL,
    `scoredRows` = NULL,
    `totalRows` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param completed The time this job instance completed in ISO UTC format (e.g. &#39;2020-02-20T18:00:02.218000Z&#39;)
    #' @param created The time this job instance was created in ISO UTC format (e.g. &#39;2020-02-20T18:00:02.218000Z&#39;)
    #' @param duration The number of seconds this job took to be completed
    #' @param failed The time this job instance failed in ISO UTC format (e.g. &#39;2020-02-20T18:00:02.218000Z&#39;)
    #' @param failedRows The number of rows that failed to score correctly
    #' @param jobRunId The ID of the job run. Used for accessing log information
    #' @param jobStatus The current status of the job
    #' @param scoredRows The number of rows that were successfully scored
    #' @param totalRows The number of rows that were processed
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `completed` = NULL, `created` = NULL, `duration` = NULL, `failed` = NULL, `failedRows` = NULL, `jobRunId` = NULL, `jobStatus` = NULL, `scoredRows` = NULL, `totalRows` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`completed`, `created`, `duration`, `failed`, `failedRows`, `jobRunId`, `jobStatus`, `scoredRows`, `totalRows`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(completed, created, duration, failed, failedRows, jobRunId, jobStatus, scoredRows, totalRows)
      }
      self$`completed` <- `completed`
      self$`created` <- `created`
      self$`duration` <- `duration`
      self$`failed` <- `failed`
      self$`failedRows` <- `failedRows`
      self$`jobRunId` <- `jobRunId`
      self$`jobStatus` <- `jobStatus`
      self$`scoredRows` <- `scoredRows`
      self$`totalRows` <- `totalRows`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(completed = self$`completed`, created = self$`created`, duration = self$`duration`, failed = self$`failed`, failedRows = self$`failedRows`, jobRunId = self$`jobRunId`, jobStatus = self$`jobStatus`, scoredRows = self$`scoredRows`, totalRows = self$`totalRows`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`completed`)) {
        sprintf(
        '"completed":
          "%s"
                ',
        format(self$`completed`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
        )},
        if (!is.null(self$`created`)) {
        sprintf(
        '"created":
          "%s"
                ',
        format(self$`created`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
        )},
        if (!is.null(self$`duration`)) {
        sprintf(
        '"duration":
          %d
                ',
        self$`duration`
        )},
        if (!is.null(self$`failed`)) {
        sprintf(
        '"failed":
          "%s"
                ',
        format(self$`failed`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
        )},
        if (!is.null(self$`failedRows`)) {
        sprintf(
        '"failedRows":
          %d
                ',
        self$`failedRows`
        )},
        if (!is.null(self$`jobRunId`)) {
        sprintf(
        '"jobRunId":
          "%s"
                ',
        self$`jobRunId`
        )},
        if (!is.null(self$`jobStatus`)) {
        sprintf(
        '"jobStatus":
          "%s"
                ',
        self$`jobStatus`
        )},
        if (!is.null(self$`scoredRows`)) {
        sprintf(
        '"scoredRows":
          %d
                ',
        self$`scoredRows`
        )},
        if (!is.null(self$`totalRows`)) {
        sprintf(
        '"totalRows":
          %d
                ',
        self$`totalRows`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionIntegrationJobRunHistoryResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionIntegrationJobRunHistoryResponseJson, validateParams = FALSE) {
      PredictionIntegrationJobRunHistoryResponseObject <- jsonlite::fromJSON(PredictionIntegrationJobRunHistoryResponseJson)
      self$`completed` <- ParseRFC3339Timestamp(PredictionIntegrationJobRunHistoryResponseObject$`completed`)
      self$`created` <- ParseRFC3339Timestamp(PredictionIntegrationJobRunHistoryResponseObject$`created`)
      self$`duration` <- PredictionIntegrationJobRunHistoryResponseObject$`duration`
      self$`failed` <- ParseRFC3339Timestamp(PredictionIntegrationJobRunHistoryResponseObject$`failed`)
      self$`failedRows` <- PredictionIntegrationJobRunHistoryResponseObject$`failedRows`
      self$`jobRunId` <- PredictionIntegrationJobRunHistoryResponseObject$`jobRunId`
      self$`jobStatus` <- PredictionIntegrationJobRunHistoryResponseObject$`jobStatus`
      self$`scoredRows` <- PredictionIntegrationJobRunHistoryResponseObject$`scoredRows`
      self$`totalRows` <- PredictionIntegrationJobRunHistoryResponseObject$`totalRows`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
