# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotificationChannel
#'
#' @description NotificationChannel Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field channelType  character The type of the new notification channel.
#'
#' @field contentType  character The content type of the messages of the new notification channel.
#'
#' @field createdAt  character The date of the notification channel creation.
#'
#' @field customHeaders  list( \link{CustomerHeader} ) Custom headers and their values to be sent in the new notification channel.
#'
#' @field emailAddress  character The email address to be used in the new notification channel.
#'
#' @field id  character The id of the notification channel.
#'
#' @field languageCode  character The preferred language code.
#'
#' @field name  character The name of the new notification channel.
#'
#' @field orgId  character The id of organization that notification channel belongs to.
#'
#' @field payloadUrl  character The payload URL of the new notification channel.
#'
#' @field secretToken  character Secret token to be used for new notification channel.
#'
#' @field uid  character The identifier of the user who created the channel.
#'
#' @field validateSsl  character Defines if  validate ssl or not in the notification channel.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotificationChannel <- R6::R6Class(
  "NotificationChannel",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`channelType` = NULL, `contentType` = NULL, `createdAt` = NULL, `customHeaders` = NULL, `emailAddress` = NULL, `id` = NULL, `languageCode` = NULL, `name` = NULL, `orgId` = NULL, `payloadUrl` = NULL, `secretToken` = NULL, `uid` = NULL, `validateSsl` = NULL) {
      if (!is.null(`channelType`)) {
        stopifnot(is.character(`channelType`), length(`channelType`) == 1)
      }
      if (!is.null(`contentType`)) {
        stopifnot(is.character(`contentType`), length(`contentType`) == 1)
      }
      if (!is.null(`createdAt`)) {
        stopifnot(inherits(`createdAt`, "POSIXt"))
      }
      if (!is.null(`customHeaders`)) {
        stopifnot(is.vector(`customHeaders`))
      }
      if (!is.null(`emailAddress`)) {
        stopifnot(is.character(`emailAddress`), length(`emailAddress`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`languageCode`)) {
        stopifnot(is.character(`languageCode`), length(`languageCode`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
      if (!is.null(`payloadUrl`)) {
        stopifnot(is.character(`payloadUrl`), length(`payloadUrl`) == 1)
      }
      if (!is.null(`secretToken`)) {
        stopifnot(is.character(`secretToken`), length(`secretToken`) == 1)
      }
      if (!is.null(`uid`)) {
        stopifnot(is.character(`uid`), length(`uid`) == 1)
      }
      if (!is.null(`validateSsl`)) {
        stopifnot(is.logical(`validateSsl`), length(`validateSsl`) == 1)
      }
    }
  ),
  public = list(
    `channelType` = NULL,
    `contentType` = NULL,
    `createdAt` = NULL,
    `customHeaders` = NULL,
    `emailAddress` = NULL,
    `id` = NULL,
    `languageCode` = NULL,
    `name` = NULL,
    `orgId` = NULL,
    `payloadUrl` = NULL,
    `secretToken` = NULL,
    `uid` = NULL,
    `validateSsl` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param channelType The type of the new notification channel.
    #' @param contentType The content type of the messages of the new notification channel.
    #' @param createdAt The date of the notification channel creation.
    #' @param customHeaders Custom headers and their values to be sent in the new notification channel.
    #' @param emailAddress The email address to be used in the new notification channel.
    #' @param id The id of the notification channel.
    #' @param languageCode The preferred language code.
    #' @param name The name of the new notification channel.
    #' @param orgId The id of organization that notification channel belongs to.
    #' @param payloadUrl The payload URL of the new notification channel.
    #' @param secretToken Secret token to be used for new notification channel.
    #' @param uid The identifier of the user who created the channel.
    #' @param validateSsl Defines if  validate ssl or not in the notification channel.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`channelType` = NULL, `contentType` = NULL, `createdAt` = NULL, `customHeaders` = NULL, `emailAddress` = NULL, `id` = NULL, `languageCode` = NULL, `name` = NULL, `orgId` = NULL, `payloadUrl` = NULL, `secretToken` = NULL, `uid` = NULL, `validateSsl` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`channelType`, `contentType`, `createdAt`, `customHeaders`, `emailAddress`, `id`, `languageCode`, `name`, `orgId`, `payloadUrl`, `secretToken`, `uid`, `validateSsl`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(channelType, contentType, createdAt, customHeaders, emailAddress, id, languageCode, name, orgId, payloadUrl, secretToken, uid, validateSsl)
      }
      self$`channelType` <- `channelType`
      self$`contentType` <- `contentType`
      self$`createdAt` <- `createdAt`
      sapply(`customHeaders`, function(x) stopifnot(R6::is.R6(x)))
      self$`emailAddress` <- `emailAddress`
      self$`id` <- `id`
      self$`languageCode` <- `languageCode`
      self$`name` <- `name`
      self$`orgId` <- `orgId`
      self$`payloadUrl` <- `payloadUrl`
      self$`secretToken` <- `secretToken`
      self$`uid` <- `uid`
      self$`validateSsl` <- `validateSsl`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(channelType = self$`channelType`, contentType = self$`contentType`, createdAt = self$`createdAt`, customHeaders = self$`customHeaders`, emailAddress = self$`emailAddress`, id = self$`id`, languageCode = self$`languageCode`, name = self$`name`, orgId = self$`orgId`, payloadUrl = self$`payloadUrl`, secretToken = self$`secretToken`, uid = self$`uid`, validateSsl = self$`validateSsl`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`channelType`)) {
          sprintf(
            '"channelType":
            "%s"
                  ',
            self$`channelType`
          )
        },
        if (!is.null(self$`contentType`)) {
          sprintf(
            '"contentType":
            "%s"
                  ',
            self$`contentType`
          )
        },
        if (!is.null(self$`createdAt`)) {
          sprintf(
            '"createdAt":
            "%s"
                  ',
            format(self$`createdAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`customHeaders`)) {
          sprintf(
            '"customHeaders":
            [%s]
      ',
            paste(sapply(self$`customHeaders`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`emailAddress`)) {
          sprintf(
            '"emailAddress":
            "%s"
                  ',
            self$`emailAddress`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`languageCode`)) {
          sprintf(
            '"languageCode":
            "%s"
                  ',
            self$`languageCode`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        },
        if (!is.null(self$`payloadUrl`)) {
          sprintf(
            '"payloadUrl":
            "%s"
                  ',
            self$`payloadUrl`
          )
        },
        if (!is.null(self$`secretToken`)) {
          sprintf(
            '"secretToken":
            "%s"
                  ',
            self$`secretToken`
          )
        },
        if (!is.null(self$`uid`)) {
          sprintf(
            '"uid":
            "%s"
                  ',
            self$`uid`
          )
        },
        if (!is.null(self$`validateSsl`)) {
          sprintf(
            '"validateSsl":
            %s
                  ',
            tolower(self$`validateSsl`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotificationChannelJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotificationChannelJson, validateParams = FALSE) {
      NotificationChannelObject <- jsonlite::fromJSON(NotificationChannelJson)
      self$`channelType` <- NotificationChannelObject$`channelType`
      self$`contentType` <- NotificationChannelObject$`contentType`
      self$`createdAt` <- ParseRFC3339Timestamp(NotificationChannelObject$`createdAt`)
      self$`customHeaders` <- ApiClient$new()$deserializeObj(NotificationChannelObject$`customHeaders`, "array[CustomerHeader]", loadNamespace("datarobot.apicore"))
      self$`emailAddress` <- NotificationChannelObject$`emailAddress`
      self$`id` <- NotificationChannelObject$`id`
      self$`languageCode` <- NotificationChannelObject$`languageCode`
      self$`name` <- NotificationChannelObject$`name`
      self$`orgId` <- NotificationChannelObject$`orgId`
      self$`payloadUrl` <- NotificationChannelObject$`payloadUrl`
      self$`secretToken` <- NotificationChannelObject$`secretToken`
      self$`uid` <- NotificationChannelObject$`uid`
      self$`validateSsl` <- NotificationChannelObject$`validateSsl`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
