# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UsageDataExport
#'
#' @description UsageDataExport Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field end  character [optional] The upper bound of stored events timestamp to include within the artifact.
#'
#' @field include  list( character ) [optional] Additional fields to be included.
#'
#' @field includeIdentifyingFields  character Indicates if identifying information like user names, project names, etc. should be included or not. Defaults to True.
#'
#' @field includeReport  list( character ) [optional] The list of reports that should be generated. Will default to &#x60;None&#x60; if not specified.
#'
#' @field noCache  character [optional] Switches off caching.
#'
#' @field projectId  character [optional] Only actions that are connected with the project will be retrieved.
#'
#' @field start  character [optional] The lower bound of stored events timestamp to include within the artifact.
#'
#' @field userId  character [optional] Only actions performed by this user will be retrieved.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UsageDataExport <- R6::R6Class(
  "UsageDataExport",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`end` = NULL, `include` = NULL, `includeIdentifyingFields` = NULL, `includeReport` = NULL, `noCache` = NULL, `projectId` = NULL, `start` = NULL, `userId` = NULL) {
      if (!is.null(`includeIdentifyingFields`)) {
        stopifnot(is.logical(`includeIdentifyingFields`), length(`includeIdentifyingFields`) == 1)
      }
      if (!is.null(`end`)) {
        stopifnot(inherits(`end`, "POSIXt"))
      }
      if (!is.null(`include`)) {
        stopifnot(is.vector(`include`))
      }
      if (!is.null(`includeReport`)) {
        stopifnot(is.vector(`includeReport`))
      }
      if (!is.null(`noCache`)) {
        stopifnot(is.logical(`noCache`), length(`noCache`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`start`)) {
        stopifnot(inherits(`start`, "POSIXt"))
      }
      if (!is.null(`userId`)) {
        stopifnot(is.character(`userId`), length(`userId`) == 1)
      }
    }
  ),
  public = list(
    `end` = NULL,
    `include` = NULL,
    `includeIdentifyingFields` = NULL,
    `includeReport` = NULL,
    `noCache` = NULL,
    `projectId` = NULL,
    `start` = NULL,
    `userId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param end The upper bound of stored events timestamp to include within the artifact.
    #' @param include Additional fields to be included.
    #' @param includeIdentifyingFields Indicates if identifying information like user names, project names, etc. should be included or not. Defaults to True.
    #' @param includeReport The list of reports that should be generated. Will default to &#x60;None&#x60; if not specified.
    #' @param noCache Switches off caching.
    #' @param projectId Only actions that are connected with the project will be retrieved.
    #' @param start The lower bound of stored events timestamp to include within the artifact.
    #' @param userId Only actions performed by this user will be retrieved.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`includeIdentifyingFields` = NULL, `end` = NULL, `include` = NULL, `includeReport` = NULL, `noCache` = NULL, `projectId` = NULL, `start` = NULL, `userId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`includeIdentifyingFields`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(end, include, includeIdentifyingFields, includeReport, noCache, projectId, start, userId)
      }
      self$`end` <- `end`
      sapply(`include`, function(x) stopifnot(is.character(x)))
      self$`includeIdentifyingFields` <- `includeIdentifyingFields`
      sapply(`includeReport`, function(x) stopifnot(is.character(x)))
      self$`noCache` <- `noCache`
      self$`projectId` <- `projectId`
      self$`start` <- `start`
      self$`userId` <- `userId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(end = self$`end`, include = self$`include`, includeIdentifyingFields = self$`includeIdentifyingFields`, includeReport = self$`includeReport`, noCache = self$`noCache`, projectId = self$`projectId`, start = self$`start`, userId = self$`userId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`end`)) {
          sprintf(
            '"end":
            "%s"
                  ',
            format(self$`end`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`include`)) {
          sprintf(
            '"include":
            [%s]
                  ',
            paste(unlist(lapply(self$`include`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`includeIdentifyingFields`)) {
          sprintf(
            '"includeIdentifyingFields":
            %s
                  ',
            tolower(self$`includeIdentifyingFields`)
          )
        },
        if (!is.null(self$`includeReport`)) {
          sprintf(
            '"includeReport":
            [%s]
                  ',
            paste(unlist(lapply(self$`includeReport`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`noCache`)) {
          sprintf(
            '"noCache":
            %s
                  ',
            tolower(self$`noCache`)
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`start`)) {
          sprintf(
            '"start":
            "%s"
                  ',
            format(self$`start`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`userId`)) {
          sprintf(
            '"userId":
            "%s"
                  ',
            self$`userId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UsageDataExportJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UsageDataExportJson, validateParams = FALSE) {
      UsageDataExportObject <- jsonlite::fromJSON(UsageDataExportJson)
      self$`end` <- ParseRFC3339Timestamp(UsageDataExportObject$`end`)
      self$`include` <- ApiClient$new()$deserializeObj(UsageDataExportObject$`include`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`includeIdentifyingFields` <- UsageDataExportObject$`includeIdentifyingFields`
      self$`includeReport` <- ApiClient$new()$deserializeObj(UsageDataExportObject$`includeReport`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`noCache` <- UsageDataExportObject$`noCache`
      self$`projectId` <- UsageDataExportObject$`projectId`
      self$`start` <- ParseRFC3339Timestamp(UsageDataExportObject$`start`)
      self$`userId` <- UsageDataExportObject$`userId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
