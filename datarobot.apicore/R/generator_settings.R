# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title GeneratorSettings
#'
#' @description GeneratorSettings Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datetimePartitionColumn  character The date column that will be used as a datetime partition column in time series project.
#'
#' @field defaultCategoricalAggregationMethod  character Default aggregation method used for categorical feature.
#'
#' @field defaultNumericAggregationMethod  character Default aggregation method used for numeric feature.
#'
#' @field defaultTextAggregationMethod  character [optional] Default aggregation method used for text feature.
#'
#' @field endToSeriesMaxDatetime  character [optional] A boolean value indicating whether generates post-aggregated series up to series maximum datetime or global maximum datetime.
#'
#' @field multiseriesIdColumns  list( character ) [optional] An array with the names of columns identifying the series to which row of the output dataset belongs. Currently, only one multiseries ID column is supported.
#'
#' @field startFromSeriesMinDatetime  character [optional] A boolean value indicating whether post-aggregated series starts from series minimum datetime or global minimum datetime.
#'
#' @field target  character [optional] The name of target for the output dataset.
#'
#' @field timeStep  integer Number of time steps for the output dataset.
#'
#' @field timeUnit  character Indicates which unit is a basis for time steps of the output dataset.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GeneratorSettings <- R6::R6Class(
  "GeneratorSettings",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datetimePartitionColumn` = NULL, `defaultCategoricalAggregationMethod` = NULL, `defaultNumericAggregationMethod` = NULL, `defaultTextAggregationMethod` = NULL, `endToSeriesMaxDatetime` = NULL, `multiseriesIdColumns` = NULL, `startFromSeriesMinDatetime` = NULL, `target` = NULL, `timeStep` = NULL, `timeUnit` = NULL) {
      if (!is.null(`datetimePartitionColumn`)) {
        stopifnot(is.character(`datetimePartitionColumn`), length(`datetimePartitionColumn`) == 1)
      }
      if (!is.null(`defaultCategoricalAggregationMethod`)) {
        stopifnot(is.character(`defaultCategoricalAggregationMethod`), length(`defaultCategoricalAggregationMethod`) == 1)
      }
      if (!is.null(`defaultNumericAggregationMethod`)) {
        stopifnot(is.character(`defaultNumericAggregationMethod`), length(`defaultNumericAggregationMethod`) == 1)
      }
      if (!is.null(`timeStep`)) {
        stopifnot(is.numeric(`timeStep`), length(`timeStep`) == 1)
      }
      if (!is.null(`timeUnit`)) {
        stopifnot(is.character(`timeUnit`), length(`timeUnit`) == 1)
      }
      if (!is.null(`defaultTextAggregationMethod`)) {
        stopifnot(is.character(`defaultTextAggregationMethod`), length(`defaultTextAggregationMethod`) == 1)
      }
      if (!is.null(`endToSeriesMaxDatetime`)) {
        stopifnot(is.logical(`endToSeriesMaxDatetime`), length(`endToSeriesMaxDatetime`) == 1)
      }
      if (!is.null(`multiseriesIdColumns`) && length(`multiseriesIdColumns`) > 0) {
        stopifnot(is.vector(`multiseriesIdColumns`), sapply(`multiseriesIdColumns`, is.character))
      }
      if (!is.null(`startFromSeriesMinDatetime`)) {
        stopifnot(is.logical(`startFromSeriesMinDatetime`), length(`startFromSeriesMinDatetime`) == 1)
      }
      if (!is.null(`target`)) {
        stopifnot(is.character(`target`), length(`target`) == 1)
      }
    }
  ),
  public = list(
    `datetimePartitionColumn` = NULL,
    `defaultCategoricalAggregationMethod` = NULL,
    `defaultNumericAggregationMethod` = NULL,
    `defaultTextAggregationMethod` = NULL,
    `endToSeriesMaxDatetime` = NULL,
    `multiseriesIdColumns` = NULL,
    `startFromSeriesMinDatetime` = NULL,
    `target` = NULL,
    `timeStep` = NULL,
    `timeUnit` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datetimePartitionColumn The date column that will be used as a datetime partition column in time series project.
    #' @param defaultCategoricalAggregationMethod Default aggregation method used for categorical feature.
    #' @param defaultNumericAggregationMethod Default aggregation method used for numeric feature.
    #' @param defaultTextAggregationMethod Default aggregation method used for text feature.
    #' @param endToSeriesMaxDatetime A boolean value indicating whether generates post-aggregated series up to series maximum datetime or global maximum datetime.
    #' @param multiseriesIdColumns An array with the names of columns identifying the series to which row of the output dataset belongs. Currently, only one multiseries ID column is supported.
    #' @param startFromSeriesMinDatetime A boolean value indicating whether post-aggregated series starts from series minimum datetime or global minimum datetime.
    #' @param target The name of target for the output dataset.
    #' @param timeStep Number of time steps for the output dataset.
    #' @param timeUnit Indicates which unit is a basis for time steps of the output dataset.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datetimePartitionColumn` = NULL, `defaultCategoricalAggregationMethod` = NULL, `defaultNumericAggregationMethod` = NULL, `timeStep` = NULL, `timeUnit` = NULL, `defaultTextAggregationMethod` = NULL, `endToSeriesMaxDatetime` = TRUE, `multiseriesIdColumns` = NULL, `startFromSeriesMinDatetime` = TRUE, `target` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datetimePartitionColumn`, `defaultCategoricalAggregationMethod`, `defaultNumericAggregationMethod`, `timeStep`, `timeUnit`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datetimePartitionColumn, defaultCategoricalAggregationMethod, defaultNumericAggregationMethod, defaultTextAggregationMethod, endToSeriesMaxDatetime, multiseriesIdColumns, startFromSeriesMinDatetime, target, timeStep, timeUnit)
      }
      self$`datetimePartitionColumn` <- `datetimePartitionColumn`
      self$`defaultCategoricalAggregationMethod` <- `defaultCategoricalAggregationMethod`
      self$`defaultNumericAggregationMethod` <- `defaultNumericAggregationMethod`
      self$`defaultTextAggregationMethod` <- `defaultTextAggregationMethod`
      self$`endToSeriesMaxDatetime` <- `endToSeriesMaxDatetime`
      self$`multiseriesIdColumns` <- `multiseriesIdColumns`
      self$`startFromSeriesMinDatetime` <- `startFromSeriesMinDatetime`
      self$`target` <- `target`
      self$`timeStep` <- `timeStep`
      self$`timeUnit` <- `timeUnit`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datetimePartitionColumn = self$`datetimePartitionColumn`, defaultCategoricalAggregationMethod = self$`defaultCategoricalAggregationMethod`, defaultNumericAggregationMethod = self$`defaultNumericAggregationMethod`, defaultTextAggregationMethod = self$`defaultTextAggregationMethod`, endToSeriesMaxDatetime = self$`endToSeriesMaxDatetime`, multiseriesIdColumns = self$`multiseriesIdColumns`, startFromSeriesMinDatetime = self$`startFromSeriesMinDatetime`, target = self$`target`, timeStep = self$`timeStep`, timeUnit = self$`timeUnit`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datetimePartitionColumn`)) {
          sprintf(
            '"datetimePartitionColumn":
            "%s"
                  ',
            self$`datetimePartitionColumn`
          )
        },
        if (!is.null(self$`defaultCategoricalAggregationMethod`)) {
          sprintf(
            '"defaultCategoricalAggregationMethod":
            "%s"
                  ',
            self$`defaultCategoricalAggregationMethod`
          )
        },
        if (!is.null(self$`defaultNumericAggregationMethod`)) {
          sprintf(
            '"defaultNumericAggregationMethod":
            "%s"
                  ',
            self$`defaultNumericAggregationMethod`
          )
        },
        if (!is.null(self$`defaultTextAggregationMethod`)) {
          sprintf(
            '"defaultTextAggregationMethod":
            "%s"
                  ',
            self$`defaultTextAggregationMethod`
          )
        },
        if (!is.null(self$`endToSeriesMaxDatetime`)) {
          sprintf(
            '"endToSeriesMaxDatetime":
            %s
                  ',
            tolower(self$`endToSeriesMaxDatetime`)
          )
        },
        if (!is.null(self$`multiseriesIdColumns`)) {
          sprintf(
            '"multiseriesIdColumns":
            [%s]
                  ',
            paste(unlist(lapply(self$`multiseriesIdColumns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`startFromSeriesMinDatetime`)) {
          sprintf(
            '"startFromSeriesMinDatetime":
            %s
                  ',
            tolower(self$`startFromSeriesMinDatetime`)
          )
        },
        if (!is.null(self$`target`)) {
          sprintf(
            '"target":
            "%s"
                  ',
            self$`target`
          )
        },
        if (!is.null(self$`timeStep`)) {
          sprintf(
            '"timeStep":
            %d
                  ',
            self$`timeStep`
          )
        },
        if (!is.null(self$`timeUnit`)) {
          sprintf(
            '"timeUnit":
            "%s"
                  ',
            self$`timeUnit`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param GeneratorSettingsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(GeneratorSettingsJson, validateParams = FALSE) {
      GeneratorSettingsObject <- jsonlite::fromJSON(GeneratorSettingsJson)
      self$`datetimePartitionColumn` <- GeneratorSettingsObject$`datetimePartitionColumn`
      self$`defaultCategoricalAggregationMethod` <- GeneratorSettingsObject$`defaultCategoricalAggregationMethod`
      self$`defaultNumericAggregationMethod` <- GeneratorSettingsObject$`defaultNumericAggregationMethod`
      self$`defaultTextAggregationMethod` <- GeneratorSettingsObject$`defaultTextAggregationMethod`
      self$`endToSeriesMaxDatetime` <- GeneratorSettingsObject$`endToSeriesMaxDatetime`
      self$`multiseriesIdColumns` <- ApiClient$new()$deserializeObj(GeneratorSettingsObject$`multiseriesIdColumns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`startFromSeriesMinDatetime` <- GeneratorSettingsObject$`startFromSeriesMinDatetime`
      self$`target` <- GeneratorSettingsObject$`target`
      self$`timeStep` <- GeneratorSettingsObject$`timeStep`
      self$`timeUnit` <- GeneratorSettingsObject$`timeUnit`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
