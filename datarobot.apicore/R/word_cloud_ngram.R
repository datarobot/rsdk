# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title WordCloudNgram
#'
#' @description WordCloudNgram Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field class  character [optional] For classification - values of the target class for corresponding word or ngram. For regression - null.
#'
#' @field coefficient  numeric Describes effect of this ngram on the target. A large negative value means a strong effect toward the negative class in classification projects and a smaller predicted target value in regression projects. A large positive value means a strong effect toward the positive class and a larger predicted target value respectively.
#'
#' @field count  integer Number of rows in the training sample where this ngram appears.
#'
#' @field frequency  numeric Frequency of this ngram relative to the most common ngram.
#'
#' @field isStopword  character True for ngrams that DataRobot evaluates as stopwords.
#'
#' @field ngram  character Word or ngram value.
#'
#' @field variable  character String representation of the ngram source - contains column name and, for some models, preprocessing details. E.g. NGRAM_OCCUR_L2_colname will be for ngram occurrences count using L2 normalization from the colname column.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
WordCloudNgram <- R6::R6Class(
  "WordCloudNgram",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`class` = NULL, `coefficient` = NULL, `count` = NULL, `frequency` = NULL, `isStopword` = NULL, `ngram` = NULL, `variable` = NULL) {
      if (!is.null(`coefficient`)) {
      }
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`frequency`)) {
      }
      if (!is.null(`isStopword`)) {
        stopifnot(is.logical(`isStopword`), length(`isStopword`) == 1)
      }
      if (!is.null(`ngram`)) {
        stopifnot(is.character(`ngram`), length(`ngram`) == 1)
      }
      if (!is.null(`variable`)) {
        stopifnot(is.character(`variable`), length(`variable`) == 1)
      }
      if (!is.null(`class`)) {
        stopifnot(is.character(`class`), length(`class`) == 1)
      }
    }
  ),
  public = list(
    `class` = NULL,
    `coefficient` = NULL,
    `count` = NULL,
    `frequency` = NULL,
    `isStopword` = NULL,
    `ngram` = NULL,
    `variable` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param class For classification - values of the target class for corresponding word or ngram. For regression - null.
    #' @param coefficient Describes effect of this ngram on the target. A large negative value means a strong effect toward the negative class in classification projects and a smaller predicted target value in regression projects. A large positive value means a strong effect toward the positive class and a larger predicted target value respectively.
    #' @param count Number of rows in the training sample where this ngram appears.
    #' @param frequency Frequency of this ngram relative to the most common ngram.
    #' @param isStopword True for ngrams that DataRobot evaluates as stopwords.
    #' @param ngram Word or ngram value.
    #' @param variable String representation of the ngram source - contains column name and, for some models, preprocessing details. E.g. NGRAM_OCCUR_L2_colname will be for ngram occurrences count using L2 normalization from the colname column.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`coefficient` = NULL, `count` = NULL, `frequency` = NULL, `isStopword` = NULL, `ngram` = NULL, `variable` = NULL, `class` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`coefficient`, `count`, `frequency`, `isStopword`, `ngram`, `variable`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(class, coefficient, count, frequency, isStopword, ngram, variable)
      }
      self$`class` <- `class`
      self$`coefficient` <- `coefficient`
      self$`count` <- `count`
      self$`frequency` <- `frequency`
      self$`isStopword` <- `isStopword`
      self$`ngram` <- `ngram`
      self$`variable` <- `variable`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(class = self$`class`, coefficient = self$`coefficient`, count = self$`count`, frequency = self$`frequency`, isStopword = self$`isStopword`, ngram = self$`ngram`, variable = self$`variable`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`class`)) {
          sprintf(
            '"class":
            "%s"
                  ',
            self$`class`
          )
        },
        if (!is.null(self$`coefficient`)) {
          sprintf(
            '"coefficient":
            %d
                  ',
            self$`coefficient`
          )
        },
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`frequency`)) {
          sprintf(
            '"frequency":
            %d
                  ',
            self$`frequency`
          )
        },
        if (!is.null(self$`isStopword`)) {
          sprintf(
            '"isStopword":
            %s
                  ',
            tolower(self$`isStopword`)
          )
        },
        if (!is.null(self$`ngram`)) {
          sprintf(
            '"ngram":
            "%s"
                  ',
            self$`ngram`
          )
        },
        if (!is.null(self$`variable`)) {
          sprintf(
            '"variable":
            "%s"
                  ',
            self$`variable`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param WordCloudNgramJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(WordCloudNgramJson, validateParams = FALSE) {
      WordCloudNgramObject <- jsonlite::fromJSON(WordCloudNgramJson)
      self$`class` <- WordCloudNgramObject$`class`
      self$`coefficient` <- WordCloudNgramObject$`coefficient`
      self$`count` <- WordCloudNgramObject$`count`
      self$`frequency` <- WordCloudNgramObject$`frequency`
      self$`isStopword` <- WordCloudNgramObject$`isStopword`
      self$`ngram` <- WordCloudNgramObject$`ngram`
      self$`variable` <- WordCloudNgramObject$`variable`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
