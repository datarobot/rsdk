# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintCreate
#'
#' @description UserBlueprintCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blueprint  \link{OneOfarrayobject} The representation of a directed acyclic graph defining a pipeline of data through tasks and a final estimator.
#'
#' @field decompressedBlueprint  character Whether to retrieve the blueprint in the decompressed format.
#'
#' @field description  character [optional] The description to give to the blueprint.
#'
#' @field getDynamicLabels  character [optional] Whether to add dynamic labels to a decompressed blueprint.
#'
#' @field isInplaceEditor  character Whether the request is sent from the in place user BP editor.
#'
#' @field modelType  character [optional] The title to give to the blueprint.
#'
#' @field projectId  character [optional] String representation of ObjectId for the currently active project. The user blueprint is created when this project is active. Necessary in the event of project specific tasks, such as column selection tasks.
#'
#' @field saveToCatalog  character Whether to save the blueprint to the catalog.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintCreate <- R6::R6Class(
  "UserBlueprintCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(blueprint = NULL, decompressedBlueprint = NULL, description = NULL, getDynamicLabels = NULL, isInplaceEditor = NULL, modelType = NULL, projectId = NULL, saveToCatalog = NULL) {
      if (!is.null(`blueprint`)) {
        .setPrimitiveProperty(typeList = list("array", "character"), propertyData = blueprint)
      }
      if (!is.null(`decompressedBlueprint`)) {
        stopifnot(is.logical(`decompressedBlueprint`), length(`decompressedBlueprint`) == 1)
      }
      if (!is.null(`isInplaceEditor`)) {
        stopifnot(is.logical(`isInplaceEditor`), length(`isInplaceEditor`) == 1)
      }
      if (!is.null(`saveToCatalog`)) {
        stopifnot(is.logical(`saveToCatalog`), length(`saveToCatalog`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`getDynamicLabels`)) {
        stopifnot(is.logical(`getDynamicLabels`), length(`getDynamicLabels`) == 1)
      }
      if (!is.null(`modelType`)) {
        stopifnot(is.character(`modelType`), length(`modelType`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `blueprint` = NULL,
    `decompressedBlueprint` = NULL,
    `description` = NULL,
    `getDynamicLabels` = NULL,
    `isInplaceEditor` = NULL,
    `modelType` = NULL,
    `projectId` = NULL,
    `saveToCatalog` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blueprint The representation of a directed acyclic graph defining a pipeline of data through tasks and a final estimator.
    #' @param decompressedBlueprint Whether to retrieve the blueprint in the decompressed format.
    #' @param description The description to give to the blueprint.
    #' @param getDynamicLabels Whether to add dynamic labels to a decompressed blueprint.
    #' @param isInplaceEditor Whether the request is sent from the in place user BP editor.
    #' @param modelType The title to give to the blueprint.
    #' @param projectId String representation of ObjectId for the currently active project. The user blueprint is created when this project is active. Necessary in the event of project specific tasks, such as column selection tasks.
    #' @param saveToCatalog Whether to save the blueprint to the catalog.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blueprint` = NULL, `decompressedBlueprint` = NULL, `isInplaceEditor` = NULL, `saveToCatalog` = NULL, `description` = NULL, `getDynamicLabels` = FALSE, `modelType` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`blueprint`, `decompressedBlueprint`, `isInplaceEditor`, `saveToCatalog`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(blueprint, decompressedBlueprint, description, getDynamicLabels, isInplaceEditor, modelType, projectId, saveToCatalog)
      }
      self$`blueprint` <- .setPrimitiveProperty(typeList = list("array", "character"), propertyData = blueprint)
      self$`decompressedBlueprint` <- `decompressedBlueprint`
      self$`description` <- `description`
      self$`getDynamicLabels` <- `getDynamicLabels`
      self$`isInplaceEditor` <- `isInplaceEditor`
      self$`modelType` <- `modelType`
      self$`projectId` <- `projectId`
      self$`saveToCatalog` <- `saveToCatalog`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(blueprint = self$`blueprint`, decompressedBlueprint = self$`decompressedBlueprint`, description = self$`description`, getDynamicLabels = self$`getDynamicLabels`, isInplaceEditor = self$`isInplaceEditor`, modelType = self$`modelType`, projectId = self$`projectId`, saveToCatalog = self$`saveToCatalog`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blueprint`)) {
          sprintf(
            '"blueprint":
          %s
        ',
            self$`blueprint`
          )
        },
        if (!is.null(self$`decompressedBlueprint`)) {
          sprintf(
            '"decompressedBlueprint":
            %s
                  ',
            tolower(self$`decompressedBlueprint`)
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`getDynamicLabels`)) {
          sprintf(
            '"getDynamicLabels":
            %s
                  ',
            tolower(self$`getDynamicLabels`)
          )
        },
        if (!is.null(self$`isInplaceEditor`)) {
          sprintf(
            '"isInplaceEditor":
            %s
                  ',
            tolower(self$`isInplaceEditor`)
          )
        },
        if (!is.null(self$`modelType`)) {
          sprintf(
            '"modelType":
            "%s"
                  ',
            self$`modelType`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`saveToCatalog`)) {
          sprintf(
            '"saveToCatalog":
            %s
                  ',
            tolower(self$`saveToCatalog`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintCreateJson, validateParams = FALSE) {
      UserBlueprintCreateObject <- jsonlite::fromJSON(UserBlueprintCreateJson)
      self$`blueprint` <- .setPrimitiveProperty(typeList = list("array", "character"), propertyData = UserBlueprintCreateObject$blueprint)
      self$`decompressedBlueprint` <- UserBlueprintCreateObject$`decompressedBlueprint`
      self$`description` <- UserBlueprintCreateObject$`description`
      self$`getDynamicLabels` <- UserBlueprintCreateObject$`getDynamicLabels`
      self$`isInplaceEditor` <- UserBlueprintCreateObject$`isInplaceEditor`
      self$`modelType` <- UserBlueprintCreateObject$`modelType`
      self$`projectId` <- UserBlueprintCreateObject$`projectId`
      self$`saveToCatalog` <- UserBlueprintCreateObject$`saveToCatalog`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
