# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AutopilotOptions
#'
#' @description AutopilotOptions Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blendBestModels  character [optional] Blend best models during Autopilot run. This option is not supported in SHAP-only mode.
#'
#' @field mode  character [optional] The autopilot mode.
#'
#' @field runLeakageRemovedFeatureList  character [optional] Run Autopilot on Leakage Removed feature list (if exists).
#'
#' @field scoringCodeOnly  character [optional] Keep only models that can be converted to scorable java code during Autopilot run.
#'
#' @field shapOnlyMode  character [optional] Include only models with SHAP value support.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AutopilotOptions <- R6::R6Class(
  "AutopilotOptions",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`blendBestModels` = NULL, `mode` = NULL, `runLeakageRemovedFeatureList` = NULL, `scoringCodeOnly` = NULL, `shapOnlyMode` = NULL) {
      if (!is.null(`blendBestModels`)) {
        stopifnot(is.logical(`blendBestModels`), length(`blendBestModels`) == 1)
      }
      if (!is.null(`mode`)) {
        stopifnot(is.character(`mode`), length(`mode`) == 1)
      }
      if (!is.null(`runLeakageRemovedFeatureList`)) {
        stopifnot(is.logical(`runLeakageRemovedFeatureList`), length(`runLeakageRemovedFeatureList`) == 1)
      }
      if (!is.null(`scoringCodeOnly`)) {
        stopifnot(is.logical(`scoringCodeOnly`), length(`scoringCodeOnly`) == 1)
      }
      if (!is.null(`shapOnlyMode`)) {
        stopifnot(is.logical(`shapOnlyMode`), length(`shapOnlyMode`) == 1)
      }
    }
  ),
  public = list(
    `blendBestModels` = NULL,
    `mode` = NULL,
    `runLeakageRemovedFeatureList` = NULL,
    `scoringCodeOnly` = NULL,
    `shapOnlyMode` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blendBestModels Blend best models during Autopilot run. This option is not supported in SHAP-only mode.
    #' @param mode The autopilot mode.
    #' @param runLeakageRemovedFeatureList Run Autopilot on Leakage Removed feature list (if exists).
    #' @param scoringCodeOnly Keep only models that can be converted to scorable java code during Autopilot run.
    #' @param shapOnlyMode Include only models with SHAP value support.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blendBestModels` = NULL, `mode` = NULL, `runLeakageRemovedFeatureList` = NULL, `scoringCodeOnly` = NULL, `shapOnlyMode` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(blendBestModels, mode, runLeakageRemovedFeatureList, scoringCodeOnly, shapOnlyMode)
      }
      self$`blendBestModels` <- `blendBestModels`
      self$`mode` <- `mode`
      self$`runLeakageRemovedFeatureList` <- `runLeakageRemovedFeatureList`
      self$`scoringCodeOnly` <- `scoringCodeOnly`
      self$`shapOnlyMode` <- `shapOnlyMode`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(blendBestModels = self$`blendBestModels`, mode = self$`mode`, runLeakageRemovedFeatureList = self$`runLeakageRemovedFeatureList`, scoringCodeOnly = self$`scoringCodeOnly`, shapOnlyMode = self$`shapOnlyMode`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blendBestModels`)) {
          sprintf(
            '"blendBestModels":
            %s
                  ',
            tolower(self$`blendBestModels`)
          )
        },
        if (!is.null(self$`mode`)) {
          sprintf(
            '"mode":
            "%s"
                  ',
            self$`mode`
          )
        },
        if (!is.null(self$`runLeakageRemovedFeatureList`)) {
          sprintf(
            '"runLeakageRemovedFeatureList":
            %s
                  ',
            tolower(self$`runLeakageRemovedFeatureList`)
          )
        },
        if (!is.null(self$`scoringCodeOnly`)) {
          sprintf(
            '"scoringCodeOnly":
            %s
                  ',
            tolower(self$`scoringCodeOnly`)
          )
        },
        if (!is.null(self$`shapOnlyMode`)) {
          sprintf(
            '"shapOnlyMode":
            %s
                  ',
            tolower(self$`shapOnlyMode`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AutopilotOptionsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AutopilotOptionsJson, validateParams = FALSE) {
      AutopilotOptionsObject <- jsonlite::fromJSON(AutopilotOptionsJson)
      self$`blendBestModels` <- AutopilotOptionsObject$`blendBestModels`
      self$`mode` <- AutopilotOptionsObject$`mode`
      self$`runLeakageRemovedFeatureList` <- AutopilotOptionsObject$`runLeakageRemovedFeatureList`
      self$`scoringCodeOnly` <- AutopilotOptionsObject$`scoringCodeOnly`
      self$`shapOnlyMode` <- AutopilotOptionsObject$`shapOnlyMode`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
