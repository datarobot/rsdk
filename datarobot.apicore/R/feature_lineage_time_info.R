# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureLineageTimeInfo
#'
#' @description FeatureLineageTimeInfo Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field duration  \link{TimeDelta}
#'
#' @field latest  \link{TimeDelta}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureLineageTimeInfo <- R6::R6Class(
  "FeatureLineageTimeInfo",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(duration = NULL, latest = NULL) {
      if (!is.null(`duration`)) {
        stopifnot(R6::is.R6(`duration`))
      }
      if (!is.null(`latest`)) {
        stopifnot(R6::is.R6(`latest`))
      }
    }
  ),
  public = list(
    `duration` = NULL,
    `latest` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param duration
    #' @param latest
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`duration` = NULL, `latest` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`duration`, `latest`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(duration, latest)
      }
      self$`duration` <- `duration`
      self$`latest` <- `latest`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(duration = self$`duration`, latest = self$`latest`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`duration`)) {
          sprintf(
            '"duration":
          %s
        ',
            jsonlite::toJSON(self$`duration`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`latest`)) {
          sprintf(
            '"latest":
          %s
        ',
            jsonlite::toJSON(self$`latest`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureLineageTimeInfoJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureLineageTimeInfoJson, validateParams = FALSE) {
      FeatureLineageTimeInfoObject <- jsonlite::fromJSON(FeatureLineageTimeInfoJson)
      self$`duration` <- TimeDelta$new()$fromJSON(jsonlite::toJSON(FeatureLineageTimeInfoObject$duration, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`latest` <- TimeDelta$new()$fromJSON(jsonlite::toJSON(FeatureLineageTimeInfoObject$latest, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
