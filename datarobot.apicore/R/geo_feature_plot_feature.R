# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title GeoFeaturePlotFeature
#'
#' @description GeoFeaturePlotFeature Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field geometry  \link{GeoJSON}
#'
#' @field properties  \link{GeoFeaturePlotFeatureProperties}
#'
#' @field type  character With a fixed value of &#39;Feature&#39;.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GeoFeaturePlotFeature <- R6::R6Class(
  "GeoFeaturePlotFeature",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(geometry = NULL, properties = NULL, type = NULL) {
      if (!is.null(`geometry`)) {
        stopifnot(R6::is.R6(`geometry`))
      }
      if (!is.null(`properties`)) {
        stopifnot(R6::is.R6(`properties`))
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
    }
  ),
  public = list(
    `geometry` = NULL,
    `properties` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param geometry
    #' @param properties
    #' @param type With a fixed value of &#39;Feature&#39;.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`geometry` = NULL, `properties` = NULL, `type` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`geometry`, `properties`, `type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(geometry, properties, type)
      }
      self$`geometry` <- `geometry`
      self$`properties` <- `properties`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(geometry = self$`geometry`, properties = self$`properties`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`geometry`)) {
          sprintf(
            '"geometry":
          %s
        ',
            jsonlite::toJSON(self$`geometry`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`properties`)) {
          sprintf(
            '"properties":
          %s
        ',
            jsonlite::toJSON(self$`properties`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param GeoFeaturePlotFeatureJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(GeoFeaturePlotFeatureJson, validateParams = FALSE) {
      GeoFeaturePlotFeatureObject <- jsonlite::fromJSON(GeoFeaturePlotFeatureJson)
      self$`geometry` <- GeoJSON$new()$fromJSON(jsonlite::toJSON(GeoFeaturePlotFeatureObject$geometry, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`properties` <- GeoFeaturePlotFeatureProperties$new()$fromJSON(jsonlite::toJSON(GeoFeaturePlotFeatureObject$properties, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`type` <- GeoFeaturePlotFeatureObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
