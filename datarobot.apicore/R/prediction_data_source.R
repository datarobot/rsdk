# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionDataSource
#'
#' @description PredictionDataSource Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualValueColumn  character [optional] The actual value column name, valid for the prediction files if the project is unsupervised and the dataset is considered as bulk predictions dataset.
#'
#' @field credentialData  \link{OneOfBasicCredentialsS3CredentialsOAuthCredentials} [optional] The credentials to authenticate with the database, to use instead of user/password or credential ID.
#'
#' @field credentialId  character [optional] The credential ID to use for database authentication.
#'
#' @field credentials  list( \link{OneOfPasswordCredentialsCredentialId} ) [optional] A list of credentials for the secondary datasets used in feature discovery project.
#'
#' @field dataSourceId  character The ID of &#x60;&#x60;DataSource&#x60;&#x60;.
#'
#' @field forecastPoint  character [optional] For time series projects only. The time in the dataset relative to which predictions are generated. This value is optional. If not specified the default value is the value in the row with the latest specified timestamp. Specifying this value for a project that is not a time series project will result in an error.
#'
#' @field password  character [optional] The password (in cleartext) for database authentication. The password will be encrypted on the server side in scope of HTTP request and never saved or stored. DEPRECATED: please use &#x60;&#x60;credentialId&#x60;&#x60; or &#x60;&#x60;credentialData&#x60;&#x60; instead.
#'
#' @field predictionsEndDate  character [optional] The end date for bulk predictions, exclusive. Used for time series projects only. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsStartDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
#'
#' @field predictionsStartDate  character [optional] The start date for bulk predictions. Used for time series projects only. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsEndDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
#'
#' @field relaxKnownInAdvanceFeaturesCheck  character [optional] For time series projects only. If true, missing values in the known in advance features are allowed in the forecast window at the prediction time. This value is optional. If omitted or false, missing values are not allowed.
#'
#' @field secondaryDatasetsConfigId  character [optional] For feature discovery projects only. The ID of the alternative secondary dataset config to use during prediction.
#'
#' @field useKerberos  character [optional] If true, use kerberos authentication for database authentication. Default is false.
#'
#' @field user  character [optional] The username for database authentication. DEPRECATED: please use &#x60;&#x60;credentialId&#x60;&#x60; or &#x60;&#x60;credentialData&#x60;&#x60; instead.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionDataSource <- R6::R6Class(
  "PredictionDataSource",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`actualValueColumn` = NULL, `credentialData` = NULL, `credentialId` = NULL, `credentials` = NULL, `dataSourceId` = NULL, `forecastPoint` = NULL, `password` = NULL, `predictionsEndDate` = NULL, `predictionsStartDate` = NULL, `relaxKnownInAdvanceFeaturesCheck` = NULL, `secondaryDatasetsConfigId` = NULL, `useKerberos` = NULL, `user` = NULL) {
      if (!is.null(`dataSourceId`)) {
        stopifnot(is.character(`dataSourceId`), length(`dataSourceId`) == 1)
      }
      if (!is.null(`actualValueColumn`)) {
        stopifnot(is.character(`actualValueColumn`), length(`actualValueColumn`) == 1)
      }
      if (!is.null(`credentialData`)) {
        .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`credentials`)) {
        .setComplexProperty(typeList = list(PasswordCredentials, CredentialId), propertyData = credentials)
      }
      if (!is.null(`forecastPoint`)) {
        stopifnot(inherits(`forecastPoint`, "POSIXt"))
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`predictionsEndDate`)) {
        stopifnot(inherits(`predictionsEndDate`, "POSIXt"))
      }
      if (!is.null(`predictionsStartDate`)) {
        stopifnot(inherits(`predictionsStartDate`, "POSIXt"))
      }
      if (!is.null(`relaxKnownInAdvanceFeaturesCheck`)) {
        stopifnot(is.logical(`relaxKnownInAdvanceFeaturesCheck`), length(`relaxKnownInAdvanceFeaturesCheck`) == 1)
      }
      if (!is.null(`secondaryDatasetsConfigId`)) {
        stopifnot(is.character(`secondaryDatasetsConfigId`), length(`secondaryDatasetsConfigId`) == 1)
      }
      if (!is.null(`useKerberos`)) {
        stopifnot(is.logical(`useKerberos`), length(`useKerberos`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `actualValueColumn` = NULL,
    `credentialData` = NULL,
    `credentialId` = NULL,
    `credentials` = NULL,
    `dataSourceId` = NULL,
    `forecastPoint` = NULL,
    `password` = NULL,
    `predictionsEndDate` = NULL,
    `predictionsStartDate` = NULL,
    `relaxKnownInAdvanceFeaturesCheck` = NULL,
    `secondaryDatasetsConfigId` = NULL,
    `useKerberos` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualValueColumn The actual value column name, valid for the prediction files if the project is unsupervised and the dataset is considered as bulk predictions dataset.
    #' @param credentialData The credentials to authenticate with the database, to use instead of user/password or credential ID.
    #' @param credentialId The credential ID to use for database authentication.
    #' @param credentials A list of credentials for the secondary datasets used in feature discovery project.
    #' @param dataSourceId The ID of &#x60;&#x60;DataSource&#x60;&#x60;.
    #' @param forecastPoint For time series projects only. The time in the dataset relative to which predictions are generated. This value is optional. If not specified the default value is the value in the row with the latest specified timestamp. Specifying this value for a project that is not a time series project will result in an error.
    #' @param password The password (in cleartext) for database authentication. The password will be encrypted on the server side in scope of HTTP request and never saved or stored. DEPRECATED: please use &#x60;&#x60;credentialId&#x60;&#x60; or &#x60;&#x60;credentialData&#x60;&#x60; instead.
    #' @param predictionsEndDate The end date for bulk predictions, exclusive. Used for time series projects only. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsStartDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
    #' @param predictionsStartDate The start date for bulk predictions. Used for time series projects only. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsEndDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
    #' @param relaxKnownInAdvanceFeaturesCheck For time series projects only. If true, missing values in the known in advance features are allowed in the forecast window at the prediction time. This value is optional. If omitted or false, missing values are not allowed.
    #' @param secondaryDatasetsConfigId For feature discovery projects only. The ID of the alternative secondary dataset config to use during prediction.
    #' @param useKerberos If true, use kerberos authentication for database authentication. Default is false.
    #' @param user The username for database authentication. DEPRECATED: please use &#x60;&#x60;credentialId&#x60;&#x60; or &#x60;&#x60;credentialData&#x60;&#x60; instead.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataSourceId` = NULL, `actualValueColumn` = NULL, `credentialData` = NULL, `credentialId` = NULL, `credentials` = NULL, `forecastPoint` = NULL, `password` = NULL, `predictionsEndDate` = NULL, `predictionsStartDate` = NULL, `relaxKnownInAdvanceFeaturesCheck` = NULL, `secondaryDatasetsConfigId` = NULL, `useKerberos` = FALSE, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`dataSourceId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(actualValueColumn, credentialData, credentialId, credentials, dataSourceId, forecastPoint, password, predictionsEndDate, predictionsStartDate, relaxKnownInAdvanceFeaturesCheck, secondaryDatasetsConfigId, useKerberos, user)
      }
      self$`actualValueColumn` <- `actualValueColumn`
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      self$`credentialId` <- `credentialId`
      self$`credentials` <- sapply(`credentials`, function(item) .setComplexProperty(typeList = list(PasswordCredentials, CredentialId), propertyData = item))
      self$`dataSourceId` <- `dataSourceId`
      self$`forecastPoint` <- `forecastPoint`
      self$`password` <- `password`
      self$`predictionsEndDate` <- `predictionsEndDate`
      self$`predictionsStartDate` <- `predictionsStartDate`
      self$`relaxKnownInAdvanceFeaturesCheck` <- `relaxKnownInAdvanceFeaturesCheck`
      self$`secondaryDatasetsConfigId` <- `secondaryDatasetsConfigId`
      self$`useKerberos` <- `useKerberos`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(actualValueColumn = self$`actualValueColumn`, credentialData = self$`credentialData`, credentialId = self$`credentialId`, credentials = self$`credentials`, dataSourceId = self$`dataSourceId`, forecastPoint = self$`forecastPoint`, password = self$`password`, predictionsEndDate = self$`predictionsEndDate`, predictionsStartDate = self$`predictionsStartDate`, relaxKnownInAdvanceFeaturesCheck = self$`relaxKnownInAdvanceFeaturesCheck`, secondaryDatasetsConfigId = self$`secondaryDatasetsConfigId`, useKerberos = self$`useKerberos`, user = self$`user`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualValueColumn`)) {
          sprintf(
            '"actualValueColumn":
            "%s"
                  ',
            self$`actualValueColumn`
          )
        },
        if (!is.null(self$`credentialData`)) {
          sprintf(
            '"credentialData":
            %s
      ',
            jsonlite::toJSON(self$`credentialData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`credentials`)) {
          sprintf(
            '"credentials":
            [%s]
      ',
            paste(sapply(self$`credentials`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`dataSourceId`)) {
          sprintf(
            '"dataSourceId":
            "%s"
                  ',
            self$`dataSourceId`
          )
        },
        if (!is.null(self$`forecastPoint`)) {
          sprintf(
            '"forecastPoint":
            "%s"
                  ',
            format(self$`forecastPoint`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`predictionsEndDate`)) {
          sprintf(
            '"predictionsEndDate":
            "%s"
                  ',
            format(self$`predictionsEndDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`predictionsStartDate`)) {
          sprintf(
            '"predictionsStartDate":
            "%s"
                  ',
            format(self$`predictionsStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`relaxKnownInAdvanceFeaturesCheck`)) {
          sprintf(
            '"relaxKnownInAdvanceFeaturesCheck":
            %s
                  ',
            tolower(self$`relaxKnownInAdvanceFeaturesCheck`)
          )
        },
        if (!is.null(self$`secondaryDatasetsConfigId`)) {
          sprintf(
            '"secondaryDatasetsConfigId":
            "%s"
                  ',
            self$`secondaryDatasetsConfigId`
          )
        },
        if (!is.null(self$`useKerberos`)) {
          sprintf(
            '"useKerberos":
            %s
                  ',
            tolower(self$`useKerberos`)
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionDataSourceJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionDataSourceJson, validateParams = FALSE) {
      PredictionDataSourceObject <- jsonlite::fromJSON(PredictionDataSourceJson)
      self$`actualValueColumn` <- PredictionDataSourceObject$`actualValueColumn`
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = PredictionDataSourceObject$credentialData)
      self$`credentialId` <- PredictionDataSourceObject$`credentialId`
      self$`credentials` <- ApiClient$new()$deserializeObj(PredictionDataSourceObject$`credentials`, "array[OneOfPasswordCredentialsCredentialId]", loadNamespace("datarobot.apicore"))
      self$`dataSourceId` <- PredictionDataSourceObject$`dataSourceId`
      self$`forecastPoint` <- ParseRFC3339Timestamp(PredictionDataSourceObject$`forecastPoint`)
      self$`password` <- PredictionDataSourceObject$`password`
      self$`predictionsEndDate` <- ParseRFC3339Timestamp(PredictionDataSourceObject$`predictionsEndDate`)
      self$`predictionsStartDate` <- ParseRFC3339Timestamp(PredictionDataSourceObject$`predictionsStartDate`)
      self$`relaxKnownInAdvanceFeaturesCheck` <- PredictionDataSourceObject$`relaxKnownInAdvanceFeaturesCheck`
      self$`secondaryDatasetsConfigId` <- PredictionDataSourceObject$`secondaryDatasetsConfigId`
      self$`useKerberos` <- PredictionDataSourceObject$`useKerberos`
      self$`user` <- PredictionDataSourceObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
