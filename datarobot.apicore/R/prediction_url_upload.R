# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionURLUpload
#'
#' @description PredictionURLUpload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualValueColumn  character [optional] Actual value column name, valid for the prediction files if the project is unsupervised and the dataset is considered as bulk predictions dataset. This value is optional.
#'
#' @field credentials  list( \link{OneOfPasswordCredentialsCredentialId} ) [optional] A list of credentials for the secondary datasets used in feature discovery project
#'
#' @field forecastPoint  character [optional] For time series projects only. The time in the dataset relative to which predictions are generated. If not specified the default value is the value in the row with the latest specified timestamp. Specifying this value for a project that is not a time series project will result in an error.
#'
#' @field predictionsEndDate  character [optional] Used for time series projects only. The end date for bulk predictions, exclusive. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsStartDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
#'
#' @field predictionsStartDate  character [optional] Used for time series projects only. The start date for bulk predictions. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsEndDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
#'
#' @field relaxKnownInAdvanceFeaturesCheck  character [optional] For time series projects only. If true, missing values in the known in advance features are allowed in the forecast window at the prediction time. This value is optional. If omitted or false, missing values are not allowed.
#'
#' @field secondaryDatasetsConfigId  character [optional] For feature discovery projects only. The ID of the alternative secondary dataset config to use during prediction.
#'
#' @field url  character The URL to download the dataset from.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionURLUpload <- R6::R6Class(
  "PredictionURLUpload",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`actualValueColumn` = NULL, `credentials` = NULL, `forecastPoint` = NULL, `predictionsEndDate` = NULL, `predictionsStartDate` = NULL, `relaxKnownInAdvanceFeaturesCheck` = NULL, `secondaryDatasetsConfigId` = NULL, `url` = NULL) {
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
      if (!is.null(`actualValueColumn`)) {
        stopifnot(is.character(`actualValueColumn`), length(`actualValueColumn`) == 1)
      }
      if (!is.null(`credentials`)) {
        .setComplexProperty(typeList = list(PasswordCredentials, CredentialId), propertyData = credentials)
      }
      if (!is.null(`forecastPoint`)) {
        stopifnot(inherits(`forecastPoint`, "POSIXt"))
      }
      if (!is.null(`predictionsEndDate`)) {
        stopifnot(inherits(`predictionsEndDate`, "POSIXt"))
      }
      if (!is.null(`predictionsStartDate`)) {
        stopifnot(inherits(`predictionsStartDate`, "POSIXt"))
      }
      if (!is.null(`relaxKnownInAdvanceFeaturesCheck`)) {
        stopifnot(is.logical(`relaxKnownInAdvanceFeaturesCheck`), length(`relaxKnownInAdvanceFeaturesCheck`) == 1)
      }
      if (!is.null(`secondaryDatasetsConfigId`)) {
        stopifnot(is.character(`secondaryDatasetsConfigId`), length(`secondaryDatasetsConfigId`) == 1)
      }
    }
  ),
  public = list(
    `actualValueColumn` = NULL,
    `credentials` = NULL,
    `forecastPoint` = NULL,
    `predictionsEndDate` = NULL,
    `predictionsStartDate` = NULL,
    `relaxKnownInAdvanceFeaturesCheck` = NULL,
    `secondaryDatasetsConfigId` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualValueColumn Actual value column name, valid for the prediction files if the project is unsupervised and the dataset is considered as bulk predictions dataset. This value is optional.
    #' @param credentials A list of credentials for the secondary datasets used in feature discovery project
    #' @param forecastPoint For time series projects only. The time in the dataset relative to which predictions are generated. If not specified the default value is the value in the row with the latest specified timestamp. Specifying this value for a project that is not a time series project will result in an error.
    #' @param predictionsEndDate Used for time series projects only. The end date for bulk predictions, exclusive. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsStartDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
    #' @param predictionsStartDate Used for time series projects only. The start date for bulk predictions. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsEndDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
    #' @param relaxKnownInAdvanceFeaturesCheck For time series projects only. If true, missing values in the known in advance features are allowed in the forecast window at the prediction time. This value is optional. If omitted or false, missing values are not allowed.
    #' @param secondaryDatasetsConfigId For feature discovery projects only. The ID of the alternative secondary dataset config to use during prediction.
    #' @param url The URL to download the dataset from.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`url` = NULL, `actualValueColumn` = NULL, `credentials` = NULL, `forecastPoint` = NULL, `predictionsEndDate` = NULL, `predictionsStartDate` = NULL, `relaxKnownInAdvanceFeaturesCheck` = NULL, `secondaryDatasetsConfigId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`url`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(actualValueColumn, credentials, forecastPoint, predictionsEndDate, predictionsStartDate, relaxKnownInAdvanceFeaturesCheck, secondaryDatasetsConfigId, url)
      }
      self$`actualValueColumn` <- `actualValueColumn`
      self$`credentials` <- sapply(`credentials`, function(item) .setComplexProperty(typeList = list(PasswordCredentials, CredentialId), propertyData = item))
      self$`forecastPoint` <- `forecastPoint`
      self$`predictionsEndDate` <- `predictionsEndDate`
      self$`predictionsStartDate` <- `predictionsStartDate`
      self$`relaxKnownInAdvanceFeaturesCheck` <- `relaxKnownInAdvanceFeaturesCheck`
      self$`secondaryDatasetsConfigId` <- `secondaryDatasetsConfigId`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(actualValueColumn = self$`actualValueColumn`, credentials = self$`credentials`, forecastPoint = self$`forecastPoint`, predictionsEndDate = self$`predictionsEndDate`, predictionsStartDate = self$`predictionsStartDate`, relaxKnownInAdvanceFeaturesCheck = self$`relaxKnownInAdvanceFeaturesCheck`, secondaryDatasetsConfigId = self$`secondaryDatasetsConfigId`, url = self$`url`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualValueColumn`)) {
          sprintf(
            '"actualValueColumn":
            "%s"
                  ',
            self$`actualValueColumn`
          )
        },
        if (!is.null(self$`credentials`)) {
          sprintf(
            '"credentials":
            [%s]
      ',
            paste(sapply(self$`credentials`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`forecastPoint`)) {
          sprintf(
            '"forecastPoint":
            "%s"
                  ',
            format(self$`forecastPoint`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`predictionsEndDate`)) {
          sprintf(
            '"predictionsEndDate":
            "%s"
                  ',
            format(self$`predictionsEndDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`predictionsStartDate`)) {
          sprintf(
            '"predictionsStartDate":
            "%s"
                  ',
            format(self$`predictionsStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`relaxKnownInAdvanceFeaturesCheck`)) {
          sprintf(
            '"relaxKnownInAdvanceFeaturesCheck":
            %s
                  ',
            tolower(self$`relaxKnownInAdvanceFeaturesCheck`)
          )
        },
        if (!is.null(self$`secondaryDatasetsConfigId`)) {
          sprintf(
            '"secondaryDatasetsConfigId":
            "%s"
                  ',
            self$`secondaryDatasetsConfigId`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionURLUploadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionURLUploadJson, validateParams = FALSE) {
      PredictionURLUploadObject <- jsonlite::fromJSON(PredictionURLUploadJson)
      self$`actualValueColumn` <- PredictionURLUploadObject$`actualValueColumn`
      self$`credentials` <- ApiClient$new()$deserializeObj(PredictionURLUploadObject$`credentials`, "array[OneOfPasswordCredentialsCredentialId]", loadNamespace("datarobot.apicore"))
      self$`forecastPoint` <- ParseRFC3339Timestamp(PredictionURLUploadObject$`forecastPoint`)
      self$`predictionsEndDate` <- ParseRFC3339Timestamp(PredictionURLUploadObject$`predictionsEndDate`)
      self$`predictionsStartDate` <- ParseRFC3339Timestamp(PredictionURLUploadObject$`predictionsStartDate`)
      self$`relaxKnownInAdvanceFeaturesCheck` <- PredictionURLUploadObject$`relaxKnownInAdvanceFeaturesCheck`
      self$`secondaryDatasetsConfigId` <- PredictionURLUploadObject$`secondaryDatasetsConfigId`
      self$`url` <- PredictionURLUploadObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
