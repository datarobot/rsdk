# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseValueTemplateClassificationData
#'
#' @description UseCaseValueTemplateClassificationData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accuracyImprovement  numeric Accuracy improvement
#'
#' @field decisionsCount  integer Estimated number of decisions per year
#'
#' @field incorrectDecisionCost  numeric Estimated cost of an individual incorrect decision
#'
#' @field incorrectDecisionsCount  integer Estimated number of incorrect decisions per year
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseValueTemplateClassificationData <- R6::R6Class(
  "UseCaseValueTemplateClassificationData",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(accuracyImprovement = NULL, decisionsCount = NULL, incorrectDecisionCost = NULL, incorrectDecisionsCount = NULL) {
      if (!is.null(`accuracyImprovement`)) {
      }
      if (!is.null(`decisionsCount`)) {
        stopifnot(is.numeric(`decisionsCount`), length(`decisionsCount`) == 1)
      }
      if (!is.null(`incorrectDecisionCost`)) {
      }
      if (!is.null(`incorrectDecisionsCount`)) {
        stopifnot(is.numeric(`incorrectDecisionsCount`), length(`incorrectDecisionsCount`) == 1)
      }
    }
  ),
  public = list(
    `accuracyImprovement` = NULL,
    `decisionsCount` = NULL,
    `incorrectDecisionCost` = NULL,
    `incorrectDecisionsCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accuracyImprovement Accuracy improvement
    #' @param decisionsCount Estimated number of decisions per year
    #' @param incorrectDecisionCost Estimated cost of an individual incorrect decision
    #' @param incorrectDecisionsCount Estimated number of incorrect decisions per year
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`accuracyImprovement` = NULL, `decisionsCount` = NULL, `incorrectDecisionCost` = NULL, `incorrectDecisionsCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`accuracyImprovement`, `decisionsCount`, `incorrectDecisionCost`, `incorrectDecisionsCount`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(accuracyImprovement, decisionsCount, incorrectDecisionCost, incorrectDecisionsCount)
      }
      self$`accuracyImprovement` <- `accuracyImprovement`
      self$`decisionsCount` <- `decisionsCount`
      self$`incorrectDecisionCost` <- `incorrectDecisionCost`
      self$`incorrectDecisionsCount` <- `incorrectDecisionsCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(accuracyImprovement = self$`accuracyImprovement`, decisionsCount = self$`decisionsCount`, incorrectDecisionCost = self$`incorrectDecisionCost`, incorrectDecisionsCount = self$`incorrectDecisionsCount`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accuracyImprovement`)) {
          sprintf(
            '"accuracyImprovement":
            %d
                  ',
            self$`accuracyImprovement`
          )
        },
        if (!is.null(self$`decisionsCount`)) {
          sprintf(
            '"decisionsCount":
            %d
                  ',
            self$`decisionsCount`
          )
        },
        if (!is.null(self$`incorrectDecisionCost`)) {
          sprintf(
            '"incorrectDecisionCost":
            %d
                  ',
            self$`incorrectDecisionCost`
          )
        },
        if (!is.null(self$`incorrectDecisionsCount`)) {
          sprintf(
            '"incorrectDecisionsCount":
            %d
                  ',
            self$`incorrectDecisionsCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseValueTemplateClassificationDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseValueTemplateClassificationDataJson, validateParams = FALSE) {
      UseCaseValueTemplateClassificationDataObject <- jsonlite::fromJSON(UseCaseValueTemplateClassificationDataJson)
      self$`accuracyImprovement` <- UseCaseValueTemplateClassificationDataObject$`accuracyImprovement`
      self$`decisionsCount` <- UseCaseValueTemplateClassificationDataObject$`decisionsCount`
      self$`incorrectDecisionCost` <- UseCaseValueTemplateClassificationDataObject$`incorrectDecisionCost`
      self$`incorrectDecisionsCount` <- UseCaseValueTemplateClassificationDataObject$`incorrectDecisionsCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
