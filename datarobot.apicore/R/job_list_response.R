# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title JobListResponse
#'
#' @description JobListResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer the number of jobs returned.
#'
#' @field jobs  list( \link{JobDetailsResponse} ) A json array of jobs
#'
#' @field next_  character URL pointing to the next page (if null, there is no next page).
#'
#' @field previous  character URL pointing to the previous page (if null, there is no previous page).
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
JobListResponse <- R6::R6Class(
  "JobListResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `jobs` = NULL, `next_` = NULL, `previous` = NULL) {
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`jobs`)) {
        stopifnot(is.vector(`jobs`))
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `jobs` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count the number of jobs returned.
    #' @param jobs A json array of jobs
    #' @param next_ URL pointing to the next page (if null, there is no next page).
    #' @param previous URL pointing to the previous page (if null, there is no previous page).
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `jobs` = NULL, `next_` = NULL, `previous` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`count`, `jobs`, `next_`, `previous`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, jobs, next_, previous)
      }
      self$`count` <- `count`
      sapply(`jobs`, function(x) stopifnot(R6::is.R6(x)))
      self$`next_` <- `next_`
      self$`previous` <- `previous`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, jobs = self$`jobs`, next_ = self$`next_`, previous = self$`previous`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`jobs`)) {
          sprintf(
            '"jobs":
            [%s]
      ',
            paste(sapply(self$`jobs`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param JobListResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(JobListResponseJson, validateParams = FALSE) {
      JobListResponseObject <- jsonlite::fromJSON(JobListResponseJson)
      self$`count` <- JobListResponseObject$`count`
      self$`jobs` <- ApiClient$new()$deserializeObj(JobListResponseObject$`jobs`, "array[JobDetailsResponse]", loadNamespace("datarobot.apicore"))
      self$`next_` <- JobListResponseObject$`next`
      self$`previous` <- JobListResponseObject$`previous`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
