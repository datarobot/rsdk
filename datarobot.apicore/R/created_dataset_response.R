# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CreatedDatasetResponse
#'
#' @description CreatedDatasetResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalogId  character The ID of the catalog entry.
#'
#' @field catalogVersionId  character The ID of the latest version of the catalog entry.
#'
#' @field statusId  character ID that can be used with :http:get:&#x60;/api/v2/status/(statusId)/&#x60; to poll for the testing job&#39;s status.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CreatedDatasetResponse <- R6::R6Class(
  "CreatedDatasetResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`catalogId` = NULL, `catalogVersionId` = NULL, `statusId` = NULL) {
      if (!is.null(`catalogId`)) {
        stopifnot(is.character(`catalogId`), length(`catalogId`) == 1)
      }
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`statusId`)) {
        stopifnot(is.character(`statusId`), length(`statusId`) == 1)
      }
    }
  ),
  public = list(
    `catalogId` = NULL,
    `catalogVersionId` = NULL,
    `statusId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalogId The ID of the catalog entry.
    #' @param catalogVersionId The ID of the latest version of the catalog entry.
    #' @param statusId ID that can be used with :http:get:&#x60;/api/v2/status/(statusId)/&#x60; to poll for the testing job&#39;s status.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`catalogId` = NULL, `catalogVersionId` = NULL, `statusId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`catalogId`, `catalogVersionId`, `statusId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(catalogId, catalogVersionId, statusId)
      }
      self$`catalogId` <- `catalogId`
      self$`catalogVersionId` <- `catalogVersionId`
      self$`statusId` <- `statusId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(catalogId = self$`catalogId`, catalogVersionId = self$`catalogVersionId`, statusId = self$`statusId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalogId`)) {
          sprintf(
            '"catalogId":
            "%s"
                  ',
            self$`catalogId`
          )
        },
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`statusId`)) {
          sprintf(
            '"statusId":
            "%s"
                  ',
            self$`statusId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CreatedDatasetResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CreatedDatasetResponseJson, validateParams = FALSE) {
      CreatedDatasetResponseObject <- jsonlite::fromJSON(CreatedDatasetResponseJson)
      self$`catalogId` <- CreatedDatasetResponseObject$`catalogId`
      self$`catalogVersionId` <- CreatedDatasetResponseObject$`catalogVersionId`
      self$`statusId` <- CreatedDatasetResponseObject$`statusId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
