# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ShapWarnings
#'
#' @description ShapWarnings Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field maxNormalizedMismatch  numeric The maximal relative normalized mismatch value.
#'
#' @field mismatchRowCount  integer The count of rows for which additivity check failed.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShapWarnings <- R6::R6Class(
  "ShapWarnings",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(maxNormalizedMismatch = NULL, mismatchRowCount = NULL) {
      if (!is.null(`maxNormalizedMismatch`)) {
      }
      if (!is.null(`mismatchRowCount`)) {
        stopifnot(is.numeric(`mismatchRowCount`), length(`mismatchRowCount`) == 1)
      }
    }
  ),
  public = list(
    `maxNormalizedMismatch` = NULL,
    `mismatchRowCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param maxNormalizedMismatch The maximal relative normalized mismatch value.
    #' @param mismatchRowCount The count of rows for which additivity check failed.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`maxNormalizedMismatch` = NULL, `mismatchRowCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`maxNormalizedMismatch`, `mismatchRowCount`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(maxNormalizedMismatch, mismatchRowCount)
      }
      self$`maxNormalizedMismatch` <- `maxNormalizedMismatch`
      self$`mismatchRowCount` <- `mismatchRowCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(maxNormalizedMismatch = self$`maxNormalizedMismatch`, mismatchRowCount = self$`mismatchRowCount`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`maxNormalizedMismatch`)) {
          sprintf(
            '"maxNormalizedMismatch":
            %d
                  ',
            self$`maxNormalizedMismatch`
          )
        },
        if (!is.null(self$`mismatchRowCount`)) {
          sprintf(
            '"mismatchRowCount":
            %d
                  ',
            self$`mismatchRowCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ShapWarningsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ShapWarningsJson, validateParams = FALSE) {
      ShapWarningsObject <- jsonlite::fromJSON(ShapWarningsJson)
      self$`maxNormalizedMismatch` <- ShapWarningsObject$`maxNormalizedMismatch`
      self$`mismatchRowCount` <- ShapWarningsObject$`mismatchRowCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
