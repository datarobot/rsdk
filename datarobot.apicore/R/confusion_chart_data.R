# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ConfusionChartData
#'
#' @description ConfusionChartData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field classMetrics  list( \link{ConfusionChartClassMatrix} ) per-class information including one vs all scores in a format specified below
#'
#' @field classes  list( character ) class labels from the dataset, union of row classes &amp; column classes. This field is deprecated as of v2.13. The rows and columns may have different class labels when using query parameters to retrieve a slice of the matrix; please use &#39;rowClasses&#39; and &#39;colClasses&#39; instead.
#'
#' @field colClasses  list( character ) class labels on columns of confusion matrix
#'
#' @field confusionMatrix  list( \link{array[integer]} ) 2d array of ints representing confusion matrix, aligned with &#x60;rowClasses&#x60; and &#39;colClasses&#39;array. For confusionMatrix[A][B] we can get an integer that represents the number of times &#39;if class with index A was correct we have class with index B predicted&#39; (if the orientation is &#39;actual&#39;).
#'
#' @field rowClasses  list( character ) class labels on rows of confusion matrix
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ConfusionChartData <- R6::R6Class(
  "ConfusionChartData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`classMetrics` = NULL, `classes` = NULL, `colClasses` = NULL, `confusionMatrix` = NULL, `rowClasses` = NULL) {
      if (!is.null(`classMetrics`)) {
        stopifnot(is.vector(`classMetrics`))
      }
      if (!is.null(`classes`)) {
        stopifnot(is.vector(`classes`))
      }
      if (!is.null(`colClasses`)) {
        stopifnot(is.vector(`colClasses`))
      }
      if (!is.null(`confusionMatrix`)) {
        stopifnot(is.vector(`confusionMatrix`))
      }
      if (!is.null(`rowClasses`)) {
        stopifnot(is.vector(`rowClasses`))
      }
    }
  ),
  public = list(
    `classMetrics` = NULL,
    `classes` = NULL,
    `colClasses` = NULL,
    `confusionMatrix` = NULL,
    `rowClasses` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param classMetrics per-class information including one vs all scores in a format specified below
    #' @param classes class labels from the dataset, union of row classes &amp; column classes. This field is deprecated as of v2.13. The rows and columns may have different class labels when using query parameters to retrieve a slice of the matrix; please use &#39;rowClasses&#39; and &#39;colClasses&#39; instead.
    #' @param colClasses class labels on columns of confusion matrix
    #' @param confusionMatrix 2d array of ints representing confusion matrix, aligned with &#x60;rowClasses&#x60; and &#39;colClasses&#39;array. For confusionMatrix[A][B] we can get an integer that represents the number of times &#39;if class with index A was correct we have class with index B predicted&#39; (if the orientation is &#39;actual&#39;).
    #' @param rowClasses class labels on rows of confusion matrix
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`classMetrics` = NULL, `classes` = NULL, `colClasses` = NULL, `confusionMatrix` = NULL, `rowClasses` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`classMetrics`, `classes`, `colClasses`, `confusionMatrix`, `rowClasses`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(classMetrics, classes, colClasses, confusionMatrix, rowClasses)
      }
      sapply(`classMetrics`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`classes`, function(x) stopifnot(is.character(x)))
      sapply(`colClasses`, function(x) stopifnot(is.character(x)))
      sapply(`confusionMatrix`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`rowClasses`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(classMetrics = self$`classMetrics`, classes = self$`classes`, colClasses = self$`colClasses`, confusionMatrix = self$`confusionMatrix`, rowClasses = self$`rowClasses`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`classMetrics`)) {
          sprintf(
            '"classMetrics":
            [%s]
      ',
            paste(sapply(self$`classMetrics`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`classes`)) {
          sprintf(
            '"classes":
            [%s]
                  ',
            paste(unlist(lapply(self$`classes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`colClasses`)) {
          sprintf(
            '"colClasses":
            [%s]
                  ',
            paste(unlist(lapply(self$`colClasses`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`confusionMatrix`)) {
          sprintf(
            '"confusionMatrix":
            [%s]
      ',
            paste(sapply(self$`confusionMatrix`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`rowClasses`)) {
          sprintf(
            '"rowClasses":
            [%s]
                  ',
            paste(unlist(lapply(self$`rowClasses`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ConfusionChartDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ConfusionChartDataJson, validateParams = FALSE) {
      ConfusionChartDataObject <- jsonlite::fromJSON(ConfusionChartDataJson)
      self$`classMetrics` <- ApiClient$new()$deserializeObj(ConfusionChartDataObject$`classMetrics`, "array[ConfusionChartClassMatrix]", loadNamespace("datarobot.apicore"))
      self$`classes` <- ApiClient$new()$deserializeObj(ConfusionChartDataObject$`classes`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`colClasses` <- ApiClient$new()$deserializeObj(ConfusionChartDataObject$`colClasses`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`confusionMatrix` <- ApiClient$new()$deserializeObj(ConfusionChartDataObject$`confusionMatrix`, "array[array[integer]]", loadNamespace("datarobot.apicore"))
      self$`rowClasses` <- ApiClient$new()$deserializeObj(ConfusionChartDataObject$`rowClasses`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
