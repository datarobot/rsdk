# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ClusterNamesMappingValidation
#'
#' @description ClusterNamesMappingValidation Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field currentName  character Current cluster name.
#'
#' @field newName  character New cluster name.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ClusterNamesMappingValidation <- R6::R6Class(
  "ClusterNamesMappingValidation",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(currentName = NULL, newName = NULL) {
      if (!is.null(`currentName`)) {
        stopifnot(is.character(`currentName`), length(`currentName`) == 1)
      }
      if (!is.null(`newName`)) {
        stopifnot(is.character(`newName`), length(`newName`) == 1)
      }
    }
  ),
  public = list(
    `currentName` = NULL,
    `newName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param currentName Current cluster name.
    #' @param newName New cluster name.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`currentName` = NULL, `newName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`currentName`, `newName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(currentName, newName)
      }
      self$`currentName` <- `currentName`
      self$`newName` <- `newName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(currentName = self$`currentName`, newName = self$`newName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`currentName`)) {
          sprintf(
            '"currentName":
            "%s"
                  ',
            self$`currentName`
          )
        },
        if (!is.null(self$`newName`)) {
          sprintf(
            '"newName":
            "%s"
                  ',
            self$`newName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ClusterNamesMappingValidationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ClusterNamesMappingValidationJson, validateParams = FALSE) {
      ClusterNamesMappingValidationObject <- jsonlite::fromJSON(ClusterNamesMappingValidationJson)
      self$`currentName` <- ClusterNamesMappingValidationObject$`currentName`
      self$`newName` <- ClusterNamesMappingValidationObject$`newName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
