# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title VertexContextItem
#'
#' @description VertexContextItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field information  \link{OneOfVertexContextItemInfo} [optional] A specification of requirements of the inputs and expectations of the output of the vertex.
#'
#' @field messages  \link{OneOfVertexContextItemMessages} [optional] Warnings about and errors with a specific vertex in the blueprint.
#'
#' @field taskId  character The id associated with a specific vertex in the blueprint.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
VertexContextItem <- R6::R6Class(
  "VertexContextItem",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`information` = NULL, `messages` = NULL, `taskId` = NULL) {
      if (!is.null(`taskId`)) {
        stopifnot(is.character(`taskId`), length(`taskId`) == 1)
      }
      if (!is.null(`information`)) {
        .setComplexProperty(typeList = list(VertexContextItemInfo), propertyData = information)
      }
      if (!is.null(`messages`)) {
        .setComplexProperty(typeList = list(VertexContextItemMessages), propertyData = messages)
      }
    }
  ),
  public = list(
    `information` = NULL,
    `messages` = NULL,
    `taskId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param information A specification of requirements of the inputs and expectations of the output of the vertex.
    #' @param messages Warnings about and errors with a specific vertex in the blueprint.
    #' @param taskId The id associated with a specific vertex in the blueprint.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`taskId` = NULL, `information` = NULL, `messages` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`taskId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(information, messages, taskId)
      }
      self$`information` <- .setComplexProperty(typeList = list(VertexContextItemInfo), propertyData = information)
      self$`messages` <- .setComplexProperty(typeList = list(VertexContextItemMessages), propertyData = messages)
      self$`taskId` <- `taskId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(information = self$`information`, messages = self$`messages`, taskId = self$`taskId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`information`)) {
          sprintf(
            '"information":
            %s
      ',
            jsonlite::toJSON(self$`information`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`messages`)) {
          sprintf(
            '"messages":
            %s
      ',
            jsonlite::toJSON(self$`messages`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`taskId`)) {
          sprintf(
            '"taskId":
            "%s"
                  ',
            self$`taskId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param VertexContextItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(VertexContextItemJson, validateParams = FALSE) {
      VertexContextItemObject <- jsonlite::fromJSON(VertexContextItemJson)
      self$`information` <- .setComplexProperty(typeList = list(VertexContextItemInfo), propertyData = VertexContextItemObject$information)
      self$`messages` <- .setComplexProperty(typeList = list(VertexContextItemMessages), propertyData = VertexContextItemObject$messages)
      self$`taskId` <- VertexContextItemObject$`taskId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
