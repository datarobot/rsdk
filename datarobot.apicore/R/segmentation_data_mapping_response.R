# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SegmentationDataMappingResponse
#'
#' @description SegmentationDataMappingResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field segmentId  character The segment name associated with the multiseries ID column by the segmentation task
#'
#' @field seriesId  character The multiseries ID column used to identify series for segmentation
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SegmentationDataMappingResponse <- R6::R6Class(
  "SegmentationDataMappingResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`segmentId` = NULL, `seriesId` = NULL) {
      if (!is.null(`segmentId`)) {
        stopifnot(is.character(`segmentId`), length(`segmentId`) == 1)
      }
      if (!is.null(`seriesId`)) {
        stopifnot(is.character(`seriesId`), length(`seriesId`) == 1)
      }
    }
  ),
  public = list(
    `segmentId` = NULL,
    `seriesId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param segmentId The segment name associated with the multiseries ID column by the segmentation task
    #' @param seriesId The multiseries ID column used to identify series for segmentation
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`segmentId` = NULL, `seriesId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`segmentId`, `seriesId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(segmentId, seriesId)
      }
      self$`segmentId` <- `segmentId`
      self$`seriesId` <- `seriesId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(segmentId = self$`segmentId`, seriesId = self$`seriesId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`segmentId`)) {
          sprintf(
            '"segmentId":
            "%s"
                  ',
            self$`segmentId`
          )
        },
        if (!is.null(self$`seriesId`)) {
          sprintf(
            '"seriesId":
            "%s"
                  ',
            self$`seriesId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SegmentationDataMappingResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SegmentationDataMappingResponseJson, validateParams = FALSE) {
      SegmentationDataMappingResponseObject <- jsonlite::fromJSON(SegmentationDataMappingResponseJson)
      self$`segmentId` <- SegmentationDataMappingResponseObject$`segmentId`
      self$`seriesId` <- SegmentationDataMappingResponseObject$`seriesId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
