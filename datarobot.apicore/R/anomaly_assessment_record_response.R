# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AnomalyAssessmentRecordResponse
#'
#' @description AnomalyAssessmentRecordResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtest  \link{OneOfintegerstring} The backtest of the record.
#'
#' @field deleteLocation  character URL to delete anomaly assessment record.
#'
#' @field endDate  character ISO-formatted last timestamp in the subset. For example: &#x60;&#x60;2019-08-30T00:00:00.000000Z&#x60;&#x60;.
#'
#' @field latestExplanationsLocation  character URL to retrieve the latest predictions with the shap explanations.
#'
#' @field modelId  character The model ID of the record.
#'
#' @field predictionThreshold  numeric The threshold, all rows with anomaly scores greater or equal to it have Shapley explanations computed.
#'
#' @field previewLocation  character URL to retrieve predictions preview for the record.
#'
#' @field projectId  character The project ID of the record.
#'
#' @field recordId  character The ID of the anomaly assessment record.
#'
#' @field seriesId  character The series id of the record. Applicable in multiseries projects
#'
#' @field source  character The source of the record
#'
#' @field startDate  character ISO-formatted first timestamp in the subset. For example: &#x60;&#x60;2019-08-01T00:00:00.000000Z&#x60;&#x60;.
#'
#' @field status  character The status of the anomaly assessment record.
#'
#' @field statusDetails  character The status details.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AnomalyAssessmentRecordResponse <- R6::R6Class(
  "AnomalyAssessmentRecordResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`backtest` = NULL, `deleteLocation` = NULL, `endDate` = NULL, `latestExplanationsLocation` = NULL, `modelId` = NULL, `predictionThreshold` = NULL, `previewLocation` = NULL, `projectId` = NULL, `recordId` = NULL, `seriesId` = NULL, `source` = NULL, `startDate` = NULL, `status` = NULL, `statusDetails` = NULL) {
      if (!is.null(`backtest`)) {
        .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = backtest)
      }
      if (!is.null(`deleteLocation`)) {
        stopifnot(is.character(`deleteLocation`), length(`deleteLocation`) == 1)
      }
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`latestExplanationsLocation`)) {
        stopifnot(is.character(`latestExplanationsLocation`), length(`latestExplanationsLocation`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`predictionThreshold`)) {
      }
      if (!is.null(`previewLocation`)) {
        stopifnot(is.character(`previewLocation`), length(`previewLocation`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`recordId`)) {
        stopifnot(is.character(`recordId`), length(`recordId`) == 1)
      }
      if (!is.null(`seriesId`)) {
        stopifnot(is.character(`seriesId`), length(`seriesId`) == 1)
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
      if (!is.null(`statusDetails`)) {
        stopifnot(is.character(`statusDetails`), length(`statusDetails`) == 1)
      }
    }
  ),
  public = list(
    `backtest` = NULL,
    `deleteLocation` = NULL,
    `endDate` = NULL,
    `latestExplanationsLocation` = NULL,
    `modelId` = NULL,
    `predictionThreshold` = NULL,
    `previewLocation` = NULL,
    `projectId` = NULL,
    `recordId` = NULL,
    `seriesId` = NULL,
    `source` = NULL,
    `startDate` = NULL,
    `status` = NULL,
    `statusDetails` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtest The backtest of the record.
    #' @param deleteLocation URL to delete anomaly assessment record.
    #' @param endDate ISO-formatted last timestamp in the subset. For example: &#x60;&#x60;2019-08-30T00:00:00.000000Z&#x60;&#x60;.
    #' @param latestExplanationsLocation URL to retrieve the latest predictions with the shap explanations.
    #' @param modelId The model ID of the record.
    #' @param predictionThreshold The threshold, all rows with anomaly scores greater or equal to it have Shapley explanations computed.
    #' @param previewLocation URL to retrieve predictions preview for the record.
    #' @param projectId The project ID of the record.
    #' @param recordId The ID of the anomaly assessment record.
    #' @param seriesId The series id of the record. Applicable in multiseries projects
    #' @param source The source of the record
    #' @param startDate ISO-formatted first timestamp in the subset. For example: &#x60;&#x60;2019-08-01T00:00:00.000000Z&#x60;&#x60;.
    #' @param status The status of the anomaly assessment record.
    #' @param statusDetails The status details.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`backtest` = NULL, `deleteLocation` = NULL, `endDate` = NULL, `latestExplanationsLocation` = NULL, `modelId` = NULL, `predictionThreshold` = NULL, `previewLocation` = NULL, `projectId` = NULL, `recordId` = NULL, `seriesId` = NULL, `source` = NULL, `startDate` = NULL, `status` = NULL, `statusDetails` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`backtest`, `deleteLocation`, `endDate`, `latestExplanationsLocation`, `modelId`, `predictionThreshold`, `previewLocation`, `projectId`, `recordId`, `seriesId`, `source`, `startDate`, `status`, `statusDetails`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(backtest, deleteLocation, endDate, latestExplanationsLocation, modelId, predictionThreshold, previewLocation, projectId, recordId, seriesId, source, startDate, status, statusDetails)
      }
      self$`backtest` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = backtest)
      self$`deleteLocation` <- `deleteLocation`
      self$`endDate` <- `endDate`
      self$`latestExplanationsLocation` <- `latestExplanationsLocation`
      self$`modelId` <- `modelId`
      self$`predictionThreshold` <- `predictionThreshold`
      self$`previewLocation` <- `previewLocation`
      self$`projectId` <- `projectId`
      self$`recordId` <- `recordId`
      self$`seriesId` <- `seriesId`
      self$`source` <- `source`
      self$`startDate` <- `startDate`
      self$`status` <- `status`
      self$`statusDetails` <- `statusDetails`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(backtest = self$`backtest`, deleteLocation = self$`deleteLocation`, endDate = self$`endDate`, latestExplanationsLocation = self$`latestExplanationsLocation`, modelId = self$`modelId`, predictionThreshold = self$`predictionThreshold`, previewLocation = self$`previewLocation`, projectId = self$`projectId`, recordId = self$`recordId`, seriesId = self$`seriesId`, source = self$`source`, startDate = self$`startDate`, status = self$`status`, statusDetails = self$`statusDetails`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtest`)) {
          sprintf(
            '"backtest":
            %s
      ',
            self$`backtest`
          )
        },
        if (!is.null(self$`deleteLocation`)) {
          sprintf(
            '"deleteLocation":
            "%s"
                  ',
            self$`deleteLocation`
          )
        },
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`latestExplanationsLocation`)) {
          sprintf(
            '"latestExplanationsLocation":
            "%s"
                  ',
            self$`latestExplanationsLocation`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        },
        if (!is.null(self$`previewLocation`)) {
          sprintf(
            '"previewLocation":
            "%s"
                  ',
            self$`previewLocation`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`recordId`)) {
          sprintf(
            '"recordId":
            "%s"
                  ',
            self$`recordId`
          )
        },
        if (!is.null(self$`seriesId`)) {
          sprintf(
            '"seriesId":
            "%s"
                  ',
            self$`seriesId`
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        },
        if (!is.null(self$`statusDetails`)) {
          sprintf(
            '"statusDetails":
            "%s"
                  ',
            self$`statusDetails`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AnomalyAssessmentRecordResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AnomalyAssessmentRecordResponseJson, validateParams = FALSE) {
      AnomalyAssessmentRecordResponseObject <- jsonlite::fromJSON(AnomalyAssessmentRecordResponseJson)
      self$`backtest` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = AnomalyAssessmentRecordResponseObject$backtest)
      self$`deleteLocation` <- AnomalyAssessmentRecordResponseObject$`deleteLocation`
      self$`endDate` <- ParseRFC3339Timestamp(AnomalyAssessmentRecordResponseObject$`endDate`)
      self$`latestExplanationsLocation` <- AnomalyAssessmentRecordResponseObject$`latestExplanationsLocation`
      self$`modelId` <- AnomalyAssessmentRecordResponseObject$`modelId`
      self$`predictionThreshold` <- AnomalyAssessmentRecordResponseObject$`predictionThreshold`
      self$`previewLocation` <- AnomalyAssessmentRecordResponseObject$`previewLocation`
      self$`projectId` <- AnomalyAssessmentRecordResponseObject$`projectId`
      self$`recordId` <- AnomalyAssessmentRecordResponseObject$`recordId`
      self$`seriesId` <- AnomalyAssessmentRecordResponseObject$`seriesId`
      self$`source` <- AnomalyAssessmentRecordResponseObject$`source`
      self$`startDate` <- ParseRFC3339Timestamp(AnomalyAssessmentRecordResponseObject$`startDate`)
      self$`status` <- AnomalyAssessmentRecordResponseObject$`status`
      self$`statusDetails` <- AnomalyAssessmentRecordResponseObject$`statusDetails`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
