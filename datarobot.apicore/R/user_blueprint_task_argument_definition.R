# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintTaskArgumentDefinition
#'
#' @description UserBlueprintTaskArgumentDefinition Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field default  \link{OneOfAnyTypearray} [optional] The default value of the argument.
#'
#' @field name  character The name of the argument.
#'
#' @field recommended  \link{OneOfAnyTypearray} [optional] The recommended value, based on frequently used values.
#'
#' @field tunable  character [optional] Whether the argument is tunable by the end-user.
#'
#' @field type  character The type of the argument (e.g. \&quot;int\&quot;, \&quot;float\&quot;, \&quot;select\&quot;, \&quot;intgrid\&quot;, \&quot;multi\&quot;, etc.)
#'
#' @field values  \link{OneOfAnyTypearrayobject} The possible values of the argument, which may be a range or list, or dictionary of ranges or lists keyed by type.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintTaskArgumentDefinition <- R6::R6Class(
  "UserBlueprintTaskArgumentDefinition",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`default` = NULL, `name` = NULL, `recommended` = NULL, `tunable` = NULL, `type` = NULL, `values` = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`values`)) {
        .setMixedProperty(typeList = list("AnyType", "array", "character"), propertyData = values)
      }
      if (!is.null(`default`)) {
        .setMixedProperty(typeList = list("AnyType", "array"), propertyData = default)
      }
      if (!is.null(`recommended`)) {
        .setMixedProperty(typeList = list("AnyType", "array"), propertyData = recommended)
      }
      if (!is.null(`tunable`)) {
        stopifnot(is.logical(`tunable`), length(`tunable`) == 1)
      }
    }
  ),
  public = list(
    `default` = NULL,
    `name` = NULL,
    `recommended` = NULL,
    `tunable` = NULL,
    `type` = NULL,
    `values` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param default The default value of the argument.
    #' @param name The name of the argument.
    #' @param recommended The recommended value, based on frequently used values.
    #' @param tunable Whether the argument is tunable by the end-user.
    #' @param type The type of the argument (e.g. \&quot;int\&quot;, \&quot;float\&quot;, \&quot;select\&quot;, \&quot;intgrid\&quot;, \&quot;multi\&quot;, etc.)
    #' @param values The possible values of the argument, which may be a range or list, or dictionary of ranges or lists keyed by type.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `type` = NULL, `values` = NULL, `default` = NULL, `recommended` = NULL, `tunable` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`name`, `type`, `values`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(default, name, recommended, tunable, type, values)
      }
      self$`default` <- .setMixedProperty(typeList = list("AnyType", "array"), propertyData = default)
      self$`name` <- `name`
      self$`recommended` <- .setMixedProperty(typeList = list("AnyType", "array"), propertyData = recommended)
      self$`tunable` <- `tunable`
      self$`type` <- `type`
      self$`values` <- .setMixedProperty(typeList = list("AnyType", "array", "character"), propertyData = values)
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(default = self$`default`, name = self$`name`, recommended = self$`recommended`, tunable = self$`tunable`, type = self$`type`, values = self$`values`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`default`)) {
          sprintf(
            '"default":
            %s
      ',
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`recommended`)) {
          sprintf(
            '"recommended":
            %s
      ',
          )
        },
        if (!is.null(self$`tunable`)) {
          sprintf(
            '"tunable":
            %s
                  ',
            tolower(self$`tunable`)
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        },
        if (!is.null(self$`values`)) {
          sprintf(
            '"values":
            %s
      ',
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintTaskArgumentDefinitionJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintTaskArgumentDefinitionJson, validateParams = FALSE) {
      UserBlueprintTaskArgumentDefinitionObject <- jsonlite::fromJSON(UserBlueprintTaskArgumentDefinitionJson)
      self$`default` <- .setMixedProperty(typeList = list("AnyType", "array"), propertyData = UserBlueprintTaskArgumentDefinitionObject$default)
      self$`name` <- UserBlueprintTaskArgumentDefinitionObject$`name`
      self$`recommended` <- .setMixedProperty(typeList = list("AnyType", "array"), propertyData = UserBlueprintTaskArgumentDefinitionObject$recommended)
      self$`tunable` <- UserBlueprintTaskArgumentDefinitionObject$`tunable`
      self$`type` <- UserBlueprintTaskArgumentDefinitionObject$`type`
      self$`values` <- .setMixedProperty(typeList = list("AnyType", "array", "character"), propertyData = UserBlueprintTaskArgumentDefinitionObject$values)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
