# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetFromFile
#'
#' @description DatasetFromFile Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field categories  \link{OneOfstringarray} [optional] An array of strings describing the intended use of the dataset.
#'
#' @field file  data.frame The data to be used for the creation.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetFromFile <- R6::R6Class(
  "DatasetFromFile",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`categories` = NULL, `file` = NULL) {
      if (!is.null(`file`)) {
      }
      if (!is.null(`categories`)) {
        .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      }
    }
  ),
  public = list(
    `categories` = NULL,
    `file` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param categories An array of strings describing the intended use of the dataset.
    #' @param file The data to be used for the creation.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`file` = NULL, `categories` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`file`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(categories, file)
      }
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      self$`file` <- `file`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(categories = self$`categories`, file = self$`file`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`categories`)) {
          sprintf(
            '"categories":
            %s
      ',
            self$`categories`
          )
        },
        if (!is.null(self$`file`)) {
          sprintf(
            '"file":
            "%s"
                  ',
            self$`file`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetFromFileJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetFromFileJson, validateParams = FALSE) {
      DatasetFromFileObject <- jsonlite::fromJSON(DatasetFromFileJson)
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = DatasetFromFileObject$categories)
      self$`file` <- DatasetFromFileObject$`file`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
