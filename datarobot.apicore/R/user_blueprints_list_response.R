# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintsListResponse
#'
#' @description UserBlueprintsListResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer Number of records on this page
#'
#' @field data  list( \link{UserBlueprintsListItem} ) List of user blueprints
#'
#' @field next_  character URL to the next page, or &#x60;null&#x60; if there is no such page
#'
#' @field previous  character URL to the previous page, or &#x60;null&#x60; if there is no such page
#'
#' @field totalCount  integer [optional] Total number of records
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintsListResponse <- R6::R6Class(
  "UserBlueprintsListResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `data` = NULL, `next_` = NULL, `previous` = NULL, `totalCount` = NULL) {
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`data`)) {
        stopifnot(is.vector(`data`), sapply(`data`, R6::is.R6))
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
      if (!is.null(`totalCount`)) {
        stopifnot(is.numeric(`totalCount`), length(`totalCount`) == 1)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `data` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    `totalCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count Number of records on this page
    #' @param data List of user blueprints
    #' @param next_ URL to the next page, or &#x60;null&#x60; if there is no such page
    #' @param previous URL to the previous page, or &#x60;null&#x60; if there is no such page
    #' @param totalCount Total number of records
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `data` = NULL, `next_` = NULL, `previous` = NULL, `totalCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`count`, `data`, `next_`, `previous`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, data, next_, previous, totalCount)
      }
      self$`count` <- `count`
      self$`data` <- `data`
      self$`next_` <- `next_`
      self$`previous` <- `previous`
      self$`totalCount` <- `totalCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, data = self$`data`, next_ = self$`next_`, previous = self$`previous`, totalCount = self$`totalCount`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
      ',
            paste(sapply(self$`data`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        },
        if (!is.null(self$`totalCount`)) {
          sprintf(
            '"totalCount":
            %d
                  ',
            self$`totalCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintsListResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintsListResponseJson, validateParams = FALSE) {
      UserBlueprintsListResponseObject <- jsonlite::fromJSON(UserBlueprintsListResponseJson)
      self$`count` <- UserBlueprintsListResponseObject$`count`
      self$`data` <- ApiClient$new()$deserializeObj(UserBlueprintsListResponseObject$`data`, "array[UserBlueprintsListItem]", loadNamespace("datarobot.apicore"))
      self$`next_` <- UserBlueprintsListResponseObject$`next`
      self$`previous` <- UserBlueprintsListResponseObject$`previous`
      self$`totalCount` <- UserBlueprintsListResponseObject$`totalCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
