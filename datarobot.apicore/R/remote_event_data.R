# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RemoteEventData
#'
#' @description RemoteEventData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field newModelId  character [optional] The identifier of the model after replacement.
#'
#' @field oldModelId  character [optional] The identifier of the model before replacement.
#'
#' @field reason  character [optional] The explanation on why the model has been replaced.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RemoteEventData <- R6::R6Class(
  "RemoteEventData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`newModelId` = NULL, `oldModelId` = NULL, `reason` = NULL) {
      if (!is.null(`newModelId`)) {
        stopifnot(is.character(`newModelId`), length(`newModelId`) == 1)
      }
      if (!is.null(`oldModelId`)) {
        stopifnot(is.character(`oldModelId`), length(`oldModelId`) == 1)
      }
      if (!is.null(`reason`)) {
        stopifnot(is.character(`reason`), length(`reason`) == 1)
      }
    }
  ),
  public = list(
    `newModelId` = NULL,
    `oldModelId` = NULL,
    `reason` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param newModelId The identifier of the model after replacement.
    #' @param oldModelId The identifier of the model before replacement.
    #' @param reason The explanation on why the model has been replaced.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`newModelId` = NULL, `oldModelId` = NULL, `reason` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(newModelId, oldModelId, reason)
      }
      self$`newModelId` <- `newModelId`
      self$`oldModelId` <- `oldModelId`
      self$`reason` <- `reason`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(newModelId = self$`newModelId`, oldModelId = self$`oldModelId`, reason = self$`reason`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`newModelId`)) {
          sprintf(
            '"newModelId":
            "%s"
                  ',
            self$`newModelId`
          )
        },
        if (!is.null(self$`oldModelId`)) {
          sprintf(
            '"oldModelId":
            "%s"
                  ',
            self$`oldModelId`
          )
        },
        if (!is.null(self$`reason`)) {
          sprintf(
            '"reason":
            "%s"
                  ',
            self$`reason`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RemoteEventDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RemoteEventDataJson, validateParams = FALSE) {
      RemoteEventDataObject <- jsonlite::fromJSON(RemoteEventDataJson)
      self$`newModelId` <- RemoteEventDataObject$`newModelId`
      self$`oldModelId` <- RemoteEventDataObject$`oldModelId`
      self$`reason` <- RemoteEventDataObject$`reason`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
