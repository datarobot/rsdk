# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetAccessInner
#'
#' @description DatasetAccessInner Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field canShare  character [optional] whether the user should be able to share with other users. If true, the user will be able to grant any role up to and including their own to other users. If &#x60;role&#x60; is empty &#x60;canShare&#x60; is ignored.
#'
#' @field canUseData  character [optional] Whether the user should be able to view, download and process data (use to create projects, predictions, etc). For OWNER &#x60;canUseData&#x60; is always True. If &#x60;role&#x60; is empty &#x60;canUseData&#x60; is ignored.
#'
#' @field role  character the role to grant to the user, or \&quot;\&quot; (empty string) to remove the users access
#'
#' @field username  character username of the user to update the access role for.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetAccessInner <- R6::R6Class(
  "DatasetAccessInner",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(canShare = NULL, canUseData = NULL, role = NULL, username = NULL) {
      if (!is.null(`role`)) {
        stopifnot(is.character(`role`), length(`role`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
      if (!is.null(`canShare`)) {
        stopifnot(is.logical(`canShare`), length(`canShare`) == 1)
      }
      if (!is.null(`canUseData`)) {
        stopifnot(is.logical(`canUseData`), length(`canUseData`) == 1)
      }
    }
  ),
  public = list(
    `canShare` = NULL,
    `canUseData` = NULL,
    `role` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param canShare whether the user should be able to share with other users. If true, the user will be able to grant any role up to and including their own to other users. If &#x60;role&#x60; is empty &#x60;canShare&#x60; is ignored.
    #' @param canUseData Whether the user should be able to view, download and process data (use to create projects, predictions, etc). For OWNER &#x60;canUseData&#x60; is always True. If &#x60;role&#x60; is empty &#x60;canUseData&#x60; is ignored.
    #' @param role the role to grant to the user, or \&quot;\&quot; (empty string) to remove the users access
    #' @param username username of the user to update the access role for.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`role` = NULL, `username` = NULL, `canShare` = NULL, `canUseData` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`role`, `username`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(canShare, canUseData, role, username)
      }
      self$`canShare` <- `canShare`
      self$`canUseData` <- `canUseData`
      self$`role` <- `role`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(canShare = self$`canShare`, canUseData = self$`canUseData`, role = self$`role`, username = self$`username`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`canShare`)) {
          sprintf(
            '"canShare":
            %s
                  ',
            tolower(self$`canShare`)
          )
        },
        if (!is.null(self$`canUseData`)) {
          sprintf(
            '"canUseData":
            %s
                  ',
            tolower(self$`canUseData`)
          )
        },
        if (!is.null(self$`role`)) {
          sprintf(
            '"role":
            "%s"
                  ',
            self$`role`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetAccessInnerJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetAccessInnerJson, validateParams = FALSE) {
      DatasetAccessInnerObject <- jsonlite::fromJSON(DatasetAccessInnerJson)
      self$`canShare` <- DatasetAccessInnerObject$`canShare`
      self$`canUseData` <- DatasetAccessInnerObject$`canUseData`
      self$`role` <- DatasetAccessInnerObject$`role`
      self$`username` <- DatasetAccessInnerObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
