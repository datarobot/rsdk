# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RelationshipsConfigResponse
#'
#' @description RelationshipsConfigResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetDefinitions  list( \link{DatasetDefinitionResponse} ) List of dataset definitions.
#'
#' @field featureDiscoveryMode  character [optional] Mode of feature discovery. Supported values are &#39;default&#39; and &#39;manual&#39;.
#'
#' @field featureDiscoverySettings  list( \link{FeatureDiscoverySettingResponse} ) [optional] List of feature discovery settings used to customize the feature discovery process.
#'
#' @field id  character [optional] ID of relationships configuration.
#'
#' @field relationships  list( \link{RelationshipResponse} ) List of relationship between datasets specified in datasetDefinitions.
#'
#' @field snowflakePushDownCompatible  character [optional] Is this configuration compatible with pushdown computation on Snowflake?
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RelationshipsConfigResponse <- R6::R6Class(
  "RelationshipsConfigResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datasetDefinitions` = NULL, `featureDiscoveryMode` = NULL, `featureDiscoverySettings` = NULL, `id` = NULL, `relationships` = NULL, `snowflakePushDownCompatible` = NULL) {
      if (!is.null(`datasetDefinitions`)) {
        stopifnot(is.vector(`datasetDefinitions`))
      }
      if (!is.null(`relationships`)) {
        stopifnot(is.vector(`relationships`))
      }
      if (!is.null(`featureDiscoveryMode`)) {
        stopifnot(is.character(`featureDiscoveryMode`), length(`featureDiscoveryMode`) == 1)
      }
      if (!is.null(`featureDiscoverySettings`)) {
        stopifnot(is.vector(`featureDiscoverySettings`))
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`snowflakePushDownCompatible`)) {
        stopifnot(is.logical(`snowflakePushDownCompatible`), length(`snowflakePushDownCompatible`) == 1)
      }
    }
  ),
  public = list(
    `datasetDefinitions` = NULL,
    `featureDiscoveryMode` = NULL,
    `featureDiscoverySettings` = NULL,
    `id` = NULL,
    `relationships` = NULL,
    `snowflakePushDownCompatible` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetDefinitions List of dataset definitions.
    #' @param featureDiscoveryMode Mode of feature discovery. Supported values are &#39;default&#39; and &#39;manual&#39;.
    #' @param featureDiscoverySettings List of feature discovery settings used to customize the feature discovery process.
    #' @param id ID of relationships configuration.
    #' @param relationships List of relationship between datasets specified in datasetDefinitions.
    #' @param snowflakePushDownCompatible Is this configuration compatible with pushdown computation on Snowflake?
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetDefinitions` = NULL, `relationships` = NULL, `featureDiscoveryMode` = NULL, `featureDiscoverySettings` = NULL, `id` = NULL, `snowflakePushDownCompatible` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datasetDefinitions`, `relationships`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datasetDefinitions, featureDiscoveryMode, featureDiscoverySettings, id, relationships, snowflakePushDownCompatible)
      }
      sapply(`datasetDefinitions`, function(x) stopifnot(R6::is.R6(x)))
      self$`featureDiscoveryMode` <- `featureDiscoveryMode`
      sapply(`featureDiscoverySettings`, function(x) stopifnot(R6::is.R6(x)))
      self$`id` <- `id`
      sapply(`relationships`, function(x) stopifnot(R6::is.R6(x)))
      self$`snowflakePushDownCompatible` <- `snowflakePushDownCompatible`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datasetDefinitions = self$`datasetDefinitions`, featureDiscoveryMode = self$`featureDiscoveryMode`, featureDiscoverySettings = self$`featureDiscoverySettings`, id = self$`id`, relationships = self$`relationships`, snowflakePushDownCompatible = self$`snowflakePushDownCompatible`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetDefinitions`)) {
          sprintf(
            '"datasetDefinitions":
            [%s]
      ',
            paste(sapply(self$`datasetDefinitions`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`featureDiscoveryMode`)) {
          sprintf(
            '"featureDiscoveryMode":
            "%s"
                  ',
            self$`featureDiscoveryMode`
          )
        },
        if (!is.null(self$`featureDiscoverySettings`)) {
          sprintf(
            '"featureDiscoverySettings":
            [%s]
      ',
            paste(sapply(self$`featureDiscoverySettings`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`relationships`)) {
          sprintf(
            '"relationships":
            [%s]
      ',
            paste(sapply(self$`relationships`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`snowflakePushDownCompatible`)) {
          sprintf(
            '"snowflakePushDownCompatible":
            %s
                  ',
            tolower(self$`snowflakePushDownCompatible`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RelationshipsConfigResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RelationshipsConfigResponseJson, validateParams = FALSE) {
      RelationshipsConfigResponseObject <- jsonlite::fromJSON(RelationshipsConfigResponseJson)
      self$`datasetDefinitions` <- ApiClient$new()$deserializeObj(RelationshipsConfigResponseObject$`datasetDefinitions`, "array[DatasetDefinitionResponse]", loadNamespace("datarobot.apicore"))
      self$`featureDiscoveryMode` <- RelationshipsConfigResponseObject$`featureDiscoveryMode`
      self$`featureDiscoverySettings` <- ApiClient$new()$deserializeObj(RelationshipsConfigResponseObject$`featureDiscoverySettings`, "array[FeatureDiscoverySettingResponse]", loadNamespace("datarobot.apicore"))
      self$`id` <- RelationshipsConfigResponseObject$`id`
      self$`relationships` <- ApiClient$new()$deserializeObj(RelationshipsConfigResponseObject$`relationships`, "array[RelationshipResponse]", loadNamespace("datarobot.apicore"))
      self$`snowflakePushDownCompatible` <- RelationshipsConfigResponseObject$`snowflakePushDownCompatible`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
