# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanationsInitializationRetrieve
#'
#' @description PredictionExplanationsInitializationRetrieve Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field modelId  character The model ID.
#'
#' @field predictionExplanationsSample  list( \link{PredictionExplanationsRow} ) Each is a PredictionExplanationsRow. They represent a small sample of prediction explanations that could be generated for a particular dataset. They will have the same schema as the &#x60;data&#x60; array in the response from :http:get:&#x60;/api/v2/projects/(projectId)/predictionExplanations/(predictionExplanationsId)/&#x60;. As of v2.21 only difference is that there is no forecastPoint in response for time series projects.
#'
#' @field projectId  character The project ID.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanationsInitializationRetrieve <- R6::R6Class(
  "PredictionExplanationsInitializationRetrieve",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`modelId` = NULL, `predictionExplanationsSample` = NULL, `projectId` = NULL) {
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`predictionExplanationsSample`)) {
        stopifnot(is.vector(`predictionExplanationsSample`))
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `modelId` = NULL,
    `predictionExplanationsSample` = NULL,
    `projectId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param modelId The model ID.
    #' @param predictionExplanationsSample Each is a PredictionExplanationsRow. They represent a small sample of prediction explanations that could be generated for a particular dataset. They will have the same schema as the &#x60;data&#x60; array in the response from :http:get:&#x60;/api/v2/projects/(projectId)/predictionExplanations/(predictionExplanationsId)/&#x60;. As of v2.21 only difference is that there is no forecastPoint in response for time series projects.
    #' @param projectId The project ID.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`modelId` = NULL, `predictionExplanationsSample` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`modelId`, `predictionExplanationsSample`, `projectId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(modelId, predictionExplanationsSample, projectId)
      }
      self$`modelId` <- `modelId`
      sapply(`predictionExplanationsSample`, function(x) stopifnot(R6::is.R6(x)))
      self$`projectId` <- `projectId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(modelId = self$`modelId`, predictionExplanationsSample = self$`predictionExplanationsSample`, projectId = self$`projectId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`predictionExplanationsSample`)) {
          sprintf(
            '"predictionExplanationsSample":
            [%s]
      ',
            paste(sapply(self$`predictionExplanationsSample`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationsInitializationRetrieveJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationsInitializationRetrieveJson, validateParams = FALSE) {
      PredictionExplanationsInitializationRetrieveObject <- jsonlite::fromJSON(PredictionExplanationsInitializationRetrieveJson)
      self$`modelId` <- PredictionExplanationsInitializationRetrieveObject$`modelId`
      self$`predictionExplanationsSample` <- ApiClient$new()$deserializeObj(PredictionExplanationsInitializationRetrieveObject$`predictionExplanationsSample`, "array[PredictionExplanationsRow]", loadNamespace("datarobot.apicore"))
      self$`projectId` <- PredictionExplanationsInitializationRetrieveObject$`projectId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
