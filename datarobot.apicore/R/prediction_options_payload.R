# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionOptionsPayload
#'
#' @description PredictionOptionsPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field errorLogColumn  character Toggle for saving errors
#'
#' @field overwrite  character Whether to overwrite the existing output or not
#'
#' @field passthroughColumns  list( character ) Array of strings of passthrough columns
#'
#' @field passthroughColumnsSchemas  list( \link{PassthroughColumnDefinitionPayload} ) [optional] (Only for Tableau) Array of passthrough column description objects with JDBC specific information for each of the users selected passthrough column, if any are included
#'
#' @field predictionExplanations  integer Quantity of prediction explanations
#'
#' @field timeseriesSettings  \link{TimeSeriesFieldsPayload} [optional] 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionOptionsPayload <- R6::R6Class(
  "PredictionOptionsPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(errorLogColumn = NULL, overwrite = NULL, passthroughColumns = NULL, passthroughColumnsSchemas = NULL, predictionExplanations = NULL, timeseriesSettings = NULL) {
      if (!is.null(`errorLogColumn`)) {
        stopifnot(is.logical(`errorLogColumn`), length(`errorLogColumn`) == 1)
      }
      if (!is.null(`overwrite`)) {
        stopifnot(is.logical(`overwrite`), length(`overwrite`) == 1)
      }
      if (!is.null(`passthroughColumns`)) {
        stopifnot(is.vector(`passthroughColumns`))
      }
      if (!is.null(`predictionExplanations`)) {
        stopifnot(is.numeric(`predictionExplanations`), length(`predictionExplanations`) == 1)
      }
      if (!is.null(`passthroughColumnsSchemas`)) {
        stopifnot(is.vector(`passthroughColumnsSchemas`))
      }
      if (!is.null(`timeseriesSettings`)) {
        stopifnot(R6::is.R6(`timeseriesSettings`))
      }
    }
  ),
  public = list(
    `errorLogColumn` = NULL,
    `overwrite` = NULL,
    `passthroughColumns` = NULL,
    `passthroughColumnsSchemas` = NULL,
    `predictionExplanations` = NULL,
    `timeseriesSettings` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param errorLogColumn Toggle for saving errors
    #' @param overwrite Whether to overwrite the existing output or not
    #' @param passthroughColumns Array of strings of passthrough columns
    #' @param passthroughColumnsSchemas (Only for Tableau) Array of passthrough column description objects with JDBC specific information for each of the users selected passthrough column, if any are included
    #' @param predictionExplanations Quantity of prediction explanations
    #' @param timeseriesSettings 
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `errorLogColumn` = NULL, `overwrite` = NULL, `passthroughColumns` = NULL, `predictionExplanations` = NULL, `passthroughColumnsSchemas` = NULL, `timeseriesSettings` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`errorLogColumn`, `overwrite`, `passthroughColumns`, `predictionExplanations`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(errorLogColumn, overwrite, passthroughColumns, passthroughColumnsSchemas, predictionExplanations, timeseriesSettings)
      }
      self$`errorLogColumn` <- `errorLogColumn`
      self$`overwrite` <- `overwrite`
      sapply(`passthroughColumns`, function(x) stopifnot(is.character(x)))
      sapply(`passthroughColumnsSchemas`, function(x) stopifnot(R6::is.R6(x)))
      self$`predictionExplanations` <- `predictionExplanations`
      self$`timeseriesSettings` <- `timeseriesSettings`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(errorLogColumn = self$`errorLogColumn`, overwrite = self$`overwrite`, passthroughColumns = self$`passthroughColumns`, passthroughColumnsSchemas = self$`passthroughColumnsSchemas`, predictionExplanations = self$`predictionExplanations`, timeseriesSettings = self$`timeseriesSettings`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`errorLogColumn`)) {
        sprintf(
        '"errorLogColumn":
          %s
                ',
        tolower(self$`errorLogColumn`)
        )},
        if (!is.null(self$`overwrite`)) {
        sprintf(
        '"overwrite":
          %s
                ',
        tolower(self$`overwrite`)
        )},
        if (!is.null(self$`passthroughColumns`)) {
        sprintf(
        '"passthroughColumns":
           [%s]
        ',
        paste(unlist(lapply(self$`passthroughColumns`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`passthroughColumnsSchemas`)) {
        sprintf(
        '"passthroughColumnsSchemas":
        [%s]
',
        paste(sapply(self$`passthroughColumnsSchemas`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
        )},
        if (!is.null(self$`predictionExplanations`)) {
        sprintf(
        '"predictionExplanations":
          %d
                ',
        self$`predictionExplanations`
        )},
        if (!is.null(self$`timeseriesSettings`)) {
        sprintf(
        '"timeseriesSettings":
        %s
        ',
        jsonlite::toJSON(self$`timeseriesSettings`$toJSON(), auto_unbox = TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionOptionsPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionOptionsPayloadJson, validateParams = FALSE) {
      PredictionOptionsPayloadObject <- jsonlite::fromJSON(PredictionOptionsPayloadJson)
      self$`errorLogColumn` <- PredictionOptionsPayloadObject$`errorLogColumn`
      self$`overwrite` <- PredictionOptionsPayloadObject$`overwrite`
      self$`passthroughColumns` <- ApiClient$new()$deserializeObj(PredictionOptionsPayloadObject$`passthroughColumns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`passthroughColumnsSchemas` <- ApiClient$new()$deserializeObj(PredictionOptionsPayloadObject$`passthroughColumnsSchemas`, "array[PassthroughColumnDefinitionPayload]", loadNamespace("datarobot.apicore"))
      self$`predictionExplanations` <- PredictionOptionsPayloadObject$`predictionExplanations`
      self$`timeseriesSettings` <- TimeSeriesFieldsPayload$new()$fromJSON(jsonlite::toJSON(PredictionOptionsPayloadObject$timeseriesSettings, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
