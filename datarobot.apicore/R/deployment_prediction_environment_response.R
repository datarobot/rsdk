# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentPredictionEnvironmentResponse
#'
#' @description DeploymentPredictionEnvironmentResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field id  character ID of the PredictionEnvironment.
#'
#' @field isManagedByManagementAgent  character True if PredictionEnvironment is using Management Agent.
#'
#' @field name  character Name of the PredictionEnvironment.
#'
#' @field platform  character Platform of the PredictionEnvironment.
#'
#' @field plugin  character [optional] Plugin name of the PredictionEnvironment.
#'
#' @field supportedModelFormats  list( character ) [optional] Model formats that the PredictionEnvironment supports.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentPredictionEnvironmentResponse <- R6::R6Class(
  "DeploymentPredictionEnvironmentResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(id = NULL, isManagedByManagementAgent = NULL, name = NULL, platform = NULL, plugin = NULL, supportedModelFormats = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`isManagedByManagementAgent`)) {
        stopifnot(is.logical(`isManagedByManagementAgent`), length(`isManagedByManagementAgent`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`platform`)) {
        stopifnot(is.character(`platform`), length(`platform`) == 1)
      }
      if (!is.null(`plugin`)) {
        stopifnot(is.character(`plugin`), length(`plugin`) == 1)
      }
      if (!is.null(`supportedModelFormats`)) {
        stopifnot(is.vector(`supportedModelFormats`))
      }
    }
  ),
  public = list(
    `id` = NULL,
    `isManagedByManagementAgent` = NULL,
    `name` = NULL,
    `platform` = NULL,
    `plugin` = NULL,
    `supportedModelFormats` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param id ID of the PredictionEnvironment.
    #' @param isManagedByManagementAgent True if PredictionEnvironment is using Management Agent.
    #' @param name Name of the PredictionEnvironment.
    #' @param platform Platform of the PredictionEnvironment.
    #' @param plugin Plugin name of the PredictionEnvironment.
    #' @param supportedModelFormats Model formats that the PredictionEnvironment supports.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `isManagedByManagementAgent` = NULL, `name` = NULL, `platform` = NULL, `plugin` = NULL, `supportedModelFormats` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`id`, `isManagedByManagementAgent`, `name`, `platform`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(id, isManagedByManagementAgent, name, platform, plugin, supportedModelFormats)
      }
      self$`id` <- `id`
      self$`isManagedByManagementAgent` <- `isManagedByManagementAgent`
      self$`name` <- `name`
      self$`platform` <- `platform`
      self$`plugin` <- `plugin`
      sapply(`supportedModelFormats`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(id = self$`id`, isManagedByManagementAgent = self$`isManagedByManagementAgent`, name = self$`name`, platform = self$`platform`, plugin = self$`plugin`, supportedModelFormats = self$`supportedModelFormats`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`isManagedByManagementAgent`)) {
          sprintf(
            '"isManagedByManagementAgent":
            %s
                  ',
            tolower(self$`isManagedByManagementAgent`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`platform`)) {
          sprintf(
            '"platform":
            "%s"
                  ',
            self$`platform`
          )
        },
        if (!is.null(self$`plugin`)) {
          sprintf(
            '"plugin":
            "%s"
                  ',
            self$`plugin`
          )
        },
        if (!is.null(self$`supportedModelFormats`)) {
          sprintf(
            '"supportedModelFormats":
              [%s]
            ',
            paste(unlist(lapply(self$`supportedModelFormats`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentPredictionEnvironmentResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentPredictionEnvironmentResponseJson, validateParams = FALSE) {
      DeploymentPredictionEnvironmentResponseObject <- jsonlite::fromJSON(DeploymentPredictionEnvironmentResponseJson)
      self$`id` <- DeploymentPredictionEnvironmentResponseObject$`id`
      self$`isManagedByManagementAgent` <- DeploymentPredictionEnvironmentResponseObject$`isManagedByManagementAgent`
      self$`name` <- DeploymentPredictionEnvironmentResponseObject$`name`
      self$`platform` <- DeploymentPredictionEnvironmentResponseObject$`platform`
      self$`plugin` <- DeploymentPredictionEnvironmentResponseObject$`plugin`
      self$`supportedModelFormats` <- ApiClient$new()$deserializeObj(DeploymentPredictionEnvironmentResponseObject$`supportedModelFormats`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
