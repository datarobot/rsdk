# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentPredictionUsageResponse
#'
#' @description DeploymentPredictionUsageResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dailyRates  list( integer ) Number of predictions made in the last 7 days.
#'
#' @field lastTimestamp  character Timestamp of the last prediction request.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentPredictionUsageResponse <- R6::R6Class(
  "DeploymentPredictionUsageResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`dailyRates` = NULL, `lastTimestamp` = NULL) {
      if (!is.null(`dailyRates`)) {
        stopifnot(is.vector(`dailyRates`))
      }
      if (!is.null(`lastTimestamp`)) {
        stopifnot(inherits(`lastTimestamp`, "POSIXt"))
      }
    }
  ),
  public = list(
    `dailyRates` = NULL,
    `lastTimestamp` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dailyRates Number of predictions made in the last 7 days.
    #' @param lastTimestamp Timestamp of the last prediction request.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dailyRates` = NULL, `lastTimestamp` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`dailyRates`, `lastTimestamp`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(dailyRates, lastTimestamp)
      }
      sapply(`dailyRates`, function(x) stopifnot(is.character(x)))
      self$`lastTimestamp` <- `lastTimestamp`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(dailyRates = self$`dailyRates`, lastTimestamp = self$`lastTimestamp`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dailyRates`)) {
          sprintf(
            '"dailyRates":
            [%s]
                  ',
            paste(unlist(lapply(self$`dailyRates`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`lastTimestamp`)) {
          sprintf(
            '"lastTimestamp":
            "%s"
                  ',
            format(self$`lastTimestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentPredictionUsageResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentPredictionUsageResponseJson, validateParams = FALSE) {
      DeploymentPredictionUsageResponseObject <- jsonlite::fromJSON(DeploymentPredictionUsageResponseJson)
      self$`dailyRates` <- ApiClient$new()$deserializeObj(DeploymentPredictionUsageResponseObject$`dailyRates`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`lastTimestamp` <- ParseRFC3339Timestamp(DeploymentPredictionUsageResponseObject$`lastTimestamp`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
