# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ExternalTSBaselineResponse
#'
#' @description ExternalTSBaselineResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtests  list( \link{Backtests} ) [optional] An array of the configured backtests.
#'
#' @field baselineValidationJobId  character The id of the validation job.
#'
#' @field catalogVersionId  character The version id of the external baseline data item in the AI catalog.
#'
#' @field datetimePartitionColumn  character The date column that will be used as the datetime partition column for the specified project.
#'
#' @field forecastWindowEnd  integer [optional] For time series projects only. How many timeUnits of the datetimePartitionColumn into the future relative to the forecast point the forecast window should end.
#'
#' @field forecastWindowStart  integer [optional] For time series projects only. How many timeUnits of the datetimePartitionColumn into the future relative to the forecast point the forecast window should start.
#'
#' @field holdoutEndDate  character [optional] The end date of holdout scoring data.
#'
#' @field holdoutStartDate  character [optional] The start date of holdout scoring data.
#'
#' @field isExternalBaselineDatasetValid  character Indicates whether the external dataset has pass the validation check or not.
#'
#' @field message  character A message providing mode detail on the validation result.
#'
#' @field multiseriesIdColumns  list( character ) [optional] An array of column names identifying the multiseries ID column(s)to use to identify series within the data. Must match the multiseries ID column(s) for the specified project. Currently, only one multiseries ID column may be specified.
#'
#' @field projectId  character The project id of the external baseline data item.
#'
#' @field target  character The selected target of the specified project.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ExternalTSBaselineResponse <- R6::R6Class(
  "ExternalTSBaselineResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`backtests` = NULL, `baselineValidationJobId` = NULL, `catalogVersionId` = NULL, `datetimePartitionColumn` = NULL, `forecastWindowEnd` = NULL, `forecastWindowStart` = NULL, `holdoutEndDate` = NULL, `holdoutStartDate` = NULL, `isExternalBaselineDatasetValid` = NULL, `message` = NULL, `multiseriesIdColumns` = NULL, `projectId` = NULL, `target` = NULL) {
      if (!is.null(`baselineValidationJobId`)) {
        stopifnot(is.character(`baselineValidationJobId`), length(`baselineValidationJobId`) == 1)
      }
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`datetimePartitionColumn`)) {
        stopifnot(is.character(`datetimePartitionColumn`), length(`datetimePartitionColumn`) == 1)
      }
      if (!is.null(`isExternalBaselineDatasetValid`)) {
        stopifnot(is.logical(`isExternalBaselineDatasetValid`), length(`isExternalBaselineDatasetValid`) == 1)
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`target`)) {
        stopifnot(is.character(`target`), length(`target`) == 1)
      }
      if (!is.null(`backtests`) && length(`backtests`) > 0) {
        stopifnot(is.vector(`backtests`), sapply(`backtests`, R6::is.R6))
      }
      if (!is.null(`forecastWindowEnd`)) {
        stopifnot(is.numeric(`forecastWindowEnd`), length(`forecastWindowEnd`) == 1)
      }
      if (!is.null(`forecastWindowStart`)) {
        stopifnot(is.numeric(`forecastWindowStart`), length(`forecastWindowStart`) == 1)
      }
      if (!is.null(`holdoutEndDate`)) {
        stopifnot(inherits(`holdoutEndDate`, "POSIXt"))
      }
      if (!is.null(`holdoutStartDate`)) {
        stopifnot(inherits(`holdoutStartDate`, "POSIXt"))
      }
      if (!is.null(`multiseriesIdColumns`) && length(`multiseriesIdColumns`) > 0) {
        stopifnot(is.vector(`multiseriesIdColumns`), sapply(`multiseriesIdColumns`, is.character))
      }
    }
  ),
  public = list(
    `backtests` = NULL,
    `baselineValidationJobId` = NULL,
    `catalogVersionId` = NULL,
    `datetimePartitionColumn` = NULL,
    `forecastWindowEnd` = NULL,
    `forecastWindowStart` = NULL,
    `holdoutEndDate` = NULL,
    `holdoutStartDate` = NULL,
    `isExternalBaselineDatasetValid` = NULL,
    `message` = NULL,
    `multiseriesIdColumns` = NULL,
    `projectId` = NULL,
    `target` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtests An array of the configured backtests.
    #' @param baselineValidationJobId The id of the validation job.
    #' @param catalogVersionId The version id of the external baseline data item in the AI catalog.
    #' @param datetimePartitionColumn The date column that will be used as the datetime partition column for the specified project.
    #' @param forecastWindowEnd For time series projects only. How many timeUnits of the datetimePartitionColumn into the future relative to the forecast point the forecast window should end.
    #' @param forecastWindowStart For time series projects only. How many timeUnits of the datetimePartitionColumn into the future relative to the forecast point the forecast window should start.
    #' @param holdoutEndDate The end date of holdout scoring data.
    #' @param holdoutStartDate The start date of holdout scoring data.
    #' @param isExternalBaselineDatasetValid Indicates whether the external dataset has pass the validation check or not.
    #' @param message A message providing mode detail on the validation result.
    #' @param multiseriesIdColumns An array of column names identifying the multiseries ID column(s)to use to identify series within the data. Must match the multiseries ID column(s) for the specified project. Currently, only one multiseries ID column may be specified.
    #' @param projectId The project id of the external baseline data item.
    #' @param target The selected target of the specified project.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`baselineValidationJobId` = NULL, `catalogVersionId` = NULL, `datetimePartitionColumn` = NULL, `isExternalBaselineDatasetValid` = NULL, `message` = NULL, `projectId` = NULL, `target` = NULL, `backtests` = NULL, `forecastWindowEnd` = NULL, `forecastWindowStart` = NULL, `holdoutEndDate` = NULL, `holdoutStartDate` = NULL, `multiseriesIdColumns` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`baselineValidationJobId`, `catalogVersionId`, `datetimePartitionColumn`, `isExternalBaselineDatasetValid`, `message`, `projectId`, `target`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(backtests, baselineValidationJobId, catalogVersionId, datetimePartitionColumn, forecastWindowEnd, forecastWindowStart, holdoutEndDate, holdoutStartDate, isExternalBaselineDatasetValid, message, multiseriesIdColumns, projectId, target)
      }
      self$`backtests` <- `backtests`
      self$`baselineValidationJobId` <- `baselineValidationJobId`
      self$`catalogVersionId` <- `catalogVersionId`
      self$`datetimePartitionColumn` <- `datetimePartitionColumn`
      self$`forecastWindowEnd` <- `forecastWindowEnd`
      self$`forecastWindowStart` <- `forecastWindowStart`
      self$`holdoutEndDate` <- `holdoutEndDate`
      self$`holdoutStartDate` <- `holdoutStartDate`
      self$`isExternalBaselineDatasetValid` <- `isExternalBaselineDatasetValid`
      self$`message` <- `message`
      self$`multiseriesIdColumns` <- `multiseriesIdColumns`
      self$`projectId` <- `projectId`
      self$`target` <- `target`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(backtests = self$`backtests`, baselineValidationJobId = self$`baselineValidationJobId`, catalogVersionId = self$`catalogVersionId`, datetimePartitionColumn = self$`datetimePartitionColumn`, forecastWindowEnd = self$`forecastWindowEnd`, forecastWindowStart = self$`forecastWindowStart`, holdoutEndDate = self$`holdoutEndDate`, holdoutStartDate = self$`holdoutStartDate`, isExternalBaselineDatasetValid = self$`isExternalBaselineDatasetValid`, message = self$`message`, multiseriesIdColumns = self$`multiseriesIdColumns`, projectId = self$`projectId`, target = self$`target`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtests`)) {
          sprintf(
            '"backtests":
            [%s]
      ',
            paste(sapply(self$`backtests`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`baselineValidationJobId`)) {
          sprintf(
            '"baselineValidationJobId":
            "%s"
                  ',
            self$`baselineValidationJobId`
          )
        },
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`datetimePartitionColumn`)) {
          sprintf(
            '"datetimePartitionColumn":
            "%s"
                  ',
            self$`datetimePartitionColumn`
          )
        },
        if (!is.null(self$`forecastWindowEnd`)) {
          sprintf(
            '"forecastWindowEnd":
            %d
                  ',
            self$`forecastWindowEnd`
          )
        },
        if (!is.null(self$`forecastWindowStart`)) {
          sprintf(
            '"forecastWindowStart":
            %d
                  ',
            self$`forecastWindowStart`
          )
        },
        if (!is.null(self$`holdoutEndDate`)) {
          sprintf(
            '"holdoutEndDate":
            "%s"
                  ',
            format(self$`holdoutEndDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`holdoutStartDate`)) {
          sprintf(
            '"holdoutStartDate":
            "%s"
                  ',
            format(self$`holdoutStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`isExternalBaselineDatasetValid`)) {
          sprintf(
            '"isExternalBaselineDatasetValid":
            %s
                  ',
            tolower(self$`isExternalBaselineDatasetValid`)
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`multiseriesIdColumns`)) {
          sprintf(
            '"multiseriesIdColumns":
            [%s]
                  ',
            paste(unlist(lapply(self$`multiseriesIdColumns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`target`)) {
          sprintf(
            '"target":
            "%s"
                  ',
            self$`target`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ExternalTSBaselineResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ExternalTSBaselineResponseJson, validateParams = FALSE) {
      ExternalTSBaselineResponseObject <- jsonlite::fromJSON(ExternalTSBaselineResponseJson)
      self$`backtests` <- ApiClient$new()$deserializeObj(ExternalTSBaselineResponseObject$`backtests`, "array[Backtests]", loadNamespace("datarobot.apicore"))
      self$`baselineValidationJobId` <- ExternalTSBaselineResponseObject$`baselineValidationJobId`
      self$`catalogVersionId` <- ExternalTSBaselineResponseObject$`catalogVersionId`
      self$`datetimePartitionColumn` <- ExternalTSBaselineResponseObject$`datetimePartitionColumn`
      self$`forecastWindowEnd` <- ExternalTSBaselineResponseObject$`forecastWindowEnd`
      self$`forecastWindowStart` <- ExternalTSBaselineResponseObject$`forecastWindowStart`
      self$`holdoutEndDate` <- ParseRFC3339Timestamp(ExternalTSBaselineResponseObject$`holdoutEndDate`)
      self$`holdoutStartDate` <- ParseRFC3339Timestamp(ExternalTSBaselineResponseObject$`holdoutStartDate`)
      self$`isExternalBaselineDatasetValid` <- ExternalTSBaselineResponseObject$`isExternalBaselineDatasetValid`
      self$`message` <- ExternalTSBaselineResponseObject$`message`
      self$`multiseriesIdColumns` <- ApiClient$new()$deserializeObj(ExternalTSBaselineResponseObject$`multiseriesIdColumns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`projectId` <- ExternalTSBaselineResponseObject$`projectId`
      self$`target` <- ExternalTSBaselineResponseObject$`target`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
