# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetTransformResponse
#'
#' @description DatasetTransformResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dateExtraction  character The value to extract from the date column.
#'
#' @field name  character The feature name.
#'
#' @field parentName  character The name of the parent feature.
#'
#' @field replacement  \link{AnyOfstringbooleannumberinteger} The replacement in case of a failed transformation.
#'
#' @field variableType  character The type of the transform.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetTransformResponse <- R6::R6Class(
  "DatasetTransformResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`dateExtraction` = NULL, `name` = NULL, `parentName` = NULL, `replacement` = NULL, `variableType` = NULL) {
      if (!is.null(`dateExtraction`)) {
        stopifnot(is.character(`dateExtraction`), length(`dateExtraction`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`parentName`)) {
        stopifnot(is.character(`parentName`), length(`parentName`) == 1)
      }
      if (!is.null(`replacement`)) {
        .setPrimitiveProperty(typeList = list("character", "logical", "numeric"), propertyData = replacement)
      }
      if (!is.null(`variableType`)) {
        stopifnot(is.character(`variableType`), length(`variableType`) == 1)
      }
    }
  ),
  public = list(
    `dateExtraction` = NULL,
    `name` = NULL,
    `parentName` = NULL,
    `replacement` = NULL,
    `variableType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dateExtraction The value to extract from the date column.
    #' @param name The feature name.
    #' @param parentName The name of the parent feature.
    #' @param replacement The replacement in case of a failed transformation.
    #' @param variableType The type of the transform.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dateExtraction` = NULL, `name` = NULL, `parentName` = NULL, `replacement` = NULL, `variableType` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`dateExtraction`, `name`, `parentName`, `replacement`, `variableType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(dateExtraction, name, parentName, replacement, variableType)
      }
      self$`dateExtraction` <- `dateExtraction`
      self$`name` <- `name`
      self$`parentName` <- `parentName`
      self$`replacement` <- .setPrimitiveProperty(typeList = list("character", "logical", "numeric"), propertyData = replacement)
      self$`variableType` <- `variableType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(dateExtraction = self$`dateExtraction`, name = self$`name`, parentName = self$`parentName`, replacement = self$`replacement`, variableType = self$`variableType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dateExtraction`)) {
          sprintf(
            '"dateExtraction":
            "%s"
                  ',
            self$`dateExtraction`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`parentName`)) {
          sprintf(
            '"parentName":
            "%s"
                  ',
            self$`parentName`
          )
        },
        if (!is.null(self$`replacement`)) {
          sprintf(
            '"replacement":
            %s
      ',
            self$`replacement`
          )
        },
        if (!is.null(self$`variableType`)) {
          sprintf(
            '"variableType":
            "%s"
                  ',
            self$`variableType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetTransformResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetTransformResponseJson, validateParams = FALSE) {
      DatasetTransformResponseObject <- jsonlite::fromJSON(DatasetTransformResponseJson)
      self$`dateExtraction` <- DatasetTransformResponseObject$`dateExtraction`
      self$`name` <- DatasetTransformResponseObject$`name`
      self$`parentName` <- DatasetTransformResponseObject$`parentName`
      self$`replacement` <- .setPrimitiveProperty(typeList = list("character", "logical", "numeric"), propertyData = DatasetTransformResponseObject$replacement)
      self$`variableType` <- DatasetTransformResponseObject$`variableType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
