# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ReviewCreate
#'
#' @description ReviewCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field changeVersionId  character ID of the change version.
#'
#' @field comment  character [optional] Free form text to comment on the review.
#'
#' @field status  character Status of the review.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ReviewCreate <- R6::R6Class(
  "ReviewCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`changeVersionId` = NULL, `comment` = NULL, `status` = NULL) {
      if (!is.null(`changeVersionId`)) {
        stopifnot(is.character(`changeVersionId`), length(`changeVersionId`) == 1)
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
      if (!is.null(`comment`)) {
        stopifnot(is.character(`comment`), length(`comment`) == 1)
      }
    }
  ),
  public = list(
    `changeVersionId` = NULL,
    `comment` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param changeVersionId ID of the change version.
    #' @param comment Free form text to comment on the review.
    #' @param status Status of the review.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`changeVersionId` = NULL, `status` = NULL, `comment` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`changeVersionId`, `status`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(changeVersionId, comment, status)
      }
      self$`changeVersionId` <- `changeVersionId`
      self$`comment` <- `comment`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(changeVersionId = self$`changeVersionId`, comment = self$`comment`, status = self$`status`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`changeVersionId`)) {
          sprintf(
            '"changeVersionId":
            "%s"
                  ',
            self$`changeVersionId`
          )
        },
        if (!is.null(self$`comment`)) {
          sprintf(
            '"comment":
            "%s"
                  ',
            self$`comment`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ReviewCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ReviewCreateJson, validateParams = FALSE) {
      ReviewCreateObject <- jsonlite::fromJSON(ReviewCreateJson)
      self$`changeVersionId` <- ReviewCreateObject$`changeVersionId`
      self$`comment` <- ReviewCreateObject$`comment`
      self$`status` <- ReviewCreateObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
