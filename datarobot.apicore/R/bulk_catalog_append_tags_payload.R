# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BulkCatalogAppendTagsPayload
#'
#' @description BulkCatalogAppendTagsPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field action  character The action to execute on the datasets. Has to be &#39;tag&#39; for this payload.
#'
#' @field tags  list( character ) The tags to append to the datasets. Tags will not be duplicated.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BulkCatalogAppendTagsPayload <- R6::R6Class(
  "BulkCatalogAppendTagsPayload",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`action` = NULL, `tags` = NULL) {
      if (!is.null(`action`)) {
        stopifnot(is.character(`action`), length(`action`) == 1)
      }
      if (!is.null(`tags`)) {
        stopifnot(is.vector(`tags`))
      }
    }
  ),
  public = list(
    `action` = NULL,
    `tags` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param action The action to execute on the datasets. Has to be &#39;tag&#39; for this payload.
    #' @param tags The tags to append to the datasets. Tags will not be duplicated.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`action` = NULL, `tags` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`action`, `tags`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(action, tags)
      }
      self$`action` <- `action`
      sapply(`tags`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(action = self$`action`, tags = self$`tags`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`action`)) {
          sprintf(
            '"action":
            "%s"
                  ',
            self$`action`
          )
        },
        if (!is.null(self$`tags`)) {
          sprintf(
            '"tags":
            [%s]
                  ',
            paste(unlist(lapply(self$`tags`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BulkCatalogAppendTagsPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BulkCatalogAppendTagsPayloadJson, validateParams = FALSE) {
      BulkCatalogAppendTagsPayloadObject <- jsonlite::fromJSON(BulkCatalogAppendTagsPayloadJson)
      self$`action` <- BulkCatalogAppendTagsPayloadObject$`action`
      self$`tags` <- ApiClient$new()$deserializeObj(BulkCatalogAppendTagsPayloadObject$`tags`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
