# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintAddToMenuResponse
#'
#' @description UserBlueprintAddToMenuResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field addedToMenu  list( \link{UserBlueprintAddedToMenuItem} ) The list of userBlueprintId and blueprintId pairs representing blueprints successfully added to the project repository.
#'
#' @field message  character [optional] A success message or a list of reasons why the list of blueprints could not be added to the project repository.
#'
#' @field notAddedToMenu  list( \link{UserBlueprintFailedToAddToMenuItem} ) [optional] The list of userBlueprintId and error message representing blueprints which failed to be added to the project repository.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintAddToMenuResponse <- R6::R6Class(
  "UserBlueprintAddToMenuResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(addedToMenu = NULL, message = NULL, notAddedToMenu = NULL) {
      if (!is.null(`addedToMenu`)) {
        stopifnot(is.vector(`addedToMenu`))
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`notAddedToMenu`)) {
        stopifnot(is.vector(`notAddedToMenu`))
      }
    }
  ),
  public = list(
    `addedToMenu` = NULL,
    `message` = NULL,
    `notAddedToMenu` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param addedToMenu The list of userBlueprintId and blueprintId pairs representing blueprints successfully added to the project repository.
    #' @param message A success message or a list of reasons why the list of blueprints could not be added to the project repository.
    #' @param notAddedToMenu The list of userBlueprintId and error message representing blueprints which failed to be added to the project repository.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`addedToMenu` = NULL, `message` = NULL, `notAddedToMenu` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`addedToMenu`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(addedToMenu, message, notAddedToMenu)
      }
      sapply(`addedToMenu`, function(x) stopifnot(R6::is.R6(x)))
      self$`message` <- `message`
      sapply(`notAddedToMenu`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(addedToMenu = self$`addedToMenu`, message = self$`message`, notAddedToMenu = self$`notAddedToMenu`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`addedToMenu`)) {
          sprintf(
            '"addedToMenu":
            [%s]
',
            paste(sapply(self$`addedToMenu`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`notAddedToMenu`)) {
          sprintf(
            '"notAddedToMenu":
            [%s]
',
            paste(sapply(self$`notAddedToMenu`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintAddToMenuResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintAddToMenuResponseJson, validateParams = FALSE) {
      UserBlueprintAddToMenuResponseObject <- jsonlite::fromJSON(UserBlueprintAddToMenuResponseJson)
      self$`addedToMenu` <- ApiClient$new()$deserializeObj(UserBlueprintAddToMenuResponseObject$`addedToMenu`, "array[UserBlueprintAddedToMenuItem]", loadNamespace("datarobot.apicore"))
      self$`message` <- UserBlueprintAddToMenuResponseObject$`message`
      self$`notAddedToMenu` <- ApiClient$new()$deserializeObj(UserBlueprintAddToMenuResponseObject$`notAddedToMenu`, "array[UserBlueprintFailedToAddToMenuItem]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
