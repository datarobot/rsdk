# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureAssociationList
#'
#' @description FeatureAssociationList Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featurelistId  character The featurelist Id.
#'
#' @field hasFam  character Whether Feature Association Matrix is calculated for featurelist.
#'
#' @field title  character The name of featurelist.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureAssociationList <- R6::R6Class(
  "FeatureAssociationList",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(featurelistId = NULL, hasFam = NULL, title = NULL) {
      if (!is.null(`featurelistId`)) {
        stopifnot(is.character(`featurelistId`), length(`featurelistId`) == 1)
      }
      if (!is.null(`hasFam`)) {
        stopifnot(is.logical(`hasFam`), length(`hasFam`) == 1)
      }
      if (!is.null(`title`)) {
        stopifnot(is.character(`title`), length(`title`) == 1)
      }
    }
  ),
  public = list(
    `featurelistId` = NULL,
    `hasFam` = NULL,
    `title` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featurelistId The featurelist Id.
    #' @param hasFam Whether Feature Association Matrix is calculated for featurelist.
    #' @param title The name of featurelist.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featurelistId` = NULL, `hasFam` = NULL, `title` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`featurelistId`, `hasFam`, `title`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(featurelistId, hasFam, title)
      }
      self$`featurelistId` <- `featurelistId`
      self$`hasFam` <- `hasFam`
      self$`title` <- `title`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(featurelistId = self$`featurelistId`, hasFam = self$`hasFam`, title = self$`title`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featurelistId`)) {
          sprintf(
            '"featurelistId":
            "%s"
                  ',
            self$`featurelistId`
          )
        },
        if (!is.null(self$`hasFam`)) {
          sprintf(
            '"hasFam":
            %s
                  ',
            tolower(self$`hasFam`)
          )
        },
        if (!is.null(self$`title`)) {
          sprintf(
            '"title":
            "%s"
                  ',
            self$`title`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureAssociationListJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureAssociationListJson, validateParams = FALSE) {
      FeatureAssociationListObject <- jsonlite::fromJSON(FeatureAssociationListJson)
      self$`featurelistId` <- FeatureAssociationListObject$`featurelistId`
      self$`hasFam` <- FeatureAssociationListObject$`hasFam`
      self$`title` <- FeatureAssociationListObject$`title`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
