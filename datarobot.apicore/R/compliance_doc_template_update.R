# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ComplianceDocTemplateUpdate
#'
#' @description ComplianceDocTemplateUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field description  character [optional] New description for the template.
#'
#' @field labels  list( character ) [optional] Names of the labels to assign to the template.
#'
#' @field name  character [optional] New name for the template. Must be unique among templates created by the user.
#'
#' @field projectType  character [optional] Type of project template.
#'
#' @field sections  list( \link{OneOfSectionDataRobotSectionUserSectionCustomSectionTableOfContents} ) [optional] List of section objects representing the structure of the document. Each section can have sub-sections that have the same schema as the parent section, i.e., the structure is recursive. The number of nested sections allowed is 5. The total number of sections allowed is 500.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ComplianceDocTemplateUpdate <- R6::R6Class(
  "ComplianceDocTemplateUpdate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`description` = NULL, `labels` = NULL, `name` = NULL, `projectType` = NULL, `sections` = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`labels`) && length(`labels`) > 0) {
        stopifnot(is.vector(`labels`), sapply(`labels`, is.character))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`projectType`)) {
        stopifnot(is.character(`projectType`), length(`projectType`) == 1)
      }
      if (!is.null(`sections`)) {
        .setComplexProperty(typeList = list(SectionDataRobot, SectionUser, SectionCustom, SectionTableOfContents), propertyData = sections)
      }
    }
  ),
  public = list(
    `description` = NULL,
    `labels` = NULL,
    `name` = NULL,
    `projectType` = NULL,
    `sections` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param description New description for the template.
    #' @param labels Names of the labels to assign to the template.
    #' @param name New name for the template. Must be unique among templates created by the user.
    #' @param projectType Type of project template.
    #' @param sections List of section objects representing the structure of the document. Each section can have sub-sections that have the same schema as the parent section, i.e., the structure is recursive. The number of nested sections allowed is 5. The total number of sections allowed is 500.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `labels` = NULL, `name` = NULL, `projectType` = NULL, `sections` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(description, labels, name, projectType, sections)
      }
      self$`description` <- `description`
      self$`labels` <- `labels`
      self$`name` <- `name`
      self$`projectType` <- `projectType`
      self$`sections` <- sapply(`sections`, function(item) .setComplexProperty(typeList = list(SectionDataRobot, SectionUser, SectionCustom, SectionTableOfContents), propertyData = item))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(description = self$`description`, labels = self$`labels`, name = self$`name`, projectType = self$`projectType`, sections = self$`sections`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`labels`)) {
          sprintf(
            '"labels":
            [%s]
                  ',
            paste(unlist(lapply(self$`labels`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`projectType`)) {
          sprintf(
            '"projectType":
            "%s"
                  ',
            self$`projectType`
          )
        },
        if (!is.null(self$`sections`)) {
          sprintf(
            '"sections":
            [%s]
      ',
            paste(sapply(self$`sections`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ComplianceDocTemplateUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ComplianceDocTemplateUpdateJson, validateParams = FALSE) {
      ComplianceDocTemplateUpdateObject <- jsonlite::fromJSON(ComplianceDocTemplateUpdateJson)
      self$`description` <- ComplianceDocTemplateUpdateObject$`description`
      self$`labels` <- ApiClient$new()$deserializeObj(ComplianceDocTemplateUpdateObject$`labels`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`name` <- ComplianceDocTemplateUpdateObject$`name`
      self$`projectType` <- ComplianceDocTemplateUpdateObject$`projectType`
      self$`sections` <- ApiClient$new()$deserializeObj(ComplianceDocTemplateUpdateObject$`sections`, "array[OneOfSectionDataRobotSectionUserSectionCustomSectionTableOfContents]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
