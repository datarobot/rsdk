# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsBodyOneOf5
#'
#' @description CredentialsBodyOneOf5 Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field clientId  character [optional] Snowflake OAUTH client ID.
#'
#' @field clientSecret  character [optional] Snowflake OAUTH client secret.
#'
#' @field credentialType  character Credentials type.
#'
#' @field description  character [optional] Credentials description.
#'
#' @field name  character Credentials name.
#'
#' @field oauthIssuerType  character [optional] OAuth issuer type.
#'
#' @field oauthIssuerUrl  character [optional] OAuth issuer URL.
#'
#' @field oauthScopes  list( character ) [optional] OAuth scopes.
#'
#' @field snowflakeAccountName  character [optional] Snowflake account name.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsBodyOneOf5 <- R6::R6Class(
  "CredentialsBodyOneOf5",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(clientId = NULL, clientSecret = NULL, credentialType = NULL, description = NULL, name = NULL, oauthIssuerType = NULL, oauthIssuerUrl = NULL, oauthScopes = NULL, snowflakeAccountName = NULL) {
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`clientId`)) {
        stopifnot(is.character(`clientId`), length(`clientId`) == 1)
      }
      if (!is.null(`clientSecret`)) {
        stopifnot(is.character(`clientSecret`), length(`clientSecret`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`oauthIssuerType`)) {
        stopifnot(is.character(`oauthIssuerType`), length(`oauthIssuerType`) == 1)
      }
      if (!is.null(`oauthIssuerUrl`)) {
        stopifnot(is.character(`oauthIssuerUrl`), length(`oauthIssuerUrl`) == 1)
      }
      if (!is.null(`oauthScopes`)) {
        stopifnot(is.vector(`oauthScopes`))
      }
      if (!is.null(`snowflakeAccountName`)) {
        stopifnot(is.character(`snowflakeAccountName`), length(`snowflakeAccountName`) == 1)
      }
    }
  ),
  public = list(
    `clientId` = NULL,
    `clientSecret` = NULL,
    `credentialType` = NULL,
    `description` = NULL,
    `name` = NULL,
    `oauthIssuerType` = NULL,
    `oauthIssuerUrl` = NULL,
    `oauthScopes` = NULL,
    `snowflakeAccountName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param clientId Snowflake OAUTH client ID.
    #' @param clientSecret Snowflake OAUTH client secret.
    #' @param credentialType Credentials type.
    #' @param description Credentials description.
    #' @param name Credentials name.
    #' @param oauthIssuerType OAuth issuer type.
    #' @param oauthIssuerUrl OAuth issuer URL.
    #' @param oauthScopes OAuth scopes.
    #' @param snowflakeAccountName Snowflake account name.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialType` = NULL, `name` = NULL, `clientId` = NULL, `clientSecret` = NULL, `description` = NULL, `oauthIssuerType` = NULL, `oauthIssuerUrl` = NULL, `oauthScopes` = NULL, `snowflakeAccountName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`credentialType`, `name`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(clientId, clientSecret, credentialType, description, name, oauthIssuerType, oauthIssuerUrl, oauthScopes, snowflakeAccountName)
      }
      self$`clientId` <- `clientId`
      self$`clientSecret` <- `clientSecret`
      self$`credentialType` <- `credentialType`
      self$`description` <- `description`
      self$`name` <- `name`
      self$`oauthIssuerType` <- `oauthIssuerType`
      self$`oauthIssuerUrl` <- `oauthIssuerUrl`
      sapply(`oauthScopes`, function(x) stopifnot(is.character(x)))
      self$`snowflakeAccountName` <- `snowflakeAccountName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(clientId = self$`clientId`, clientSecret = self$`clientSecret`, credentialType = self$`credentialType`, description = self$`description`, name = self$`name`, oauthIssuerType = self$`oauthIssuerType`, oauthIssuerUrl = self$`oauthIssuerUrl`, oauthScopes = self$`oauthScopes`, snowflakeAccountName = self$`snowflakeAccountName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`clientId`)) {
          sprintf(
            '"clientId":
            "%s"
                  ',
            self$`clientId`
          )
        },
        if (!is.null(self$`clientSecret`)) {
          sprintf(
            '"clientSecret":
            "%s"
                  ',
            self$`clientSecret`
          )
        },
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`oauthIssuerType`)) {
          sprintf(
            '"oauthIssuerType":
            "%s"
                  ',
            self$`oauthIssuerType`
          )
        },
        if (!is.null(self$`oauthIssuerUrl`)) {
          sprintf(
            '"oauthIssuerUrl":
            "%s"
                  ',
            self$`oauthIssuerUrl`
          )
        },
        if (!is.null(self$`oauthScopes`)) {
          sprintf(
            '"oauthScopes":
              [%s]
            ',
            paste(unlist(lapply(self$`oauthScopes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`snowflakeAccountName`)) {
          sprintf(
            '"snowflakeAccountName":
            "%s"
                  ',
            self$`snowflakeAccountName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsBodyOneOf5Json A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsBodyOneOf5Json, validateParams = FALSE) {
      CredentialsBodyOneOf5Object <- jsonlite::fromJSON(CredentialsBodyOneOf5Json)
      self$`clientId` <- CredentialsBodyOneOf5Object$`clientId`
      self$`clientSecret` <- CredentialsBodyOneOf5Object$`clientSecret`
      self$`credentialType` <- CredentialsBodyOneOf5Object$`credentialType`
      self$`description` <- CredentialsBodyOneOf5Object$`description`
      self$`name` <- CredentialsBodyOneOf5Object$`name`
      self$`oauthIssuerType` <- CredentialsBodyOneOf5Object$`oauthIssuerType`
      self$`oauthIssuerUrl` <- CredentialsBodyOneOf5Object$`oauthIssuerUrl`
      self$`oauthScopes` <- ApiClient$new()$deserializeObj(CredentialsBodyOneOf5Object$`oauthScopes`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`snowflakeAccountName` <- CredentialsBodyOneOf5Object$`snowflakeAccountName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
