# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PerClassFairness
#'
#' @description PerClassFairness Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field absoluteValue  numeric The absolute fairness score for the class.
#'
#' @field className  character Name of the protected class the score is calculated for.
#'
#' @field entriesCount  integer The number of entries of the class in the analysed data.
#'
#' @field isStatisticallySignificant  character Flag to tell whether the score can be treated as statistically significant. In other words, whether we are confident enough with the score for this protected class.
#'
#' @field value  numeric The relative fairness score for the class.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PerClassFairness <- R6::R6Class(
  "PerClassFairness",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(absoluteValue = NULL, className = NULL, entriesCount = NULL, isStatisticallySignificant = NULL, value = NULL) {
      if (!is.null(`absoluteValue`)) {
      }
      if (!is.null(`className`)) {
        stopifnot(is.character(`className`), length(`className`) == 1)
      }
      if (!is.null(`entriesCount`)) {
        stopifnot(is.numeric(`entriesCount`), length(`entriesCount`) == 1)
      }
      if (!is.null(`isStatisticallySignificant`)) {
        stopifnot(is.logical(`isStatisticallySignificant`), length(`isStatisticallySignificant`) == 1)
      }
      if (!is.null(`value`)) {
      }
    }
  ),
  public = list(
    `absoluteValue` = NULL,
    `className` = NULL,
    `entriesCount` = NULL,
    `isStatisticallySignificant` = NULL,
    `value` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param absoluteValue The absolute fairness score for the class.
    #' @param className Name of the protected class the score is calculated for.
    #' @param entriesCount The number of entries of the class in the analysed data.
    #' @param isStatisticallySignificant Flag to tell whether the score can be treated as statistically significant. In other words, whether we are confident enough with the score for this protected class.
    #' @param value The relative fairness score for the class.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`absoluteValue` = NULL, `className` = NULL, `entriesCount` = NULL, `isStatisticallySignificant` = NULL, `value` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`absoluteValue`, `className`, `entriesCount`, `isStatisticallySignificant`, `value`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(absoluteValue, className, entriesCount, isStatisticallySignificant, value)
      }
      self$`absoluteValue` <- `absoluteValue`
      self$`className` <- `className`
      self$`entriesCount` <- `entriesCount`
      self$`isStatisticallySignificant` <- `isStatisticallySignificant`
      self$`value` <- `value`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(absoluteValue = self$`absoluteValue`, className = self$`className`, entriesCount = self$`entriesCount`, isStatisticallySignificant = self$`isStatisticallySignificant`, value = self$`value`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`absoluteValue`)) {
          sprintf(
            '"absoluteValue":
            %d
                  ',
            self$`absoluteValue`
          )
        },
        if (!is.null(self$`className`)) {
          sprintf(
            '"className":
            "%s"
                  ',
            self$`className`
          )
        },
        if (!is.null(self$`entriesCount`)) {
          sprintf(
            '"entriesCount":
            %d
                  ',
            self$`entriesCount`
          )
        },
        if (!is.null(self$`isStatisticallySignificant`)) {
          sprintf(
            '"isStatisticallySignificant":
            %s
                  ',
            tolower(self$`isStatisticallySignificant`)
          )
        },
        if (!is.null(self$`value`)) {
          sprintf(
            '"value":
            %d
                  ',
            self$`value`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PerClassFairnessJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PerClassFairnessJson, validateParams = FALSE) {
      PerClassFairnessObject <- jsonlite::fromJSON(PerClassFairnessJson)
      self$`absoluteValue` <- PerClassFairnessObject$`absoluteValue`
      self$`className` <- PerClassFairnessObject$`className`
      self$`entriesCount` <- PerClassFairnessObject$`entriesCount`
      self$`isStatisticallySignificant` <- PerClassFairnessObject$`isStatisticallySignificant`
      self$`value` <- PerClassFairnessObject$`value`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
