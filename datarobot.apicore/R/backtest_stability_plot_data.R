# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BacktestStabilityPlotData
#'
#' @description BacktestStabilityPlotData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtestIndex  integer An integer representing the index of the backtest, starting from 0. For holdout, this field will be null.
#'
#' @field partition  character Identifier of the partition. Can either identify a specific backtest (\&quot;backtest0\&quot;, \&quot;backtest1\&quot;, ...) or the holdout set (\&quot;holdout\&quot;).
#'
#' @field score  numeric Score for this partition. Can be null if the score is unavailable for this partition (e.g. holdout is locked or backtesting has not been run yet).
#'
#' @field scoringEndDate  character End date of the subset used for scoring.
#'
#' @field scoringStartDate  character Start date of the subset used for scoring.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BacktestStabilityPlotData <- R6::R6Class(
  "BacktestStabilityPlotData",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(backtestIndex = NULL, partition = NULL, score = NULL, scoringEndDate = NULL, scoringStartDate = NULL) {
      if (!is.null(`backtestIndex`)) {
        stopifnot(is.numeric(`backtestIndex`), length(`backtestIndex`) == 1)
      }
      if (!is.null(`partition`)) {
        stopifnot(is.character(`partition`), length(`partition`) == 1)
      }
      if (!is.null(`score`)) {
      }
      if (!is.null(`scoringEndDate`)) {
        stopifnot(inherits(`scoringEndDate`, "POSIXt"))
      }
      if (!is.null(`scoringStartDate`)) {
        stopifnot(inherits(`scoringStartDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `backtestIndex` = NULL,
    `partition` = NULL,
    `score` = NULL,
    `scoringEndDate` = NULL,
    `scoringStartDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtestIndex An integer representing the index of the backtest, starting from 0. For holdout, this field will be null.
    #' @param partition Identifier of the partition. Can either identify a specific backtest (\&quot;backtest0\&quot;, \&quot;backtest1\&quot;, ...) or the holdout set (\&quot;holdout\&quot;).
    #' @param score Score for this partition. Can be null if the score is unavailable for this partition (e.g. holdout is locked or backtesting has not been run yet).
    #' @param scoringEndDate End date of the subset used for scoring.
    #' @param scoringStartDate Start date of the subset used for scoring.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`backtestIndex` = NULL, `partition` = NULL, `score` = NULL, `scoringEndDate` = NULL, `scoringStartDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`backtestIndex`, `partition`, `score`, `scoringEndDate`, `scoringStartDate`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(backtestIndex, partition, score, scoringEndDate, scoringStartDate)
      }
      self$`backtestIndex` <- `backtestIndex`
      self$`partition` <- `partition`
      self$`score` <- `score`
      self$`scoringEndDate` <- `scoringEndDate`
      self$`scoringStartDate` <- `scoringStartDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(backtestIndex = self$`backtestIndex`, partition = self$`partition`, score = self$`score`, scoringEndDate = self$`scoringEndDate`, scoringStartDate = self$`scoringStartDate`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtestIndex`)) {
          sprintf(
            '"backtestIndex":
            %d
                  ',
            self$`backtestIndex`
          )
        },
        if (!is.null(self$`partition`)) {
          sprintf(
            '"partition":
            "%s"
                  ',
            self$`partition`
          )
        },
        if (!is.null(self$`score`)) {
          sprintf(
            '"score":
            %d
                  ',
            self$`score`
          )
        },
        if (!is.null(self$`scoringEndDate`)) {
          sprintf(
            '"scoringEndDate":
            "%s"
                  ',
            format(self$`scoringEndDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`scoringStartDate`)) {
          sprintf(
            '"scoringStartDate":
            "%s"
                  ',
            format(self$`scoringStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BacktestStabilityPlotDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BacktestStabilityPlotDataJson, validateParams = FALSE) {
      BacktestStabilityPlotDataObject <- jsonlite::fromJSON(BacktestStabilityPlotDataJson)
      self$`backtestIndex` <- BacktestStabilityPlotDataObject$`backtestIndex`
      self$`partition` <- BacktestStabilityPlotDataObject$`partition`
      self$`score` <- BacktestStabilityPlotDataObject$`score`
      self$`scoringEndDate` <- ParseRFC3339Timestamp(BacktestStabilityPlotDataObject$`scoringEndDate`)
      self$`scoringStartDate` <- ParseRFC3339Timestamp(BacktestStabilityPlotDataObject$`scoringStartDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
