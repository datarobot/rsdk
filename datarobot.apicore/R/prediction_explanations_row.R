# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanationsRow
#'
#' @description PredictionExplanationsRow Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field adjustedPrediction  numeric The exposure-adjusted output of the model for this row.
#'
#' @field adjustedPredictionValues  list( \link{PredictionExplanationsPredictionValues} ) The exposure-adjusted output of the model for this row.
#'
#' @field forecastDistance  integer Forecast distance for the row. For time series projects only.
#'
#' @field forecastPoint  character Forecast point for the row. For time series projects only.
#'
#' @field prediction  numeric The output of the model for this row.
#'
#' @field predictionExplanations  list( \link{PredictionExplanation} ) A list of prediction explanations.
#'
#' @field predictionThreshold  numeric The threshold value used for classification prediction.
#'
#' @field predictionValues  list( \link{PredictionExplanationsPredictionValues} ) A list of prediction values.
#'
#' @field rowId  integer Which row this PredictionExplanationsRow describes.
#'
#' @field seriesId  character The ID of the series value for the row in a multiseries project. For a single series project this will be null. For time series projects only.
#'
#' @field timestamp  character Timestamp for the row. For time series projects only.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanationsRow <- R6::R6Class(
  "PredictionExplanationsRow",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`adjustedPrediction` = NULL, `adjustedPredictionValues` = NULL, `forecastDistance` = NULL, `forecastPoint` = NULL, `prediction` = NULL, `predictionExplanations` = NULL, `predictionThreshold` = NULL, `predictionValues` = NULL, `rowId` = NULL, `seriesId` = NULL, `timestamp` = NULL) {
      if (!is.null(`adjustedPrediction`)) {
      }
      if (!is.null(`adjustedPredictionValues`)) {
        stopifnot(is.vector(`adjustedPredictionValues`), sapply(`adjustedPredictionValues`, R6::is.R6))
      }
      if (!is.null(`forecastDistance`)) {
        stopifnot(is.numeric(`forecastDistance`), length(`forecastDistance`) == 1)
      }
      if (!is.null(`forecastPoint`)) {
        stopifnot(is.character(`forecastPoint`), length(`forecastPoint`) == 1)
      }
      if (!is.null(`prediction`)) {
      }
      if (!is.null(`predictionExplanations`)) {
        stopifnot(is.vector(`predictionExplanations`), sapply(`predictionExplanations`, R6::is.R6))
      }
      if (!is.null(`predictionThreshold`)) {
      }
      if (!is.null(`predictionValues`)) {
        stopifnot(is.vector(`predictionValues`), sapply(`predictionValues`, R6::is.R6))
      }
      if (!is.null(`rowId`)) {
        stopifnot(is.numeric(`rowId`), length(`rowId`) == 1)
      }
      if (!is.null(`seriesId`)) {
        stopifnot(is.character(`seriesId`), length(`seriesId`) == 1)
      }
      if (!is.null(`timestamp`)) {
        stopifnot(is.character(`timestamp`), length(`timestamp`) == 1)
      }
    }
  ),
  public = list(
    `adjustedPrediction` = NULL,
    `adjustedPredictionValues` = NULL,
    `forecastDistance` = NULL,
    `forecastPoint` = NULL,
    `prediction` = NULL,
    `predictionExplanations` = NULL,
    `predictionThreshold` = NULL,
    `predictionValues` = NULL,
    `rowId` = NULL,
    `seriesId` = NULL,
    `timestamp` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param adjustedPrediction The exposure-adjusted output of the model for this row.
    #' @param adjustedPredictionValues The exposure-adjusted output of the model for this row.
    #' @param forecastDistance Forecast distance for the row. For time series projects only.
    #' @param forecastPoint Forecast point for the row. For time series projects only.
    #' @param prediction The output of the model for this row.
    #' @param predictionExplanations A list of prediction explanations.
    #' @param predictionThreshold The threshold value used for classification prediction.
    #' @param predictionValues A list of prediction values.
    #' @param rowId Which row this PredictionExplanationsRow describes.
    #' @param seriesId The ID of the series value for the row in a multiseries project. For a single series project this will be null. For time series projects only.
    #' @param timestamp Timestamp for the row. For time series projects only.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`adjustedPrediction` = NULL, `adjustedPredictionValues` = NULL, `forecastDistance` = NULL, `forecastPoint` = NULL, `prediction` = NULL, `predictionExplanations` = NULL, `predictionThreshold` = NULL, `predictionValues` = NULL, `rowId` = NULL, `seriesId` = NULL, `timestamp` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`adjustedPrediction`, `adjustedPredictionValues`, `forecastDistance`, `forecastPoint`, `prediction`, `predictionExplanations`, `predictionThreshold`, `predictionValues`, `rowId`, `seriesId`, `timestamp`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(adjustedPrediction, adjustedPredictionValues, forecastDistance, forecastPoint, prediction, predictionExplanations, predictionThreshold, predictionValues, rowId, seriesId, timestamp)
      }
      self$`adjustedPrediction` <- `adjustedPrediction`
      self$`adjustedPredictionValues` <- `adjustedPredictionValues`
      self$`forecastDistance` <- `forecastDistance`
      self$`forecastPoint` <- `forecastPoint`
      self$`prediction` <- `prediction`
      self$`predictionExplanations` <- `predictionExplanations`
      self$`predictionThreshold` <- `predictionThreshold`
      self$`predictionValues` <- `predictionValues`
      self$`rowId` <- `rowId`
      self$`seriesId` <- `seriesId`
      self$`timestamp` <- `timestamp`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(adjustedPrediction = self$`adjustedPrediction`, adjustedPredictionValues = self$`adjustedPredictionValues`, forecastDistance = self$`forecastDistance`, forecastPoint = self$`forecastPoint`, prediction = self$`prediction`, predictionExplanations = self$`predictionExplanations`, predictionThreshold = self$`predictionThreshold`, predictionValues = self$`predictionValues`, rowId = self$`rowId`, seriesId = self$`seriesId`, timestamp = self$`timestamp`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`adjustedPrediction`)) {
          sprintf(
            '"adjustedPrediction":
            %d
                  ',
            self$`adjustedPrediction`
          )
        },
        if (!is.null(self$`adjustedPredictionValues`)) {
          sprintf(
            '"adjustedPredictionValues":
            [%s]
      ',
            paste(sapply(self$`adjustedPredictionValues`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`forecastDistance`)) {
          sprintf(
            '"forecastDistance":
            %d
                  ',
            self$`forecastDistance`
          )
        },
        if (!is.null(self$`forecastPoint`)) {
          sprintf(
            '"forecastPoint":
            "%s"
                  ',
            self$`forecastPoint`
          )
        },
        if (!is.null(self$`prediction`)) {
          sprintf(
            '"prediction":
            %d
                  ',
            self$`prediction`
          )
        },
        if (!is.null(self$`predictionExplanations`)) {
          sprintf(
            '"predictionExplanations":
            [%s]
      ',
            paste(sapply(self$`predictionExplanations`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        },
        if (!is.null(self$`predictionValues`)) {
          sprintf(
            '"predictionValues":
            [%s]
      ',
            paste(sapply(self$`predictionValues`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`rowId`)) {
          sprintf(
            '"rowId":
            %d
                  ',
            self$`rowId`
          )
        },
        if (!is.null(self$`seriesId`)) {
          sprintf(
            '"seriesId":
            "%s"
                  ',
            self$`seriesId`
          )
        },
        if (!is.null(self$`timestamp`)) {
          sprintf(
            '"timestamp":
            "%s"
                  ',
            self$`timestamp`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationsRowJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationsRowJson, validateParams = FALSE) {
      PredictionExplanationsRowObject <- jsonlite::fromJSON(PredictionExplanationsRowJson)
      self$`adjustedPrediction` <- PredictionExplanationsRowObject$`adjustedPrediction`
      self$`adjustedPredictionValues` <- ApiClient$new()$deserializeObj(PredictionExplanationsRowObject$`adjustedPredictionValues`, "array[PredictionExplanationsPredictionValues]", loadNamespace("datarobot.apicore"))
      self$`forecastDistance` <- PredictionExplanationsRowObject$`forecastDistance`
      self$`forecastPoint` <- PredictionExplanationsRowObject$`forecastPoint`
      self$`prediction` <- PredictionExplanationsRowObject$`prediction`
      self$`predictionExplanations` <- ApiClient$new()$deserializeObj(PredictionExplanationsRowObject$`predictionExplanations`, "array[PredictionExplanation]", loadNamespace("datarobot.apicore"))
      self$`predictionThreshold` <- PredictionExplanationsRowObject$`predictionThreshold`
      self$`predictionValues` <- ApiClient$new()$deserializeObj(PredictionExplanationsRowObject$`predictionValues`, "array[PredictionExplanationsPredictionValues]", loadNamespace("datarobot.apicore"))
      self$`rowId` <- PredictionExplanationsRowObject$`rowId`
      self$`seriesId` <- PredictionExplanationsRowObject$`seriesId`
      self$`timestamp` <- PredictionExplanationsRowObject$`timestamp`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
