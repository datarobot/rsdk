# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsToAdd
#'
#' @description CredentialsToAdd Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field objectId  character Object ID identifying the object to be associated with the credentials.
#'
#' @field objectType  character Type of object associated with the credentials.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsToAdd <- R6::R6Class(
  "CredentialsToAdd",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`objectId` = NULL, `objectType` = NULL) {
      if (!is.null(`objectId`)) {
        stopifnot(is.character(`objectId`), length(`objectId`) == 1)
      }
      if (!is.null(`objectType`)) {
        stopifnot(is.character(`objectType`), length(`objectType`) == 1)
      }
    }
  ),
  public = list(
    `objectId` = NULL,
    `objectType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param objectId Object ID identifying the object to be associated with the credentials.
    #' @param objectType Type of object associated with the credentials.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`objectId` = NULL, `objectType` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`objectId`, `objectType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(objectId, objectType)
      }
      self$`objectId` <- `objectId`
      self$`objectType` <- `objectType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(objectId = self$`objectId`, objectType = self$`objectType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`objectId`)) {
          sprintf(
            '"objectId":
            "%s"
                  ',
            self$`objectId`
          )
        },
        if (!is.null(self$`objectType`)) {
          sprintf(
            '"objectType":
            "%s"
                  ',
            self$`objectType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsToAddJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsToAddJson, validateParams = FALSE) {
      CredentialsToAddObject <- jsonlite::fromJSON(CredentialsToAddJson)
      self$`objectId` <- CredentialsToAddObject$`objectId`
      self$`objectType` <- CredentialsToAddObject$`objectType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
