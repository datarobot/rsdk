# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title JDBCDataStoreDetails
#'
#' @description JDBCDataStoreDetails Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field driverId  character Driver ID.
#'
#' @field jdbcFieldSchemas  list( \link{JDBCFieldSchemas} ) [optional] Fields to show when creating a data store, their defaults, whether or not they are required, and more.
#'
#' @field jdbcFields  list( \link{JDBCFields} ) [optional] The fields used to create the JDBC URL, in the form of a JSON object where parameter name/value pairs are the items in the object. For example: &#x60;{\&quot;address\&quot;: localhost:5432, \&quot;database\&quot;: \&quot;fooBar\&quot;, \&quot;connectTimeout\&quot;: 10}&#x60;. In most cases, all keys in &#x60;jdbcFields&#x60; should be defined by a schema listed in &#x60;jdbcFieldSchemas&#x60; from &#x60;DriverConfiguration&#x60;. The request will be rejected if there are required parameters (as defined by &#x60;jdbcFieldSchemas&#x60;) that are not provided.
#'
#' @field jdbcUrl  character [optional] JDBC URL.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
JDBCDataStoreDetails <- R6::R6Class(
  "JDBCDataStoreDetails",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(driverId = NULL, jdbcFieldSchemas = NULL, jdbcFields = NULL, jdbcUrl = NULL) {
      if (!is.null(`driverId`)) {
        stopifnot(is.character(`driverId`), length(`driverId`) == 1)
      }
      if (!is.null(`jdbcFieldSchemas`)) {
        stopifnot(is.vector(`jdbcFieldSchemas`))
      }
      if (!is.null(`jdbcFields`)) {
        stopifnot(is.vector(`jdbcFields`))
      }
      if (!is.null(`jdbcUrl`)) {
        stopifnot(is.character(`jdbcUrl`), length(`jdbcUrl`) == 1)
      }
    }
  ),
  public = list(
    `driverId` = NULL,
    `jdbcFieldSchemas` = NULL,
    `jdbcFields` = NULL,
    `jdbcUrl` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param driverId Driver ID.
    #' @param jdbcFieldSchemas Fields to show when creating a data store, their defaults, whether or not they are required, and more.
    #' @param jdbcFields The fields used to create the JDBC URL, in the form of a JSON object where parameter name/value pairs are the items in the object. For example: &#x60;{\&quot;address\&quot;: localhost:5432, \&quot;database\&quot;: \&quot;fooBar\&quot;, \&quot;connectTimeout\&quot;: 10}&#x60;. In most cases, all keys in &#x60;jdbcFields&#x60; should be defined by a schema listed in &#x60;jdbcFieldSchemas&#x60; from &#x60;DriverConfiguration&#x60;. The request will be rejected if there are required parameters (as defined by &#x60;jdbcFieldSchemas&#x60;) that are not provided.
    #' @param jdbcUrl JDBC URL.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`driverId` = NULL, `jdbcFieldSchemas` = c(), `jdbcFields` = NULL, `jdbcUrl` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`driverId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(driverId, jdbcFieldSchemas, jdbcFields, jdbcUrl)
      }
      self$`driverId` <- `driverId`
      sapply(`jdbcFieldSchemas`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`jdbcFields`, function(x) stopifnot(R6::is.R6(x)))
      self$`jdbcUrl` <- `jdbcUrl`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(driverId = self$`driverId`, jdbcFieldSchemas = self$`jdbcFieldSchemas`, jdbcFields = self$`jdbcFields`, jdbcUrl = self$`jdbcUrl`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`driverId`)) {
          sprintf(
            '"driverId":
            "%s"
                  ',
            self$`driverId`
          )
        },
        if (!is.null(self$`jdbcFieldSchemas`)) {
          sprintf(
            '"jdbcFieldSchemas":
            [%s]
',
            paste(sapply(self$`jdbcFieldSchemas`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`jdbcFields`)) {
          sprintf(
            '"jdbcFields":
            [%s]
',
            paste(sapply(self$`jdbcFields`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`jdbcUrl`)) {
          sprintf(
            '"jdbcUrl":
            "%s"
                  ',
            self$`jdbcUrl`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param JDBCDataStoreDetailsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(JDBCDataStoreDetailsJson, validateParams = FALSE) {
      JDBCDataStoreDetailsObject <- jsonlite::fromJSON(JDBCDataStoreDetailsJson)
      self$`driverId` <- JDBCDataStoreDetailsObject$`driverId`
      self$`jdbcFieldSchemas` <- ApiClient$new()$deserializeObj(JDBCDataStoreDetailsObject$`jdbcFieldSchemas`, "array[JDBCFieldSchemas]", loadNamespace("datarobot.apicore"))
      self$`jdbcFields` <- ApiClient$new()$deserializeObj(JDBCDataStoreDetailsObject$`jdbcFields`, "array[JDBCFields]", loadNamespace("datarobot.apicore"))
      self$`jdbcUrl` <- JDBCDataStoreDetailsObject$`jdbcUrl`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
