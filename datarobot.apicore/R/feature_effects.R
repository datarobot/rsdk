# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureEffects
#'
#' @description FeatureEffects Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featureImpactScore  numeric Feature impact score.
#'
#' @field featureName  character Name of the feature.
#'
#' @field featureType  character Feature type, either numeric or categorical.
#'
#' @field isBinnable  character Whether values can be grouped into bins.
#'
#' @field isScalable  character Whether numeric feature values can be reported on a log scale.
#'
#' @field partialDependence  \link{PartialDependence} [optional]
#'
#' @field predictedVsActual  \link{PredictedVsActual} [optional]
#'
#' @field weightLabel  character Weight label if weight was configured for the project.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureEffects <- R6::R6Class(
  "FeatureEffects",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(featureImpactScore = NULL, featureName = NULL, featureType = NULL, isBinnable = NULL, isScalable = NULL, partialDependence = NULL, predictedVsActual = NULL, weightLabel = NULL) {
      if (!is.null(`featureImpactScore`)) {
      }
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`featureType`)) {
        stopifnot(is.character(`featureType`), length(`featureType`) == 1)
      }
      if (!is.null(`isBinnable`)) {
        stopifnot(is.logical(`isBinnable`), length(`isBinnable`) == 1)
      }
      if (!is.null(`isScalable`)) {
        stopifnot(is.logical(`isScalable`), length(`isScalable`) == 1)
      }
      if (!is.null(`weightLabel`)) {
        stopifnot(is.character(`weightLabel`), length(`weightLabel`) == 1)
      }
      if (!is.null(`partialDependence`)) {
        stopifnot(R6::is.R6(`partialDependence`))
      }
      if (!is.null(`predictedVsActual`)) {
        stopifnot(R6::is.R6(`predictedVsActual`))
      }
    }
  ),
  public = list(
    `featureImpactScore` = NULL,
    `featureName` = NULL,
    `featureType` = NULL,
    `isBinnable` = NULL,
    `isScalable` = NULL,
    `partialDependence` = NULL,
    `predictedVsActual` = NULL,
    `weightLabel` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featureImpactScore Feature impact score.
    #' @param featureName Name of the feature.
    #' @param featureType Feature type, either numeric or categorical.
    #' @param isBinnable Whether values can be grouped into bins.
    #' @param isScalable Whether numeric feature values can be reported on a log scale.
    #' @param partialDependence
    #' @param predictedVsActual
    #' @param weightLabel Weight label if weight was configured for the project.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featureImpactScore` = NULL, `featureName` = NULL, `featureType` = NULL, `isBinnable` = NULL, `isScalable` = NULL, `weightLabel` = NULL, `partialDependence` = NULL, `predictedVsActual` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`featureImpactScore`, `featureName`, `featureType`, `isBinnable`, `isScalable`, `weightLabel`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(featureImpactScore, featureName, featureType, isBinnable, isScalable, partialDependence, predictedVsActual, weightLabel)
      }
      self$`featureImpactScore` <- `featureImpactScore`
      self$`featureName` <- `featureName`
      self$`featureType` <- `featureType`
      self$`isBinnable` <- `isBinnable`
      self$`isScalable` <- `isScalable`
      self$`partialDependence` <- `partialDependence`
      self$`predictedVsActual` <- `predictedVsActual`
      self$`weightLabel` <- `weightLabel`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(featureImpactScore = self$`featureImpactScore`, featureName = self$`featureName`, featureType = self$`featureType`, isBinnable = self$`isBinnable`, isScalable = self$`isScalable`, partialDependence = self$`partialDependence`, predictedVsActual = self$`predictedVsActual`, weightLabel = self$`weightLabel`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featureImpactScore`)) {
          sprintf(
            '"featureImpactScore":
            %d
                  ',
            self$`featureImpactScore`
          )
        },
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`featureType`)) {
          sprintf(
            '"featureType":
            "%s"
                  ',
            self$`featureType`
          )
        },
        if (!is.null(self$`isBinnable`)) {
          sprintf(
            '"isBinnable":
            %s
                  ',
            tolower(self$`isBinnable`)
          )
        },
        if (!is.null(self$`isScalable`)) {
          sprintf(
            '"isScalable":
            %s
                  ',
            tolower(self$`isScalable`)
          )
        },
        if (!is.null(self$`partialDependence`)) {
          sprintf(
            '"partialDependence":
          %s
        ',
            jsonlite::toJSON(self$`partialDependence`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`predictedVsActual`)) {
          sprintf(
            '"predictedVsActual":
          %s
        ',
            jsonlite::toJSON(self$`predictedVsActual`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`weightLabel`)) {
          sprintf(
            '"weightLabel":
            "%s"
                  ',
            self$`weightLabel`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureEffectsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureEffectsJson, validateParams = FALSE) {
      FeatureEffectsObject <- jsonlite::fromJSON(FeatureEffectsJson)
      self$`featureImpactScore` <- FeatureEffectsObject$`featureImpactScore`
      self$`featureName` <- FeatureEffectsObject$`featureName`
      self$`featureType` <- FeatureEffectsObject$`featureType`
      self$`isBinnable` <- FeatureEffectsObject$`isBinnable`
      self$`isScalable` <- FeatureEffectsObject$`isScalable`
      self$`partialDependence` <- PartialDependence$new()$fromJSON(jsonlite::toJSON(FeatureEffectsObject$partialDependence, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`predictedVsActual` <- PredictedVsActual$new()$fromJSON(jsonlite::toJSON(FeatureEffectsObject$predictedVsActual, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`weightLabel` <- FeatureEffectsObject$`weightLabel`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
