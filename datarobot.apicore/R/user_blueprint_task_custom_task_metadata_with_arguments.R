# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintTaskCustomTaskMetadataWithArguments
#'
#' @description UserBlueprintTaskCustomTaskMetadataWithArguments Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field id  character Id of the custom task version. The ID can be latest_&lt;task_id&gt; which implies to use the latest version of that custom task.
#'
#' @field label  character The name of the custom task version.
#'
#' @field versionMajor  integer Major version of the custom task.
#'
#' @field versionMinor  integer Minor version of the custom task.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintTaskCustomTaskMetadataWithArguments <- R6::R6Class(
  "UserBlueprintTaskCustomTaskMetadataWithArguments",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(id = NULL, label = NULL, versionMajor = NULL, versionMinor = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`versionMajor`)) {
        stopifnot(is.numeric(`versionMajor`), length(`versionMajor`) == 1)
      }
      if (!is.null(`versionMinor`)) {
        stopifnot(is.numeric(`versionMinor`), length(`versionMinor`) == 1)
      }
    }
  ),
  public = list(
    `id` = NULL,
    `label` = NULL,
    `versionMajor` = NULL,
    `versionMinor` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param id Id of the custom task version. The ID can be latest_&lt;task_id&gt; which implies to use the latest version of that custom task.
    #' @param label The name of the custom task version.
    #' @param versionMajor Major version of the custom task.
    #' @param versionMinor Minor version of the custom task.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `label` = NULL, `versionMajor` = NULL, `versionMinor` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`id`, `label`, `versionMajor`, `versionMinor`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(id, label, versionMajor, versionMinor)
      }
      self$`id` <- `id`
      self$`label` <- `label`
      self$`versionMajor` <- `versionMajor`
      self$`versionMinor` <- `versionMinor`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(id = self$`id`, label = self$`label`, versionMajor = self$`versionMajor`, versionMinor = self$`versionMinor`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`versionMajor`)) {
          sprintf(
            '"versionMajor":
            %d
                  ',
            self$`versionMajor`
          )
        },
        if (!is.null(self$`versionMinor`)) {
          sprintf(
            '"versionMinor":
            %d
                  ',
            self$`versionMinor`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintTaskCustomTaskMetadataWithArgumentsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintTaskCustomTaskMetadataWithArgumentsJson, validateParams = FALSE) {
      UserBlueprintTaskCustomTaskMetadataWithArgumentsObject <- jsonlite::fromJSON(UserBlueprintTaskCustomTaskMetadataWithArgumentsJson)
      self$`id` <- UserBlueprintTaskCustomTaskMetadataWithArgumentsObject$`id`
      self$`label` <- UserBlueprintTaskCustomTaskMetadataWithArgumentsObject$`label`
      self$`versionMajor` <- UserBlueprintTaskCustomTaskMetadataWithArgumentsObject$`versionMajor`
      self$`versionMinor` <- UserBlueprintTaskCustomTaskMetadataWithArgumentsObject$`versionMinor`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
