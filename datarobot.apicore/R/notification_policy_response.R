# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotificationPolicyResponse
#'
#' @description NotificationPolicyResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field active  character Defines if the notification policy is active or not.
#'
#' @field channel  \link{NotificationChannel}
#'
#' @field channelId  character The id of the notification channel to be used to send the notification.
#'
#' @field created  character The date when the policy was created.
#'
#' @field eventGroup  \link{EventGroup}
#'
#' @field eventType  \link{EventType}
#'
#' @field id  character The id of the policy.
#'
#' @field lastTriggered  character The date when the last notification with the policy was triggered.
#'
#' @field name  character The name of the notification policy.
#'
#' @field orgId  character The id of the organization that owns the notification policy.
#'
#' @field uid  character The identifier of the user who created the policy.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotificationPolicyResponse <- R6::R6Class(
  "NotificationPolicyResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`active` = NULL, `channel` = NULL, `channelId` = NULL, `created` = NULL, `eventGroup` = NULL, `eventType` = NULL, `id` = NULL, `lastTriggered` = NULL, `name` = NULL, `orgId` = NULL, `uid` = NULL) {
      if (!is.null(`active`)) {
        stopifnot(is.logical(`active`), length(`active`) == 1)
      }
      if (!is.null(`channel`)) {
        stopifnot(R6::is.R6(`channel`))
      }
      if (!is.null(`channelId`)) {
        stopifnot(is.character(`channelId`), length(`channelId`) == 1)
      }
      if (!is.null(`created`)) {
        stopifnot(inherits(`created`, "POSIXt"))
      }
      if (!is.null(`eventGroup`)) {
        stopifnot(R6::is.R6(`eventGroup`))
      }
      if (!is.null(`eventType`)) {
        stopifnot(R6::is.R6(`eventType`))
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`lastTriggered`)) {
        stopifnot(inherits(`lastTriggered`, "POSIXt"))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
      if (!is.null(`uid`)) {
        stopifnot(is.character(`uid`), length(`uid`) == 1)
      }
    }
  ),
  public = list(
    `active` = NULL,
    `channel` = NULL,
    `channelId` = NULL,
    `created` = NULL,
    `eventGroup` = NULL,
    `eventType` = NULL,
    `id` = NULL,
    `lastTriggered` = NULL,
    `name` = NULL,
    `orgId` = NULL,
    `uid` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param active Defines if the notification policy is active or not.
    #' @param channel
    #' @param channelId The id of the notification channel to be used to send the notification.
    #' @param created The date when the policy was created.
    #' @param eventGroup
    #' @param eventType
    #' @param id The id of the policy.
    #' @param lastTriggered The date when the last notification with the policy was triggered.
    #' @param name The name of the notification policy.
    #' @param orgId The id of the organization that owns the notification policy.
    #' @param uid The identifier of the user who created the policy.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`active` = NULL, `channel` = NULL, `channelId` = NULL, `created` = NULL, `eventGroup` = NULL, `eventType` = NULL, `id` = NULL, `lastTriggered` = NULL, `name` = NULL, `orgId` = NULL, `uid` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`active`, `channel`, `channelId`, `created`, `eventGroup`, `eventType`, `id`, `lastTriggered`, `name`, `orgId`, `uid`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(active, channel, channelId, created, eventGroup, eventType, id, lastTriggered, name, orgId, uid)
      }
      self$`active` <- `active`
      self$`channel` <- `channel`
      self$`channelId` <- `channelId`
      self$`created` <- `created`
      self$`eventGroup` <- `eventGroup`
      self$`eventType` <- `eventType`
      self$`id` <- `id`
      self$`lastTriggered` <- `lastTriggered`
      self$`name` <- `name`
      self$`orgId` <- `orgId`
      self$`uid` <- `uid`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(active = self$`active`, channel = self$`channel`, channelId = self$`channelId`, created = self$`created`, eventGroup = self$`eventGroup`, eventType = self$`eventType`, id = self$`id`, lastTriggered = self$`lastTriggered`, name = self$`name`, orgId = self$`orgId`, uid = self$`uid`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`active`)) {
          sprintf(
            '"active":
            %s
                  ',
            tolower(self$`active`)
          )
        },
        if (!is.null(self$`channel`)) {
          sprintf(
            '"channel":
            %s
      ',
            jsonlite::toJSON(self$`channel`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`channelId`)) {
          sprintf(
            '"channelId":
            "%s"
                  ',
            self$`channelId`
          )
        },
        if (!is.null(self$`created`)) {
          sprintf(
            '"created":
            "%s"
                  ',
            format(self$`created`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`eventGroup`)) {
          sprintf(
            '"eventGroup":
            %s
      ',
            jsonlite::toJSON(self$`eventGroup`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`eventType`)) {
          sprintf(
            '"eventType":
            %s
      ',
            jsonlite::toJSON(self$`eventType`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`lastTriggered`)) {
          sprintf(
            '"lastTriggered":
            "%s"
                  ',
            format(self$`lastTriggered`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        },
        if (!is.null(self$`uid`)) {
          sprintf(
            '"uid":
            "%s"
                  ',
            self$`uid`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotificationPolicyResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotificationPolicyResponseJson, validateParams = FALSE) {
      NotificationPolicyResponseObject <- jsonlite::fromJSON(NotificationPolicyResponseJson)
      self$`active` <- NotificationPolicyResponseObject$`active`
      self$`channel` <- NotificationChannel$new()$fromJSON(jsonlite::toJSON(NotificationPolicyResponseObject$channel, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`channelId` <- NotificationPolicyResponseObject$`channelId`
      self$`created` <- ParseRFC3339Timestamp(NotificationPolicyResponseObject$`created`)
      self$`eventGroup` <- EventGroup$new()$fromJSON(jsonlite::toJSON(NotificationPolicyResponseObject$eventGroup, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`eventType` <- EventType$new()$fromJSON(jsonlite::toJSON(NotificationPolicyResponseObject$eventType, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`id` <- NotificationPolicyResponseObject$`id`
      self$`lastTriggered` <- ParseRFC3339Timestamp(NotificationPolicyResponseObject$`lastTriggered`)
      self$`name` <- NotificationPolicyResponseObject$`name`
      self$`orgId` <- NotificationPolicyResponseObject$`orgId`
      self$`uid` <- NotificationPolicyResponseObject$`uid`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
