# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeletedDeploymentResponse
#'
#' @description DeletedDeploymentResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field deletedAt  character The date and time of when the deployment was deleted, in ISO 8601 format.
#'
#' @field id  character ID of the deployment.
#'
#' @field label  character Label of the deployment.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeletedDeploymentResponse <- R6::R6Class(
  "DeletedDeploymentResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`deletedAt` = NULL, `id` = NULL, `label` = NULL) {
      if (!is.null(`deletedAt`)) {
        stopifnot(inherits(`deletedAt`, "POSIXt"))
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
    }
  ),
  public = list(
    `deletedAt` = NULL,
    `id` = NULL,
    `label` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param deletedAt The date and time of when the deployment was deleted, in ISO 8601 format.
    #' @param id ID of the deployment.
    #' @param label Label of the deployment.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`deletedAt` = NULL, `id` = NULL, `label` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`deletedAt`, `id`, `label`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(deletedAt, id, label)
      }
      self$`deletedAt` <- `deletedAt`
      self$`id` <- `id`
      self$`label` <- `label`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(deletedAt = self$`deletedAt`, id = self$`id`, label = self$`label`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`deletedAt`)) {
          sprintf(
            '"deletedAt":
            "%s"
                  ',
            format(self$`deletedAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeletedDeploymentResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeletedDeploymentResponseJson, validateParams = FALSE) {
      DeletedDeploymentResponseObject <- jsonlite::fromJSON(DeletedDeploymentResponseJson)
      self$`deletedAt` <- ParseRFC3339Timestamp(DeletedDeploymentResponseObject$`deletedAt`)
      self$`id` <- DeletedDeploymentResponseObject$`id`
      self$`label` <- DeletedDeploymentResponseObject$`label`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
