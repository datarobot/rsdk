# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Trigger
#'
#' @description Trigger Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field schedule  \link{Schedule}
#'
#' @field statusDeclinesToFailing  character Identifies when trigger type is based on deployment a health status, whether the policy will run when health status declines to failing.
#'
#' @field statusDeclinesToWarning  character Identifies when trigger type is based on deployment a health status, whether the policy will run when health status declines to warning.
#'
#' @field type  character Type of retraining policy trigger.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Trigger <- R6::R6Class(
  "Trigger",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`schedule` = NULL, `statusDeclinesToFailing` = NULL, `statusDeclinesToWarning` = NULL, `type` = NULL) {
      if (!is.null(`schedule`)) {
        stopifnot(R6::is.R6(`schedule`))
      }
      if (!is.null(`statusDeclinesToFailing`)) {
        stopifnot(is.logical(`statusDeclinesToFailing`), length(`statusDeclinesToFailing`) == 1)
      }
      if (!is.null(`statusDeclinesToWarning`)) {
        stopifnot(is.logical(`statusDeclinesToWarning`), length(`statusDeclinesToWarning`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
    }
  ),
  public = list(
    `schedule` = NULL,
    `statusDeclinesToFailing` = NULL,
    `statusDeclinesToWarning` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param schedule
    #' @param statusDeclinesToFailing Identifies when trigger type is based on deployment a health status, whether the policy will run when health status declines to failing.
    #' @param statusDeclinesToWarning Identifies when trigger type is based on deployment a health status, whether the policy will run when health status declines to warning.
    #' @param type Type of retraining policy trigger.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`schedule` = NULL, `statusDeclinesToFailing` = NULL, `statusDeclinesToWarning` = NULL, `type` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`schedule`, `statusDeclinesToFailing`, `statusDeclinesToWarning`, `type`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(schedule, statusDeclinesToFailing, statusDeclinesToWarning, type)
      }
      self$`schedule` <- `schedule`
      self$`statusDeclinesToFailing` <- `statusDeclinesToFailing`
      self$`statusDeclinesToWarning` <- `statusDeclinesToWarning`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(schedule = self$`schedule`, statusDeclinesToFailing = self$`statusDeclinesToFailing`, statusDeclinesToWarning = self$`statusDeclinesToWarning`, type = self$`type`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`schedule`)) {
          sprintf(
            '"schedule":
            %s
      ',
            jsonlite::toJSON(self$`schedule`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`statusDeclinesToFailing`)) {
          sprintf(
            '"statusDeclinesToFailing":
            %s
                  ',
            tolower(self$`statusDeclinesToFailing`)
          )
        },
        if (!is.null(self$`statusDeclinesToWarning`)) {
          sprintf(
            '"statusDeclinesToWarning":
            %s
                  ',
            tolower(self$`statusDeclinesToWarning`)
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TriggerJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TriggerJson, validateParams = FALSE) {
      TriggerObject <- jsonlite::fromJSON(TriggerJson)
      self$`schedule` <- Schedule$new()$fromJSON(jsonlite::toJSON(TriggerObject$schedule, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`statusDeclinesToFailing` <- TriggerObject$`statusDeclinesToFailing`
      self$`statusDeclinesToWarning` <- TriggerObject$`statusDeclinesToWarning`
      self$`type` <- TriggerObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
