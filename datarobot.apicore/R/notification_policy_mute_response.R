# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotificationPolicyMuteResponse
#'
#' @description NotificationPolicyMuteResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field entityId  character The id of the entity to mute the notification for
#'
#' @field id  character The id of the notification policy mute
#'
#' @field policyId  character The id of the policy to mute notification for
#'
#' @field uid  character The uid of the notification policy mute
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotificationPolicyMuteResponse <- R6::R6Class(
  "NotificationPolicyMuteResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(entityId = NULL, id = NULL, policyId = NULL, uid = NULL) {
      if (!is.null(`entityId`)) {
        stopifnot(is.character(`entityId`), length(`entityId`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`policyId`)) {
        stopifnot(is.character(`policyId`), length(`policyId`) == 1)
      }
      if (!is.null(`uid`)) {
        stopifnot(is.character(`uid`), length(`uid`) == 1)
      }
    }
  ),
  public = list(
    `entityId` = NULL,
    `id` = NULL,
    `policyId` = NULL,
    `uid` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param entityId The id of the entity to mute the notification for
    #' @param id The id of the notification policy mute
    #' @param policyId The id of the policy to mute notification for
    #' @param uid The uid of the notification policy mute
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`entityId` = NULL, `id` = NULL, `policyId` = NULL, `uid` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`entityId`, `id`, `policyId`, `uid`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(entityId, id, policyId, uid)
      }
      self$`entityId` <- `entityId`
      self$`id` <- `id`
      self$`policyId` <- `policyId`
      self$`uid` <- `uid`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(entityId = self$`entityId`, id = self$`id`, policyId = self$`policyId`, uid = self$`uid`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`entityId`)) {
          sprintf(
            '"entityId":
            "%s"
                  ',
            self$`entityId`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`policyId`)) {
          sprintf(
            '"policyId":
            "%s"
                  ',
            self$`policyId`
          )
        },
        if (!is.null(self$`uid`)) {
          sprintf(
            '"uid":
            "%s"
                  ',
            self$`uid`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotificationPolicyMuteResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotificationPolicyMuteResponseJson, validateParams = FALSE) {
      NotificationPolicyMuteResponseObject <- jsonlite::fromJSON(NotificationPolicyMuteResponseJson)
      self$`entityId` <- NotificationPolicyMuteResponseObject$`entityId`
      self$`id` <- NotificationPolicyMuteResponseObject$`id`
      self$`policyId` <- NotificationPolicyMuteResponseObject$`policyId`
      self$`uid` <- NotificationPolicyMuteResponseObject$`uid`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
