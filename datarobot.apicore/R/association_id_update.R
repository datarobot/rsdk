# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AssociationIDUpdate
#'
#' @description AssociationIDUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field columnNames  list( character ) [optional] List of column names used to represent an association id. Currently, exactly one column name may be provided.
#'
#' @field requiredInPredictionRequests  character [optional] Whether or not association id values are required in each prediction request for this deployment. Note that you may not change an association ID&#39;s column names after they have been set and predictions including those columns have been made
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AssociationIDUpdate <- R6::R6Class(
  "AssociationIDUpdate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`columnNames` = NULL, `requiredInPredictionRequests` = NULL) {
      if (!is.null(`columnNames`) && length(`columnNames`) > 0) {
        stopifnot(is.vector(`columnNames`), sapply(`columnNames`, is.character))
      }
      if (!is.null(`requiredInPredictionRequests`)) {
        stopifnot(is.logical(`requiredInPredictionRequests`), length(`requiredInPredictionRequests`) == 1)
      }
    }
  ),
  public = list(
    `columnNames` = NULL,
    `requiredInPredictionRequests` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param columnNames List of column names used to represent an association id. Currently, exactly one column name may be provided.
    #' @param requiredInPredictionRequests Whether or not association id values are required in each prediction request for this deployment. Note that you may not change an association ID&#39;s column names after they have been set and predictions including those columns have been made
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`columnNames` = NULL, `requiredInPredictionRequests` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(columnNames, requiredInPredictionRequests)
      }
      self$`columnNames` <- `columnNames`
      self$`requiredInPredictionRequests` <- `requiredInPredictionRequests`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(columnNames = self$`columnNames`, requiredInPredictionRequests = self$`requiredInPredictionRequests`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`columnNames`)) {
          sprintf(
            '"columnNames":
            [%s]
                  ',
            paste(unlist(lapply(self$`columnNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`requiredInPredictionRequests`)) {
          sprintf(
            '"requiredInPredictionRequests":
            %s
                  ',
            tolower(self$`requiredInPredictionRequests`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AssociationIDUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AssociationIDUpdateJson, validateParams = FALSE) {
      AssociationIDUpdateObject <- jsonlite::fromJSON(AssociationIDUpdateJson)
      self$`columnNames` <- ApiClient$new()$deserializeObj(AssociationIDUpdateObject$`columnNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`requiredInPredictionRequests` <- AssociationIDUpdateObject$`requiredInPredictionRequests`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
