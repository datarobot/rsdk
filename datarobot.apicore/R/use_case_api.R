# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCase operations
#' @description datarobot.apicore.UseCase
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#' @export
UseCaseApi <- R6::R6Class(
  "UseCaseApi",
  public = list(
    apiClient = NULL,

    #' @param apiClient A configurable `ApiClient` instance. If none provided, a new client with default configuration will be created.
    initialize = function(apiClient) {
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      } else {
        self$apiClient <- ApiClient$new()
      }
    },
    #' @description Calculate value of template with given template parameters.
    #' Produces: "application/json"
    #'
    #' @details Calculate value of template with given template parameters.
    #' @param accuracyImprovement numeric. Accuracy improvement
    #' @param decisionsCount integer. Estimated number of decisions per year
    #' @param templateType Enum < [classification, regression] > The name of the use case value template to be retrieved
    #' @param incorrectDecisionsCount integer. Estimated number of incorrect decisions per year. Required for templateType CLASSIFICATION
    #' @param incorrectDecisionCost numeric. Estimated cost of an individual incorrect decision. Required for templateType CLASSIFICATION
    #' @param targetValue numeric. Target value. Required for templateType REGRESSION
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseValueTemplateCalculateResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`400`** Bad request
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' accuracyImprovement <- 3.4 # numeric | Accuracy improvement
    #' decisionsCount <- 56 # integer | Estimated number of decisions per year
    #' templateType <- 'templateType_example' # character | The name of the use case value template to be retrieved
    #' incorrectDecisionsCount <- 56 # integer | Estimated number of incorrect decisions per year. Required for templateType CLASSIFICATION
    #' incorrectDecisionCost <- 3.4 # numeric | Estimated cost of an individual incorrect decision. Required for templateType CLASSIFICATION
    #' targetValue <- 3.4 # numeric | Target value. Required for templateType REGRESSION
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCaseValueTemplatesCalculationList(accuracyImprovement, decisionsCount, templateType, incorrectDecisionsCount=incorrectDecisionsCount, incorrectDecisionCost=incorrectDecisionCost, targetValue=targetValue)
    #' }
    UseCaseValueTemplatesCalculationList = function(accuracyImprovement, decisionsCount, templateType, incorrectDecisionsCount = NULL, incorrectDecisionCost = NULL, targetValue = NULL, ...) {
      apiResponse <- private$UseCaseValueTemplatesCalculationListWithHttpInfo(accuracyImprovement, decisionsCount, templateType, incorrectDecisionsCount, incorrectDecisionCost, targetValue, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List available use cases templates.
    #' Produces: "application/json"
    #'
    #' @details List available use cases templates.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return list( \link{UseCaseValueTemplateResponse} )
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCaseValueTemplatesList()
    #' }
    UseCaseValueTemplatesList = function(...) {
      apiResponse <- private$UseCaseValueTemplatesListWithHttpInfo(...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Get an individual use case value template by its name.
    #' Produces: "application/json"
    #'
    #' @details Get an individual use case value template by its name.
    #' @param templateType Enum < [classification, regression] > The name of the use case value template to be retrieved
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseValueTemplateResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`400`** Bad request
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' templateType <- 'templateType_example' # character | The name of the use case value template to be retrieved
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCaseValueTemplatesRetrieve(templateType)
    #' }
    UseCaseValueTemplatesRetrieve = function(templateType, ...) {
      apiResponse <- private$UseCaseValueTemplatesRetrieveWithHttpInfo(templateType, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve the activities of a use case.
    #' Produces: "application/json"
    #'
    #' @details Retrieve the activities of a use case.
    #' @param useCaseId character. The id of the use case
    #' @param offset integer. The number of records to skip over. Default 0.
    #' @param limit integer. The number of records to return in the range from 1 to 100. Default 100.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseActivityListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The id of the use case
    #' offset <- 0 # integer | The number of records to skip over. Default 0.
    #' limit <- 100 # integer | The number of records to return in the range from 1 to 100. Default 100.
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesActivitiesList(useCaseId, offset=offset, limit=limit)
    #' }
    UseCasesActivitiesList = function(useCaseId, offset = 0, limit = 100, ...) {
      apiResponse <- private$UseCasesActivitiesListWithHttpInfo(useCaseId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Attach a list of resources to this use case.
    #' Produces: "application/json"
    #'
    #' @details Attach a list of resources to this use case.
    #' @param useCaseId character. The id of the use case
    #' @param createUseCaseAttachment \link{CreateUseCaseAttachment}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return list( \link{UseCaseAttachment} )
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`201`**
    #' \itemize{
    #' }
    #' \item **`404`** Use case or object not found or Use case not writable or object not readable.
    #' \itemize{
    #' }
    #' \item **`422`** The request was formatted improperly
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The id of the use case
    #' createUseCaseAttachment <- CreateUseCaseAttachment$new() # CreateUseCaseAttachment |
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesAttachmentsCreate(useCaseId, createUseCaseAttachment=createUseCaseAttachment)
    #' }
    UseCasesAttachmentsCreate = function(useCaseId, createUseCaseAttachment = NULL, ...) {
      apiResponse <- private$UseCasesAttachmentsCreateWithHttpInfo(useCaseId, createUseCaseAttachment, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Removes a resource from a use case.
    #' Produces: NA
    #'
    #' @details Removes a resource from a use case.
    #' @param useCaseId character. The id of the use case
    #' @param attachmentId character. The id of the attachment
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`**
    #' \itemize{
    #' }
    #' \item **`403`** The user does not have permission to remove objects from this use case
    #' \itemize{
    #' }
    #' \item **`404`** Either the use case does not exist or the user does not have permissions to view this use case.
    #' \itemize{
    #' }
    #' \item **`422`** The request was formatted improperly, or the requested object to delete was not found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The id of the use case
    #' attachmentId <- 'attachmentId_example' # character | The id of the attachment
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesAttachmentsDelete(useCaseId, attachmentId)
    #' }
    UseCasesAttachmentsDelete = function(useCaseId, attachmentId, ...) {
      apiResponse <- private$UseCasesAttachmentsDeleteWithHttpInfo(useCaseId, attachmentId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Get a list of resources attached to this use case.
    #' Produces: "application/json"
    #'
    #' @details Get a list of resources attached to this use case.
    #' @param useCaseId character. The id of the use case
    #' @param offset integer. This many results will be skipped
    #' @param limit integer. This many results are returned
    #' @param type Enum < [dataset, modelingProject, deployment, customModel, modelPackage, application] > Use case attachment type
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseAttachmentListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`404`** Either the use case does not exist or the user does not have permissions to view this use case.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The id of the use case
    #' offset <- 0 # integer | This many results will be skipped
    #' limit <- 100 # integer | This many results are returned
    #' type <- 'type_example' # character | Use case attachment type
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesAttachmentsList(useCaseId, offset=offset, limit=limit, type=type)
    #' }
    UseCasesAttachmentsList = function(useCaseId, offset = 0, limit = 100, type = NULL, ...) {
      apiResponse <- private$UseCasesAttachmentsListWithHttpInfo(useCaseId, offset, limit, type, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Get a resource that is attached to a use case.
    #' Produces: "application/json"
    #'
    #' @details Get a resource that is attached to a use case.
    #' @param useCaseId character. The id of the use case
    #' @param attachmentId character. The id of the attachment
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseAttachmentResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`403`** The user does not have permission to attach objects to this use case
    #' \itemize{
    #' }
    #' \item **`404`** Either the use case does not exist or the user does not have permissions to view this use case.
    #' \itemize{
    #' }
    #' \item **`422`** The request was formatted improperly, or the requested object to attach was not found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The id of the use case
    #' attachmentId <- 'attachmentId_example' # character | The id of the attachment
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesAttachmentsRetrieve(useCaseId, attachmentId)
    #' }
    UseCasesAttachmentsRetrieve = function(useCaseId, attachmentId, ...) {
      apiResponse <- private$UseCasesAttachmentsRetrieveWithHttpInfo(useCaseId, attachmentId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Create a new use case.
    #' Produces: "application/json"
    #'
    #' @details Create a new use case.
    #' @param useCase \link{UseCase}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseCreateResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCase <- UseCase$new() # UseCase |
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesCreate(useCase=useCase)
    #' }
    UseCasesCreate = function(useCase = NULL, ...) {
      apiResponse <- private$UseCasesCreateWithHttpInfo(useCase, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Delete a use case
    #' Produces: NA
    #'
    #' @details Delete a use case
    #' @param useCaseId character. The id of the use case
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`204`** The use case was deleted
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The id of the use case
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesDelete(useCaseId)
    #' }
    UseCasesDelete = function(useCaseId, ...) {
      apiResponse <- private$UseCasesDeleteWithHttpInfo(useCaseId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List use cases the requesting user has access to.
    #' Produces: "application/json"
    #'
    #' @details List use cases the requesting user has access to.
    #' @param offset integer. The number of records to skip over. Default 0.
    #' @param limit integer. The number of records to return in the range from 1 to 100. Default 100.
    #' @param orderBy Enum < [businessImpact, -businessImpact, potentialValue, -potentialValue, realizedValue, -realizedValue, feasibility, -feasibility, stage, -stage] > Sort the use cases by a field of the use case.
    #' @param namePart character. Only return use cases with names that match the given string.
    #' @param stage Enum < [ideation, queued, dataPrepAndModeling, validatingAndDeploying, inProduction, retired, onHold] > Filter results by the current stage of the use cases.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' offset <- 0 # integer | The number of records to skip over. Default 0.
    #' limit <- 100 # integer | The number of records to return in the range from 1 to 100. Default 100.
    #' orderBy <- 'orderBy_example' # character | Sort the use cases by a field of the use case.
    #' namePart <- 'namePart_example' # character | Only return use cases with names that match the given string.
    #' stage <- 'stage_example' # character | Filter results by the current stage of the use cases.
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesList(offset=offset, limit=limit, orderBy=orderBy, namePart=namePart, stage=stage)
    #' }
    UseCasesList = function(offset = 0, limit = 100, orderBy = NULL, namePart = NULL, stage = NULL, ...) {
      apiResponse <- private$UseCasesListWithHttpInfo(offset, limit, orderBy, namePart, stage, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Update a use case.
    #' Produces: "application/json"
    #'
    #' @details Update a use case.
    #' @param useCaseId character. The id of the use case
    #' @param useCaseUpdate \link{UseCaseUpdate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The id of the use case
    #' useCaseUpdate <- UseCaseUpdate$new() # UseCaseUpdate |
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesPatch(useCaseId, useCaseUpdate=useCaseUpdate)
    #' }
    UseCasesPatch = function(useCaseId, useCaseUpdate = NULL, ...) {
      apiResponse <- private$UseCasesPatchWithHttpInfo(useCaseId, useCaseUpdate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve realized value information for a given use case over a period of time
    #' Produces: "application/json"
    #'
    #' @details Retrieve realized value information for a given use case over a period of time
    #' @param useCaseId character. The ID of the use case
    #' @param start character. RFC3339 datetime. Start of time period to retrieve the features drift for. If not set, defaults to 1 week from the end of the period.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
    #' @param end character. RFC3339 datetime. End of time period to retrieve the features drift for. If not set, defaults to the next top of the hour from now.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
    #' @param bucketSize character. The time duration of a bucket. Needs to be multiple of one hour. Can not be longer than the total length of the period. If not set, a default value will be calculated based on the start and end time.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseRealizedValuesOverTimeResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The ID of the use case
    #' start <- 'start_example' # character | RFC3339 datetime. Start of time period to retrieve the features drift for. If not set, defaults to 1 week from the end of the period.Note: this field only accepts top of the hour RFC3339 datetime strings For example: ``2019-08-01T00:00:00Z``.
    #' end <- 'end_example' # character | RFC3339 datetime. End of time period to retrieve the features drift for. If not set, defaults to the next top of the hour from now.Note: this field only accepts top of the hour RFC3339 datetime strings For example: ``2019-08-01T00:00:00Z``.
    #' bucketSize <- 'bucketSize_example' # character | The time duration of a bucket. Needs to be multiple of one hour. Can not be longer than the total length of the period. If not set, a default value will be calculated based on the start and end time.
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesRealizedValueOverTimeList(useCaseId, start=start, end=end, bucketSize=bucketSize)
    #' }
    UseCasesRealizedValueOverTimeList = function(useCaseId, start = NULL, end = NULL, bucketSize = NULL, ...) {
      apiResponse <- private$UseCasesRealizedValueOverTimeListWithHttpInfo(useCaseId, start, end, bucketSize, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve a use case.
    #' Produces: "application/json"
    #'
    #' @details Retrieve a use case.
    #' @param useCaseId character. The id of the use case
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The id of the use case
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesRetrieve(useCaseId)
    #' }
    UseCasesRetrieve = function(useCaseId, ...) {
      apiResponse <- private$UseCasesRetrieveWithHttpInfo(useCaseId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Get a list of users, groups and organizations that have an access to this use case
    #' Produces: "application/json"
    #'
    #' @details Get a list of users, groups and organizations that have an access to this use case
    #' @param offset integer. This many results will be skipped
    #' @param limit integer. At most this many results are returned
    #' @param useCaseId character. The ID of the use case
    #' @param id character. Only return roles for a user, group or organization with this identifier.
    #' @param name character. Only return roles for a user, group or organization with this name.
    #' @param shareRecipientType Enum < [user, group, organization] > List access controls for recipients with this type.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{UseCaseSharingListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' offset <- 0 # integer | This many results will be skipped
    #' limit <- 10 # integer | At most this many results are returned
    #' useCaseId <- 'useCaseId_example' # character | The ID of the use case
    #' id <- 'id_example' # character | Only return roles for a user, group or organization with this identifier.
    #' name <- 'name_example' # character | Only return roles for a user, group or organization with this name.
    #' shareRecipientType <- 'shareRecipientType_example' # character | List access controls for recipients with this type.
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesSharedRolesList(offset, limit, useCaseId, id=id, name=name, shareRecipientType=shareRecipientType)
    #' }
    UseCasesSharedRolesList = function(offset, limit, useCaseId, id = NULL, name = NULL, shareRecipientType = NULL, ...) {
      apiResponse <- private$UseCasesSharedRolesListWithHttpInfo(offset, limit, useCaseId, id, name, shareRecipientType, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Share a use case with a user, group or organization.
    #' Produces: NA
    #'
    #' @details Share a use case with a user, group or organization.
    #' @param useCaseId character. The ID of the use case
    #' @param useCaseSharingUpdate \link{UseCaseSharingUpdate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`204`** The roles updated successfully
    #' \itemize{
    #' }
    #' \item **`422`** The request was formatted improperly
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' useCaseId <- 'useCaseId_example' # character | The ID of the use case
    #' useCaseSharingUpdate <- UseCaseSharingUpdate$new() # UseCaseSharingUpdate |
    #'
    #' api.instance <- UseCaseApi$new()
    #' result <- api.instance$UseCasesSharedRolesPatchMany(useCaseId, useCaseSharingUpdate=useCaseSharingUpdate)
    #' }
    UseCasesSharedRolesPatchMany = function(useCaseId, useCaseSharingUpdate = NULL, ...) {
      apiResponse <- private$UseCasesSharedRolesPatchManyWithHttpInfo(useCaseId, useCaseSharingUpdate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    }
  ),
  private = list(
    # A helper function to invoke the API operation `UseCaseValueTemplatesCalculationList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCaseValueTemplatesCalculationListWithHttpInfo = function(accuracyImprovement, decisionsCount, templateType, incorrectDecisionsCount = NULL, incorrectDecisionCost = NULL, targetValue = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`accuracyImprovement`)) {
        stop("Missing required parameter `accuracyImprovement`.")
      }

      if (missing(`decisionsCount`)) {
        stop("Missing required parameter `decisionsCount`.")
      }

      if (missing(`templateType`)) {
        stop("Missing required parameter `templateType`.")
      }

      queryParams["accuracyImprovement"] <- accuracyImprovement

      queryParams["decisionsCount"] <- decisionsCount

      queryParams["incorrectDecisionsCount"] <- incorrectDecisionsCount

      queryParams["incorrectDecisionCost"] <- incorrectDecisionCost

      queryParams["targetValue"] <- targetValue

      body <- NULL
      urlPath <- "/useCaseValueTemplates/{templateType}/calculation/"
      if (!missing(`templateType`)) {
        urlPath <- gsub(paste0("\\{", "templateType", "\\}"), URLencode(as.character(`templateType`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseValueTemplateCalculateResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCaseValueTemplatesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCaseValueTemplatesListWithHttpInfo = function(...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      body <- NULL
      urlPath <- "/useCaseValueTemplates/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "array[UseCaseValueTemplateResponse]", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCaseValueTemplatesRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCaseValueTemplatesRetrieveWithHttpInfo = function(templateType, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`templateType`)) {
        stop("Missing required parameter `templateType`.")
      }

      body <- NULL
      urlPath <- "/useCaseValueTemplates/{templateType}/"
      if (!missing(`templateType`)) {
        urlPath <- gsub(paste0("\\{", "templateType", "\\}"), URLencode(as.character(`templateType`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseValueTemplateResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesActivitiesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesActivitiesListWithHttpInfo = function(useCaseId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/useCases/{useCaseId}/activities/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseActivityListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesAttachmentsCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesAttachmentsCreateWithHttpInfo = function(useCaseId, createUseCaseAttachment = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      if (!missing(`createUseCaseAttachment`) && isa(createUseCaseAttachment, c("CreateUseCaseAttachment", "R6"))) {
        body <- `createUseCaseAttachment`$toJSON()
      } else {
        stop("UseCasesAttachmentsCreateWithHttpInfo requires parameter createUseCaseAttachment to be of type CreateUseCaseAttachment.")
      }

      urlPath <- "/useCases/{useCaseId}/attachments/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "array[UseCaseAttachment]", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesAttachmentsDelete`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesAttachmentsDeleteWithHttpInfo = function(useCaseId, attachmentId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      if (missing(`attachmentId`)) {
        stop("Missing required parameter `attachmentId`.")
      }

      body <- NULL
      urlPath <- "/useCases/{useCaseId}/attachments/{attachmentId}/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }

      if (!missing(`attachmentId`)) {
        urlPath <- gsub(paste0("\\{", "attachmentId", "\\}"), URLencode(as.character(`attachmentId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "DELETE",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesAttachmentsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesAttachmentsListWithHttpInfo = function(useCaseId, offset = 0, limit = 100, type = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["type"] <- type

      body <- NULL
      urlPath <- "/useCases/{useCaseId}/attachments/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseAttachmentListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesAttachmentsRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesAttachmentsRetrieveWithHttpInfo = function(useCaseId, attachmentId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      if (missing(`attachmentId`)) {
        stop("Missing required parameter `attachmentId`.")
      }

      body <- NULL
      urlPath <- "/useCases/{useCaseId}/attachments/{attachmentId}/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }

      if (!missing(`attachmentId`)) {
        urlPath <- gsub(paste0("\\{", "attachmentId", "\\}"), URLencode(as.character(`attachmentId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseAttachmentResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesCreateWithHttpInfo = function(useCase = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (!missing(`useCase`) && isa(useCase, c("UseCase", "R6"))) {
        body <- `useCase`$toJSON()
      } else {
        stop("UseCasesCreateWithHttpInfo requires parameter useCase to be of type UseCase.")
      }

      urlPath <- "/useCases/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseCreateResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesDelete`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesDeleteWithHttpInfo = function(useCaseId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      body <- NULL
      urlPath <- "/useCases/{useCaseId}/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "DELETE",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesListWithHttpInfo = function(offset = 0, limit = 100, orderBy = NULL, namePart = NULL, stage = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["orderBy"] <- orderBy

      queryParams["namePart"] <- namePart

      queryParams["stage"] <- stage

      body <- NULL
      urlPath <- "/useCases/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesPatch`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesPatchWithHttpInfo = function(useCaseId, useCaseUpdate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      if (!missing(`useCaseUpdate`) && isa(useCaseUpdate, c("UseCaseUpdate", "R6"))) {
        body <- `useCaseUpdate`$toJSON()
      } else {
        stop("UseCasesPatchWithHttpInfo requires parameter useCaseUpdate to be of type UseCaseUpdate.")
      }

      urlPath <- "/useCases/{useCaseId}/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesRealizedValueOverTimeList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesRealizedValueOverTimeListWithHttpInfo = function(useCaseId, start = NULL, end = NULL, bucketSize = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      queryParams["start"] <- start

      queryParams["end"] <- end

      queryParams["bucketSize"] <- bucketSize

      body <- NULL
      urlPath <- "/useCases/{useCaseId}/realizedValueOverTime/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseRealizedValuesOverTimeResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesRetrieveWithHttpInfo = function(useCaseId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      body <- NULL
      urlPath <- "/useCases/{useCaseId}/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesSharedRolesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesSharedRolesListWithHttpInfo = function(offset, limit, useCaseId, id = NULL, name = NULL, shareRecipientType = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      queryParams["id"] <- id

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["name"] <- name

      queryParams["shareRecipientType"] <- shareRecipientType

      body <- NULL
      urlPath <- "/useCases/{useCaseId}/sharedRoles/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "UseCaseSharingListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `UseCasesSharedRolesPatchMany`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    UseCasesSharedRolesPatchManyWithHttpInfo = function(useCaseId, useCaseSharingUpdate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`useCaseId`)) {
        stop("Missing required parameter `useCaseId`.")
      }

      if (!missing(`useCaseSharingUpdate`) && isa(useCaseSharingUpdate, c("UseCaseSharingUpdate", "R6"))) {
        body <- `useCaseSharingUpdate`$toJSON()
      } else {
        stop("UseCasesSharedRolesPatchManyWithHttpInfo requires parameter useCaseSharingUpdate to be of type UseCaseSharingUpdate.")
      }

      urlPath <- "/useCases/{useCaseId}/sharedRoles/"
      if (!missing(`useCaseId`)) {
        urlPath <- gsub(paste0("\\{", "useCaseId", "\\}"), URLencode(as.character(`useCaseId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    }
  )
)
