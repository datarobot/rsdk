# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CreateDataEngineQueryGenerator
#'
#' @description CreateDataEngineQueryGenerator Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasets  list( \link{DataEngineDataset} ) Source datasets in the Data Engine workspace.
#'
#' @field generatorSettings  \link{GeneratorSettings}
#'
#' @field generatorType  character Type of data engine query generator
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CreateDataEngineQueryGenerator <- R6::R6Class(
  "CreateDataEngineQueryGenerator",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datasets` = NULL, `generatorSettings` = NULL, `generatorType` = NULL) {
      if (!is.null(`datasets`)) {
        stopifnot(is.vector(`datasets`))
      }
      if (!is.null(`generatorSettings`)) {
        stopifnot(R6::is.R6(`generatorSettings`))
      }
      if (!is.null(`generatorType`)) {
        stopifnot(is.character(`generatorType`), length(`generatorType`) == 1)
      }
    }
  ),
  public = list(
    `datasets` = NULL,
    `generatorSettings` = NULL,
    `generatorType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasets Source datasets in the Data Engine workspace.
    #' @param generatorSettings
    #' @param generatorType Type of data engine query generator
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasets` = NULL, `generatorSettings` = NULL, `generatorType` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datasets`, `generatorSettings`, `generatorType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datasets, generatorSettings, generatorType)
      }
      sapply(`datasets`, function(x) stopifnot(R6::is.R6(x)))
      self$`generatorSettings` <- `generatorSettings`
      self$`generatorType` <- `generatorType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datasets = self$`datasets`, generatorSettings = self$`generatorSettings`, generatorType = self$`generatorType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasets`)) {
          sprintf(
            '"datasets":
            [%s]
      ',
            paste(sapply(self$`datasets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`generatorSettings`)) {
          sprintf(
            '"generatorSettings":
            %s
      ',
            jsonlite::toJSON(self$`generatorSettings`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`generatorType`)) {
          sprintf(
            '"generatorType":
            "%s"
                  ',
            self$`generatorType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CreateDataEngineQueryGeneratorJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CreateDataEngineQueryGeneratorJson, validateParams = FALSE) {
      CreateDataEngineQueryGeneratorObject <- jsonlite::fromJSON(CreateDataEngineQueryGeneratorJson)
      self$`datasets` <- ApiClient$new()$deserializeObj(CreateDataEngineQueryGeneratorObject$`datasets`, "array[DataEngineDataset]", loadNamespace("datarobot.apicore"))
      self$`generatorSettings` <- GeneratorSettings$new()$fromJSON(jsonlite::toJSON(CreateDataEngineQueryGeneratorObject$generatorSettings, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`generatorType` <- CreateDataEngineQueryGeneratorObject$`generatorType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
