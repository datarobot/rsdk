# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PassthroughColumnDefinitionPayload
#'
#' @description PassthroughColumnDefinitionPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dataType  integer Column data type
#'
#' @field name  character Column name
#'
#' @field precision  integer Precision of the column
#'
#' @field scale  integer Scale of the column
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PassthroughColumnDefinitionPayload <- R6::R6Class(
  "PassthroughColumnDefinitionPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(dataType = NULL, name = NULL, precision = NULL, scale = NULL) {
      if (!is.null(`dataType`)) {
        stopifnot(is.numeric(`dataType`), length(`dataType`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`precision`)) {
        stopifnot(is.numeric(`precision`), length(`precision`) == 1)
      }
      if (!is.null(`scale`)) {
        stopifnot(is.numeric(`scale`), length(`scale`) == 1)
      }
    }
  ),
  public = list(
    `dataType` = NULL,
    `name` = NULL,
    `precision` = NULL,
    `scale` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dataType Column data type
    #' @param name Column name
    #' @param precision Precision of the column
    #' @param scale Scale of the column
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `dataType` = NULL, `name` = NULL, `precision` = NULL, `scale` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`dataType`, `name`, `precision`, `scale`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(dataType, name, precision, scale)
      }
      self$`dataType` <- `dataType`
      self$`name` <- `name`
      self$`precision` <- `precision`
      self$`scale` <- `scale`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(dataType = self$`dataType`, name = self$`name`, precision = self$`precision`, scale = self$`scale`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dataType`)) {
        sprintf(
        '"dataType":
          %d
                ',
        self$`dataType`
        )},
        if (!is.null(self$`name`)) {
        sprintf(
        '"name":
          "%s"
                ',
        self$`name`
        )},
        if (!is.null(self$`precision`)) {
        sprintf(
        '"precision":
          %d
                ',
        self$`precision`
        )},
        if (!is.null(self$`scale`)) {
        sprintf(
        '"scale":
          %d
                ',
        self$`scale`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PassthroughColumnDefinitionPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PassthroughColumnDefinitionPayloadJson, validateParams = FALSE) {
      PassthroughColumnDefinitionPayloadObject <- jsonlite::fromJSON(PassthroughColumnDefinitionPayloadJson)
      self$`dataType` <- PassthroughColumnDefinitionPayloadObject$`dataType`
      self$`name` <- PassthroughColumnDefinitionPayloadObject$`name`
      self$`precision` <- PassthroughColumnDefinitionPayloadObject$`precision`
      self$`scale` <- PassthroughColumnDefinitionPayloadObject$`scale`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
