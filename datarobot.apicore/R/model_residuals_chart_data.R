# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelResidualsChartData
#'
#' @description ModelResidualsChartData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field coefficientOfDetermination  numeric Also known as the r-squared value. This value is calculated over the downsampled dataset, not the full input
#'
#' @field data  list( \link{array[numeric]} ) The rows of chart data in [actual, predicted, residual, row number] form. If the row number was not available at the time of model creation, the row number will be null. **NOTE**: In DataRobot v5.2, the row number will not be included.
#'
#' @field histogram  list( \link{ModelResidualsHistogram} ) Data to plot a histogram of residual values
#'
#' @field residualMean  numeric The arithmetic mean of the predicted value minus the actual value over the downsampled dataset
#'
#' @field standardDeviation  numeric A measure of deviation from the group as a whole
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelResidualsChartData <- R6::R6Class(
  "ModelResidualsChartData",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(coefficientOfDetermination = NULL, data = NULL, histogram = NULL, residualMean = NULL, standardDeviation = NULL) {
      if (!is.null(`coefficientOfDetermination`)) {
      }
      if (!is.null(`data`)) {
        stopifnot(is.vector(`data`))
      }
      if (!is.null(`histogram`)) {
        stopifnot(is.vector(`histogram`))
      }
      if (!is.null(`residualMean`)) {
      }
      if (!is.null(`standardDeviation`)) {
      }
    }
  ),
  public = list(
    `coefficientOfDetermination` = NULL,
    `data` = NULL,
    `histogram` = NULL,
    `residualMean` = NULL,
    `standardDeviation` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param coefficientOfDetermination Also known as the r-squared value. This value is calculated over the downsampled dataset, not the full input
    #' @param data The rows of chart data in [actual, predicted, residual, row number] form. If the row number was not available at the time of model creation, the row number will be null. **NOTE**: In DataRobot v5.2, the row number will not be included.
    #' @param histogram Data to plot a histogram of residual values
    #' @param residualMean The arithmetic mean of the predicted value minus the actual value over the downsampled dataset
    #' @param standardDeviation A measure of deviation from the group as a whole
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`coefficientOfDetermination` = NULL, `data` = NULL, `histogram` = NULL, `residualMean` = NULL, `standardDeviation` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`coefficientOfDetermination`, `data`, `histogram`, `residualMean`, `standardDeviation`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(coefficientOfDetermination, data, histogram, residualMean, standardDeviation)
      }
      self$`coefficientOfDetermination` <- `coefficientOfDetermination`
      sapply(`data`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`histogram`, function(x) stopifnot(R6::is.R6(x)))
      self$`residualMean` <- `residualMean`
      self$`standardDeviation` <- `standardDeviation`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(coefficientOfDetermination = self$`coefficientOfDetermination`, data = self$`data`, histogram = self$`histogram`, residualMean = self$`residualMean`, standardDeviation = self$`standardDeviation`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`coefficientOfDetermination`)) {
          sprintf(
            '"coefficientOfDetermination":
            %d
                  ',
            self$`coefficientOfDetermination`
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
',
            paste(sapply(self$`data`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`histogram`)) {
          sprintf(
            '"histogram":
            [%s]
',
            paste(sapply(self$`histogram`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`residualMean`)) {
          sprintf(
            '"residualMean":
            %d
                  ',
            self$`residualMean`
          )
        },
        if (!is.null(self$`standardDeviation`)) {
          sprintf(
            '"standardDeviation":
            %d
                  ',
            self$`standardDeviation`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelResidualsChartDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelResidualsChartDataJson, validateParams = FALSE) {
      ModelResidualsChartDataObject <- jsonlite::fromJSON(ModelResidualsChartDataJson)
      self$`coefficientOfDetermination` <- ModelResidualsChartDataObject$`coefficientOfDetermination`
      self$`data` <- ApiClient$new()$deserializeObj(ModelResidualsChartDataObject$`data`, "array[array[numeric]]", loadNamespace("datarobot.apicore"))
      self$`histogram` <- ApiClient$new()$deserializeObj(ModelResidualsChartDataObject$`histogram`, "array[ModelResidualsHistogram]", loadNamespace("datarobot.apicore"))
      self$`residualMean` <- ModelResidualsChartDataObject$`residualMean`
      self$`standardDeviation` <- ModelResidualsChartDataObject$`standardDeviation`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
