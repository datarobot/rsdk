# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureDriftOverTimeBucket
#'
#' @description FeatureDriftOverTimeBucket Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field baselineSampleSize  integer The sample size of the training data used during model creation
#'
#' @field driftScore  numeric The aggregated drift score for the target.
#'
#' @field featureImpact  numeric The feature impact score of the feature.
#'
#' @field featureName  character Name of the feature.
#'
#' @field period  \link{TimeRange}
#'
#' @field sampleSize  integer The sample size in the query period used to calculate drift score.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureDriftOverTimeBucket <- R6::R6Class(
  "FeatureDriftOverTimeBucket",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`baselineSampleSize` = NULL, `driftScore` = NULL, `featureImpact` = NULL, `featureName` = NULL, `period` = NULL, `sampleSize` = NULL) {
      if (!is.null(`baselineSampleSize`)) {
        stopifnot(is.numeric(`baselineSampleSize`), length(`baselineSampleSize`) == 1)
      }
      if (!is.null(`driftScore`)) {
      }
      if (!is.null(`featureImpact`)) {
      }
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`period`)) {
        stopifnot(R6::is.R6(`period`))
      }
      if (!is.null(`sampleSize`)) {
        stopifnot(is.numeric(`sampleSize`), length(`sampleSize`) == 1)
      }
    }
  ),
  public = list(
    `baselineSampleSize` = NULL,
    `driftScore` = NULL,
    `featureImpact` = NULL,
    `featureName` = NULL,
    `period` = NULL,
    `sampleSize` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param baselineSampleSize The sample size of the training data used during model creation
    #' @param driftScore The aggregated drift score for the target.
    #' @param featureImpact The feature impact score of the feature.
    #' @param featureName Name of the feature.
    #' @param period
    #' @param sampleSize The sample size in the query period used to calculate drift score.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`baselineSampleSize` = NULL, `driftScore` = NULL, `featureImpact` = NULL, `featureName` = NULL, `period` = NULL, `sampleSize` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`baselineSampleSize`, `driftScore`, `featureImpact`, `featureName`, `period`, `sampleSize`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(baselineSampleSize, driftScore, featureImpact, featureName, period, sampleSize)
      }
      self$`baselineSampleSize` <- `baselineSampleSize`
      self$`driftScore` <- `driftScore`
      self$`featureImpact` <- `featureImpact`
      self$`featureName` <- `featureName`
      self$`period` <- `period`
      self$`sampleSize` <- `sampleSize`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(baselineSampleSize = self$`baselineSampleSize`, driftScore = self$`driftScore`, featureImpact = self$`featureImpact`, featureName = self$`featureName`, period = self$`period`, sampleSize = self$`sampleSize`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`baselineSampleSize`)) {
          sprintf(
            '"baselineSampleSize":
            %d
                  ',
            self$`baselineSampleSize`
          )
        },
        if (!is.null(self$`driftScore`)) {
          sprintf(
            '"driftScore":
            %d
                  ',
            self$`driftScore`
          )
        },
        if (!is.null(self$`featureImpact`)) {
          sprintf(
            '"featureImpact":
            %d
                  ',
            self$`featureImpact`
          )
        },
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`period`)) {
          sprintf(
            '"period":
            %s
      ',
            jsonlite::toJSON(self$`period`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`sampleSize`)) {
          sprintf(
            '"sampleSize":
            %d
                  ',
            self$`sampleSize`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureDriftOverTimeBucketJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureDriftOverTimeBucketJson, validateParams = FALSE) {
      FeatureDriftOverTimeBucketObject <- jsonlite::fromJSON(FeatureDriftOverTimeBucketJson)
      self$`baselineSampleSize` <- FeatureDriftOverTimeBucketObject$`baselineSampleSize`
      self$`driftScore` <- FeatureDriftOverTimeBucketObject$`driftScore`
      self$`featureImpact` <- FeatureDriftOverTimeBucketObject$`featureImpact`
      self$`featureName` <- FeatureDriftOverTimeBucketObject$`featureName`
      self$`period` <- TimeRange$new()$fromJSON(jsonlite::toJSON(FeatureDriftOverTimeBucketObject$period, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`sampleSize` <- FeatureDriftOverTimeBucketObject$`sampleSize`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
