# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelPackageSourceMeta
#'
#' @description ModelPackageSourceMeta Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field decisionFlowId  character The ID of the flow associated with this model package
#'
#' @field decisionFlowVersionId  character The version ID of the flow associated with this model package
#'
#' @field environmentUrl  character If available, URL of the source model
#'
#' @field projectId  character If available, the project id used for this model
#'
#' @field projectName  character If available, the project name for this model
#'
#' @field scoringCode  \link{ModelPacakgeScoringCodeMeta}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelPackageSourceMeta <- R6::R6Class(
  "ModelPackageSourceMeta",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(decisionFlowId = NULL, decisionFlowVersionId = NULL, environmentUrl = NULL, projectId = NULL, projectName = NULL, scoringCode = NULL) {
      if (!is.null(`decisionFlowId`)) {
        stopifnot(is.character(`decisionFlowId`), length(`decisionFlowId`) == 1)
      }
      if (!is.null(`decisionFlowVersionId`)) {
        stopifnot(is.character(`decisionFlowVersionId`), length(`decisionFlowVersionId`) == 1)
      }
      if (!is.null(`environmentUrl`)) {
        stopifnot(is.character(`environmentUrl`), length(`environmentUrl`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`projectName`)) {
        stopifnot(is.character(`projectName`), length(`projectName`) == 1)
      }
      if (!is.null(`scoringCode`)) {
        stopifnot(R6::is.R6(`scoringCode`))
      }
    }
  ),
  public = list(
    `decisionFlowId` = NULL,
    `decisionFlowVersionId` = NULL,
    `environmentUrl` = NULL,
    `projectId` = NULL,
    `projectName` = NULL,
    `scoringCode` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param decisionFlowId The ID of the flow associated with this model package
    #' @param decisionFlowVersionId The version ID of the flow associated with this model package
    #' @param environmentUrl If available, URL of the source model
    #' @param projectId If available, the project id used for this model
    #' @param projectName If available, the project name for this model
    #' @param scoringCode
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`decisionFlowId` = NULL, `decisionFlowVersionId` = NULL, `environmentUrl` = NULL, `projectId` = NULL, `projectName` = NULL, `scoringCode` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`decisionFlowId`, `decisionFlowVersionId`, `environmentUrl`, `projectId`, `projectName`, `scoringCode`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(decisionFlowId, decisionFlowVersionId, environmentUrl, projectId, projectName, scoringCode)
      }
      self$`decisionFlowId` <- `decisionFlowId`
      self$`decisionFlowVersionId` <- `decisionFlowVersionId`
      self$`environmentUrl` <- `environmentUrl`
      self$`projectId` <- `projectId`
      self$`projectName` <- `projectName`
      self$`scoringCode` <- `scoringCode`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(decisionFlowId = self$`decisionFlowId`, decisionFlowVersionId = self$`decisionFlowVersionId`, environmentUrl = self$`environmentUrl`, projectId = self$`projectId`, projectName = self$`projectName`, scoringCode = self$`scoringCode`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`decisionFlowId`)) {
          sprintf(
            '"decisionFlowId":
            "%s"
                  ',
            self$`decisionFlowId`
          )
        },
        if (!is.null(self$`decisionFlowVersionId`)) {
          sprintf(
            '"decisionFlowVersionId":
            "%s"
                  ',
            self$`decisionFlowVersionId`
          )
        },
        if (!is.null(self$`environmentUrl`)) {
          sprintf(
            '"environmentUrl":
            "%s"
                  ',
            self$`environmentUrl`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`projectName`)) {
          sprintf(
            '"projectName":
            "%s"
                  ',
            self$`projectName`
          )
        },
        if (!is.null(self$`scoringCode`)) {
          sprintf(
            '"scoringCode":
          %s
        ',
            jsonlite::toJSON(self$`scoringCode`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelPackageSourceMetaJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelPackageSourceMetaJson, validateParams = FALSE) {
      ModelPackageSourceMetaObject <- jsonlite::fromJSON(ModelPackageSourceMetaJson)
      self$`decisionFlowId` <- ModelPackageSourceMetaObject$`decisionFlowId`
      self$`decisionFlowVersionId` <- ModelPackageSourceMetaObject$`decisionFlowVersionId`
      self$`environmentUrl` <- ModelPackageSourceMetaObject$`environmentUrl`
      self$`projectId` <- ModelPackageSourceMetaObject$`projectId`
      self$`projectName` <- ModelPackageSourceMetaObject$`projectName`
      self$`scoringCode` <- ModelPacakgeScoringCodeMeta$new()$fromJSON(jsonlite::toJSON(ModelPackageSourceMetaObject$scoringCode, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
