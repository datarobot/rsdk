# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BlueprintResponse
#'
#' @description BlueprintResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blueprintCategory  character describes the category of the blueprint and indicates the kind of model this blueprint produces. Will be either \&quot;DataRobot\&quot; or \&quot;Scaleout DataRobot\&quot;.
#'
#' @field id  character the blueprint ID of this blueprint - note that this is not an ObjectId.
#'
#' @field modelType  character the model this blueprint will produce.
#'
#' @field monotonicDecreasingFeaturelistId  character the ID of the featurelist that defines the set of features with a monotonically decreasing relationship to the target. If null, no such constraints are enforced.
#'
#' @field monotonicIncreasingFeaturelistId  character null or str, the ID of the featurelist that defines the set of features with a monotonically increasing relationship to the target. If null, no such constraints are enforced.
#'
#' @field processes  list( character ) a list of strings representing processes the blueprint uses.
#'
#' @field projectId  character the project the blueprint belongs to.
#'
#' @field recommendedFeaturelistId  character The ID of the feature list recommended for this blueprint. If this field is not present, then there is no recommended feature list.
#'
#' @field supportsComposableMl  character indicates whether this blueprint is supported in Composable ML.
#'
#' @field supportsMonotonicConstraints  character whether this model supports enforcing monotonic constraints.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BlueprintResponse <- R6::R6Class(
  "BlueprintResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(blueprintCategory = NULL, id = NULL, modelType = NULL, monotonicDecreasingFeaturelistId = NULL, monotonicIncreasingFeaturelistId = NULL, processes = NULL, projectId = NULL, recommendedFeaturelistId = NULL, supportsComposableMl = NULL, supportsMonotonicConstraints = NULL) {
      if (!is.null(`blueprintCategory`)) {
        stopifnot(is.character(`blueprintCategory`), length(`blueprintCategory`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`modelType`)) {
        stopifnot(is.character(`modelType`), length(`modelType`) == 1)
      }
      if (!is.null(`monotonicDecreasingFeaturelistId`)) {
        stopifnot(is.character(`monotonicDecreasingFeaturelistId`), length(`monotonicDecreasingFeaturelistId`) == 1)
      }
      if (!is.null(`monotonicIncreasingFeaturelistId`)) {
        stopifnot(is.character(`monotonicIncreasingFeaturelistId`), length(`monotonicIncreasingFeaturelistId`) == 1)
      }
      if (!is.null(`processes`)) {
        stopifnot(is.vector(`processes`))
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`recommendedFeaturelistId`)) {
        stopifnot(is.character(`recommendedFeaturelistId`), length(`recommendedFeaturelistId`) == 1)
      }
      if (!is.null(`supportsComposableMl`)) {
        stopifnot(is.logical(`supportsComposableMl`), length(`supportsComposableMl`) == 1)
      }
      if (!is.null(`supportsMonotonicConstraints`)) {
        stopifnot(is.logical(`supportsMonotonicConstraints`), length(`supportsMonotonicConstraints`) == 1)
      }
    }
  ),
  public = list(
    `blueprintCategory` = NULL,
    `id` = NULL,
    `modelType` = NULL,
    `monotonicDecreasingFeaturelistId` = NULL,
    `monotonicIncreasingFeaturelistId` = NULL,
    `processes` = NULL,
    `projectId` = NULL,
    `recommendedFeaturelistId` = NULL,
    `supportsComposableMl` = NULL,
    `supportsMonotonicConstraints` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blueprintCategory describes the category of the blueprint and indicates the kind of model this blueprint produces. Will be either \&quot;DataRobot\&quot; or \&quot;Scaleout DataRobot\&quot;.
    #' @param id the blueprint ID of this blueprint - note that this is not an ObjectId.
    #' @param modelType the model this blueprint will produce.
    #' @param monotonicDecreasingFeaturelistId the ID of the featurelist that defines the set of features with a monotonically decreasing relationship to the target. If null, no such constraints are enforced.
    #' @param monotonicIncreasingFeaturelistId null or str, the ID of the featurelist that defines the set of features with a monotonically increasing relationship to the target. If null, no such constraints are enforced.
    #' @param processes a list of strings representing processes the blueprint uses.
    #' @param projectId the project the blueprint belongs to.
    #' @param recommendedFeaturelistId The ID of the feature list recommended for this blueprint. If this field is not present, then there is no recommended feature list.
    #' @param supportsComposableMl indicates whether this blueprint is supported in Composable ML.
    #' @param supportsMonotonicConstraints whether this model supports enforcing monotonic constraints.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blueprintCategory` = NULL, `id` = NULL, `modelType` = NULL, `monotonicDecreasingFeaturelistId` = NULL, `monotonicIncreasingFeaturelistId` = NULL, `processes` = NULL, `projectId` = NULL, `recommendedFeaturelistId` = NULL, `supportsComposableMl` = NULL, `supportsMonotonicConstraints` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`blueprintCategory`, `id`, `modelType`, `monotonicDecreasingFeaturelistId`, `monotonicIncreasingFeaturelistId`, `processes`, `projectId`, `recommendedFeaturelistId`, `supportsComposableMl`, `supportsMonotonicConstraints`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(blueprintCategory, id, modelType, monotonicDecreasingFeaturelistId, monotonicIncreasingFeaturelistId, processes, projectId, recommendedFeaturelistId, supportsComposableMl, supportsMonotonicConstraints)
      }
      self$`blueprintCategory` <- `blueprintCategory`
      self$`id` <- `id`
      self$`modelType` <- `modelType`
      self$`monotonicDecreasingFeaturelistId` <- `monotonicDecreasingFeaturelistId`
      self$`monotonicIncreasingFeaturelistId` <- `monotonicIncreasingFeaturelistId`
      sapply(`processes`, function(x) stopifnot(is.character(x)))
      self$`projectId` <- `projectId`
      self$`recommendedFeaturelistId` <- `recommendedFeaturelistId`
      self$`supportsComposableMl` <- `supportsComposableMl`
      self$`supportsMonotonicConstraints` <- `supportsMonotonicConstraints`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(blueprintCategory = self$`blueprintCategory`, id = self$`id`, modelType = self$`modelType`, monotonicDecreasingFeaturelistId = self$`monotonicDecreasingFeaturelistId`, monotonicIncreasingFeaturelistId = self$`monotonicIncreasingFeaturelistId`, processes = self$`processes`, projectId = self$`projectId`, recommendedFeaturelistId = self$`recommendedFeaturelistId`, supportsComposableMl = self$`supportsComposableMl`, supportsMonotonicConstraints = self$`supportsMonotonicConstraints`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blueprintCategory`)) {
          sprintf(
            '"blueprintCategory":
            "%s"
                  ',
            self$`blueprintCategory`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`modelType`)) {
          sprintf(
            '"modelType":
            "%s"
                  ',
            self$`modelType`
          )
        },
        if (!is.null(self$`monotonicDecreasingFeaturelistId`)) {
          sprintf(
            '"monotonicDecreasingFeaturelistId":
            "%s"
                  ',
            self$`monotonicDecreasingFeaturelistId`
          )
        },
        if (!is.null(self$`monotonicIncreasingFeaturelistId`)) {
          sprintf(
            '"monotonicIncreasingFeaturelistId":
            "%s"
                  ',
            self$`monotonicIncreasingFeaturelistId`
          )
        },
        if (!is.null(self$`processes`)) {
          sprintf(
            '"processes":
              [%s]
            ',
            paste(unlist(lapply(self$`processes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`recommendedFeaturelistId`)) {
          sprintf(
            '"recommendedFeaturelistId":
            "%s"
                  ',
            self$`recommendedFeaturelistId`
          )
        },
        if (!is.null(self$`supportsComposableMl`)) {
          sprintf(
            '"supportsComposableMl":
            %s
                  ',
            tolower(self$`supportsComposableMl`)
          )
        },
        if (!is.null(self$`supportsMonotonicConstraints`)) {
          sprintf(
            '"supportsMonotonicConstraints":
            %s
                  ',
            tolower(self$`supportsMonotonicConstraints`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BlueprintResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BlueprintResponseJson, validateParams = FALSE) {
      BlueprintResponseObject <- jsonlite::fromJSON(BlueprintResponseJson)
      self$`blueprintCategory` <- BlueprintResponseObject$`blueprintCategory`
      self$`id` <- BlueprintResponseObject$`id`
      self$`modelType` <- BlueprintResponseObject$`modelType`
      self$`monotonicDecreasingFeaturelistId` <- BlueprintResponseObject$`monotonicDecreasingFeaturelistId`
      self$`monotonicIncreasingFeaturelistId` <- BlueprintResponseObject$`monotonicIncreasingFeaturelistId`
      self$`processes` <- ApiClient$new()$deserializeObj(BlueprintResponseObject$`processes`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`projectId` <- BlueprintResponseObject$`projectId`
      self$`recommendedFeaturelistId` <- BlueprintResponseObject$`recommendedFeaturelistId`
      self$`supportsComposableMl` <- BlueprintResponseObject$`supportsComposableMl`
      self$`supportsMonotonicConstraints` <- BlueprintResponseObject$`supportsMonotonicConstraints`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
