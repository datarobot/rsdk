# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title OrganizationUserPatch
#'
#' @description OrganizationUserPatch Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accessRoleId  character [optional] [DEPRECATED - Use accessRoleIds instead] ID of the access role assigned to the user
#'
#' @field accessRoleIds  list( character ) [optional] List of access role ids assigned to the user
#'
#' @field activated  character [optional] Whether the organization user is activated.
#'
#' @field expirationDate  character [optional] User expiration date
#'
#' @field maxWorkers  integer [optional] User max workers
#'
#' @field orgAdmin  character [optional] Mark user as an organizational admin
#'
#' @field organizationId  character [optional] The organization to move the user to
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
OrganizationUserPatch <- R6::R6Class(
  "OrganizationUserPatch",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`accessRoleId` = NULL, `accessRoleIds` = NULL, `activated` = NULL, `expirationDate` = NULL, `maxWorkers` = NULL, `orgAdmin` = NULL, `organizationId` = NULL) {
      if (!is.null(`accessRoleId`)) {
        stopifnot(is.character(`accessRoleId`), length(`accessRoleId`) == 1)
      }
      if (!is.null(`accessRoleIds`) && length(`accessRoleIds`) > 0) {
        stopifnot(is.vector(`accessRoleIds`), sapply(`accessRoleIds`, is.character))
      }
      if (!is.null(`activated`)) {
        stopifnot(is.logical(`activated`), length(`activated`) == 1)
      }
      if (!is.null(`expirationDate`)) {
        stopifnot(inherits(`expirationDate`, "POSIXt"))
      }
      if (!is.null(`maxWorkers`)) {
        stopifnot(is.numeric(`maxWorkers`), length(`maxWorkers`) == 1)
      }
      if (!is.null(`orgAdmin`)) {
        stopifnot(is.logical(`orgAdmin`), length(`orgAdmin`) == 1)
      }
      if (!is.null(`organizationId`)) {
        stopifnot(is.character(`organizationId`), length(`organizationId`) == 1)
      }
    }
  ),
  public = list(
    `accessRoleId` = NULL,
    `accessRoleIds` = NULL,
    `activated` = NULL,
    `expirationDate` = NULL,
    `maxWorkers` = NULL,
    `orgAdmin` = NULL,
    `organizationId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accessRoleId [DEPRECATED - Use accessRoleIds instead] ID of the access role assigned to the user
    #' @param accessRoleIds List of access role ids assigned to the user
    #' @param activated Whether the organization user is activated.
    #' @param expirationDate User expiration date
    #' @param maxWorkers User max workers
    #' @param orgAdmin Mark user as an organizational admin
    #' @param organizationId The organization to move the user to
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`accessRoleId` = NULL, `accessRoleIds` = NULL, `activated` = NULL, `expirationDate` = NULL, `maxWorkers` = NULL, `orgAdmin` = NULL, `organizationId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(accessRoleId, accessRoleIds, activated, expirationDate, maxWorkers, orgAdmin, organizationId)
      }
      self$`accessRoleId` <- `accessRoleId`
      self$`accessRoleIds` <- `accessRoleIds`
      self$`activated` <- `activated`
      self$`expirationDate` <- `expirationDate`
      self$`maxWorkers` <- `maxWorkers`
      self$`orgAdmin` <- `orgAdmin`
      self$`organizationId` <- `organizationId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(accessRoleId = self$`accessRoleId`, accessRoleIds = self$`accessRoleIds`, activated = self$`activated`, expirationDate = self$`expirationDate`, maxWorkers = self$`maxWorkers`, orgAdmin = self$`orgAdmin`, organizationId = self$`organizationId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accessRoleId`)) {
          sprintf(
            '"accessRoleId":
            "%s"
                  ',
            self$`accessRoleId`
          )
        },
        if (!is.null(self$`accessRoleIds`)) {
          sprintf(
            '"accessRoleIds":
            [%s]
                  ',
            paste(unlist(lapply(self$`accessRoleIds`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`activated`)) {
          sprintf(
            '"activated":
            %s
                  ',
            tolower(self$`activated`)
          )
        },
        if (!is.null(self$`expirationDate`)) {
          sprintf(
            '"expirationDate":
            "%s"
                  ',
            format(self$`expirationDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`maxWorkers`)) {
          sprintf(
            '"maxWorkers":
            %d
                  ',
            self$`maxWorkers`
          )
        },
        if (!is.null(self$`orgAdmin`)) {
          sprintf(
            '"orgAdmin":
            %s
                  ',
            tolower(self$`orgAdmin`)
          )
        },
        if (!is.null(self$`organizationId`)) {
          sprintf(
            '"organizationId":
            "%s"
                  ',
            self$`organizationId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param OrganizationUserPatchJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(OrganizationUserPatchJson, validateParams = FALSE) {
      OrganizationUserPatchObject <- jsonlite::fromJSON(OrganizationUserPatchJson)
      self$`accessRoleId` <- OrganizationUserPatchObject$`accessRoleId`
      self$`accessRoleIds` <- ApiClient$new()$deserializeObj(OrganizationUserPatchObject$`accessRoleIds`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`activated` <- OrganizationUserPatchObject$`activated`
      self$`expirationDate` <- ParseRFC3339Timestamp(OrganizationUserPatchObject$`expirationDate`)
      self$`maxWorkers` <- OrganizationUserPatchObject$`maxWorkers`
      self$`orgAdmin` <- OrganizationUserPatchObject$`orgAdmin`
      self$`organizationId` <- OrganizationUserPatchObject$`organizationId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
