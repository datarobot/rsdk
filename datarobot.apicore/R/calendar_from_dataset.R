# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CalendarFromDataset
#'
#' @description CalendarFromDataset Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetId  character The ID of the dataset from which to create the calendar.
#'
#' @field datasetVersionId  character [optional] The ID of the dataset version from which to create the calendar.
#'
#' @field deleteOnError  character [optional] Whether delete calendar file from Catalog if it&#39;s not valid.
#'
#' @field multiseriesIdColumns  list( character ) [optional] Optional multiseries id columns for calendar.
#'
#' @field name  character [optional] Optional name for catalog.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CalendarFromDataset <- R6::R6Class(
  "CalendarFromDataset",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datasetId = NULL, datasetVersionId = NULL, deleteOnError = NULL, multiseriesIdColumns = NULL, name = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`datasetVersionId`)) {
        stopifnot(is.character(`datasetVersionId`), length(`datasetVersionId`) == 1)
      }
      if (!is.null(`deleteOnError`)) {
        stopifnot(is.logical(`deleteOnError`), length(`deleteOnError`) == 1)
      }
      if (!is.null(`multiseriesIdColumns`)) {
        stopifnot(is.vector(`multiseriesIdColumns`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
    }
  ),
  public = list(
    `datasetId` = NULL,
    `datasetVersionId` = NULL,
    `deleteOnError` = NULL,
    `multiseriesIdColumns` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetId The ID of the dataset from which to create the calendar.
    #' @param datasetVersionId The ID of the dataset version from which to create the calendar.
    #' @param deleteOnError Whether delete calendar file from Catalog if it&#39;s not valid.
    #' @param multiseriesIdColumns Optional multiseries id columns for calendar.
    #' @param name Optional name for catalog.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `datasetVersionId` = NULL, `deleteOnError` = NULL, `multiseriesIdColumns` = NULL, `name` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasetId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datasetId, datasetVersionId, deleteOnError, multiseriesIdColumns, name)
      }
      self$`datasetId` <- `datasetId`
      self$`datasetVersionId` <- `datasetVersionId`
      self$`deleteOnError` <- `deleteOnError`
      sapply(`multiseriesIdColumns`, function(x) stopifnot(is.character(x)))
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datasetId = self$`datasetId`, datasetVersionId = self$`datasetVersionId`, deleteOnError = self$`deleteOnError`, multiseriesIdColumns = self$`multiseriesIdColumns`, name = self$`name`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetVersionId`)) {
          sprintf(
            '"datasetVersionId":
            "%s"
                  ',
            self$`datasetVersionId`
          )
        },
        if (!is.null(self$`deleteOnError`)) {
          sprintf(
            '"deleteOnError":
            %s
                  ',
            tolower(self$`deleteOnError`)
          )
        },
        if (!is.null(self$`multiseriesIdColumns`)) {
          sprintf(
            '"multiseriesIdColumns":
              [%s]
            ',
            paste(unlist(lapply(self$`multiseriesIdColumns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CalendarFromDatasetJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CalendarFromDatasetJson, validateParams = FALSE) {
      CalendarFromDatasetObject <- jsonlite::fromJSON(CalendarFromDatasetJson)
      self$`datasetId` <- CalendarFromDatasetObject$`datasetId`
      self$`datasetVersionId` <- CalendarFromDatasetObject$`datasetVersionId`
      self$`deleteOnError` <- CalendarFromDatasetObject$`deleteOnError`
      self$`multiseriesIdColumns` <- ApiClient$new()$deserializeObj(CalendarFromDatasetObject$`multiseriesIdColumns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`name` <- CalendarFromDatasetObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
