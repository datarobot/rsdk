# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MulticlassFeatureEffectCreate
#'
#' @description MulticlassFeatureEffectCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field features  list( character ) [optional] The list of features to use to calculate feature effects.
#'
#' @field rowCount  integer [optional] The number of rows from dataset to use for Feature Impact calculation.
#'
#' @field topNFeatures  integer [optional] Number of top features (ranked by feature impact) to use to calculate feature effects.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MulticlassFeatureEffectCreate <- R6::R6Class(
  "MulticlassFeatureEffectCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`features` = NULL, `rowCount` = NULL, `topNFeatures` = NULL) {
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`))
      }
      if (!is.null(`rowCount`)) {
        stopifnot(is.numeric(`rowCount`), length(`rowCount`) == 1)
      }
      if (!is.null(`topNFeatures`)) {
        stopifnot(is.numeric(`topNFeatures`), length(`topNFeatures`) == 1)
      }
    }
  ),
  public = list(
    `features` = NULL,
    `rowCount` = NULL,
    `topNFeatures` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param features The list of features to use to calculate feature effects.
    #' @param rowCount The number of rows from dataset to use for Feature Impact calculation.
    #' @param topNFeatures Number of top features (ranked by feature impact) to use to calculate feature effects.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`features` = NULL, `rowCount` = NULL, `topNFeatures` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(features, rowCount, topNFeatures)
      }
      sapply(`features`, function(x) stopifnot(is.character(x)))
      self$`rowCount` <- `rowCount`
      self$`topNFeatures` <- `topNFeatures`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(features = self$`features`, rowCount = self$`rowCount`, topNFeatures = self$`topNFeatures`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
                  ',
            paste(unlist(lapply(self$`features`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`rowCount`)) {
          sprintf(
            '"rowCount":
            %d
                  ',
            self$`rowCount`
          )
        },
        if (!is.null(self$`topNFeatures`)) {
          sprintf(
            '"topNFeatures":
            %d
                  ',
            self$`topNFeatures`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MulticlassFeatureEffectCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MulticlassFeatureEffectCreateJson, validateParams = FALSE) {
      MulticlassFeatureEffectCreateObject <- jsonlite::fromJSON(MulticlassFeatureEffectCreateJson)
      self$`features` <- ApiClient$new()$deserializeObj(MulticlassFeatureEffectCreateObject$`features`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`rowCount` <- MulticlassFeatureEffectCreateObject$`rowCount`
      self$`topNFeatures` <- MulticlassFeatureEffectCreateObject$`topNFeatures`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
