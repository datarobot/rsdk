# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionServerResponse
#'
#' @description PredictionServerResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field batchPredictions  \link{BatchPredictionStatus}
#'
#' @field datarobotMinuskey  character The &#x60;datarobot-key&#x60; header used in requests to this prediction server.
#'
#' @field id  character ID of the prediction server.
#'
#' @field url  character URL of the prediction server.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionServerResponse <- R6::R6Class(
  "PredictionServerResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(batchPredictions = NULL, datarobotMinuskey = NULL, id = NULL, url = NULL) {
      if (!is.null(`batchPredictions`)) {
        stopifnot(R6::is.R6(`batchPredictions`))
      }
      if (!is.null(`datarobotMinuskey`)) {
        stopifnot(is.character(`datarobotMinuskey`), length(`datarobotMinuskey`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
    }
  ),
  public = list(
    `batchPredictions` = NULL,
    `datarobotMinuskey` = NULL,
    `id` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param batchPredictions
    #' @param datarobotMinuskey The &#x60;datarobot-key&#x60; header used in requests to this prediction server.
    #' @param id ID of the prediction server.
    #' @param url URL of the prediction server.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`batchPredictions` = NULL, `datarobotMinuskey` = NULL, `id` = NULL, `url` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`batchPredictions`, `datarobotMinuskey`, `id`, `url`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(batchPredictions, datarobotMinuskey, id, url)
      }
      self$`batchPredictions` <- `batchPredictions`
      self$`datarobotMinuskey` <- `datarobotMinuskey`
      self$`id` <- `id`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(batchPredictions = self$`batchPredictions`, datarobotMinuskey = self$`datarobotMinuskey`, id = self$`id`, url = self$`url`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`batchPredictions`)) {
          sprintf(
            '"batchPredictions":
          %s
        ',
            jsonlite::toJSON(self$`batchPredictions`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`datarobotMinuskey`)) {
          sprintf(
            '"datarobot-key":
            "%s"
                  ',
            self$`datarobotMinuskey`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionServerResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionServerResponseJson, validateParams = FALSE) {
      PredictionServerResponseObject <- jsonlite::fromJSON(PredictionServerResponseJson)
      self$`batchPredictions` <- BatchPredictionStatus$new()$fromJSON(jsonlite::toJSON(PredictionServerResponseObject$batchPredictions, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`datarobotMinuskey` <- PredictionServerResponseObject$`datarobot-key`
      self$`id` <- PredictionServerResponseObject$`id`
      self$`url` <- PredictionServerResponseObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
