# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title LabelRelevancePlot
#'
#' @description LabelRelevancePlot Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field labelRelevance  integer Label relevance value.
#'
#' @field rowCount  integer Number of rows for which the label has the given relevance.
#'
#' @field rowPct  numeric Percentage of rows for which the label has the given relevance.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
LabelRelevancePlot <- R6::R6Class(
  "LabelRelevancePlot",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(labelRelevance = NULL, rowCount = NULL, rowPct = NULL) {
      if (!is.null(`labelRelevance`)) {
        stopifnot(is.numeric(`labelRelevance`), length(`labelRelevance`) == 1)
      }
      if (!is.null(`rowCount`)) {
        stopifnot(is.numeric(`rowCount`), length(`rowCount`) == 1)
      }
      if (!is.null(`rowPct`)) {
      }
    }
  ),
  public = list(
    `labelRelevance` = NULL,
    `rowCount` = NULL,
    `rowPct` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param labelRelevance Label relevance value.
    #' @param rowCount Number of rows for which the label has the given relevance.
    #' @param rowPct Percentage of rows for which the label has the given relevance.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`labelRelevance` = NULL, `rowCount` = NULL, `rowPct` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`labelRelevance`, `rowCount`, `rowPct`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(labelRelevance, rowCount, rowPct)
      }
      self$`labelRelevance` <- `labelRelevance`
      self$`rowCount` <- `rowCount`
      self$`rowPct` <- `rowPct`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(labelRelevance = self$`labelRelevance`, rowCount = self$`rowCount`, rowPct = self$`rowPct`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`labelRelevance`)) {
          sprintf(
            '"labelRelevance":
            %d
                  ',
            self$`labelRelevance`
          )
        },
        if (!is.null(self$`rowCount`)) {
          sprintf(
            '"rowCount":
            %d
                  ',
            self$`rowCount`
          )
        },
        if (!is.null(self$`rowPct`)) {
          sprintf(
            '"rowPct":
            %d
                  ',
            self$`rowPct`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param LabelRelevancePlotJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(LabelRelevancePlotJson, validateParams = FALSE) {
      LabelRelevancePlotObject <- jsonlite::fromJSON(LabelRelevancePlotJson)
      self$`labelRelevance` <- LabelRelevancePlotObject$`labelRelevance`
      self$`rowCount` <- LabelRelevancePlotObject$`rowCount`
      self$`rowPct` <- LabelRelevancePlotObject$`rowPct`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
