# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataDisparityInsights
#'
#' @description DataDisparityInsights Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field features  list( \link{AnalyzedFeature} ) [optional] A mapping of the feature name to the corresponding values on the graph.
#'
#' @field metric  character [optional] Metric used to calculate the impact of a feature on data disparity.
#'
#' @field protectedFeature  character [optional] Feature for which insights were computed.
#'
#' @field values  list( \link{FeatureCounts} ) [optional] Class count details for each class being compared.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataDisparityInsights <- R6::R6Class(
  "DataDisparityInsights",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`features` = NULL, `metric` = NULL, `protectedFeature` = NULL, `values` = NULL) {
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`))
      }
      if (!is.null(`metric`)) {
        stopifnot(is.character(`metric`), length(`metric`) == 1)
      }
      if (!is.null(`protectedFeature`)) {
        stopifnot(is.character(`protectedFeature`), length(`protectedFeature`) == 1)
      }
      if (!is.null(`values`)) {
        stopifnot(is.vector(`values`))
      }
    }
  ),
  public = list(
    `features` = NULL,
    `metric` = NULL,
    `protectedFeature` = NULL,
    `values` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param features A mapping of the feature name to the corresponding values on the graph.
    #' @param metric Metric used to calculate the impact of a feature on data disparity.
    #' @param protectedFeature Feature for which insights were computed.
    #' @param values Class count details for each class being compared.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`features` = NULL, `metric` = NULL, `protectedFeature` = NULL, `values` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(features, metric, protectedFeature, values)
      }
      sapply(`features`, function(x) stopifnot(R6::is.R6(x)))
      self$`metric` <- `metric`
      self$`protectedFeature` <- `protectedFeature`
      sapply(`values`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(features = self$`features`, metric = self$`metric`, protectedFeature = self$`protectedFeature`, values = self$`values`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
      ',
            paste(sapply(self$`features`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`metric`)) {
          sprintf(
            '"metric":
            "%s"
                  ',
            self$`metric`
          )
        },
        if (!is.null(self$`protectedFeature`)) {
          sprintf(
            '"protectedFeature":
            "%s"
                  ',
            self$`protectedFeature`
          )
        },
        if (!is.null(self$`values`)) {
          sprintf(
            '"values":
            [%s]
      ',
            paste(sapply(self$`values`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataDisparityInsightsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataDisparityInsightsJson, validateParams = FALSE) {
      DataDisparityInsightsObject <- jsonlite::fromJSON(DataDisparityInsightsJson)
      self$`features` <- ApiClient$new()$deserializeObj(DataDisparityInsightsObject$`features`, "array[AnalyzedFeature]", loadNamespace("datarobot.apicore"))
      self$`metric` <- DataDisparityInsightsObject$`metric`
      self$`protectedFeature` <- DataDisparityInsightsObject$`protectedFeature`
      self$`values` <- ApiClient$new()$deserializeObj(DataDisparityInsightsObject$`values`, "array[FeatureCounts]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
