# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentPermanentDelete
#'
#' @description DeploymentPermanentDelete Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field action  character Action to perform on these deleted deployments.
#'
#' @field deploymentIds  list( character ) ID of a list of deleted deployments to perform action on.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentPermanentDelete <- R6::R6Class(
  "DeploymentPermanentDelete",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(action = NULL, deploymentIds = NULL) {
      if (!is.null(`action`)) {
        stopifnot(is.character(`action`), length(`action`) == 1)
      }
      if (!is.null(`deploymentIds`)) {
        stopifnot(is.vector(`deploymentIds`))
      }
    }
  ),
  public = list(
    `action` = NULL,
    `deploymentIds` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param action Action to perform on these deleted deployments.
    #' @param deploymentIds ID of a list of deleted deployments to perform action on.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`action` = NULL, `deploymentIds` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`action`, `deploymentIds`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(action, deploymentIds)
      }
      self$`action` <- `action`
      sapply(`deploymentIds`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(action = self$`action`, deploymentIds = self$`deploymentIds`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`action`)) {
          sprintf(
            '"action":
            "%s"
                  ',
            self$`action`
          )
        },
        if (!is.null(self$`deploymentIds`)) {
          sprintf(
            '"deploymentIds":
              [%s]
            ',
            paste(unlist(lapply(self$`deploymentIds`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentPermanentDeleteJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentPermanentDeleteJson, validateParams = FALSE) {
      DeploymentPermanentDeleteObject <- jsonlite::fromJSON(DeploymentPermanentDeleteJson)
      self$`action` <- DeploymentPermanentDeleteObject$`action`
      self$`deploymentIds` <- ApiClient$new()$deserializeObj(DeploymentPermanentDeleteObject$`deploymentIds`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
