# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PerClusterText
#'
#' @description PerClusterText Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field clusterName  character Cluster name.
#'
#' @field missingRowsPercent  numeric [optional] A percentage of all rows that have a missing value for this feature.
#'
#' @field perValueStatistics  list( \link{PerValueStatisticTextListItem} ) Statistic value for feature values in all data or a cluster.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PerClusterText <- R6::R6Class(
  "PerClusterText",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(clusterName = NULL, missingRowsPercent = NULL, perValueStatistics = NULL) {
      if (!is.null(`clusterName`)) {
        stopifnot(is.character(`clusterName`), length(`clusterName`) == 1)
      }
      if (!is.null(`perValueStatistics`)) {
        stopifnot(is.vector(`perValueStatistics`))
      }
      if (!is.null(`missingRowsPercent`)) {
        stopifnot(is.numeric(`missingRowsPercent`), length(`missingRowsPercent`) == 1)
      }
    }
  ),
  public = list(
    `clusterName` = NULL,
    `missingRowsPercent` = NULL,
    `perValueStatistics` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param clusterName Cluster name.
    #' @param missingRowsPercent A percentage of all rows that have a missing value for this feature.
    #' @param perValueStatistics Statistic value for feature values in all data or a cluster.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`clusterName` = NULL, `perValueStatistics` = NULL, `missingRowsPercent` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`clusterName`, `perValueStatistics`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(clusterName, missingRowsPercent, perValueStatistics)
      }
      self$`clusterName` <- `clusterName`
      self$`missingRowsPercent` <- `missingRowsPercent`
      sapply(`perValueStatistics`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(clusterName = self$`clusterName`, missingRowsPercent = self$`missingRowsPercent`, perValueStatistics = self$`perValueStatistics`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`clusterName`)) {
          sprintf(
            '"clusterName":
            "%s"
                  ',
            self$`clusterName`
          )
        },
        if (!is.null(self$`missingRowsPercent`)) {
          sprintf(
            '"missingRowsPercent":
            %d
                  ',
            self$`missingRowsPercent`
          )
        },
        if (!is.null(self$`perValueStatistics`)) {
          sprintf(
            '"perValueStatistics":
            [%s]
',
            paste(sapply(self$`perValueStatistics`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PerClusterTextJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PerClusterTextJson, validateParams = FALSE) {
      PerClusterTextObject <- jsonlite::fromJSON(PerClusterTextJson)
      self$`clusterName` <- PerClusterTextObject$`clusterName`
      self$`missingRowsPercent` <- PerClusterTextObject$`missingRowsPercent`
      self$`perValueStatistics` <- ApiClient$new()$deserializeObj(PerClusterTextObject$`perValueStatistics`, "array[PerValueStatisticTextListItem]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
