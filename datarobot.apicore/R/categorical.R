# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Categorical
#'
#' @description Categorical Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field allData  \link{PerValueStatistics}
#'
#' @field insightName  character Insight name.
#'
#' @field perCluster  list( \link{PerClusterCategorical} ) Statistic values for different feature values in this cluster.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Categorical <- R6::R6Class(
  "Categorical",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`allData` = NULL, `insightName` = NULL, `perCluster` = NULL) {
      if (!is.null(`allData`)) {
        stopifnot(R6::is.R6(`allData`))
      }
      if (!is.null(`insightName`)) {
        stopifnot(is.character(`insightName`), length(`insightName`) == 1)
      }
      if (!is.null(`perCluster`)) {
        stopifnot(is.vector(`perCluster`), sapply(`perCluster`, R6::is.R6))
      }
    }
  ),
  public = list(
    `allData` = NULL,
    `insightName` = NULL,
    `perCluster` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param allData
    #' @param insightName Insight name.
    #' @param perCluster Statistic values for different feature values in this cluster.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`allData` = NULL, `insightName` = NULL, `perCluster` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`allData`, `insightName`, `perCluster`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(allData, insightName, perCluster)
      }
      self$`allData` <- `allData`
      self$`insightName` <- `insightName`
      self$`perCluster` <- `perCluster`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(allData = self$`allData`, insightName = self$`insightName`, perCluster = self$`perCluster`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`allData`)) {
          sprintf(
            '"allData":
            %s
      ',
            jsonlite::toJSON(self$`allData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`insightName`)) {
          sprintf(
            '"insightName":
            "%s"
                  ',
            self$`insightName`
          )
        },
        if (!is.null(self$`perCluster`)) {
          sprintf(
            '"perCluster":
            [%s]
      ',
            paste(sapply(self$`perCluster`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CategoricalJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CategoricalJson, validateParams = FALSE) {
      CategoricalObject <- jsonlite::fromJSON(CategoricalJson)
      self$`allData` <- PerValueStatistics$new()$fromJSON(jsonlite::toJSON(CategoricalObject$allData, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`insightName` <- CategoricalObject$`insightName`
      self$`perCluster` <- ApiClient$new()$deserializeObj(CategoricalObject$`perCluster`, "array[PerClusterCategorical]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
