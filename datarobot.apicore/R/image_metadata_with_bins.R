# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ImageMetadataWithBins
#'
#' @description ImageMetadataWithBins Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field height  integer Height of the image in pixels
#'
#' @field imageId  character Id of the image. The actual image file can be retrieved with :http:get:&#x60;/api/v2/projects/(projectId)/images/(imageId)/file/&#x60;
#'
#' @field targetBinEnd  integer [optional] Target value for bin end for regression, null for classification
#'
#' @field targetBinRowCount  integer Number of rows in this target bin
#'
#' @field targetBinStart  integer [optional] Target value for bin start for regression, null for classification
#'
#' @field targetValue  numeric [optional] Target value
#'
#' @field width  integer Width of the image in pixels
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ImageMetadataWithBins <- R6::R6Class(
  "ImageMetadataWithBins",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(height = NULL, imageId = NULL, targetBinEnd = NULL, targetBinRowCount = NULL, targetBinStart = NULL, targetValue = NULL, width = NULL) {
      if (!is.null(`height`)) {
        stopifnot(is.numeric(`height`), length(`height`) == 1)
      }
      if (!is.null(`imageId`)) {
        stopifnot(is.character(`imageId`), length(`imageId`) == 1)
      }
      if (!is.null(`targetBinRowCount`)) {
        stopifnot(is.numeric(`targetBinRowCount`), length(`targetBinRowCount`) == 1)
      }
      if (!is.null(`width`)) {
        stopifnot(is.numeric(`width`), length(`width`) == 1)
      }
      if (!is.null(`targetBinEnd`)) {
        stopifnot(is.numeric(`targetBinEnd`), length(`targetBinEnd`) == 1)
      }
      if (!is.null(`targetBinStart`)) {
        stopifnot(is.numeric(`targetBinStart`), length(`targetBinStart`) == 1)
      }
      if (!is.null(`targetValue`)) {
        stopifnot(is.numeric(`targetValue`), length(`targetValue`) == 1)
      }
    }
  ),
  public = list(
    `height` = NULL,
    `imageId` = NULL,
    `targetBinEnd` = NULL,
    `targetBinRowCount` = NULL,
    `targetBinStart` = NULL,
    `targetValue` = NULL,
    `width` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param height Height of the image in pixels
    #' @param imageId Id of the image. The actual image file can be retrieved with :http:get:&#x60;/api/v2/projects/(projectId)/images/(imageId)/file/&#x60;
    #' @param targetBinEnd Target value for bin end for regression, null for classification
    #' @param targetBinRowCount Number of rows in this target bin
    #' @param targetBinStart Target value for bin start for regression, null for classification
    #' @param targetValue Target value
    #' @param width Width of the image in pixels
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`height` = NULL, `imageId` = NULL, `targetBinRowCount` = NULL, `width` = NULL, `targetBinEnd` = NULL, `targetBinStart` = NULL, `targetValue` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`height`, `imageId`, `targetBinRowCount`, `width`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(height, imageId, targetBinEnd, targetBinRowCount, targetBinStart, targetValue, width)
      }
      self$`height` <- `height`
      self$`imageId` <- `imageId`
      self$`targetBinEnd` <- `targetBinEnd`
      self$`targetBinRowCount` <- `targetBinRowCount`
      self$`targetBinStart` <- `targetBinStart`
      self$`targetValue` <- `targetValue`
      self$`width` <- `width`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(height = self$`height`, imageId = self$`imageId`, targetBinEnd = self$`targetBinEnd`, targetBinRowCount = self$`targetBinRowCount`, targetBinStart = self$`targetBinStart`, targetValue = self$`targetValue`, width = self$`width`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`height`)) {
          sprintf(
            '"height":
            %d
                  ',
            self$`height`
          )
        },
        if (!is.null(self$`imageId`)) {
          sprintf(
            '"imageId":
            "%s"
                  ',
            self$`imageId`
          )
        },
        if (!is.null(self$`targetBinEnd`)) {
          sprintf(
            '"targetBinEnd":
            %d
                  ',
            self$`targetBinEnd`
          )
        },
        if (!is.null(self$`targetBinRowCount`)) {
          sprintf(
            '"targetBinRowCount":
            %d
                  ',
            self$`targetBinRowCount`
          )
        },
        if (!is.null(self$`targetBinStart`)) {
          sprintf(
            '"targetBinStart":
            %d
                  ',
            self$`targetBinStart`
          )
        },
        if (!is.null(self$`targetValue`)) {
          sprintf(
            '"targetValue":
            %d
                  ',
            self$`targetValue`
          )
        },
        if (!is.null(self$`width`)) {
          sprintf(
            '"width":
            %d
                  ',
            self$`width`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ImageMetadataWithBinsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ImageMetadataWithBinsJson, validateParams = FALSE) {
      ImageMetadataWithBinsObject <- jsonlite::fromJSON(ImageMetadataWithBinsJson)
      self$`height` <- ImageMetadataWithBinsObject$`height`
      self$`imageId` <- ImageMetadataWithBinsObject$`imageId`
      self$`targetBinEnd` <- ImageMetadataWithBinsObject$`targetBinEnd`
      self$`targetBinRowCount` <- ImageMetadataWithBinsObject$`targetBinRowCount`
      self$`targetBinStart` <- ImageMetadataWithBinsObject$`targetBinStart`
      self$`targetValue` <- ImageMetadataWithBinsObject$`targetValue`
      self$`width` <- ImageMetadataWithBinsObject$`width`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
