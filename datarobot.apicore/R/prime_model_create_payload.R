# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PrimeModelCreatePayload
#'
#' @description PrimeModelCreatePayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field parentModelId  character the model being approximated
#'
#' @field rulesetId  integer the ID of the ruleset to use
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PrimeModelCreatePayload <- R6::R6Class(
  "PrimeModelCreatePayload",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`parentModelId` = NULL, `rulesetId` = NULL) {
      if (!is.null(`parentModelId`)) {
        stopifnot(is.character(`parentModelId`), length(`parentModelId`) == 1)
      }
      if (!is.null(`rulesetId`)) {
        stopifnot(is.numeric(`rulesetId`), length(`rulesetId`) == 1)
      }
    }
  ),
  public = list(
    `parentModelId` = NULL,
    `rulesetId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param parentModelId the model being approximated
    #' @param rulesetId the ID of the ruleset to use
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`parentModelId` = NULL, `rulesetId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`parentModelId`, `rulesetId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(parentModelId, rulesetId)
      }
      self$`parentModelId` <- `parentModelId`
      self$`rulesetId` <- `rulesetId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(parentModelId = self$`parentModelId`, rulesetId = self$`rulesetId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`parentModelId`)) {
          sprintf(
            '"parentModelId":
            "%s"
                  ',
            self$`parentModelId`
          )
        },
        if (!is.null(self$`rulesetId`)) {
          sprintf(
            '"rulesetId":
            %d
                  ',
            self$`rulesetId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PrimeModelCreatePayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PrimeModelCreatePayloadJson, validateParams = FALSE) {
      PrimeModelCreatePayloadObject <- jsonlite::fromJSON(PrimeModelCreatePayloadJson)
      self$`parentModelId` <- PrimeModelCreatePayloadObject$`parentModelId`
      self$`rulesetId` <- PrimeModelCreatePayloadObject$`rulesetId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
