# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApplicationRecentEvents
#'
#' @description ApplicationRecentEvents Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field eventType  character The type of event
#'
#' @field metadata  \link{RecentEventsMetadata}
#'
#' @field timestamp  character The timestamp when the event was recorded
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApplicationRecentEvents <- R6::R6Class(
  "ApplicationRecentEvents",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(eventType = NULL, metadata = NULL, timestamp = NULL) {
      if (!is.null(`eventType`)) {
        stopifnot(is.character(`eventType`), length(`eventType`) == 1)
      }
      if (!is.null(`metadata`)) {
        stopifnot(R6::is.R6(`metadata`))
      }
      if (!is.null(`timestamp`)) {
        stopifnot(inherits(`timestamp`, "POSIXt"))
      }
    }
  ),
  public = list(
    `eventType` = NULL,
    `metadata` = NULL,
    `timestamp` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param eventType The type of event
    #' @param metadata
    #' @param timestamp The timestamp when the event was recorded
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`eventType` = NULL, `metadata` = NULL, `timestamp` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`eventType`, `metadata`, `timestamp`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(eventType, metadata, timestamp)
      }
      self$`eventType` <- `eventType`
      self$`metadata` <- `metadata`
      self$`timestamp` <- `timestamp`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(eventType = self$`eventType`, metadata = self$`metadata`, timestamp = self$`timestamp`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`eventType`)) {
          sprintf(
            '"eventType":
            "%s"
                  ',
            self$`eventType`
          )
        },
        if (!is.null(self$`metadata`)) {
          sprintf(
            '"metadata":
          %s
        ',
            jsonlite::toJSON(self$`metadata`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`timestamp`)) {
          sprintf(
            '"timestamp":
            "%s"
                  ',
            format(self$`timestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApplicationRecentEventsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApplicationRecentEventsJson, validateParams = FALSE) {
      ApplicationRecentEventsObject <- jsonlite::fromJSON(ApplicationRecentEventsJson)
      self$`eventType` <- ApplicationRecentEventsObject$`eventType`
      self$`metadata` <- RecentEventsMetadata$new()$fromJSON(jsonlite::toJSON(ApplicationRecentEventsObject$metadata, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`timestamp` <- ParseRFC3339Timestamp(ApplicationRecentEventsObject$`timestamp`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
