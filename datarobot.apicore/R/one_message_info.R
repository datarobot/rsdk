# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title OneMessageInfo
#'
#' @description OneMessageInfo Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field additionalInfo  list( character ) [optional] Zero or more text strings for secondary display after user clicks for more information.
#'
#' @field messageLevel  character Message severity level.
#'
#' @field messageText  character Text for primary display in UI.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
OneMessageInfo <- R6::R6Class(
  "OneMessageInfo",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`additionalInfo` = NULL, `messageLevel` = NULL, `messageText` = NULL) {
      if (!is.null(`messageLevel`)) {
        stopifnot(is.character(`messageLevel`), length(`messageLevel`) == 1)
      }
      if (!is.null(`messageText`)) {
        stopifnot(is.character(`messageText`), length(`messageText`) == 1)
      }
      if (!is.null(`additionalInfo`)) {
        stopifnot(is.vector(`additionalInfo`))
      }
    }
  ),
  public = list(
    `additionalInfo` = NULL,
    `messageLevel` = NULL,
    `messageText` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param additionalInfo Zero or more text strings for secondary display after user clicks for more information.
    #' @param messageLevel Message severity level.
    #' @param messageText Text for primary display in UI.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`messageLevel` = NULL, `messageText` = NULL, `additionalInfo` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`messageLevel`, `messageText`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(additionalInfo, messageLevel, messageText)
      }
      sapply(`additionalInfo`, function(x) stopifnot(is.character(x)))
      self$`messageLevel` <- `messageLevel`
      self$`messageText` <- `messageText`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(additionalInfo = self$`additionalInfo`, messageLevel = self$`messageLevel`, messageText = self$`messageText`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`additionalInfo`)) {
          sprintf(
            '"additionalInfo":
            [%s]
                  ',
            paste(unlist(lapply(self$`additionalInfo`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`messageLevel`)) {
          sprintf(
            '"messageLevel":
            "%s"
                  ',
            self$`messageLevel`
          )
        },
        if (!is.null(self$`messageText`)) {
          sprintf(
            '"messageText":
            "%s"
                  ',
            self$`messageText`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param OneMessageInfoJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(OneMessageInfoJson, validateParams = FALSE) {
      OneMessageInfoObject <- jsonlite::fromJSON(OneMessageInfoJson)
      self$`additionalInfo` <- ApiClient$new()$deserializeObj(OneMessageInfoObject$`additionalInfo`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`messageLevel` <- OneMessageInfoObject$`messageLevel`
      self$`messageText` <- OneMessageInfoObject$`messageText`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
