# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SecondaryDatasetCreate
#'
#' @description SecondaryDatasetCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field config  list( \link{SecondaryDatasetConfig} ) [optional] Graph-specific secondary datasets. Deprecated in version v2.23
#'
#' @field featurelistId  character [optional] Feature list ID of the configuration.
#'
#' @field modelId  character [optional] ID of the model.
#'
#' @field name  character Name of the configuration.
#'
#' @field save  character [optional] ID of the model.
#'
#' @field secondaryDatasets  list( \link{SecondaryDataset} ) [optional] Whether to save the configuration. If set to False the configuration is validated but not saved. Defaults to True.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SecondaryDatasetCreate <- R6::R6Class(
  "SecondaryDatasetCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`config` = NULL, `featurelistId` = NULL, `modelId` = NULL, `name` = NULL, `save` = NULL, `secondaryDatasets` = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`config`)) {
        stopifnot(is.vector(`config`))
      }
      if (!is.null(`featurelistId`)) {
        stopifnot(is.character(`featurelistId`), length(`featurelistId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`save`)) {
        stopifnot(is.logical(`save`), length(`save`) == 1)
      }
      if (!is.null(`secondaryDatasets`)) {
        stopifnot(is.vector(`secondaryDatasets`))
      }
    }
  ),
  public = list(
    `config` = NULL,
    `featurelistId` = NULL,
    `modelId` = NULL,
    `name` = NULL,
    `save` = NULL,
    `secondaryDatasets` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param config Graph-specific secondary datasets. Deprecated in version v2.23
    #' @param featurelistId Feature list ID of the configuration.
    #' @param modelId ID of the model.
    #' @param name Name of the configuration.
    #' @param save ID of the model.
    #' @param secondaryDatasets Whether to save the configuration. If set to False the configuration is validated but not saved. Defaults to True.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `config` = NULL, `featurelistId` = NULL, `modelId` = NULL, `save` = TRUE, `secondaryDatasets` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`name`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(config, featurelistId, modelId, name, save, secondaryDatasets)
      }
      sapply(`config`, function(x) stopifnot(R6::is.R6(x)))
      self$`featurelistId` <- `featurelistId`
      self$`modelId` <- `modelId`
      self$`name` <- `name`
      self$`save` <- `save`
      sapply(`secondaryDatasets`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(config = self$`config`, featurelistId = self$`featurelistId`, modelId = self$`modelId`, name = self$`name`, save = self$`save`, secondaryDatasets = self$`secondaryDatasets`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`config`)) {
          sprintf(
            '"config":
            [%s]
      ',
            paste(sapply(self$`config`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`featurelistId`)) {
          sprintf(
            '"featurelistId":
            "%s"
                  ',
            self$`featurelistId`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`save`)) {
          sprintf(
            '"save":
            %s
                  ',
            tolower(self$`save`)
          )
        },
        if (!is.null(self$`secondaryDatasets`)) {
          sprintf(
            '"secondaryDatasets":
            [%s]
      ',
            paste(sapply(self$`secondaryDatasets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SecondaryDatasetCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SecondaryDatasetCreateJson, validateParams = FALSE) {
      SecondaryDatasetCreateObject <- jsonlite::fromJSON(SecondaryDatasetCreateJson)
      self$`config` <- ApiClient$new()$deserializeObj(SecondaryDatasetCreateObject$`config`, "array[SecondaryDatasetConfig]", loadNamespace("datarobot.apicore"))
      self$`featurelistId` <- SecondaryDatasetCreateObject$`featurelistId`
      self$`modelId` <- SecondaryDatasetCreateObject$`modelId`
      self$`name` <- SecondaryDatasetCreateObject$`name`
      self$`save` <- SecondaryDatasetCreateObject$`save`
      self$`secondaryDatasets` <- ApiClient$new()$deserializeObj(SecondaryDatasetCreateObject$`secondaryDatasets`, "array[SecondaryDataset]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
