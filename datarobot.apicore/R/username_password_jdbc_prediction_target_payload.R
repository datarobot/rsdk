# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UsernamePasswordJdbcPredictionTargetPayload
#'
#' @description UsernamePasswordJdbcPredictionTargetPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasourceId  character Id of the DSS data source object
#'
#' @field password  character User password to connect to the data location
#'
#' @field typeId  character For JDBC should be set to &#x60;jdbcDatasource&#x60;
#'
#' @field userName  character User name to connect to the data location
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UsernamePasswordJdbcPredictionTargetPayload <- R6::R6Class(
  "UsernamePasswordJdbcPredictionTargetPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datasourceId = NULL, password = NULL, typeId = NULL, userName = NULL) {
      if (!is.null(`datasourceId`)) {
        stopifnot(is.character(`datasourceId`), length(`datasourceId`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`typeId`)) {
        stopifnot(is.character(`typeId`), length(`typeId`) == 1)
      }
      if (!is.null(`userName`)) {
        stopifnot(is.character(`userName`), length(`userName`) == 1)
      }
    }
  ),
  public = list(
    `datasourceId` = NULL,
    `password` = NULL,
    `typeId` = NULL,
    `userName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasourceId Id of the DSS data source object
    #' @param password User password to connect to the data location
    #' @param typeId For JDBC should be set to &#x60;jdbcDatasource&#x60;
    #' @param userName User name to connect to the data location
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `datasourceId` = NULL, `password` = NULL, `typeId` = NULL, `userName` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasourceId`, `password`, `typeId`, `userName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datasourceId, password, typeId, userName)
      }
      self$`datasourceId` <- `datasourceId`
      self$`password` <- `password`
      self$`typeId` <- `typeId`
      self$`userName` <- `userName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datasourceId = self$`datasourceId`, password = self$`password`, typeId = self$`typeId`, userName = self$`userName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasourceId`)) {
        sprintf(
        '"datasourceId":
          "%s"
                ',
        self$`datasourceId`
        )},
        if (!is.null(self$`password`)) {
        sprintf(
        '"password":
          "%s"
                ',
        self$`password`
        )},
        if (!is.null(self$`typeId`)) {
        sprintf(
        '"typeId":
          "%s"
                ',
        self$`typeId`
        )},
        if (!is.null(self$`userName`)) {
        sprintf(
        '"userName":
          "%s"
                ',
        self$`userName`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UsernamePasswordJdbcPredictionTargetPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UsernamePasswordJdbcPredictionTargetPayloadJson, validateParams = FALSE) {
      UsernamePasswordJdbcPredictionTargetPayloadObject <- jsonlite::fromJSON(UsernamePasswordJdbcPredictionTargetPayloadJson)
      self$`datasourceId` <- UsernamePasswordJdbcPredictionTargetPayloadObject$`datasourceId`
      self$`password` <- UsernamePasswordJdbcPredictionTargetPayloadObject$`password`
      self$`typeId` <- UsernamePasswordJdbcPredictionTargetPayloadObject$`typeId`
      self$`userName` <- UsernamePasswordJdbcPredictionTargetPayloadObject$`userName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
