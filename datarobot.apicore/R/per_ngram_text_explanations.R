# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PerNgramTextExplanations
#'
#' @description PerNgramTextExplanations Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field isUnknown  character Whether the ngram is identifiable by the blueprint or not.
#'
#' @field ngrams  list( \link{PredictionExplanationsPredictionValues} ) List of JSON objects with the ngram starting index, ngram ending index and unknown ngram information.
#'
#' @field qualitativateStrength  character A human-readable description of how strongly these ngrams&#39;s affected the prediction(e.g. &#39;+++&#39;, &#39;--&#39;, &#39;+&#39;, &#39;&lt;+&#39;, &#39;&lt;-&#39;).
#'
#' @field strength  numeric The amount these ngrams&#39;s affected the prediction.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PerNgramTextExplanations <- R6::R6Class(
  "PerNgramTextExplanations",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`isUnknown` = NULL, `ngrams` = NULL, `qualitativateStrength` = NULL, `strength` = NULL) {
      if (!is.null(`isUnknown`)) {
        stopifnot(is.logical(`isUnknown`), length(`isUnknown`) == 1)
      }
      if (!is.null(`ngrams`)) {
        stopifnot(is.vector(`ngrams`), sapply(`ngrams`, R6::is.R6))
      }
      if (!is.null(`qualitativateStrength`)) {
        stopifnot(is.character(`qualitativateStrength`), length(`qualitativateStrength`) == 1)
      }
      if (!is.null(`strength`)) {
      }
    }
  ),
  public = list(
    `isUnknown` = NULL,
    `ngrams` = NULL,
    `qualitativateStrength` = NULL,
    `strength` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param isUnknown Whether the ngram is identifiable by the blueprint or not.
    #' @param ngrams List of JSON objects with the ngram starting index, ngram ending index and unknown ngram information.
    #' @param qualitativateStrength A human-readable description of how strongly these ngrams&#39;s affected the prediction(e.g. &#39;+++&#39;, &#39;--&#39;, &#39;+&#39;, &#39;&lt;+&#39;, &#39;&lt;-&#39;).
    #' @param strength The amount these ngrams&#39;s affected the prediction.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`isUnknown` = NULL, `ngrams` = NULL, `qualitativateStrength` = NULL, `strength` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`isUnknown`, `ngrams`, `qualitativateStrength`, `strength`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(isUnknown, ngrams, qualitativateStrength, strength)
      }
      self$`isUnknown` <- `isUnknown`
      self$`ngrams` <- `ngrams`
      self$`qualitativateStrength` <- `qualitativateStrength`
      self$`strength` <- `strength`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(isUnknown = self$`isUnknown`, ngrams = self$`ngrams`, qualitativateStrength = self$`qualitativateStrength`, strength = self$`strength`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`isUnknown`)) {
          sprintf(
            '"isUnknown":
            %s
                  ',
            tolower(self$`isUnknown`)
          )
        },
        if (!is.null(self$`ngrams`)) {
          sprintf(
            '"ngrams":
            [%s]
      ',
            paste(sapply(self$`ngrams`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`qualitativateStrength`)) {
          sprintf(
            '"qualitativateStrength":
            "%s"
                  ',
            self$`qualitativateStrength`
          )
        },
        if (!is.null(self$`strength`)) {
          sprintf(
            '"strength":
            %d
                  ',
            self$`strength`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PerNgramTextExplanationsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PerNgramTextExplanationsJson, validateParams = FALSE) {
      PerNgramTextExplanationsObject <- jsonlite::fromJSON(PerNgramTextExplanationsJson)
      self$`isUnknown` <- PerNgramTextExplanationsObject$`isUnknown`
      self$`ngrams` <- ApiClient$new()$deserializeObj(PerNgramTextExplanationsObject$`ngrams`, "array[PredictionExplanationsPredictionValues]", loadNamespace("datarobot.apicore"))
      self$`qualitativateStrength` <- PerNgramTextExplanationsObject$`qualitativateStrength`
      self$`strength` <- PerNgramTextExplanationsObject$`strength`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
