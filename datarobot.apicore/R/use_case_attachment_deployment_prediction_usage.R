# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseAttachmentDeploymentPredictionUsage
#'
#' @description UseCaseAttachmentDeploymentPredictionUsage Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dailyRates  list( integer ) List of daily request counts
#'
#' @field lastTimestamp  character Last prediction time
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseAttachmentDeploymentPredictionUsage <- R6::R6Class(
  "UseCaseAttachmentDeploymentPredictionUsage",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(dailyRates = NULL, lastTimestamp = NULL) {
      if (!is.null(`dailyRates`)) {
        stopifnot(is.vector(`dailyRates`))
      }
      if (!is.null(`lastTimestamp`)) {
        stopifnot(inherits(`lastTimestamp`, "POSIXt"))
      }
    }
  ),
  public = list(
    `dailyRates` = NULL,
    `lastTimestamp` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dailyRates List of daily request counts
    #' @param lastTimestamp Last prediction time
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dailyRates` = NULL, `lastTimestamp` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`dailyRates`, `lastTimestamp`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(dailyRates, lastTimestamp)
      }
      sapply(`dailyRates`, function(x) stopifnot(is.character(x)))
      self$`lastTimestamp` <- `lastTimestamp`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(dailyRates = self$`dailyRates`, lastTimestamp = self$`lastTimestamp`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dailyRates`)) {
          sprintf(
            '"dailyRates":
              [%s]
            ',
            paste(unlist(lapply(self$`dailyRates`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`lastTimestamp`)) {
          sprintf(
            '"lastTimestamp":
            "%s"
                  ',
            format(self$`lastTimestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseAttachmentDeploymentPredictionUsageJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseAttachmentDeploymentPredictionUsageJson, validateParams = FALSE) {
      UseCaseAttachmentDeploymentPredictionUsageObject <- jsonlite::fromJSON(UseCaseAttachmentDeploymentPredictionUsageJson)
      self$`dailyRates` <- ApiClient$new()$deserializeObj(UseCaseAttachmentDeploymentPredictionUsageObject$`dailyRates`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`lastTimestamp` <- ParseRFC3339Timestamp(UseCaseAttachmentDeploymentPredictionUsageObject$`lastTimestamp`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
