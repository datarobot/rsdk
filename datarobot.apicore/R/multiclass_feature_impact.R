# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MulticlassFeatureImpact
#'
#' @description MulticlassFeatureImpact Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field class  character Target class label.
#'
#' @field featureImpacts  list( \link{FeatureImpactItem} ) A list which contains feature impact scores for each feature used by a model. If the model has more than 1000 features, the most important 1000 features are returned.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MulticlassFeatureImpact <- R6::R6Class(
  "MulticlassFeatureImpact",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`class` = NULL, `featureImpacts` = NULL) {
      if (!is.null(`class`)) {
        stopifnot(is.character(`class`), length(`class`) == 1)
      }
      if (!is.null(`featureImpacts`)) {
        stopifnot(is.vector(`featureImpacts`), sapply(`featureImpacts`, R6::is.R6))
      }
    }
  ),
  public = list(
    `class` = NULL,
    `featureImpacts` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param class Target class label.
    #' @param featureImpacts A list which contains feature impact scores for each feature used by a model. If the model has more than 1000 features, the most important 1000 features are returned.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`class` = NULL, `featureImpacts` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`class`, `featureImpacts`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(class, featureImpacts)
      }
      self$`class` <- `class`
      self$`featureImpacts` <- `featureImpacts`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(class = self$`class`, featureImpacts = self$`featureImpacts`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`class`)) {
          sprintf(
            '"class":
            "%s"
                  ',
            self$`class`
          )
        },
        if (!is.null(self$`featureImpacts`)) {
          sprintf(
            '"featureImpacts":
            [%s]
      ',
            paste(sapply(self$`featureImpacts`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MulticlassFeatureImpactJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MulticlassFeatureImpactJson, validateParams = FALSE) {
      MulticlassFeatureImpactObject <- jsonlite::fromJSON(MulticlassFeatureImpactJson)
      self$`class` <- MulticlassFeatureImpactObject$`class`
      self$`featureImpacts` <- ApiClient$new()$deserializeObj(MulticlassFeatureImpactObject$`featureImpacts`, "array[FeatureImpactItem]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
