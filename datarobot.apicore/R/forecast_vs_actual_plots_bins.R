# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ForecastVsActualPlotsBins
#'
#' @description ForecastVsActualPlotsBins Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actual  numeric Average actual value of the target in the bin. &#x60;null&#x60; if there are no entries in the bin.
#'
#' @field endDate  character The datetime of the end of the bin (exclusive).
#'
#' @field error  numeric Average absolute residual value of the bin. &#x60;null&#x60; if there are no entries in the bin.
#'
#' @field forecasts  list( numeric ) An array of average forecasts for the model for each forecast distance. Empty if there are no forecasts in the bin. Each index in the &#x60;forecasts&#x60; array maps to &#x60;forecastDistances&#x60; array index.
#'
#' @field frequency  integer Indicates number of values averaged in bin in case of a resolution change.
#'
#' @field normalizedError  numeric Normalized average absolute residual value of the bin. &#x60;null&#x60; if there are no entries in the bin.
#'
#' @field startDate  character The datetime of the start of the bin (inclusive).
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ForecastVsActualPlotsBins <- R6::R6Class(
  "ForecastVsActualPlotsBins",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`actual` = NULL, `endDate` = NULL, `error` = NULL, `forecasts` = NULL, `frequency` = NULL, `normalizedError` = NULL, `startDate` = NULL) {
      if (!is.null(`actual`)) {
      }
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`error`)) {
      }
      if (!is.null(`forecasts`)) {
        stopifnot(is.vector(`forecasts`), sapply(`forecasts`, is.numeric))
      }
      if (!is.null(`frequency`)) {
        stopifnot(is.numeric(`frequency`), length(`frequency`) == 1)
      }
      if (!is.null(`normalizedError`)) {
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `actual` = NULL,
    `endDate` = NULL,
    `error` = NULL,
    `forecasts` = NULL,
    `frequency` = NULL,
    `normalizedError` = NULL,
    `startDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actual Average actual value of the target in the bin. &#x60;null&#x60; if there are no entries in the bin.
    #' @param endDate The datetime of the end of the bin (exclusive).
    #' @param error Average absolute residual value of the bin. &#x60;null&#x60; if there are no entries in the bin.
    #' @param forecasts An array of average forecasts for the model for each forecast distance. Empty if there are no forecasts in the bin. Each index in the &#x60;forecasts&#x60; array maps to &#x60;forecastDistances&#x60; array index.
    #' @param frequency Indicates number of values averaged in bin in case of a resolution change.
    #' @param normalizedError Normalized average absolute residual value of the bin. &#x60;null&#x60; if there are no entries in the bin.
    #' @param startDate The datetime of the start of the bin (inclusive).
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actual` = NULL, `endDate` = NULL, `error` = NULL, `forecasts` = NULL, `frequency` = NULL, `normalizedError` = NULL, `startDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`actual`, `endDate`, `error`, `forecasts`, `frequency`, `normalizedError`, `startDate`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(actual, endDate, error, forecasts, frequency, normalizedError, startDate)
      }
      self$`actual` <- `actual`
      self$`endDate` <- `endDate`
      self$`error` <- `error`
      self$`forecasts` <- `forecasts`
      self$`frequency` <- `frequency`
      self$`normalizedError` <- `normalizedError`
      self$`startDate` <- `startDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(actual = self$`actual`, endDate = self$`endDate`, error = self$`error`, forecasts = self$`forecasts`, frequency = self$`frequency`, normalizedError = self$`normalizedError`, startDate = self$`startDate`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actual`)) {
          sprintf(
            '"actual":
            %d
                  ',
            self$`actual`
          )
        },
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`error`)) {
          sprintf(
            '"error":
            %d
                  ',
            self$`error`
          )
        },
        if (!is.null(self$`forecasts`)) {
          sprintf(
            '"forecasts":
            [%s]
                  ',
            paste(unlist(lapply(self$`forecasts`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`frequency`)) {
          sprintf(
            '"frequency":
            %d
                  ',
            self$`frequency`
          )
        },
        if (!is.null(self$`normalizedError`)) {
          sprintf(
            '"normalizedError":
            %d
                  ',
            self$`normalizedError`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ForecastVsActualPlotsBinsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ForecastVsActualPlotsBinsJson, validateParams = FALSE) {
      ForecastVsActualPlotsBinsObject <- jsonlite::fromJSON(ForecastVsActualPlotsBinsJson)
      self$`actual` <- ForecastVsActualPlotsBinsObject$`actual`
      self$`endDate` <- ParseRFC3339Timestamp(ForecastVsActualPlotsBinsObject$`endDate`)
      self$`error` <- ForecastVsActualPlotsBinsObject$`error`
      self$`forecasts` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsBinsObject$`forecasts`, "array[numeric]", loadNamespace("datarobot.apicore"))
      self$`frequency` <- ForecastVsActualPlotsBinsObject$`frequency`
      self$`normalizedError` <- ForecastVsActualPlotsBinsObject$`normalizedError`
      self$`startDate` <- ParseRFC3339Timestamp(ForecastVsActualPlotsBinsObject$`startDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
