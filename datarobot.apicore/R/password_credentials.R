# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PasswordCredentials
#'
#' @description PasswordCredentials Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalogVersionId  character [optional] The ID of the latest version of the catalog entry.
#'
#' @field password  character The password (in cleartext) for database authentication. The password will be encrypted on the server side in scope of HTTP request and never saved or stored.
#'
#' @field url  character [optional] The link to retrieve more detailed information about the entity that uses this catalog dataset.
#'
#' @field user  character The username for database authentication.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PasswordCredentials <- R6::R6Class(
  "PasswordCredentials",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(catalogVersionId = NULL, password = NULL, url = NULL, user = NULL) {
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
    }
  ),
  public = list(
    `catalogVersionId` = NULL,
    `password` = NULL,
    `url` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalogVersionId The ID of the latest version of the catalog entry.
    #' @param password The password (in cleartext) for database authentication. The password will be encrypted on the server side in scope of HTTP request and never saved or stored.
    #' @param url The link to retrieve more detailed information about the entity that uses this catalog dataset.
    #' @param user The username for database authentication.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`password` = NULL, `user` = NULL, `catalogVersionId` = NULL, `url` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`password`, `user`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(catalogVersionId, password, url, user)
      }
      self$`catalogVersionId` <- `catalogVersionId`
      self$`password` <- `password`
      self$`url` <- `url`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(catalogVersionId = self$`catalogVersionId`, password = self$`password`, url = self$`url`, user = self$`user`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PasswordCredentialsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PasswordCredentialsJson, validateParams = FALSE) {
      PasswordCredentialsObject <- jsonlite::fromJSON(PasswordCredentialsJson)
      self$`catalogVersionId` <- PasswordCredentialsObject$`catalogVersionId`
      self$`password` <- PasswordCredentialsObject$`password`
      self$`url` <- PasswordCredentialsObject$`url`
      self$`user` <- PasswordCredentialsObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
