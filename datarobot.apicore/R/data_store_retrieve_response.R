# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataStoreRetrieveResponse
#'
#' @description DataStoreRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field associatedAuthTypes  list( character ) [optional] Supported authentication types for the JDBC configuration.
#'
#' @field canonicalName  character The user-friendly name of the data store.
#'
#' @field creator  character ID of the user who created the data store.
#'
#' @field id  character Data store ID.
#'
#' @field params  \link{OneOfJDBCDataStoreDetailsDRConnectorV1Details} Data store configuration.
#'
#' @field role  character Role of the user making the request on this data store.
#'
#' @field type  character Data store type.
#'
#' @field updated  character ISO 8601-formatted date/time of the data store update.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataStoreRetrieveResponse <- R6::R6Class(
  "DataStoreRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`associatedAuthTypes` = NULL, `canonicalName` = NULL, `creator` = NULL, `id` = NULL, `params` = NULL, `role` = NULL, `type` = NULL, `updated` = NULL) {
      if (!is.null(`canonicalName`)) {
        stopifnot(is.character(`canonicalName`), length(`canonicalName`) == 1)
      }
      if (!is.null(`creator`)) {
        stopifnot(is.character(`creator`), length(`creator`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`params`)) {
        .setComplexProperty(typeList = list(JDBCDataStoreDetails, DRConnectorV1Details), propertyData = params)
      }
      if (!is.null(`role`)) {
        stopifnot(is.character(`role`), length(`role`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`updated`)) {
        stopifnot(is.character(`updated`), length(`updated`) == 1)
      }
      if (!is.null(`associatedAuthTypes`) && length(`associatedAuthTypes`) > 0) {
        stopifnot(is.vector(`associatedAuthTypes`), sapply(`associatedAuthTypes`, is.character))
      }
    }
  ),
  public = list(
    `associatedAuthTypes` = NULL,
    `canonicalName` = NULL,
    `creator` = NULL,
    `id` = NULL,
    `params` = NULL,
    `role` = NULL,
    `type` = NULL,
    `updated` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param associatedAuthTypes Supported authentication types for the JDBC configuration.
    #' @param canonicalName The user-friendly name of the data store.
    #' @param creator ID of the user who created the data store.
    #' @param id Data store ID.
    #' @param params Data store configuration.
    #' @param role Role of the user making the request on this data store.
    #' @param type Data store type.
    #' @param updated ISO 8601-formatted date/time of the data store update.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`canonicalName` = NULL, `creator` = NULL, `id` = NULL, `params` = NULL, `role` = NULL, `type` = NULL, `updated` = NULL, `associatedAuthTypes` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`canonicalName`, `creator`, `id`, `params`, `role`, `type`, `updated`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(associatedAuthTypes, canonicalName, creator, id, params, role, type, updated)
      }
      self$`associatedAuthTypes` <- `associatedAuthTypes`
      self$`canonicalName` <- `canonicalName`
      self$`creator` <- `creator`
      self$`id` <- `id`
      self$`params` <- .setComplexProperty(typeList = list(JDBCDataStoreDetails, DRConnectorV1Details), propertyData = params)
      self$`role` <- `role`
      self$`type` <- `type`
      self$`updated` <- `updated`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(associatedAuthTypes = self$`associatedAuthTypes`, canonicalName = self$`canonicalName`, creator = self$`creator`, id = self$`id`, params = self$`params`, role = self$`role`, type = self$`type`, updated = self$`updated`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`associatedAuthTypes`)) {
          sprintf(
            '"associatedAuthTypes":
            [%s]
                  ',
            paste(unlist(lapply(self$`associatedAuthTypes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`canonicalName`)) {
          sprintf(
            '"canonicalName":
            "%s"
                  ',
            self$`canonicalName`
          )
        },
        if (!is.null(self$`creator`)) {
          sprintf(
            '"creator":
            "%s"
                  ',
            self$`creator`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`params`)) {
          sprintf(
            '"params":
            %s
      ',
            jsonlite::toJSON(self$`params`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`role`)) {
          sprintf(
            '"role":
            "%s"
                  ',
            self$`role`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        },
        if (!is.null(self$`updated`)) {
          sprintf(
            '"updated":
            "%s"
                  ',
            self$`updated`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataStoreRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataStoreRetrieveResponseJson, validateParams = FALSE) {
      DataStoreRetrieveResponseObject <- jsonlite::fromJSON(DataStoreRetrieveResponseJson)
      self$`associatedAuthTypes` <- ApiClient$new()$deserializeObj(DataStoreRetrieveResponseObject$`associatedAuthTypes`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`canonicalName` <- DataStoreRetrieveResponseObject$`canonicalName`
      self$`creator` <- DataStoreRetrieveResponseObject$`creator`
      self$`id` <- DataStoreRetrieveResponseObject$`id`
      self$`params` <- .setComplexProperty(typeList = list(JDBCDataStoreDetails, DRConnectorV1Details), propertyData = DataStoreRetrieveResponseObject$params)
      self$`role` <- DataStoreRetrieveResponseObject$`role`
      self$`type` <- DataStoreRetrieveResponseObject$`type`
      self$`updated` <- DataStoreRetrieveResponseObject$`updated`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
