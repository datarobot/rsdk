# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BaseBlenderBuilderCreate
#'
#' @description BaseBlenderBuilderCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blendMethods  list( character ) The blender method names, either GLM, ENET, AVERAGE, LGBM, TF, RF, PLS, MAE, MAEL1, MAX, or MIN.
#'
#' @field correlationWeight  numeric [optional] The weighting between the correlation score and accuracy score
#'
#' @field heuristic  character The blender builder heuristic name, either VALIDATION or CORRELATION.
#'
#' @field maxModels  integer The number of models to blend
#'
#' @field models  list( character ) [optional] The leaderboard IDs for the models to be considered for blending.
#'
#' @field topN  integer [optional] Use the Top N models in the leaderboard instead of a list of models.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BaseBlenderBuilderCreate <- R6::R6Class(
  "BaseBlenderBuilderCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(blendMethods = NULL, correlationWeight = NULL, heuristic = NULL, maxModels = NULL, models = NULL, topN = NULL) {
      if (!is.null(`blendMethods`)) {
        stopifnot(is.vector(`blendMethods`))
      }
      if (!is.null(`heuristic`)) {
        stopifnot(is.character(`heuristic`), length(`heuristic`) == 1)
      }
      if (!is.null(`maxModels`)) {
        stopifnot(is.numeric(`maxModels`), length(`maxModels`) == 1)
      }
      if (!is.null(`correlationWeight`)) {
        stopifnot(is.numeric(`correlationWeight`), length(`correlationWeight`) == 1)
      }
      if (!is.null(`models`)) {
        stopifnot(is.vector(`models`))
      }
      if (!is.null(`topN`)) {
        stopifnot(is.numeric(`topN`), length(`topN`) == 1)
      }
    }
  ),
  public = list(
    `blendMethods` = NULL,
    `correlationWeight` = NULL,
    `heuristic` = NULL,
    `maxModels` = NULL,
    `models` = NULL,
    `topN` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blendMethods The blender method names, either GLM, ENET, AVERAGE, LGBM, TF, RF, PLS, MAE, MAEL1, MAX, or MIN.
    #' @param correlationWeight The weighting between the correlation score and accuracy score
    #' @param heuristic The blender builder heuristic name, either VALIDATION or CORRELATION.
    #' @param maxModels The number of models to blend
    #' @param models The leaderboard IDs for the models to be considered for blending.
    #' @param topN Use the Top N models in the leaderboard instead of a list of models.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blendMethods` = NULL, `heuristic` = NULL, `maxModels` = NULL, `correlationWeight` = NULL, `models` = NULL, `topN` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`blendMethods`, `heuristic`, `maxModels`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(blendMethods, correlationWeight, heuristic, maxModels, models, topN)
      }
      sapply(`blendMethods`, function(x) stopifnot(is.character(x)))
      self$`correlationWeight` <- `correlationWeight`
      self$`heuristic` <- `heuristic`
      self$`maxModels` <- `maxModels`
      sapply(`models`, function(x) stopifnot(is.character(x)))
      self$`topN` <- `topN`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(blendMethods = self$`blendMethods`, correlationWeight = self$`correlationWeight`, heuristic = self$`heuristic`, maxModels = self$`maxModels`, models = self$`models`, topN = self$`topN`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blendMethods`)) {
          sprintf(
            '"blendMethods":
              [%s]
            ',
            paste(unlist(lapply(self$`blendMethods`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`correlationWeight`)) {
          sprintf(
            '"correlationWeight":
            %d
                  ',
            self$`correlationWeight`
          )
        },
        if (!is.null(self$`heuristic`)) {
          sprintf(
            '"heuristic":
            "%s"
                  ',
            self$`heuristic`
          )
        },
        if (!is.null(self$`maxModels`)) {
          sprintf(
            '"maxModels":
            %d
                  ',
            self$`maxModels`
          )
        },
        if (!is.null(self$`models`)) {
          sprintf(
            '"models":
              [%s]
            ',
            paste(unlist(lapply(self$`models`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`topN`)) {
          sprintf(
            '"topN":
            %d
                  ',
            self$`topN`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BaseBlenderBuilderCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BaseBlenderBuilderCreateJson, validateParams = FALSE) {
      BaseBlenderBuilderCreateObject <- jsonlite::fromJSON(BaseBlenderBuilderCreateJson)
      self$`blendMethods` <- ApiClient$new()$deserializeObj(BaseBlenderBuilderCreateObject$`blendMethods`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`correlationWeight` <- BaseBlenderBuilderCreateObject$`correlationWeight`
      self$`heuristic` <- BaseBlenderBuilderCreateObject$`heuristic`
      self$`maxModels` <- BaseBlenderBuilderCreateObject$`maxModels`
      self$`models` <- ApiClient$new()$deserializeObj(BaseBlenderBuilderCreateObject$`models`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`topN` <- BaseBlenderBuilderCreateObject$`topN`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
