# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BacktestOldMethodForOpenApi
#'
#' @description BacktestOldMethodForOpenApi Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field gapDuration  character A duration string representing the duration of the gap between the training and the validation data for this backtest.
#'
#' @field index  integer The index from zero of the backtest.
#'
#' @field validationDuration  character A duration string representing the duration of the validation data for this backtest.
#'
#' @field validationStartDate  character A datetime string representing the start date of the validation data for this backtest.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BacktestOldMethodForOpenApi <- R6::R6Class(
  "BacktestOldMethodForOpenApi",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(gapDuration = NULL, index = NULL, validationDuration = NULL, validationStartDate = NULL) {
      if (!is.null(`gapDuration`)) {
        stopifnot(is.character(`gapDuration`), length(`gapDuration`) == 1)
      }
      if (!is.null(`index`)) {
        stopifnot(is.numeric(`index`), length(`index`) == 1)
      }
      if (!is.null(`validationDuration`)) {
        stopifnot(is.character(`validationDuration`), length(`validationDuration`) == 1)
      }
      if (!is.null(`validationStartDate`)) {
        stopifnot(inherits(`validationStartDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `gapDuration` = NULL,
    `index` = NULL,
    `validationDuration` = NULL,
    `validationStartDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param gapDuration A duration string representing the duration of the gap between the training and the validation data for this backtest.
    #' @param index The index from zero of the backtest.
    #' @param validationDuration A duration string representing the duration of the validation data for this backtest.
    #' @param validationStartDate A datetime string representing the start date of the validation data for this backtest.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`gapDuration` = NULL, `index` = NULL, `validationDuration` = NULL, `validationStartDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`gapDuration`, `index`, `validationDuration`, `validationStartDate`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(gapDuration, index, validationDuration, validationStartDate)
      }
      self$`gapDuration` <- `gapDuration`
      self$`index` <- `index`
      self$`validationDuration` <- `validationDuration`
      self$`validationStartDate` <- `validationStartDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(gapDuration = self$`gapDuration`, index = self$`index`, validationDuration = self$`validationDuration`, validationStartDate = self$`validationStartDate`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`gapDuration`)) {
          sprintf(
            '"gapDuration":
            "%s"
                  ',
            self$`gapDuration`
          )
        },
        if (!is.null(self$`index`)) {
          sprintf(
            '"index":
            %d
                  ',
            self$`index`
          )
        },
        if (!is.null(self$`validationDuration`)) {
          sprintf(
            '"validationDuration":
            "%s"
                  ',
            self$`validationDuration`
          )
        },
        if (!is.null(self$`validationStartDate`)) {
          sprintf(
            '"validationStartDate":
            "%s"
                  ',
            format(self$`validationStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BacktestOldMethodForOpenApiJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BacktestOldMethodForOpenApiJson, validateParams = FALSE) {
      BacktestOldMethodForOpenApiObject <- jsonlite::fromJSON(BacktestOldMethodForOpenApiJson)
      self$`gapDuration` <- BacktestOldMethodForOpenApiObject$`gapDuration`
      self$`index` <- BacktestOldMethodForOpenApiObject$`index`
      self$`validationDuration` <- BacktestOldMethodForOpenApiObject$`validationDuration`
      self$`validationStartDate` <- ParseRFC3339Timestamp(BacktestOldMethodForOpenApiObject$`validationStartDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
