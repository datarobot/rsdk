# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Governance operations
#' @description datarobot.apicore.Governance
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#' @export
GovernanceApi <- R6::R6Class(
  "GovernanceApi",
  public = list(
    apiClient = NULL,

    #' @param apiClient A configurable `ApiClient` instance. If none provided, a new client with default configuration will be created.
    initialize = function(apiClient) {
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      } else {
        self$apiClient <- ApiClient$new()
      }
    },
    #' @description Create a new Approval Policy.
    #' Produces: "application/json"
    #'
    #' @details Create a new Approval Policy.
    #' @details This method invokes `POST /approvalPolicies/` in the DataRobot Public API.
    #' @param approvalPolicy \link{ApprovalPolicy}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ApprovalPolicyResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`201`** Approval Policy has been successfully created.
    #' \itemize{
    #' }
    #' \item **`403`** Approval Policy management feature is disabled for the user.
    #' \itemize{
    #' }
    #' \item **`422`** Approval Policy could not be created with the given input.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' approvalPolicy <- ApprovalPolicy$new() # ApprovalPolicy |
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ApprovalPoliciesCreate(approvalPolicy=approvalPolicy)
    #' }
    ApprovalPoliciesCreate = function(approvalPolicy = NULL, ...) {
      apiResponse <- private$ApprovalPoliciesCreateWithHttpInfo(approvalPolicy, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Delete an Approval Policy.
    #' Produces: NA
    #'
    #' @details Delete the policy with the given ID.
    #' @details This method invokes `DELETE /approvalPolicies/{approvalPolicyId}/` in the DataRobot Public API.
    #' @param approvalPolicyId character. ID of the Approval Policy.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`** Approval Policy has been successfully deleted.
    #' \itemize{
    #' }
    #' \item **`403`** Approval Policy management feature is disabled for the user.
    #' \itemize{
    #' }
    #' \item **`404`** Approval Policy does not exist or the user doesn&#39;t have access to it.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' approvalPolicyId <- 'approvalPolicyId_example' # character | ID of the Approval Policy.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ApprovalPoliciesDelete(approvalPolicyId)
    #' }
    ApprovalPoliciesDelete = function(approvalPolicyId, ...) {
      apiResponse <- private$ApprovalPoliciesDeleteWithHttpInfo(approvalPolicyId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List Approval Policies.
    #' Produces: "application/json"
    #'
    #' @details List Approval Policies.
    #' @details This method invokes `GET /approvalPolicies/` in the DataRobot Public API.
    #' @param offset integer. Number of results to skip.
    #' @param limit integer. At most this many results are returned. The default may change without notice.
    #' @param entityType Enum < [deployment, Deployment, DEPLOYMENT, deploymentModel, DeploymentModel, DEPLOYMENT_MODEL, deploymentConfig, DeploymentConfig, DEPLOYMENT_CONFIG, deploymentStatus, DeploymentStatus, DEPLOYMENT_STATUS, deploymentMonitoringData, DeploymentMonitoringData, DEPLOYMENT_MONITORING_DATA] > Type of entity to filter policies by.
    #' @param namePart character. Part of the policy name to search by.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ApprovalPolicyListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Approval Policy has been successfully updated.
    #' \itemize{
    #' }
    #' \item **`403`** Approval Policy management feature is disabled for the user.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' offset <- 0 # integer | Number of results to skip.
    #' limit <- 100 # integer | At most this many results are returned. The default may change without notice.
    #' entityType <- 'entityType_example' # character | Type of entity to filter policies by.
    #' namePart <- 'namePart_example' # character | Part of the policy name to search by.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ApprovalPoliciesList(offset=offset, limit=limit, entityType=entityType, namePart=namePart)
    #' }
    ApprovalPoliciesList = function(offset = 0, limit = 100, entityType = NULL, namePart = NULL, ...) {
      apiResponse <- private$ApprovalPoliciesListWithHttpInfo(offset, limit, entityType, namePart, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Update an Approval Policy.
    #' Produces: "application/json"
    #'
    #' @details Update the policy with the given ID.
    #' @details This method invokes `PUT /approvalPolicies/{approvalPolicyId}/` in the DataRobot Public API.
    #' @param approvalPolicyId character. ID of the Approval Policy.
    #' @param approvalPolicy \link{ApprovalPolicy}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ApprovalPolicyResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Approval Policy has been successfully updated.
    #' \itemize{
    #' }
    #' \item **`403`** Approval Policy management feature is disabled for the user.
    #' \itemize{
    #' }
    #' \item **`404`** Approval Policy does not exist or the user doesn&#39;t have access to it.
    #' \itemize{
    #' }
    #' \item **`422`** Approval Policy could not be created with the given input.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' approvalPolicyId <- 'approvalPolicyId_example' # character | ID of the Approval Policy.
    #' approvalPolicy <- ApprovalPolicy$new() # ApprovalPolicy |
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ApprovalPoliciesPut(approvalPolicyId, approvalPolicy=approvalPolicy)
    #' }
    ApprovalPoliciesPut = function(approvalPolicyId, approvalPolicy = NULL, ...) {
      apiResponse <- private$ApprovalPoliciesPutWithHttpInfo(approvalPolicyId, approvalPolicy, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve an Approval Policy.
    #' Produces: "application/json"
    #'
    #' @details Retrieve the policy with the given ID.
    #' @details This method invokes `GET /approvalPolicies/{approvalPolicyId}/` in the DataRobot Public API.
    #' @param approvalPolicyId character. ID of the Approval Policy.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ApprovalPolicyResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Approval Policy has been successfully retrieved.
    #' \itemize{
    #' }
    #' \item **`403`** Approval Policy management feature is disabled for the user.
    #' \itemize{
    #' }
    #' \item **`404`** Approval Policy does not exist or the user doesn&#39;t have access to it.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' approvalPolicyId <- 'approvalPolicyId_example' # character | ID of the Approval Policy.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ApprovalPoliciesRetrieve(approvalPolicyId)
    #' }
    ApprovalPoliciesRetrieve = function(approvalPolicyId, ...) {
      apiResponse <- private$ApprovalPoliciesRetrieveWithHttpInfo(approvalPolicyId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve associated Change Requests Info.
    #' Produces: "application/json"
    #'
    #' @details Get information about Change Requests submitted for a certain Approval Policy.
    #' @details This method invokes `GET /approvalPolicies/{approvalPolicyId}/shareableChangeRequests/` in the DataRobot Public API.
    #' @param approvalPolicyId character. ID of the Approval Policy.
    #' @param offset integer. Number of results to skip.
    #' @param limit integer. At most this many results are returned. The default may change without notice.
    #' @param orderBy character. Attribute to order Change Requests by.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ChangeRequestInfoListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Change Requests information has been successfully retrieved.
    #' \itemize{
    #' }
    #' \item **`403`** Approval Policy management feature is disabled for the user.
    #' \itemize{
    #' }
    #' \item **`404`** Approval Policy does not exist or the user doesn&#39;t have access to it.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' approvalPolicyId <- 'approvalPolicyId_example' # character | ID of the Approval Policy.
    #' offset <- 0 # integer | Number of results to skip.
    #' limit <- 100 # integer | At most this many results are returned. The default may change without notice.
    #' orderBy <- "" # character | Attribute to order Change Requests by.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ApprovalPoliciesShareableChangeRequestsList(approvalPolicyId, offset=offset, limit=limit, orderBy=orderBy)
    #' }
    ApprovalPoliciesShareableChangeRequestsList = function(approvalPolicyId, offset = 0, limit = 100, orderBy = "", ...) {
      apiResponse <- private$ApprovalPoliciesShareableChangeRequestsListWithHttpInfo(approvalPolicyId, offset, limit, orderBy, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Get policy ID matching the query
    #' Produces: "application/json"
    #'
    #' @details Get policy ID matching the query
    #' @details This method invokes `GET /approvalPolicyMatch/` in the DataRobot Public API.
    #' @param entityType Enum < [deployment, Deployment, DEPLOYMENT, deploymentModel, DeploymentModel, DEPLOYMENT_MODEL, deploymentConfig, DeploymentConfig, DEPLOYMENT_CONFIG, deploymentStatus, DeploymentStatus, DEPLOYMENT_STATUS, deploymentMonitoringData, DeploymentMonitoringData, DEPLOYMENT_MONITORING_DATA] > Searched typed of the entity.
    #' @param action Enum < [create, Create, CREATE, update, Update, UPDATE, delete, Delete, DELETE] > Searched policy action.
    #' @param fieldName character. Name of the entity field to filter policies by.
    #' @param fieldValue character. Value of the entity field to filter policies by.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ApprovalPolicyMatchResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Search executed successfully.
    #' \itemize{
    #' }
    #' \item **`403`** Approval Policy management feature is disabled for the user.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' entityType <- 'entityType_example' # character | Searched typed of the entity.
    #' action <- 'action_example' # character | Searched policy action.
    #' fieldName <- 'fieldName_example' # character | Name of the entity field to filter policies by.
    #' fieldValue <- 'fieldValue_example' # character | Value of the entity field to filter policies by.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ApprovalPolicyMatchList(entityType, action, fieldName=fieldName, fieldValue=fieldValue)
    #' }
    ApprovalPolicyMatchList = function(entityType, action, fieldName = NULL, fieldValue = NULL, ...) {
      apiResponse <- private$ApprovalPolicyMatchListWithHttpInfo(entityType, action, fieldName, fieldValue, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Get a list of available policy triggers.
    #' Produces: "application/json"
    #'
    #' @details Get a list of available policy triggers.
    #' @details This method invokes `GET /approvalPolicyTriggers/` in the DataRobot Public API.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ApprovalWorkflowListTriggerResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** The list of triggers is successfully generated.
    #' \itemize{
    #' }
    #' \item **`403`** Approval Policy management feature is disabled for the user.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ApprovalPolicyTriggersList()
    #' }
    ApprovalPolicyTriggersList = function(...) {
      apiResponse <- private$ApprovalPolicyTriggersListWithHttpInfo(...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Create Change Request.
    #' Produces: "application/json"
    #'
    #' @details Request changes for a supported product entity. For now, you can request changes for deployments only.
    #' @details This method invokes `POST /changeRequests/` in the DataRobot Public API.
    #' @param changeRequestCreate \link{ChangeRequestCreate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ChangeRequestResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`201`** Change Request Created.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' changeRequestCreate <- ChangeRequestCreate$new() # ChangeRequestCreate |
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsCreate(changeRequestCreate=changeRequestCreate)
    #' }
    ChangeRequestsCreate = function(changeRequestCreate = NULL, ...) {
      apiResponse <- private$ChangeRequestsCreateWithHttpInfo(changeRequestCreate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List Change Requests.
    #' Produces: "application/json"
    #'
    #' @details List all Change Requests accessible by the user for the given product entity type.
    #' @details This method invokes `GET /changeRequests/` in the DataRobot Public API.
    #' @param entityType Enum < [deployment, Deployment, DEPLOYMENT] > Type of the entity to filter requests by.
    #' @param offset integer. Number of results to skip.
    #' @param limit integer. At most this many results are returned. The default may change without notice.
    #' @param entityId \link{OneOfstringarray}. ID of the entity to filter change requests by.
    #' @param myRequests Enum < [false, False, true, True] > Filter change requests by the owner. If &#x60;true&#x60;, only returns change requests owned by the user. If &#x60;false&#x60;, only returns change requests owned by other users but accessible to the requester.
    #' @param showApproved Enum < [false, False, true, True] > Filter change requests by status. If &#x60;true&#x60;, only returns approved change requests. If &#x60;false&#x60;, only returns not approved change requests.
    #' @param showCancelled Enum < [false, False, true, True] > Filter change requests by status. If &#x60;true&#x60;, only returns cancelled change requests. If &#x60;false&#x60;, only returns not cancelled change requests.
    #' @param status \link{OneOfstringarray}. Filter change requests by status.
    #' @param orderBy Enum < [createdAt, -createdAt, processedAt, -processedAt, updatedAt, -updatedAt] > The order that the results should be retrieved in.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ChangeRequestsListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`201`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' entityType <- 'entityType_example' # character | Type of the entity to filter requests by.
    #' offset <- 0 # integer | Number of results to skip.
    #' limit <- 100 # integer | At most this many results are returned. The default may change without notice.
    #' entityId <- list(c('entityId_example')) # OneOfstringarray | ID of the entity to filter change requests by.
    #' myRequests <- 'myRequests_example' # character | Filter change requests by the owner. If `true`, only returns change requests owned by the user. If `false`, only returns change requests owned by other users but accessible to the requester.
    #' showApproved <- 'showApproved_example' # character | Filter change requests by status. If `true`, only returns approved change requests. If `false`, only returns not approved change requests.
    #' showCancelled <- 'showCancelled_example' # character | Filter change requests by status. If `true`, only returns cancelled change requests. If `false`, only returns not cancelled change requests.
    #' status <- list(c('status_example')) # OneOfstringarray | Filter change requests by status.
    #' orderBy <- 'orderBy_example' # character | The order that the results should be retrieved in.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsList(entityType, offset=offset, limit=limit, entityId=entityId, myRequests=myRequests, showApproved=showApproved, showCancelled=showCancelled, status=status, orderBy=orderBy)
    #' }
    ChangeRequestsList = function(entityType, offset = 0, limit = 100, entityId = NULL, myRequests = NULL, showApproved = NULL, showCancelled = NULL, status = NULL, orderBy = NULL, ...) {
      apiResponse <- private$ChangeRequestsListWithHttpInfo(entityType, offset, limit, entityId, myRequests, showApproved, showCancelled, status, orderBy, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Update Change Request.
    #' Produces: "application/json"
    #'
    #' @details Update Change Request with the given ID.
    #' @details This method invokes `PATCH /changeRequests/{changeRequestId}/` in the DataRobot Public API.
    #' @param changeRequestId character. ID of the Change Request.
    #' @param changeRequestUpdate \link{ChangeRequestUpdate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ChangeRequestResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' changeRequestId <- 'changeRequestId_example' # character | ID of the Change Request.
    #' changeRequestUpdate <- ChangeRequestUpdate$new() # ChangeRequestUpdate |
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsPatch(changeRequestId, changeRequestUpdate=changeRequestUpdate)
    #' }
    ChangeRequestsPatch = function(changeRequestId, changeRequestUpdate = NULL, ...) {
      apiResponse <- private$ChangeRequestsPatchWithHttpInfo(changeRequestId, changeRequestUpdate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve Change Request.
    #' Produces: "application/json"
    #'
    #' @details Retrieve Change Request by ID.
    #' @details This method invokes `GET /changeRequests/{changeRequestId}/` in the DataRobot Public API.
    #' @param changeRequestId character. ID of the Change Request.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ChangeRequestResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' changeRequestId <- 'changeRequestId_example' # character | ID of the Change Request.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsRetrieve(changeRequestId)
    #' }
    ChangeRequestsRetrieve = function(changeRequestId, ...) {
      apiResponse <- private$ChangeRequestsRetrieveWithHttpInfo(changeRequestId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Create review.
    #' Produces: "application/json"
    #'
    #' @details Review the Change Request.
    #' @details This method invokes `POST /changeRequests/{changeRequestId}/reviews/` in the DataRobot Public API.
    #' @param changeRequestId character. ID of the Change Request.
    #' @param reviewCreate \link{ReviewCreate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ReviewResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`201`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' changeRequestId <- 'changeRequestId_example' # character | ID of the Change Request.
    #' reviewCreate <- ReviewCreate$new() # ReviewCreate |
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsReviewsCreate(changeRequestId, reviewCreate=reviewCreate)
    #' }
    ChangeRequestsReviewsCreate = function(changeRequestId, reviewCreate = NULL, ...) {
      apiResponse <- private$ChangeRequestsReviewsCreateWithHttpInfo(changeRequestId, reviewCreate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List Change Request reviews.
    #' Produces: "application/json"
    #'
    #' @details List Change Request reviews.
    #' @details This method invokes `GET /changeRequests/{changeRequestId}/reviews/` in the DataRobot Public API.
    #' @param changeRequestId character. ID of the Change Request.
    #' @param offset integer. Number of results to skip.
    #' @param limit integer. At most this many results are returned. The default may change without notice.
    #' @param status Enum < [approved, Approved, APPROVED, changesRequested, ChangesRequested, CHANGES_REQUESTED, commented, Commented, COMMENTED] > Review status to filter by.
    #' @param changeVersionId character. ID of the change version to filter by
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ReviewsListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' changeRequestId <- 'changeRequestId_example' # character | ID of the Change Request.
    #' offset <- 0 # integer | Number of results to skip.
    #' limit <- 100 # integer | At most this many results are returned. The default may change without notice.
    #' status <- 'status_example' # character | Review status to filter by.
    #' changeVersionId <- 'changeVersionId_example' # character | ID of the change version to filter by
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsReviewsList(changeRequestId, offset=offset, limit=limit, status=status, changeVersionId=changeVersionId)
    #' }
    ChangeRequestsReviewsList = function(changeRequestId, offset = 0, limit = 100, status = NULL, changeVersionId = NULL, ...) {
      apiResponse <- private$ChangeRequestsReviewsListWithHttpInfo(changeRequestId, offset, limit, status, changeVersionId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve review.
    #' Produces: "application/json"
    #'
    #' @details Retrieve a review by ID.
    #' @details This method invokes `GET /changeRequests/{changeRequestId}/reviews/{reviewId}/` in the DataRobot Public API.
    #' @param changeRequestId character. ID of the Change Request.
    #' @param reviewId character. ID of the review.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ReviewResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' changeRequestId <- 'changeRequestId_example' # character | ID of the Change Request.
    #' reviewId <- 'reviewId_example' # character | ID of the review.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsReviewsRetrieve(changeRequestId, reviewId)
    #' }
    ChangeRequestsReviewsRetrieve = function(changeRequestId, reviewId, ...) {
      apiResponse <- private$ChangeRequestsReviewsRetrieveWithHttpInfo(changeRequestId, reviewId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Resolve or Cancel the Change Request.
    #' Produces: "application/json"
    #'
    #' @details Resolve or Cancel the Change Request.
    #' @details This method invokes `PATCH /changeRequests/{changeRequestId}/status/` in the DataRobot Public API.
    #' @param changeRequestId character. ID of the Change Request.
    #' @param changeRequestUpdateStatus \link{ChangeRequestUpdateStatus}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ChangeRequestUpdateStatus}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' \item **`Location`** A url that can be polled to check the status.
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' changeRequestId <- 'changeRequestId_example' # character | ID of the Change Request.
    #' changeRequestUpdateStatus <- ChangeRequestUpdateStatus$new() # ChangeRequestUpdateStatus |
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsStatusPatchMany(changeRequestId, changeRequestUpdateStatus=changeRequestUpdateStatus)
    #' }
    ChangeRequestsStatusPatchMany = function(changeRequestId, changeRequestUpdateStatus = NULL, ...) {
      apiResponse <- private$ChangeRequestsStatusPatchManyWithHttpInfo(changeRequestId, changeRequestUpdateStatus, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List suggested reviewers.
    #' Produces: "application/json"
    #'
    #' @details List users, suggested to review the Change Request.
    #' @details This method invokes `GET /changeRequests/{changeRequestId}/suggestedReviewers/` in the DataRobot Public API.
    #' @param changeRequestId character. ID of the Change Request.
    #' @param offset integer. Number of results to skip.
    #' @param limit integer. At most this many results are returned. The default may change without notice.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{SuggestedReviewersResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' changeRequestId <- 'changeRequestId_example' # character | ID of the Change Request.
    #' offset <- 0 # integer | Number of results to skip.
    #' limit <- 100 # integer | At most this many results are returned. The default may change without notice.
    #'
    #' api.instance <- GovernanceApi$new()
    #' result <- api.instance$ChangeRequestsSuggestedReviewersList(changeRequestId, offset=offset, limit=limit)
    #' }
    ChangeRequestsSuggestedReviewersList = function(changeRequestId, offset = 0, limit = 100, ...) {
      apiResponse <- private$ChangeRequestsSuggestedReviewersListWithHttpInfo(changeRequestId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    }
  ),
  private = list(
    # A helper function to invoke the API operation `ApprovalPoliciesCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ApprovalPoliciesCreateWithHttpInfo = function(approvalPolicy = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (!missing(`approvalPolicy`) && isa(approvalPolicy, c("ApprovalPolicy", "R6"))) {
        body <- `approvalPolicy`$toJSON()
      } else {
        stop("ApprovalPoliciesCreateWithHttpInfo requires parameter approvalPolicy to be of type ApprovalPolicy.")
      }

      urlPath <- "/approvalPolicies/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ApprovalPolicyResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ApprovalPoliciesDelete`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ApprovalPoliciesDeleteWithHttpInfo = function(approvalPolicyId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`approvalPolicyId`)) {
        stop("Missing required parameter `approvalPolicyId`.")
      }

      body <- NULL
      urlPath <- "/approvalPolicies/{approvalPolicyId}/"
      if (!missing(`approvalPolicyId`)) {
        urlPath <- gsub(paste0("\\{", "approvalPolicyId", "\\}"), URLencode(as.character(`approvalPolicyId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "DELETE",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ApprovalPoliciesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ApprovalPoliciesListWithHttpInfo = function(offset = 0, limit = 100, entityType = NULL, namePart = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["entityType"] <- entityType

      queryParams["namePart"] <- namePart

      body <- NULL
      urlPath <- "/approvalPolicies/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ApprovalPolicyListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ApprovalPoliciesPut`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ApprovalPoliciesPutWithHttpInfo = function(approvalPolicyId, approvalPolicy = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`approvalPolicyId`)) {
        stop("Missing required parameter `approvalPolicyId`.")
      }

      if (!missing(`approvalPolicy`) && isa(approvalPolicy, c("ApprovalPolicy", "R6"))) {
        body <- `approvalPolicy`$toJSON()
      } else {
        stop("ApprovalPoliciesPutWithHttpInfo requires parameter approvalPolicy to be of type ApprovalPolicy.")
      }

      urlPath <- "/approvalPolicies/{approvalPolicyId}/"
      if (!missing(`approvalPolicyId`)) {
        urlPath <- gsub(paste0("\\{", "approvalPolicyId", "\\}"), URLencode(as.character(`approvalPolicyId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PUT",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ApprovalPolicyResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ApprovalPoliciesRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ApprovalPoliciesRetrieveWithHttpInfo = function(approvalPolicyId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`approvalPolicyId`)) {
        stop("Missing required parameter `approvalPolicyId`.")
      }

      body <- NULL
      urlPath <- "/approvalPolicies/{approvalPolicyId}/"
      if (!missing(`approvalPolicyId`)) {
        urlPath <- gsub(paste0("\\{", "approvalPolicyId", "\\}"), URLencode(as.character(`approvalPolicyId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ApprovalPolicyResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ApprovalPoliciesShareableChangeRequestsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ApprovalPoliciesShareableChangeRequestsListWithHttpInfo = function(approvalPolicyId, offset = 0, limit = 100, orderBy = "", ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`approvalPolicyId`)) {
        stop("Missing required parameter `approvalPolicyId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["orderBy"] <- orderBy

      body <- NULL
      urlPath <- "/approvalPolicies/{approvalPolicyId}/shareableChangeRequests/"
      if (!missing(`approvalPolicyId`)) {
        urlPath <- gsub(paste0("\\{", "approvalPolicyId", "\\}"), URLencode(as.character(`approvalPolicyId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ChangeRequestInfoListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ApprovalPolicyMatchList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ApprovalPolicyMatchListWithHttpInfo = function(entityType, action, fieldName = NULL, fieldValue = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entityType`)) {
        stop("Missing required parameter `entityType`.")
      }

      if (missing(`action`)) {
        stop("Missing required parameter `action`.")
      }

      queryParams["entityType"] <- entityType

      queryParams["action"] <- action

      queryParams["fieldName"] <- fieldName

      queryParams["fieldValue"] <- fieldValue

      body <- NULL
      urlPath <- "/approvalPolicyMatch/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ApprovalPolicyMatchResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ApprovalPolicyTriggersList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ApprovalPolicyTriggersListWithHttpInfo = function(...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      body <- NULL
      urlPath <- "/approvalPolicyTriggers/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ApprovalWorkflowListTriggerResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsCreateWithHttpInfo = function(changeRequestCreate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (!missing(`changeRequestCreate`) && isa(changeRequestCreate, c("ChangeRequestCreate", "R6"))) {
        body <- `changeRequestCreate`$toJSON()
      } else {
        stop("ChangeRequestsCreateWithHttpInfo requires parameter changeRequestCreate to be of type ChangeRequestCreate.")
      }

      urlPath <- "/changeRequests/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ChangeRequestResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsListWithHttpInfo = function(entityType, offset = 0, limit = 100, entityId = NULL, myRequests = NULL, showApproved = NULL, showCancelled = NULL, status = NULL, orderBy = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entityType`)) {
        stop("Missing required parameter `entityType`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["entityType"] <- entityType

      if (!is.null(entityId)) {
        queryParams["entityId"] <- paste0(entityId, collapse = ",")
      }

      queryParams["myRequests"] <- myRequests

      queryParams["showApproved"] <- showApproved

      queryParams["showCancelled"] <- showCancelled

      if (!is.null(status)) {
        queryParams["status"] <- paste0(status, collapse = ",")
      }

      queryParams["orderBy"] <- orderBy

      body <- NULL
      urlPath <- "/changeRequests/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ChangeRequestsListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsPatch`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsPatchWithHttpInfo = function(changeRequestId, changeRequestUpdate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`changeRequestId`)) {
        stop("Missing required parameter `changeRequestId`.")
      }

      if (!missing(`changeRequestUpdate`) && isa(changeRequestUpdate, c("ChangeRequestUpdate", "R6"))) {
        body <- `changeRequestUpdate`$toJSON()
      } else {
        stop("ChangeRequestsPatchWithHttpInfo requires parameter changeRequestUpdate to be of type ChangeRequestUpdate.")
      }

      urlPath <- "/changeRequests/{changeRequestId}/"
      if (!missing(`changeRequestId`)) {
        urlPath <- gsub(paste0("\\{", "changeRequestId", "\\}"), URLencode(as.character(`changeRequestId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ChangeRequestResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsRetrieveWithHttpInfo = function(changeRequestId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`changeRequestId`)) {
        stop("Missing required parameter `changeRequestId`.")
      }

      body <- NULL
      urlPath <- "/changeRequests/{changeRequestId}/"
      if (!missing(`changeRequestId`)) {
        urlPath <- gsub(paste0("\\{", "changeRequestId", "\\}"), URLencode(as.character(`changeRequestId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ChangeRequestResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsReviewsCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsReviewsCreateWithHttpInfo = function(changeRequestId, reviewCreate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`changeRequestId`)) {
        stop("Missing required parameter `changeRequestId`.")
      }

      if (!missing(`reviewCreate`) && isa(reviewCreate, c("ReviewCreate", "R6"))) {
        body <- `reviewCreate`$toJSON()
      } else {
        stop("ChangeRequestsReviewsCreateWithHttpInfo requires parameter reviewCreate to be of type ReviewCreate.")
      }

      urlPath <- "/changeRequests/{changeRequestId}/reviews/"
      if (!missing(`changeRequestId`)) {
        urlPath <- gsub(paste0("\\{", "changeRequestId", "\\}"), URLencode(as.character(`changeRequestId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ReviewResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsReviewsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsReviewsListWithHttpInfo = function(changeRequestId, offset = 0, limit = 100, status = NULL, changeVersionId = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`changeRequestId`)) {
        stop("Missing required parameter `changeRequestId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["status"] <- status

      queryParams["changeVersionId"] <- changeVersionId

      body <- NULL
      urlPath <- "/changeRequests/{changeRequestId}/reviews/"
      if (!missing(`changeRequestId`)) {
        urlPath <- gsub(paste0("\\{", "changeRequestId", "\\}"), URLencode(as.character(`changeRequestId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ReviewsListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsReviewsRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsReviewsRetrieveWithHttpInfo = function(changeRequestId, reviewId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`changeRequestId`)) {
        stop("Missing required parameter `changeRequestId`.")
      }

      if (missing(`reviewId`)) {
        stop("Missing required parameter `reviewId`.")
      }

      body <- NULL
      urlPath <- "/changeRequests/{changeRequestId}/reviews/{reviewId}/"
      if (!missing(`changeRequestId`)) {
        urlPath <- gsub(paste0("\\{", "changeRequestId", "\\}"), URLencode(as.character(`changeRequestId`), reserved = TRUE), urlPath)
      }

      if (!missing(`reviewId`)) {
        urlPath <- gsub(paste0("\\{", "reviewId", "\\}"), URLencode(as.character(`reviewId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ReviewResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsStatusPatchMany`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsStatusPatchManyWithHttpInfo = function(changeRequestId, changeRequestUpdateStatus = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`changeRequestId`)) {
        stop("Missing required parameter `changeRequestId`.")
      }

      if (!missing(`changeRequestUpdateStatus`) && isa(changeRequestUpdateStatus, c("ChangeRequestUpdateStatus", "R6"))) {
        body <- `changeRequestUpdateStatus`$toJSON()
      } else {
        stop("ChangeRequestsStatusPatchManyWithHttpInfo requires parameter changeRequestUpdateStatus to be of type ChangeRequestUpdateStatus.")
      }

      urlPath <- "/changeRequests/{changeRequestId}/status/"
      if (!missing(`changeRequestId`)) {
        urlPath <- gsub(paste0("\\{", "changeRequestId", "\\}"), URLencode(as.character(`changeRequestId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ChangeRequestUpdateStatus", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ChangeRequestsSuggestedReviewersList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ChangeRequestsSuggestedReviewersListWithHttpInfo = function(changeRequestId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`changeRequestId`)) {
        stop("Missing required parameter `changeRequestId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/changeRequests/{changeRequestId}/suggestedReviewers/"
      if (!missing(`changeRequestId`)) {
        urlPath <- gsub(paste0("\\{", "changeRequestId", "\\}"), URLencode(as.character(`changeRequestId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "SuggestedReviewersResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    }
  )
)
