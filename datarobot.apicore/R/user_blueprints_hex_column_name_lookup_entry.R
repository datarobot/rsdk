# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintsHexColumnNameLookupEntry
#'
#' @description UserBlueprintsHexColumnNameLookupEntry Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field colname  character The name of the column.
#'
#' @field hex  character A safe hex representation of the column name.
#'
#' @field projectId  character [optional] The id of the project from which the column name originates.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintsHexColumnNameLookupEntry <- R6::R6Class(
  "UserBlueprintsHexColumnNameLookupEntry",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`colname` = NULL, `hex` = NULL, `projectId` = NULL) {
      if (!is.null(`colname`)) {
        stopifnot(is.character(`colname`), length(`colname`) == 1)
      }
      if (!is.null(`hex`)) {
        stopifnot(is.character(`hex`), length(`hex`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `colname` = NULL,
    `hex` = NULL,
    `projectId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param colname The name of the column.
    #' @param hex A safe hex representation of the column name.
    #' @param projectId The id of the project from which the column name originates.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`colname` = NULL, `hex` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`colname`, `hex`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(colname, hex, projectId)
      }
      self$`colname` <- `colname`
      self$`hex` <- `hex`
      self$`projectId` <- `projectId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(colname = self$`colname`, hex = self$`hex`, projectId = self$`projectId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`colname`)) {
          sprintf(
            '"colname":
            "%s"
                  ',
            self$`colname`
          )
        },
        if (!is.null(self$`hex`)) {
          sprintf(
            '"hex":
            "%s"
                  ',
            self$`hex`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintsHexColumnNameLookupEntryJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintsHexColumnNameLookupEntryJson, validateParams = FALSE) {
      UserBlueprintsHexColumnNameLookupEntryObject <- jsonlite::fromJSON(UserBlueprintsHexColumnNameLookupEntryJson)
      self$`colname` <- UserBlueprintsHexColumnNameLookupEntryObject$`colname`
      self$`hex` <- UserBlueprintsHexColumnNameLookupEntryObject$`hex`
      self$`projectId` <- UserBlueprintsHexColumnNameLookupEntryObject$`projectId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
