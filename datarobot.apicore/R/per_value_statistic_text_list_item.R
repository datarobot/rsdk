# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PerValueStatisticTextListItem
#'
#' @description PerValueStatisticTextListItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field contextualExtracts  list( character ) Contextual extracts that show context for the n-gram.
#'
#' @field importance  numeric Importance value for this n-gram.
#'
#' @field ngram  character An n-gram.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PerValueStatisticTextListItem <- R6::R6Class(
  "PerValueStatisticTextListItem",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`contextualExtracts` = NULL, `importance` = NULL, `ngram` = NULL) {
      if (!is.null(`contextualExtracts`)) {
        stopifnot(is.vector(`contextualExtracts`), sapply(`contextualExtracts`, is.character))
      }
      if (!is.null(`importance`)) {
      }
      if (!is.null(`ngram`)) {
        stopifnot(is.character(`ngram`), length(`ngram`) == 1)
      }
    }
  ),
  public = list(
    `contextualExtracts` = NULL,
    `importance` = NULL,
    `ngram` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param contextualExtracts Contextual extracts that show context for the n-gram.
    #' @param importance Importance value for this n-gram.
    #' @param ngram An n-gram.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`contextualExtracts` = NULL, `importance` = NULL, `ngram` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`contextualExtracts`, `importance`, `ngram`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(contextualExtracts, importance, ngram)
      }
      self$`contextualExtracts` <- `contextualExtracts`
      self$`importance` <- `importance`
      self$`ngram` <- `ngram`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(contextualExtracts = self$`contextualExtracts`, importance = self$`importance`, ngram = self$`ngram`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`contextualExtracts`)) {
          sprintf(
            '"contextualExtracts":
            [%s]
                  ',
            paste(unlist(lapply(self$`contextualExtracts`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`importance`)) {
          sprintf(
            '"importance":
            %d
                  ',
            self$`importance`
          )
        },
        if (!is.null(self$`ngram`)) {
          sprintf(
            '"ngram":
            "%s"
                  ',
            self$`ngram`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PerValueStatisticTextListItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PerValueStatisticTextListItemJson, validateParams = FALSE) {
      PerValueStatisticTextListItemObject <- jsonlite::fromJSON(PerValueStatisticTextListItemJson)
      self$`contextualExtracts` <- ApiClient$new()$deserializeObj(PerValueStatisticTextListItemObject$`contextualExtracts`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`importance` <- PerValueStatisticTextListItemObject$`importance`
      self$`ngram` <- PerValueStatisticTextListItemObject$`ngram`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
