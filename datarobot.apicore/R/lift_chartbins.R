# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title LiftChartbins
#'
#' @description LiftChartbins Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actual  numeric The average of the actual target values for the rows in the bin.
#'
#' @field binWeight  numeric How much data is in the bin. For projects with weights, it is the sum of the weights of all rows in the bins; otherwise, it is the number of rows in the bin.
#'
#' @field predicted  numeric The average of predicted values of the target for the rows in the bin.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
LiftChartbins <- R6::R6Class(
  "LiftChartbins",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`actual` = NULL, `binWeight` = NULL, `predicted` = NULL) {
      if (!is.null(`actual`)) {
      }
      if (!is.null(`binWeight`)) {
      }
      if (!is.null(`predicted`)) {
      }
    }
  ),
  public = list(
    `actual` = NULL,
    `binWeight` = NULL,
    `predicted` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actual The average of the actual target values for the rows in the bin.
    #' @param binWeight How much data is in the bin. For projects with weights, it is the sum of the weights of all rows in the bins; otherwise, it is the number of rows in the bin.
    #' @param predicted The average of predicted values of the target for the rows in the bin.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actual` = NULL, `binWeight` = NULL, `predicted` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`actual`, `binWeight`, `predicted`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(actual, binWeight, predicted)
      }
      self$`actual` <- `actual`
      self$`binWeight` <- `binWeight`
      self$`predicted` <- `predicted`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(actual = self$`actual`, binWeight = self$`binWeight`, predicted = self$`predicted`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actual`)) {
          sprintf(
            '"actual":
            %d
                  ',
            self$`actual`
          )
        },
        if (!is.null(self$`binWeight`)) {
          sprintf(
            '"binWeight":
            %d
                  ',
            self$`binWeight`
          )
        },
        if (!is.null(self$`predicted`)) {
          sprintf(
            '"predicted":
            %d
                  ',
            self$`predicted`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param LiftChartbinsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(LiftChartbinsJson, validateParams = FALSE) {
      LiftChartbinsObject <- jsonlite::fromJSON(LiftChartbinsJson)
      self$`actual` <- LiftChartbinsObject$`actual`
      self$`binWeight` <- LiftChartbinsObject$`binWeight`
      self$`predicted` <- LiftChartbinsObject$`predicted`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
