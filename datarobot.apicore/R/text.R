# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Text
#'
#' @description Text Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field allData  \link{AllDataText}
#'
#' @field insightName  character Insight name.
#'
#' @field perCluster  list( \link{PerClusterText} ) Statistic values for different feature values in this cluster.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Text <- R6::R6Class(
  "Text",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(allData = NULL, insightName = NULL, perCluster = NULL) {
      if (!is.null(`allData`)) {
        stopifnot(R6::is.R6(`allData`))
      }
      if (!is.null(`insightName`)) {
        stopifnot(is.character(`insightName`), length(`insightName`) == 1)
      }
      if (!is.null(`perCluster`)) {
        stopifnot(is.vector(`perCluster`))
      }
    }
  ),
  public = list(
    `allData` = NULL,
    `insightName` = NULL,
    `perCluster` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param allData
    #' @param insightName Insight name.
    #' @param perCluster Statistic values for different feature values in this cluster.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`allData` = NULL, `insightName` = NULL, `perCluster` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`allData`, `insightName`, `perCluster`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(allData, insightName, perCluster)
      }
      self$`allData` <- `allData`
      self$`insightName` <- `insightName`
      sapply(`perCluster`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(allData = self$`allData`, insightName = self$`insightName`, perCluster = self$`perCluster`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`allData`)) {
          sprintf(
            '"allData":
          %s
        ',
            jsonlite::toJSON(self$`allData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`insightName`)) {
          sprintf(
            '"insightName":
            "%s"
                  ',
            self$`insightName`
          )
        },
        if (!is.null(self$`perCluster`)) {
          sprintf(
            '"perCluster":
            [%s]
',
            paste(sapply(self$`perCluster`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TextJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TextJson, validateParams = FALSE) {
      TextObject <- jsonlite::fromJSON(TextJson)
      self$`allData` <- AllDataText$new()$fromJSON(jsonlite::toJSON(TextObject$allData, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`insightName` <- TextObject$`insightName`
      self$`perCluster` <- ApiClient$new()$deserializeObj(TextObject$`perCluster`, "array[PerClusterText]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
