# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title JobDetailsResponse
#'
#' @description JobDetailsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field id  character The job ID.
#'
#' @field isBlocked  character True if the job is waiting for its dependencies to be resolved first.
#'
#' @field jobType  character The job type.
#'
#' @field message  character Error message in case of failure.
#'
#' @field modelId  character The model this job is associated with.
#'
#' @field projectId  character The project the job belongs to.
#'
#' @field status  character The job status.
#'
#' @field url  character A URL that can be used to request details about the job.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
JobDetailsResponse <- R6::R6Class(
  "JobDetailsResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`id` = NULL, `isBlocked` = NULL, `jobType` = NULL, `message` = NULL, `modelId` = NULL, `projectId` = NULL, `status` = NULL, `url` = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`isBlocked`)) {
        stopifnot(is.logical(`isBlocked`), length(`isBlocked`) == 1)
      }
      if (!is.null(`jobType`)) {
        stopifnot(is.character(`jobType`), length(`jobType`) == 1)
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
    }
  ),
  public = list(
    `id` = NULL,
    `isBlocked` = NULL,
    `jobType` = NULL,
    `message` = NULL,
    `modelId` = NULL,
    `projectId` = NULL,
    `status` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param id The job ID.
    #' @param isBlocked True if the job is waiting for its dependencies to be resolved first.
    #' @param jobType The job type.
    #' @param message Error message in case of failure.
    #' @param modelId The model this job is associated with.
    #' @param projectId The project the job belongs to.
    #' @param status The job status.
    #' @param url A URL that can be used to request details about the job.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `isBlocked` = NULL, `jobType` = NULL, `message` = NULL, `modelId` = NULL, `projectId` = NULL, `status` = NULL, `url` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`id`, `isBlocked`, `jobType`, `message`, `modelId`, `projectId`, `status`, `url`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(id, isBlocked, jobType, message, modelId, projectId, status, url)
      }
      self$`id` <- `id`
      self$`isBlocked` <- `isBlocked`
      self$`jobType` <- `jobType`
      self$`message` <- `message`
      self$`modelId` <- `modelId`
      self$`projectId` <- `projectId`
      self$`status` <- `status`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(id = self$`id`, isBlocked = self$`isBlocked`, jobType = self$`jobType`, message = self$`message`, modelId = self$`modelId`, projectId = self$`projectId`, status = self$`status`, url = self$`url`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`isBlocked`)) {
          sprintf(
            '"isBlocked":
            %s
                  ',
            tolower(self$`isBlocked`)
          )
        },
        if (!is.null(self$`jobType`)) {
          sprintf(
            '"jobType":
            "%s"
                  ',
            self$`jobType`
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param JobDetailsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(JobDetailsResponseJson, validateParams = FALSE) {
      JobDetailsResponseObject <- jsonlite::fromJSON(JobDetailsResponseJson)
      self$`id` <- JobDetailsResponseObject$`id`
      self$`isBlocked` <- JobDetailsResponseObject$`isBlocked`
      self$`jobType` <- JobDetailsResponseObject$`jobType`
      self$`message` <- JobDetailsResponseObject$`message`
      self$`modelId` <- JobDetailsResponseObject$`modelId`
      self$`projectId` <- JobDetailsResponseObject$`projectId`
      self$`status` <- JobDetailsResponseObject$`status`
      self$`url` <- JobDetailsResponseObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
