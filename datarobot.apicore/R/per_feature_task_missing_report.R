# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PerFeatureTaskMissingReport
#'
#' @description PerFeatureTaskMissingReport Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field descriptions  list( character ) Human readable aggregated information about how the task handles missing values. The following descriptions may be present: what value is imputed for missing values, whether the feature being missing is treated as a feature by the task, whether missing values are treated as infrequent values, whether infrequent values are treated as missing values, and whether missing values are ignored.
#'
#' @field name  character Task name, e.g. &#39;Ordinal encoding of categorical variables&#39;
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PerFeatureTaskMissingReport <- R6::R6Class(
  "PerFeatureTaskMissingReport",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(descriptions = NULL, name = NULL) {
      if (!is.null(`descriptions`)) {
        stopifnot(is.vector(`descriptions`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
    }
  ),
  public = list(
    `descriptions` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param descriptions Human readable aggregated information about how the task handles missing values. The following descriptions may be present: what value is imputed for missing values, whether the feature being missing is treated as a feature by the task, whether missing values are treated as infrequent values, whether infrequent values are treated as missing values, and whether missing values are ignored.
    #' @param name Task name, e.g. &#39;Ordinal encoding of categorical variables&#39;
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`descriptions` = NULL, `name` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`descriptions`, `name`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(descriptions, name)
      }
      sapply(`descriptions`, function(x) stopifnot(is.character(x)))
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(descriptions = self$`descriptions`, name = self$`name`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`descriptions`)) {
          sprintf(
            '"descriptions":
              [%s]
            ',
            paste(unlist(lapply(self$`descriptions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PerFeatureTaskMissingReportJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PerFeatureTaskMissingReportJson, validateParams = FALSE) {
      PerFeatureTaskMissingReportObject <- jsonlite::fromJSON(PerFeatureTaskMissingReportJson)
      self$`descriptions` <- ApiClient$new()$deserializeObj(PerFeatureTaskMissingReportObject$`descriptions`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`name` <- PerFeatureTaskMissingReportObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
