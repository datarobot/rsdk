# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BlenderCreate
#'
#' @description BlenderCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blenderMethod  character The blender method, one of \&quot;PLS\&quot;, \&quot;GLM\&quot;, \&quot;AVG\&quot;, \&quot;ENET\&quot;, \&quot;MED\&quot;, \&quot;MAE\&quot;, \&quot;MAEL1\&quot;, \&quot;TF\&quot;, \&quot;RF\&quot;, \&quot;LGBM\&quot;, \&quot;FORECAST_DISTANCE_ENET\&quot; (new in v2.18), \&quot;FORECAST_DISTANCE_AVG\&quot; (new in v2.18), \&quot;MIN\&quot;, \&quot;MAX\&quot;.
#'
#' @field modelIds  list( character ) The list of models to use in blender.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BlenderCreate <- R6::R6Class(
  "BlenderCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`blenderMethod` = NULL, `modelIds` = NULL) {
      if (!is.null(`blenderMethod`)) {
        stopifnot(is.character(`blenderMethod`), length(`blenderMethod`) == 1)
      }
      if (!is.null(`modelIds`)) {
        stopifnot(is.vector(`modelIds`), sapply(`modelIds`, is.character))
      }
    }
  ),
  public = list(
    `blenderMethod` = NULL,
    `modelIds` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blenderMethod The blender method, one of \&quot;PLS\&quot;, \&quot;GLM\&quot;, \&quot;AVG\&quot;, \&quot;ENET\&quot;, \&quot;MED\&quot;, \&quot;MAE\&quot;, \&quot;MAEL1\&quot;, \&quot;TF\&quot;, \&quot;RF\&quot;, \&quot;LGBM\&quot;, \&quot;FORECAST_DISTANCE_ENET\&quot; (new in v2.18), \&quot;FORECAST_DISTANCE_AVG\&quot; (new in v2.18), \&quot;MIN\&quot;, \&quot;MAX\&quot;.
    #' @param modelIds The list of models to use in blender.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blenderMethod` = NULL, `modelIds` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`blenderMethod`, `modelIds`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(blenderMethod, modelIds)
      }
      self$`blenderMethod` <- `blenderMethod`
      self$`modelIds` <- `modelIds`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(blenderMethod = self$`blenderMethod`, modelIds = self$`modelIds`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blenderMethod`)) {
          sprintf(
            '"blenderMethod":
            "%s"
                  ',
            self$`blenderMethod`
          )
        },
        if (!is.null(self$`modelIds`)) {
          sprintf(
            '"modelIds":
            [%s]
                  ',
            paste(unlist(lapply(self$`modelIds`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BlenderCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BlenderCreateJson, validateParams = FALSE) {
      BlenderCreateObject <- jsonlite::fromJSON(BlenderCreateJson)
      self$`blenderMethod` <- BlenderCreateObject$`blenderMethod`
      self$`modelIds` <- ApiClient$new()$deserializeObj(BlenderCreateObject$`modelIds`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
