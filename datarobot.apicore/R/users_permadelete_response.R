# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UsersPermadeleteResponse
#'
#' @description UsersPermadeleteResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field orgId  character [optional] Organization which&#39;s users to be permanently deleted.
#'
#' @field orphansOwner  character [optional] User which becomes an owner of any projects or deployments that otherwise cause an error because they would be orphaned.
#'
#' @field orphansOwnerName  character [optional] User which becomes an owner of any projects or deployments that otherwise cause an error because they would be orphaned.
#'
#' @field userIds  list( character ) [optional] Users to be permanently deleted.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UsersPermadeleteResponse <- R6::R6Class(
  "UsersPermadeleteResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`orgId` = NULL, `orphansOwner` = NULL, `orphansOwnerName` = NULL, `userIds` = NULL) {
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
      if (!is.null(`orphansOwner`)) {
        stopifnot(is.character(`orphansOwner`), length(`orphansOwner`) == 1)
      }
      if (!is.null(`orphansOwnerName`)) {
        stopifnot(is.character(`orphansOwnerName`), length(`orphansOwnerName`) == 1)
      }
      if (!is.null(`userIds`) && length(`userIds`) > 0) {
        stopifnot(is.vector(`userIds`), sapply(`userIds`, is.character))
      }
    }
  ),
  public = list(
    `orgId` = NULL,
    `orphansOwner` = NULL,
    `orphansOwnerName` = NULL,
    `userIds` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param orgId Organization which&#39;s users to be permanently deleted.
    #' @param orphansOwner User which becomes an owner of any projects or deployments that otherwise cause an error because they would be orphaned.
    #' @param orphansOwnerName User which becomes an owner of any projects or deployments that otherwise cause an error because they would be orphaned.
    #' @param userIds Users to be permanently deleted.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`orgId` = NULL, `orphansOwner` = NULL, `orphansOwnerName` = NULL, `userIds` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(orgId, orphansOwner, orphansOwnerName, userIds)
      }
      self$`orgId` <- `orgId`
      self$`orphansOwner` <- `orphansOwner`
      self$`orphansOwnerName` <- `orphansOwnerName`
      self$`userIds` <- `userIds`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(orgId = self$`orgId`, orphansOwner = self$`orphansOwner`, orphansOwnerName = self$`orphansOwnerName`, userIds = self$`userIds`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        },
        if (!is.null(self$`orphansOwner`)) {
          sprintf(
            '"orphansOwner":
            "%s"
                  ',
            self$`orphansOwner`
          )
        },
        if (!is.null(self$`orphansOwnerName`)) {
          sprintf(
            '"orphansOwnerName":
            "%s"
                  ',
            self$`orphansOwnerName`
          )
        },
        if (!is.null(self$`userIds`)) {
          sprintf(
            '"userIds":
            [%s]
                  ',
            paste(unlist(lapply(self$`userIds`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UsersPermadeleteResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UsersPermadeleteResponseJson, validateParams = FALSE) {
      UsersPermadeleteResponseObject <- jsonlite::fromJSON(UsersPermadeleteResponseJson)
      self$`orgId` <- UsersPermadeleteResponseObject$`orgId`
      self$`orphansOwner` <- UsersPermadeleteResponseObject$`orphansOwner`
      self$`orphansOwnerName` <- UsersPermadeleteResponseObject$`orphansOwnerName`
      self$`userIds` <- ApiClient$new()$deserializeObj(UsersPermadeleteResponseObject$`userIds`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
