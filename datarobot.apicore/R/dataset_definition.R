# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetDefinition
#'
#' @description DatasetDefinition Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalogId  character ID of the catalog item.
#'
#' @field catalogVersionId  character ID of the catalog item version.
#'
#' @field featureListId  character [optional] ID of the feature list. This decides which columns in the dataset are used for feature generation.
#'
#' @field identifier  character Short name of the dataset (used directly as part of the generated feature names).
#'
#' @field primaryTemporalKey  character [optional] Name of the column indicating time of record creation.
#'
#' @field snapshotPolicy  character [optional] Policy for using dataset snapshots when creating a project or making predictions. Must be one of the following values: &#39;specified&#39;: Use specific snapshot specified by catalogVersionId. &#39;latest&#39;: Use latest snapshot from the same catalog item. &#39;dynamic&#39;: Get data from the source (only applicable for JDBC datasets).
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetDefinition <- R6::R6Class(
  "DatasetDefinition",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(catalogId = NULL, catalogVersionId = NULL, featureListId = NULL, identifier = NULL, primaryTemporalKey = NULL, snapshotPolicy = NULL) {
      if (!is.null(`catalogId`)) {
        stopifnot(is.character(`catalogId`), length(`catalogId`) == 1)
      }
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`identifier`)) {
        stopifnot(is.character(`identifier`), length(`identifier`) == 1)
      }
      if (!is.null(`featureListId`)) {
        stopifnot(is.character(`featureListId`), length(`featureListId`) == 1)
      }
      if (!is.null(`primaryTemporalKey`)) {
        stopifnot(is.character(`primaryTemporalKey`), length(`primaryTemporalKey`) == 1)
      }
      if (!is.null(`snapshotPolicy`)) {
        stopifnot(is.character(`snapshotPolicy`), length(`snapshotPolicy`) == 1)
      }
    }
  ),
  public = list(
    `catalogId` = NULL,
    `catalogVersionId` = NULL,
    `featureListId` = NULL,
    `identifier` = NULL,
    `primaryTemporalKey` = NULL,
    `snapshotPolicy` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalogId ID of the catalog item.
    #' @param catalogVersionId ID of the catalog item version.
    #' @param featureListId ID of the feature list. This decides which columns in the dataset are used for feature generation.
    #' @param identifier Short name of the dataset (used directly as part of the generated feature names).
    #' @param primaryTemporalKey Name of the column indicating time of record creation.
    #' @param snapshotPolicy Policy for using dataset snapshots when creating a project or making predictions. Must be one of the following values: &#39;specified&#39;: Use specific snapshot specified by catalogVersionId. &#39;latest&#39;: Use latest snapshot from the same catalog item. &#39;dynamic&#39;: Get data from the source (only applicable for JDBC datasets).
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`catalogId` = NULL, `catalogVersionId` = NULL, `identifier` = NULL, `featureListId` = NULL, `primaryTemporalKey` = NULL, `snapshotPolicy` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`catalogId`, `catalogVersionId`, `identifier`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(catalogId, catalogVersionId, featureListId, identifier, primaryTemporalKey, snapshotPolicy)
      }
      self$`catalogId` <- `catalogId`
      self$`catalogVersionId` <- `catalogVersionId`
      self$`featureListId` <- `featureListId`
      self$`identifier` <- `identifier`
      self$`primaryTemporalKey` <- `primaryTemporalKey`
      self$`snapshotPolicy` <- `snapshotPolicy`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(catalogId = self$`catalogId`, catalogVersionId = self$`catalogVersionId`, featureListId = self$`featureListId`, identifier = self$`identifier`, primaryTemporalKey = self$`primaryTemporalKey`, snapshotPolicy = self$`snapshotPolicy`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalogId`)) {
          sprintf(
            '"catalogId":
            "%s"
                  ',
            self$`catalogId`
          )
        },
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`featureListId`)) {
          sprintf(
            '"featureListId":
            "%s"
                  ',
            self$`featureListId`
          )
        },
        if (!is.null(self$`identifier`)) {
          sprintf(
            '"identifier":
            "%s"
                  ',
            self$`identifier`
          )
        },
        if (!is.null(self$`primaryTemporalKey`)) {
          sprintf(
            '"primaryTemporalKey":
            "%s"
                  ',
            self$`primaryTemporalKey`
          )
        },
        if (!is.null(self$`snapshotPolicy`)) {
          sprintf(
            '"snapshotPolicy":
            "%s"
                  ',
            self$`snapshotPolicy`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetDefinitionJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetDefinitionJson, validateParams = FALSE) {
      DatasetDefinitionObject <- jsonlite::fromJSON(DatasetDefinitionJson)
      self$`catalogId` <- DatasetDefinitionObject$`catalogId`
      self$`catalogVersionId` <- DatasetDefinitionObject$`catalogVersionId`
      self$`featureListId` <- DatasetDefinitionObject$`featureListId`
      self$`identifier` <- DatasetDefinitionObject$`identifier`
      self$`primaryTemporalKey` <- DatasetDefinitionObject$`primaryTemporalKey`
      self$`snapshotPolicy` <- DatasetDefinitionObject$`snapshotPolicy`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
