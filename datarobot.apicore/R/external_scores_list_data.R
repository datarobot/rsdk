# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ExternalScoresListData
#'
#' @description ExternalScoresListData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualValueColumn  character The name of the column with actuals that was used to calculate the scores.
#'
#' @field datasetId  character The datasetId the data comes from.
#'
#' @field modelId  character The modelId for the scores.
#'
#' @field projectId  character The projectId for the scores.
#'
#' @field scores  list( \link{ExternalScoresListDataScore} ) A json array of the computed scores.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ExternalScoresListData <- R6::R6Class(
  "ExternalScoresListData",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(actualValueColumn = NULL, datasetId = NULL, modelId = NULL, projectId = NULL, scores = NULL) {
      if (!is.null(`actualValueColumn`)) {
        stopifnot(is.character(`actualValueColumn`), length(`actualValueColumn`) == 1)
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`scores`)) {
        stopifnot(is.vector(`scores`))
      }
    }
  ),
  public = list(
    `actualValueColumn` = NULL,
    `datasetId` = NULL,
    `modelId` = NULL,
    `projectId` = NULL,
    `scores` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualValueColumn The name of the column with actuals that was used to calculate the scores.
    #' @param datasetId The datasetId the data comes from.
    #' @param modelId The modelId for the scores.
    #' @param projectId The projectId for the scores.
    #' @param scores A json array of the computed scores.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actualValueColumn` = NULL, `datasetId` = NULL, `modelId` = NULL, `projectId` = NULL, `scores` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`actualValueColumn`, `datasetId`, `modelId`, `projectId`, `scores`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(actualValueColumn, datasetId, modelId, projectId, scores)
      }
      self$`actualValueColumn` <- `actualValueColumn`
      self$`datasetId` <- `datasetId`
      self$`modelId` <- `modelId`
      self$`projectId` <- `projectId`
      sapply(`scores`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(actualValueColumn = self$`actualValueColumn`, datasetId = self$`datasetId`, modelId = self$`modelId`, projectId = self$`projectId`, scores = self$`scores`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualValueColumn`)) {
          sprintf(
            '"actualValueColumn":
            "%s"
                  ',
            self$`actualValueColumn`
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`scores`)) {
          sprintf(
            '"scores":
            [%s]
',
            paste(sapply(self$`scores`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ExternalScoresListDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ExternalScoresListDataJson, validateParams = FALSE) {
      ExternalScoresListDataObject <- jsonlite::fromJSON(ExternalScoresListDataJson)
      self$`actualValueColumn` <- ExternalScoresListDataObject$`actualValueColumn`
      self$`datasetId` <- ExternalScoresListDataObject$`datasetId`
      self$`modelId` <- ExternalScoresListDataObject$`modelId`
      self$`projectId` <- ExternalScoresListDataObject$`projectId`
      self$`scores` <- ApiClient$new()$deserializeObj(ExternalScoresListDataObject$`scores`, "array[ExternalScoresListDataScore]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
