# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ProjectStatusResponse
#'
#' @description ProjectStatusResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field autopilotDone  character whether the current autopilot run has finished
#'
#' @field stage  character the current stage of the project, where modeling indicates that the target has been successfully set and modeling and predictions may proceed
#'
#' @field stageDescription  character a description of the current stage of the project
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ProjectStatusResponse <- R6::R6Class(
  "ProjectStatusResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(autopilotDone = NULL, stage = NULL, stageDescription = NULL) {
      if (!is.null(`autopilotDone`)) {
        stopifnot(is.logical(`autopilotDone`), length(`autopilotDone`) == 1)
      }
      if (!is.null(`stage`)) {
        stopifnot(is.character(`stage`), length(`stage`) == 1)
      }
      if (!is.null(`stageDescription`)) {
        stopifnot(is.character(`stageDescription`), length(`stageDescription`) == 1)
      }
    }
  ),
  public = list(
    `autopilotDone` = NULL,
    `stage` = NULL,
    `stageDescription` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param autopilotDone whether the current autopilot run has finished
    #' @param stage the current stage of the project, where modeling indicates that the target has been successfully set and modeling and predictions may proceed
    #' @param stageDescription a description of the current stage of the project
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`autopilotDone` = NULL, `stage` = NULL, `stageDescription` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`autopilotDone`, `stage`, `stageDescription`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(autopilotDone, stage, stageDescription)
      }
      self$`autopilotDone` <- `autopilotDone`
      self$`stage` <- `stage`
      self$`stageDescription` <- `stageDescription`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(autopilotDone = self$`autopilotDone`, stage = self$`stage`, stageDescription = self$`stageDescription`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`autopilotDone`)) {
          sprintf(
            '"autopilotDone":
            %s
                  ',
            tolower(self$`autopilotDone`)
          )
        },
        if (!is.null(self$`stage`)) {
          sprintf(
            '"stage":
            "%s"
                  ',
            self$`stage`
          )
        },
        if (!is.null(self$`stageDescription`)) {
          sprintf(
            '"stageDescription":
            "%s"
                  ',
            self$`stageDescription`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ProjectStatusResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ProjectStatusResponseJson, validateParams = FALSE) {
      ProjectStatusResponseObject <- jsonlite::fromJSON(ProjectStatusResponseJson)
      self$`autopilotDone` <- ProjectStatusResponseObject$`autopilotDone`
      self$`stage` <- ProjectStatusResponseObject$`stage`
      self$`stageDescription` <- ProjectStatusResponseObject$`stageDescription`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
