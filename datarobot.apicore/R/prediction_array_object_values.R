# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionArrayObjectValues
#'
#' @description PredictionArrayObjectValues Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field label  \link{OneOfstringnumber} For regression problems this will be the name of the target column, &#39;Anomaly score&#39; or ignored field. For classification projects this will be the name of the class.
#'
#' @field threshold  numeric [optional] Threshold used in multilabel classification for this class.
#'
#' @field value  numeric The predicted probability of the class identified by the label.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionArrayObjectValues <- R6::R6Class(
  "PredictionArrayObjectValues",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(label = NULL, threshold = NULL, value = NULL) {
      if (!is.null(`label`)) {
        .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = label)
      }
      if (!is.null(`value`)) {
      }
      if (!is.null(`threshold`)) {
        stopifnot(is.numeric(`threshold`), length(`threshold`) == 1)
      }
    }
  ),
  public = list(
    `label` = NULL,
    `threshold` = NULL,
    `value` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param label For regression problems this will be the name of the target column, &#39;Anomaly score&#39; or ignored field. For classification projects this will be the name of the class.
    #' @param threshold Threshold used in multilabel classification for this class.
    #' @param value The predicted probability of the class identified by the label.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`label` = NULL, `value` = NULL, `threshold` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`label`, `value`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(label, threshold, value)
      }
      self$`label` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = label)
      self$`threshold` <- `threshold`
      self$`value` <- `value`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(label = self$`label`, threshold = self$`threshold`, value = self$`value`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
          %s
        ',
            self$`label`
          )
        },
        if (!is.null(self$`threshold`)) {
          sprintf(
            '"threshold":
            %d
                  ',
            self$`threshold`
          )
        },
        if (!is.null(self$`value`)) {
          sprintf(
            '"value":
            %d
                  ',
            self$`value`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionArrayObjectValuesJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionArrayObjectValuesJson, validateParams = FALSE) {
      PredictionArrayObjectValuesObject <- jsonlite::fromJSON(PredictionArrayObjectValuesJson)
      self$`label` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = PredictionArrayObjectValuesObject$label)
      self$`threshold` <- PredictionArrayObjectValuesObject$`threshold`
      self$`value` <- PredictionArrayObjectValuesObject$`value`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
