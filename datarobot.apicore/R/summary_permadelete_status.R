# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SummaryPermadeleteStatus
#'
#' @description SummaryPermadeleteStatus Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field message  character May contain further information about the status.
#'
#' @field reportId  character Report ID
#'
#' @field status  character The processing state of users perma-delete task.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SummaryPermadeleteStatus <- R6::R6Class(
  "SummaryPermadeleteStatus",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(message = NULL, reportId = NULL, status = NULL) {
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`reportId`)) {
        stopifnot(is.character(`reportId`), length(`reportId`) == 1)
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
    }
  ),
  public = list(
    `message` = NULL,
    `reportId` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param message May contain further information about the status.
    #' @param reportId Report ID
    #' @param status The processing state of users perma-delete task.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`message` = NULL, `reportId` = NULL, `status` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`message`, `reportId`, `status`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(message, reportId, status)
      }
      self$`message` <- `message`
      self$`reportId` <- `reportId`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(message = self$`message`, reportId = self$`reportId`, status = self$`status`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`reportId`)) {
          sprintf(
            '"reportId":
            "%s"
                  ',
            self$`reportId`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SummaryPermadeleteStatusJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SummaryPermadeleteStatusJson, validateParams = FALSE) {
      SummaryPermadeleteStatusObject <- jsonlite::fromJSON(SummaryPermadeleteStatusJson)
      self$`message` <- SummaryPermadeleteStatusObject$`message`
      self$`reportId` <- SummaryPermadeleteStatusObject$`reportId`
      self$`status` <- SummaryPermadeleteStatusObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
