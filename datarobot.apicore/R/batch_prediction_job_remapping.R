# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BatchPredictionJobRemapping
#'
#' @description BatchPredictionJobRemapping Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field inputName  character Rename column with this name
#'
#' @field outputName  character Rename column to this name (leave as null to remove from the output)
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BatchPredictionJobRemapping <- R6::R6Class(
  "BatchPredictionJobRemapping",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(inputName = NULL, outputName = NULL) {
      if (!is.null(`inputName`)) {
        stopifnot(is.character(`inputName`), length(`inputName`) == 1)
      }
      if (!is.null(`outputName`)) {
        stopifnot(is.character(`outputName`), length(`outputName`) == 1)
      }
    }
  ),
  public = list(
    `inputName` = NULL,
    `outputName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param inputName Rename column with this name
    #' @param outputName Rename column to this name (leave as null to remove from the output)
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`inputName` = NULL, `outputName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`inputName`, `outputName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(inputName, outputName)
      }
      self$`inputName` <- `inputName`
      self$`outputName` <- `outputName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(inputName = self$`inputName`, outputName = self$`outputName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`inputName`)) {
          sprintf(
            '"inputName":
            "%s"
                  ',
            self$`inputName`
          )
        },
        if (!is.null(self$`outputName`)) {
          sprintf(
            '"outputName":
            "%s"
                  ',
            self$`outputName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BatchPredictionJobRemappingJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BatchPredictionJobRemappingJson, validateParams = FALSE) {
      BatchPredictionJobRemappingObject <- jsonlite::fromJSON(BatchPredictionJobRemappingJson)
      self$`inputName` <- BatchPredictionJobRemappingObject$`inputName`
      self$`outputName` <- BatchPredictionJobRemappingObject$`outputName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
