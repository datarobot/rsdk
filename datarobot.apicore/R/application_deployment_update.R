# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApplicationDeploymentUpdate
#'
#' @description ApplicationDeploymentUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field description  character [optional] The description of the application
#'
#' @field domain  character [optional] The new domain prefix for the app
#'
#' @field name  character [optional] The name of the app
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApplicationDeploymentUpdate <- R6::R6Class(
  "ApplicationDeploymentUpdate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(description = NULL, domain = NULL, name = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`domain`)) {
        stopifnot(is.character(`domain`), length(`domain`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
    }
  ),
  public = list(
    `description` = NULL,
    `domain` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param description The description of the application
    #' @param domain The new domain prefix for the app
    #' @param name The name of the app
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `domain` = NULL, `name` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(description, domain, name)
      }
      self$`description` <- `description`
      self$`domain` <- `domain`
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(description = self$`description`, domain = self$`domain`, name = self$`name`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`domain`)) {
          sprintf(
            '"domain":
            "%s"
                  ',
            self$`domain`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApplicationDeploymentUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApplicationDeploymentUpdateJson, validateParams = FALSE) {
      ApplicationDeploymentUpdateObject <- jsonlite::fromJSON(ApplicationDeploymentUpdateJson)
      self$`description` <- ApplicationDeploymentUpdateObject$`description`
      self$`domain` <- ApplicationDeploymentUpdateObject$`domain`
      self$`name` <- ApplicationDeploymentUpdateObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
