# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TimeSeriesFeatureLogListControllerResponse
#'
#' @description TimeSeriesFeatureLogListControllerResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer The number of items returned on this page.
#'
#' @field featureLog  character The content of the feature log.
#'
#' @field next_  character URL pointing to the next page (if null, there is no next page).
#'
#' @field previous  character URL pointing to the previous page (if null, there is no previous page).
#'
#' @field totalLogLines  integer The total number of lines in feature derivation log.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TimeSeriesFeatureLogListControllerResponse <- R6::R6Class(
  "TimeSeriesFeatureLogListControllerResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `featureLog` = NULL, `next_` = NULL, `previous` = NULL, `totalLogLines` = NULL) {
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`featureLog`)) {
        stopifnot(is.character(`featureLog`), length(`featureLog`) == 1)
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
      if (!is.null(`totalLogLines`)) {
        stopifnot(is.numeric(`totalLogLines`), length(`totalLogLines`) == 1)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `featureLog` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    `totalLogLines` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count The number of items returned on this page.
    #' @param featureLog The content of the feature log.
    #' @param next_ URL pointing to the next page (if null, there is no next page).
    #' @param previous URL pointing to the previous page (if null, there is no previous page).
    #' @param totalLogLines The total number of lines in feature derivation log.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `featureLog` = NULL, `next_` = NULL, `previous` = NULL, `totalLogLines` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`count`, `featureLog`, `next_`, `previous`, `totalLogLines`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, featureLog, next_, previous, totalLogLines)
      }
      self$`count` <- `count`
      self$`featureLog` <- `featureLog`
      self$`next_` <- `next_`
      self$`previous` <- `previous`
      self$`totalLogLines` <- `totalLogLines`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, featureLog = self$`featureLog`, next_ = self$`next_`, previous = self$`previous`, totalLogLines = self$`totalLogLines`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`featureLog`)) {
          sprintf(
            '"featureLog":
            "%s"
                  ',
            self$`featureLog`
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        },
        if (!is.null(self$`totalLogLines`)) {
          sprintf(
            '"totalLogLines":
            %d
                  ',
            self$`totalLogLines`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TimeSeriesFeatureLogListControllerResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TimeSeriesFeatureLogListControllerResponseJson, validateParams = FALSE) {
      TimeSeriesFeatureLogListControllerResponseObject <- jsonlite::fromJSON(TimeSeriesFeatureLogListControllerResponseJson)
      self$`count` <- TimeSeriesFeatureLogListControllerResponseObject$`count`
      self$`featureLog` <- TimeSeriesFeatureLogListControllerResponseObject$`featureLog`
      self$`next_` <- TimeSeriesFeatureLogListControllerResponseObject$`next`
      self$`previous` <- TimeSeriesFeatureLogListControllerResponseObject$`previous`
      self$`totalLogLines` <- TimeSeriesFeatureLogListControllerResponseObject$`totalLogLines`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
