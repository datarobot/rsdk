# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AccuracyOverTimePlotsBins
#'
#' @description AccuracyOverTimePlotsBins Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actual  numeric Average actual value of the target in the bin. &#x60;null&#x60; if there are no entries in the bin.
#'
#' @field endDate  character The datetime of the end of the bin (exclusive).
#'
#' @field frequency  integer Indicates number of values averaged in bin in case of a resolution change.
#'
#' @field predicted  numeric Average prediction of the model in the bin. &#x60;null&#x60; if there are no entries in the bin.
#'
#' @field startDate  character The datetime of the start of the bin (inclusive).
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AccuracyOverTimePlotsBins <- R6::R6Class(
  "AccuracyOverTimePlotsBins",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(actual = NULL, endDate = NULL, frequency = NULL, predicted = NULL, startDate = NULL) {
      if (!is.null(`actual`)) {
      }
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`frequency`)) {
        stopifnot(is.numeric(`frequency`), length(`frequency`) == 1)
      }
      if (!is.null(`predicted`)) {
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `actual` = NULL,
    `endDate` = NULL,
    `frequency` = NULL,
    `predicted` = NULL,
    `startDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actual Average actual value of the target in the bin. &#x60;null&#x60; if there are no entries in the bin.
    #' @param endDate The datetime of the end of the bin (exclusive).
    #' @param frequency Indicates number of values averaged in bin in case of a resolution change.
    #' @param predicted Average prediction of the model in the bin. &#x60;null&#x60; if there are no entries in the bin.
    #' @param startDate The datetime of the start of the bin (inclusive).
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actual` = NULL, `endDate` = NULL, `frequency` = NULL, `predicted` = NULL, `startDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`actual`, `endDate`, `frequency`, `predicted`, `startDate`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(actual, endDate, frequency, predicted, startDate)
      }
      self$`actual` <- `actual`
      self$`endDate` <- `endDate`
      self$`frequency` <- `frequency`
      self$`predicted` <- `predicted`
      self$`startDate` <- `startDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(actual = self$`actual`, endDate = self$`endDate`, frequency = self$`frequency`, predicted = self$`predicted`, startDate = self$`startDate`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actual`)) {
          sprintf(
            '"actual":
            %d
                  ',
            self$`actual`
          )
        },
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`frequency`)) {
          sprintf(
            '"frequency":
            %d
                  ',
            self$`frequency`
          )
        },
        if (!is.null(self$`predicted`)) {
          sprintf(
            '"predicted":
            %d
                  ',
            self$`predicted`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AccuracyOverTimePlotsBinsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AccuracyOverTimePlotsBinsJson, validateParams = FALSE) {
      AccuracyOverTimePlotsBinsObject <- jsonlite::fromJSON(AccuracyOverTimePlotsBinsJson)
      self$`actual` <- AccuracyOverTimePlotsBinsObject$`actual`
      self$`endDate` <- ParseRFC3339Timestamp(AccuracyOverTimePlotsBinsObject$`endDate`)
      self$`frequency` <- AccuracyOverTimePlotsBinsObject$`frequency`
      self$`predicted` <- AccuracyOverTimePlotsBinsObject$`predicted`
      self$`startDate` <- ParseRFC3339Timestamp(AccuracyOverTimePlotsBinsObject$`startDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
