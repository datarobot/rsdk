# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseAttachmentModelingProjectOverview
#'
#' @description UseCaseAttachmentModelingProjectOverview Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field createdAt  character Time when project was created
#'
#' @field datasetName  character Name of default dataset
#'
#' @field name  character Name of the project
#'
#' @field numberOfModels  integer Number of the models in the project
#'
#' @field owner  character Name of the modeling project owner
#'
#' @field targetName  character Name of modeling target
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseAttachmentModelingProjectOverview <- R6::R6Class(
  "UseCaseAttachmentModelingProjectOverview",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`createdAt` = NULL, `datasetName` = NULL, `name` = NULL, `numberOfModels` = NULL, `owner` = NULL, `targetName` = NULL) {
      if (!is.null(`createdAt`)) {
        stopifnot(inherits(`createdAt`, "POSIXt"))
      }
      if (!is.null(`datasetName`)) {
        stopifnot(is.character(`datasetName`), length(`datasetName`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`numberOfModels`)) {
        stopifnot(is.numeric(`numberOfModels`), length(`numberOfModels`) == 1)
      }
      if (!is.null(`owner`)) {
        stopifnot(is.character(`owner`), length(`owner`) == 1)
      }
      if (!is.null(`targetName`)) {
        stopifnot(is.character(`targetName`), length(`targetName`) == 1)
      }
    }
  ),
  public = list(
    `createdAt` = NULL,
    `datasetName` = NULL,
    `name` = NULL,
    `numberOfModels` = NULL,
    `owner` = NULL,
    `targetName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param createdAt Time when project was created
    #' @param datasetName Name of default dataset
    #' @param name Name of the project
    #' @param numberOfModels Number of the models in the project
    #' @param owner Name of the modeling project owner
    #' @param targetName Name of modeling target
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`createdAt` = NULL, `datasetName` = NULL, `name` = NULL, `numberOfModels` = NULL, `owner` = NULL, `targetName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`createdAt`, `datasetName`, `name`, `numberOfModels`, `owner`, `targetName`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(createdAt, datasetName, name, numberOfModels, owner, targetName)
      }
      self$`createdAt` <- `createdAt`
      self$`datasetName` <- `datasetName`
      self$`name` <- `name`
      self$`numberOfModels` <- `numberOfModels`
      self$`owner` <- `owner`
      self$`targetName` <- `targetName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(createdAt = self$`createdAt`, datasetName = self$`datasetName`, name = self$`name`, numberOfModels = self$`numberOfModels`, owner = self$`owner`, targetName = self$`targetName`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`createdAt`)) {
          sprintf(
            '"createdAt":
            "%s"
                  ',
            format(self$`createdAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`datasetName`)) {
          sprintf(
            '"datasetName":
            "%s"
                  ',
            self$`datasetName`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`numberOfModels`)) {
          sprintf(
            '"numberOfModels":
            %d
                  ',
            self$`numberOfModels`
          )
        },
        if (!is.null(self$`owner`)) {
          sprintf(
            '"owner":
            "%s"
                  ',
            self$`owner`
          )
        },
        if (!is.null(self$`targetName`)) {
          sprintf(
            '"targetName":
            "%s"
                  ',
            self$`targetName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseAttachmentModelingProjectOverviewJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseAttachmentModelingProjectOverviewJson, validateParams = FALSE) {
      UseCaseAttachmentModelingProjectOverviewObject <- jsonlite::fromJSON(UseCaseAttachmentModelingProjectOverviewJson)
      self$`createdAt` <- ParseRFC3339Timestamp(UseCaseAttachmentModelingProjectOverviewObject$`createdAt`)
      self$`datasetName` <- UseCaseAttachmentModelingProjectOverviewObject$`datasetName`
      self$`name` <- UseCaseAttachmentModelingProjectOverviewObject$`name`
      self$`numberOfModels` <- UseCaseAttachmentModelingProjectOverviewObject$`numberOfModels`
      self$`owner` <- UseCaseAttachmentModelingProjectOverviewObject$`owner`
      self$`targetName` <- UseCaseAttachmentModelingProjectOverviewObject$`targetName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
