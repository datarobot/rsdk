# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ChangeRequestCreate
#'
#' @description ChangeRequestCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field action  character Actions the user can take on the entity. Each entity type has a specific set of actions.
#'
#' @field autoApply  character Whether to automatically apply the change when the request is approved. If &#x60;true&#x60;, the requested changes will be applied on approval.
#'
#' @field change  \link{DeploymentApproveChange}
#'
#' @field comment  character [optional] Free form text to comment on the requested changes.
#'
#' @field entityId  character ID of the Product Entity the request is intended to change.
#'
#' @field entityType  character Type of the Product Entity that is requested to be changed.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ChangeRequestCreate <- R6::R6Class(
  "ChangeRequestCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`action` = NULL, `autoApply` = NULL, `change` = NULL, `comment` = NULL, `entityId` = NULL, `entityType` = NULL) {
      if (!is.null(`action`)) {
        stopifnot(is.character(`action`), length(`action`) == 1)
      }
      if (!is.null(`autoApply`)) {
        stopifnot(is.logical(`autoApply`), length(`autoApply`) == 1)
      }
      if (!is.null(`change`)) {
        stopifnot(R6::is.R6(`change`))
      }
      if (!is.null(`entityId`)) {
        stopifnot(is.character(`entityId`), length(`entityId`) == 1)
      }
      if (!is.null(`entityType`)) {
        stopifnot(is.character(`entityType`), length(`entityType`) == 1)
      }
      if (!is.null(`comment`)) {
        stopifnot(is.character(`comment`), length(`comment`) == 1)
      }
    }
  ),
  public = list(
    `action` = NULL,
    `autoApply` = NULL,
    `change` = NULL,
    `comment` = NULL,
    `entityId` = NULL,
    `entityType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param action Actions the user can take on the entity. Each entity type has a specific set of actions.
    #' @param autoApply Whether to automatically apply the change when the request is approved. If &#x60;true&#x60;, the requested changes will be applied on approval.
    #' @param change
    #' @param comment Free form text to comment on the requested changes.
    #' @param entityId ID of the Product Entity the request is intended to change.
    #' @param entityType Type of the Product Entity that is requested to be changed.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`action` = NULL, `autoApply` = NULL, `change` = NULL, `entityId` = NULL, `entityType` = NULL, `comment` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`action`, `autoApply`, `change`, `entityId`, `entityType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(action, autoApply, change, comment, entityId, entityType)
      }
      self$`action` <- `action`
      self$`autoApply` <- `autoApply`
      self$`change` <- `change`
      self$`comment` <- `comment`
      self$`entityId` <- `entityId`
      self$`entityType` <- `entityType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(action = self$`action`, autoApply = self$`autoApply`, change = self$`change`, comment = self$`comment`, entityId = self$`entityId`, entityType = self$`entityType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`action`)) {
          sprintf(
            '"action":
            "%s"
                  ',
            self$`action`
          )
        },
        if (!is.null(self$`autoApply`)) {
          sprintf(
            '"autoApply":
            %s
                  ',
            tolower(self$`autoApply`)
          )
        },
        if (!is.null(self$`change`)) {
          sprintf(
            '"change":
            %s
      ',
            jsonlite::toJSON(self$`change`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`comment`)) {
          sprintf(
            '"comment":
            "%s"
                  ',
            self$`comment`
          )
        },
        if (!is.null(self$`entityId`)) {
          sprintf(
            '"entityId":
            "%s"
                  ',
            self$`entityId`
          )
        },
        if (!is.null(self$`entityType`)) {
          sprintf(
            '"entityType":
            "%s"
                  ',
            self$`entityType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ChangeRequestCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ChangeRequestCreateJson, validateParams = FALSE) {
      ChangeRequestCreateObject <- jsonlite::fromJSON(ChangeRequestCreateJson)
      self$`action` <- ChangeRequestCreateObject$`action`
      self$`autoApply` <- ChangeRequestCreateObject$`autoApply`
      self$`change` <- DeploymentApproveChange$new()$fromJSON(jsonlite::toJSON(ChangeRequestCreateObject$change, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`comment` <- ChangeRequestCreateObject$`comment`
      self$`entityId` <- ChangeRequestCreateObject$`entityId`
      self$`entityType` <- ChangeRequestCreateObject$`entityType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
