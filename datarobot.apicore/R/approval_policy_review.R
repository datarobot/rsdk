# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApprovalPolicyReview
#'
#' @description ApprovalPolicyReview Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field groups  list( \link{PolicyUserGroup} ) [optional] A list of user groups that will be added as required reviewers on Change Requests for the entities that match the policy.
#'
#' @field reminderPeriod  character [optional] Duration period in ISO 8601 format that indicates when to send a reminder for reviewing a Change Request after its creation or last reminder if it hasn&#39;t been approved yet. If &#x60;&#x60;null&#x60;&#x60;, no review reminders are sent to the reviewers.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApprovalPolicyReview <- R6::R6Class(
  "ApprovalPolicyReview",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(groups = NULL, reminderPeriod = NULL) {
      if (!is.null(`groups`)) {
        stopifnot(is.vector(`groups`))
      }
      if (!is.null(`reminderPeriod`)) {
        stopifnot(is.character(`reminderPeriod`), length(`reminderPeriod`) == 1)
      }
    }
  ),
  public = list(
    `groups` = NULL,
    `reminderPeriod` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param groups A list of user groups that will be added as required reviewers on Change Requests for the entities that match the policy.
    #' @param reminderPeriod Duration period in ISO 8601 format that indicates when to send a reminder for reviewing a Change Request after its creation or last reminder if it hasn&#39;t been approved yet. If &#x60;&#x60;null&#x60;&#x60;, no review reminders are sent to the reviewers.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`groups` = NULL, `reminderPeriod` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(groups, reminderPeriod)
      }
      sapply(`groups`, function(x) stopifnot(R6::is.R6(x)))
      self$`reminderPeriod` <- `reminderPeriod`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(groups = self$`groups`, reminderPeriod = self$`reminderPeriod`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`groups`)) {
          sprintf(
            '"groups":
            [%s]
',
            paste(sapply(self$`groups`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`reminderPeriod`)) {
          sprintf(
            '"reminderPeriod":
            "%s"
                  ',
            self$`reminderPeriod`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApprovalPolicyReviewJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApprovalPolicyReviewJson, validateParams = FALSE) {
      ApprovalPolicyReviewObject <- jsonlite::fromJSON(ApprovalPolicyReviewJson)
      self$`groups` <- ApiClient$new()$deserializeObj(ApprovalPolicyReviewObject$`groups`, "array[PolicyUserGroup]", loadNamespace("datarobot.apicore"))
      self$`reminderPeriod` <- ApprovalPolicyReviewObject$`reminderPeriod`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
