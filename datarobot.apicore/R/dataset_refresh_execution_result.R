# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetRefreshExecutionResult
#'
#' @description DatasetRefreshExecutionResult Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field completedAt  character UTC completion date, in RFC-3339 format.
#'
#' @field datasetId  character Dataset ID associated with this result.
#'
#' @field datasetVersionId  character Dataset version ID associated with this result
#'
#' @field executionId  character Result ID.
#'
#' @field jobId  character Job ID associated with this result.
#'
#' @field message  character Current status of execution.
#'
#' @field startedAt  character UTC start date, in RFC-3339 format.
#'
#' @field status  character Status of this dataset refresh.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetRefreshExecutionResult <- R6::R6Class(
  "DatasetRefreshExecutionResult",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(completedAt = NULL, datasetId = NULL, datasetVersionId = NULL, executionId = NULL, jobId = NULL, message = NULL, startedAt = NULL, status = NULL) {
      if (!is.null(`completedAt`)) {
        stopifnot(inherits(`completedAt`, "POSIXt"))
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`datasetVersionId`)) {
        stopifnot(is.character(`datasetVersionId`), length(`datasetVersionId`) == 1)
      }
      if (!is.null(`executionId`)) {
        stopifnot(is.character(`executionId`), length(`executionId`) == 1)
      }
      if (!is.null(`jobId`)) {
        stopifnot(is.character(`jobId`), length(`jobId`) == 1)
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`startedAt`)) {
        stopifnot(inherits(`startedAt`, "POSIXt"))
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
    }
  ),
  public = list(
    `completedAt` = NULL,
    `datasetId` = NULL,
    `datasetVersionId` = NULL,
    `executionId` = NULL,
    `jobId` = NULL,
    `message` = NULL,
    `startedAt` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param completedAt UTC completion date, in RFC-3339 format.
    #' @param datasetId Dataset ID associated with this result.
    #' @param datasetVersionId Dataset version ID associated with this result
    #' @param executionId Result ID.
    #' @param jobId Job ID associated with this result.
    #' @param message Current status of execution.
    #' @param startedAt UTC start date, in RFC-3339 format.
    #' @param status Status of this dataset refresh.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`completedAt` = NULL, `datasetId` = NULL, `datasetVersionId` = NULL, `executionId` = NULL, `jobId` = NULL, `message` = NULL, `startedAt` = NULL, `status` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`completedAt`, `datasetId`, `datasetVersionId`, `executionId`, `jobId`, `message`, `startedAt`, `status`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(completedAt, datasetId, datasetVersionId, executionId, jobId, message, startedAt, status)
      }
      self$`completedAt` <- `completedAt`
      self$`datasetId` <- `datasetId`
      self$`datasetVersionId` <- `datasetVersionId`
      self$`executionId` <- `executionId`
      self$`jobId` <- `jobId`
      self$`message` <- `message`
      self$`startedAt` <- `startedAt`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(completedAt = self$`completedAt`, datasetId = self$`datasetId`, datasetVersionId = self$`datasetVersionId`, executionId = self$`executionId`, jobId = self$`jobId`, message = self$`message`, startedAt = self$`startedAt`, status = self$`status`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`completedAt`)) {
          sprintf(
            '"completedAt":
            "%s"
                  ',
            format(self$`completedAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetVersionId`)) {
          sprintf(
            '"datasetVersionId":
            "%s"
                  ',
            self$`datasetVersionId`
          )
        },
        if (!is.null(self$`executionId`)) {
          sprintf(
            '"executionId":
            "%s"
                  ',
            self$`executionId`
          )
        },
        if (!is.null(self$`jobId`)) {
          sprintf(
            '"jobId":
            "%s"
                  ',
            self$`jobId`
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`startedAt`)) {
          sprintf(
            '"startedAt":
            "%s"
                  ',
            format(self$`startedAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetRefreshExecutionResultJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetRefreshExecutionResultJson, validateParams = FALSE) {
      DatasetRefreshExecutionResultObject <- jsonlite::fromJSON(DatasetRefreshExecutionResultJson)
      self$`completedAt` <- ParseRFC3339Timestamp(DatasetRefreshExecutionResultObject$`completedAt`)
      self$`datasetId` <- DatasetRefreshExecutionResultObject$`datasetId`
      self$`datasetVersionId` <- DatasetRefreshExecutionResultObject$`datasetVersionId`
      self$`executionId` <- DatasetRefreshExecutionResultObject$`executionId`
      self$`jobId` <- DatasetRefreshExecutionResultObject$`jobId`
      self$`message` <- DatasetRefreshExecutionResultObject$`message`
      self$`startedAt` <- ParseRFC3339Timestamp(DatasetRefreshExecutionResultObject$`startedAt`)
      self$`status` <- DatasetRefreshExecutionResultObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
