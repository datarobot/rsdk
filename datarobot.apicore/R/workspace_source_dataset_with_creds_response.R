# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title WorkspaceSourceDatasetWithCredsResponse
#'
#' @description WorkspaceSourceDatasetWithCredsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field alias  character Alias to be used as the table name.
#'
#' @field datasetId  character ID of a dataset in the catalog.
#'
#' @field datasetVersionId  character ID of a dataset version in the catalog.
#'
#' @field needsCredentials  character Whether a user must provide credentials for source datasets.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
WorkspaceSourceDatasetWithCredsResponse <- R6::R6Class(
  "WorkspaceSourceDatasetWithCredsResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`alias` = NULL, `datasetId` = NULL, `datasetVersionId` = NULL, `needsCredentials` = NULL) {
      if (!is.null(`alias`)) {
        stopifnot(is.character(`alias`), length(`alias`) == 1)
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`datasetVersionId`)) {
        stopifnot(is.character(`datasetVersionId`), length(`datasetVersionId`) == 1)
      }
      if (!is.null(`needsCredentials`)) {
        stopifnot(is.logical(`needsCredentials`), length(`needsCredentials`) == 1)
      }
    }
  ),
  public = list(
    `alias` = NULL,
    `datasetId` = NULL,
    `datasetVersionId` = NULL,
    `needsCredentials` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param alias Alias to be used as the table name.
    #' @param datasetId ID of a dataset in the catalog.
    #' @param datasetVersionId ID of a dataset version in the catalog.
    #' @param needsCredentials Whether a user must provide credentials for source datasets.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`alias` = NULL, `datasetId` = NULL, `datasetVersionId` = NULL, `needsCredentials` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`alias`, `datasetId`, `datasetVersionId`, `needsCredentials`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(alias, datasetId, datasetVersionId, needsCredentials)
      }
      self$`alias` <- `alias`
      self$`datasetId` <- `datasetId`
      self$`datasetVersionId` <- `datasetVersionId`
      self$`needsCredentials` <- `needsCredentials`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(alias = self$`alias`, datasetId = self$`datasetId`, datasetVersionId = self$`datasetVersionId`, needsCredentials = self$`needsCredentials`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`alias`)) {
          sprintf(
            '"alias":
            "%s"
                  ',
            self$`alias`
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetVersionId`)) {
          sprintf(
            '"datasetVersionId":
            "%s"
                  ',
            self$`datasetVersionId`
          )
        },
        if (!is.null(self$`needsCredentials`)) {
          sprintf(
            '"needsCredentials":
            %s
                  ',
            tolower(self$`needsCredentials`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param WorkspaceSourceDatasetWithCredsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(WorkspaceSourceDatasetWithCredsResponseJson, validateParams = FALSE) {
      WorkspaceSourceDatasetWithCredsResponseObject <- jsonlite::fromJSON(WorkspaceSourceDatasetWithCredsResponseJson)
      self$`alias` <- WorkspaceSourceDatasetWithCredsResponseObject$`alias`
      self$`datasetId` <- WorkspaceSourceDatasetWithCredsResponseObject$`datasetId`
      self$`datasetVersionId` <- WorkspaceSourceDatasetWithCredsResponseObject$`datasetVersionId`
      self$`needsCredentials` <- WorkspaceSourceDatasetWithCredsResponseObject$`needsCredentials`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
