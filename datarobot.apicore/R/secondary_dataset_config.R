# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SecondaryDatasetConfig
#'
#' @description SecondaryDatasetConfig Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featureEngineeringGraphId  character Id of the feature engineering graph
#'
#' @field secondaryDatasets  list( \link{SecondaryDataset} ) list of secondary datasets used by the feature engineering graph
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SecondaryDatasetConfig <- R6::R6Class(
  "SecondaryDatasetConfig",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(featureEngineeringGraphId = NULL, secondaryDatasets = NULL) {
      if (!is.null(`featureEngineeringGraphId`)) {
        stopifnot(is.character(`featureEngineeringGraphId`), length(`featureEngineeringGraphId`) == 1)
      }
      if (!is.null(`secondaryDatasets`)) {
        stopifnot(is.vector(`secondaryDatasets`))
      }
    }
  ),
  public = list(
    `featureEngineeringGraphId` = NULL,
    `secondaryDatasets` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featureEngineeringGraphId Id of the feature engineering graph
    #' @param secondaryDatasets list of secondary datasets used by the feature engineering graph
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featureEngineeringGraphId` = NULL, `secondaryDatasets` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`featureEngineeringGraphId`, `secondaryDatasets`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(featureEngineeringGraphId, secondaryDatasets)
      }
      self$`featureEngineeringGraphId` <- `featureEngineeringGraphId`
      sapply(`secondaryDatasets`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(featureEngineeringGraphId = self$`featureEngineeringGraphId`, secondaryDatasets = self$`secondaryDatasets`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featureEngineeringGraphId`)) {
          sprintf(
            '"featureEngineeringGraphId":
            "%s"
                  ',
            self$`featureEngineeringGraphId`
          )
        },
        if (!is.null(self$`secondaryDatasets`)) {
          sprintf(
            '"secondaryDatasets":
            [%s]
',
            paste(sapply(self$`secondaryDatasets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SecondaryDatasetConfigJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SecondaryDatasetConfigJson, validateParams = FALSE) {
      SecondaryDatasetConfigObject <- jsonlite::fromJSON(SecondaryDatasetConfigJson)
      self$`featureEngineeringGraphId` <- SecondaryDatasetConfigObject$`featureEngineeringGraphId`
      self$`secondaryDatasets` <- ApiClient$new()$deserializeObj(SecondaryDatasetConfigObject$`secondaryDatasets`, "array[SecondaryDataset]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
