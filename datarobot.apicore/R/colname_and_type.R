# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ColnameAndType
#'
#' @description ColnameAndType Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field colname  character The column name.
#'
#' @field hex  character A safe hex representation of the column name.
#'
#' @field type  character The data type of the column.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ColnameAndType <- R6::R6Class(
  "ColnameAndType",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(colname = NULL, hex = NULL, type = NULL) {
      if (!is.null(`colname`)) {
        stopifnot(is.character(`colname`), length(`colname`) == 1)
      }
      if (!is.null(`hex`)) {
        stopifnot(is.character(`hex`), length(`hex`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
    }
  ),
  public = list(
    `colname` = NULL,
    `hex` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param colname The column name.
    #' @param hex A safe hex representation of the column name.
    #' @param type The data type of the column.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`colname` = NULL, `hex` = NULL, `type` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`colname`, `hex`, `type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(colname, hex, type)
      }
      self$`colname` <- `colname`
      self$`hex` <- `hex`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(colname = self$`colname`, hex = self$`hex`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`colname`)) {
          sprintf(
            '"colname":
            "%s"
                  ',
            self$`colname`
          )
        },
        if (!is.null(self$`hex`)) {
          sprintf(
            '"hex":
            "%s"
                  ',
            self$`hex`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ColnameAndTypeJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ColnameAndTypeJson, validateParams = FALSE) {
      ColnameAndTypeObject <- jsonlite::fromJSON(ColnameAndTypeJson)
      self$`colname` <- ColnameAndTypeObject$`colname`
      self$`hex` <- ColnameAndTypeObject$`hex`
      self$`type` <- ColnameAndTypeObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
