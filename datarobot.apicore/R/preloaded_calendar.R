# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PreloadedCalendar
#'
#' @description PreloadedCalendar Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field countryCode  character Code of the country for which holidays should be generated. Code needs to be uppercase and should belong to the list of countries which can be retrieved via :http:get:&#x60;/api/v2/calendarCountryCodes/&#x60;
#'
#' @field endDate  character Last date of the range of dates for which holidays are generated.
#'
#' @field startDate  character First date of the range of dates for which holidays are generated.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PreloadedCalendar <- R6::R6Class(
  "PreloadedCalendar",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(countryCode = NULL, endDate = NULL, startDate = NULL) {
      if (!is.null(`countryCode`)) {
        stopifnot(is.character(`countryCode`), length(`countryCode`) == 1)
      }
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `countryCode` = NULL,
    `endDate` = NULL,
    `startDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param countryCode Code of the country for which holidays should be generated. Code needs to be uppercase and should belong to the list of countries which can be retrieved via :http:get:&#x60;/api/v2/calendarCountryCodes/&#x60;
    #' @param endDate Last date of the range of dates for which holidays are generated.
    #' @param startDate First date of the range of dates for which holidays are generated.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`countryCode` = NULL, `endDate` = NULL, `startDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`countryCode`, `endDate`, `startDate`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(countryCode, endDate, startDate)
      }
      self$`countryCode` <- `countryCode`
      self$`endDate` <- `endDate`
      self$`startDate` <- `startDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(countryCode = self$`countryCode`, endDate = self$`endDate`, startDate = self$`startDate`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`countryCode`)) {
          sprintf(
            '"countryCode":
            "%s"
                  ',
            self$`countryCode`
          )
        },
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PreloadedCalendarJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PreloadedCalendarJson, validateParams = FALSE) {
      PreloadedCalendarObject <- jsonlite::fromJSON(PreloadedCalendarJson)
      self$`countryCode` <- PreloadedCalendarObject$`countryCode`
      self$`endDate` <- ParseRFC3339Timestamp(PreloadedCalendarObject$`endDate`)
      self$`startDate` <- ParseRFC3339Timestamp(PreloadedCalendarObject$`startDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
