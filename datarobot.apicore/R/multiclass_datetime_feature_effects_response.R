# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MulticlassDatetimeFeatureEffectsResponse
#'
#' @description MulticlassDatetimeFeatureEffectsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtestIndex  character The backtest index. For example: &#x60;0&#x60;, &#x60;1&#x60;, ..., &#x60;20&#x60;, &#x60;holdout&#x60;, &#x60;startstop&#x60;.
#'
#' @field count  integer [optional] Number of items returned on this page.
#'
#' @field data  list( \link{MulticlassFeatureEffects} ) A list of feature effects scores for each class in multiclass project.
#'
#' @field modelId  character The model ID
#'
#' @field next_  character URL pointing to the next page (if null, there is no next page).
#'
#' @field previous  character URL pointing to the previous page (if null, there is no previous page).
#'
#' @field projectId  character The project ID
#'
#' @field source  character Models data source.
#'
#' @field totalCount  integer The total number of items across all pages.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MulticlassDatetimeFeatureEffectsResponse <- R6::R6Class(
  "MulticlassDatetimeFeatureEffectsResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`backtestIndex` = NULL, `count` = NULL, `data` = NULL, `modelId` = NULL, `next_` = NULL, `previous` = NULL, `projectId` = NULL, `source` = NULL, `totalCount` = NULL) {
      if (!is.null(`backtestIndex`)) {
        stopifnot(is.character(`backtestIndex`), length(`backtestIndex`) == 1)
      }
      if (!is.null(`data`)) {
        stopifnot(is.vector(`data`), sapply(`data`, R6::is.R6))
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
      if (!is.null(`totalCount`)) {
        stopifnot(is.numeric(`totalCount`), length(`totalCount`) == 1)
      }
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
    }
  ),
  public = list(
    `backtestIndex` = NULL,
    `count` = NULL,
    `data` = NULL,
    `modelId` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    `projectId` = NULL,
    `source` = NULL,
    `totalCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtestIndex The backtest index. For example: &#x60;0&#x60;, &#x60;1&#x60;, ..., &#x60;20&#x60;, &#x60;holdout&#x60;, &#x60;startstop&#x60;.
    #' @param count Number of items returned on this page.
    #' @param data A list of feature effects scores for each class in multiclass project.
    #' @param modelId The model ID
    #' @param next_ URL pointing to the next page (if null, there is no next page).
    #' @param previous URL pointing to the previous page (if null, there is no previous page).
    #' @param projectId The project ID
    #' @param source Models data source.
    #' @param totalCount The total number of items across all pages.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`backtestIndex` = NULL, `data` = NULL, `modelId` = NULL, `next_` = NULL, `previous` = NULL, `projectId` = NULL, `source` = NULL, `totalCount` = NULL, `count` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`backtestIndex`, `data`, `modelId`, `next_`, `previous`, `projectId`, `source`, `totalCount`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(backtestIndex, count, data, modelId, next_, previous, projectId, source, totalCount)
      }
      self$`backtestIndex` <- `backtestIndex`
      self$`count` <- `count`
      self$`data` <- `data`
      self$`modelId` <- `modelId`
      self$`next_` <- `next_`
      self$`previous` <- `previous`
      self$`projectId` <- `projectId`
      self$`source` <- `source`
      self$`totalCount` <- `totalCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(backtestIndex = self$`backtestIndex`, count = self$`count`, data = self$`data`, modelId = self$`modelId`, next_ = self$`next_`, previous = self$`previous`, projectId = self$`projectId`, source = self$`source`, totalCount = self$`totalCount`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtestIndex`)) {
          sprintf(
            '"backtestIndex":
            "%s"
                  ',
            self$`backtestIndex`
          )
        },
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
      ',
            paste(sapply(self$`data`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        },
        if (!is.null(self$`totalCount`)) {
          sprintf(
            '"totalCount":
            %d
                  ',
            self$`totalCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MulticlassDatetimeFeatureEffectsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MulticlassDatetimeFeatureEffectsResponseJson, validateParams = FALSE) {
      MulticlassDatetimeFeatureEffectsResponseObject <- jsonlite::fromJSON(MulticlassDatetimeFeatureEffectsResponseJson)
      self$`backtestIndex` <- MulticlassDatetimeFeatureEffectsResponseObject$`backtestIndex`
      self$`count` <- MulticlassDatetimeFeatureEffectsResponseObject$`count`
      self$`data` <- ApiClient$new()$deserializeObj(MulticlassDatetimeFeatureEffectsResponseObject$`data`, "array[MulticlassFeatureEffects]", loadNamespace("datarobot.apicore"))
      self$`modelId` <- MulticlassDatetimeFeatureEffectsResponseObject$`modelId`
      self$`next_` <- MulticlassDatetimeFeatureEffectsResponseObject$`next`
      self$`previous` <- MulticlassDatetimeFeatureEffectsResponseObject$`previous`
      self$`projectId` <- MulticlassDatetimeFeatureEffectsResponseObject$`projectId`
      self$`source` <- MulticlassDatetimeFeatureEffectsResponseObject$`source`
      self$`totalCount` <- MulticlassDatetimeFeatureEffectsResponseObject$`totalCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
