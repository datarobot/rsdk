# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataDisparityCreatePayload
#'
#' @description DataDisparityCreatePayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field comparedClassNames  list( character ) An array of classes to calculate data disparity for.
#'
#' @field feature  character Feature for which insight is computed.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataDisparityCreatePayload <- R6::R6Class(
  "DataDisparityCreatePayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(comparedClassNames = NULL, feature = NULL) {
      if (!is.null(`comparedClassNames`)) {
        stopifnot(is.vector(`comparedClassNames`))
      }
      if (!is.null(`feature`)) {
        stopifnot(is.character(`feature`), length(`feature`) == 1)
      }
    }
  ),
  public = list(
    `comparedClassNames` = NULL,
    `feature` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param comparedClassNames An array of classes to calculate data disparity for.
    #' @param feature Feature for which insight is computed.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`comparedClassNames` = NULL, `feature` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`comparedClassNames`, `feature`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(comparedClassNames, feature)
      }
      sapply(`comparedClassNames`, function(x) stopifnot(is.character(x)))
      self$`feature` <- `feature`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(comparedClassNames = self$`comparedClassNames`, feature = self$`feature`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`comparedClassNames`)) {
          sprintf(
            '"comparedClassNames":
              [%s]
            ',
            paste(unlist(lapply(self$`comparedClassNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`feature`)) {
          sprintf(
            '"feature":
            "%s"
                  ',
            self$`feature`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataDisparityCreatePayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataDisparityCreatePayloadJson, validateParams = FALSE) {
      DataDisparityCreatePayloadObject <- jsonlite::fromJSON(DataDisparityCreatePayloadJson)
      self$`comparedClassNames` <- ApiClient$new()$deserializeObj(DataDisparityCreatePayloadObject$`comparedClassNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`feature` <- DataDisparityCreatePayloadObject$`feature`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
