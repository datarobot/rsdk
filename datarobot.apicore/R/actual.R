# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Actual
#'
#' @description Actual Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualValue  \link{AnyOfintegernumberstring} The actual value of a prediction. Will be numeric for regression models and a string label for classification models.
#'
#' @field associationId  character A unique identifier used with a predicted row.
#'
#' @field timestamp  character [optional] The datetime when actual values were obtained, formatted according to RFC3339.
#'
#' @field wasActedOn  character [optional] Indicates if the prediction was acted on in a way that could have affected the actual outcome. For example, if a hospital patient is predicted to be readmitted in 30 days, extra procedures or new medication might be given to mitigate this problem, influencing the actual outcome of the prediction.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Actual <- R6::R6Class(
  "Actual",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`actualValue` = NULL, `associationId` = NULL, `timestamp` = NULL, `wasActedOn` = NULL) {
      if (!is.null(`actualValue`)) {
        .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = actualValue)
      }
      if (!is.null(`associationId`)) {
        stopifnot(is.character(`associationId`), length(`associationId`) == 1)
      }
      if (!is.null(`timestamp`)) {
        stopifnot(inherits(`timestamp`, "POSIXt"))
      }
      if (!is.null(`wasActedOn`)) {
        stopifnot(is.logical(`wasActedOn`), length(`wasActedOn`) == 1)
      }
    }
  ),
  public = list(
    `actualValue` = NULL,
    `associationId` = NULL,
    `timestamp` = NULL,
    `wasActedOn` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualValue The actual value of a prediction. Will be numeric for regression models and a string label for classification models.
    #' @param associationId A unique identifier used with a predicted row.
    #' @param timestamp The datetime when actual values were obtained, formatted according to RFC3339.
    #' @param wasActedOn Indicates if the prediction was acted on in a way that could have affected the actual outcome. For example, if a hospital patient is predicted to be readmitted in 30 days, extra procedures or new medication might be given to mitigate this problem, influencing the actual outcome of the prediction.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actualValue` = NULL, `associationId` = NULL, `timestamp` = NULL, `wasActedOn` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`actualValue`, `associationId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(actualValue, associationId, timestamp, wasActedOn)
      }
      self$`actualValue` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = actualValue)
      self$`associationId` <- `associationId`
      self$`timestamp` <- `timestamp`
      self$`wasActedOn` <- `wasActedOn`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(actualValue = self$`actualValue`, associationId = self$`associationId`, timestamp = self$`timestamp`, wasActedOn = self$`wasActedOn`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualValue`)) {
          sprintf(
            '"actualValue":
            %s
      ',
            self$`actualValue`
          )
        },
        if (!is.null(self$`associationId`)) {
          sprintf(
            '"associationId":
            "%s"
                  ',
            self$`associationId`
          )
        },
        if (!is.null(self$`timestamp`)) {
          sprintf(
            '"timestamp":
            "%s"
                  ',
            format(self$`timestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`wasActedOn`)) {
          sprintf(
            '"wasActedOn":
            %s
                  ',
            tolower(self$`wasActedOn`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ActualJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ActualJson, validateParams = FALSE) {
      ActualObject <- jsonlite::fromJSON(ActualJson)
      self$`actualValue` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = ActualObject$actualValue)
      self$`associationId` <- ActualObject$`associationId`
      self$`timestamp` <- ParseRFC3339Timestamp(ActualObject$`timestamp`)
      self$`wasActedOn` <- ActualObject$`wasActedOn`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
