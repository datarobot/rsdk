# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseAttachmentApplicationOverview
#'
#' @description UseCaseAttachmentApplicationOverview Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field appType  character Type of the application
#'
#' @field createdAt  character Time when application was created
#'
#' @field lastModifiedAt  character Time of the last modification
#'
#' @field name  character Name of the application
#'
#' @field version  character Version string of the app
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseAttachmentApplicationOverview <- R6::R6Class(
  "UseCaseAttachmentApplicationOverview",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`appType` = NULL, `createdAt` = NULL, `lastModifiedAt` = NULL, `name` = NULL, `version` = NULL) {
      if (!is.null(`appType`)) {
        stopifnot(is.character(`appType`), length(`appType`) == 1)
      }
      if (!is.null(`createdAt`)) {
        stopifnot(inherits(`createdAt`, "POSIXt"))
      }
      if (!is.null(`lastModifiedAt`)) {
        stopifnot(inherits(`lastModifiedAt`, "POSIXt"))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`version`)) {
        stopifnot(is.character(`version`), length(`version`) == 1)
      }
    }
  ),
  public = list(
    `appType` = NULL,
    `createdAt` = NULL,
    `lastModifiedAt` = NULL,
    `name` = NULL,
    `version` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param appType Type of the application
    #' @param createdAt Time when application was created
    #' @param lastModifiedAt Time of the last modification
    #' @param name Name of the application
    #' @param version Version string of the app
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`appType` = NULL, `createdAt` = NULL, `lastModifiedAt` = NULL, `name` = NULL, `version` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`appType`, `createdAt`, `lastModifiedAt`, `name`, `version`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(appType, createdAt, lastModifiedAt, name, version)
      }
      self$`appType` <- `appType`
      self$`createdAt` <- `createdAt`
      self$`lastModifiedAt` <- `lastModifiedAt`
      self$`name` <- `name`
      self$`version` <- `version`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(appType = self$`appType`, createdAt = self$`createdAt`, lastModifiedAt = self$`lastModifiedAt`, name = self$`name`, version = self$`version`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`appType`)) {
          sprintf(
            '"appType":
            "%s"
                  ',
            self$`appType`
          )
        },
        if (!is.null(self$`createdAt`)) {
          sprintf(
            '"createdAt":
            "%s"
                  ',
            format(self$`createdAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`lastModifiedAt`)) {
          sprintf(
            '"lastModifiedAt":
            "%s"
                  ',
            format(self$`lastModifiedAt`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`version`)) {
          sprintf(
            '"version":
            "%s"
                  ',
            self$`version`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseAttachmentApplicationOverviewJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseAttachmentApplicationOverviewJson, validateParams = FALSE) {
      UseCaseAttachmentApplicationOverviewObject <- jsonlite::fromJSON(UseCaseAttachmentApplicationOverviewJson)
      self$`appType` <- UseCaseAttachmentApplicationOverviewObject$`appType`
      self$`createdAt` <- ParseRFC3339Timestamp(UseCaseAttachmentApplicationOverviewObject$`createdAt`)
      self$`lastModifiedAt` <- ParseRFC3339Timestamp(UseCaseAttachmentApplicationOverviewObject$`lastModifiedAt`)
      self$`name` <- UseCaseAttachmentApplicationOverviewObject$`name`
      self$`version` <- UseCaseAttachmentApplicationOverviewObject$`version`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
