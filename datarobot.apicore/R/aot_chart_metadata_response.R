# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AOTChartMetadataResponse
#'
#' @description AOTChartMetadataResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetId  character The dataset id which was used to compute the AOT chart.
#'
#' @field datasetMetadata  \link{AOTChartMetadataDatasetMetadata}
#'
#' @field frequencyType  character How to interpret the frequency attribute of each datetimeTrendBin. One of [&#39;rowCount&#39;, &#39;weightedRowCount&#39;, &#39;exposure&#39;, &#39;weightedExposure&#39;].
#'
#' @field metricName  character The metric used to score each bin and the calculate the metric attribute of each datetimeTrendBin.
#'
#' @field modelId  character The model id which was used to compute the AOT chart.
#'
#' @field projectId  character The project id which was used to compute the AOT chart.
#'
#' @field resolutions  list( character ) Suggested time resolutions where a resolution is one of [&#39;milliseconds&#39;, &#39;seconds&#39;, &#39;minutes&#39;, &#39;hours&#39;, &#39;days&#39;, &#39;weeks&#39;, &#39;months&#39;, &#39;years&#39;].
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AOTChartMetadataResponse <- R6::R6Class(
  "AOTChartMetadataResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datasetId` = NULL, `datasetMetadata` = NULL, `frequencyType` = NULL, `metricName` = NULL, `modelId` = NULL, `projectId` = NULL, `resolutions` = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`datasetMetadata`)) {
        stopifnot(R6::is.R6(`datasetMetadata`))
      }
      if (!is.null(`frequencyType`)) {
        stopifnot(is.character(`frequencyType`), length(`frequencyType`) == 1)
      }
      if (!is.null(`metricName`)) {
        stopifnot(is.character(`metricName`), length(`metricName`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`resolutions`)) {
        stopifnot(is.vector(`resolutions`))
      }
    }
  ),
  public = list(
    `datasetId` = NULL,
    `datasetMetadata` = NULL,
    `frequencyType` = NULL,
    `metricName` = NULL,
    `modelId` = NULL,
    `projectId` = NULL,
    `resolutions` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetId The dataset id which was used to compute the AOT chart.
    #' @param datasetMetadata
    #' @param frequencyType How to interpret the frequency attribute of each datetimeTrendBin. One of [&#39;rowCount&#39;, &#39;weightedRowCount&#39;, &#39;exposure&#39;, &#39;weightedExposure&#39;].
    #' @param metricName The metric used to score each bin and the calculate the metric attribute of each datetimeTrendBin.
    #' @param modelId The model id which was used to compute the AOT chart.
    #' @param projectId The project id which was used to compute the AOT chart.
    #' @param resolutions Suggested time resolutions where a resolution is one of [&#39;milliseconds&#39;, &#39;seconds&#39;, &#39;minutes&#39;, &#39;hours&#39;, &#39;days&#39;, &#39;weeks&#39;, &#39;months&#39;, &#39;years&#39;].
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `datasetMetadata` = NULL, `frequencyType` = NULL, `metricName` = NULL, `modelId` = NULL, `projectId` = NULL, `resolutions` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datasetId`, `datasetMetadata`, `frequencyType`, `metricName`, `modelId`, `projectId`, `resolutions`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datasetId, datasetMetadata, frequencyType, metricName, modelId, projectId, resolutions)
      }
      self$`datasetId` <- `datasetId`
      self$`datasetMetadata` <- `datasetMetadata`
      self$`frequencyType` <- `frequencyType`
      self$`metricName` <- `metricName`
      self$`modelId` <- `modelId`
      self$`projectId` <- `projectId`
      sapply(`resolutions`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datasetId = self$`datasetId`, datasetMetadata = self$`datasetMetadata`, frequencyType = self$`frequencyType`, metricName = self$`metricName`, modelId = self$`modelId`, projectId = self$`projectId`, resolutions = self$`resolutions`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetMetadata`)) {
          sprintf(
            '"datasetMetadata":
            %s
      ',
            jsonlite::toJSON(self$`datasetMetadata`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`frequencyType`)) {
          sprintf(
            '"frequencyType":
            "%s"
                  ',
            self$`frequencyType`
          )
        },
        if (!is.null(self$`metricName`)) {
          sprintf(
            '"metricName":
            "%s"
                  ',
            self$`metricName`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`resolutions`)) {
          sprintf(
            '"resolutions":
            [%s]
                  ',
            paste(unlist(lapply(self$`resolutions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AOTChartMetadataResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AOTChartMetadataResponseJson, validateParams = FALSE) {
      AOTChartMetadataResponseObject <- jsonlite::fromJSON(AOTChartMetadataResponseJson)
      self$`datasetId` <- AOTChartMetadataResponseObject$`datasetId`
      self$`datasetMetadata` <- AOTChartMetadataDatasetMetadata$new()$fromJSON(jsonlite::toJSON(AOTChartMetadataResponseObject$datasetMetadata, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`frequencyType` <- AOTChartMetadataResponseObject$`frequencyType`
      self$`metricName` <- AOTChartMetadataResponseObject$`metricName`
      self$`modelId` <- AOTChartMetadataResponseObject$`modelId`
      self$`projectId` <- AOTChartMetadataResponseObject$`projectId`
      self$`resolutions` <- ApiClient$new()$deserializeObj(AOTChartMetadataResponseObject$`resolutions`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
