# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ShapImpactsResponse
#'
#' @description ShapImpactsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featureName  character The feature name in dataset
#'
#' @field impactNormalized  numeric The normalized impact score value (largest value is 1)
#'
#' @field impactUnnormalized  numeric The raw impact score value
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShapImpactsResponse <- R6::R6Class(
  "ShapImpactsResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`featureName` = NULL, `impactNormalized` = NULL, `impactUnnormalized` = NULL) {
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`impactNormalized`)) {
      }
      if (!is.null(`impactUnnormalized`)) {
      }
    }
  ),
  public = list(
    `featureName` = NULL,
    `impactNormalized` = NULL,
    `impactUnnormalized` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featureName The feature name in dataset
    #' @param impactNormalized The normalized impact score value (largest value is 1)
    #' @param impactUnnormalized The raw impact score value
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featureName` = NULL, `impactNormalized` = NULL, `impactUnnormalized` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`featureName`, `impactNormalized`, `impactUnnormalized`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(featureName, impactNormalized, impactUnnormalized)
      }
      self$`featureName` <- `featureName`
      self$`impactNormalized` <- `impactNormalized`
      self$`impactUnnormalized` <- `impactUnnormalized`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(featureName = self$`featureName`, impactNormalized = self$`impactNormalized`, impactUnnormalized = self$`impactUnnormalized`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`impactNormalized`)) {
          sprintf(
            '"impactNormalized":
            %d
                  ',
            self$`impactNormalized`
          )
        },
        if (!is.null(self$`impactUnnormalized`)) {
          sprintf(
            '"impactUnnormalized":
            %d
                  ',
            self$`impactUnnormalized`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ShapImpactsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ShapImpactsResponseJson, validateParams = FALSE) {
      ShapImpactsResponseObject <- jsonlite::fromJSON(ShapImpactsResponseJson)
      self$`featureName` <- ShapImpactsResponseObject$`featureName`
      self$`impactNormalized` <- ShapImpactsResponseObject$`impactNormalized`
      self$`impactUnnormalized` <- ShapImpactsResponseObject$`impactUnnormalized`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
