# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsJdbcPredictionTargetPayload
#'
#' @description CredentialsJdbcPredictionTargetPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialId  character Id of the stored credentials
#'
#' @field datasourceId  character Id of the DSS data source object
#'
#' @field typeId  character For JDBC should be set to &#x60;jdbcDatasource&#x60;
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsJdbcPredictionTargetPayload <- R6::R6Class(
  "CredentialsJdbcPredictionTargetPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(credentialId = NULL, datasourceId = NULL, typeId = NULL) {
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`datasourceId`)) {
        stopifnot(is.character(`datasourceId`), length(`datasourceId`) == 1)
      }
      if (!is.null(`typeId`)) {
        stopifnot(is.character(`typeId`), length(`typeId`) == 1)
      }
    }
  ),
  public = list(
    `credentialId` = NULL,
    `datasourceId` = NULL,
    `typeId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialId Id of the stored credentials
    #' @param datasourceId Id of the DSS data source object
    #' @param typeId For JDBC should be set to &#x60;jdbcDatasource&#x60;
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `credentialId` = NULL, `datasourceId` = NULL, `typeId` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`credentialId`, `datasourceId`, `typeId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(credentialId, datasourceId, typeId)
      }
      self$`credentialId` <- `credentialId`
      self$`datasourceId` <- `datasourceId`
      self$`typeId` <- `typeId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(credentialId = self$`credentialId`, datasourceId = self$`datasourceId`, typeId = self$`typeId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialId`)) {
        sprintf(
        '"credentialId":
          "%s"
                ',
        self$`credentialId`
        )},
        if (!is.null(self$`datasourceId`)) {
        sprintf(
        '"datasourceId":
          "%s"
                ',
        self$`datasourceId`
        )},
        if (!is.null(self$`typeId`)) {
        sprintf(
        '"typeId":
          "%s"
                ',
        self$`typeId`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsJdbcPredictionTargetPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsJdbcPredictionTargetPayloadJson, validateParams = FALSE) {
      CredentialsJdbcPredictionTargetPayloadObject <- jsonlite::fromJSON(CredentialsJdbcPredictionTargetPayloadJson)
      self$`credentialId` <- CredentialsJdbcPredictionTargetPayloadObject$`credentialId`
      self$`datasourceId` <- CredentialsJdbcPredictionTargetPayloadObject$`datasourceId`
      self$`typeId` <- CredentialsJdbcPredictionTargetPayloadObject$`typeId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
