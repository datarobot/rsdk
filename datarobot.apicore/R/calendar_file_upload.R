# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CalendarFileUpload
#'
#' @description CalendarFileUpload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field file  data.frame The calendar file used to create a calendar. The calendar file expect to meet the following criteria:  Must be in a csv or xlsx format.  Must have a header row. The names themselves in the header row can be anything.  Must have a single date column, in YYYY-MM-DD format.  May optionally have a name column as the second column.  May optionally have one series ID column that states what series each event is applicable for. If present, the name of this column must be specified in the &#x60;multiseriesIdColumns&#x60; parameter.
#'
#' @field multiseriesIdColumns  character [optional] An array of multiseries ID column names for the calendar file. Currently only one multiseries ID column is supported. If not specified, the calendar is considered to be single series.
#'
#' @field name  character [optional] The name of the calendar file. If not provided, this will be set to the name of the provided file.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CalendarFileUpload <- R6::R6Class(
  "CalendarFileUpload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(file = NULL, multiseriesIdColumns = NULL, name = NULL) {
      if (!is.null(`file`)) {
      }
      if (!is.null(`multiseriesIdColumns`)) {
        stopifnot(is.character(`multiseriesIdColumns`), length(`multiseriesIdColumns`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
    }
  ),
  public = list(
    `file` = NULL,
    `multiseriesIdColumns` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param file The calendar file used to create a calendar. The calendar file expect to meet the following criteria:  Must be in a csv or xlsx format.  Must have a header row. The names themselves in the header row can be anything.  Must have a single date column, in YYYY-MM-DD format.  May optionally have a name column as the second column.  May optionally have one series ID column that states what series each event is applicable for. If present, the name of this column must be specified in the &#x60;multiseriesIdColumns&#x60; parameter.
    #' @param multiseriesIdColumns An array of multiseries ID column names for the calendar file. Currently only one multiseries ID column is supported. If not specified, the calendar is considered to be single series.
    #' @param name The name of the calendar file. If not provided, this will be set to the name of the provided file.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`file` = NULL, `multiseriesIdColumns` = NULL, `name` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`file`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(file, multiseriesIdColumns, name)
      }
      self$`file` <- `file`
      self$`multiseriesIdColumns` <- `multiseriesIdColumns`
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(file = self$`file`, multiseriesIdColumns = self$`multiseriesIdColumns`, name = self$`name`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`file`)) {
          sprintf(
            '"file":
            "%s"
                  ',
            self$`file`
          )
        },
        if (!is.null(self$`multiseriesIdColumns`)) {
          sprintf(
            '"multiseriesIdColumns":
            "%s"
                  ',
            self$`multiseriesIdColumns`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CalendarFileUploadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CalendarFileUploadJson, validateParams = FALSE) {
      CalendarFileUploadObject <- jsonlite::fromJSON(CalendarFileUploadJson)
      self$`file` <- CalendarFileUploadObject$`file`
      self$`multiseriesIdColumns` <- CalendarFileUploadObject$`multiseriesIdColumns`
      self$`name` <- CalendarFileUploadObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
