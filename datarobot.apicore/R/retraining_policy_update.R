# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RetrainingPolicyUpdate
#'
#' @description RetrainingPolicyUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field action  character [optional] Configure the action to take on the resultant new model.
#'
#' @field autopilotOptions  \link{AutopilotOptions} [optional]
#'
#' @field description  character [optional] Description of the retraining policy.
#'
#' @field featureListStrategy  character [optional] Configure the feature list strategy used for modeling.
#'
#' @field modelSelectionStrategy  character [optional] Configure how new model is selected when the retraining policy runs.
#'
#' @field name  character [optional] Name of the retraining policy.
#'
#' @field projectOptions  \link{ProjectOptions} [optional]
#'
#' @field projectOptionsStrategy  character [optional] Configure the project option strategy used for modeling.
#'
#' @field timeSeriesOptions  \link{TimeSeriesOptions} [optional]
#'
#' @field trigger  \link{Trigger} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RetrainingPolicyUpdate <- R6::R6Class(
  "RetrainingPolicyUpdate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`action` = NULL, `autopilotOptions` = NULL, `description` = NULL, `featureListStrategy` = NULL, `modelSelectionStrategy` = NULL, `name` = NULL, `projectOptions` = NULL, `projectOptionsStrategy` = NULL, `timeSeriesOptions` = NULL, `trigger` = NULL) {
      if (!is.null(`action`)) {
        stopifnot(is.character(`action`), length(`action`) == 1)
      }
      if (!is.null(`autopilotOptions`)) {
        stopifnot(R6::is.R6(`autopilotOptions`))
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`featureListStrategy`)) {
        stopifnot(is.character(`featureListStrategy`), length(`featureListStrategy`) == 1)
      }
      if (!is.null(`modelSelectionStrategy`)) {
        stopifnot(is.character(`modelSelectionStrategy`), length(`modelSelectionStrategy`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`projectOptions`)) {
        stopifnot(R6::is.R6(`projectOptions`))
      }
      if (!is.null(`projectOptionsStrategy`)) {
        stopifnot(is.character(`projectOptionsStrategy`), length(`projectOptionsStrategy`) == 1)
      }
      if (!is.null(`timeSeriesOptions`)) {
        stopifnot(R6::is.R6(`timeSeriesOptions`))
      }
      if (!is.null(`trigger`)) {
        stopifnot(R6::is.R6(`trigger`))
      }
    }
  ),
  public = list(
    `action` = NULL,
    `autopilotOptions` = NULL,
    `description` = NULL,
    `featureListStrategy` = NULL,
    `modelSelectionStrategy` = NULL,
    `name` = NULL,
    `projectOptions` = NULL,
    `projectOptionsStrategy` = NULL,
    `timeSeriesOptions` = NULL,
    `trigger` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param action Configure the action to take on the resultant new model.
    #' @param autopilotOptions
    #' @param description Description of the retraining policy.
    #' @param featureListStrategy Configure the feature list strategy used for modeling.
    #' @param modelSelectionStrategy Configure how new model is selected when the retraining policy runs.
    #' @param name Name of the retraining policy.
    #' @param projectOptions
    #' @param projectOptionsStrategy Configure the project option strategy used for modeling.
    #' @param timeSeriesOptions
    #' @param trigger
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`action` = NULL, `autopilotOptions` = NULL, `description` = NULL, `featureListStrategy` = NULL, `modelSelectionStrategy` = NULL, `name` = NULL, `projectOptions` = NULL, `projectOptionsStrategy` = NULL, `timeSeriesOptions` = NULL, `trigger` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(action, autopilotOptions, description, featureListStrategy, modelSelectionStrategy, name, projectOptions, projectOptionsStrategy, timeSeriesOptions, trigger)
      }
      self$`action` <- `action`
      self$`autopilotOptions` <- `autopilotOptions`
      self$`description` <- `description`
      self$`featureListStrategy` <- `featureListStrategy`
      self$`modelSelectionStrategy` <- `modelSelectionStrategy`
      self$`name` <- `name`
      self$`projectOptions` <- `projectOptions`
      self$`projectOptionsStrategy` <- `projectOptionsStrategy`
      self$`timeSeriesOptions` <- `timeSeriesOptions`
      self$`trigger` <- `trigger`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(action = self$`action`, autopilotOptions = self$`autopilotOptions`, description = self$`description`, featureListStrategy = self$`featureListStrategy`, modelSelectionStrategy = self$`modelSelectionStrategy`, name = self$`name`, projectOptions = self$`projectOptions`, projectOptionsStrategy = self$`projectOptionsStrategy`, timeSeriesOptions = self$`timeSeriesOptions`, trigger = self$`trigger`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`action`)) {
          sprintf(
            '"action":
            "%s"
                  ',
            self$`action`
          )
        },
        if (!is.null(self$`autopilotOptions`)) {
          sprintf(
            '"autopilotOptions":
            %s
      ',
            jsonlite::toJSON(self$`autopilotOptions`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`featureListStrategy`)) {
          sprintf(
            '"featureListStrategy":
            "%s"
                  ',
            self$`featureListStrategy`
          )
        },
        if (!is.null(self$`modelSelectionStrategy`)) {
          sprintf(
            '"modelSelectionStrategy":
            "%s"
                  ',
            self$`modelSelectionStrategy`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`projectOptions`)) {
          sprintf(
            '"projectOptions":
            %s
      ',
            jsonlite::toJSON(self$`projectOptions`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`projectOptionsStrategy`)) {
          sprintf(
            '"projectOptionsStrategy":
            "%s"
                  ',
            self$`projectOptionsStrategy`
          )
        },
        if (!is.null(self$`timeSeriesOptions`)) {
          sprintf(
            '"timeSeriesOptions":
            %s
      ',
            jsonlite::toJSON(self$`timeSeriesOptions`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`trigger`)) {
          sprintf(
            '"trigger":
            %s
      ',
            jsonlite::toJSON(self$`trigger`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RetrainingPolicyUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RetrainingPolicyUpdateJson, validateParams = FALSE) {
      RetrainingPolicyUpdateObject <- jsonlite::fromJSON(RetrainingPolicyUpdateJson)
      self$`action` <- RetrainingPolicyUpdateObject$`action`
      self$`autopilotOptions` <- AutopilotOptions$new()$fromJSON(jsonlite::toJSON(RetrainingPolicyUpdateObject$autopilotOptions, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`description` <- RetrainingPolicyUpdateObject$`description`
      self$`featureListStrategy` <- RetrainingPolicyUpdateObject$`featureListStrategy`
      self$`modelSelectionStrategy` <- RetrainingPolicyUpdateObject$`modelSelectionStrategy`
      self$`name` <- RetrainingPolicyUpdateObject$`name`
      self$`projectOptions` <- ProjectOptions$new()$fromJSON(jsonlite::toJSON(RetrainingPolicyUpdateObject$projectOptions, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`projectOptionsStrategy` <- RetrainingPolicyUpdateObject$`projectOptionsStrategy`
      self$`timeSeriesOptions` <- TimeSeriesOptions$new()$fromJSON(jsonlite::toJSON(RetrainingPolicyUpdateObject$timeSeriesOptions, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`trigger` <- Trigger$new()$fromJSON(jsonlite::toJSON(RetrainingPolicyUpdateObject$trigger, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
