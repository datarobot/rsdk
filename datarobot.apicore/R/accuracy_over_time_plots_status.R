# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AccuracyOverTimePlotsStatus
#'
#' @description AccuracyOverTimePlotsStatus Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field training  character The status for the training.
#'
#' @field validation  character The status for the validation.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AccuracyOverTimePlotsStatus <- R6::R6Class(
  "AccuracyOverTimePlotsStatus",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(training = NULL, validation = NULL) {
      if (!is.null(`training`)) {
        stopifnot(is.character(`training`), length(`training`) == 1)
      }
      if (!is.null(`validation`)) {
        stopifnot(is.character(`validation`), length(`validation`) == 1)
      }
    }
  ),
  public = list(
    `training` = NULL,
    `validation` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param training The status for the training.
    #' @param validation The status for the validation.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`training` = NULL, `validation` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`training`, `validation`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(training, validation)
      }
      self$`training` <- `training`
      self$`validation` <- `validation`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(training = self$`training`, validation = self$`validation`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`training`)) {
          sprintf(
            '"training":
            "%s"
                  ',
            self$`training`
          )
        },
        if (!is.null(self$`validation`)) {
          sprintf(
            '"validation":
            "%s"
                  ',
            self$`validation`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AccuracyOverTimePlotsStatusJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AccuracyOverTimePlotsStatusJson, validateParams = FALSE) {
      AccuracyOverTimePlotsStatusObject <- jsonlite::fromJSON(AccuracyOverTimePlotsStatusJson)
      self$`training` <- AccuracyOverTimePlotsStatusObject$`training`
      self$`validation` <- AccuracyOverTimePlotsStatusObject$`validation`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
