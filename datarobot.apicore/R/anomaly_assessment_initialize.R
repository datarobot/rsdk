# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AnomalyAssessmentInitialize
#'
#' @description AnomalyAssessmentInitialize Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtest  \link{OneOfintegerstring} The backtest to compute insight for.
#'
#' @field seriesId  character [optional] Required for multiseries projects. The series id to compute insight for.
#'
#' @field source  character The source to compute insight for.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AnomalyAssessmentInitialize <- R6::R6Class(
  "AnomalyAssessmentInitialize",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`backtest` = NULL, `seriesId` = NULL, `source` = NULL) {
      if (!is.null(`backtest`)) {
        .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = backtest)
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
      if (!is.null(`seriesId`)) {
        stopifnot(is.character(`seriesId`), length(`seriesId`) == 1)
      }
    }
  ),
  public = list(
    `backtest` = NULL,
    `seriesId` = NULL,
    `source` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtest The backtest to compute insight for.
    #' @param seriesId Required for multiseries projects. The series id to compute insight for.
    #' @param source The source to compute insight for.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`backtest` = NULL, `source` = NULL, `seriesId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`backtest`, `source`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(backtest, seriesId, source)
      }
      self$`backtest` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = backtest)
      self$`seriesId` <- `seriesId`
      self$`source` <- `source`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(backtest = self$`backtest`, seriesId = self$`seriesId`, source = self$`source`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtest`)) {
          sprintf(
            '"backtest":
            %s
      ',
            self$`backtest`
          )
        },
        if (!is.null(self$`seriesId`)) {
          sprintf(
            '"seriesId":
            "%s"
                  ',
            self$`seriesId`
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AnomalyAssessmentInitializeJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AnomalyAssessmentInitializeJson, validateParams = FALSE) {
      AnomalyAssessmentInitializeObject <- jsonlite::fromJSON(AnomalyAssessmentInitializeJson)
      self$`backtest` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = AnomalyAssessmentInitializeObject$backtest)
      self$`seriesId` <- AnomalyAssessmentInitializeObject$`seriesId`
      self$`source` <- AnomalyAssessmentInitializeObject$`source`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
