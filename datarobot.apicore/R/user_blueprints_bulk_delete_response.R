# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintsBulkDeleteResponse
#'
#' @description UserBlueprintsBulkDeleteResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field failedToDelete  list( character ) List of IDs of User Blueprints which failed to be deleted.
#'
#' @field successfullyDeleted  list( character ) List of IDs of User Blueprints successfully deleted.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintsBulkDeleteResponse <- R6::R6Class(
  "UserBlueprintsBulkDeleteResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(failedToDelete = NULL, successfullyDeleted = NULL) {
      if (!is.null(`failedToDelete`)) {
        stopifnot(is.vector(`failedToDelete`))
      }
      if (!is.null(`successfullyDeleted`)) {
        stopifnot(is.vector(`successfullyDeleted`))
      }
    }
  ),
  public = list(
    `failedToDelete` = NULL,
    `successfullyDeleted` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param failedToDelete List of IDs of User Blueprints which failed to be deleted.
    #' @param successfullyDeleted List of IDs of User Blueprints successfully deleted.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`failedToDelete` = NULL, `successfullyDeleted` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`failedToDelete`, `successfullyDeleted`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(failedToDelete, successfullyDeleted)
      }
      sapply(`failedToDelete`, function(x) stopifnot(is.character(x)))
      sapply(`successfullyDeleted`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(failedToDelete = self$`failedToDelete`, successfullyDeleted = self$`successfullyDeleted`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`failedToDelete`)) {
          sprintf(
            '"failedToDelete":
              [%s]
            ',
            paste(unlist(lapply(self$`failedToDelete`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`successfullyDeleted`)) {
          sprintf(
            '"successfullyDeleted":
              [%s]
            ',
            paste(unlist(lapply(self$`successfullyDeleted`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintsBulkDeleteResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintsBulkDeleteResponseJson, validateParams = FALSE) {
      UserBlueprintsBulkDeleteResponseObject <- jsonlite::fromJSON(UserBlueprintsBulkDeleteResponseJson)
      self$`failedToDelete` <- ApiClient$new()$deserializeObj(UserBlueprintsBulkDeleteResponseObject$`failedToDelete`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`successfullyDeleted` <- ApiClient$new()$deserializeObj(UserBlueprintsBulkDeleteResponseObject$`successfullyDeleted`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
