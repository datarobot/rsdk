# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RecommendedModelResponse
#'
#' @description RecommendedModelResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field modelId  character the ID of the recommended model
#'
#' @field recommendationType  character the type of model recommendation
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RecommendedModelResponse <- R6::R6Class(
  "RecommendedModelResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(modelId = NULL, recommendationType = NULL) {
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`recommendationType`)) {
        stopifnot(is.character(`recommendationType`), length(`recommendationType`) == 1)
      }
    }
  ),
  public = list(
    `modelId` = NULL,
    `recommendationType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param modelId the ID of the recommended model
    #' @param recommendationType the type of model recommendation
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`modelId` = NULL, `recommendationType` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`modelId`, `recommendationType`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(modelId, recommendationType)
      }
      self$`modelId` <- `modelId`
      self$`recommendationType` <- `recommendationType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(modelId = self$`modelId`, recommendationType = self$`recommendationType`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`recommendationType`)) {
          sprintf(
            '"recommendationType":
            "%s"
                  ',
            self$`recommendationType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RecommendedModelResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RecommendedModelResponseJson, validateParams = FALSE) {
      RecommendedModelResponseObject <- jsonlite::fromJSON(RecommendedModelResponseJson)
      self$`modelId` <- RecommendedModelResponseObject$`modelId`
      self$`recommendationType` <- RecommendedModelResponseObject$`recommendationType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
