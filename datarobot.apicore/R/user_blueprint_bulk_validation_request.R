# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintBulkValidationRequest
#'
#' @description UserBlueprintBulkValidationRequest Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field projectId  character [optional] String representation of ObjectId for the currently active project. The user blueprint is validated when this project is active. Necessary in the event of project specific tasks, such as column selection tasks.
#'
#' @field userBlueprintIds  list( character ) The ids of the user blueprints to validate in bulk.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintBulkValidationRequest <- R6::R6Class(
  "UserBlueprintBulkValidationRequest",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`projectId` = NULL, `userBlueprintIds` = NULL) {
      if (!is.null(`userBlueprintIds`)) {
        stopifnot(is.vector(`userBlueprintIds`))
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `projectId` = NULL,
    `userBlueprintIds` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param projectId String representation of ObjectId for the currently active project. The user blueprint is validated when this project is active. Necessary in the event of project specific tasks, such as column selection tasks.
    #' @param userBlueprintIds The ids of the user blueprints to validate in bulk.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`userBlueprintIds` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`userBlueprintIds`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(projectId, userBlueprintIds)
      }
      self$`projectId` <- `projectId`
      sapply(`userBlueprintIds`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(projectId = self$`projectId`, userBlueprintIds = self$`userBlueprintIds`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`userBlueprintIds`)) {
          sprintf(
            '"userBlueprintIds":
            [%s]
                  ',
            paste(unlist(lapply(self$`userBlueprintIds`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintBulkValidationRequestJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintBulkValidationRequestJson, validateParams = FALSE) {
      UserBlueprintBulkValidationRequestObject <- jsonlite::fromJSON(UserBlueprintBulkValidationRequestJson)
      self$`projectId` <- UserBlueprintBulkValidationRequestObject$`projectId`
      self$`userBlueprintIds` <- ApiClient$new()$deserializeObj(UserBlueprintBulkValidationRequestObject$`userBlueprintIds`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
