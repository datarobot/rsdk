# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CrossClassAccuracy
#'
#' @description CrossClassAccuracy Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field feature  character The name of the categorical feature.
#'
#' @field modelId  character ID of the model for the cross-class accuracy scores.
#'
#' @field perClassAccuracyScores  list( \link{PerClassAccuracy} ) An array of metric scores for each class of the feature.
#'
#' @field predictionThreshold  numeric Value of the prediction threshold for the model.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CrossClassAccuracy <- R6::R6Class(
  "CrossClassAccuracy",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(feature = NULL, modelId = NULL, perClassAccuracyScores = NULL, predictionThreshold = NULL) {
      if (!is.null(`feature`)) {
        stopifnot(is.character(`feature`), length(`feature`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`perClassAccuracyScores`)) {
        stopifnot(is.vector(`perClassAccuracyScores`))
      }
      if (!is.null(`predictionThreshold`)) {
      }
    }
  ),
  public = list(
    `feature` = NULL,
    `modelId` = NULL,
    `perClassAccuracyScores` = NULL,
    `predictionThreshold` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param feature The name of the categorical feature.
    #' @param modelId ID of the model for the cross-class accuracy scores.
    #' @param perClassAccuracyScores An array of metric scores for each class of the feature.
    #' @param predictionThreshold Value of the prediction threshold for the model.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`feature` = NULL, `modelId` = NULL, `perClassAccuracyScores` = NULL, `predictionThreshold` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`feature`, `modelId`, `perClassAccuracyScores`, `predictionThreshold`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(feature, modelId, perClassAccuracyScores, predictionThreshold)
      }
      self$`feature` <- `feature`
      self$`modelId` <- `modelId`
      sapply(`perClassAccuracyScores`, function(x) stopifnot(R6::is.R6(x)))
      self$`predictionThreshold` <- `predictionThreshold`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(feature = self$`feature`, modelId = self$`modelId`, perClassAccuracyScores = self$`perClassAccuracyScores`, predictionThreshold = self$`predictionThreshold`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`feature`)) {
          sprintf(
            '"feature":
            "%s"
                  ',
            self$`feature`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`perClassAccuracyScores`)) {
          sprintf(
            '"perClassAccuracyScores":
            [%s]
',
            paste(sapply(self$`perClassAccuracyScores`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CrossClassAccuracyJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CrossClassAccuracyJson, validateParams = FALSE) {
      CrossClassAccuracyObject <- jsonlite::fromJSON(CrossClassAccuracyJson)
      self$`feature` <- CrossClassAccuracyObject$`feature`
      self$`modelId` <- CrossClassAccuracyObject$`modelId`
      self$`perClassAccuracyScores` <- ApiClient$new()$deserializeObj(CrossClassAccuracyObject$`perClassAccuracyScores`, "array[PerClassAccuracy]", loadNamespace("datarobot.apicore"))
      self$`predictionThreshold` <- CrossClassAccuracyObject$`predictionThreshold`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
