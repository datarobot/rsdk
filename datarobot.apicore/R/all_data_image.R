# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AllDataImage
#'
#' @description AllDataImage Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field images  list( character ) A list of b64 encoded images.
#'
#' @field percentageOfMissingImages  numeric A percentage of image rows that have a missing value for this feature.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AllDataImage <- R6::R6Class(
  "AllDataImage",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`images` = NULL, `percentageOfMissingImages` = NULL) {
      if (!is.null(`images`)) {
        stopifnot(is.vector(`images`), sapply(`images`, is.character))
      }
      if (!is.null(`percentageOfMissingImages`)) {
      }
    }
  ),
  public = list(
    `images` = NULL,
    `percentageOfMissingImages` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param images A list of b64 encoded images.
    #' @param percentageOfMissingImages A percentage of image rows that have a missing value for this feature.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`images` = NULL, `percentageOfMissingImages` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`images`, `percentageOfMissingImages`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(images, percentageOfMissingImages)
      }
      self$`images` <- `images`
      self$`percentageOfMissingImages` <- `percentageOfMissingImages`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(images = self$`images`, percentageOfMissingImages = self$`percentageOfMissingImages`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`images`)) {
          sprintf(
            '"images":
            [%s]
                  ',
            paste(unlist(lapply(self$`images`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`percentageOfMissingImages`)) {
          sprintf(
            '"percentageOfMissingImages":
            %d
                  ',
            self$`percentageOfMissingImages`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AllDataImageJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AllDataImageJson, validateParams = FALSE) {
      AllDataImageObject <- jsonlite::fromJSON(AllDataImageJson)
      self$`images` <- ApiClient$new()$deserializeObj(AllDataImageObject$`images`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`percentageOfMissingImages` <- AllDataImageObject$`percentageOfMissingImages`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
