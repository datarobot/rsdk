# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelPacakgeScoringCodeMeta
#'
#' @description ModelPacakgeScoringCodeMeta Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dataRobotPredictionVersion  character DataRobot prediction API version for the scoring code
#'
#' @field location  character Location of the scoring code
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelPacakgeScoringCodeMeta <- R6::R6Class(
  "ModelPacakgeScoringCodeMeta",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(dataRobotPredictionVersion = NULL, location = NULL) {
      if (!is.null(`dataRobotPredictionVersion`)) {
        stopifnot(is.character(`dataRobotPredictionVersion`), length(`dataRobotPredictionVersion`) == 1)
      }
      if (!is.null(`location`)) {
        stopifnot(is.character(`location`), length(`location`) == 1)
      }
    }
  ),
  public = list(
    `dataRobotPredictionVersion` = NULL,
    `location` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dataRobotPredictionVersion DataRobot prediction API version for the scoring code
    #' @param location Location of the scoring code
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataRobotPredictionVersion` = NULL, `location` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`dataRobotPredictionVersion`, `location`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(dataRobotPredictionVersion, location)
      }
      self$`dataRobotPredictionVersion` <- `dataRobotPredictionVersion`
      self$`location` <- `location`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(dataRobotPredictionVersion = self$`dataRobotPredictionVersion`, location = self$`location`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dataRobotPredictionVersion`)) {
          sprintf(
            '"dataRobotPredictionVersion":
            "%s"
                  ',
            self$`dataRobotPredictionVersion`
          )
        },
        if (!is.null(self$`location`)) {
          sprintf(
            '"location":
            "%s"
                  ',
            self$`location`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelPacakgeScoringCodeMetaJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelPacakgeScoringCodeMetaJson, validateParams = FALSE) {
      ModelPacakgeScoringCodeMetaObject <- jsonlite::fromJSON(ModelPacakgeScoringCodeMetaJson)
      self$`dataRobotPredictionVersion` <- ModelPacakgeScoringCodeMetaObject$`dataRobotPredictionVersion`
      self$`location` <- ModelPacakgeScoringCodeMetaObject$`location`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
