# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FrequentValueData
#'
#' @description FrequentValueData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer Count of specified frequent value in the sample, weighted by exposure or weights
#'
#' @field dataQuality  character Any data quality issue associated with this particularvalue of the feature. Possible data quality types include &#39;excess_zero&#39;, &#39;inlier&#39;, &#39;disguised_missing_value&#39;, and &#39;no_issues_found&#39; and the relevant statistics.
#'
#' @field target  numeric Average target value for the specified frequent value if the target is binary or numeric. With weights or exposure, this becomes a weighted average. If the target is not set, it returns None.
#'
#' @field value  \link{AnyOfnumberstring} Specified frequent value, either a float or a string, like &#x60;&#x3D;All Others+&#x60;
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FrequentValueData <- R6::R6Class(
  "FrequentValueData",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(count = NULL, dataQuality = NULL, target = NULL, value = NULL) {
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`dataQuality`)) {
        stopifnot(is.character(`dataQuality`), length(`dataQuality`) == 1)
      }
      if (!is.null(`target`)) {
      }
      if (!is.null(`value`)) {
        .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = value)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `dataQuality` = NULL,
    `target` = NULL,
    `value` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count Count of specified frequent value in the sample, weighted by exposure or weights
    #' @param dataQuality Any data quality issue associated with this particularvalue of the feature. Possible data quality types include &#39;excess_zero&#39;, &#39;inlier&#39;, &#39;disguised_missing_value&#39;, and &#39;no_issues_found&#39; and the relevant statistics.
    #' @param target Average target value for the specified frequent value if the target is binary or numeric. With weights or exposure, this becomes a weighted average. If the target is not set, it returns None.
    #' @param value Specified frequent value, either a float or a string, like &#x60;&#x3D;All Others+&#x60;
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `dataQuality` = NULL, `target` = NULL, `value` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`count`, `dataQuality`, `target`, `value`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(count, dataQuality, target, value)
      }
      self$`count` <- `count`
      self$`dataQuality` <- `dataQuality`
      self$`target` <- `target`
      self$`value` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = value)
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(count = self$`count`, dataQuality = self$`dataQuality`, target = self$`target`, value = self$`value`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`dataQuality`)) {
          sprintf(
            '"dataQuality":
            "%s"
                  ',
            self$`dataQuality`
          )
        },
        if (!is.null(self$`target`)) {
          sprintf(
            '"target":
            %d
                  ',
            self$`target`
          )
        },
        if (!is.null(self$`value`)) {
          sprintf(
            '"value":
          %s
        ',
            self$`value`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FrequentValueDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FrequentValueDataJson, validateParams = FALSE) {
      FrequentValueDataObject <- jsonlite::fromJSON(FrequentValueDataJson)
      self$`count` <- FrequentValueDataObject$`count`
      self$`dataQuality` <- FrequentValueDataObject$`dataQuality`
      self$`target` <- FrequentValueDataObject$`target`
      self$`value` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = FrequentValueDataObject$value)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
