# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentsScoringCodeBuildPayload
#'
#' @description DeploymentsScoringCodeBuildPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field includeAgent  character [optional] Whether the Scoring Code built will include tracking agent
#'
#' @field includePredictionExplanations  character [optional] Whether the Scoring Code built will include prediction explanations
#'
#' @field includePredictionIntervals  character [optional] Whether the Scoring Code built will include prediction intervals
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentsScoringCodeBuildPayload <- R6::R6Class(
  "DeploymentsScoringCodeBuildPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(includeAgent = NULL, includePredictionExplanations = NULL, includePredictionIntervals = NULL) {
      if (!is.null(`includeAgent`)) {
        stopifnot(is.logical(`includeAgent`), length(`includeAgent`) == 1)
      }
      if (!is.null(`includePredictionExplanations`)) {
        stopifnot(is.logical(`includePredictionExplanations`), length(`includePredictionExplanations`) == 1)
      }
      if (!is.null(`includePredictionIntervals`)) {
        stopifnot(is.logical(`includePredictionIntervals`), length(`includePredictionIntervals`) == 1)
      }
    }
  ),
  public = list(
    `includeAgent` = NULL,
    `includePredictionExplanations` = NULL,
    `includePredictionIntervals` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param includeAgent Whether the Scoring Code built will include tracking agent
    #' @param includePredictionExplanations Whether the Scoring Code built will include prediction explanations
    #' @param includePredictionIntervals Whether the Scoring Code built will include prediction intervals
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`includeAgent` = NULL, `includePredictionExplanations` = NULL, `includePredictionIntervals` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(includeAgent, includePredictionExplanations, includePredictionIntervals)
      }
      self$`includeAgent` <- `includeAgent`
      self$`includePredictionExplanations` <- `includePredictionExplanations`
      self$`includePredictionIntervals` <- `includePredictionIntervals`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(includeAgent = self$`includeAgent`, includePredictionExplanations = self$`includePredictionExplanations`, includePredictionIntervals = self$`includePredictionIntervals`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`includeAgent`)) {
          sprintf(
            '"includeAgent":
            %s
                  ',
            tolower(self$`includeAgent`)
          )
        },
        if (!is.null(self$`includePredictionExplanations`)) {
          sprintf(
            '"includePredictionExplanations":
            %s
                  ',
            tolower(self$`includePredictionExplanations`)
          )
        },
        if (!is.null(self$`includePredictionIntervals`)) {
          sprintf(
            '"includePredictionIntervals":
            %s
                  ',
            tolower(self$`includePredictionIntervals`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentsScoringCodeBuildPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentsScoringCodeBuildPayloadJson, validateParams = FALSE) {
      DeploymentsScoringCodeBuildPayloadObject <- jsonlite::fromJSON(DeploymentsScoringCodeBuildPayloadJson)
      self$`includeAgent` <- DeploymentsScoringCodeBuildPayloadObject$`includeAgent`
      self$`includePredictionExplanations` <- DeploymentsScoringCodeBuildPayloadObject$`includePredictionExplanations`
      self$`includePredictionIntervals` <- DeploymentsScoringCodeBuildPayloadObject$`includePredictionIntervals`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
