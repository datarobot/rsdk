# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SegmentationTaskCreate
#'
#' @description SegmentationTaskCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datetimePartitionColumn  character [optional] The date column that will be used to identify the date in time series segmentation.
#'
#' @field modelPackageId  character [optional] Model package id for using an external model registry package.
#'
#' @field multiseriesIdColumns  list( character ) [optional] List of one or more names of multiseries id columns.
#'
#' @field target  character The target for the dataset
#'
#' @field useAutomatedSegmentation  character [optional] Enable the use of automated segmentation tasks
#'
#' @field useTimeSeries  character [optional] Enable time series based segmentation tasks
#'
#' @field userDefinedSegmentIdColumns  list( character ) [optional] List of one or more names of columns to be used for user-defined business rule segmentations.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SegmentationTaskCreate <- R6::R6Class(
  "SegmentationTaskCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datetimePartitionColumn = NULL, modelPackageId = NULL, multiseriesIdColumns = NULL, target = NULL, useAutomatedSegmentation = NULL, useTimeSeries = NULL, userDefinedSegmentIdColumns = NULL) {
      if (!is.null(`target`)) {
        stopifnot(is.character(`target`), length(`target`) == 1)
      }
      if (!is.null(`datetimePartitionColumn`)) {
        stopifnot(is.character(`datetimePartitionColumn`), length(`datetimePartitionColumn`) == 1)
      }
      if (!is.null(`modelPackageId`)) {
        stopifnot(is.character(`modelPackageId`), length(`modelPackageId`) == 1)
      }
      if (!is.null(`multiseriesIdColumns`)) {
        stopifnot(is.vector(`multiseriesIdColumns`))
      }
      if (!is.null(`useAutomatedSegmentation`)) {
        stopifnot(is.logical(`useAutomatedSegmentation`), length(`useAutomatedSegmentation`) == 1)
      }
      if (!is.null(`useTimeSeries`)) {
        stopifnot(is.logical(`useTimeSeries`), length(`useTimeSeries`) == 1)
      }
      if (!is.null(`userDefinedSegmentIdColumns`)) {
        stopifnot(is.vector(`userDefinedSegmentIdColumns`))
      }
    }
  ),
  public = list(
    `datetimePartitionColumn` = NULL,
    `modelPackageId` = NULL,
    `multiseriesIdColumns` = NULL,
    `target` = NULL,
    `useAutomatedSegmentation` = NULL,
    `useTimeSeries` = NULL,
    `userDefinedSegmentIdColumns` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datetimePartitionColumn The date column that will be used to identify the date in time series segmentation.
    #' @param modelPackageId Model package id for using an external model registry package.
    #' @param multiseriesIdColumns List of one or more names of multiseries id columns.
    #' @param target The target for the dataset
    #' @param useAutomatedSegmentation Enable the use of automated segmentation tasks
    #' @param useTimeSeries Enable time series based segmentation tasks
    #' @param userDefinedSegmentIdColumns List of one or more names of columns to be used for user-defined business rule segmentations.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`target` = NULL, `datetimePartitionColumn` = NULL, `modelPackageId` = NULL, `multiseriesIdColumns` = NULL, `useAutomatedSegmentation` = FALSE, `useTimeSeries` = FALSE, `userDefinedSegmentIdColumns` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`target`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datetimePartitionColumn, modelPackageId, multiseriesIdColumns, target, useAutomatedSegmentation, useTimeSeries, userDefinedSegmentIdColumns)
      }
      self$`datetimePartitionColumn` <- `datetimePartitionColumn`
      self$`modelPackageId` <- `modelPackageId`
      sapply(`multiseriesIdColumns`, function(x) stopifnot(is.character(x)))
      self$`target` <- `target`
      self$`useAutomatedSegmentation` <- `useAutomatedSegmentation`
      self$`useTimeSeries` <- `useTimeSeries`
      sapply(`userDefinedSegmentIdColumns`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datetimePartitionColumn = self$`datetimePartitionColumn`, modelPackageId = self$`modelPackageId`, multiseriesIdColumns = self$`multiseriesIdColumns`, target = self$`target`, useAutomatedSegmentation = self$`useAutomatedSegmentation`, useTimeSeries = self$`useTimeSeries`, userDefinedSegmentIdColumns = self$`userDefinedSegmentIdColumns`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datetimePartitionColumn`)) {
          sprintf(
            '"datetimePartitionColumn":
            "%s"
                  ',
            self$`datetimePartitionColumn`
          )
        },
        if (!is.null(self$`modelPackageId`)) {
          sprintf(
            '"modelPackageId":
            "%s"
                  ',
            self$`modelPackageId`
          )
        },
        if (!is.null(self$`multiseriesIdColumns`)) {
          sprintf(
            '"multiseriesIdColumns":
              [%s]
            ',
            paste(unlist(lapply(self$`multiseriesIdColumns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`target`)) {
          sprintf(
            '"target":
            "%s"
                  ',
            self$`target`
          )
        },
        if (!is.null(self$`useAutomatedSegmentation`)) {
          sprintf(
            '"useAutomatedSegmentation":
            %s
                  ',
            tolower(self$`useAutomatedSegmentation`)
          )
        },
        if (!is.null(self$`useTimeSeries`)) {
          sprintf(
            '"useTimeSeries":
            %s
                  ',
            tolower(self$`useTimeSeries`)
          )
        },
        if (!is.null(self$`userDefinedSegmentIdColumns`)) {
          sprintf(
            '"userDefinedSegmentIdColumns":
              [%s]
            ',
            paste(unlist(lapply(self$`userDefinedSegmentIdColumns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SegmentationTaskCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SegmentationTaskCreateJson, validateParams = FALSE) {
      SegmentationTaskCreateObject <- jsonlite::fromJSON(SegmentationTaskCreateJson)
      self$`datetimePartitionColumn` <- SegmentationTaskCreateObject$`datetimePartitionColumn`
      self$`modelPackageId` <- SegmentationTaskCreateObject$`modelPackageId`
      self$`multiseriesIdColumns` <- ApiClient$new()$deserializeObj(SegmentationTaskCreateObject$`multiseriesIdColumns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`target` <- SegmentationTaskCreateObject$`target`
      self$`useAutomatedSegmentation` <- SegmentationTaskCreateObject$`useAutomatedSegmentation`
      self$`useTimeSeries` <- SegmentationTaskCreateObject$`useTimeSeries`
      self$`userDefinedSegmentIdColumns` <- ApiClient$new()$deserializeObj(SegmentationTaskCreateObject$`userDefinedSegmentIdColumns`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
