# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApplicationSharingUpdateOrRemove
#'
#' @description ApplicationSharingUpdateOrRemove Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field note  character [optional] A note to go with the project share
#'
#' @field operation  character Name of the action being taken. The only operation is &#39;updateRoles&#39;.
#'
#' @field roles  list( \link{OneOfGrantAccessControlWithUsernameGrantAccessControlWithId} ) Array of GrantAccessControl objects., up to maximum 100 objects.
#'
#' @field sendNotification  character [optional] Send a notification?
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApplicationSharingUpdateOrRemove <- R6::R6Class(
  "ApplicationSharingUpdateOrRemove",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(note = NULL, operation = NULL, roles = NULL, sendNotification = NULL) {
      if (!is.null(`operation`)) {
        stopifnot(is.character(`operation`), length(`operation`) == 1)
      }
      if (!is.null(`roles`)) {
        .setComplexProperty(typeList = list(GrantAccessControlWithUsername, GrantAccessControlWithId), propertyData = roles)
      }
      if (!is.null(`note`)) {
        stopifnot(is.character(`note`), length(`note`) == 1)
      }
      if (!is.null(`sendNotification`)) {
        stopifnot(is.logical(`sendNotification`), length(`sendNotification`) == 1)
      }
    }
  ),
  public = list(
    `note` = NULL,
    `operation` = NULL,
    `roles` = NULL,
    `sendNotification` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param note A note to go with the project share
    #' @param operation Name of the action being taken. The only operation is &#39;updateRoles&#39;.
    #' @param roles Array of GrantAccessControl objects., up to maximum 100 objects.
    #' @param sendNotification Send a notification?
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`operation` = NULL, `roles` = NULL, `note` = "", `sendNotification` = FALSE, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`operation`, `roles`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(note, operation, roles, sendNotification)
      }
      self$`note` <- `note`
      self$`operation` <- `operation`
      self$`roles` <- .setComplexProperty(typeList = list(GrantAccessControlWithUsername, GrantAccessControlWithId), propertyData = roles)
      self$`sendNotification` <- `sendNotification`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(note = self$`note`, operation = self$`operation`, roles = self$`roles`, sendNotification = self$`sendNotification`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`note`)) {
          sprintf(
            '"note":
            "%s"
                  ',
            self$`note`
          )
        },
        if (!is.null(self$`operation`)) {
          sprintf(
            '"operation":
            "%s"
                  ',
            self$`operation`
          )
        },
        if (!is.null(self$`roles`)) {
          sprintf(
            '"roles":
            [%s]
',
            paste(sapply(self$`roles`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`sendNotification`)) {
          sprintf(
            '"sendNotification":
            %s
                  ',
            tolower(self$`sendNotification`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApplicationSharingUpdateOrRemoveJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApplicationSharingUpdateOrRemoveJson, validateParams = FALSE) {
      ApplicationSharingUpdateOrRemoveObject <- jsonlite::fromJSON(ApplicationSharingUpdateOrRemoveJson)
      self$`note` <- ApplicationSharingUpdateOrRemoveObject$`note`
      self$`operation` <- ApplicationSharingUpdateOrRemoveObject$`operation`
      self$`roles` <- ApiClient$new()$deserializeObj(ApplicationSharingUpdateOrRemoveObject$`roles`, "array[OneOfGrantAccessControlWithUsernameGrantAccessControlWithId]", loadNamespace("datarobot.apicore"))
      self$`sendNotification` <- ApplicationSharingUpdateOrRemoveObject$`sendNotification`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
