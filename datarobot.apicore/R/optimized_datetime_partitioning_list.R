# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title OptimizedDatetimePartitioningList
#'
#' @description OptimizedDatetimePartitioningList Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datetimePartitionColumn  character The date column that will be used as a datetime partition column.
#'
#' @field id  character The ID of the datetime partitioning.
#'
#' @field partitionData  \link{DatetimePartitioningResponse}
#'
#' @field target  character The name of the target column.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
OptimizedDatetimePartitioningList <- R6::R6Class(
  "OptimizedDatetimePartitioningList",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datetimePartitionColumn` = NULL, `id` = NULL, `partitionData` = NULL, `target` = NULL) {
      if (!is.null(`datetimePartitionColumn`)) {
        stopifnot(is.character(`datetimePartitionColumn`), length(`datetimePartitionColumn`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`partitionData`)) {
        stopifnot(R6::is.R6(`partitionData`))
      }
      if (!is.null(`target`)) {
        stopifnot(is.character(`target`), length(`target`) == 1)
      }
    }
  ),
  public = list(
    `datetimePartitionColumn` = NULL,
    `id` = NULL,
    `partitionData` = NULL,
    `target` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datetimePartitionColumn The date column that will be used as a datetime partition column.
    #' @param id The ID of the datetime partitioning.
    #' @param partitionData
    #' @param target The name of the target column.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datetimePartitionColumn` = NULL, `id` = NULL, `partitionData` = NULL, `target` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datetimePartitionColumn`, `id`, `partitionData`, `target`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datetimePartitionColumn, id, partitionData, target)
      }
      self$`datetimePartitionColumn` <- `datetimePartitionColumn`
      self$`id` <- `id`
      self$`partitionData` <- `partitionData`
      self$`target` <- `target`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datetimePartitionColumn = self$`datetimePartitionColumn`, id = self$`id`, partitionData = self$`partitionData`, target = self$`target`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datetimePartitionColumn`)) {
          sprintf(
            '"datetimePartitionColumn":
            "%s"
                  ',
            self$`datetimePartitionColumn`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`partitionData`)) {
          sprintf(
            '"partitionData":
            %s
      ',
            jsonlite::toJSON(self$`partitionData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`target`)) {
          sprintf(
            '"target":
            "%s"
                  ',
            self$`target`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param OptimizedDatetimePartitioningListJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(OptimizedDatetimePartitioningListJson, validateParams = FALSE) {
      OptimizedDatetimePartitioningListObject <- jsonlite::fromJSON(OptimizedDatetimePartitioningListJson)
      self$`datetimePartitionColumn` <- OptimizedDatetimePartitioningListObject$`datetimePartitionColumn`
      self$`id` <- OptimizedDatetimePartitioningListObject$`id`
      self$`partitionData` <- DatetimePartitioningResponse$new()$fromJSON(jsonlite::toJSON(OptimizedDatetimePartitioningListObject$partitionData, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`target` <- OptimizedDatetimePartitioningListObject$`target`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
