# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FromSpecific
#'
#' @description FromSpecific Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field categories  \link{OneOfstringarray} [optional] An array of strings describing the intended use of the dataset.
#'
#' @field credentialData  \link{OneOfBasicCredentialsS3CredentialsOAuthCredentials} [optional] The credentials to authenticate with the database, to be used instead of credential ID.
#'
#' @field credentialId  character [optional] The ID of the set of credentials to authenticate with the database.
#'
#' @field credentials  character [optional] A list of credentials to use if this is a Spark dataset that requires credentials.
#'
#' @field password  character [optional] The password (in cleartext) for database authentication. The password will be encrypted on the server-side HTTP request and never saved or stored. Required only if the previous data source was a data source. DEPRECATED: please use credentialId or credentialData instead.
#'
#' @field useKerberos  character [optional] If true, use Kerberos for database authentication.
#'
#' @field user  character [optional] The username for database authentication. Required only if the dataset was initially created from a data source. DEPRECATED: please use credentialId or credentialData instead.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FromSpecific <- R6::R6Class(
  "FromSpecific",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(categories = NULL, credentialData = NULL, credentialId = NULL, credentials = NULL, password = NULL, useKerberos = NULL, user = NULL) {
      if (!is.null(`categories`)) {
        .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      }
      if (!is.null(`credentialData`)) {
        .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`credentials`)) {
        stopifnot(is.character(`credentials`), length(`credentials`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`useKerberos`)) {
        stopifnot(is.logical(`useKerberos`), length(`useKerberos`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `categories` = NULL,
    `credentialData` = NULL,
    `credentialId` = NULL,
    `credentials` = NULL,
    `password` = NULL,
    `useKerberos` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param categories An array of strings describing the intended use of the dataset.
    #' @param credentialData The credentials to authenticate with the database, to be used instead of credential ID.
    #' @param credentialId The ID of the set of credentials to authenticate with the database.
    #' @param credentials A list of credentials to use if this is a Spark dataset that requires credentials.
    #' @param password The password (in cleartext) for database authentication. The password will be encrypted on the server-side HTTP request and never saved or stored. Required only if the previous data source was a data source. DEPRECATED: please use credentialId or credentialData instead.
    #' @param useKerberos If true, use Kerberos for database authentication.
    #' @param user The username for database authentication. Required only if the dataset was initially created from a data source. DEPRECATED: please use credentialId or credentialData instead.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`categories` = NULL, `credentialData` = NULL, `credentialId` = NULL, `credentials` = NULL, `password` = NULL, `useKerberos` = FALSE, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(categories, credentialData, credentialId, credentials, password, useKerberos, user)
      }
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = categories)
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      self$`credentialId` <- `credentialId`
      self$`credentials` <- `credentials`
      self$`password` <- `password`
      self$`useKerberos` <- `useKerberos`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(categories = self$`categories`, credentialData = self$`credentialData`, credentialId = self$`credentialId`, credentials = self$`credentials`, password = self$`password`, useKerberos = self$`useKerberos`, user = self$`user`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`categories`)) {
          sprintf(
            '"categories":
          %s
        ',
            self$`categories`
          )
        },
        if (!is.null(self$`credentialData`)) {
          sprintf(
            '"credentialData":
          %s
        ',
            jsonlite::toJSON(self$`credentialData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`credentials`)) {
          sprintf(
            '"credentials":
            "%s"
                  ',
            self$`credentials`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`useKerberos`)) {
          sprintf(
            '"useKerberos":
            %s
                  ',
            tolower(self$`useKerberos`)
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FromSpecificJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FromSpecificJson, validateParams = FALSE) {
      FromSpecificObject <- jsonlite::fromJSON(FromSpecificJson)
      self$`categories` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = FromSpecificObject$categories)
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = FromSpecificObject$credentialData)
      self$`credentialId` <- FromSpecificObject$`credentialId`
      self$`credentials` <- FromSpecificObject$`credentials`
      self$`password` <- FromSpecificObject$`password`
      self$`useKerberos` <- FromSpecificObject$`useKerberos`
      self$`user` <- FromSpecificObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
