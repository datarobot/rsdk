# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SharingRoleUpdateData
#'
#' @description SharingRoleUpdateData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field id  character [optional] The ID of the recipient
#'
#' @field role  character The assigned role
#'
#' @field shareRecipientType  character The recipient type
#'
#' @field username  character [optional] The name of the user, group, or organization
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SharingRoleUpdateData <- R6::R6Class(
  "SharingRoleUpdateData",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(id = NULL, role = NULL, shareRecipientType = NULL, username = NULL) {
      if (!is.null(`role`)) {
        stopifnot(is.character(`role`), length(`role`) == 1)
      }
      if (!is.null(`shareRecipientType`)) {
        stopifnot(is.character(`shareRecipientType`), length(`shareRecipientType`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
    }
  ),
  public = list(
    `id` = NULL,
    `role` = NULL,
    `shareRecipientType` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param id The ID of the recipient
    #' @param role The assigned role
    #' @param shareRecipientType The recipient type
    #' @param username The name of the user, group, or organization
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`role` = NULL, `shareRecipientType` = NULL, `id` = NULL, `username` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`role`, `shareRecipientType`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(id, role, shareRecipientType, username)
      }
      self$`id` <- `id`
      self$`role` <- `role`
      self$`shareRecipientType` <- `shareRecipientType`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(id = self$`id`, role = self$`role`, shareRecipientType = self$`shareRecipientType`, username = self$`username`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`role`)) {
          sprintf(
            '"role":
            "%s"
                  ',
            self$`role`
          )
        },
        if (!is.null(self$`shareRecipientType`)) {
          sprintf(
            '"shareRecipientType":
            "%s"
                  ',
            self$`shareRecipientType`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SharingRoleUpdateDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SharingRoleUpdateDataJson, validateParams = FALSE) {
      SharingRoleUpdateDataObject <- jsonlite::fromJSON(SharingRoleUpdateDataJson)
      self$`id` <- SharingRoleUpdateDataObject$`id`
      self$`role` <- SharingRoleUpdateDataObject$`role`
      self$`shareRecipientType` <- SharingRoleUpdateDataObject$`shareRecipientType`
      self$`username` <- SharingRoleUpdateDataObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
