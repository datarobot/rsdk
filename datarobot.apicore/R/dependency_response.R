# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DependencyResponse
#'
#' @description DependencyResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field constraints  list( \link{DependencyConstraint} ) Constraints that should be applied to the dependency when installed.
#'
#' @field line  character The original line from the requirements.txt file.
#'
#' @field lineNumber  integer The line number the requirement was on in requirements.txt.
#'
#' @field packageName  character The dependency&#39;s package name.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DependencyResponse <- R6::R6Class(
  "DependencyResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`constraints` = NULL, `line` = NULL, `lineNumber` = NULL, `packageName` = NULL) {
      if (!is.null(`constraints`)) {
        stopifnot(is.vector(`constraints`))
      }
      if (!is.null(`line`)) {
        stopifnot(is.character(`line`), length(`line`) == 1)
      }
      if (!is.null(`lineNumber`)) {
        stopifnot(is.numeric(`lineNumber`), length(`lineNumber`) == 1)
      }
      if (!is.null(`packageName`)) {
        stopifnot(is.character(`packageName`), length(`packageName`) == 1)
      }
    }
  ),
  public = list(
    `constraints` = NULL,
    `line` = NULL,
    `lineNumber` = NULL,
    `packageName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param constraints Constraints that should be applied to the dependency when installed.
    #' @param line The original line from the requirements.txt file.
    #' @param lineNumber The line number the requirement was on in requirements.txt.
    #' @param packageName The dependency&#39;s package name.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`constraints` = NULL, `line` = NULL, `lineNumber` = NULL, `packageName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`constraints`, `line`, `lineNumber`, `packageName`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(constraints, line, lineNumber, packageName)
      }
      sapply(`constraints`, function(x) stopifnot(R6::is.R6(x)))
      self$`line` <- `line`
      self$`lineNumber` <- `lineNumber`
      self$`packageName` <- `packageName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(constraints = self$`constraints`, line = self$`line`, lineNumber = self$`lineNumber`, packageName = self$`packageName`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`constraints`)) {
          sprintf(
            '"constraints":
            [%s]
      ',
            paste(sapply(self$`constraints`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`line`)) {
          sprintf(
            '"line":
            "%s"
                  ',
            self$`line`
          )
        },
        if (!is.null(self$`lineNumber`)) {
          sprintf(
            '"lineNumber":
            %d
                  ',
            self$`lineNumber`
          )
        },
        if (!is.null(self$`packageName`)) {
          sprintf(
            '"packageName":
            "%s"
                  ',
            self$`packageName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DependencyResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DependencyResponseJson, validateParams = FALSE) {
      DependencyResponseObject <- jsonlite::fromJSON(DependencyResponseJson)
      self$`constraints` <- ApiClient$new()$deserializeObj(DependencyResponseObject$`constraints`, "array[DependencyConstraint]", loadNamespace("datarobot.apicore"))
      self$`line` <- DependencyResponseObject$`line`
      self$`lineNumber` <- DependencyResponseObject$`lineNumber`
      self$`packageName` <- DependencyResponseObject$`packageName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
