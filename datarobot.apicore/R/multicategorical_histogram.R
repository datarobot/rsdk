# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MulticategoricalHistogram
#'
#' @description MulticategoricalHistogram Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featureName  character Feature name.
#'
#' @field histogram  list( \link{MulticategoricalHistogramItem} ) Feature histogram.
#'
#' @field projectId  character Project Id.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MulticategoricalHistogram <- R6::R6Class(
  "MulticategoricalHistogram",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`featureName` = NULL, `histogram` = NULL, `projectId` = NULL) {
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`histogram`)) {
        stopifnot(is.vector(`histogram`), sapply(`histogram`, R6::is.R6))
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `featureName` = NULL,
    `histogram` = NULL,
    `projectId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featureName Feature name.
    #' @param histogram Feature histogram.
    #' @param projectId Project Id.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featureName` = NULL, `histogram` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`featureName`, `histogram`, `projectId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(featureName, histogram, projectId)
      }
      self$`featureName` <- `featureName`
      self$`histogram` <- `histogram`
      self$`projectId` <- `projectId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(featureName = self$`featureName`, histogram = self$`histogram`, projectId = self$`projectId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`histogram`)) {
          sprintf(
            '"histogram":
            [%s]
      ',
            paste(sapply(self$`histogram`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MulticategoricalHistogramJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MulticategoricalHistogramJson, validateParams = FALSE) {
      MulticategoricalHistogramObject <- jsonlite::fromJSON(MulticategoricalHistogramJson)
      self$`featureName` <- MulticategoricalHistogramObject$`featureName`
      self$`histogram` <- ApiClient$new()$deserializeObj(MulticategoricalHistogramObject$`histogram`, "array[MulticategoricalHistogramItem]", loadNamespace("datarobot.apicore"))
      self$`projectId` <- MulticategoricalHistogramObject$`projectId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
