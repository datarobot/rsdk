# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AnomalyInsightTableData
#'
#' @description AnomalyInsightTableData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field columns  list( character ) array of columns that contain columns from training dataset and &#x60;anomalyScore&#x60; column.
#'
#' @field data  list( numeric ) array of arrays with actual data. Order in each array corresponds to order in columns array.
#'
#' @field rowId  list( integer ) index 0-based array. Each rowId corresponds to the actual row number of training data
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AnomalyInsightTableData <- R6::R6Class(
  "AnomalyInsightTableData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`columns` = NULL, `data` = NULL, `rowId` = NULL) {
      if (!is.null(`columns`)) {
        stopifnot(is.vector(`columns`), sapply(`columns`, is.character))
      }
      if (!is.null(`data`)) {
        stopifnot(is.vector(`data`), sapply(`data`, is.numeric))
      }
      if (!is.null(`rowId`)) {
        stopifnot(is.vector(`rowId`), sapply(`rowId`, is.numeric))
      }
    }
  ),
  public = list(
    `columns` = NULL,
    `data` = NULL,
    `rowId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param columns array of columns that contain columns from training dataset and &#x60;anomalyScore&#x60; column.
    #' @param data array of arrays with actual data. Order in each array corresponds to order in columns array.
    #' @param rowId index 0-based array. Each rowId corresponds to the actual row number of training data
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`columns` = NULL, `data` = NULL, `rowId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`columns`, `data`, `rowId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(columns, data, rowId)
      }
      self$`columns` <- `columns`
      self$`data` <- `data`
      self$`rowId` <- `rowId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(columns = self$`columns`, data = self$`data`, rowId = self$`rowId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`columns`)) {
          sprintf(
            '"columns":
            [%s]
                  ',
            paste(unlist(lapply(self$`columns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
                  ',
            paste(unlist(lapply(self$`data`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`rowId`)) {
          sprintf(
            '"rowId":
            [%s]
                  ',
            paste(unlist(lapply(self$`rowId`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AnomalyInsightTableDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AnomalyInsightTableDataJson, validateParams = FALSE) {
      AnomalyInsightTableDataObject <- jsonlite::fromJSON(AnomalyInsightTableDataJson)
      self$`columns` <- ApiClient$new()$deserializeObj(AnomalyInsightTableDataObject$`columns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`data` <- ApiClient$new()$deserializeObj(AnomalyInsightTableDataObject$`data`, "array[numeric]", loadNamespace("datarobot.apicore"))
      self$`rowId` <- ApiClient$new()$deserializeObj(AnomalyInsightTableDataObject$`rowId`, "array[integer]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
