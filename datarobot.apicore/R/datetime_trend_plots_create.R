# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatetimeTrendPlotsCreate
#'
#' @description DatetimeTrendPlotsCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtest  \link{OneOfintegerstring} [optional] Compute plots for a specific backtest (use the backtest index starting from zero) or &#x60;holdout&#x60;. If not specified the first backtest (backtest index 0) will be used.
#'
#' @field forecastDistanceEnd  integer [optional] The end of forecast distance range (forecast window) to compute. If not specified, the last forecast distance for this project will be used. Forecast distance specifies the number of time steps between the predicted point and the origin point. Only available for time series supervised projects.
#'
#' @field forecastDistanceStart  integer [optional] The start of forecast distance range (forecast window) to compute. If not specified, the first forecast distance for this project will be used. Forecast distance specifies the number of time steps between the predicted point and the origin point. Only available for time series supervised projects.
#'
#' @field fullAverage  character [optional] Whether to compute an average plot for all series. Only available for time series multiseries projects.
#'
#' @field seriesIds  list( character ) [optional] Only available for time series multiseries projects. Each element should be a name of a single series in a multiseries project. It is possible to compute a maximum of 1000 series per one request. If not specified the first 1000 series in alphabetical order will be computed. It is not possible to specify &#x60;fullAverage: true&#x60; while also setting &#x60;seriesIds&#x60;. This parameter can only be specified after first 1000 series in alphabetical order are computed.
#'
#' @field source  character [optional] The source of the data for the backtest/holdout.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatetimeTrendPlotsCreate <- R6::R6Class(
  "DatetimeTrendPlotsCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`backtest` = NULL, `forecastDistanceEnd` = NULL, `forecastDistanceStart` = NULL, `fullAverage` = NULL, `seriesIds` = NULL, `source` = NULL) {
      if (!is.null(`backtest`)) {
        .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = backtest)
      }
      if (!is.null(`forecastDistanceEnd`)) {
        stopifnot(is.numeric(`forecastDistanceEnd`), length(`forecastDistanceEnd`) == 1)
      }
      if (!is.null(`forecastDistanceStart`)) {
        stopifnot(is.numeric(`forecastDistanceStart`), length(`forecastDistanceStart`) == 1)
      }
      if (!is.null(`fullAverage`)) {
        stopifnot(is.logical(`fullAverage`), length(`fullAverage`) == 1)
      }
      if (!is.null(`seriesIds`) && length(`seriesIds`) > 0) {
        stopifnot(is.vector(`seriesIds`), sapply(`seriesIds`, is.character))
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
    }
  ),
  public = list(
    `backtest` = NULL,
    `forecastDistanceEnd` = NULL,
    `forecastDistanceStart` = NULL,
    `fullAverage` = NULL,
    `seriesIds` = NULL,
    `source` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtest Compute plots for a specific backtest (use the backtest index starting from zero) or &#x60;holdout&#x60;. If not specified the first backtest (backtest index 0) will be used.
    #' @param forecastDistanceEnd The end of forecast distance range (forecast window) to compute. If not specified, the last forecast distance for this project will be used. Forecast distance specifies the number of time steps between the predicted point and the origin point. Only available for time series supervised projects.
    #' @param forecastDistanceStart The start of forecast distance range (forecast window) to compute. If not specified, the first forecast distance for this project will be used. Forecast distance specifies the number of time steps between the predicted point and the origin point. Only available for time series supervised projects.
    #' @param fullAverage Whether to compute an average plot for all series. Only available for time series multiseries projects.
    #' @param seriesIds Only available for time series multiseries projects. Each element should be a name of a single series in a multiseries project. It is possible to compute a maximum of 1000 series per one request. If not specified the first 1000 series in alphabetical order will be computed. It is not possible to specify &#x60;fullAverage: true&#x60; while also setting &#x60;seriesIds&#x60;. This parameter can only be specified after first 1000 series in alphabetical order are computed.
    #' @param source The source of the data for the backtest/holdout.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`backtest` = NULL, `forecastDistanceEnd` = NULL, `forecastDistanceStart` = NULL, `fullAverage` = FALSE, `seriesIds` = NULL, `source` = "validation", validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(backtest, forecastDistanceEnd, forecastDistanceStart, fullAverage, seriesIds, source)
      }
      self$`backtest` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = backtest)
      self$`forecastDistanceEnd` <- `forecastDistanceEnd`
      self$`forecastDistanceStart` <- `forecastDistanceStart`
      self$`fullAverage` <- `fullAverage`
      self$`seriesIds` <- `seriesIds`
      self$`source` <- `source`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(backtest = self$`backtest`, forecastDistanceEnd = self$`forecastDistanceEnd`, forecastDistanceStart = self$`forecastDistanceStart`, fullAverage = self$`fullAverage`, seriesIds = self$`seriesIds`, source = self$`source`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtest`)) {
          sprintf(
            '"backtest":
            %s
      ',
            self$`backtest`
          )
        },
        if (!is.null(self$`forecastDistanceEnd`)) {
          sprintf(
            '"forecastDistanceEnd":
            %d
                  ',
            self$`forecastDistanceEnd`
          )
        },
        if (!is.null(self$`forecastDistanceStart`)) {
          sprintf(
            '"forecastDistanceStart":
            %d
                  ',
            self$`forecastDistanceStart`
          )
        },
        if (!is.null(self$`fullAverage`)) {
          sprintf(
            '"fullAverage":
            %s
                  ',
            tolower(self$`fullAverage`)
          )
        },
        if (!is.null(self$`seriesIds`)) {
          sprintf(
            '"seriesIds":
            [%s]
                  ',
            paste(unlist(lapply(self$`seriesIds`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatetimeTrendPlotsCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatetimeTrendPlotsCreateJson, validateParams = FALSE) {
      DatetimeTrendPlotsCreateObject <- jsonlite::fromJSON(DatetimeTrendPlotsCreateJson)
      self$`backtest` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = DatetimeTrendPlotsCreateObject$backtest)
      self$`forecastDistanceEnd` <- DatetimeTrendPlotsCreateObject$`forecastDistanceEnd`
      self$`forecastDistanceStart` <- DatetimeTrendPlotsCreateObject$`forecastDistanceStart`
      self$`fullAverage` <- DatetimeTrendPlotsCreateObject$`fullAverage`
      self$`seriesIds` <- ApiClient$new()$deserializeObj(DatetimeTrendPlotsCreateObject$`seriesIds`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`source` <- DatetimeTrendPlotsCreateObject$`source`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
