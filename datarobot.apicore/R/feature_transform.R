# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureTransform
#'
#' @description FeatureTransform Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dateExtraction  character [optional] The value to extract from the date column, of these options: &#x60;[year|yearDay|month|monthDay|week|weekDay]&#x60;. Required for transformation of a date column. Otherwise must not be provided.
#'
#' @field name  character The name of the new feature. Must not be the same as any existing features for this project. Must not contain &#39;/&#39; character.
#'
#' @field parentName  character The name of the parent feature.
#'
#' @field replacement  \link{AnyOfstringbooleannumberinteger} [optional] The replacement in case of a failed transformation.
#'
#' @field variableType  character The type of the new feature. Must be one of &#x60;text&#x60;, &#x60;categorical&#x60; (Deprecated in version v2.21), &#x60;numeric&#x60;, or &#x60;categoricalInt&#x60;. See the description of this method for more information.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureTransform <- R6::R6Class(
  "FeatureTransform",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(dateExtraction = NULL, name = NULL, parentName = NULL, replacement = NULL, variableType = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`parentName`)) {
        stopifnot(is.character(`parentName`), length(`parentName`) == 1)
      }
      if (!is.null(`variableType`)) {
        stopifnot(is.character(`variableType`), length(`variableType`) == 1)
      }
      if (!is.null(`dateExtraction`)) {
        stopifnot(is.character(`dateExtraction`), length(`dateExtraction`) == 1)
      }
      if (!is.null(`replacement`)) {
        .setPrimitiveProperty(typeList = list("character", "boolean", "numeric"), propertyData = replacement)
      }
    }
  ),
  public = list(
    `dateExtraction` = NULL,
    `name` = NULL,
    `parentName` = NULL,
    `replacement` = NULL,
    `variableType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dateExtraction The value to extract from the date column, of these options: &#x60;[year|yearDay|month|monthDay|week|weekDay]&#x60;. Required for transformation of a date column. Otherwise must not be provided.
    #' @param name The name of the new feature. Must not be the same as any existing features for this project. Must not contain &#39;/&#39; character.
    #' @param parentName The name of the parent feature.
    #' @param replacement The replacement in case of a failed transformation.
    #' @param variableType The type of the new feature. Must be one of &#x60;text&#x60;, &#x60;categorical&#x60; (Deprecated in version v2.21), &#x60;numeric&#x60;, or &#x60;categoricalInt&#x60;. See the description of this method for more information.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `parentName` = NULL, `variableType` = NULL, `dateExtraction` = NULL, `replacement` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`name`, `parentName`, `variableType`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(dateExtraction, name, parentName, replacement, variableType)
      }
      self$`dateExtraction` <- `dateExtraction`
      self$`name` <- `name`
      self$`parentName` <- `parentName`
      self$`replacement` <- .setPrimitiveProperty(typeList = list("character", "boolean", "numeric"), propertyData = replacement)
      self$`variableType` <- `variableType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(dateExtraction = self$`dateExtraction`, name = self$`name`, parentName = self$`parentName`, replacement = self$`replacement`, variableType = self$`variableType`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dateExtraction`)) {
          sprintf(
            '"dateExtraction":
            "%s"
                  ',
            self$`dateExtraction`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`parentName`)) {
          sprintf(
            '"parentName":
            "%s"
                  ',
            self$`parentName`
          )
        },
        if (!is.null(self$`replacement`)) {
          sprintf(
            '"replacement":
          %s
        ',
            self$`replacement`
          )
        },
        if (!is.null(self$`variableType`)) {
          sprintf(
            '"variableType":
            "%s"
                  ',
            self$`variableType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureTransformJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureTransformJson, validateParams = FALSE) {
      FeatureTransformObject <- jsonlite::fromJSON(FeatureTransformJson)
      self$`dateExtraction` <- FeatureTransformObject$`dateExtraction`
      self$`name` <- FeatureTransformObject$`name`
      self$`parentName` <- FeatureTransformObject$`parentName`
      self$`replacement` <- .setPrimitiveProperty(typeList = list("character", "boolean", "numeric"), propertyData = FeatureTransformObject$replacement)
      self$`variableType` <- FeatureTransformObject$`variableType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
