# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintTaskArgument
#'
#' @description UserBlueprintTaskArgument Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field argument  \link{OneOfUserBlueprintTaskArgumentDefinition} The definition of a task argument, used to specify a certain aspect of the task.
#'
#' @field key  character The unique key of the argument
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintTaskArgument <- R6::R6Class(
  "UserBlueprintTaskArgument",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(argument = NULL, key = NULL) {
      if (!is.null(`argument`)) {
        .setComplexProperty(typeList = list(UserBlueprintTaskArgumentDefinition), propertyData = argument)
      }
      if (!is.null(`key`)) {
        stopifnot(is.character(`key`), length(`key`) == 1)
      }
    }
  ),
  public = list(
    `argument` = NULL,
    `key` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param argument The definition of a task argument, used to specify a certain aspect of the task.
    #' @param key The unique key of the argument
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`argument` = NULL, `key` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`argument`, `key`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(argument, key)
      }
      self$`argument` <- .setComplexProperty(typeList = list(UserBlueprintTaskArgumentDefinition), propertyData = argument)
      self$`key` <- `key`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(argument = self$`argument`, key = self$`key`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`argument`)) {
          sprintf(
            '"argument":
          %s
        ',
            jsonlite::toJSON(self$`argument`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`key`)) {
          sprintf(
            '"key":
            "%s"
                  ',
            self$`key`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintTaskArgumentJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintTaskArgumentJson, validateParams = FALSE) {
      UserBlueprintTaskArgumentObject <- jsonlite::fromJSON(UserBlueprintTaskArgumentJson)
      self$`argument` <- .setComplexProperty(typeList = list(UserBlueprintTaskArgumentDefinition), propertyData = UserBlueprintTaskArgumentObject$argument)
      self$`key` <- UserBlueprintTaskArgumentObject$`key`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
