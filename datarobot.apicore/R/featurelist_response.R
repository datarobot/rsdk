# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeaturelistResponse
#'
#' @description FeaturelistResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field created  character A :ref:&#x60;timestamp &lt;time_format&gt;&#x60; string specifying when the featurelist was created.
#'
#' @field description  character User-friendly description of the featurelist, which can be updated by users.
#'
#' @field features  list( character ) Names of features included in the featurelist.
#'
#' @field id  character Featurelist ID.
#'
#' @field isUserCreated  character Whether the featurelist was created manually by a user or by DataRobot automation.
#'
#' @field name  character the name of the featurelist
#'
#' @field numModels  integer The number of models that currently use this featurelist. A model is considered to use a featurelist if it is used to train the model or as a monotonic constraint featurelist, or if the model is a blender with at least one component model using the featurelist.
#'
#' @field projectId  character Project ID the featurelist belongs to.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeaturelistResponse <- R6::R6Class(
  "FeaturelistResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`created` = NULL, `description` = NULL, `features` = NULL, `id` = NULL, `isUserCreated` = NULL, `name` = NULL, `numModels` = NULL, `projectId` = NULL) {
      if (!is.null(`created`)) {
        stopifnot(is.character(`created`), length(`created`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`), sapply(`features`, is.character))
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`isUserCreated`)) {
        stopifnot(is.logical(`isUserCreated`), length(`isUserCreated`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`numModels`)) {
        stopifnot(is.numeric(`numModels`), length(`numModels`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `created` = NULL,
    `description` = NULL,
    `features` = NULL,
    `id` = NULL,
    `isUserCreated` = NULL,
    `name` = NULL,
    `numModels` = NULL,
    `projectId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param created A :ref:&#x60;timestamp &lt;time_format&gt;&#x60; string specifying when the featurelist was created.
    #' @param description User-friendly description of the featurelist, which can be updated by users.
    #' @param features Names of features included in the featurelist.
    #' @param id Featurelist ID.
    #' @param isUserCreated Whether the featurelist was created manually by a user or by DataRobot automation.
    #' @param name the name of the featurelist
    #' @param numModels The number of models that currently use this featurelist. A model is considered to use a featurelist if it is used to train the model or as a monotonic constraint featurelist, or if the model is a blender with at least one component model using the featurelist.
    #' @param projectId Project ID the featurelist belongs to.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`created` = NULL, `description` = NULL, `features` = NULL, `id` = NULL, `isUserCreated` = NULL, `name` = NULL, `numModels` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`created`, `description`, `features`, `id`, `isUserCreated`, `name`, `numModels`, `projectId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(created, description, features, id, isUserCreated, name, numModels, projectId)
      }
      self$`created` <- `created`
      self$`description` <- `description`
      self$`features` <- `features`
      self$`id` <- `id`
      self$`isUserCreated` <- `isUserCreated`
      self$`name` <- `name`
      self$`numModels` <- `numModels`
      self$`projectId` <- `projectId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(created = self$`created`, description = self$`description`, features = self$`features`, id = self$`id`, isUserCreated = self$`isUserCreated`, name = self$`name`, numModels = self$`numModels`, projectId = self$`projectId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`created`)) {
          sprintf(
            '"created":
            "%s"
                  ',
            self$`created`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
                  ',
            paste(unlist(lapply(self$`features`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`isUserCreated`)) {
          sprintf(
            '"isUserCreated":
            %s
                  ',
            tolower(self$`isUserCreated`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`numModels`)) {
          sprintf(
            '"numModels":
            %d
                  ',
            self$`numModels`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeaturelistResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeaturelistResponseJson, validateParams = FALSE) {
      FeaturelistResponseObject <- jsonlite::fromJSON(FeaturelistResponseJson)
      self$`created` <- FeaturelistResponseObject$`created`
      self$`description` <- FeaturelistResponseObject$`description`
      self$`features` <- ApiClient$new()$deserializeObj(FeaturelistResponseObject$`features`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`id` <- FeaturelistResponseObject$`id`
      self$`isUserCreated` <- FeaturelistResponseObject$`isUserCreated`
      self$`name` <- FeaturelistResponseObject$`name`
      self$`numModels` <- FeaturelistResponseObject$`numModels`
      self$`projectId` <- FeaturelistResponseObject$`projectId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
