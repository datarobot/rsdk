# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BiasAndFairness
#'
#' @description BiasAndFairness Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field fairnessMetricsSet  character A set of fairness metrics to use for calculating fairness.
#'
#' @field fairnessThreshold  numeric Threshold value of the fairness metric. Can be in a range of &#x60;&#x60;[0:1]&#x60;&#x60;.
#'
#' @field preferableTargetValue  \link{AnyOfbooleanintegerstring} A target value that should be treated as a positive outcome for the prediction. For example if we want to check gender discrimination for giving a loan and our target named &#x60;&#x60;is_bad&#x60;&#x60;, then the positive outcome for the prediction would be &#x60;&#x60;No&#x60;&#x60;, meaning that the loan is good and that&#39;s what we treat as a preferable result for the loaner.
#'
#' @field protectedFeatures  list( character ) A list of feature to mark as protected for Bias and Fairness measurement. A common example of those would &#x60;race&#x60;, &#x60;gender&#x60;, &#x60;age_group&#x60;. All protected features should be tracked as segment attributes. Only categorical features can be marked as protected.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BiasAndFairness <- R6::R6Class(
  "BiasAndFairness",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`fairnessMetricsSet` = NULL, `fairnessThreshold` = NULL, `preferableTargetValue` = NULL, `protectedFeatures` = NULL) {
      if (!is.null(`fairnessMetricsSet`)) {
        stopifnot(is.character(`fairnessMetricsSet`), length(`fairnessMetricsSet`) == 1)
      }
      if (!is.null(`fairnessThreshold`)) {
      }
      if (!is.null(`preferableTargetValue`)) {
        .setPrimitiveProperty(typeList = list("logical", "numeric", "character"), propertyData = preferableTargetValue)
      }
      if (!is.null(`protectedFeatures`)) {
        stopifnot(is.vector(`protectedFeatures`), sapply(`protectedFeatures`, is.character))
      }
    }
  ),
  public = list(
    `fairnessMetricsSet` = NULL,
    `fairnessThreshold` = NULL,
    `preferableTargetValue` = NULL,
    `protectedFeatures` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param fairnessMetricsSet A set of fairness metrics to use for calculating fairness.
    #' @param fairnessThreshold Threshold value of the fairness metric. Can be in a range of &#x60;&#x60;[0:1]&#x60;&#x60;.
    #' @param preferableTargetValue A target value that should be treated as a positive outcome for the prediction. For example if we want to check gender discrimination for giving a loan and our target named &#x60;&#x60;is_bad&#x60;&#x60;, then the positive outcome for the prediction would be &#x60;&#x60;No&#x60;&#x60;, meaning that the loan is good and that&#39;s what we treat as a preferable result for the loaner.
    #' @param protectedFeatures A list of feature to mark as protected for Bias and Fairness measurement. A common example of those would &#x60;race&#x60;, &#x60;gender&#x60;, &#x60;age_group&#x60;. All protected features should be tracked as segment attributes. Only categorical features can be marked as protected.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`fairnessMetricsSet` = NULL, `fairnessThreshold` = NULL, `preferableTargetValue` = NULL, `protectedFeatures` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`fairnessMetricsSet`, `fairnessThreshold`, `preferableTargetValue`, `protectedFeatures`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(fairnessMetricsSet, fairnessThreshold, preferableTargetValue, protectedFeatures)
      }
      self$`fairnessMetricsSet` <- `fairnessMetricsSet`
      self$`fairnessThreshold` <- `fairnessThreshold`
      self$`preferableTargetValue` <- .setPrimitiveProperty(typeList = list("logical", "numeric", "character"), propertyData = preferableTargetValue)
      self$`protectedFeatures` <- `protectedFeatures`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(fairnessMetricsSet = self$`fairnessMetricsSet`, fairnessThreshold = self$`fairnessThreshold`, preferableTargetValue = self$`preferableTargetValue`, protectedFeatures = self$`protectedFeatures`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`fairnessMetricsSet`)) {
          sprintf(
            '"fairnessMetricsSet":
            "%s"
                  ',
            self$`fairnessMetricsSet`
          )
        },
        if (!is.null(self$`fairnessThreshold`)) {
          sprintf(
            '"fairnessThreshold":
            %d
                  ',
            self$`fairnessThreshold`
          )
        },
        if (!is.null(self$`preferableTargetValue`)) {
          sprintf(
            '"preferableTargetValue":
            %s
      ',
            self$`preferableTargetValue`
          )
        },
        if (!is.null(self$`protectedFeatures`)) {
          sprintf(
            '"protectedFeatures":
            [%s]
                  ',
            paste(unlist(lapply(self$`protectedFeatures`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BiasAndFairnessJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BiasAndFairnessJson, validateParams = FALSE) {
      BiasAndFairnessObject <- jsonlite::fromJSON(BiasAndFairnessJson)
      self$`fairnessMetricsSet` <- BiasAndFairnessObject$`fairnessMetricsSet`
      self$`fairnessThreshold` <- BiasAndFairnessObject$`fairnessThreshold`
      self$`preferableTargetValue` <- .setPrimitiveProperty(typeList = list("logical", "numeric", "character"), propertyData = BiasAndFairnessObject$preferableTargetValue)
      self$`protectedFeatures` <- ApiClient$new()$deserializeObj(BiasAndFairnessObject$`protectedFeatures`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
