# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DependencyConstraint
#'
#' @description DependencyConstraint Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field constraintType  character The constraint type to apply to the version.
#'
#' @field version  character The version label to use in the constraint.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DependencyConstraint <- R6::R6Class(
  "DependencyConstraint",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(constraintType = NULL, version = NULL) {
      if (!is.null(`constraintType`)) {
        stopifnot(is.character(`constraintType`), length(`constraintType`) == 1)
      }
      if (!is.null(`version`)) {
        stopifnot(is.character(`version`), length(`version`) == 1)
      }
    }
  ),
  public = list(
    `constraintType` = NULL,
    `version` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param constraintType The constraint type to apply to the version.
    #' @param version The version label to use in the constraint.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`constraintType` = NULL, `version` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`constraintType`, `version`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(constraintType, version)
      }
      self$`constraintType` <- `constraintType`
      self$`version` <- `version`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(constraintType = self$`constraintType`, version = self$`version`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`constraintType`)) {
          sprintf(
            '"constraintType":
            "%s"
                  ',
            self$`constraintType`
          )
        },
        if (!is.null(self$`version`)) {
          sprintf(
            '"version":
            "%s"
                  ',
            self$`version`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DependencyConstraintJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DependencyConstraintJson, validateParams = FALSE) {
      DependencyConstraintObject <- jsonlite::fromJSON(DependencyConstraintJson)
      self$`constraintType` <- DependencyConstraintObject$`constraintType`
      self$`version` <- DependencyConstraintObject$`version`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
