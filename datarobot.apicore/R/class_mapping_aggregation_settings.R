# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ClassMappingAggregationSettings
#'
#' @description ClassMappingAggregationSettings Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field aggregationClassName  character [optional] The name of the class that will be assigned to all rows with aggregated classes. Should not match any excluded_from_aggregation or we will have 2 classes with the same name and no way to distinguish between them. This option is only available formulticlass projects. By default &#39;DR_RARE_TARGET_VALUES&#39; is used.
#'
#' @field excludedFromAggregation  list( character ) [optional] List of target values that should be guaranteed to kept as is, regardless of other settings.
#'
#' @field maxUnaggregatedClassValues  integer [optional] The maximum number of unique labels before aggregation kicks in. Should be at least len(excludedFromAggregation) + 1 for multiclass and at least len(excludedFromAggregation) for multilabel.
#'
#' @field minClassSupport  integer [optional] Minimum number of instances necessary for each target value in the dataset. All values with fewer instances than this value will be aggregated
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ClassMappingAggregationSettings <- R6::R6Class(
  "ClassMappingAggregationSettings",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`aggregationClassName` = NULL, `excludedFromAggregation` = NULL, `maxUnaggregatedClassValues` = NULL, `minClassSupport` = NULL) {
      if (!is.null(`aggregationClassName`)) {
        stopifnot(is.character(`aggregationClassName`), length(`aggregationClassName`) == 1)
      }
      if (!is.null(`excludedFromAggregation`) && length(`excludedFromAggregation`) > 0) {
        stopifnot(is.vector(`excludedFromAggregation`), sapply(`excludedFromAggregation`, is.character))
      }
      if (!is.null(`maxUnaggregatedClassValues`)) {
        stopifnot(is.numeric(`maxUnaggregatedClassValues`), length(`maxUnaggregatedClassValues`) == 1)
      }
      if (!is.null(`minClassSupport`)) {
        stopifnot(is.numeric(`minClassSupport`), length(`minClassSupport`) == 1)
      }
    }
  ),
  public = list(
    `aggregationClassName` = NULL,
    `excludedFromAggregation` = NULL,
    `maxUnaggregatedClassValues` = NULL,
    `minClassSupport` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param aggregationClassName The name of the class that will be assigned to all rows with aggregated classes. Should not match any excluded_from_aggregation or we will have 2 classes with the same name and no way to distinguish between them. This option is only available formulticlass projects. By default &#39;DR_RARE_TARGET_VALUES&#39; is used.
    #' @param excludedFromAggregation List of target values that should be guaranteed to kept as is, regardless of other settings.
    #' @param maxUnaggregatedClassValues The maximum number of unique labels before aggregation kicks in. Should be at least len(excludedFromAggregation) + 1 for multiclass and at least len(excludedFromAggregation) for multilabel.
    #' @param minClassSupport Minimum number of instances necessary for each target value in the dataset. All values with fewer instances than this value will be aggregated
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`aggregationClassName` = NULL, `excludedFromAggregation` = c(), `maxUnaggregatedClassValues` = 1000, `minClassSupport` = 1, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(aggregationClassName, excludedFromAggregation, maxUnaggregatedClassValues, minClassSupport)
      }
      self$`aggregationClassName` <- `aggregationClassName`
      self$`excludedFromAggregation` <- `excludedFromAggregation`
      self$`maxUnaggregatedClassValues` <- `maxUnaggregatedClassValues`
      self$`minClassSupport` <- `minClassSupport`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(aggregationClassName = self$`aggregationClassName`, excludedFromAggregation = self$`excludedFromAggregation`, maxUnaggregatedClassValues = self$`maxUnaggregatedClassValues`, minClassSupport = self$`minClassSupport`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`aggregationClassName`)) {
          sprintf(
            '"aggregationClassName":
            "%s"
                  ',
            self$`aggregationClassName`
          )
        },
        if (!is.null(self$`excludedFromAggregation`)) {
          sprintf(
            '"excludedFromAggregation":
            [%s]
                  ',
            paste(unlist(lapply(self$`excludedFromAggregation`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`maxUnaggregatedClassValues`)) {
          sprintf(
            '"maxUnaggregatedClassValues":
            %d
                  ',
            self$`maxUnaggregatedClassValues`
          )
        },
        if (!is.null(self$`minClassSupport`)) {
          sprintf(
            '"minClassSupport":
            %d
                  ',
            self$`minClassSupport`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ClassMappingAggregationSettingsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ClassMappingAggregationSettingsJson, validateParams = FALSE) {
      ClassMappingAggregationSettingsObject <- jsonlite::fromJSON(ClassMappingAggregationSettingsJson)
      self$`aggregationClassName` <- ClassMappingAggregationSettingsObject$`aggregationClassName`
      self$`excludedFromAggregation` <- ApiClient$new()$deserializeObj(ClassMappingAggregationSettingsObject$`excludedFromAggregation`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`maxUnaggregatedClassValues` <- ClassMappingAggregationSettingsObject$`maxUnaggregatedClassValues`
      self$`minClassSupport` <- ClassMappingAggregationSettingsObject$`minClassSupport`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
