# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Credentials operations
#' @description datarobot.apicore.Credentials
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#' @export
CredentialsApi <- R6::R6Class(
  "CredentialsApi",
  public = list(
    apiClient = NULL,

    #' @param apiClient A configurable `ApiClient` instance. If none provided, a new client with default configuration will be created.
    initialize = function(apiClient) {
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      } else {
        self$apiClient <- ApiClient$new()
      }
    },
    #' @description List all objects associated with the specified credentials.
    #' Produces: "application/json"
    #'
    #' @details List all objects associated with specified credentials for the current user.
    #' @param credentialId character. Credentials entity ID.
    #' @param offset integer. Number of results to skip.
    #' @param limit integer. At most this many results are returned. The default may change without notice.
    #' @param types \link{OneOfstringarray}. Includes only credentials of the specified type. Repeat the parameter for filtering on multiple statuses.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{CredentialsListAssociationsResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Objects associated with the specified credentials for the current user.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' credentialId <- 'credentialId_example' # character | Credentials entity ID.
    #' offset <- 0 # integer | Number of results to skip.
    #' limit <- 100 # integer | At most this many results are returned. The default may change without notice.
    #' types <- OneOfstringarray$new() # OneOfstringarray | Includes only credentials of the specified type. Repeat the parameter for filtering on multiple statuses.
    #'
    #' api.instance <- CredentialsApi$new()
    #' result <- api.instance$CredentialsAssociationsList(credentialId, offset=offset, limit=limit, types=types)
    #' }
    CredentialsAssociationsList = function(credentialId, offset = 0, limit = 100, types = NULL, ...) {
      apiResponse <- private$CredentialsAssociationsListWithHttpInfo(credentialId, offset, limit, types, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Add objects associated with credentials
    #' Produces: NA
    #'
    #' @details Add objects associated with credentials
    #' @param credentialId character. Credentials entity ID.
    #' @param credentialsAssociationUpdate \link{CredentialsAssociationUpdate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`204`** Credentials association updated
    #' \itemize{
    #' }
    #' \item **`404`** Requested credentials not found
    #' \itemize{
    #' }
    #' \item **`409`** One or more associations already exists when adding or one or more associations do not exist
    #' \itemize{
    #' }
    #' \item **`422`** Must specify a field to update or must specify only one of &#x60;credentialsToAdd&#x60; or &#x60;credentialsToRemove&#x60;.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' credentialId <- 'credentialId_example' # character | Credentials entity ID.
    #' credentialsAssociationUpdate <- CredentialsAssociationUpdate$new() # CredentialsAssociationUpdate |
    #'
    #' api.instance <- CredentialsApi$new()
    #' result <- api.instance$CredentialsAssociationsPatchMany(credentialId, credentialsAssociationUpdate=credentialsAssociationUpdate)
    #' }
    CredentialsAssociationsPatchMany = function(credentialId, credentialsAssociationUpdate = NULL, ...) {
      apiResponse <- private$CredentialsAssociationsPatchManyWithHttpInfo(credentialId, credentialsAssociationUpdate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Store a new set of credentials which can be used for data sources creation.
    #' Produces: "application/json"
    #'
    #' @details Store a new set of credentials.
    #' @param credentialsBody \link{CredentialsBody}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{CreateCredentialsResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`201`** Credentials stored successfully.
    #' \itemize{
    #' }
    #' \item **`409`** The specified name is already in use.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' credentialsBody <- CredentialsBody$new() # CredentialsBody |
    #'
    #' api.instance <- CredentialsApi$new()
    #' result <- api.instance$CredentialsCreate(credentialsBody=credentialsBody)
    #' }
    CredentialsCreate = function(credentialsBody = NULL, ...) {
      apiResponse <- private$CredentialsCreateWithHttpInfo(credentialsBody, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Delete the credentials set.
    #' Produces: NA
    #'
    #' @details Delete the credential set matching the specified ID if it is not used by any data connection or batch prediction job.
    #' @param credentialId character. Credentials entity ID.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`** Credentials deleted successfully.
    #' \itemize{
    #' }
    #' \item **`409`** Credentials are in use by one or more data connections or batch prediction jobs.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' credentialId <- 'credentialId_example' # character | Credentials entity ID.
    #'
    #' api.instance <- CredentialsApi$new()
    #' result <- api.instance$CredentialsDelete(credentialId)
    #' }
    CredentialsDelete = function(credentialId, ...) {
      apiResponse <- private$CredentialsDeleteWithHttpInfo(credentialId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List credentials.
    #' Produces: "application/json"
    #'
    #' @details List all sets of credentials available for given user.
    #' @param offset integer. Number of results to skip.
    #' @param limit integer. At most this many results are returned. The default may change without notice.
    #' @param types \link{OneOfstringarray}. Includes only credentials of the specified type. Repeat the parameter for filtering on multiple statuses.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{CredentialsListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** A paginated list of credentials.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' offset <- 0 # integer | Number of results to skip.
    #' limit <- 100 # integer | At most this many results are returned. The default may change without notice.
    #' types <- OneOfstringarray$new() # OneOfstringarray | Includes only credentials of the specified type. Repeat the parameter for filtering on multiple statuses.
    #'
    #' api.instance <- CredentialsApi$new()
    #' result <- api.instance$CredentialsList(offset=offset, limit=limit, types=types)
    #' }
    CredentialsList = function(offset = 0, limit = 100, types = NULL, ...) {
      apiResponse <- private$CredentialsListWithHttpInfo(offset, limit, types, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Update specified credentials
    #' Produces: NA
    #'
    #' @details Update specified credentials
    #' @param credentialId character. Credentials entity ID.
    #' @param credentialsUpdate \link{CredentialsUpdate}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Set of credentials updated
    #' \itemize{
    #' }
    #' \item **`404`** Requested credentials not found
    #' \itemize{
    #' }
    #' \item **`409`** The name specified for credentials is already in use.
    #' \itemize{
    #' }
    #' \item **`422`** Must specify at least one field to update
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' credentialId <- 'credentialId_example' # character | Credentials entity ID.
    #' credentialsUpdate <- CredentialsUpdate$new() # CredentialsUpdate |
    #'
    #' api.instance <- CredentialsApi$new()
    #' result <- api.instance$CredentialsPatch(credentialId, credentialsUpdate=credentialsUpdate)
    #' }
    CredentialsPatch = function(credentialId, credentialsUpdate = NULL, ...) {
      apiResponse <- private$CredentialsPatchWithHttpInfo(credentialId, credentialsUpdate, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve the credentials set.
    #' Produces: "application/json"
    #'
    #' @details Return the credential set matching the specified ID.
    #' @param credentialId character. Credentials entity ID.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{CreateCredentialsResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Credentials retrieved successfully.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' credentialId <- 'credentialId_example' # character | Credentials entity ID.
    #'
    #' api.instance <- CredentialsApi$new()
    #' result <- api.instance$CredentialsRetrieve(credentialId)
    #' }
    CredentialsRetrieve = function(credentialId, ...) {
      apiResponse <- private$CredentialsRetrieveWithHttpInfo(credentialId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    }
  ),
  private = list(
    # A helper function to invoke the API operation `CredentialsAssociationsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    CredentialsAssociationsListWithHttpInfo = function(credentialId, offset = 0, limit = 100, types = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`credentialId`)) {
        stop("Missing required parameter `credentialId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["types"] <- types

      body <- NULL
      urlPath <- "/credentials/{credentialId}/associations/"
      if (!missing(`credentialId`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), URLencode(as.character(`credentialId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "CredentialsListAssociationsResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `CredentialsAssociationsPatchMany`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    CredentialsAssociationsPatchManyWithHttpInfo = function(credentialId, credentialsAssociationUpdate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`credentialId`)) {
        stop("Missing required parameter `credentialId`.")
      }

      if (!missing(`credentialsAssociationUpdate`) && isa(credentialsAssociationUpdate, c("CredentialsAssociationUpdate", "R6"))) {
        body <- `credentialsAssociationUpdate`$toJSON()
      } else {
        stop("CredentialsAssociationsPatchManyWithHttpInfo requires parameter credentialsAssociationUpdate to be of type CredentialsAssociationUpdate.")
      }

      urlPath <- "/credentials/{credentialId}/associations/"
      if (!missing(`credentialId`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), URLencode(as.character(`credentialId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `CredentialsCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    CredentialsCreateWithHttpInfo = function(credentialsBody = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (!missing(`credentialsBody`) && isa(credentialsBody, c("CredentialsBody", "R6"))) {
        body <- `credentialsBody`$toJSON()
      } else {
        stop("CredentialsCreateWithHttpInfo requires parameter credentialsBody to be of type CredentialsBody.")
      }

      urlPath <- "/credentials/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "CreateCredentialsResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `CredentialsDelete`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    CredentialsDeleteWithHttpInfo = function(credentialId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`credentialId`)) {
        stop("Missing required parameter `credentialId`.")
      }

      body <- NULL
      urlPath <- "/credentials/{credentialId}/"
      if (!missing(`credentialId`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), URLencode(as.character(`credentialId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "DELETE",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `CredentialsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    CredentialsListWithHttpInfo = function(offset = 0, limit = 100, types = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["types"] <- types

      body <- NULL
      urlPath <- "/credentials/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "CredentialsListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `CredentialsPatch`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    CredentialsPatchWithHttpInfo = function(credentialId, credentialsUpdate = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`credentialId`)) {
        stop("Missing required parameter `credentialId`.")
      }

      if (!missing(`credentialsUpdate`) && isa(credentialsUpdate, c("CredentialsUpdate", "R6"))) {
        body <- `credentialsUpdate`$toJSON()
      } else {
        stop("CredentialsPatchWithHttpInfo requires parameter credentialsUpdate to be of type CredentialsUpdate.")
      }

      urlPath <- "/credentials/{credentialId}/"
      if (!missing(`credentialId`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), URLencode(as.character(`credentialId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `CredentialsRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    CredentialsRetrieveWithHttpInfo = function(credentialId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`credentialId`)) {
        stop("Missing required parameter `credentialId`.")
      }

      body <- NULL
      urlPath <- "/credentials/{credentialId}/"
      if (!missing(`credentialId`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), URLencode(as.character(`credentialId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "CreateCredentialsResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    }
  )
)
