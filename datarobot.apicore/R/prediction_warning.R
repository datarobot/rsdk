# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionWarning
#'
#' @description PredictionWarning Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field customBoundaries  \link{CustomBoundaries}
#'
#' @field enabled  character Indicates whether prediction warnings are enabled for this deployment.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionWarning <- R6::R6Class(
  "PredictionWarning",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(customBoundaries = NULL, enabled = NULL) {
      if (!is.null(`customBoundaries`)) {
        stopifnot(R6::is.R6(`customBoundaries`))
      }
      if (!is.null(`enabled`)) {
        stopifnot(is.logical(`enabled`), length(`enabled`) == 1)
      }
    }
  ),
  public = list(
    `customBoundaries` = NULL,
    `enabled` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param customBoundaries
    #' @param enabled Indicates whether prediction warnings are enabled for this deployment.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`customBoundaries` = NULL, `enabled` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`customBoundaries`, `enabled`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(customBoundaries, enabled)
      }
      self$`customBoundaries` <- `customBoundaries`
      self$`enabled` <- `enabled`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(customBoundaries = self$`customBoundaries`, enabled = self$`enabled`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`customBoundaries`)) {
          sprintf(
            '"customBoundaries":
          %s
        ',
            jsonlite::toJSON(self$`customBoundaries`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`enabled`)) {
          sprintf(
            '"enabled":
            %s
                  ',
            tolower(self$`enabled`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionWarningJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionWarningJson, validateParams = FALSE) {
      PredictionWarningObject <- jsonlite::fromJSON(PredictionWarningJson)
      self$`customBoundaries` <- CustomBoundaries$new()$fromJSON(jsonlite::toJSON(PredictionWarningObject$customBoundaries, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`enabled` <- PredictionWarningObject$`enabled`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
