# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TrainingDataAssignment
#'
#' @description TrainingDataAssignment Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetId  character The ID of the dataset.
#'
#' @field datasetVersionId  character [optional] The ID of the dataset version.
#'
#' @field partitionColumn  character [optional] The name of the column containing the partition assignments.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TrainingDataAssignment <- R6::R6Class(
  "TrainingDataAssignment",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datasetId = NULL, datasetVersionId = NULL, partitionColumn = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`datasetVersionId`)) {
        stopifnot(is.character(`datasetVersionId`), length(`datasetVersionId`) == 1)
      }
      if (!is.null(`partitionColumn`)) {
        stopifnot(is.character(`partitionColumn`), length(`partitionColumn`) == 1)
      }
    }
  ),
  public = list(
    `datasetId` = NULL,
    `datasetVersionId` = NULL,
    `partitionColumn` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetId The ID of the dataset.
    #' @param datasetVersionId The ID of the dataset version.
    #' @param partitionColumn The name of the column containing the partition assignments.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `datasetVersionId` = NULL, `partitionColumn` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasetId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datasetId, datasetVersionId, partitionColumn)
      }
      self$`datasetId` <- `datasetId`
      self$`datasetVersionId` <- `datasetVersionId`
      self$`partitionColumn` <- `partitionColumn`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datasetId = self$`datasetId`, datasetVersionId = self$`datasetVersionId`, partitionColumn = self$`partitionColumn`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetVersionId`)) {
          sprintf(
            '"datasetVersionId":
            "%s"
                  ',
            self$`datasetVersionId`
          )
        },
        if (!is.null(self$`partitionColumn`)) {
          sprintf(
            '"partitionColumn":
            "%s"
                  ',
            self$`partitionColumn`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TrainingDataAssignmentJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TrainingDataAssignmentJson, validateParams = FALSE) {
      TrainingDataAssignmentObject <- jsonlite::fromJSON(TrainingDataAssignmentJson)
      self$`datasetId` <- TrainingDataAssignmentObject$`datasetId`
      self$`datasetVersionId` <- TrainingDataAssignmentObject$`datasetVersionId`
      self$`partitionColumn` <- TrainingDataAssignmentObject$`partitionColumn`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
