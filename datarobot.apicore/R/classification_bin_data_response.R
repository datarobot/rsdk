# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ClassificationBinDataResponse
#'
#' @description ClassificationBinDataResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field binEnd  numeric The end of the numeric range for the current bin. Note that &#x60;binEnd&#x60; - &#x60;binStart&#x60; should be a constant, modulo floating-point rounding error, for all bins in a single plot.
#'
#' @field binStart  numeric The start of the numeric range for the current bin. Must be equal to the &#x60;binEnd&#x60; of the previous bin.
#'
#' @field negatives  integer The number of records in the dataset where the model&#39;s predicted value falls into this bin and the target is negative.
#'
#' @field positives  integer The number of records in the dataset where the model&#39;s predicted value falls into this bin and the target is positive.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ClassificationBinDataResponse <- R6::R6Class(
  "ClassificationBinDataResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`binEnd` = NULL, `binStart` = NULL, `negatives` = NULL, `positives` = NULL) {
      if (!is.null(`binEnd`)) {
      }
      if (!is.null(`binStart`)) {
      }
      if (!is.null(`negatives`)) {
        stopifnot(is.numeric(`negatives`), length(`negatives`) == 1)
      }
      if (!is.null(`positives`)) {
        stopifnot(is.numeric(`positives`), length(`positives`) == 1)
      }
    }
  ),
  public = list(
    `binEnd` = NULL,
    `binStart` = NULL,
    `negatives` = NULL,
    `positives` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param binEnd The end of the numeric range for the current bin. Note that &#x60;binEnd&#x60; - &#x60;binStart&#x60; should be a constant, modulo floating-point rounding error, for all bins in a single plot.
    #' @param binStart The start of the numeric range for the current bin. Must be equal to the &#x60;binEnd&#x60; of the previous bin.
    #' @param negatives The number of records in the dataset where the model&#39;s predicted value falls into this bin and the target is negative.
    #' @param positives The number of records in the dataset where the model&#39;s predicted value falls into this bin and the target is positive.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`binEnd` = NULL, `binStart` = NULL, `negatives` = NULL, `positives` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`binEnd`, `binStart`, `negatives`, `positives`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(binEnd, binStart, negatives, positives)
      }
      self$`binEnd` <- `binEnd`
      self$`binStart` <- `binStart`
      self$`negatives` <- `negatives`
      self$`positives` <- `positives`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(binEnd = self$`binEnd`, binStart = self$`binStart`, negatives = self$`negatives`, positives = self$`positives`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`binEnd`)) {
          sprintf(
            '"binEnd":
            %d
                  ',
            self$`binEnd`
          )
        },
        if (!is.null(self$`binStart`)) {
          sprintf(
            '"binStart":
            %d
                  ',
            self$`binStart`
          )
        },
        if (!is.null(self$`negatives`)) {
          sprintf(
            '"negatives":
            %d
                  ',
            self$`negatives`
          )
        },
        if (!is.null(self$`positives`)) {
          sprintf(
            '"positives":
            %d
                  ',
            self$`positives`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ClassificationBinDataResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ClassificationBinDataResponseJson, validateParams = FALSE) {
      ClassificationBinDataResponseObject <- jsonlite::fromJSON(ClassificationBinDataResponseJson)
      self$`binEnd` <- ClassificationBinDataResponseObject$`binEnd`
      self$`binStart` <- ClassificationBinDataResponseObject$`binStart`
      self$`negatives` <- ClassificationBinDataResponseObject$`negatives`
      self$`positives` <- ClassificationBinDataResponseObject$`positives`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
