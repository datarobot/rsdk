# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintTask
#'
#' @description UserBlueprintTask Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field arguments  list( \link{UserBlueprintTaskArgument} ) A list of definitions of each argument which can be set for the task.
#'
#' @field categories  list( character ) The categories which the task is in.
#'
#' @field colnamesAndTypes  list( \link{ColnameAndType} ) [optional] The column names, their types, and their hex representation, available in the specified project for the task.
#'
#' @field customTaskId  character [optional] The id of the custom task, if it is a custom task.
#'
#' @field customTaskVersions  list( \link{UserBlueprintTaskCustomTaskMetadataWithArguments} ) [optional] Metadata for all of the custom task&#39;s versions.
#'
#' @field description  character A description of the task.
#'
#' @field icon  integer The integer representing the id to be displayed when the blueprint is trained.
#'
#' @field isCommonTask  character [optional] Whether the task is a common task.
#'
#' @field isCustomTask  character [optional] Whether the task is custom code written by the user.
#'
#' @field isVisibleInComposableMl  character [optional] Whether the task is visible in the ComposableML menu
#'
#' @field label  character The generic / default title or label for the task.
#'
#' @field outputMethods  list( character ) The methods which the task can use to produce output.
#'
#' @field supportsScoringCode  character Whether the task supports Scoring Code.
#'
#' @field taskCode  character The unique code which represents the task to be constructed and executed
#'
#' @field timeSeriesOnly  character Whether the task can only be used with time series projects.
#'
#' @field url  \link{OneOfobjectstring} The URL of the documentation of the task.
#'
#' @field validInputs  list( character ) The supported input types of the task.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintTask <- R6::R6Class(
  "UserBlueprintTask",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(arguments = NULL, categories = NULL, colnamesAndTypes = NULL, customTaskId = NULL, customTaskVersions = NULL, description = NULL, icon = NULL, isCommonTask = NULL, isCustomTask = NULL, isVisibleInComposableMl = NULL, label = NULL, outputMethods = NULL, supportsScoringCode = NULL, taskCode = NULL, timeSeriesOnly = NULL, url = NULL, validInputs = NULL) {
      if (!is.null(`arguments`)) {
        stopifnot(is.vector(`arguments`))
      }
      if (!is.null(`categories`)) {
        stopifnot(is.vector(`categories`))
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`icon`)) {
        stopifnot(is.numeric(`icon`), length(`icon`) == 1)
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`outputMethods`)) {
        stopifnot(is.vector(`outputMethods`))
      }
      if (!is.null(`supportsScoringCode`)) {
        stopifnot(is.logical(`supportsScoringCode`), length(`supportsScoringCode`) == 1)
      }
      if (!is.null(`taskCode`)) {
        stopifnot(is.character(`taskCode`), length(`taskCode`) == 1)
      }
      if (!is.null(`timeSeriesOnly`)) {
        stopifnot(is.logical(`timeSeriesOnly`), length(`timeSeriesOnly`) == 1)
      }
      if (!is.null(`url`)) {
        .setPrimitiveProperty(typeList = list("character"), propertyData = url)
      }
      if (!is.null(`validInputs`)) {
        stopifnot(is.vector(`validInputs`))
      }
      if (!is.null(`colnamesAndTypes`)) {
        stopifnot(is.vector(`colnamesAndTypes`))
      }
      if (!is.null(`customTaskId`)) {
        stopifnot(is.character(`customTaskId`), length(`customTaskId`) == 1)
      }
      if (!is.null(`customTaskVersions`)) {
        stopifnot(is.vector(`customTaskVersions`))
      }
      if (!is.null(`isCommonTask`)) {
        stopifnot(is.logical(`isCommonTask`), length(`isCommonTask`) == 1)
      }
      if (!is.null(`isCustomTask`)) {
        stopifnot(is.logical(`isCustomTask`), length(`isCustomTask`) == 1)
      }
      if (!is.null(`isVisibleInComposableMl`)) {
        stopifnot(is.logical(`isVisibleInComposableMl`), length(`isVisibleInComposableMl`) == 1)
      }
    }
  ),
  public = list(
    `arguments` = NULL,
    `categories` = NULL,
    `colnamesAndTypes` = NULL,
    `customTaskId` = NULL,
    `customTaskVersions` = NULL,
    `description` = NULL,
    `icon` = NULL,
    `isCommonTask` = NULL,
    `isCustomTask` = NULL,
    `isVisibleInComposableMl` = NULL,
    `label` = NULL,
    `outputMethods` = NULL,
    `supportsScoringCode` = NULL,
    `taskCode` = NULL,
    `timeSeriesOnly` = NULL,
    `url` = NULL,
    `validInputs` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param arguments A list of definitions of each argument which can be set for the task.
    #' @param categories The categories which the task is in.
    #' @param colnamesAndTypes The column names, their types, and their hex representation, available in the specified project for the task.
    #' @param customTaskId The id of the custom task, if it is a custom task.
    #' @param customTaskVersions Metadata for all of the custom task&#39;s versions.
    #' @param description A description of the task.
    #' @param icon The integer representing the id to be displayed when the blueprint is trained.
    #' @param isCommonTask Whether the task is a common task.
    #' @param isCustomTask Whether the task is custom code written by the user.
    #' @param isVisibleInComposableMl Whether the task is visible in the ComposableML menu
    #' @param label The generic / default title or label for the task.
    #' @param outputMethods The methods which the task can use to produce output.
    #' @param supportsScoringCode Whether the task supports Scoring Code.
    #' @param taskCode The unique code which represents the task to be constructed and executed
    #' @param timeSeriesOnly Whether the task can only be used with time series projects.
    #' @param url The URL of the documentation of the task.
    #' @param validInputs The supported input types of the task.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`arguments` = NULL, `categories` = NULL, `description` = NULL, `icon` = NULL, `label` = NULL, `outputMethods` = NULL, `supportsScoringCode` = NULL, `taskCode` = NULL, `timeSeriesOnly` = NULL, `url` = NULL, `validInputs` = NULL, `colnamesAndTypes` = NULL, `customTaskId` = NULL, `customTaskVersions` = NULL, `isCommonTask` = FALSE, `isCustomTask` = NULL, `isVisibleInComposableMl` = TRUE, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`arguments`, `categories`, `description`, `icon`, `label`, `outputMethods`, `supportsScoringCode`, `taskCode`, `timeSeriesOnly`, `url`, `validInputs`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(arguments, categories, colnamesAndTypes, customTaskId, customTaskVersions, description, icon, isCommonTask, isCustomTask, isVisibleInComposableMl, label, outputMethods, supportsScoringCode, taskCode, timeSeriesOnly, url, validInputs)
      }
      sapply(`arguments`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`categories`, function(x) stopifnot(is.character(x)))
      sapply(`colnamesAndTypes`, function(x) stopifnot(R6::is.R6(x)))
      self$`customTaskId` <- `customTaskId`
      sapply(`customTaskVersions`, function(x) stopifnot(R6::is.R6(x)))
      self$`description` <- `description`
      self$`icon` <- `icon`
      self$`isCommonTask` <- `isCommonTask`
      self$`isCustomTask` <- `isCustomTask`
      self$`isVisibleInComposableMl` <- `isVisibleInComposableMl`
      self$`label` <- `label`
      sapply(`outputMethods`, function(x) stopifnot(is.character(x)))
      self$`supportsScoringCode` <- `supportsScoringCode`
      self$`taskCode` <- `taskCode`
      self$`timeSeriesOnly` <- `timeSeriesOnly`
      self$`url` <- .setPrimitiveProperty(typeList = list("character"), propertyData = url)
      sapply(`validInputs`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(arguments = self$`arguments`, categories = self$`categories`, colnamesAndTypes = self$`colnamesAndTypes`, customTaskId = self$`customTaskId`, customTaskVersions = self$`customTaskVersions`, description = self$`description`, icon = self$`icon`, isCommonTask = self$`isCommonTask`, isCustomTask = self$`isCustomTask`, isVisibleInComposableMl = self$`isVisibleInComposableMl`, label = self$`label`, outputMethods = self$`outputMethods`, supportsScoringCode = self$`supportsScoringCode`, taskCode = self$`taskCode`, timeSeriesOnly = self$`timeSeriesOnly`, url = self$`url`, validInputs = self$`validInputs`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`arguments`)) {
          sprintf(
            '"arguments":
            [%s]
',
            paste(sapply(self$`arguments`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`categories`)) {
          sprintf(
            '"categories":
              [%s]
            ',
            paste(unlist(lapply(self$`categories`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`colnamesAndTypes`)) {
          sprintf(
            '"colnamesAndTypes":
            [%s]
',
            paste(sapply(self$`colnamesAndTypes`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`customTaskId`)) {
          sprintf(
            '"customTaskId":
            "%s"
                  ',
            self$`customTaskId`
          )
        },
        if (!is.null(self$`customTaskVersions`)) {
          sprintf(
            '"customTaskVersions":
            [%s]
',
            paste(sapply(self$`customTaskVersions`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`icon`)) {
          sprintf(
            '"icon":
            %d
                  ',
            self$`icon`
          )
        },
        if (!is.null(self$`isCommonTask`)) {
          sprintf(
            '"isCommonTask":
            %s
                  ',
            tolower(self$`isCommonTask`)
          )
        },
        if (!is.null(self$`isCustomTask`)) {
          sprintf(
            '"isCustomTask":
            %s
                  ',
            tolower(self$`isCustomTask`)
          )
        },
        if (!is.null(self$`isVisibleInComposableMl`)) {
          sprintf(
            '"isVisibleInComposableMl":
            %s
                  ',
            tolower(self$`isVisibleInComposableMl`)
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`outputMethods`)) {
          sprintf(
            '"outputMethods":
              [%s]
            ',
            paste(unlist(lapply(self$`outputMethods`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`supportsScoringCode`)) {
          sprintf(
            '"supportsScoringCode":
            %s
                  ',
            tolower(self$`supportsScoringCode`)
          )
        },
        if (!is.null(self$`taskCode`)) {
          sprintf(
            '"taskCode":
            "%s"
                  ',
            self$`taskCode`
          )
        },
        if (!is.null(self$`timeSeriesOnly`)) {
          sprintf(
            '"timeSeriesOnly":
            %s
                  ',
            tolower(self$`timeSeriesOnly`)
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
          %s
        ',
            self$`url`
          )
        },
        if (!is.null(self$`validInputs`)) {
          sprintf(
            '"validInputs":
              [%s]
            ',
            paste(unlist(lapply(self$`validInputs`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintTaskJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintTaskJson, validateParams = FALSE) {
      UserBlueprintTaskObject <- jsonlite::fromJSON(UserBlueprintTaskJson)
      self$`arguments` <- ApiClient$new()$deserializeObj(UserBlueprintTaskObject$`arguments`, "array[UserBlueprintTaskArgument]", loadNamespace("datarobot.apicore"))
      self$`categories` <- ApiClient$new()$deserializeObj(UserBlueprintTaskObject$`categories`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`colnamesAndTypes` <- ApiClient$new()$deserializeObj(UserBlueprintTaskObject$`colnamesAndTypes`, "array[ColnameAndType]", loadNamespace("datarobot.apicore"))
      self$`customTaskId` <- UserBlueprintTaskObject$`customTaskId`
      self$`customTaskVersions` <- ApiClient$new()$deserializeObj(UserBlueprintTaskObject$`customTaskVersions`, "array[UserBlueprintTaskCustomTaskMetadataWithArguments]", loadNamespace("datarobot.apicore"))
      self$`description` <- UserBlueprintTaskObject$`description`
      self$`icon` <- UserBlueprintTaskObject$`icon`
      self$`isCommonTask` <- UserBlueprintTaskObject$`isCommonTask`
      self$`isCustomTask` <- UserBlueprintTaskObject$`isCustomTask`
      self$`isVisibleInComposableMl` <- UserBlueprintTaskObject$`isVisibleInComposableMl`
      self$`label` <- UserBlueprintTaskObject$`label`
      self$`outputMethods` <- ApiClient$new()$deserializeObj(UserBlueprintTaskObject$`outputMethods`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`supportsScoringCode` <- UserBlueprintTaskObject$`supportsScoringCode`
      self$`taskCode` <- UserBlueprintTaskObject$`taskCode`
      self$`timeSeriesOnly` <- UserBlueprintTaskObject$`timeSeriesOnly`
      self$`url` <- .setPrimitiveProperty(typeList = list("character"), propertyData = UserBlueprintTaskObject$url)
      self$`validInputs` <- ApiClient$new()$deserializeObj(UserBlueprintTaskObject$`validInputs`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
