# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TableDescription
#'
#' @description TableDescription Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalog  character [optional] Name of the catalog.
#'
#' @field name  character Name of the table.
#'
#' @field schema  character [optional] Schema of the table.
#'
#' @field type  character Type of table.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TableDescription <- R6::R6Class(
  "TableDescription",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`catalog` = NULL, `name` = NULL, `schema` = NULL, `type` = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`catalog`)) {
        stopifnot(is.character(`catalog`), length(`catalog`) == 1)
      }
      if (!is.null(`schema`)) {
        stopifnot(is.character(`schema`), length(`schema`) == 1)
      }
    }
  ),
  public = list(
    `catalog` = NULL,
    `name` = NULL,
    `schema` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalog Name of the catalog.
    #' @param name Name of the table.
    #' @param schema Schema of the table.
    #' @param type Type of table.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `type` = NULL, `catalog` = NULL, `schema` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`name`, `type`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(catalog, name, schema, type)
      }
      self$`catalog` <- `catalog`
      self$`name` <- `name`
      self$`schema` <- `schema`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(catalog = self$`catalog`, name = self$`name`, schema = self$`schema`, type = self$`type`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalog`)) {
          sprintf(
            '"catalog":
            "%s"
                  ',
            self$`catalog`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`schema`)) {
          sprintf(
            '"schema":
            "%s"
                  ',
            self$`schema`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TableDescriptionJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TableDescriptionJson, validateParams = FALSE) {
      TableDescriptionObject <- jsonlite::fromJSON(TableDescriptionJson)
      self$`catalog` <- TableDescriptionObject$`catalog`
      self$`name` <- TableDescriptionObject$`name`
      self$`schema` <- TableDescriptionObject$`schema`
      self$`type` <- TableDescriptionObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
