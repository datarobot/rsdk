# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DriverResponse
#'
#' @description DriverResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field associatedAuthTypes  list( character ) [optional] Authentication types associated with this connector configuration.
#'
#' @field baseNames  list( character ) [optional] Original file name(s) of the uploaded JAR file(s). If there are multiple JAR files required for the driver, each of the original file names should be present in the list in the same order as found in &#39;localJarUrls&#39;
#'
#' @field canonicalName  character [optional] User-friendly driver name.
#'
#' @field className  character [optional] Driver class name. For example &#39;com.amazon.redshift.jdbc.Driver&#39;)
#'
#' @field configurationId  character [optional] Driver configuration ID if it was provided during driver upload.
#'
#' @field creator  character ID of the user who uploaded the driver.
#'
#' @field id  character Driver ID.
#'
#' @field version  character [optional] Driver version, which is used to construct the canonical name if driver configuration ID was provided during driver upload.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DriverResponse <- R6::R6Class(
  "DriverResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`associatedAuthTypes` = NULL, `baseNames` = NULL, `canonicalName` = NULL, `className` = NULL, `configurationId` = NULL, `creator` = NULL, `id` = NULL, `version` = NULL) {
      if (!is.null(`creator`)) {
        stopifnot(is.character(`creator`), length(`creator`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`associatedAuthTypes`) && length(`associatedAuthTypes`) > 0) {
        stopifnot(is.vector(`associatedAuthTypes`), sapply(`associatedAuthTypes`, is.character))
      }
      if (!is.null(`baseNames`) && length(`baseNames`) > 0) {
        stopifnot(is.vector(`baseNames`), sapply(`baseNames`, is.character))
      }
      if (!is.null(`canonicalName`)) {
        stopifnot(is.character(`canonicalName`), length(`canonicalName`) == 1)
      }
      if (!is.null(`className`)) {
        stopifnot(is.character(`className`), length(`className`) == 1)
      }
      if (!is.null(`configurationId`)) {
        stopifnot(is.character(`configurationId`), length(`configurationId`) == 1)
      }
      if (!is.null(`version`)) {
        stopifnot(is.character(`version`), length(`version`) == 1)
      }
    }
  ),
  public = list(
    `associatedAuthTypes` = NULL,
    `baseNames` = NULL,
    `canonicalName` = NULL,
    `className` = NULL,
    `configurationId` = NULL,
    `creator` = NULL,
    `id` = NULL,
    `version` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param associatedAuthTypes Authentication types associated with this connector configuration.
    #' @param baseNames Original file name(s) of the uploaded JAR file(s). If there are multiple JAR files required for the driver, each of the original file names should be present in the list in the same order as found in &#39;localJarUrls&#39;
    #' @param canonicalName User-friendly driver name.
    #' @param className Driver class name. For example &#39;com.amazon.redshift.jdbc.Driver&#39;)
    #' @param configurationId Driver configuration ID if it was provided during driver upload.
    #' @param creator ID of the user who uploaded the driver.
    #' @param id Driver ID.
    #' @param version Driver version, which is used to construct the canonical name if driver configuration ID was provided during driver upload.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`creator` = NULL, `id` = NULL, `associatedAuthTypes` = NULL, `baseNames` = NULL, `canonicalName` = NULL, `className` = NULL, `configurationId` = NULL, `version` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`creator`, `id`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(associatedAuthTypes, baseNames, canonicalName, className, configurationId, creator, id, version)
      }
      self$`associatedAuthTypes` <- `associatedAuthTypes`
      self$`baseNames` <- `baseNames`
      self$`canonicalName` <- `canonicalName`
      self$`className` <- `className`
      self$`configurationId` <- `configurationId`
      self$`creator` <- `creator`
      self$`id` <- `id`
      self$`version` <- `version`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(associatedAuthTypes = self$`associatedAuthTypes`, baseNames = self$`baseNames`, canonicalName = self$`canonicalName`, className = self$`className`, configurationId = self$`configurationId`, creator = self$`creator`, id = self$`id`, version = self$`version`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`associatedAuthTypes`)) {
          sprintf(
            '"associatedAuthTypes":
            [%s]
                  ',
            paste(unlist(lapply(self$`associatedAuthTypes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`baseNames`)) {
          sprintf(
            '"baseNames":
            [%s]
                  ',
            paste(unlist(lapply(self$`baseNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`canonicalName`)) {
          sprintf(
            '"canonicalName":
            "%s"
                  ',
            self$`canonicalName`
          )
        },
        if (!is.null(self$`className`)) {
          sprintf(
            '"className":
            "%s"
                  ',
            self$`className`
          )
        },
        if (!is.null(self$`configurationId`)) {
          sprintf(
            '"configurationId":
            "%s"
                  ',
            self$`configurationId`
          )
        },
        if (!is.null(self$`creator`)) {
          sprintf(
            '"creator":
            "%s"
                  ',
            self$`creator`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`version`)) {
          sprintf(
            '"version":
            "%s"
                  ',
            self$`version`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DriverResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DriverResponseJson, validateParams = FALSE) {
      DriverResponseObject <- jsonlite::fromJSON(DriverResponseJson)
      self$`associatedAuthTypes` <- ApiClient$new()$deserializeObj(DriverResponseObject$`associatedAuthTypes`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`baseNames` <- ApiClient$new()$deserializeObj(DriverResponseObject$`baseNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`canonicalName` <- DriverResponseObject$`canonicalName`
      self$`className` <- DriverResponseObject$`className`
      self$`configurationId` <- DriverResponseObject$`configurationId`
      self$`creator` <- DriverResponseObject$`creator`
      self$`id` <- DriverResponseObject$`id`
      self$`version` <- DriverResponseObject$`version`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
