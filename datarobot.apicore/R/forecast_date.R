# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ForecastDate
#'
#' @description ForecastDate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field columnName  character [optional] The column name in prediction datasets to be used as forecast date
#'
#' @field datetimeFormat  character [optional] The datetime format of the forecast date column in prediction datasets.
#'
#' @field enabled  character Indicates whether predictions by forecast dates is enabled for the deployment.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ForecastDate <- R6::R6Class(
  "ForecastDate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`columnName` = NULL, `datetimeFormat` = NULL, `enabled` = NULL) {
      if (!is.null(`enabled`)) {
        stopifnot(is.logical(`enabled`), length(`enabled`) == 1)
      }
      if (!is.null(`columnName`)) {
        stopifnot(is.character(`columnName`), length(`columnName`) == 1)
      }
      if (!is.null(`datetimeFormat`)) {
        stopifnot(is.character(`datetimeFormat`), length(`datetimeFormat`) == 1)
      }
    }
  ),
  public = list(
    `columnName` = NULL,
    `datetimeFormat` = NULL,
    `enabled` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param columnName The column name in prediction datasets to be used as forecast date
    #' @param datetimeFormat The datetime format of the forecast date column in prediction datasets.
    #' @param enabled Indicates whether predictions by forecast dates is enabled for the deployment.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`enabled` = NULL, `columnName` = NULL, `datetimeFormat` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`enabled`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(columnName, datetimeFormat, enabled)
      }
      self$`columnName` <- `columnName`
      self$`datetimeFormat` <- `datetimeFormat`
      self$`enabled` <- `enabled`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(columnName = self$`columnName`, datetimeFormat = self$`datetimeFormat`, enabled = self$`enabled`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`columnName`)) {
          sprintf(
            '"columnName":
            "%s"
                  ',
            self$`columnName`
          )
        },
        if (!is.null(self$`datetimeFormat`)) {
          sprintf(
            '"datetimeFormat":
            "%s"
                  ',
            self$`datetimeFormat`
          )
        },
        if (!is.null(self$`enabled`)) {
          sprintf(
            '"enabled":
            %s
                  ',
            tolower(self$`enabled`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ForecastDateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ForecastDateJson, validateParams = FALSE) {
      ForecastDateObject <- jsonlite::fromJSON(ForecastDateJson)
      self$`columnName` <- ForecastDateObject$`columnName`
      self$`datetimeFormat` <- ForecastDateObject$`datetimeFormat`
      self$`enabled` <- ForecastDateObject$`enabled`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
