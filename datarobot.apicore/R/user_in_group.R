# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserInGroup
#'
#' @description UserInGroup Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field firstName  character The first name of the user.
#'
#' @field lastName  character The last name of the user.
#'
#' @field organization  character The name of the organization the user is part of.
#'
#' @field scheduledForDeletion  character [optional] If the user is scheduled for deletion. Will be returned when an appropriate FF is enabled.
#'
#' @field status  character The status of the user, &#x60;active&#x60; if the has been activated, &#x60;inactive&#x60; otherwise.
#'
#' @field userId  character The identifier of the user.
#'
#' @field username  character The name of the user.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserInGroup <- R6::R6Class(
  "UserInGroup",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(firstName = NULL, lastName = NULL, organization = NULL, scheduledForDeletion = NULL, status = NULL, userId = NULL, username = NULL) {
      if (!is.null(`firstName`)) {
        stopifnot(is.character(`firstName`), length(`firstName`) == 1)
      }
      if (!is.null(`lastName`)) {
        stopifnot(is.character(`lastName`), length(`lastName`) == 1)
      }
      if (!is.null(`organization`)) {
        stopifnot(is.character(`organization`), length(`organization`) == 1)
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
      if (!is.null(`userId`)) {
        stopifnot(is.character(`userId`), length(`userId`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
      if (!is.null(`scheduledForDeletion`)) {
        stopifnot(is.logical(`scheduledForDeletion`), length(`scheduledForDeletion`) == 1)
      }
    }
  ),
  public = list(
    `firstName` = NULL,
    `lastName` = NULL,
    `organization` = NULL,
    `scheduledForDeletion` = NULL,
    `status` = NULL,
    `userId` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param firstName The first name of the user.
    #' @param lastName The last name of the user.
    #' @param organization The name of the organization the user is part of.
    #' @param scheduledForDeletion If the user is scheduled for deletion. Will be returned when an appropriate FF is enabled.
    #' @param status The status of the user, &#x60;active&#x60; if the has been activated, &#x60;inactive&#x60; otherwise.
    #' @param userId The identifier of the user.
    #' @param username The name of the user.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`firstName` = NULL, `lastName` = NULL, `organization` = NULL, `status` = NULL, `userId` = NULL, `username` = NULL, `scheduledForDeletion` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`firstName`, `lastName`, `organization`, `status`, `userId`, `username`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(firstName, lastName, organization, scheduledForDeletion, status, userId, username)
      }
      self$`firstName` <- `firstName`
      self$`lastName` <- `lastName`
      self$`organization` <- `organization`
      self$`scheduledForDeletion` <- `scheduledForDeletion`
      self$`status` <- `status`
      self$`userId` <- `userId`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(firstName = self$`firstName`, lastName = self$`lastName`, organization = self$`organization`, scheduledForDeletion = self$`scheduledForDeletion`, status = self$`status`, userId = self$`userId`, username = self$`username`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`firstName`)) {
          sprintf(
            '"firstName":
            "%s"
                  ',
            self$`firstName`
          )
        },
        if (!is.null(self$`lastName`)) {
          sprintf(
            '"lastName":
            "%s"
                  ',
            self$`lastName`
          )
        },
        if (!is.null(self$`organization`)) {
          sprintf(
            '"organization":
            "%s"
                  ',
            self$`organization`
          )
        },
        if (!is.null(self$`scheduledForDeletion`)) {
          sprintf(
            '"scheduledForDeletion":
            %s
                  ',
            tolower(self$`scheduledForDeletion`)
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        },
        if (!is.null(self$`userId`)) {
          sprintf(
            '"userId":
            "%s"
                  ',
            self$`userId`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserInGroupJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserInGroupJson, validateParams = FALSE) {
      UserInGroupObject <- jsonlite::fromJSON(UserInGroupJson)
      self$`firstName` <- UserInGroupObject$`firstName`
      self$`lastName` <- UserInGroupObject$`lastName`
      self$`organization` <- UserInGroupObject$`organization`
      self$`scheduledForDeletion` <- UserInGroupObject$`scheduledForDeletion`
      self$`status` <- UserInGroupObject$`status`
      self$`userId` <- UserInGroupObject$`userId`
      self$`username` <- UserInGroupObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
