# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataSourceRetrieveResponse
#'
#' @description DataSourceRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field canonicalName  character Data source canonical name.
#'
#' @field creator  character ID of the user who created the data source.
#'
#' @field id  character Data source ID.
#'
#' @field params  \link{OneOfJDBCTableDataSourceJDBCQueryDataSourceDRConnectorV1DataSource} Data source configuration.
#'
#' @field role  character Role of the user making the request on this data source.
#'
#' @field type  character Data source type.
#'
#' @field updated  character ISO 8601-formatted date/time of the data source update.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataSourceRetrieveResponse <- R6::R6Class(
  "DataSourceRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(canonicalName = NULL, creator = NULL, id = NULL, params = NULL, role = NULL, type = NULL, updated = NULL) {
      if (!is.null(`canonicalName`)) {
        stopifnot(is.character(`canonicalName`), length(`canonicalName`) == 1)
      }
      if (!is.null(`creator`)) {
        stopifnot(is.character(`creator`), length(`creator`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`params`)) {
        .setComplexProperty(typeList = list(JDBCTableDataSource, JDBCQueryDataSource, DRConnectorV1DataSource), propertyData = params)
      }
      if (!is.null(`role`)) {
        stopifnot(is.character(`role`), length(`role`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`updated`)) {
        stopifnot(is.character(`updated`), length(`updated`) == 1)
      }
    }
  ),
  public = list(
    `canonicalName` = NULL,
    `creator` = NULL,
    `id` = NULL,
    `params` = NULL,
    `role` = NULL,
    `type` = NULL,
    `updated` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param canonicalName Data source canonical name.
    #' @param creator ID of the user who created the data source.
    #' @param id Data source ID.
    #' @param params Data source configuration.
    #' @param role Role of the user making the request on this data source.
    #' @param type Data source type.
    #' @param updated ISO 8601-formatted date/time of the data source update.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`canonicalName` = NULL, `creator` = NULL, `id` = NULL, `params` = NULL, `role` = NULL, `type` = NULL, `updated` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`canonicalName`, `creator`, `id`, `params`, `role`, `type`, `updated`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(canonicalName, creator, id, params, role, type, updated)
      }
      self$`canonicalName` <- `canonicalName`
      self$`creator` <- `creator`
      self$`id` <- `id`
      self$`params` <- .setComplexProperty(typeList = list(JDBCTableDataSource, JDBCQueryDataSource, DRConnectorV1DataSource), propertyData = params)
      self$`role` <- `role`
      self$`type` <- `type`
      self$`updated` <- `updated`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(canonicalName = self$`canonicalName`, creator = self$`creator`, id = self$`id`, params = self$`params`, role = self$`role`, type = self$`type`, updated = self$`updated`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`canonicalName`)) {
          sprintf(
            '"canonicalName":
            "%s"
                  ',
            self$`canonicalName`
          )
        },
        if (!is.null(self$`creator`)) {
          sprintf(
            '"creator":
            "%s"
                  ',
            self$`creator`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`params`)) {
          sprintf(
            '"params":
          %s
        ',
            jsonlite::toJSON(self$`params`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`role`)) {
          sprintf(
            '"role":
            "%s"
                  ',
            self$`role`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        },
        if (!is.null(self$`updated`)) {
          sprintf(
            '"updated":
            "%s"
                  ',
            self$`updated`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataSourceRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataSourceRetrieveResponseJson, validateParams = FALSE) {
      DataSourceRetrieveResponseObject <- jsonlite::fromJSON(DataSourceRetrieveResponseJson)
      self$`canonicalName` <- DataSourceRetrieveResponseObject$`canonicalName`
      self$`creator` <- DataSourceRetrieveResponseObject$`creator`
      self$`id` <- DataSourceRetrieveResponseObject$`id`
      self$`params` <- .setComplexProperty(typeList = list(JDBCTableDataSource, JDBCQueryDataSource, DRConnectorV1DataSource), propertyData = DataSourceRetrieveResponseObject$params)
      self$`role` <- DataSourceRetrieveResponseObject$`role`
      self$`type` <- DataSourceRetrieveResponseObject$`type`
      self$`updated` <- DataSourceRetrieveResponseObject$`updated`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
