# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NumIterationsTrainedData
#'
#' @description NumIterationsTrainedData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field numIterations  integer The number of iterations run in this stage of modeling.
#'
#' @field stage  character Modeling stage or None if it is a single-stage model
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NumIterationsTrainedData <- R6::R6Class(
  "NumIterationsTrainedData",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(numIterations = NULL, stage = NULL) {
      if (!is.null(`numIterations`)) {
        stopifnot(is.numeric(`numIterations`), length(`numIterations`) == 1)
      }
      if (!is.null(`stage`)) {
        stopifnot(is.character(`stage`), length(`stage`) == 1)
      }
    }
  ),
  public = list(
    `numIterations` = NULL,
    `stage` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param numIterations The number of iterations run in this stage of modeling.
    #' @param stage Modeling stage or None if it is a single-stage model
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`numIterations` = NULL, `stage` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`numIterations`, `stage`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(numIterations, stage)
      }
      self$`numIterations` <- `numIterations`
      self$`stage` <- `stage`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(numIterations = self$`numIterations`, stage = self$`stage`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`numIterations`)) {
          sprintf(
            '"numIterations":
            %d
                  ',
            self$`numIterations`
          )
        },
        if (!is.null(self$`stage`)) {
          sprintf(
            '"stage":
            "%s"
                  ',
            self$`stage`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NumIterationsTrainedDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NumIterationsTrainedDataJson, validateParams = FALSE) {
      NumIterationsTrainedDataObject <- jsonlite::fromJSON(NumIterationsTrainedDataJson)
      self$`numIterations` <- NumIterationsTrainedDataObject$`numIterations`
      self$`stage` <- NumIterationsTrainedDataObject$`stage`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
