# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TraningPredictions
#'
#' @description TraningPredictions Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dataSubset  character Subset of data predicted on
#'
#' @field explanationAlgorithm  character [optional] The method used for calculating prediction explanations
#'
#' @field id  character ID of the training prediction job
#'
#' @field maxExplanations  integer [optional] the number of top contributors that are included in prediction explanations. Defaults to null for datasets narrower than 100 columns, defaults to 100 for datasets wider than 100 columns
#'
#' @field modelId  character ID of the model
#'
#' @field shapWarnings  list( \link{ShapWarning} ) [optional] Will be present if \&quot;explanationAlgorithm\&quot; was set to \&quot;shap\&quot; and there were additivity failures during SHAP values calculation
#'
#' @field url  character The location of these predictions
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TraningPredictions <- R6::R6Class(
  "TraningPredictions",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`dataSubset` = NULL, `explanationAlgorithm` = NULL, `id` = NULL, `maxExplanations` = NULL, `modelId` = NULL, `shapWarnings` = NULL, `url` = NULL) {
      if (!is.null(`dataSubset`)) {
        stopifnot(is.character(`dataSubset`), length(`dataSubset`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
      if (!is.null(`explanationAlgorithm`)) {
        stopifnot(is.character(`explanationAlgorithm`), length(`explanationAlgorithm`) == 1)
      }
      if (!is.null(`maxExplanations`)) {
        stopifnot(is.numeric(`maxExplanations`), length(`maxExplanations`) == 1)
      }
      if (!is.null(`shapWarnings`)) {
        stopifnot(is.vector(`shapWarnings`))
      }
    }
  ),
  public = list(
    `dataSubset` = NULL,
    `explanationAlgorithm` = NULL,
    `id` = NULL,
    `maxExplanations` = NULL,
    `modelId` = NULL,
    `shapWarnings` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dataSubset Subset of data predicted on
    #' @param explanationAlgorithm The method used for calculating prediction explanations
    #' @param id ID of the training prediction job
    #' @param maxExplanations the number of top contributors that are included in prediction explanations. Defaults to null for datasets narrower than 100 columns, defaults to 100 for datasets wider than 100 columns
    #' @param modelId ID of the model
    #' @param shapWarnings Will be present if \&quot;explanationAlgorithm\&quot; was set to \&quot;shap\&quot; and there were additivity failures during SHAP values calculation
    #' @param url The location of these predictions
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataSubset` = NULL, `id` = NULL, `modelId` = NULL, `url` = NULL, `explanationAlgorithm` = NULL, `maxExplanations` = NULL, `shapWarnings` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`dataSubset`, `id`, `modelId`, `url`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(dataSubset, explanationAlgorithm, id, maxExplanations, modelId, shapWarnings, url)
      }
      self$`dataSubset` <- `dataSubset`
      self$`explanationAlgorithm` <- `explanationAlgorithm`
      self$`id` <- `id`
      self$`maxExplanations` <- `maxExplanations`
      self$`modelId` <- `modelId`
      sapply(`shapWarnings`, function(x) stopifnot(R6::is.R6(x)))
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(dataSubset = self$`dataSubset`, explanationAlgorithm = self$`explanationAlgorithm`, id = self$`id`, maxExplanations = self$`maxExplanations`, modelId = self$`modelId`, shapWarnings = self$`shapWarnings`, url = self$`url`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dataSubset`)) {
          sprintf(
            '"dataSubset":
            "%s"
                  ',
            self$`dataSubset`
          )
        },
        if (!is.null(self$`explanationAlgorithm`)) {
          sprintf(
            '"explanationAlgorithm":
            "%s"
                  ',
            self$`explanationAlgorithm`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`maxExplanations`)) {
          sprintf(
            '"maxExplanations":
            %d
                  ',
            self$`maxExplanations`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`shapWarnings`)) {
          sprintf(
            '"shapWarnings":
            [%s]
      ',
            paste(sapply(self$`shapWarnings`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TraningPredictionsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TraningPredictionsJson, validateParams = FALSE) {
      TraningPredictionsObject <- jsonlite::fromJSON(TraningPredictionsJson)
      self$`dataSubset` <- TraningPredictionsObject$`dataSubset`
      self$`explanationAlgorithm` <- TraningPredictionsObject$`explanationAlgorithm`
      self$`id` <- TraningPredictionsObject$`id`
      self$`maxExplanations` <- TraningPredictionsObject$`maxExplanations`
      self$`modelId` <- TraningPredictionsObject$`modelId`
      self$`shapWarnings` <- ApiClient$new()$deserializeObj(TraningPredictionsObject$`shapWarnings`, "array[ShapWarning]", loadNamespace("datarobot.apicore"))
      self$`url` <- TraningPredictionsObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
