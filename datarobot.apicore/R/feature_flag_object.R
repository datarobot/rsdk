# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureFlagObject
#'
#' @description FeatureFlagObject Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field uiLabel  character [optional] String representation of the feature flag
#'
#' @field uiTooltip  character [optional] Detailed description of the feature flag
#'
#' @field value  character Value of the feature flag
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureFlagObject <- R6::R6Class(
  "FeatureFlagObject",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(uiLabel = NULL, uiTooltip = NULL, value = NULL) {
      if (!is.null(`value`)) {
        stopifnot(is.logical(`value`), length(`value`) == 1)
      }
      if (!is.null(`uiLabel`)) {
        stopifnot(is.character(`uiLabel`), length(`uiLabel`) == 1)
      }
      if (!is.null(`uiTooltip`)) {
        stopifnot(is.character(`uiTooltip`), length(`uiTooltip`) == 1)
      }
    }
  ),
  public = list(
    `uiLabel` = NULL,
    `uiTooltip` = NULL,
    `value` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param uiLabel String representation of the feature flag
    #' @param uiTooltip Detailed description of the feature flag
    #' @param value Value of the feature flag
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`value` = NULL, `uiLabel` = NULL, `uiTooltip` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`value`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(uiLabel, uiTooltip, value)
      }
      self$`uiLabel` <- `uiLabel`
      self$`uiTooltip` <- `uiTooltip`
      self$`value` <- `value`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(uiLabel = self$`uiLabel`, uiTooltip = self$`uiTooltip`, value = self$`value`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`uiLabel`)) {
          sprintf(
            '"uiLabel":
            "%s"
                  ',
            self$`uiLabel`
          )
        },
        if (!is.null(self$`uiTooltip`)) {
          sprintf(
            '"uiTooltip":
            "%s"
                  ',
            self$`uiTooltip`
          )
        },
        if (!is.null(self$`value`)) {
          sprintf(
            '"value":
            %s
                  ',
            tolower(self$`value`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureFlagObjectJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureFlagObjectJson, validateParams = FALSE) {
      FeatureFlagObjectObject <- jsonlite::fromJSON(FeatureFlagObjectJson)
      self$`uiLabel` <- FeatureFlagObjectObject$`uiLabel`
      self$`uiTooltip` <- FeatureFlagObjectObject$`uiTooltip`
      self$`value` <- FeatureFlagObjectObject$`value`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
