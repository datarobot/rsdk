# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureMetricDetailsResponse
#'
#' @description FeatureMetricDetailsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field ascending  character Should the metric be sorted in ascending order
#'
#' @field metricName  character Name of the metric
#'
#' @field supportsBinary  character This metric is valid for binary classifciaton
#'
#' @field supportsMulticlass  character This metric is valid for mutliclass classifciaton
#'
#' @field supportsRegression  character This metric is valid for regression
#'
#' @field supportsTimeseries  character This metric is valid for timeseries
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureMetricDetailsResponse <- R6::R6Class(
  "FeatureMetricDetailsResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(ascending = NULL, metricName = NULL, supportsBinary = NULL, supportsMulticlass = NULL, supportsRegression = NULL, supportsTimeseries = NULL) {
      if (!is.null(`ascending`)) {
        stopifnot(is.logical(`ascending`), length(`ascending`) == 1)
      }
      if (!is.null(`metricName`)) {
        stopifnot(is.character(`metricName`), length(`metricName`) == 1)
      }
      if (!is.null(`supportsBinary`)) {
        stopifnot(is.logical(`supportsBinary`), length(`supportsBinary`) == 1)
      }
      if (!is.null(`supportsMulticlass`)) {
        stopifnot(is.logical(`supportsMulticlass`), length(`supportsMulticlass`) == 1)
      }
      if (!is.null(`supportsRegression`)) {
        stopifnot(is.logical(`supportsRegression`), length(`supportsRegression`) == 1)
      }
      if (!is.null(`supportsTimeseries`)) {
        stopifnot(is.logical(`supportsTimeseries`), length(`supportsTimeseries`) == 1)
      }
    }
  ),
  public = list(
    `ascending` = NULL,
    `metricName` = NULL,
    `supportsBinary` = NULL,
    `supportsMulticlass` = NULL,
    `supportsRegression` = NULL,
    `supportsTimeseries` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param ascending Should the metric be sorted in ascending order
    #' @param metricName Name of the metric
    #' @param supportsBinary This metric is valid for binary classifciaton
    #' @param supportsMulticlass This metric is valid for mutliclass classifciaton
    #' @param supportsRegression This metric is valid for regression
    #' @param supportsTimeseries This metric is valid for timeseries
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`ascending` = NULL, `metricName` = NULL, `supportsBinary` = NULL, `supportsMulticlass` = NULL, `supportsRegression` = NULL, `supportsTimeseries` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`ascending`, `metricName`, `supportsBinary`, `supportsMulticlass`, `supportsRegression`, `supportsTimeseries`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(ascending, metricName, supportsBinary, supportsMulticlass, supportsRegression, supportsTimeseries)
      }
      self$`ascending` <- `ascending`
      self$`metricName` <- `metricName`
      self$`supportsBinary` <- `supportsBinary`
      self$`supportsMulticlass` <- `supportsMulticlass`
      self$`supportsRegression` <- `supportsRegression`
      self$`supportsTimeseries` <- `supportsTimeseries`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(ascending = self$`ascending`, metricName = self$`metricName`, supportsBinary = self$`supportsBinary`, supportsMulticlass = self$`supportsMulticlass`, supportsRegression = self$`supportsRegression`, supportsTimeseries = self$`supportsTimeseries`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`ascending`)) {
          sprintf(
            '"ascending":
            %s
                  ',
            tolower(self$`ascending`)
          )
        },
        if (!is.null(self$`metricName`)) {
          sprintf(
            '"metricName":
            "%s"
                  ',
            self$`metricName`
          )
        },
        if (!is.null(self$`supportsBinary`)) {
          sprintf(
            '"supportsBinary":
            %s
                  ',
            tolower(self$`supportsBinary`)
          )
        },
        if (!is.null(self$`supportsMulticlass`)) {
          sprintf(
            '"supportsMulticlass":
            %s
                  ',
            tolower(self$`supportsMulticlass`)
          )
        },
        if (!is.null(self$`supportsRegression`)) {
          sprintf(
            '"supportsRegression":
            %s
                  ',
            tolower(self$`supportsRegression`)
          )
        },
        if (!is.null(self$`supportsTimeseries`)) {
          sprintf(
            '"supportsTimeseries":
            %s
                  ',
            tolower(self$`supportsTimeseries`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureMetricDetailsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureMetricDetailsResponseJson, validateParams = FALSE) {
      FeatureMetricDetailsResponseObject <- jsonlite::fromJSON(FeatureMetricDetailsResponseJson)
      self$`ascending` <- FeatureMetricDetailsResponseObject$`ascending`
      self$`metricName` <- FeatureMetricDetailsResponseObject$`metricName`
      self$`supportsBinary` <- FeatureMetricDetailsResponseObject$`supportsBinary`
      self$`supportsMulticlass` <- FeatureMetricDetailsResponseObject$`supportsMulticlass`
      self$`supportsRegression` <- FeatureMetricDetailsResponseObject$`supportsRegression`
      self$`supportsTimeseries` <- FeatureMetricDetailsResponseObject$`supportsTimeseries`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
