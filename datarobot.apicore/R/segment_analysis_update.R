# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SegmentAnalysisUpdate
#'
#' @description SegmentAnalysisUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field attributes  list( character ) [optional] The segment attributes to be tracked. Note that only categorical columns can be specified as tracked segment attributes. The target column may not be specified
#'
#' @field enabled  character Indicates whether service health, drift, and accuracy are tracked for segments of prediction data.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SegmentAnalysisUpdate <- R6::R6Class(
  "SegmentAnalysisUpdate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`attributes` = NULL, `enabled` = NULL) {
      if (!is.null(`enabled`)) {
        stopifnot(is.logical(`enabled`), length(`enabled`) == 1)
      }
      if (!is.null(`attributes`)) {
        stopifnot(is.vector(`attributes`))
      }
    }
  ),
  public = list(
    `attributes` = NULL,
    `enabled` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param attributes The segment attributes to be tracked. Note that only categorical columns can be specified as tracked segment attributes. The target column may not be specified
    #' @param enabled Indicates whether service health, drift, and accuracy are tracked for segments of prediction data.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`enabled` = NULL, `attributes` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`enabled`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(attributes, enabled)
      }
      sapply(`attributes`, function(x) stopifnot(is.character(x)))
      self$`enabled` <- `enabled`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(attributes = self$`attributes`, enabled = self$`enabled`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`attributes`)) {
          sprintf(
            '"attributes":
            [%s]
                  ',
            paste(unlist(lapply(self$`attributes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`enabled`)) {
          sprintf(
            '"enabled":
            %s
                  ',
            tolower(self$`enabled`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SegmentAnalysisUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SegmentAnalysisUpdateJson, validateParams = FALSE) {
      SegmentAnalysisUpdateObject <- jsonlite::fromJSON(SegmentAnalysisUpdateJson)
      self$`attributes` <- ApiClient$new()$deserializeObj(SegmentAnalysisUpdateObject$`attributes`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`enabled` <- SegmentAnalysisUpdateObject$`enabled`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
