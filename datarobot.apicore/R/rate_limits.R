# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RateLimits
#'
#' @description RateLimits Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field limit  integer The number of instances of the resource allowed during the period
#'
#' @field resource  character The resource being limited
#'
#' @field usageTracked  character [optional] Whether or not a persistent record of usage of this resource is recorded
#'
#' @field window  integer The length of the window, in &#39;windowType&#39; units.  For example 60 seconds, or 1 month
#'
#' @field windowType  character [optional] The unit of time to use for the rate limit window.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RateLimits <- R6::R6Class(
  "RateLimits",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(limit = NULL, resource = NULL, usageTracked = NULL, window = NULL, windowType = NULL) {
      if (!is.null(`limit`)) {
        stopifnot(is.numeric(`limit`), length(`limit`) == 1)
      }
      if (!is.null(`resource`)) {
        stopifnot(is.character(`resource`), length(`resource`) == 1)
      }
      if (!is.null(`window`)) {
        stopifnot(is.numeric(`window`), length(`window`) == 1)
      }
      if (!is.null(`usageTracked`)) {
        stopifnot(is.logical(`usageTracked`), length(`usageTracked`) == 1)
      }
      if (!is.null(`windowType`)) {
        stopifnot(is.character(`windowType`), length(`windowType`) == 1)
      }
    }
  ),
  public = list(
    `limit` = NULL,
    `resource` = NULL,
    `usageTracked` = NULL,
    `window` = NULL,
    `windowType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param limit The number of instances of the resource allowed during the period
    #' @param resource The resource being limited
    #' @param usageTracked Whether or not a persistent record of usage of this resource is recorded
    #' @param window The length of the window, in &#39;windowType&#39; units.  For example 60 seconds, or 1 month
    #' @param windowType The unit of time to use for the rate limit window.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`limit` = NULL, `resource` = NULL, `window` = NULL, `usageTracked` = NULL, `windowType` = "seconds", validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`limit`, `resource`, `window`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(limit, resource, usageTracked, window, windowType)
      }
      self$`limit` <- `limit`
      self$`resource` <- `resource`
      self$`usageTracked` <- `usageTracked`
      self$`window` <- `window`
      self$`windowType` <- `windowType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(limit = self$`limit`, resource = self$`resource`, usageTracked = self$`usageTracked`, window = self$`window`, windowType = self$`windowType`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`limit`)) {
          sprintf(
            '"limit":
            %d
                  ',
            self$`limit`
          )
        },
        if (!is.null(self$`resource`)) {
          sprintf(
            '"resource":
            "%s"
                  ',
            self$`resource`
          )
        },
        if (!is.null(self$`usageTracked`)) {
          sprintf(
            '"usageTracked":
            %s
                  ',
            tolower(self$`usageTracked`)
          )
        },
        if (!is.null(self$`window`)) {
          sprintf(
            '"window":
            %d
                  ',
            self$`window`
          )
        },
        if (!is.null(self$`windowType`)) {
          sprintf(
            '"windowType":
            "%s"
                  ',
            self$`windowType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RateLimitsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RateLimitsJson, validateParams = FALSE) {
      RateLimitsObject <- jsonlite::fromJSON(RateLimitsJson)
      self$`limit` <- RateLimitsObject$`limit`
      self$`resource` <- RateLimitsObject$`resource`
      self$`usageTracked` <- RateLimitsObject$`usageTracked`
      self$`window` <- RateLimitsObject$`window`
      self$`windowType` <- RateLimitsObject$`windowType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
