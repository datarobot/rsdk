# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ShapMatrixListDataField
#'
#' @description ShapMatrixListDataField Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetId  character The dataset ID.
#'
#' @field id  character The ID of the SHAP matrix record.
#'
#' @field metadata  \link{ShapMatrixMetadataField}
#'
#' @field modelId  character The model ID.
#'
#' @field projectId  character The project ID.
#'
#' @field url  character The url at which you can retrieve the SHAP matrix.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShapMatrixListDataField <- R6::R6Class(
  "ShapMatrixListDataField",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datasetId = NULL, id = NULL, metadata = NULL, modelId = NULL, projectId = NULL, url = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`metadata`)) {
        stopifnot(R6::is.R6(`metadata`))
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
    }
  ),
  public = list(
    `datasetId` = NULL,
    `id` = NULL,
    `metadata` = NULL,
    `modelId` = NULL,
    `projectId` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetId The dataset ID.
    #' @param id The ID of the SHAP matrix record.
    #' @param metadata
    #' @param modelId The model ID.
    #' @param projectId The project ID.
    #' @param url The url at which you can retrieve the SHAP matrix.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `id` = NULL, `metadata` = NULL, `modelId` = NULL, `projectId` = NULL, `url` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasetId`, `id`, `metadata`, `modelId`, `projectId`, `url`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datasetId, id, metadata, modelId, projectId, url)
      }
      self$`datasetId` <- `datasetId`
      self$`id` <- `id`
      self$`metadata` <- `metadata`
      self$`modelId` <- `modelId`
      self$`projectId` <- `projectId`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datasetId = self$`datasetId`, id = self$`id`, metadata = self$`metadata`, modelId = self$`modelId`, projectId = self$`projectId`, url = self$`url`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`metadata`)) {
          sprintf(
            '"metadata":
          %s
        ',
            jsonlite::toJSON(self$`metadata`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ShapMatrixListDataFieldJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ShapMatrixListDataFieldJson, validateParams = FALSE) {
      ShapMatrixListDataFieldObject <- jsonlite::fromJSON(ShapMatrixListDataFieldJson)
      self$`datasetId` <- ShapMatrixListDataFieldObject$`datasetId`
      self$`id` <- ShapMatrixListDataFieldObject$`id`
      self$`metadata` <- ShapMatrixMetadataField$new()$fromJSON(jsonlite::toJSON(ShapMatrixListDataFieldObject$metadata, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`modelId` <- ShapMatrixListDataFieldObject$`modelId`
      self$`projectId` <- ShapMatrixListDataFieldObject$`projectId`
      self$`url` <- ShapMatrixListDataFieldObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
