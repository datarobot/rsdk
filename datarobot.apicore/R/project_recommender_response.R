# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ProjectRecommenderResponse
#'
#' @description ProjectRecommenderResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field isRecommender  character Whether the project is a recommender or not.
#'
#' @field recommenderItemId  character The recommender item ID.
#'
#' @field recommenderUserId  character The recommender user ID.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ProjectRecommenderResponse <- R6::R6Class(
  "ProjectRecommenderResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(isRecommender = NULL, recommenderItemId = NULL, recommenderUserId = NULL) {
      if (!is.null(`isRecommender`)) {
        stopifnot(is.logical(`isRecommender`), length(`isRecommender`) == 1)
      }
      if (!is.null(`recommenderItemId`)) {
        stopifnot(is.character(`recommenderItemId`), length(`recommenderItemId`) == 1)
      }
      if (!is.null(`recommenderUserId`)) {
        stopifnot(is.character(`recommenderUserId`), length(`recommenderUserId`) == 1)
      }
    }
  ),
  public = list(
    `isRecommender` = NULL,
    `recommenderItemId` = NULL,
    `recommenderUserId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param isRecommender Whether the project is a recommender or not.
    #' @param recommenderItemId The recommender item ID.
    #' @param recommenderUserId The recommender user ID.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`isRecommender` = NULL, `recommenderItemId` = NULL, `recommenderUserId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`isRecommender`, `recommenderItemId`, `recommenderUserId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(isRecommender, recommenderItemId, recommenderUserId)
      }
      self$`isRecommender` <- `isRecommender`
      self$`recommenderItemId` <- `recommenderItemId`
      self$`recommenderUserId` <- `recommenderUserId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(isRecommender = self$`isRecommender`, recommenderItemId = self$`recommenderItemId`, recommenderUserId = self$`recommenderUserId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`isRecommender`)) {
          sprintf(
            '"isRecommender":
            %s
                  ',
            tolower(self$`isRecommender`)
          )
        },
        if (!is.null(self$`recommenderItemId`)) {
          sprintf(
            '"recommenderItemId":
            "%s"
                  ',
            self$`recommenderItemId`
          )
        },
        if (!is.null(self$`recommenderUserId`)) {
          sprintf(
            '"recommenderUserId":
            "%s"
                  ',
            self$`recommenderUserId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ProjectRecommenderResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ProjectRecommenderResponseJson, validateParams = FALSE) {
      ProjectRecommenderResponseObject <- jsonlite::fromJSON(ProjectRecommenderResponseJson)
      self$`isRecommender` <- ProjectRecommenderResponseObject$`isRecommender`
      self$`recommenderItemId` <- ProjectRecommenderResponseObject$`recommenderItemId`
      self$`recommenderUserId` <- ProjectRecommenderResponseObject$`recommenderUserId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
