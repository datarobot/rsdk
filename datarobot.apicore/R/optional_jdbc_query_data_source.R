# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title OptionalJDBCQueryDataSource
#'
#' @description OptionalJDBCQueryDataSource Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dataStoreId  character [optional] Data store ID for this data source.
#'
#' @field fetchSize  integer [optional] User-specified fetch size.
#'
#' @field query  character [optional] The user-specified SQL query. If this is used, then catalog, schema and table will not be used.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
OptionalJDBCQueryDataSource <- R6::R6Class(
  "OptionalJDBCQueryDataSource",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`dataStoreId` = NULL, `fetchSize` = NULL, `query` = NULL) {
      if (!is.null(`dataStoreId`)) {
        stopifnot(is.character(`dataStoreId`), length(`dataStoreId`) == 1)
      }
      if (!is.null(`fetchSize`)) {
        stopifnot(is.numeric(`fetchSize`), length(`fetchSize`) == 1)
      }
      if (!is.null(`query`)) {
        stopifnot(is.character(`query`), length(`query`) == 1)
      }
    }
  ),
  public = list(
    `dataStoreId` = NULL,
    `fetchSize` = NULL,
    `query` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dataStoreId Data store ID for this data source.
    #' @param fetchSize User-specified fetch size.
    #' @param query The user-specified SQL query. If this is used, then catalog, schema and table will not be used.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataStoreId` = NULL, `fetchSize` = NULL, `query` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(dataStoreId, fetchSize, query)
      }
      self$`dataStoreId` <- `dataStoreId`
      self$`fetchSize` <- `fetchSize`
      self$`query` <- `query`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(dataStoreId = self$`dataStoreId`, fetchSize = self$`fetchSize`, query = self$`query`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dataStoreId`)) {
          sprintf(
            '"dataStoreId":
            "%s"
                  ',
            self$`dataStoreId`
          )
        },
        if (!is.null(self$`fetchSize`)) {
          sprintf(
            '"fetchSize":
            %d
                  ',
            self$`fetchSize`
          )
        },
        if (!is.null(self$`query`)) {
          sprintf(
            '"query":
            "%s"
                  ',
            self$`query`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param OptionalJDBCQueryDataSourceJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(OptionalJDBCQueryDataSourceJson, validateParams = FALSE) {
      OptionalJDBCQueryDataSourceObject <- jsonlite::fromJSON(OptionalJDBCQueryDataSourceJson)
      self$`dataStoreId` <- OptionalJDBCQueryDataSourceObject$`dataStoreId`
      self$`fetchSize` <- OptionalJDBCQueryDataSourceObject$`fetchSize`
      self$`query` <- OptionalJDBCQueryDataSourceObject$`query`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
