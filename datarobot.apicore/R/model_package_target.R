# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelPackageTarget
#'
#' @description ModelPackageTarget Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field classCount  integer Number of classes for classification models.
#'
#' @field classNames  list( character ) Class names for prediction results. When target type is Binary, two class names are returned. The first element is the minority (positive) class and the second element is the majority (negative) class. Limited to 100 returned for Multiclass.
#'
#' @field name  character name of the target column
#'
#' @field predictionProbabilitiesColumn  character Field or column name containing prediction probabilities
#'
#' @field predictionThreshold  numeric Prediction threshold used for binary classification models
#'
#' @field type  character Target type of the model.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelPackageTarget <- R6::R6Class(
  "ModelPackageTarget",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(classCount = NULL, classNames = NULL, name = NULL, predictionProbabilitiesColumn = NULL, predictionThreshold = NULL, type = NULL) {
      if (!is.null(`classCount`)) {
        stopifnot(is.numeric(`classCount`), length(`classCount`) == 1)
      }
      if (!is.null(`classNames`)) {
        stopifnot(is.vector(`classNames`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`predictionProbabilitiesColumn`)) {
        stopifnot(is.character(`predictionProbabilitiesColumn`), length(`predictionProbabilitiesColumn`) == 1)
      }
      if (!is.null(`predictionThreshold`)) {
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
    }
  ),
  public = list(
    `classCount` = NULL,
    `classNames` = NULL,
    `name` = NULL,
    `predictionProbabilitiesColumn` = NULL,
    `predictionThreshold` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param classCount Number of classes for classification models.
    #' @param classNames Class names for prediction results. When target type is Binary, two class names are returned. The first element is the minority (positive) class and the second element is the majority (negative) class. Limited to 100 returned for Multiclass.
    #' @param name name of the target column
    #' @param predictionProbabilitiesColumn Field or column name containing prediction probabilities
    #' @param predictionThreshold Prediction threshold used for binary classification models
    #' @param type Target type of the model.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`classCount` = NULL, `classNames` = NULL, `name` = NULL, `predictionProbabilitiesColumn` = NULL, `predictionThreshold` = NULL, `type` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`classCount`, `classNames`, `name`, `predictionProbabilitiesColumn`, `predictionThreshold`, `type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(classCount, classNames, name, predictionProbabilitiesColumn, predictionThreshold, type)
      }
      self$`classCount` <- `classCount`
      sapply(`classNames`, function(x) stopifnot(is.character(x)))
      self$`name` <- `name`
      self$`predictionProbabilitiesColumn` <- `predictionProbabilitiesColumn`
      self$`predictionThreshold` <- `predictionThreshold`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(classCount = self$`classCount`, classNames = self$`classNames`, name = self$`name`, predictionProbabilitiesColumn = self$`predictionProbabilitiesColumn`, predictionThreshold = self$`predictionThreshold`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`classCount`)) {
          sprintf(
            '"classCount":
            %d
                  ',
            self$`classCount`
          )
        },
        if (!is.null(self$`classNames`)) {
          sprintf(
            '"classNames":
              [%s]
            ',
            paste(unlist(lapply(self$`classNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`predictionProbabilitiesColumn`)) {
          sprintf(
            '"predictionProbabilitiesColumn":
            "%s"
                  ',
            self$`predictionProbabilitiesColumn`
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelPackageTargetJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelPackageTargetJson, validateParams = FALSE) {
      ModelPackageTargetObject <- jsonlite::fromJSON(ModelPackageTargetJson)
      self$`classCount` <- ModelPackageTargetObject$`classCount`
      self$`classNames` <- ApiClient$new()$deserializeObj(ModelPackageTargetObject$`classNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`name` <- ModelPackageTargetObject$`name`
      self$`predictionProbabilitiesColumn` <- ModelPackageTargetObject$`predictionProbabilitiesColumn`
      self$`predictionThreshold` <- ModelPackageTargetObject$`predictionThreshold`
      self$`type` <- ModelPackageTargetObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
