# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AddDeploymentToApplication
#'
#' @description AddDeploymentToApplication Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field linkName  character [optional] Internal name of deployment to match in the application
#'
#' @field modelDeploymentId  character The ID of the model deployment to link to the application.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AddDeploymentToApplication <- R6::R6Class(
  "AddDeploymentToApplication",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(linkName = NULL, modelDeploymentId = NULL) {
      if (!is.null(`modelDeploymentId`)) {
        stopifnot(is.character(`modelDeploymentId`), length(`modelDeploymentId`) == 1)
      }
      if (!is.null(`linkName`)) {
        stopifnot(is.character(`linkName`), length(`linkName`) == 1)
      }
    }
  ),
  public = list(
    `linkName` = NULL,
    `modelDeploymentId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param linkName Internal name of deployment to match in the application
    #' @param modelDeploymentId The ID of the model deployment to link to the application.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`modelDeploymentId` = NULL, `linkName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`modelDeploymentId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(linkName, modelDeploymentId)
      }
      self$`linkName` <- `linkName`
      self$`modelDeploymentId` <- `modelDeploymentId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(linkName = self$`linkName`, modelDeploymentId = self$`modelDeploymentId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`linkName`)) {
          sprintf(
            '"linkName":
            "%s"
                  ',
            self$`linkName`
          )
        },
        if (!is.null(self$`modelDeploymentId`)) {
          sprintf(
            '"modelDeploymentId":
            "%s"
                  ',
            self$`modelDeploymentId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AddDeploymentToApplicationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AddDeploymentToApplicationJson, validateParams = FALSE) {
      AddDeploymentToApplicationObject <- jsonlite::fromJSON(AddDeploymentToApplicationJson)
      self$`linkName` <- AddDeploymentToApplicationObject$`linkName`
      self$`modelDeploymentId` <- AddDeploymentToApplicationObject$`modelDeploymentId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
