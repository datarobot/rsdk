# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ForecastVsActualPlotsDataResponse
#'
#' @description ForecastVsActualPlotsDataResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field bins  list( \link{ForecastVsActualPlotsBins} ) An array of bins for the retrieved plots.
#'
#' @field calendarEvents  list( \link{CalendarEvent} ) An array of calendar events for a retrieved plot.
#'
#' @field endDate  character The datetime of the end of the chart data (exclusive).
#'
#' @field forecastDistances  list( integer ) An array of forecast distances. Forecast distance specifies the number of time steps between the predicted point and the origin point.
#'
#' @field resolution  character The resolution that is used for binning.
#'
#' @field startDate  character The datetime of the start of the chart data (inclusive).
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ForecastVsActualPlotsDataResponse <- R6::R6Class(
  "ForecastVsActualPlotsDataResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`bins` = NULL, `calendarEvents` = NULL, `endDate` = NULL, `forecastDistances` = NULL, `resolution` = NULL, `startDate` = NULL) {
      if (!is.null(`bins`)) {
        stopifnot(is.vector(`bins`))
      }
      if (!is.null(`calendarEvents`)) {
        stopifnot(is.vector(`calendarEvents`))
      }
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`forecastDistances`)) {
        stopifnot(is.vector(`forecastDistances`))
      }
      if (!is.null(`resolution`)) {
        stopifnot(is.character(`resolution`), length(`resolution`) == 1)
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `bins` = NULL,
    `calendarEvents` = NULL,
    `endDate` = NULL,
    `forecastDistances` = NULL,
    `resolution` = NULL,
    `startDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param bins An array of bins for the retrieved plots.
    #' @param calendarEvents An array of calendar events for a retrieved plot.
    #' @param endDate The datetime of the end of the chart data (exclusive).
    #' @param forecastDistances An array of forecast distances. Forecast distance specifies the number of time steps between the predicted point and the origin point.
    #' @param resolution The resolution that is used for binning.
    #' @param startDate The datetime of the start of the chart data (inclusive).
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`bins` = NULL, `calendarEvents` = NULL, `endDate` = NULL, `forecastDistances` = NULL, `resolution` = NULL, `startDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`bins`, `calendarEvents`, `endDate`, `forecastDistances`, `resolution`, `startDate`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(bins, calendarEvents, endDate, forecastDistances, resolution, startDate)
      }
      sapply(`bins`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`calendarEvents`, function(x) stopifnot(R6::is.R6(x)))
      self$`endDate` <- `endDate`
      sapply(`forecastDistances`, function(x) stopifnot(is.character(x)))
      self$`resolution` <- `resolution`
      self$`startDate` <- `startDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(bins = self$`bins`, calendarEvents = self$`calendarEvents`, endDate = self$`endDate`, forecastDistances = self$`forecastDistances`, resolution = self$`resolution`, startDate = self$`startDate`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`bins`)) {
          sprintf(
            '"bins":
            [%s]
      ',
            paste(sapply(self$`bins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`calendarEvents`)) {
          sprintf(
            '"calendarEvents":
            [%s]
      ',
            paste(sapply(self$`calendarEvents`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`forecastDistances`)) {
          sprintf(
            '"forecastDistances":
            [%s]
                  ',
            paste(unlist(lapply(self$`forecastDistances`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`resolution`)) {
          sprintf(
            '"resolution":
            "%s"
                  ',
            self$`resolution`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ForecastVsActualPlotsDataResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ForecastVsActualPlotsDataResponseJson, validateParams = FALSE) {
      ForecastVsActualPlotsDataResponseObject <- jsonlite::fromJSON(ForecastVsActualPlotsDataResponseJson)
      self$`bins` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsDataResponseObject$`bins`, "array[ForecastVsActualPlotsBins]", loadNamespace("datarobot.apicore"))
      self$`calendarEvents` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsDataResponseObject$`calendarEvents`, "array[CalendarEvent]", loadNamespace("datarobot.apicore"))
      self$`endDate` <- ParseRFC3339Timestamp(ForecastVsActualPlotsDataResponseObject$`endDate`)
      self$`forecastDistances` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsDataResponseObject$`forecastDistances`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`resolution` <- ForecastVsActualPlotsDataResponseObject$`resolution`
      self$`startDate` <- ParseRFC3339Timestamp(ForecastVsActualPlotsDataResponseObject$`startDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
