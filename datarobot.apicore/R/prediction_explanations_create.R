# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanationsCreate
#'
#' @description PredictionExplanationsCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetId  character The dataset ID.
#'
#' @field maxExplanations  integer [optional] The maximum number of prediction explanations to supply per row of the dataset.
#'
#' @field modelId  character The model ID.
#'
#' @field thresholdHigh  numeric [optional] The high threshold, above which a prediction must score in order for prediction explanations to be computed. If neither thresholdHigh nor thresholdLow is specified, prediction explanations will be computed for all rows.
#'
#' @field thresholdLow  numeric [optional] The lower threshold, below which a prediction must score in order for prediction explanations to be computed for a row in the dataset. If neither thresholdHigh nor thresholdLow is specified, prediction explanations will be computed for all rows.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanationsCreate <- R6::R6Class(
  "PredictionExplanationsCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datasetId = NULL, maxExplanations = NULL, modelId = NULL, thresholdHigh = NULL, thresholdLow = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`maxExplanations`)) {
        stopifnot(is.numeric(`maxExplanations`), length(`maxExplanations`) == 1)
      }
      if (!is.null(`thresholdHigh`)) {
        stopifnot(is.numeric(`thresholdHigh`), length(`thresholdHigh`) == 1)
      }
      if (!is.null(`thresholdLow`)) {
        stopifnot(is.numeric(`thresholdLow`), length(`thresholdLow`) == 1)
      }
    }
  ),
  public = list(
    `datasetId` = NULL,
    `maxExplanations` = NULL,
    `modelId` = NULL,
    `thresholdHigh` = NULL,
    `thresholdLow` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetId The dataset ID.
    #' @param maxExplanations The maximum number of prediction explanations to supply per row of the dataset.
    #' @param modelId The model ID.
    #' @param thresholdHigh The high threshold, above which a prediction must score in order for prediction explanations to be computed. If neither thresholdHigh nor thresholdLow is specified, prediction explanations will be computed for all rows.
    #' @param thresholdLow The lower threshold, below which a prediction must score in order for prediction explanations to be computed for a row in the dataset. If neither thresholdHigh nor thresholdLow is specified, prediction explanations will be computed for all rows.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `modelId` = NULL, `maxExplanations` = 3, `thresholdHigh` = NULL, `thresholdLow` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasetId`, `modelId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datasetId, maxExplanations, modelId, thresholdHigh, thresholdLow)
      }
      self$`datasetId` <- `datasetId`
      self$`maxExplanations` <- `maxExplanations`
      self$`modelId` <- `modelId`
      self$`thresholdHigh` <- `thresholdHigh`
      self$`thresholdLow` <- `thresholdLow`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datasetId = self$`datasetId`, maxExplanations = self$`maxExplanations`, modelId = self$`modelId`, thresholdHigh = self$`thresholdHigh`, thresholdLow = self$`thresholdLow`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`maxExplanations`)) {
          sprintf(
            '"maxExplanations":
            %d
                  ',
            self$`maxExplanations`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`thresholdHigh`)) {
          sprintf(
            '"thresholdHigh":
            %d
                  ',
            self$`thresholdHigh`
          )
        },
        if (!is.null(self$`thresholdLow`)) {
          sprintf(
            '"thresholdLow":
            %d
                  ',
            self$`thresholdLow`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationsCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationsCreateJson, validateParams = FALSE) {
      PredictionExplanationsCreateObject <- jsonlite::fromJSON(PredictionExplanationsCreateJson)
      self$`datasetId` <- PredictionExplanationsCreateObject$`datasetId`
      self$`maxExplanations` <- PredictionExplanationsCreateObject$`maxExplanations`
      self$`modelId` <- PredictionExplanationsCreateObject$`modelId`
      self$`thresholdHigh` <- PredictionExplanationsCreateObject$`thresholdHigh`
      self$`thresholdLow` <- PredictionExplanationsCreateObject$`thresholdLow`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
