# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentAccuracyHealthResponse
#'
#' @description DeploymentAccuracyHealthResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field endDate  character End date of accuracy health period.
#'
#' @field message  character A message providing more detail on the status.
#'
#' @field startDate  character Start date of accuracy health period.
#'
#' @field status  character Accuracy health status.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentAccuracyHealthResponse <- R6::R6Class(
  "DeploymentAccuracyHealthResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`endDate` = NULL, `message` = NULL, `startDate` = NULL, `status` = NULL) {
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
    }
  ),
  public = list(
    `endDate` = NULL,
    `message` = NULL,
    `startDate` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param endDate End date of accuracy health period.
    #' @param message A message providing more detail on the status.
    #' @param startDate Start date of accuracy health period.
    #' @param status Accuracy health status.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`endDate` = NULL, `message` = NULL, `startDate` = NULL, `status` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`endDate`, `message`, `startDate`, `status`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(endDate, message, startDate, status)
      }
      self$`endDate` <- `endDate`
      self$`message` <- `message`
      self$`startDate` <- `startDate`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(endDate = self$`endDate`, message = self$`message`, startDate = self$`startDate`, status = self$`status`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentAccuracyHealthResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentAccuracyHealthResponseJson, validateParams = FALSE) {
      DeploymentAccuracyHealthResponseObject <- jsonlite::fromJSON(DeploymentAccuracyHealthResponseJson)
      self$`endDate` <- ParseRFC3339Timestamp(DeploymentAccuracyHealthResponseObject$`endDate`)
      self$`message` <- DeploymentAccuracyHealthResponseObject$`message`
      self$`startDate` <- ParseRFC3339Timestamp(DeploymentAccuracyHealthResponseObject$`startDate`)
      self$`status` <- DeploymentAccuracyHealthResponseObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
