# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RetrainingPolicyRunRetrieve
#'
#' @description RetrainingPolicyRunRetrieve Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field challengerId  character ID of the challenger created from this retraining policy run.
#'
#' @field errorMessage  character Error message of the retraining policy run.
#'
#' @field finishTime  character Finish time of the retraining policy run.
#'
#' @field id  character ID of the retraining policy run.
#'
#' @field modelPackageId  character ID of the model package created from this retraining policy run.
#'
#' @field projectId  character ID of the project created from this retraining policy run.
#'
#' @field startTime  character Start time of the retraining policy run.
#'
#' @field status  character Status of the retraining policy run.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RetrainingPolicyRunRetrieve <- R6::R6Class(
  "RetrainingPolicyRunRetrieve",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`challengerId` = NULL, `errorMessage` = NULL, `finishTime` = NULL, `id` = NULL, `modelPackageId` = NULL, `projectId` = NULL, `startTime` = NULL, `status` = NULL) {
      if (!is.null(`challengerId`)) {
        stopifnot(is.character(`challengerId`), length(`challengerId`) == 1)
      }
      if (!is.null(`errorMessage`)) {
        stopifnot(is.character(`errorMessage`), length(`errorMessage`) == 1)
      }
      if (!is.null(`finishTime`)) {
        stopifnot(inherits(`finishTime`, "POSIXt"))
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`modelPackageId`)) {
        stopifnot(is.character(`modelPackageId`), length(`modelPackageId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`startTime`)) {
        stopifnot(inherits(`startTime`, "POSIXt"))
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
    }
  ),
  public = list(
    `challengerId` = NULL,
    `errorMessage` = NULL,
    `finishTime` = NULL,
    `id` = NULL,
    `modelPackageId` = NULL,
    `projectId` = NULL,
    `startTime` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param challengerId ID of the challenger created from this retraining policy run.
    #' @param errorMessage Error message of the retraining policy run.
    #' @param finishTime Finish time of the retraining policy run.
    #' @param id ID of the retraining policy run.
    #' @param modelPackageId ID of the model package created from this retraining policy run.
    #' @param projectId ID of the project created from this retraining policy run.
    #' @param startTime Start time of the retraining policy run.
    #' @param status Status of the retraining policy run.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`challengerId` = NULL, `errorMessage` = NULL, `finishTime` = NULL, `id` = NULL, `modelPackageId` = NULL, `projectId` = NULL, `startTime` = NULL, `status` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`challengerId`, `errorMessage`, `finishTime`, `id`, `modelPackageId`, `projectId`, `startTime`, `status`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(challengerId, errorMessage, finishTime, id, modelPackageId, projectId, startTime, status)
      }
      self$`challengerId` <- `challengerId`
      self$`errorMessage` <- `errorMessage`
      self$`finishTime` <- `finishTime`
      self$`id` <- `id`
      self$`modelPackageId` <- `modelPackageId`
      self$`projectId` <- `projectId`
      self$`startTime` <- `startTime`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(challengerId = self$`challengerId`, errorMessage = self$`errorMessage`, finishTime = self$`finishTime`, id = self$`id`, modelPackageId = self$`modelPackageId`, projectId = self$`projectId`, startTime = self$`startTime`, status = self$`status`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`challengerId`)) {
          sprintf(
            '"challengerId":
            "%s"
                  ',
            self$`challengerId`
          )
        },
        if (!is.null(self$`errorMessage`)) {
          sprintf(
            '"errorMessage":
            "%s"
                  ',
            self$`errorMessage`
          )
        },
        if (!is.null(self$`finishTime`)) {
          sprintf(
            '"finishTime":
            "%s"
                  ',
            format(self$`finishTime`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`modelPackageId`)) {
          sprintf(
            '"modelPackageId":
            "%s"
                  ',
            self$`modelPackageId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`startTime`)) {
          sprintf(
            '"startTime":
            "%s"
                  ',
            format(self$`startTime`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RetrainingPolicyRunRetrieveJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RetrainingPolicyRunRetrieveJson, validateParams = FALSE) {
      RetrainingPolicyRunRetrieveObject <- jsonlite::fromJSON(RetrainingPolicyRunRetrieveJson)
      self$`challengerId` <- RetrainingPolicyRunRetrieveObject$`challengerId`
      self$`errorMessage` <- RetrainingPolicyRunRetrieveObject$`errorMessage`
      self$`finishTime` <- ParseRFC3339Timestamp(RetrainingPolicyRunRetrieveObject$`finishTime`)
      self$`id` <- RetrainingPolicyRunRetrieveObject$`id`
      self$`modelPackageId` <- RetrainingPolicyRunRetrieveObject$`modelPackageId`
      self$`projectId` <- RetrainingPolicyRunRetrieveObject$`projectId`
      self$`startTime` <- ParseRFC3339Timestamp(RetrainingPolicyRunRetrieveObject$`startTime`)
      self$`status` <- RetrainingPolicyRunRetrieveObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
