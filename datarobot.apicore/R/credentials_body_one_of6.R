# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsBodyOneOf6
#'
#' @description CredentialsBodyOneOf6 Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field clientId  character ADLS Gen2 OAuth client ID.
#'
#' @field clientSecret  character ADLS Gen2 OAuth client secret.
#'
#' @field credentialType  character Credentials type.
#'
#' @field description  character [optional] Credentials description.
#'
#' @field name  character Credentials name.
#'
#' @field oauthScopes  list( character ) ADLS Gen2 OAuth scopes.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsBodyOneOf6 <- R6::R6Class(
  "CredentialsBodyOneOf6",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(clientId = NULL, clientSecret = NULL, credentialType = NULL, description = NULL, name = NULL, oauthScopes = NULL) {
      if (!is.null(`clientId`)) {
        stopifnot(is.character(`clientId`), length(`clientId`) == 1)
      }
      if (!is.null(`clientSecret`)) {
        stopifnot(is.character(`clientSecret`), length(`clientSecret`) == 1)
      }
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`oauthScopes`)) {
        stopifnot(is.vector(`oauthScopes`))
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
    }
  ),
  public = list(
    `clientId` = NULL,
    `clientSecret` = NULL,
    `credentialType` = NULL,
    `description` = NULL,
    `name` = NULL,
    `oauthScopes` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param clientId ADLS Gen2 OAuth client ID.
    #' @param clientSecret ADLS Gen2 OAuth client secret.
    #' @param credentialType Credentials type.
    #' @param description Credentials description.
    #' @param name Credentials name.
    #' @param oauthScopes ADLS Gen2 OAuth scopes.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`clientId` = NULL, `clientSecret` = NULL, `credentialType` = NULL, `name` = NULL, `oauthScopes` = NULL, `description` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`clientId`, `clientSecret`, `credentialType`, `name`, `oauthScopes`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(clientId, clientSecret, credentialType, description, name, oauthScopes)
      }
      self$`clientId` <- `clientId`
      self$`clientSecret` <- `clientSecret`
      self$`credentialType` <- `credentialType`
      self$`description` <- `description`
      self$`name` <- `name`
      sapply(`oauthScopes`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(clientId = self$`clientId`, clientSecret = self$`clientSecret`, credentialType = self$`credentialType`, description = self$`description`, name = self$`name`, oauthScopes = self$`oauthScopes`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`clientId`)) {
          sprintf(
            '"clientId":
            "%s"
                  ',
            self$`clientId`
          )
        },
        if (!is.null(self$`clientSecret`)) {
          sprintf(
            '"clientSecret":
            "%s"
                  ',
            self$`clientSecret`
          )
        },
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`oauthScopes`)) {
          sprintf(
            '"oauthScopes":
              [%s]
            ',
            paste(unlist(lapply(self$`oauthScopes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsBodyOneOf6Json A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsBodyOneOf6Json, validateParams = FALSE) {
      CredentialsBodyOneOf6Object <- jsonlite::fromJSON(CredentialsBodyOneOf6Json)
      self$`clientId` <- CredentialsBodyOneOf6Object$`clientId`
      self$`clientSecret` <- CredentialsBodyOneOf6Object$`clientSecret`
      self$`credentialType` <- CredentialsBodyOneOf6Object$`credentialType`
      self$`description` <- CredentialsBodyOneOf6Object$`description`
      self$`name` <- CredentialsBodyOneOf6Object$`name`
      self$`oauthScopes` <- ApiClient$new()$deserializeObj(CredentialsBodyOneOf6Object$`oauthScopes`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
