# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RocCurveForDatasets
#'
#' @description RocCurveForDatasets Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetId  character The ID of dataset which was used to compute ROC curve.
#'
#' @field negativeClassPredictions  list( numeric ) List of example predictions for the negative class.
#'
#' @field positiveClassPredictions  list( numeric ) List of example predictions for the negative class.
#'
#' @field rocPoints  \link{RocPointsResponse}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RocCurveForDatasets <- R6::R6Class(
  "RocCurveForDatasets",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datasetId` = NULL, `negativeClassPredictions` = NULL, `positiveClassPredictions` = NULL, `rocPoints` = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`negativeClassPredictions`)) {
        stopifnot(is.vector(`negativeClassPredictions`), sapply(`negativeClassPredictions`, is.numeric))
      }
      if (!is.null(`positiveClassPredictions`)) {
        stopifnot(is.vector(`positiveClassPredictions`), sapply(`positiveClassPredictions`, is.numeric))
      }
      if (!is.null(`rocPoints`)) {
        stopifnot(R6::is.R6(`rocPoints`))
      }
    }
  ),
  public = list(
    `datasetId` = NULL,
    `negativeClassPredictions` = NULL,
    `positiveClassPredictions` = NULL,
    `rocPoints` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetId The ID of dataset which was used to compute ROC curve.
    #' @param negativeClassPredictions List of example predictions for the negative class.
    #' @param positiveClassPredictions List of example predictions for the negative class.
    #' @param rocPoints
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `negativeClassPredictions` = NULL, `positiveClassPredictions` = NULL, `rocPoints` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datasetId`, `negativeClassPredictions`, `positiveClassPredictions`, `rocPoints`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datasetId, negativeClassPredictions, positiveClassPredictions, rocPoints)
      }
      self$`datasetId` <- `datasetId`
      self$`negativeClassPredictions` <- `negativeClassPredictions`
      self$`positiveClassPredictions` <- `positiveClassPredictions`
      self$`rocPoints` <- `rocPoints`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datasetId = self$`datasetId`, negativeClassPredictions = self$`negativeClassPredictions`, positiveClassPredictions = self$`positiveClassPredictions`, rocPoints = self$`rocPoints`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`negativeClassPredictions`)) {
          sprintf(
            '"negativeClassPredictions":
            [%s]
                  ',
            paste(unlist(lapply(self$`negativeClassPredictions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`positiveClassPredictions`)) {
          sprintf(
            '"positiveClassPredictions":
            [%s]
                  ',
            paste(unlist(lapply(self$`positiveClassPredictions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`rocPoints`)) {
          sprintf(
            '"rocPoints":
            %s
      ',
            jsonlite::toJSON(self$`rocPoints`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RocCurveForDatasetsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RocCurveForDatasetsJson, validateParams = FALSE) {
      RocCurveForDatasetsObject <- jsonlite::fromJSON(RocCurveForDatasetsJson)
      self$`datasetId` <- RocCurveForDatasetsObject$`datasetId`
      self$`negativeClassPredictions` <- ApiClient$new()$deserializeObj(RocCurveForDatasetsObject$`negativeClassPredictions`, "array[numeric]", loadNamespace("datarobot.apicore"))
      self$`positiveClassPredictions` <- ApiClient$new()$deserializeObj(RocCurveForDatasetsObject$`positiveClassPredictions`, "array[numeric]", loadNamespace("datarobot.apicore"))
      self$`rocPoints` <- RocPointsResponse$new()$fromJSON(jsonlite::toJSON(RocCurveForDatasetsObject$rocPoints, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
