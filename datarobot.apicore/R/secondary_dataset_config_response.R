# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SecondaryDatasetConfigResponse
#'
#' @description SecondaryDatasetConfigResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field created  character DR-formatted datetime, null for legacy (before DR 6.0) db records
#'
#' @field creatorFullName  character Fullname or email of the user created this config. null for legacy (before DR 6.0) db records
#'
#' @field creatorUserId  character ID of the user created this config, null for legacy (before DR 6.0) db records
#'
#' @field credentialIds  list( \link{DatasetsCredential} ) [optional] List of credentials used by the secondary datasets if the datasets used in the configuration are from datasource
#'
#' @field featurelistId  character Id of the feature list
#'
#' @field isDefault  character Secondary datasets config is default config or not
#'
#' @field isDeleted  character Secondary datasets config is soft deleted or not
#'
#' @field name  character Name of the secondary datasets config
#'
#' @field projectId  character ID of the project
#'
#' @field secondaryDatasetConfigId  character ID of the secondary datasets configuration
#'
#' @field secondaryDatasets  list( \link{DeploymentSecondaryDataset} ) [optional] List of secondary datasets used in the config
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SecondaryDatasetConfigResponse <- R6::R6Class(
  "SecondaryDatasetConfigResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`created` = NULL, `creatorFullName` = NULL, `creatorUserId` = NULL, `credentialIds` = NULL, `featurelistId` = NULL, `isDefault` = NULL, `isDeleted` = NULL, `name` = NULL, `projectId` = NULL, `secondaryDatasetConfigId` = NULL, `secondaryDatasets` = NULL) {
      if (!is.null(`created`)) {
        stopifnot(inherits(`created`, "POSIXt"))
      }
      if (!is.null(`creatorFullName`)) {
        stopifnot(is.character(`creatorFullName`), length(`creatorFullName`) == 1)
      }
      if (!is.null(`creatorUserId`)) {
        stopifnot(is.character(`creatorUserId`), length(`creatorUserId`) == 1)
      }
      if (!is.null(`featurelistId`)) {
        stopifnot(is.character(`featurelistId`), length(`featurelistId`) == 1)
      }
      if (!is.null(`isDefault`)) {
        stopifnot(is.logical(`isDefault`), length(`isDefault`) == 1)
      }
      if (!is.null(`isDeleted`)) {
        stopifnot(is.logical(`isDeleted`), length(`isDeleted`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`secondaryDatasetConfigId`)) {
        stopifnot(is.character(`secondaryDatasetConfigId`), length(`secondaryDatasetConfigId`) == 1)
      }
      if (!is.null(`credentialIds`)) {
        stopifnot(is.vector(`credentialIds`))
      }
      if (!is.null(`secondaryDatasets`)) {
        stopifnot(is.vector(`secondaryDatasets`))
      }
    }
  ),
  public = list(
    `created` = NULL,
    `creatorFullName` = NULL,
    `creatorUserId` = NULL,
    `credentialIds` = NULL,
    `featurelistId` = NULL,
    `isDefault` = NULL,
    `isDeleted` = NULL,
    `name` = NULL,
    `projectId` = NULL,
    `secondaryDatasetConfigId` = NULL,
    `secondaryDatasets` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param created DR-formatted datetime, null for legacy (before DR 6.0) db records
    #' @param creatorFullName Fullname or email of the user created this config. null for legacy (before DR 6.0) db records
    #' @param creatorUserId ID of the user created this config, null for legacy (before DR 6.0) db records
    #' @param credentialIds List of credentials used by the secondary datasets if the datasets used in the configuration are from datasource
    #' @param featurelistId Id of the feature list
    #' @param isDefault Secondary datasets config is default config or not
    #' @param isDeleted Secondary datasets config is soft deleted or not
    #' @param name Name of the secondary datasets config
    #' @param projectId ID of the project
    #' @param secondaryDatasetConfigId ID of the secondary datasets configuration
    #' @param secondaryDatasets List of secondary datasets used in the config
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`created` = NULL, `creatorFullName` = NULL, `creatorUserId` = NULL, `featurelistId` = NULL, `isDefault` = NULL, `isDeleted` = NULL, `name` = NULL, `projectId` = NULL, `secondaryDatasetConfigId` = NULL, `credentialIds` = NULL, `secondaryDatasets` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`created`, `creatorFullName`, `creatorUserId`, `featurelistId`, `isDefault`, `isDeleted`, `name`, `projectId`, `secondaryDatasetConfigId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(created, creatorFullName, creatorUserId, credentialIds, featurelistId, isDefault, isDeleted, name, projectId, secondaryDatasetConfigId, secondaryDatasets)
      }
      self$`created` <- `created`
      self$`creatorFullName` <- `creatorFullName`
      self$`creatorUserId` <- `creatorUserId`
      sapply(`credentialIds`, function(x) stopifnot(R6::is.R6(x)))
      self$`featurelistId` <- `featurelistId`
      self$`isDefault` <- `isDefault`
      self$`isDeleted` <- `isDeleted`
      self$`name` <- `name`
      self$`projectId` <- `projectId`
      self$`secondaryDatasetConfigId` <- `secondaryDatasetConfigId`
      sapply(`secondaryDatasets`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(created = self$`created`, creatorFullName = self$`creatorFullName`, creatorUserId = self$`creatorUserId`, credentialIds = self$`credentialIds`, featurelistId = self$`featurelistId`, isDefault = self$`isDefault`, isDeleted = self$`isDeleted`, name = self$`name`, projectId = self$`projectId`, secondaryDatasetConfigId = self$`secondaryDatasetConfigId`, secondaryDatasets = self$`secondaryDatasets`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`created`)) {
          sprintf(
            '"created":
            "%s"
                  ',
            format(self$`created`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`creatorFullName`)) {
          sprintf(
            '"creatorFullName":
            "%s"
                  ',
            self$`creatorFullName`
          )
        },
        if (!is.null(self$`creatorUserId`)) {
          sprintf(
            '"creatorUserId":
            "%s"
                  ',
            self$`creatorUserId`
          )
        },
        if (!is.null(self$`credentialIds`)) {
          sprintf(
            '"credentialIds":
            [%s]
      ',
            paste(sapply(self$`credentialIds`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`featurelistId`)) {
          sprintf(
            '"featurelistId":
            "%s"
                  ',
            self$`featurelistId`
          )
        },
        if (!is.null(self$`isDefault`)) {
          sprintf(
            '"isDefault":
            %s
                  ',
            tolower(self$`isDefault`)
          )
        },
        if (!is.null(self$`isDeleted`)) {
          sprintf(
            '"isDeleted":
            %s
                  ',
            tolower(self$`isDeleted`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`secondaryDatasetConfigId`)) {
          sprintf(
            '"secondaryDatasetConfigId":
            "%s"
                  ',
            self$`secondaryDatasetConfigId`
          )
        },
        if (!is.null(self$`secondaryDatasets`)) {
          sprintf(
            '"secondaryDatasets":
            [%s]
      ',
            paste(sapply(self$`secondaryDatasets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SecondaryDatasetConfigResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SecondaryDatasetConfigResponseJson, validateParams = FALSE) {
      SecondaryDatasetConfigResponseObject <- jsonlite::fromJSON(SecondaryDatasetConfigResponseJson)
      self$`created` <- ParseRFC3339Timestamp(SecondaryDatasetConfigResponseObject$`created`)
      self$`creatorFullName` <- SecondaryDatasetConfigResponseObject$`creatorFullName`
      self$`creatorUserId` <- SecondaryDatasetConfigResponseObject$`creatorUserId`
      self$`credentialIds` <- ApiClient$new()$deserializeObj(SecondaryDatasetConfigResponseObject$`credentialIds`, "array[DatasetsCredential]", loadNamespace("datarobot.apicore"))
      self$`featurelistId` <- SecondaryDatasetConfigResponseObject$`featurelistId`
      self$`isDefault` <- SecondaryDatasetConfigResponseObject$`isDefault`
      self$`isDeleted` <- SecondaryDatasetConfigResponseObject$`isDeleted`
      self$`name` <- SecondaryDatasetConfigResponseObject$`name`
      self$`projectId` <- SecondaryDatasetConfigResponseObject$`projectId`
      self$`secondaryDatasetConfigId` <- SecondaryDatasetConfigResponseObject$`secondaryDatasetConfigId`
      self$`secondaryDatasets` <- ApiClient$new()$deserializeObj(SecondaryDatasetConfigResponseObject$`secondaryDatasets`, "array[DeploymentSecondaryDataset]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
