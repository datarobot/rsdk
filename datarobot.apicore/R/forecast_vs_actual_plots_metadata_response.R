# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ForecastVsActualPlotsMetadataResponse
#'
#' @description ForecastVsActualPlotsMetadataResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtestMetadata  list( \link{DatetimeTrendPlotsBacktestMetadata} ) An array of metadata information for each backtest. The array index of metadata object is the backtest index.
#'
#' @field backtestStatuses  list( \link{ForecastVsActualPlotsStatus} ) An array of status information for each backtest. The array index of status object is the backtest index.
#'
#' @field estimatedSeriesLimit  integer [optional] Estimated number of series that can be calculated in one request for 1 FD.
#'
#' @field holdoutMetadata  \link{DatetimeTrendPlotsBacktestMetadata}
#'
#' @field holdoutStatuses  \link{ForecastVsActualPlotsStatus}
#'
#' @field resolutions  list( character ) An array of available time resolutions for which plots can be retrieved.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ForecastVsActualPlotsMetadataResponse <- R6::R6Class(
  "ForecastVsActualPlotsMetadataResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`backtestMetadata` = NULL, `backtestStatuses` = NULL, `estimatedSeriesLimit` = NULL, `holdoutMetadata` = NULL, `holdoutStatuses` = NULL, `resolutions` = NULL) {
      if (!is.null(`backtestMetadata`)) {
        stopifnot(is.vector(`backtestMetadata`), sapply(`backtestMetadata`, R6::is.R6))
      }
      if (!is.null(`backtestStatuses`)) {
        stopifnot(is.vector(`backtestStatuses`), sapply(`backtestStatuses`, R6::is.R6))
      }
      if (!is.null(`holdoutMetadata`)) {
        stopifnot(R6::is.R6(`holdoutMetadata`))
      }
      if (!is.null(`holdoutStatuses`)) {
        stopifnot(R6::is.R6(`holdoutStatuses`))
      }
      if (!is.null(`resolutions`)) {
        stopifnot(is.vector(`resolutions`), sapply(`resolutions`, is.character))
      }
      if (!is.null(`estimatedSeriesLimit`)) {
        stopifnot(is.numeric(`estimatedSeriesLimit`), length(`estimatedSeriesLimit`) == 1)
      }
    }
  ),
  public = list(
    `backtestMetadata` = NULL,
    `backtestStatuses` = NULL,
    `estimatedSeriesLimit` = NULL,
    `holdoutMetadata` = NULL,
    `holdoutStatuses` = NULL,
    `resolutions` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtestMetadata An array of metadata information for each backtest. The array index of metadata object is the backtest index.
    #' @param backtestStatuses An array of status information for each backtest. The array index of status object is the backtest index.
    #' @param estimatedSeriesLimit Estimated number of series that can be calculated in one request for 1 FD.
    #' @param holdoutMetadata
    #' @param holdoutStatuses
    #' @param resolutions An array of available time resolutions for which plots can be retrieved.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`backtestMetadata` = NULL, `backtestStatuses` = NULL, `holdoutMetadata` = NULL, `holdoutStatuses` = NULL, `resolutions` = NULL, `estimatedSeriesLimit` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`backtestMetadata`, `backtestStatuses`, `holdoutMetadata`, `holdoutStatuses`, `resolutions`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(backtestMetadata, backtestStatuses, estimatedSeriesLimit, holdoutMetadata, holdoutStatuses, resolutions)
      }
      self$`backtestMetadata` <- `backtestMetadata`
      self$`backtestStatuses` <- `backtestStatuses`
      self$`estimatedSeriesLimit` <- `estimatedSeriesLimit`
      self$`holdoutMetadata` <- `holdoutMetadata`
      self$`holdoutStatuses` <- `holdoutStatuses`
      self$`resolutions` <- `resolutions`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(backtestMetadata = self$`backtestMetadata`, backtestStatuses = self$`backtestStatuses`, estimatedSeriesLimit = self$`estimatedSeriesLimit`, holdoutMetadata = self$`holdoutMetadata`, holdoutStatuses = self$`holdoutStatuses`, resolutions = self$`resolutions`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtestMetadata`)) {
          sprintf(
            '"backtestMetadata":
            [%s]
      ',
            paste(sapply(self$`backtestMetadata`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`backtestStatuses`)) {
          sprintf(
            '"backtestStatuses":
            [%s]
      ',
            paste(sapply(self$`backtestStatuses`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`estimatedSeriesLimit`)) {
          sprintf(
            '"estimatedSeriesLimit":
            %d
                  ',
            self$`estimatedSeriesLimit`
          )
        },
        if (!is.null(self$`holdoutMetadata`)) {
          sprintf(
            '"holdoutMetadata":
            %s
      ',
            jsonlite::toJSON(self$`holdoutMetadata`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`holdoutStatuses`)) {
          sprintf(
            '"holdoutStatuses":
            %s
      ',
            jsonlite::toJSON(self$`holdoutStatuses`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`resolutions`)) {
          sprintf(
            '"resolutions":
            [%s]
                  ',
            paste(unlist(lapply(self$`resolutions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ForecastVsActualPlotsMetadataResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ForecastVsActualPlotsMetadataResponseJson, validateParams = FALSE) {
      ForecastVsActualPlotsMetadataResponseObject <- jsonlite::fromJSON(ForecastVsActualPlotsMetadataResponseJson)
      self$`backtestMetadata` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsMetadataResponseObject$`backtestMetadata`, "array[DatetimeTrendPlotsBacktestMetadata]", loadNamespace("datarobot.apicore"))
      self$`backtestStatuses` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsMetadataResponseObject$`backtestStatuses`, "array[ForecastVsActualPlotsStatus]", loadNamespace("datarobot.apicore"))
      self$`estimatedSeriesLimit` <- ForecastVsActualPlotsMetadataResponseObject$`estimatedSeriesLimit`
      self$`holdoutMetadata` <- DatetimeTrendPlotsBacktestMetadata$new()$fromJSON(jsonlite::toJSON(ForecastVsActualPlotsMetadataResponseObject$holdoutMetadata, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`holdoutStatuses` <- ForecastVsActualPlotsStatus$new()$fromJSON(jsonlite::toJSON(ForecastVsActualPlotsMetadataResponseObject$holdoutStatuses, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`resolutions` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsMetadataResponseObject$`resolutions`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
