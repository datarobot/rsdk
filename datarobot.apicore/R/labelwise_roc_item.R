# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title LabelwiseROCItem
#'
#' @description LabelwiseROCItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field auc  numeric Area under the curve.
#'
#' @field kolmogorovSmirnovMetric  numeric Kolmogorov-Smirnov metric.
#'
#' @field label  character Label name.
#'
#' @field negativeClassPredictions  list( numeric ) List of example predictions for the negative class.
#'
#' @field positiveClassPredictions  list( numeric ) List of example predictions for the positive class.
#'
#' @field rocPoints  list( \link{LabelwiseROCPoint} ) ROC characteristics for label.
#'
#' @field source  character Data source of ROC characteristics.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
LabelwiseROCItem <- R6::R6Class(
  "LabelwiseROCItem",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`auc` = NULL, `kolmogorovSmirnovMetric` = NULL, `label` = NULL, `negativeClassPredictions` = NULL, `positiveClassPredictions` = NULL, `rocPoints` = NULL, `source` = NULL) {
      if (!is.null(`auc`)) {
      }
      if (!is.null(`kolmogorovSmirnovMetric`)) {
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`negativeClassPredictions`)) {
        stopifnot(is.vector(`negativeClassPredictions`))
      }
      if (!is.null(`positiveClassPredictions`)) {
        stopifnot(is.vector(`positiveClassPredictions`))
      }
      if (!is.null(`rocPoints`)) {
        stopifnot(is.vector(`rocPoints`))
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
    }
  ),
  public = list(
    `auc` = NULL,
    `kolmogorovSmirnovMetric` = NULL,
    `label` = NULL,
    `negativeClassPredictions` = NULL,
    `positiveClassPredictions` = NULL,
    `rocPoints` = NULL,
    `source` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param auc Area under the curve.
    #' @param kolmogorovSmirnovMetric Kolmogorov-Smirnov metric.
    #' @param label Label name.
    #' @param negativeClassPredictions List of example predictions for the negative class.
    #' @param positiveClassPredictions List of example predictions for the positive class.
    #' @param rocPoints ROC characteristics for label.
    #' @param source Data source of ROC characteristics.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`auc` = NULL, `kolmogorovSmirnovMetric` = NULL, `label` = NULL, `negativeClassPredictions` = NULL, `positiveClassPredictions` = NULL, `rocPoints` = NULL, `source` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`auc`, `kolmogorovSmirnovMetric`, `label`, `negativeClassPredictions`, `positiveClassPredictions`, `rocPoints`, `source`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(auc, kolmogorovSmirnovMetric, label, negativeClassPredictions, positiveClassPredictions, rocPoints, source)
      }
      self$`auc` <- `auc`
      self$`kolmogorovSmirnovMetric` <- `kolmogorovSmirnovMetric`
      self$`label` <- `label`
      sapply(`negativeClassPredictions`, function(x) stopifnot(is.character(x)))
      sapply(`positiveClassPredictions`, function(x) stopifnot(is.character(x)))
      sapply(`rocPoints`, function(x) stopifnot(R6::is.R6(x)))
      self$`source` <- `source`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(auc = self$`auc`, kolmogorovSmirnovMetric = self$`kolmogorovSmirnovMetric`, label = self$`label`, negativeClassPredictions = self$`negativeClassPredictions`, positiveClassPredictions = self$`positiveClassPredictions`, rocPoints = self$`rocPoints`, source = self$`source`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`auc`)) {
          sprintf(
            '"auc":
            %d
                  ',
            self$`auc`
          )
        },
        if (!is.null(self$`kolmogorovSmirnovMetric`)) {
          sprintf(
            '"kolmogorovSmirnovMetric":
            %d
                  ',
            self$`kolmogorovSmirnovMetric`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`negativeClassPredictions`)) {
          sprintf(
            '"negativeClassPredictions":
            [%s]
                  ',
            paste(unlist(lapply(self$`negativeClassPredictions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`positiveClassPredictions`)) {
          sprintf(
            '"positiveClassPredictions":
            [%s]
                  ',
            paste(unlist(lapply(self$`positiveClassPredictions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`rocPoints`)) {
          sprintf(
            '"rocPoints":
            [%s]
      ',
            paste(sapply(self$`rocPoints`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param LabelwiseROCItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(LabelwiseROCItemJson, validateParams = FALSE) {
      LabelwiseROCItemObject <- jsonlite::fromJSON(LabelwiseROCItemJson)
      self$`auc` <- LabelwiseROCItemObject$`auc`
      self$`kolmogorovSmirnovMetric` <- LabelwiseROCItemObject$`kolmogorovSmirnovMetric`
      self$`label` <- LabelwiseROCItemObject$`label`
      self$`negativeClassPredictions` <- ApiClient$new()$deserializeObj(LabelwiseROCItemObject$`negativeClassPredictions`, "array[numeric]", loadNamespace("datarobot.apicore"))
      self$`positiveClassPredictions` <- ApiClient$new()$deserializeObj(LabelwiseROCItemObject$`positiveClassPredictions`, "array[numeric]", loadNamespace("datarobot.apicore"))
      self$`rocPoints` <- ApiClient$new()$deserializeObj(LabelwiseROCItemObject$`rocPoints`, "array[LabelwiseROCPoint]", loadNamespace("datarobot.apicore"))
      self$`source` <- LabelwiseROCItemObject$`source`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
