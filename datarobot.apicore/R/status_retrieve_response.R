# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title StatusRetrieveResponse
#'
#' @description StatusRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field code  integer If no error occurred, 0.  Otherwise, may contain a status code reflecting the error.
#'
#' @field created  character The time the status record was created.
#'
#' @field description  character A description of the Task being performed, if applicable.
#'
#' @field message  character May contain further information about the status.
#'
#' @field status  character The status of the task.
#'
#' @field statusId  character The ID of the status object.
#'
#' @field statusType  character [optional] The type of thing being created by the Task, if applicable.
#'
#' @field type  character [optional] The type of thing being created by the Task, if applicable.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
StatusRetrieveResponse <- R6::R6Class(
  "StatusRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(code = NULL, created = NULL, description = NULL, message = NULL, status = NULL, statusId = NULL, statusType = NULL, type = NULL) {
      if (!is.null(`code`)) {
        stopifnot(is.numeric(`code`), length(`code`) == 1)
      }
      if (!is.null(`created`)) {
        stopifnot(inherits(`created`, "POSIXt"))
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`message`)) {
        stopifnot(is.character(`message`), length(`message`) == 1)
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
      if (!is.null(`statusId`)) {
        stopifnot(is.character(`statusId`), length(`statusId`) == 1)
      }
      if (!is.null(`statusType`)) {
        stopifnot(is.character(`statusType`), length(`statusType`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
    }
  ),
  public = list(
    `code` = NULL,
    `created` = NULL,
    `description` = NULL,
    `message` = NULL,
    `status` = NULL,
    `statusId` = NULL,
    `statusType` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param code If no error occurred, 0.  Otherwise, may contain a status code reflecting the error.
    #' @param created The time the status record was created.
    #' @param description A description of the Task being performed, if applicable.
    #' @param message May contain further information about the status.
    #' @param status The status of the task.
    #' @param statusId The ID of the status object.
    #' @param statusType The type of thing being created by the Task, if applicable.
    #' @param type The type of thing being created by the Task, if applicable.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`code` = NULL, `created` = NULL, `description` = NULL, `message` = NULL, `status` = NULL, `statusId` = NULL, `statusType` = NULL, `type` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`code`, `created`, `description`, `message`, `status`, `statusId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(code, created, description, message, status, statusId, statusType, type)
      }
      self$`code` <- `code`
      self$`created` <- `created`
      self$`description` <- `description`
      self$`message` <- `message`
      self$`status` <- `status`
      self$`statusId` <- `statusId`
      self$`statusType` <- `statusType`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(code = self$`code`, created = self$`created`, description = self$`description`, message = self$`message`, status = self$`status`, statusId = self$`statusId`, statusType = self$`statusType`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`code`)) {
          sprintf(
            '"code":
            %d
                  ',
            self$`code`
          )
        },
        if (!is.null(self$`created`)) {
          sprintf(
            '"created":
            "%s"
                  ',
            format(self$`created`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`message`)) {
          sprintf(
            '"message":
            "%s"
                  ',
            self$`message`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        },
        if (!is.null(self$`statusId`)) {
          sprintf(
            '"statusId":
            "%s"
                  ',
            self$`statusId`
          )
        },
        if (!is.null(self$`statusType`)) {
          sprintf(
            '"statusType":
            "%s"
                  ',
            self$`statusType`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param StatusRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(StatusRetrieveResponseJson, validateParams = FALSE) {
      StatusRetrieveResponseObject <- jsonlite::fromJSON(StatusRetrieveResponseJson)
      self$`code` <- StatusRetrieveResponseObject$`code`
      self$`created` <- ParseRFC3339Timestamp(StatusRetrieveResponseObject$`created`)
      self$`description` <- StatusRetrieveResponseObject$`description`
      self$`message` <- StatusRetrieveResponseObject$`message`
      self$`status` <- StatusRetrieveResponseObject$`status`
      self$`statusId` <- StatusRetrieveResponseObject$`statusId`
      self$`statusType` <- StatusRetrieveResponseObject$`statusType`
      self$`type` <- StatusRetrieveResponseObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
