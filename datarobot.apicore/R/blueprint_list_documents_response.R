# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BlueprintListDocumentsResponse
#'
#' @description BlueprintListDocumentsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field description  character The task description.
#'
#' @field links  list( \link{BlueprintDocLinks} ) A list of external documentation links.
#'
#' @field parameters  list( \link{BlueprintDocParameters} ) An array of task parameters.
#'
#' @field references  list( \link{BlueprintDocReferences} ) A list of reference links.
#'
#' @field task  character The task described in document.
#'
#' @field title  character The document title.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BlueprintListDocumentsResponse <- R6::R6Class(
  "BlueprintListDocumentsResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`description` = NULL, `links` = NULL, `parameters` = NULL, `references` = NULL, `task` = NULL, `title` = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`links`)) {
        stopifnot(is.vector(`links`))
      }
      if (!is.null(`parameters`)) {
        stopifnot(is.vector(`parameters`))
      }
      if (!is.null(`references`)) {
        stopifnot(is.vector(`references`))
      }
      if (!is.null(`task`)) {
        stopifnot(is.character(`task`), length(`task`) == 1)
      }
      if (!is.null(`title`)) {
        stopifnot(is.character(`title`), length(`title`) == 1)
      }
    }
  ),
  public = list(
    `description` = NULL,
    `links` = NULL,
    `parameters` = NULL,
    `references` = NULL,
    `task` = NULL,
    `title` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param description The task description.
    #' @param links A list of external documentation links.
    #' @param parameters An array of task parameters.
    #' @param references A list of reference links.
    #' @param task The task described in document.
    #' @param title The document title.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `links` = NULL, `parameters` = NULL, `references` = NULL, `task` = NULL, `title` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`description`, `links`, `parameters`, `references`, `task`, `title`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(description, links, parameters, references, task, title)
      }
      self$`description` <- `description`
      sapply(`links`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`parameters`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`references`, function(x) stopifnot(R6::is.R6(x)))
      self$`task` <- `task`
      self$`title` <- `title`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(description = self$`description`, links = self$`links`, parameters = self$`parameters`, references = self$`references`, task = self$`task`, title = self$`title`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`links`)) {
          sprintf(
            '"links":
            [%s]
      ',
            paste(sapply(self$`links`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`parameters`)) {
          sprintf(
            '"parameters":
            [%s]
      ',
            paste(sapply(self$`parameters`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`references`)) {
          sprintf(
            '"references":
            [%s]
      ',
            paste(sapply(self$`references`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`task`)) {
          sprintf(
            '"task":
            "%s"
                  ',
            self$`task`
          )
        },
        if (!is.null(self$`title`)) {
          sprintf(
            '"title":
            "%s"
                  ',
            self$`title`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BlueprintListDocumentsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BlueprintListDocumentsResponseJson, validateParams = FALSE) {
      BlueprintListDocumentsResponseObject <- jsonlite::fromJSON(BlueprintListDocumentsResponseJson)
      self$`description` <- BlueprintListDocumentsResponseObject$`description`
      self$`links` <- ApiClient$new()$deserializeObj(BlueprintListDocumentsResponseObject$`links`, "array[BlueprintDocLinks]", loadNamespace("datarobot.apicore"))
      self$`parameters` <- ApiClient$new()$deserializeObj(BlueprintListDocumentsResponseObject$`parameters`, "array[BlueprintDocParameters]", loadNamespace("datarobot.apicore"))
      self$`references` <- ApiClient$new()$deserializeObj(BlueprintListDocumentsResponseObject$`references`, "array[BlueprintDocReferences]", loadNamespace("datarobot.apicore"))
      self$`task` <- BlueprintListDocumentsResponseObject$`task`
      self$`title` <- BlueprintListDocumentsResponseObject$`title`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
