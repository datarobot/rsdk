# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotificationEmailChannelVerificationStatus
#'
#' @description NotificationEmailChannelVerificationStatus Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field emailAddress  character The email address of the recipient
#'
#' @field verificationCode  character The user entered verification code
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotificationEmailChannelVerificationStatus <- R6::R6Class(
  "NotificationEmailChannelVerificationStatus",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(emailAddress = NULL, verificationCode = NULL) {
      if (!is.null(`emailAddress`)) {
        stopifnot(is.character(`emailAddress`), length(`emailAddress`) == 1)
      }
      if (!is.null(`verificationCode`)) {
        stopifnot(is.character(`verificationCode`), length(`verificationCode`) == 1)
      }
    }
  ),
  public = list(
    `emailAddress` = NULL,
    `verificationCode` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param emailAddress The email address of the recipient
    #' @param verificationCode The user entered verification code
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`emailAddress` = NULL, `verificationCode` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`emailAddress`, `verificationCode`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(emailAddress, verificationCode)
      }
      self$`emailAddress` <- `emailAddress`
      self$`verificationCode` <- `verificationCode`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(emailAddress = self$`emailAddress`, verificationCode = self$`verificationCode`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`emailAddress`)) {
          sprintf(
            '"emailAddress":
            "%s"
                  ',
            self$`emailAddress`
          )
        },
        if (!is.null(self$`verificationCode`)) {
          sprintf(
            '"verificationCode":
            "%s"
                  ',
            self$`verificationCode`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotificationEmailChannelVerificationStatusJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotificationEmailChannelVerificationStatusJson, validateParams = FALSE) {
      NotificationEmailChannelVerificationStatusObject <- jsonlite::fromJSON(NotificationEmailChannelVerificationStatusJson)
      self$`emailAddress` <- NotificationEmailChannelVerificationStatusObject$`emailAddress`
      self$`verificationCode` <- NotificationEmailChannelVerificationStatusObject$`verificationCode`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
