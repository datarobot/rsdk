# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureListCreate
#'
#' @description FeatureListCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field description  character [optional] Description of the featurelist
#'
#' @field features  list( character ) List of names of features to be included in the new featurelist, all features listed must be part of the universe. All features for this dataset for the request to succeed.
#'
#' @field name  character Name of the featurelist to be created
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureListCreate <- R6::R6Class(
  "FeatureListCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`description` = NULL, `features` = NULL, `name` = NULL) {
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
    }
  ),
  public = list(
    `description` = NULL,
    `features` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param description Description of the featurelist
    #' @param features List of names of features to be included in the new featurelist, all features listed must be part of the universe. All features for this dataset for the request to succeed.
    #' @param name Name of the featurelist to be created
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`features` = NULL, `name` = NULL, `description` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`features`, `name`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(description, features, name)
      }
      self$`description` <- `description`
      sapply(`features`, function(x) stopifnot(is.character(x)))
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(description = self$`description`, features = self$`features`, name = self$`name`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
                  ',
            paste(unlist(lapply(self$`features`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureListCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureListCreateJson, validateParams = FALSE) {
      FeatureListCreateObject <- jsonlite::fromJSON(FeatureListCreateJson)
      self$`description` <- FeatureListCreateObject$`description`
      self$`features` <- ApiClient$new()$deserializeObj(FeatureListCreateObject$`features`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`name` <- FeatureListCreateObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
