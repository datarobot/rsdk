# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseActivity
#'
#' @description UseCaseActivity Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field eventDescription  \link{EventDescriptions}
#'
#' @field eventType  character The type of activity
#'
#' @field firstName  character [optional] First name of use case owner
#'
#' @field id  character [optional] The id of the use case activity
#'
#' @field lastName  character [optional] Last of use case owner
#'
#' @field timestamp  character The timestamp of the activity
#'
#' @field user  \link{DrUserId}
#'
#' @field username  character [optional] Username of use case owner
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseActivity <- R6::R6Class(
  "UseCaseActivity",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(eventDescription = NULL, eventType = NULL, firstName = NULL, id = NULL, lastName = NULL, timestamp = NULL, user = NULL, username = NULL) {
      if (!is.null(`eventDescription`)) {
        stopifnot(R6::is.R6(`eventDescription`))
      }
      if (!is.null(`eventType`)) {
        stopifnot(is.character(`eventType`), length(`eventType`) == 1)
      }
      if (!is.null(`timestamp`)) {
        stopifnot(inherits(`timestamp`, "POSIXt"))
      }
      if (!is.null(`user`)) {
        stopifnot(R6::is.R6(`user`))
      }
      if (!is.null(`firstName`)) {
        stopifnot(is.character(`firstName`), length(`firstName`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`lastName`)) {
        stopifnot(is.character(`lastName`), length(`lastName`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
    }
  ),
  public = list(
    `eventDescription` = NULL,
    `eventType` = NULL,
    `firstName` = NULL,
    `id` = NULL,
    `lastName` = NULL,
    `timestamp` = NULL,
    `user` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param eventDescription
    #' @param eventType The type of activity
    #' @param firstName First name of use case owner
    #' @param id The id of the use case activity
    #' @param lastName Last of use case owner
    #' @param timestamp The timestamp of the activity
    #' @param user
    #' @param username Username of use case owner
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`eventDescription` = NULL, `eventType` = NULL, `timestamp` = NULL, `user` = NULL, `firstName` = NULL, `id` = NULL, `lastName` = NULL, `username` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`eventDescription`, `eventType`, `timestamp`, `user`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(eventDescription, eventType, firstName, id, lastName, timestamp, user, username)
      }
      self$`eventDescription` <- `eventDescription`
      self$`eventType` <- `eventType`
      self$`firstName` <- `firstName`
      self$`id` <- `id`
      self$`lastName` <- `lastName`
      self$`timestamp` <- `timestamp`
      self$`user` <- `user`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(eventDescription = self$`eventDescription`, eventType = self$`eventType`, firstName = self$`firstName`, id = self$`id`, lastName = self$`lastName`, timestamp = self$`timestamp`, user = self$`user`, username = self$`username`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`eventDescription`)) {
          sprintf(
            '"eventDescription":
          %s
        ',
            jsonlite::toJSON(self$`eventDescription`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`eventType`)) {
          sprintf(
            '"eventType":
            "%s"
                  ',
            self$`eventType`
          )
        },
        if (!is.null(self$`firstName`)) {
          sprintf(
            '"firstName":
            "%s"
                  ',
            self$`firstName`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`lastName`)) {
          sprintf(
            '"lastName":
            "%s"
                  ',
            self$`lastName`
          )
        },
        if (!is.null(self$`timestamp`)) {
          sprintf(
            '"timestamp":
            "%s"
                  ',
            format(self$`timestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
          %s
        ',
            jsonlite::toJSON(self$`user`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseActivityJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseActivityJson, validateParams = FALSE) {
      UseCaseActivityObject <- jsonlite::fromJSON(UseCaseActivityJson)
      self$`eventDescription` <- EventDescriptions$new()$fromJSON(jsonlite::toJSON(UseCaseActivityObject$eventDescription, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`eventType` <- UseCaseActivityObject$`eventType`
      self$`firstName` <- UseCaseActivityObject$`firstName`
      self$`id` <- UseCaseActivityObject$`id`
      self$`lastName` <- UseCaseActivityObject$`lastName`
      self$`timestamp` <- ParseRFC3339Timestamp(UseCaseActivityObject$`timestamp`)
      self$`user` <- DrUserId$new()$fromJSON(jsonlite::toJSON(UseCaseActivityObject$user, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`username` <- UseCaseActivityObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
