# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AOTChartRetrieveResponse
#'
#' @description AOTChartRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field bins  list( \link{AOTChartBins} ) The datetime chart data for that source.
#'
#' @field datasetId  character The dataset id to which the chart data belongs.
#'
#' @field endDate  character [optional] The requested &#x60;endDate&#x60;, or, if not specified, the end date for this dataset (exclusive). Example: &#39;2010-05-13T00:00:00.000000Z&#39;.
#'
#' @field modelId  character The model id to which the chart data belongs.
#'
#' @field projectId  character The project id to which the chart data belongs.
#'
#' @field resolution  character The resolution used for binning where a resolution is one of [&#39;milliseconds&#39;, &#39;seconds&#39;, &#39;minutes&#39;, &#39;hours&#39;, &#39;days&#39;, &#39;weeks&#39;, &#39;months&#39;, &#39;years&#39;].
#'
#' @field startDate  character [optional] The requested &#x60;startDate&#x60;, or, if not specified, the start date for this dataset. Example: &#39;2010-05-13T00:00:00.000000Z&#39;.
#'
#' @field statistics  \link{AOTChartStatistics}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AOTChartRetrieveResponse <- R6::R6Class(
  "AOTChartRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`bins` = NULL, `datasetId` = NULL, `endDate` = NULL, `modelId` = NULL, `projectId` = NULL, `resolution` = NULL, `startDate` = NULL, `statistics` = NULL) {
      if (!is.null(`bins`)) {
        stopifnot(is.vector(`bins`), sapply(`bins`, R6::is.R6))
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`resolution`)) {
        stopifnot(is.character(`resolution`), length(`resolution`) == 1)
      }
      if (!is.null(`statistics`)) {
        stopifnot(R6::is.R6(`statistics`))
      }
      if (!is.null(`endDate`)) {
        stopifnot(inherits(`endDate`, "POSIXt"))
      }
      if (!is.null(`startDate`)) {
        stopifnot(inherits(`startDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `bins` = NULL,
    `datasetId` = NULL,
    `endDate` = NULL,
    `modelId` = NULL,
    `projectId` = NULL,
    `resolution` = NULL,
    `startDate` = NULL,
    `statistics` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param bins The datetime chart data for that source.
    #' @param datasetId The dataset id to which the chart data belongs.
    #' @param endDate The requested &#x60;endDate&#x60;, or, if not specified, the end date for this dataset (exclusive). Example: &#39;2010-05-13T00:00:00.000000Z&#39;.
    #' @param modelId The model id to which the chart data belongs.
    #' @param projectId The project id to which the chart data belongs.
    #' @param resolution The resolution used for binning where a resolution is one of [&#39;milliseconds&#39;, &#39;seconds&#39;, &#39;minutes&#39;, &#39;hours&#39;, &#39;days&#39;, &#39;weeks&#39;, &#39;months&#39;, &#39;years&#39;].
    #' @param startDate The requested &#x60;startDate&#x60;, or, if not specified, the start date for this dataset. Example: &#39;2010-05-13T00:00:00.000000Z&#39;.
    #' @param statistics
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`bins` = NULL, `datasetId` = NULL, `modelId` = NULL, `projectId` = NULL, `resolution` = NULL, `statistics` = NULL, `endDate` = NULL, `startDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`bins`, `datasetId`, `modelId`, `projectId`, `resolution`, `statistics`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(bins, datasetId, endDate, modelId, projectId, resolution, startDate, statistics)
      }
      self$`bins` <- `bins`
      self$`datasetId` <- `datasetId`
      self$`endDate` <- `endDate`
      self$`modelId` <- `modelId`
      self$`projectId` <- `projectId`
      self$`resolution` <- `resolution`
      self$`startDate` <- `startDate`
      self$`statistics` <- `statistics`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(bins = self$`bins`, datasetId = self$`datasetId`, endDate = self$`endDate`, modelId = self$`modelId`, projectId = self$`projectId`, resolution = self$`resolution`, startDate = self$`startDate`, statistics = self$`statistics`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`bins`)) {
          sprintf(
            '"bins":
            [%s]
      ',
            paste(sapply(self$`bins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`endDate`)) {
          sprintf(
            '"endDate":
            "%s"
                  ',
            format(self$`endDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`resolution`)) {
          sprintf(
            '"resolution":
            "%s"
                  ',
            self$`resolution`
          )
        },
        if (!is.null(self$`startDate`)) {
          sprintf(
            '"startDate":
            "%s"
                  ',
            format(self$`startDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`statistics`)) {
          sprintf(
            '"statistics":
            %s
      ',
            jsonlite::toJSON(self$`statistics`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AOTChartRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AOTChartRetrieveResponseJson, validateParams = FALSE) {
      AOTChartRetrieveResponseObject <- jsonlite::fromJSON(AOTChartRetrieveResponseJson)
      self$`bins` <- ApiClient$new()$deserializeObj(AOTChartRetrieveResponseObject$`bins`, "array[AOTChartBins]", loadNamespace("datarobot.apicore"))
      self$`datasetId` <- AOTChartRetrieveResponseObject$`datasetId`
      self$`endDate` <- ParseRFC3339Timestamp(AOTChartRetrieveResponseObject$`endDate`)
      self$`modelId` <- AOTChartRetrieveResponseObject$`modelId`
      self$`projectId` <- AOTChartRetrieveResponseObject$`projectId`
      self$`resolution` <- AOTChartRetrieveResponseObject$`resolution`
      self$`startDate` <- ParseRFC3339Timestamp(AOTChartRetrieveResponseObject$`startDate`)
      self$`statistics` <- AOTChartStatistics$new()$fromJSON(jsonlite::toJSON(AOTChartRetrieveResponseObject$statistics, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
