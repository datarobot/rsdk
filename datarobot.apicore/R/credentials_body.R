# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsBody
#'
#' @description CredentialsBody Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialType  character [optional] Credentials type.
#'
#' @field description  character [optional] Credentials description.
#'
#' @field name  character Credentials name.
#'
#' @field password  character Password to store for this credentials.
#'
#' @field snowflakeAccountName  character [optional] Snowflake account name.
#'
#' @field user  character Username to store for this credentials.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsBody <- R6::R6Class(
  "CredentialsBody",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`credentialType` = NULL, `description` = NULL, `name` = NULL, `password` = NULL, `snowflakeAccountName` = NULL, `user` = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`snowflakeAccountName`)) {
        stopifnot(is.character(`snowflakeAccountName`), length(`snowflakeAccountName`) == 1)
      }
    }
  ),
  public = list(
    `credentialType` = NULL,
    `description` = NULL,
    `name` = NULL,
    `password` = NULL,
    `snowflakeAccountName` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialType Credentials type.
    #' @param description Credentials description.
    #' @param name Credentials name.
    #' @param password Password to store for this credentials.
    #' @param snowflakeAccountName Snowflake account name.
    #' @param user Username to store for this credentials.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `password` = NULL, `user` = NULL, `credentialType` = NULL, `description` = NULL, `snowflakeAccountName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`name`, `password`, `user`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(credentialType, description, name, password, snowflakeAccountName, user)
      }
      self$`credentialType` <- `credentialType`
      self$`description` <- `description`
      self$`name` <- `name`
      self$`password` <- `password`
      self$`snowflakeAccountName` <- `snowflakeAccountName`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(credentialType = self$`credentialType`, description = self$`description`, name = self$`name`, password = self$`password`, snowflakeAccountName = self$`snowflakeAccountName`, user = self$`user`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`snowflakeAccountName`)) {
          sprintf(
            '"snowflakeAccountName":
            "%s"
                  ',
            self$`snowflakeAccountName`
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsBodyJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsBodyJson, validateParams = FALSE) {
      CredentialsBodyObject <- jsonlite::fromJSON(CredentialsBodyJson)
      self$`credentialType` <- CredentialsBodyObject$`credentialType`
      self$`description` <- CredentialsBodyObject$`description`
      self$`name` <- CredentialsBodyObject$`name`
      self$`password` <- CredentialsBodyObject$`password`
      self$`snowflakeAccountName` <- CredentialsBodyObject$`snowflakeAccountName`
      self$`user` <- CredentialsBodyObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
