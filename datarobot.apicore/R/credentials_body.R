# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsBody
#'
#' @description CredentialsBody Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialType  character Credentials type.
#'
#' @field description  character [optional] Credentials description.
#'
#' @field name  character Credentials name.
#'
#' @field password  character Password to store for this credentials.
#'
#' @field snowflakeAccountName  character [optional] Snowflake account name.
#'
#' @field user  character Username to store for this credentials.
#'
#' @field refreshToken  character OAUTH token.
#'
#' @field token  character OAUTH token.
#'
#' @field awsAccessKeyId  character [optional] AWS access key ID.
#'
#' @field awsSecretAccessKey  character [optional] AWS secret access key.
#'
#' @field awsSessionToken  character [optional] AWS session token.
#'
#' @field gcpKey  \link{GCPKey}
#'
#' @field azureConnectionString  character Azure connection string.
#'
#' @field clientId  character ADLS Gen2 OAuth client ID.
#'
#' @field clientSecret  character ADLS Gen2 OAuth client secret.
#'
#' @field oauthIssuerType  character [optional] OAuth issuer type.
#'
#' @field oauthIssuerUrl  character [optional] OAuth issuer URL.
#'
#' @field oauthScopes  list( character ) ADLS Gen2 OAuth scopes.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsBody <- R6::R6Class(
  "CredentialsBody",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(credentialType = NULL, description = NULL, name = NULL, password = NULL, snowflakeAccountName = NULL, user = NULL, refreshToken = NULL, token = NULL, awsAccessKeyId = NULL, awsSecretAccessKey = NULL, awsSessionToken = NULL, gcpKey = NULL, azureConnectionString = NULL, clientId = NULL, clientSecret = NULL, oauthIssuerType = NULL, oauthIssuerUrl = NULL, oauthScopes = NULL) {
      if (!is.null(`credentialType`)) {
        stopifnot(is.character(`credentialType`), length(`credentialType`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
      if (!is.null(`refreshToken`)) {
        stopifnot(is.character(`refreshToken`), length(`refreshToken`) == 1)
      }
      if (!is.null(`token`)) {
        stopifnot(is.character(`token`), length(`token`) == 1)
      }
      if (!is.null(`gcpKey`)) {
        stopifnot(R6::is.R6(`gcpKey`))
      }
      if (!is.null(`azureConnectionString`)) {
        stopifnot(is.character(`azureConnectionString`), length(`azureConnectionString`) == 1)
      }
      if (!is.null(`clientId`)) {
        stopifnot(is.character(`clientId`), length(`clientId`) == 1)
      }
      if (!is.null(`clientSecret`)) {
        stopifnot(is.character(`clientSecret`), length(`clientSecret`) == 1)
      }
      if (!is.null(`oauthScopes`)) {
        stopifnot(is.vector(`oauthScopes`))
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`snowflakeAccountName`)) {
        stopifnot(is.character(`snowflakeAccountName`), length(`snowflakeAccountName`) == 1)
      }
      if (!is.null(`awsAccessKeyId`)) {
        stopifnot(is.character(`awsAccessKeyId`), length(`awsAccessKeyId`) == 1)
      }
      if (!is.null(`awsSecretAccessKey`)) {
        stopifnot(is.character(`awsSecretAccessKey`), length(`awsSecretAccessKey`) == 1)
      }
      if (!is.null(`awsSessionToken`)) {
        stopifnot(is.character(`awsSessionToken`), length(`awsSessionToken`) == 1)
      }
      if (!is.null(`oauthIssuerType`)) {
        stopifnot(is.character(`oauthIssuerType`), length(`oauthIssuerType`) == 1)
      }
      if (!is.null(`oauthIssuerUrl`)) {
        stopifnot(is.character(`oauthIssuerUrl`), length(`oauthIssuerUrl`) == 1)
      }
    }
  ),
  public = list(
    `credentialType` = NULL,
    `description` = NULL,
    `name` = NULL,
    `password` = NULL,
    `snowflakeAccountName` = NULL,
    `user` = NULL,
    `refreshToken` = NULL,
    `token` = NULL,
    `awsAccessKeyId` = NULL,
    `awsSecretAccessKey` = NULL,
    `awsSessionToken` = NULL,
    `gcpKey` = NULL,
    `azureConnectionString` = NULL,
    `clientId` = NULL,
    `clientSecret` = NULL,
    `oauthIssuerType` = NULL,
    `oauthIssuerUrl` = NULL,
    `oauthScopes` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialType Credentials type.
    #' @param description Credentials description.
    #' @param name Credentials name.
    #' @param password Password to store for this credentials.
    #' @param snowflakeAccountName Snowflake account name.
    #' @param user Username to store for this credentials.
    #' @param refreshToken OAUTH token.
    #' @param token OAUTH token.
    #' @param awsAccessKeyId AWS access key ID.
    #' @param awsSecretAccessKey AWS secret access key.
    #' @param awsSessionToken AWS session token.
    #' @param gcpKey
    #' @param azureConnectionString Azure connection string.
    #' @param clientId ADLS Gen2 OAuth client ID.
    #' @param clientSecret ADLS Gen2 OAuth client secret.
    #' @param oauthIssuerType OAuth issuer type.
    #' @param oauthIssuerUrl OAuth issuer URL.
    #' @param oauthScopes ADLS Gen2 OAuth scopes.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialType` = NULL, `name` = NULL, `password` = NULL, `user` = NULL, `refreshToken` = NULL, `token` = NULL, `gcpKey` = NULL, `azureConnectionString` = NULL, `clientId` = NULL, `clientSecret` = NULL, `oauthScopes` = NULL, `description` = NULL, `snowflakeAccountName` = NULL, `awsAccessKeyId` = NULL, `awsSecretAccessKey` = NULL, `awsSessionToken` = NULL, `oauthIssuerType` = NULL, `oauthIssuerUrl` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`credentialType`, `name`, `password`, `user`, `refreshToken`, `token`, `gcpKey`, `azureConnectionString`, `clientId`, `clientSecret`, `oauthScopes`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(credentialType, description, name, password, snowflakeAccountName, user, refreshToken, token, awsAccessKeyId, awsSecretAccessKey, awsSessionToken, gcpKey, azureConnectionString, clientId, clientSecret, oauthIssuerType, oauthIssuerUrl, oauthScopes)
      }
      self$`credentialType` <- `credentialType`
      self$`description` <- `description`
      self$`name` <- `name`
      self$`password` <- `password`
      self$`snowflakeAccountName` <- `snowflakeAccountName`
      self$`user` <- `user`
      self$`refreshToken` <- `refreshToken`
      self$`token` <- `token`
      self$`awsAccessKeyId` <- `awsAccessKeyId`
      self$`awsSecretAccessKey` <- `awsSecretAccessKey`
      self$`awsSessionToken` <- `awsSessionToken`
      self$`gcpKey` <- `gcpKey`
      self$`azureConnectionString` <- `azureConnectionString`
      self$`clientId` <- `clientId`
      self$`clientSecret` <- `clientSecret`
      self$`oauthIssuerType` <- `oauthIssuerType`
      self$`oauthIssuerUrl` <- `oauthIssuerUrl`
      sapply(`oauthScopes`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(credentialType = self$`credentialType`, description = self$`description`, name = self$`name`, password = self$`password`, snowflakeAccountName = self$`snowflakeAccountName`, user = self$`user`, refreshToken = self$`refreshToken`, token = self$`token`, awsAccessKeyId = self$`awsAccessKeyId`, awsSecretAccessKey = self$`awsSecretAccessKey`, awsSessionToken = self$`awsSessionToken`, gcpKey = self$`gcpKey`, azureConnectionString = self$`azureConnectionString`, clientId = self$`clientId`, clientSecret = self$`clientSecret`, oauthIssuerType = self$`oauthIssuerType`, oauthIssuerUrl = self$`oauthIssuerUrl`, oauthScopes = self$`oauthScopes`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialType`)) {
          sprintf(
            '"credentialType":
            "%s"
                  ',
            self$`credentialType`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`snowflakeAccountName`)) {
          sprintf(
            '"snowflakeAccountName":
            "%s"
                  ',
            self$`snowflakeAccountName`
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        },
        if (!is.null(self$`refreshToken`)) {
          sprintf(
            '"refreshToken":
            "%s"
                  ',
            self$`refreshToken`
          )
        },
        if (!is.null(self$`token`)) {
          sprintf(
            '"token":
            "%s"
                  ',
            self$`token`
          )
        },
        if (!is.null(self$`awsAccessKeyId`)) {
          sprintf(
            '"awsAccessKeyId":
            "%s"
                  ',
            self$`awsAccessKeyId`
          )
        },
        if (!is.null(self$`awsSecretAccessKey`)) {
          sprintf(
            '"awsSecretAccessKey":
            "%s"
                  ',
            self$`awsSecretAccessKey`
          )
        },
        if (!is.null(self$`awsSessionToken`)) {
          sprintf(
            '"awsSessionToken":
            "%s"
                  ',
            self$`awsSessionToken`
          )
        },
        if (!is.null(self$`gcpKey`)) {
          sprintf(
            '"gcpKey":
          %s
        ',
            jsonlite::toJSON(self$`gcpKey`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`azureConnectionString`)) {
          sprintf(
            '"azureConnectionString":
            "%s"
                  ',
            self$`azureConnectionString`
          )
        },
        if (!is.null(self$`clientId`)) {
          sprintf(
            '"clientId":
            "%s"
                  ',
            self$`clientId`
          )
        },
        if (!is.null(self$`clientSecret`)) {
          sprintf(
            '"clientSecret":
            "%s"
                  ',
            self$`clientSecret`
          )
        },
        if (!is.null(self$`oauthIssuerType`)) {
          sprintf(
            '"oauthIssuerType":
            "%s"
                  ',
            self$`oauthIssuerType`
          )
        },
        if (!is.null(self$`oauthIssuerUrl`)) {
          sprintf(
            '"oauthIssuerUrl":
            "%s"
                  ',
            self$`oauthIssuerUrl`
          )
        },
        if (!is.null(self$`oauthScopes`)) {
          sprintf(
            '"oauthScopes":
              [%s]
            ',
            paste(unlist(lapply(self$`oauthScopes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsBodyJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsBodyJson, validateParams = FALSE) {
      CredentialsBodyObject <- jsonlite::fromJSON(CredentialsBodyJson)
      self$`credentialType` <- CredentialsBodyObject$`credentialType`
      self$`description` <- CredentialsBodyObject$`description`
      self$`name` <- CredentialsBodyObject$`name`
      self$`password` <- CredentialsBodyObject$`password`
      self$`snowflakeAccountName` <- CredentialsBodyObject$`snowflakeAccountName`
      self$`user` <- CredentialsBodyObject$`user`
      self$`refreshToken` <- CredentialsBodyObject$`refreshToken`
      self$`token` <- CredentialsBodyObject$`token`
      self$`awsAccessKeyId` <- CredentialsBodyObject$`awsAccessKeyId`
      self$`awsSecretAccessKey` <- CredentialsBodyObject$`awsSecretAccessKey`
      self$`awsSessionToken` <- CredentialsBodyObject$`awsSessionToken`
      self$`gcpKey` <- GCPKey$new()$fromJSON(jsonlite::toJSON(CredentialsBodyObject$gcpKey, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`azureConnectionString` <- CredentialsBodyObject$`azureConnectionString`
      self$`clientId` <- CredentialsBodyObject$`clientId`
      self$`clientSecret` <- CredentialsBodyObject$`clientSecret`
      self$`oauthIssuerType` <- CredentialsBodyObject$`oauthIssuerType`
      self$`oauthIssuerUrl` <- CredentialsBodyObject$`oauthIssuerUrl`
      self$`oauthScopes` <- ApiClient$new()$deserializeObj(CredentialsBodyObject$`oauthScopes`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
