# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title HdfsProjectCreate
#'
#' @description HdfsProjectCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field password  character [optional] Password for authenticating to HDFS using Kerberos. The password will be encrypted on the server side in scope of HTTP request and never saved or stored.
#'
#' @field port  integer [optional] Port of the WebHDFS Namenode server. If not specified, defaults to HDFS default port 50070.
#'
#' @field projectName  character [optional] Name of the project to be created. If not specified, project name will be based on the file name.
#'
#' @field url  character URL of the WebHDFS resource. Represent the file using the &#x60;hdfs://&#x60; protocol marker (for example,  &#x60;hdfs:///tmp/somedataset.csv&#x60;).
#'
#' @field user  character [optional] Username for authenticating to HDFS using Kerberos
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
HdfsProjectCreate <- R6::R6Class(
  "HdfsProjectCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`password` = NULL, `port` = NULL, `projectName` = NULL, `url` = NULL, `user` = NULL) {
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`port`)) {
        stopifnot(is.numeric(`port`), length(`port`) == 1)
      }
      if (!is.null(`projectName`)) {
        stopifnot(is.character(`projectName`), length(`projectName`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `password` = NULL,
    `port` = NULL,
    `projectName` = NULL,
    `url` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param password Password for authenticating to HDFS using Kerberos. The password will be encrypted on the server side in scope of HTTP request and never saved or stored.
    #' @param port Port of the WebHDFS Namenode server. If not specified, defaults to HDFS default port 50070.
    #' @param projectName Name of the project to be created. If not specified, project name will be based on the file name.
    #' @param url URL of the WebHDFS resource. Represent the file using the &#x60;hdfs://&#x60; protocol marker (for example,  &#x60;hdfs:///tmp/somedataset.csv&#x60;).
    #' @param user Username for authenticating to HDFS using Kerberos
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`url` = NULL, `password` = NULL, `port` = NULL, `projectName` = NULL, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`url`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(password, port, projectName, url, user)
      }
      self$`password` <- `password`
      self$`port` <- `port`
      self$`projectName` <- `projectName`
      self$`url` <- `url`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(password = self$`password`, port = self$`port`, projectName = self$`projectName`, url = self$`url`, user = self$`user`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`port`)) {
          sprintf(
            '"port":
            %d
                  ',
            self$`port`
          )
        },
        if (!is.null(self$`projectName`)) {
          sprintf(
            '"projectName":
            "%s"
                  ',
            self$`projectName`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param HdfsProjectCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(HdfsProjectCreateJson, validateParams = FALSE) {
      HdfsProjectCreateObject <- jsonlite::fromJSON(HdfsProjectCreateJson)
      self$`password` <- HdfsProjectCreateObject$`password`
      self$`port` <- HdfsProjectCreateObject$`port`
      self$`projectName` <- HdfsProjectCreateObject$`projectName`
      self$`url` <- HdfsProjectCreateObject$`url`
      self$`user` <- HdfsProjectCreateObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
