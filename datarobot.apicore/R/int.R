# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Int
#'
#' @description Int Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field max  integer Maximum value for the parameter.
#'
#' @field min  integer Minimum value for the parameter.
#'
#' @field supportsGridSearch  character When True, Grid Search is supported for this parameter.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Int <- R6::R6Class(
  "Int",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`max` = NULL, `min` = NULL, `supportsGridSearch` = NULL) {
      if (!is.null(`max`)) {
        stopifnot(is.numeric(`max`), length(`max`) == 1)
      }
      if (!is.null(`min`)) {
        stopifnot(is.numeric(`min`), length(`min`) == 1)
      }
      if (!is.null(`supportsGridSearch`)) {
        stopifnot(is.logical(`supportsGridSearch`), length(`supportsGridSearch`) == 1)
      }
    }
  ),
  public = list(
    `max` = NULL,
    `min` = NULL,
    `supportsGridSearch` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param max Maximum value for the parameter.
    #' @param min Minimum value for the parameter.
    #' @param supportsGridSearch When True, Grid Search is supported for this parameter.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`max` = NULL, `min` = NULL, `supportsGridSearch` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`max`, `min`, `supportsGridSearch`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(max, min, supportsGridSearch)
      }
      self$`max` <- `max`
      self$`min` <- `min`
      self$`supportsGridSearch` <- `supportsGridSearch`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(max = self$`max`, min = self$`min`, supportsGridSearch = self$`supportsGridSearch`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`max`)) {
          sprintf(
            '"max":
            %d
                  ',
            self$`max`
          )
        },
        if (!is.null(self$`min`)) {
          sprintf(
            '"min":
            %d
                  ',
            self$`min`
          )
        },
        if (!is.null(self$`supportsGridSearch`)) {
          sprintf(
            '"supportsGridSearch":
            %s
                  ',
            tolower(self$`supportsGridSearch`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param IntJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(IntJson, validateParams = FALSE) {
      IntObject <- jsonlite::fromJSON(IntJson)
      self$`max` <- IntObject$`max`
      self$`min` <- IntObject$`min`
      self$`supportsGridSearch` <- IntObject$`supportsGridSearch`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
