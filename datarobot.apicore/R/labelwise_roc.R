# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title LabelwiseROC
#'
#' @description LabelwiseROC Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field averageModelMetrics  \link{AverageModelMetricsField}
#'
#' @field charts  list( \link{LabelwiseROCItem} ) ROC data for all labels from one data source.
#'
#' @field count  integer [optional] Number of labels returned on this page.
#'
#' @field labels  list( character ) All available target labels for this insight.
#'
#' @field next_  character URL pointing to the next page (if null, there is no next page).
#'
#' @field previous  character URL pointing to the previous page (if null, there is no previous page).
#'
#' @field rocType  character Type of ROC.
#'
#' @field totalCount  integer Total number of labels across all pages.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
LabelwiseROC <- R6::R6Class(
  "LabelwiseROC",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(averageModelMetrics = NULL, charts = NULL, count = NULL, labels = NULL, next_ = NULL, previous = NULL, rocType = NULL, totalCount = NULL) {
      if (!is.null(`averageModelMetrics`)) {
        stopifnot(R6::is.R6(`averageModelMetrics`))
      }
      if (!is.null(`charts`)) {
        stopifnot(is.vector(`charts`))
      }
      if (!is.null(`labels`)) {
        stopifnot(is.vector(`labels`))
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
      if (!is.null(`rocType`)) {
        stopifnot(is.character(`rocType`), length(`rocType`) == 1)
      }
      if (!is.null(`totalCount`)) {
        stopifnot(is.numeric(`totalCount`), length(`totalCount`) == 1)
      }
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
    }
  ),
  public = list(
    `averageModelMetrics` = NULL,
    `charts` = NULL,
    `count` = NULL,
    `labels` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    `rocType` = NULL,
    `totalCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param averageModelMetrics
    #' @param charts ROC data for all labels from one data source.
    #' @param count Number of labels returned on this page.
    #' @param labels All available target labels for this insight.
    #' @param next_ URL pointing to the next page (if null, there is no next page).
    #' @param previous URL pointing to the previous page (if null, there is no previous page).
    #' @param rocType Type of ROC.
    #' @param totalCount Total number of labels across all pages.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`averageModelMetrics` = NULL, `charts` = NULL, `labels` = NULL, `next_` = NULL, `previous` = NULL, `rocType` = NULL, `totalCount` = NULL, `count` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`averageModelMetrics`, `charts`, `labels`, `next_`, `previous`, `rocType`, `totalCount`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(averageModelMetrics, charts, count, labels, next_, previous, rocType, totalCount)
      }
      self$`averageModelMetrics` <- `averageModelMetrics`
      sapply(`charts`, function(x) stopifnot(R6::is.R6(x)))
      self$`count` <- `count`
      sapply(`labels`, function(x) stopifnot(is.character(x)))
      self$`next_` <- `next_`
      self$`previous` <- `previous`
      self$`rocType` <- `rocType`
      self$`totalCount` <- `totalCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(averageModelMetrics = self$`averageModelMetrics`, charts = self$`charts`, count = self$`count`, labels = self$`labels`, next_ = self$`next_`, previous = self$`previous`, rocType = self$`rocType`, totalCount = self$`totalCount`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`averageModelMetrics`)) {
          sprintf(
            '"averageModelMetrics":
          %s
        ',
            jsonlite::toJSON(self$`averageModelMetrics`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`charts`)) {
          sprintf(
            '"charts":
            [%s]
',
            paste(sapply(self$`charts`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`labels`)) {
          sprintf(
            '"labels":
              [%s]
            ',
            paste(unlist(lapply(self$`labels`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        },
        if (!is.null(self$`rocType`)) {
          sprintf(
            '"rocType":
            "%s"
                  ',
            self$`rocType`
          )
        },
        if (!is.null(self$`totalCount`)) {
          sprintf(
            '"totalCount":
            %d
                  ',
            self$`totalCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param LabelwiseROCJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(LabelwiseROCJson, validateParams = FALSE) {
      LabelwiseROCObject <- jsonlite::fromJSON(LabelwiseROCJson)
      self$`averageModelMetrics` <- AverageModelMetricsField$new()$fromJSON(jsonlite::toJSON(LabelwiseROCObject$averageModelMetrics, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`charts` <- ApiClient$new()$deserializeObj(LabelwiseROCObject$`charts`, "array[LabelwiseROCItem]", loadNamespace("datarobot.apicore"))
      self$`count` <- LabelwiseROCObject$`count`
      self$`labels` <- ApiClient$new()$deserializeObj(LabelwiseROCObject$`labels`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`next_` <- LabelwiseROCObject$`next`
      self$`previous` <- LabelwiseROCObject$`previous`
      self$`rocType` <- LabelwiseROCObject$`rocType`
      self$`totalCount` <- LabelwiseROCObject$`totalCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
