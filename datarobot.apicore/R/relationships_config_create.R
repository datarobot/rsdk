# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RelationshipsConfigCreate
#'
#' @description RelationshipsConfigCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetDefinitions  list( \link{DatasetDefinition} ) List of dataset definitions.
#'
#' @field featureDiscoveryMode  character [optional] Mode of feature discovery. Supported values are &#39;default&#39; and &#39;manual&#39;.
#'
#' @field featureDiscoverySettings  list( \link{FeatureDiscoverySetting} ) [optional] List of feature discovery settings used to customize the feature discovery process. Applicable when feature_discovery_mode is &#39;manual&#39;.
#'
#' @field relationships  list( \link{Relationship} ) List of relationship between datasets specified in datasetDefinitions.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RelationshipsConfigCreate <- R6::R6Class(
  "RelationshipsConfigCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datasetDefinitions = NULL, featureDiscoveryMode = NULL, featureDiscoverySettings = NULL, relationships = NULL) {
      if (!is.null(`datasetDefinitions`)) {
        stopifnot(is.vector(`datasetDefinitions`))
      }
      if (!is.null(`relationships`)) {
        stopifnot(is.vector(`relationships`))
      }
      if (!is.null(`featureDiscoveryMode`)) {
        stopifnot(is.character(`featureDiscoveryMode`), length(`featureDiscoveryMode`) == 1)
      }
      if (!is.null(`featureDiscoverySettings`)) {
        stopifnot(is.vector(`featureDiscoverySettings`))
      }
    }
  ),
  public = list(
    `datasetDefinitions` = NULL,
    `featureDiscoveryMode` = NULL,
    `featureDiscoverySettings` = NULL,
    `relationships` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetDefinitions List of dataset definitions.
    #' @param featureDiscoveryMode Mode of feature discovery. Supported values are &#39;default&#39; and &#39;manual&#39;.
    #' @param featureDiscoverySettings List of feature discovery settings used to customize the feature discovery process. Applicable when feature_discovery_mode is &#39;manual&#39;.
    #' @param relationships List of relationship between datasets specified in datasetDefinitions.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetDefinitions` = NULL, `relationships` = NULL, `featureDiscoveryMode` = NULL, `featureDiscoverySettings` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasetDefinitions`, `relationships`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datasetDefinitions, featureDiscoveryMode, featureDiscoverySettings, relationships)
      }
      sapply(`datasetDefinitions`, function(x) stopifnot(R6::is.R6(x)))
      self$`featureDiscoveryMode` <- `featureDiscoveryMode`
      sapply(`featureDiscoverySettings`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`relationships`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datasetDefinitions = self$`datasetDefinitions`, featureDiscoveryMode = self$`featureDiscoveryMode`, featureDiscoverySettings = self$`featureDiscoverySettings`, relationships = self$`relationships`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetDefinitions`)) {
          sprintf(
            '"datasetDefinitions":
            [%s]
',
            paste(sapply(self$`datasetDefinitions`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`featureDiscoveryMode`)) {
          sprintf(
            '"featureDiscoveryMode":
            "%s"
                  ',
            self$`featureDiscoveryMode`
          )
        },
        if (!is.null(self$`featureDiscoverySettings`)) {
          sprintf(
            '"featureDiscoverySettings":
            [%s]
',
            paste(sapply(self$`featureDiscoverySettings`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`relationships`)) {
          sprintf(
            '"relationships":
            [%s]
',
            paste(sapply(self$`relationships`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RelationshipsConfigCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RelationshipsConfigCreateJson, validateParams = FALSE) {
      RelationshipsConfigCreateObject <- jsonlite::fromJSON(RelationshipsConfigCreateJson)
      self$`datasetDefinitions` <- ApiClient$new()$deserializeObj(RelationshipsConfigCreateObject$`datasetDefinitions`, "array[DatasetDefinition]", loadNamespace("datarobot.apicore"))
      self$`featureDiscoveryMode` <- RelationshipsConfigCreateObject$`featureDiscoveryMode`
      self$`featureDiscoverySettings` <- ApiClient$new()$deserializeObj(RelationshipsConfigCreateObject$`featureDiscoverySettings`, "array[FeatureDiscoverySetting]", loadNamespace("datarobot.apicore"))
      self$`relationships` <- ApiClient$new()$deserializeObj(RelationshipsConfigCreateObject$`relationships`, "array[Relationship]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
