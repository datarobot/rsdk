# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentCapability
#'
#' @description DeploymentCapability Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field messages  list( character ) Messages explaining why the capability is supported or not supported.
#'
#' @field name  character The name of the capability.
#'
#' @field supported  character If the capability is supported.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentCapability <- R6::R6Class(
  "DeploymentCapability",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`messages` = NULL, `name` = NULL, `supported` = NULL) {
      if (!is.null(`messages`)) {
        stopifnot(is.vector(`messages`), sapply(`messages`, is.character))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`supported`)) {
        stopifnot(is.logical(`supported`), length(`supported`) == 1)
      }
    }
  ),
  public = list(
    `messages` = NULL,
    `name` = NULL,
    `supported` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param messages Messages explaining why the capability is supported or not supported.
    #' @param name The name of the capability.
    #' @param supported If the capability is supported.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`messages` = NULL, `name` = NULL, `supported` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`messages`, `name`, `supported`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(messages, name, supported)
      }
      self$`messages` <- `messages`
      self$`name` <- `name`
      self$`supported` <- `supported`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(messages = self$`messages`, name = self$`name`, supported = self$`supported`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`messages`)) {
          sprintf(
            '"messages":
            [%s]
                  ',
            paste(unlist(lapply(self$`messages`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`supported`)) {
          sprintf(
            '"supported":
            %s
                  ',
            tolower(self$`supported`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentCapabilityJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentCapabilityJson, validateParams = FALSE) {
      DeploymentCapabilityObject <- jsonlite::fromJSON(DeploymentCapabilityJson)
      self$`messages` <- ApiClient$new()$deserializeObj(DeploymentCapabilityObject$`messages`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`name` <- DeploymentCapabilityObject$`name`
      self$`supported` <- DeploymentCapabilityObject$`supported`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
