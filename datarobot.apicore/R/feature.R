# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Feature
#'
#' @description Feature Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dateFormat  character The date format string for how this feature was interpreted.
#'
#' @field featureType  character Feature type.
#'
#' @field importance  numeric Numeric measure of the relationship strength between the feature and target (independent of model or other features).
#'
#' @field knownInAdvance  character Whether the feature was selected as known in advance in a time-series model, false for non-time-series models.
#'
#' @field name  character Feature name.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Feature <- R6::R6Class(
  "Feature",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`dateFormat` = NULL, `featureType` = NULL, `importance` = NULL, `knownInAdvance` = NULL, `name` = NULL) {
      if (!is.null(`dateFormat`)) {
        stopifnot(is.character(`dateFormat`), length(`dateFormat`) == 1)
      }
      if (!is.null(`featureType`)) {
        stopifnot(is.character(`featureType`), length(`featureType`) == 1)
      }
      if (!is.null(`importance`)) {
      }
      if (!is.null(`knownInAdvance`)) {
        stopifnot(is.logical(`knownInAdvance`), length(`knownInAdvance`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
    }
  ),
  public = list(
    `dateFormat` = NULL,
    `featureType` = NULL,
    `importance` = NULL,
    `knownInAdvance` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dateFormat The date format string for how this feature was interpreted.
    #' @param featureType Feature type.
    #' @param importance Numeric measure of the relationship strength between the feature and target (independent of model or other features).
    #' @param knownInAdvance Whether the feature was selected as known in advance in a time-series model, false for non-time-series models.
    #' @param name Feature name.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dateFormat` = NULL, `featureType` = NULL, `importance` = NULL, `knownInAdvance` = NULL, `name` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`dateFormat`, `featureType`, `importance`, `knownInAdvance`, `name`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(dateFormat, featureType, importance, knownInAdvance, name)
      }
      self$`dateFormat` <- `dateFormat`
      self$`featureType` <- `featureType`
      self$`importance` <- `importance`
      self$`knownInAdvance` <- `knownInAdvance`
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(dateFormat = self$`dateFormat`, featureType = self$`featureType`, importance = self$`importance`, knownInAdvance = self$`knownInAdvance`, name = self$`name`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dateFormat`)) {
          sprintf(
            '"dateFormat":
            "%s"
                  ',
            self$`dateFormat`
          )
        },
        if (!is.null(self$`featureType`)) {
          sprintf(
            '"featureType":
            "%s"
                  ',
            self$`featureType`
          )
        },
        if (!is.null(self$`importance`)) {
          sprintf(
            '"importance":
            %d
                  ',
            self$`importance`
          )
        },
        if (!is.null(self$`knownInAdvance`)) {
          sprintf(
            '"knownInAdvance":
            %s
                  ',
            tolower(self$`knownInAdvance`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureJson, validateParams = FALSE) {
      FeatureObject <- jsonlite::fromJSON(FeatureJson)
      self$`dateFormat` <- FeatureObject$`dateFormat`
      self$`featureType` <- FeatureObject$`featureType`
      self$`importance` <- FeatureObject$`importance`
      self$`knownInAdvance` <- FeatureObject$`knownInAdvance`
      self$`name` <- FeatureObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
