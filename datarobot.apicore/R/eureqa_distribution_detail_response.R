# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title EureqaDistributionDetailResponse
#'
#' @description EureqaDistributionDetailResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field bins  list( \link{ClassificationBinDataResponse} ) The distribution plot data.
#'
#' @field complexity  integer The complexity score for this solution. Complexity score is a function of the mathematical operators used in the current solution. The complexity calculation can be tuned via model hyperparameters.
#'
#' @field error  numeric The error for the current solution, as computed by eureqa using the &#x60;errorMetric&#x60; error metric. None if Eureqa model refitted existing solutions.
#'
#' @field errorMetric  character The Eureqa error metric identifier used to compute error metrics for this search. Note that Eureqa error metrics do not correspond 1:1 with DataRobot error metrics - the available metrics are not the same, and even equivalent metrics may be computed slightly differently.
#'
#' @field eureqaSolutionId  character The ID of the solution.
#'
#' @field expression  character The eureqa \&quot;solution string\&quot;. This is a mathematical expression; human-readable but with strict syntax specifications defined by Eureqa.
#'
#' @field expressionAnnotated  character The &#x60;expression&#x60;, rendered with additional tags to assist in automatic parsing.
#'
#' @field threshold  numeric Classifier threshold selected by the backend, used to determine which model values are binned as positive and which are binned as negative. Must have a value between the &#x60;binStart&#x60; of the first bin and &#x60;binEnd&#x60; of the last bin.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
EureqaDistributionDetailResponse <- R6::R6Class(
  "EureqaDistributionDetailResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(bins = NULL, complexity = NULL, error = NULL, errorMetric = NULL, eureqaSolutionId = NULL, expression = NULL, expressionAnnotated = NULL, threshold = NULL) {
      if (!is.null(`bins`)) {
        stopifnot(is.vector(`bins`))
      }
      if (!is.null(`complexity`)) {
        stopifnot(is.numeric(`complexity`), length(`complexity`) == 1)
      }
      if (!is.null(`error`)) {
      }
      if (!is.null(`errorMetric`)) {
        stopifnot(is.character(`errorMetric`), length(`errorMetric`) == 1)
      }
      if (!is.null(`eureqaSolutionId`)) {
        stopifnot(is.character(`eureqaSolutionId`), length(`eureqaSolutionId`) == 1)
      }
      if (!is.null(`expression`)) {
        stopifnot(is.character(`expression`), length(`expression`) == 1)
      }
      if (!is.null(`expressionAnnotated`)) {
        stopifnot(is.character(`expressionAnnotated`), length(`expressionAnnotated`) == 1)
      }
      if (!is.null(`threshold`)) {
      }
    }
  ),
  public = list(
    `bins` = NULL,
    `complexity` = NULL,
    `error` = NULL,
    `errorMetric` = NULL,
    `eureqaSolutionId` = NULL,
    `expression` = NULL,
    `expressionAnnotated` = NULL,
    `threshold` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param bins The distribution plot data.
    #' @param complexity The complexity score for this solution. Complexity score is a function of the mathematical operators used in the current solution. The complexity calculation can be tuned via model hyperparameters.
    #' @param error The error for the current solution, as computed by eureqa using the &#x60;errorMetric&#x60; error metric. None if Eureqa model refitted existing solutions.
    #' @param errorMetric The Eureqa error metric identifier used to compute error metrics for this search. Note that Eureqa error metrics do not correspond 1:1 with DataRobot error metrics - the available metrics are not the same, and even equivalent metrics may be computed slightly differently.
    #' @param eureqaSolutionId The ID of the solution.
    #' @param expression The eureqa \&quot;solution string\&quot;. This is a mathematical expression; human-readable but with strict syntax specifications defined by Eureqa.
    #' @param expressionAnnotated The &#x60;expression&#x60;, rendered with additional tags to assist in automatic parsing.
    #' @param threshold Classifier threshold selected by the backend, used to determine which model values are binned as positive and which are binned as negative. Must have a value between the &#x60;binStart&#x60; of the first bin and &#x60;binEnd&#x60; of the last bin.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`bins` = NULL, `complexity` = NULL, `error` = NULL, `errorMetric` = NULL, `eureqaSolutionId` = NULL, `expression` = NULL, `expressionAnnotated` = NULL, `threshold` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`bins`, `complexity`, `error`, `errorMetric`, `eureqaSolutionId`, `expression`, `expressionAnnotated`, `threshold`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(bins, complexity, error, errorMetric, eureqaSolutionId, expression, expressionAnnotated, threshold)
      }
      sapply(`bins`, function(x) stopifnot(R6::is.R6(x)))
      self$`complexity` <- `complexity`
      self$`error` <- `error`
      self$`errorMetric` <- `errorMetric`
      self$`eureqaSolutionId` <- `eureqaSolutionId`
      self$`expression` <- `expression`
      self$`expressionAnnotated` <- `expressionAnnotated`
      self$`threshold` <- `threshold`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(bins = self$`bins`, complexity = self$`complexity`, error = self$`error`, errorMetric = self$`errorMetric`, eureqaSolutionId = self$`eureqaSolutionId`, expression = self$`expression`, expressionAnnotated = self$`expressionAnnotated`, threshold = self$`threshold`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`bins`)) {
          sprintf(
            '"bins":
            [%s]
',
            paste(sapply(self$`bins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`complexity`)) {
          sprintf(
            '"complexity":
            %d
                  ',
            self$`complexity`
          )
        },
        if (!is.null(self$`error`)) {
          sprintf(
            '"error":
            %d
                  ',
            self$`error`
          )
        },
        if (!is.null(self$`errorMetric`)) {
          sprintf(
            '"errorMetric":
            "%s"
                  ',
            self$`errorMetric`
          )
        },
        if (!is.null(self$`eureqaSolutionId`)) {
          sprintf(
            '"eureqaSolutionId":
            "%s"
                  ',
            self$`eureqaSolutionId`
          )
        },
        if (!is.null(self$`expression`)) {
          sprintf(
            '"expression":
            "%s"
                  ',
            self$`expression`
          )
        },
        if (!is.null(self$`expressionAnnotated`)) {
          sprintf(
            '"expressionAnnotated":
            "%s"
                  ',
            self$`expressionAnnotated`
          )
        },
        if (!is.null(self$`threshold`)) {
          sprintf(
            '"threshold":
            %d
                  ',
            self$`threshold`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param EureqaDistributionDetailResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(EureqaDistributionDetailResponseJson, validateParams = FALSE) {
      EureqaDistributionDetailResponseObject <- jsonlite::fromJSON(EureqaDistributionDetailResponseJson)
      self$`bins` <- ApiClient$new()$deserializeObj(EureqaDistributionDetailResponseObject$`bins`, "array[ClassificationBinDataResponse]", loadNamespace("datarobot.apicore"))
      self$`complexity` <- EureqaDistributionDetailResponseObject$`complexity`
      self$`error` <- EureqaDistributionDetailResponseObject$`error`
      self$`errorMetric` <- EureqaDistributionDetailResponseObject$`errorMetric`
      self$`eureqaSolutionId` <- EureqaDistributionDetailResponseObject$`eureqaSolutionId`
      self$`expression` <- EureqaDistributionDetailResponseObject$`expression`
      self$`expressionAnnotated` <- EureqaDistributionDetailResponseObject$`expressionAnnotated`
      self$`threshold` <- EureqaDistributionDetailResponseObject$`threshold`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
