# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelRocCurveResponse
#'
#' @description ModelRocCurveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field auc  numeric AUC value
#'
#' @field kolmogorovSmirnovMetric  numeric Kolmogorov-Smirnov metric value
#'
#' @field negativeClassPredictions  list( numeric ) List of example predictions for the negative class.
#'
#' @field positiveClassPredictions  list( numeric ) List of example predictions for the positive class.
#'
#' @field rocPoints  list( \link{RocPointsResponse} ) The ROC curve data for that source, as specified below.
#'
#' @field source  character Source of the data.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelRocCurveResponse <- R6::R6Class(
  "ModelRocCurveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`auc` = NULL, `kolmogorovSmirnovMetric` = NULL, `negativeClassPredictions` = NULL, `positiveClassPredictions` = NULL, `rocPoints` = NULL, `source` = NULL) {
      if (!is.null(`auc`)) {
      }
      if (!is.null(`kolmogorovSmirnovMetric`)) {
      }
      if (!is.null(`negativeClassPredictions`)) {
        stopifnot(is.vector(`negativeClassPredictions`), sapply(`negativeClassPredictions`, is.numeric))
      }
      if (!is.null(`positiveClassPredictions`)) {
        stopifnot(is.vector(`positiveClassPredictions`), sapply(`positiveClassPredictions`, is.numeric))
      }
      if (!is.null(`rocPoints`)) {
        stopifnot(is.vector(`rocPoints`), sapply(`rocPoints`, R6::is.R6))
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
    }
  ),
  public = list(
    `auc` = NULL,
    `kolmogorovSmirnovMetric` = NULL,
    `negativeClassPredictions` = NULL,
    `positiveClassPredictions` = NULL,
    `rocPoints` = NULL,
    `source` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param auc AUC value
    #' @param kolmogorovSmirnovMetric Kolmogorov-Smirnov metric value
    #' @param negativeClassPredictions List of example predictions for the negative class.
    #' @param positiveClassPredictions List of example predictions for the positive class.
    #' @param rocPoints The ROC curve data for that source, as specified below.
    #' @param source Source of the data.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`auc` = NULL, `kolmogorovSmirnovMetric` = NULL, `negativeClassPredictions` = NULL, `positiveClassPredictions` = NULL, `rocPoints` = NULL, `source` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`auc`, `kolmogorovSmirnovMetric`, `negativeClassPredictions`, `positiveClassPredictions`, `rocPoints`, `source`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(auc, kolmogorovSmirnovMetric, negativeClassPredictions, positiveClassPredictions, rocPoints, source)
      }
      self$`auc` <- `auc`
      self$`kolmogorovSmirnovMetric` <- `kolmogorovSmirnovMetric`
      self$`negativeClassPredictions` <- `negativeClassPredictions`
      self$`positiveClassPredictions` <- `positiveClassPredictions`
      self$`rocPoints` <- `rocPoints`
      self$`source` <- `source`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(auc = self$`auc`, kolmogorovSmirnovMetric = self$`kolmogorovSmirnovMetric`, negativeClassPredictions = self$`negativeClassPredictions`, positiveClassPredictions = self$`positiveClassPredictions`, rocPoints = self$`rocPoints`, source = self$`source`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`auc`)) {
          sprintf(
            '"auc":
            %d
                  ',
            self$`auc`
          )
        },
        if (!is.null(self$`kolmogorovSmirnovMetric`)) {
          sprintf(
            '"kolmogorovSmirnovMetric":
            %d
                  ',
            self$`kolmogorovSmirnovMetric`
          )
        },
        if (!is.null(self$`negativeClassPredictions`)) {
          sprintf(
            '"negativeClassPredictions":
            [%s]
                  ',
            paste(unlist(lapply(self$`negativeClassPredictions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`positiveClassPredictions`)) {
          sprintf(
            '"positiveClassPredictions":
            [%s]
                  ',
            paste(unlist(lapply(self$`positiveClassPredictions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`rocPoints`)) {
          sprintf(
            '"rocPoints":
            [%s]
      ',
            paste(sapply(self$`rocPoints`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelRocCurveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelRocCurveResponseJson, validateParams = FALSE) {
      ModelRocCurveResponseObject <- jsonlite::fromJSON(ModelRocCurveResponseJson)
      self$`auc` <- ModelRocCurveResponseObject$`auc`
      self$`kolmogorovSmirnovMetric` <- ModelRocCurveResponseObject$`kolmogorovSmirnovMetric`
      self$`negativeClassPredictions` <- ApiClient$new()$deserializeObj(ModelRocCurveResponseObject$`negativeClassPredictions`, "array[numeric]", loadNamespace("datarobot.apicore"))
      self$`positiveClassPredictions` <- ApiClient$new()$deserializeObj(ModelRocCurveResponseObject$`positiveClassPredictions`, "array[numeric]", loadNamespace("datarobot.apicore"))
      self$`rocPoints` <- ApiClient$new()$deserializeObj(ModelRocCurveResponseObject$`rocPoints`, "array[RocPointsResponse]", loadNamespace("datarobot.apicore"))
      self$`source` <- ModelRocCurveResponseObject$`source`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
