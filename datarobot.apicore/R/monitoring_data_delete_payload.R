# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MonitoringDataDeletePayload
#'
#' @description MonitoringDataDeletePayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field end  character [optional] RFC3339 datetime. End of time period to delete monitoring data.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
#'
#' @field modelId  character The id of the model for which monitoring data are being deleted.
#'
#' @field start  character [optional] RFC3339 datetime. Start of time period to delete monitoring data.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MonitoringDataDeletePayload <- R6::R6Class(
  "MonitoringDataDeletePayload",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`end` = NULL, `modelId` = NULL, `start` = NULL) {
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`end`)) {
        stopifnot(inherits(`end`, "POSIXt"))
      }
      if (!is.null(`start`)) {
        stopifnot(inherits(`start`, "POSIXt"))
      }
    }
  ),
  public = list(
    `end` = NULL,
    `modelId` = NULL,
    `start` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param end RFC3339 datetime. End of time period to delete monitoring data.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
    #' @param modelId The id of the model for which monitoring data are being deleted.
    #' @param start RFC3339 datetime. Start of time period to delete monitoring data.Note: this field only accepts top of the hour RFC3339 datetime strings For example: &#x60;&#x60;2019-08-01T00:00:00Z&#x60;&#x60;.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`modelId` = NULL, `end` = NULL, `start` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`modelId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(end, modelId, start)
      }
      self$`end` <- `end`
      self$`modelId` <- `modelId`
      self$`start` <- `start`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(end = self$`end`, modelId = self$`modelId`, start = self$`start`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`end`)) {
          sprintf(
            '"end":
            "%s"
                  ',
            format(self$`end`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`start`)) {
          sprintf(
            '"start":
            "%s"
                  ',
            format(self$`start`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MonitoringDataDeletePayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MonitoringDataDeletePayloadJson, validateParams = FALSE) {
      MonitoringDataDeletePayloadObject <- jsonlite::fromJSON(MonitoringDataDeletePayloadJson)
      self$`end` <- ParseRFC3339Timestamp(MonitoringDataDeletePayloadObject$`end`)
      self$`modelId` <- MonitoringDataDeletePayloadObject$`modelId`
      self$`start` <- ParseRFC3339Timestamp(MonitoringDataDeletePayloadObject$`start`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
