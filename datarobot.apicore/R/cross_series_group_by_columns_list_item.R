# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CrossSeriesGroupByColumnsListItem
#'
#' @description CrossSeriesGroupByColumnsListItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field eligibility  character Information about the column&#39;s eligibility. If the column is not eligible, this will include the reason why.
#'
#' @field isEligible  character Indicates whether this column can be used as a group-by column.
#'
#' @field name  character The name of the column.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CrossSeriesGroupByColumnsListItem <- R6::R6Class(
  "CrossSeriesGroupByColumnsListItem",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(eligibility = NULL, isEligible = NULL, name = NULL) {
      if (!is.null(`eligibility`)) {
        stopifnot(is.character(`eligibility`), length(`eligibility`) == 1)
      }
      if (!is.null(`isEligible`)) {
        stopifnot(is.logical(`isEligible`), length(`isEligible`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
    }
  ),
  public = list(
    `eligibility` = NULL,
    `isEligible` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param eligibility Information about the column&#39;s eligibility. If the column is not eligible, this will include the reason why.
    #' @param isEligible Indicates whether this column can be used as a group-by column.
    #' @param name The name of the column.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`eligibility` = NULL, `isEligible` = NULL, `name` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`eligibility`, `isEligible`, `name`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(eligibility, isEligible, name)
      }
      self$`eligibility` <- `eligibility`
      self$`isEligible` <- `isEligible`
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(eligibility = self$`eligibility`, isEligible = self$`isEligible`, name = self$`name`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`eligibility`)) {
          sprintf(
            '"eligibility":
            "%s"
                  ',
            self$`eligibility`
          )
        },
        if (!is.null(self$`isEligible`)) {
          sprintf(
            '"isEligible":
            %s
                  ',
            tolower(self$`isEligible`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CrossSeriesGroupByColumnsListItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CrossSeriesGroupByColumnsListItemJson, validateParams = FALSE) {
      CrossSeriesGroupByColumnsListItemObject <- jsonlite::fromJSON(CrossSeriesGroupByColumnsListItemJson)
      self$`eligibility` <- CrossSeriesGroupByColumnsListItemObject$`eligibility`
      self$`isEligible` <- CrossSeriesGroupByColumnsListItemObject$`isEligible`
      self$`name` <- CrossSeriesGroupByColumnsListItemObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
