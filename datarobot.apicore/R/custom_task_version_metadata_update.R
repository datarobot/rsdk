# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CustomTaskVersionMetadataUpdate
#'
#' @description CustomTaskVersionMetadataUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field description  character [optional] New description for the custom task or model.
#'
#' @field requiredMetadata  object [optional] Additional parameters required by the execution environment. The required keys are defined by the fieldNames in the base environment&#39;s requiredMetadataKeys.
#'
#' @field requiredMetadataValues  list( \link{RequiredMetadataValue} ) [optional] Additional parameters required by the execution environment. The required fieldNames are defined by the fieldNames in the base environment&#39;s requiredMetadataKeys.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CustomTaskVersionMetadataUpdate <- R6::R6Class(
  "CustomTaskVersionMetadataUpdate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(description = NULL, requiredMetadata = NULL, requiredMetadataValues = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`requiredMetadata`)) {
      }
      if (!is.null(`requiredMetadataValues`)) {
        stopifnot(is.vector(`requiredMetadataValues`))
      }
    }
  ),
  public = list(
    `description` = NULL,
    `requiredMetadata` = NULL,
    `requiredMetadataValues` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param description New description for the custom task or model.
    #' @param requiredMetadata Additional parameters required by the execution environment. The required keys are defined by the fieldNames in the base environment&#39;s requiredMetadataKeys.
    #' @param requiredMetadataValues Additional parameters required by the execution environment. The required fieldNames are defined by the fieldNames in the base environment&#39;s requiredMetadataKeys.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `requiredMetadata` = NULL, `requiredMetadataValues` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(description, requiredMetadata, requiredMetadataValues)
      }
      self$`description` <- `description`
      self$`requiredMetadata` <- `requiredMetadata`
      sapply(`requiredMetadataValues`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(description = self$`description`, requiredMetadata = self$`requiredMetadata`, requiredMetadataValues = self$`requiredMetadataValues`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`requiredMetadata`)) {
          sprintf(
            '"requiredMetadata":
            "%s"
                  ',
            self$`requiredMetadata`
          )
        },
        if (!is.null(self$`requiredMetadataValues`)) {
          sprintf(
            '"requiredMetadataValues":
            [%s]
',
            paste(sapply(self$`requiredMetadataValues`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CustomTaskVersionMetadataUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CustomTaskVersionMetadataUpdateJson, validateParams = FALSE) {
      CustomTaskVersionMetadataUpdateObject <- jsonlite::fromJSON(CustomTaskVersionMetadataUpdateJson)
      self$`description` <- CustomTaskVersionMetadataUpdateObject$`description`
      self$`requiredMetadata` <- CustomTaskVersionMetadataUpdateObject$`requiredMetadata`
      self$`requiredMetadataValues` <- ApiClient$new()$deserializeObj(CustomTaskVersionMetadataUpdateObject$`requiredMetadataValues`, "array[RequiredMetadataValue]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
