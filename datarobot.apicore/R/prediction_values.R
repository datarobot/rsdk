# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionValues
#'
#' @description PredictionValues Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field label  \link{OneOfstringnumber} In the case of classification, The label of the class.
#'
#' @field value  numeric In the case of classification, the predicted probability of the class identified by the label.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionValues <- R6::R6Class(
  "PredictionValues",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(label = NULL, value = NULL) {
      if (!is.null(`label`)) {
        .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = label)
      }
      if (!is.null(`value`)) {
      }
    }
  ),
  public = list(
    `label` = NULL,
    `value` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param label In the case of classification, The label of the class.
    #' @param value In the case of classification, the predicted probability of the class identified by the label.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`label` = NULL, `value` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`label`, `value`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(label, value)
      }
      self$`label` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = label)
      self$`value` <- `value`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(label = self$`label`, value = self$`value`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
          %s
        ',
            self$`label`
          )
        },
        if (!is.null(self$`value`)) {
          sprintf(
            '"value":
            %d
                  ',
            self$`value`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionValuesJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionValuesJson, validateParams = FALSE) {
      PredictionValuesObject <- jsonlite::fromJSON(PredictionValuesJson)
      self$`label` <- .setPrimitiveProperty(typeList = list("character", "numeric"), propertyData = PredictionValuesObject$label)
      self$`value` <- PredictionValuesObject$`value`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
