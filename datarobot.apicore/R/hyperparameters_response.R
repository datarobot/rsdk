# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title HyperparametersResponse
#'
#' @description HyperparametersResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field buildingBlocks  object Mathematical operators and other components that comprise Eureqa Expressions.
#'
#' @field errorMetric  character [optional] Error Metric Eureqa used internally, to evaluate which models to keep on its internal Pareto Front.
#'
#' @field maxGenerations  integer The maximum number of evolutionary generations to run.
#'
#' @field numThreads  integer The number of threads Eureqa will run with.
#'
#' @field priorSolutions  list( character ) Prior Eureqa Solutions.
#'
#' @field randomSeed  integer Constant to seed Eureqa&#39;s pseudo-random number generator.
#'
#' @field splitMode  character Whether to perform in-order (2) or random (1) splitting within the training set, for evolutionary re-training and re-validatoon.
#'
#' @field syncMigrations  character Whether Eureqa&#39;s migrations are synchronized.
#'
#' @field targetExpressionFormat  character Constrain the target expression to the specified format.
#'
#' @field targetExpressionString  character Eureqa Expression to constrain the form of the models that Eureqa will consider.
#'
#' @field timeoutSec  numeric The duration of time to run the Eureqa search algorithm for Eureqa will run until either of max_generations or timeout_sec is reached.
#'
#' @field trainingFraction  numeric The fraction of the DataRobot training data to use for Eureqa evolutionary training.
#'
#' @field trainingSplitExpr  character Valid Eureqa Expression to do Eureqa internal training splits.
#'
#' @field validationFraction  numeric The fraction of the DataRobot training data to use for Eureqa evolutionary validation.
#'
#' @field validationSplitExpr  character Valid Eureqa Expression to do Eureqa internal validation splits.
#'
#' @field weightExpr  character Eureqa Weight Expression.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
HyperparametersResponse <- R6::R6Class(
  "HyperparametersResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(buildingBlocks = NULL, errorMetric = NULL, maxGenerations = NULL, numThreads = NULL, priorSolutions = NULL, randomSeed = NULL, splitMode = NULL, syncMigrations = NULL, targetExpressionFormat = NULL, targetExpressionString = NULL, timeoutSec = NULL, trainingFraction = NULL, trainingSplitExpr = NULL, validationFraction = NULL, validationSplitExpr = NULL, weightExpr = NULL) {
      if (!is.null(`buildingBlocks`)) {
      }
      if (!is.null(`maxGenerations`)) {
        stopifnot(is.numeric(`maxGenerations`), length(`maxGenerations`) == 1)
      }
      if (!is.null(`numThreads`)) {
        stopifnot(is.numeric(`numThreads`), length(`numThreads`) == 1)
      }
      if (!is.null(`priorSolutions`)) {
        stopifnot(is.vector(`priorSolutions`))
      }
      if (!is.null(`randomSeed`)) {
        stopifnot(is.numeric(`randomSeed`), length(`randomSeed`) == 1)
      }
      if (!is.null(`splitMode`)) {
        stopifnot(is.character(`splitMode`), length(`splitMode`) == 1)
      }
      if (!is.null(`syncMigrations`)) {
        stopifnot(is.logical(`syncMigrations`), length(`syncMigrations`) == 1)
      }
      if (!is.null(`targetExpressionFormat`)) {
        stopifnot(is.character(`targetExpressionFormat`), length(`targetExpressionFormat`) == 1)
      }
      if (!is.null(`targetExpressionString`)) {
        stopifnot(is.character(`targetExpressionString`), length(`targetExpressionString`) == 1)
      }
      if (!is.null(`timeoutSec`)) {
      }
      if (!is.null(`trainingFraction`)) {
      }
      if (!is.null(`trainingSplitExpr`)) {
        stopifnot(is.character(`trainingSplitExpr`), length(`trainingSplitExpr`) == 1)
      }
      if (!is.null(`validationFraction`)) {
      }
      if (!is.null(`validationSplitExpr`)) {
        stopifnot(is.character(`validationSplitExpr`), length(`validationSplitExpr`) == 1)
      }
      if (!is.null(`weightExpr`)) {
        stopifnot(is.character(`weightExpr`), length(`weightExpr`) == 1)
      }
      if (!is.null(`errorMetric`)) {
        stopifnot(is.character(`errorMetric`), length(`errorMetric`) == 1)
      }
    }
  ),
  public = list(
    `buildingBlocks` = NULL,
    `errorMetric` = NULL,
    `maxGenerations` = NULL,
    `numThreads` = NULL,
    `priorSolutions` = NULL,
    `randomSeed` = NULL,
    `splitMode` = NULL,
    `syncMigrations` = NULL,
    `targetExpressionFormat` = NULL,
    `targetExpressionString` = NULL,
    `timeoutSec` = NULL,
    `trainingFraction` = NULL,
    `trainingSplitExpr` = NULL,
    `validationFraction` = NULL,
    `validationSplitExpr` = NULL,
    `weightExpr` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param buildingBlocks Mathematical operators and other components that comprise Eureqa Expressions.
    #' @param errorMetric Error Metric Eureqa used internally, to evaluate which models to keep on its internal Pareto Front.
    #' @param maxGenerations The maximum number of evolutionary generations to run.
    #' @param numThreads The number of threads Eureqa will run with.
    #' @param priorSolutions Prior Eureqa Solutions.
    #' @param randomSeed Constant to seed Eureqa&#39;s pseudo-random number generator.
    #' @param splitMode Whether to perform in-order (2) or random (1) splitting within the training set, for evolutionary re-training and re-validatoon.
    #' @param syncMigrations Whether Eureqa&#39;s migrations are synchronized.
    #' @param targetExpressionFormat Constrain the target expression to the specified format.
    #' @param targetExpressionString Eureqa Expression to constrain the form of the models that Eureqa will consider.
    #' @param timeoutSec The duration of time to run the Eureqa search algorithm for Eureqa will run until either of max_generations or timeout_sec is reached.
    #' @param trainingFraction The fraction of the DataRobot training data to use for Eureqa evolutionary training.
    #' @param trainingSplitExpr Valid Eureqa Expression to do Eureqa internal training splits.
    #' @param validationFraction The fraction of the DataRobot training data to use for Eureqa evolutionary validation.
    #' @param validationSplitExpr Valid Eureqa Expression to do Eureqa internal validation splits.
    #' @param weightExpr Eureqa Weight Expression.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`buildingBlocks` = NULL, `maxGenerations` = NULL, `numThreads` = NULL, `priorSolutions` = NULL, `randomSeed` = NULL, `splitMode` = NULL, `syncMigrations` = NULL, `targetExpressionFormat` = NULL, `targetExpressionString` = NULL, `timeoutSec` = NULL, `trainingFraction` = NULL, `trainingSplitExpr` = NULL, `validationFraction` = NULL, `validationSplitExpr` = NULL, `weightExpr` = NULL, `errorMetric` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`buildingBlocks`, `maxGenerations`, `numThreads`, `priorSolutions`, `randomSeed`, `splitMode`, `syncMigrations`, `targetExpressionFormat`, `targetExpressionString`, `timeoutSec`, `trainingFraction`, `trainingSplitExpr`, `validationFraction`, `validationSplitExpr`, `weightExpr`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(buildingBlocks, errorMetric, maxGenerations, numThreads, priorSolutions, randomSeed, splitMode, syncMigrations, targetExpressionFormat, targetExpressionString, timeoutSec, trainingFraction, trainingSplitExpr, validationFraction, validationSplitExpr, weightExpr)
      }
      self$`buildingBlocks` <- `buildingBlocks`
      self$`errorMetric` <- `errorMetric`
      self$`maxGenerations` <- `maxGenerations`
      self$`numThreads` <- `numThreads`
      sapply(`priorSolutions`, function(x) stopifnot(is.character(x)))
      self$`randomSeed` <- `randomSeed`
      self$`splitMode` <- `splitMode`
      self$`syncMigrations` <- `syncMigrations`
      self$`targetExpressionFormat` <- `targetExpressionFormat`
      self$`targetExpressionString` <- `targetExpressionString`
      self$`timeoutSec` <- `timeoutSec`
      self$`trainingFraction` <- `trainingFraction`
      self$`trainingSplitExpr` <- `trainingSplitExpr`
      self$`validationFraction` <- `validationFraction`
      self$`validationSplitExpr` <- `validationSplitExpr`
      self$`weightExpr` <- `weightExpr`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(buildingBlocks = self$`buildingBlocks`, errorMetric = self$`errorMetric`, maxGenerations = self$`maxGenerations`, numThreads = self$`numThreads`, priorSolutions = self$`priorSolutions`, randomSeed = self$`randomSeed`, splitMode = self$`splitMode`, syncMigrations = self$`syncMigrations`, targetExpressionFormat = self$`targetExpressionFormat`, targetExpressionString = self$`targetExpressionString`, timeoutSec = self$`timeoutSec`, trainingFraction = self$`trainingFraction`, trainingSplitExpr = self$`trainingSplitExpr`, validationFraction = self$`validationFraction`, validationSplitExpr = self$`validationSplitExpr`, weightExpr = self$`weightExpr`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`buildingBlocks`)) {
          sprintf(
            '"buildingBlocks":
            "%s"
                  ',
            self$`buildingBlocks`
          )
        },
        if (!is.null(self$`errorMetric`)) {
          sprintf(
            '"errorMetric":
            "%s"
                  ',
            self$`errorMetric`
          )
        },
        if (!is.null(self$`maxGenerations`)) {
          sprintf(
            '"maxGenerations":
            %d
                  ',
            self$`maxGenerations`
          )
        },
        if (!is.null(self$`numThreads`)) {
          sprintf(
            '"numThreads":
            %d
                  ',
            self$`numThreads`
          )
        },
        if (!is.null(self$`priorSolutions`)) {
          sprintf(
            '"priorSolutions":
              [%s]
            ',
            paste(unlist(lapply(self$`priorSolutions`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`randomSeed`)) {
          sprintf(
            '"randomSeed":
            %d
                  ',
            self$`randomSeed`
          )
        },
        if (!is.null(self$`splitMode`)) {
          sprintf(
            '"splitMode":
            "%s"
                  ',
            self$`splitMode`
          )
        },
        if (!is.null(self$`syncMigrations`)) {
          sprintf(
            '"syncMigrations":
            %s
                  ',
            tolower(self$`syncMigrations`)
          )
        },
        if (!is.null(self$`targetExpressionFormat`)) {
          sprintf(
            '"targetExpressionFormat":
            "%s"
                  ',
            self$`targetExpressionFormat`
          )
        },
        if (!is.null(self$`targetExpressionString`)) {
          sprintf(
            '"targetExpressionString":
            "%s"
                  ',
            self$`targetExpressionString`
          )
        },
        if (!is.null(self$`timeoutSec`)) {
          sprintf(
            '"timeoutSec":
            %d
                  ',
            self$`timeoutSec`
          )
        },
        if (!is.null(self$`trainingFraction`)) {
          sprintf(
            '"trainingFraction":
            %d
                  ',
            self$`trainingFraction`
          )
        },
        if (!is.null(self$`trainingSplitExpr`)) {
          sprintf(
            '"trainingSplitExpr":
            "%s"
                  ',
            self$`trainingSplitExpr`
          )
        },
        if (!is.null(self$`validationFraction`)) {
          sprintf(
            '"validationFraction":
            %d
                  ',
            self$`validationFraction`
          )
        },
        if (!is.null(self$`validationSplitExpr`)) {
          sprintf(
            '"validationSplitExpr":
            "%s"
                  ',
            self$`validationSplitExpr`
          )
        },
        if (!is.null(self$`weightExpr`)) {
          sprintf(
            '"weightExpr":
            "%s"
                  ',
            self$`weightExpr`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param HyperparametersResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(HyperparametersResponseJson, validateParams = FALSE) {
      HyperparametersResponseObject <- jsonlite::fromJSON(HyperparametersResponseJson)
      self$`buildingBlocks` <- HyperparametersResponseObject$`buildingBlocks`
      self$`errorMetric` <- HyperparametersResponseObject$`errorMetric`
      self$`maxGenerations` <- HyperparametersResponseObject$`maxGenerations`
      self$`numThreads` <- HyperparametersResponseObject$`numThreads`
      self$`priorSolutions` <- ApiClient$new()$deserializeObj(HyperparametersResponseObject$`priorSolutions`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`randomSeed` <- HyperparametersResponseObject$`randomSeed`
      self$`splitMode` <- HyperparametersResponseObject$`splitMode`
      self$`syncMigrations` <- HyperparametersResponseObject$`syncMigrations`
      self$`targetExpressionFormat` <- HyperparametersResponseObject$`targetExpressionFormat`
      self$`targetExpressionString` <- HyperparametersResponseObject$`targetExpressionString`
      self$`timeoutSec` <- HyperparametersResponseObject$`timeoutSec`
      self$`trainingFraction` <- HyperparametersResponseObject$`trainingFraction`
      self$`trainingSplitExpr` <- HyperparametersResponseObject$`trainingSplitExpr`
      self$`validationFraction` <- HyperparametersResponseObject$`validationFraction`
      self$`validationSplitExpr` <- HyperparametersResponseObject$`validationSplitExpr`
      self$`weightExpr` <- HyperparametersResponseObject$`weightExpr`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
