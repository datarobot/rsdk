# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureLineageStep
#'
#' @description FeatureLineageStep Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field arguments  object [optional] Generic key-value pairs to describe **action** step additional parameters.
#'
#' @field catalogId  character [optional] id of the catalog for a **data** step.
#'
#' @field catalogVersionId  character [optional] id of the catalog version for a **data** step.
#'
#' @field description  character [optional] Description of the step.
#'
#' @field groupBy  list( character ) [optional] List of columns which this **action** step aggregated.
#'
#' @field id  integer Step id starting with 0.
#'
#' @field isTimeAware  character [optional] Indicator of step being time aware. Mandatory only for **action** and **join** steps. **action** step provides additional information about feature derivation window in the &#x60;timeInfo&#x60; field.
#'
#' @field joinInfo  \link{FeatureLineageJoin} [optional]
#'
#' @field name  character [optional] Name of the step.
#'
#' @field parents  list( integer ) &#x60;id&#x60; of steps which use this step output as their input.
#'
#' @field stepType  character One of four types of a step. **data** - source features. **action** - data aggregation or trasformation. **join** - SQL JOIN. **generatedData** - final feature. There is always one **generatedData** step and at least one **data** step.
#'
#' @field timeInfo  \link{FeatureLineageTimeInfo} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureLineageStep <- R6::R6Class(
  "FeatureLineageStep",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`arguments` = NULL, `catalogId` = NULL, `catalogVersionId` = NULL, `description` = NULL, `groupBy` = NULL, `id` = NULL, `isTimeAware` = NULL, `joinInfo` = NULL, `name` = NULL, `parents` = NULL, `stepType` = NULL, `timeInfo` = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.numeric(`id`), length(`id`) == 1)
      }
      if (!is.null(`parents`)) {
        stopifnot(is.vector(`parents`))
      }
      if (!is.null(`stepType`)) {
        stopifnot(is.character(`stepType`), length(`stepType`) == 1)
      }
      if (!is.null(`arguments`)) {
      }
      if (!is.null(`catalogId`)) {
        stopifnot(is.character(`catalogId`), length(`catalogId`) == 1)
      }
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`groupBy`)) {
        stopifnot(is.vector(`groupBy`))
      }
      if (!is.null(`isTimeAware`)) {
        stopifnot(is.logical(`isTimeAware`), length(`isTimeAware`) == 1)
      }
      if (!is.null(`joinInfo`)) {
        stopifnot(R6::is.R6(`joinInfo`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`timeInfo`)) {
        stopifnot(R6::is.R6(`timeInfo`))
      }
    }
  ),
  public = list(
    `arguments` = NULL,
    `catalogId` = NULL,
    `catalogVersionId` = NULL,
    `description` = NULL,
    `groupBy` = NULL,
    `id` = NULL,
    `isTimeAware` = NULL,
    `joinInfo` = NULL,
    `name` = NULL,
    `parents` = NULL,
    `stepType` = NULL,
    `timeInfo` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param arguments Generic key-value pairs to describe **action** step additional parameters.
    #' @param catalogId id of the catalog for a **data** step.
    #' @param catalogVersionId id of the catalog version for a **data** step.
    #' @param description Description of the step.
    #' @param groupBy List of columns which this **action** step aggregated.
    #' @param id Step id starting with 0.
    #' @param isTimeAware Indicator of step being time aware. Mandatory only for **action** and **join** steps. **action** step provides additional information about feature derivation window in the &#x60;timeInfo&#x60; field.
    #' @param joinInfo
    #' @param name Name of the step.
    #' @param parents &#x60;id&#x60; of steps which use this step output as their input.
    #' @param stepType One of four types of a step. **data** - source features. **action** - data aggregation or trasformation. **join** - SQL JOIN. **generatedData** - final feature. There is always one **generatedData** step and at least one **data** step.
    #' @param timeInfo
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `parents` = NULL, `stepType` = NULL, `arguments` = NULL, `catalogId` = NULL, `catalogVersionId` = NULL, `description` = NULL, `groupBy` = NULL, `isTimeAware` = NULL, `joinInfo` = NULL, `name` = NULL, `timeInfo` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`id`, `parents`, `stepType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(arguments, catalogId, catalogVersionId, description, groupBy, id, isTimeAware, joinInfo, name, parents, stepType, timeInfo)
      }
      self$`arguments` <- `arguments`
      self$`catalogId` <- `catalogId`
      self$`catalogVersionId` <- `catalogVersionId`
      self$`description` <- `description`
      sapply(`groupBy`, function(x) stopifnot(is.character(x)))
      self$`id` <- `id`
      self$`isTimeAware` <- `isTimeAware`
      self$`joinInfo` <- `joinInfo`
      self$`name` <- `name`
      sapply(`parents`, function(x) stopifnot(is.character(x)))
      self$`stepType` <- `stepType`
      self$`timeInfo` <- `timeInfo`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(arguments = self$`arguments`, catalogId = self$`catalogId`, catalogVersionId = self$`catalogVersionId`, description = self$`description`, groupBy = self$`groupBy`, id = self$`id`, isTimeAware = self$`isTimeAware`, joinInfo = self$`joinInfo`, name = self$`name`, parents = self$`parents`, stepType = self$`stepType`, timeInfo = self$`timeInfo`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`arguments`)) {
          sprintf(
            '"arguments":
            "%s"
                  ',
            self$`arguments`
          )
        },
        if (!is.null(self$`catalogId`)) {
          sprintf(
            '"catalogId":
            "%s"
                  ',
            self$`catalogId`
          )
        },
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`groupBy`)) {
          sprintf(
            '"groupBy":
            [%s]
                  ',
            paste(unlist(lapply(self$`groupBy`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            %d
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`isTimeAware`)) {
          sprintf(
            '"isTimeAware":
            %s
                  ',
            tolower(self$`isTimeAware`)
          )
        },
        if (!is.null(self$`joinInfo`)) {
          sprintf(
            '"joinInfo":
            %s
      ',
            jsonlite::toJSON(self$`joinInfo`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`parents`)) {
          sprintf(
            '"parents":
            [%s]
                  ',
            paste(unlist(lapply(self$`parents`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`stepType`)) {
          sprintf(
            '"stepType":
            "%s"
                  ',
            self$`stepType`
          )
        },
        if (!is.null(self$`timeInfo`)) {
          sprintf(
            '"timeInfo":
            %s
      ',
            jsonlite::toJSON(self$`timeInfo`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureLineageStepJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureLineageStepJson, validateParams = FALSE) {
      FeatureLineageStepObject <- jsonlite::fromJSON(FeatureLineageStepJson)
      self$`arguments` <- FeatureLineageStepObject$`arguments`
      self$`catalogId` <- FeatureLineageStepObject$`catalogId`
      self$`catalogVersionId` <- FeatureLineageStepObject$`catalogVersionId`
      self$`description` <- FeatureLineageStepObject$`description`
      self$`groupBy` <- ApiClient$new()$deserializeObj(FeatureLineageStepObject$`groupBy`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`id` <- FeatureLineageStepObject$`id`
      self$`isTimeAware` <- FeatureLineageStepObject$`isTimeAware`
      self$`joinInfo` <- FeatureLineageJoin$new()$fromJSON(jsonlite::toJSON(FeatureLineageStepObject$joinInfo, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`name` <- FeatureLineageStepObject$`name`
      self$`parents` <- ApiClient$new()$deserializeObj(FeatureLineageStepObject$`parents`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`stepType` <- FeatureLineageStepObject$`stepType`
      self$`timeInfo` <- FeatureLineageTimeInfo$new()$fromJSON(jsonlite::toJSON(FeatureLineageStepObject$timeInfo, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
