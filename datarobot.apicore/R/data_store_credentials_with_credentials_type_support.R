# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataStoreCredentialsWithCredentialsTypeSupport
#'
#' @description DataStoreCredentialsWithCredentialsTypeSupport Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialData  \link{OneOfBasicCredentialsS3CredentialsOAuthCredentials} [optional] Type of credentials to use with the data store.
#'
#' @field credentialId  character [optional] ID of the set of credentials to use instead of username and password.
#'
#' @field password  character [optional] Password for data store authentication.
#'
#' @field useKerberos  character [optional] Whether to use Kerberos for data store authentication.
#'
#' @field user  character [optional] Username for data store authentication.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataStoreCredentialsWithCredentialsTypeSupport <- R6::R6Class(
  "DataStoreCredentialsWithCredentialsTypeSupport",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`credentialData` = NULL, `credentialId` = NULL, `password` = NULL, `useKerberos` = NULL, `user` = NULL) {
      if (!is.null(`credentialData`)) {
        .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`useKerberos`)) {
        stopifnot(is.logical(`useKerberos`), length(`useKerberos`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `credentialData` = NULL,
    `credentialId` = NULL,
    `password` = NULL,
    `useKerberos` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialData Type of credentials to use with the data store.
    #' @param credentialId ID of the set of credentials to use instead of username and password.
    #' @param password Password for data store authentication.
    #' @param useKerberos Whether to use Kerberos for data store authentication.
    #' @param user Username for data store authentication.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialData` = NULL, `credentialId` = NULL, `password` = NULL, `useKerberos` = FALSE, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(credentialData, credentialId, password, useKerberos, user)
      }
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = credentialData)
      self$`credentialId` <- `credentialId`
      self$`password` <- `password`
      self$`useKerberos` <- `useKerberos`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(credentialData = self$`credentialData`, credentialId = self$`credentialId`, password = self$`password`, useKerberos = self$`useKerberos`, user = self$`user`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialData`)) {
          sprintf(
            '"credentialData":
            %s
      ',
            jsonlite::toJSON(self$`credentialData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`useKerberos`)) {
          sprintf(
            '"useKerberos":
            %s
                  ',
            tolower(self$`useKerberos`)
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataStoreCredentialsWithCredentialsTypeSupportJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataStoreCredentialsWithCredentialsTypeSupportJson, validateParams = FALSE) {
      DataStoreCredentialsWithCredentialsTypeSupportObject <- jsonlite::fromJSON(DataStoreCredentialsWithCredentialsTypeSupportJson)
      self$`credentialData` <- .setComplexProperty(typeList = list(BasicCredentials, S3Credentials, OAuthCredentials), propertyData = DataStoreCredentialsWithCredentialsTypeSupportObject$credentialData)
      self$`credentialId` <- DataStoreCredentialsWithCredentialsTypeSupportObject$`credentialId`
      self$`password` <- DataStoreCredentialsWithCredentialsTypeSupportObject$`password`
      self$`useKerberos` <- DataStoreCredentialsWithCredentialsTypeSupportObject$`useKerberos`
      self$`user` <- DataStoreCredentialsWithCredentialsTypeSupportObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
