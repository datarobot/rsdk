# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BlenderInfoRetrieveResponse
#'
#' @description BlenderInfoRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blendable  character If True, the models can be blended.
#'
#' @field reason  character Useful info as to why a model can&#39;t be blended.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BlenderInfoRetrieveResponse <- R6::R6Class(
  "BlenderInfoRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(blendable = NULL, reason = NULL) {
      if (!is.null(`blendable`)) {
        stopifnot(is.logical(`blendable`), length(`blendable`) == 1)
      }
      if (!is.null(`reason`)) {
        stopifnot(is.character(`reason`), length(`reason`) == 1)
      }
    }
  ),
  public = list(
    `blendable` = NULL,
    `reason` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blendable If True, the models can be blended.
    #' @param reason Useful info as to why a model can&#39;t be blended.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blendable` = NULL, `reason` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`blendable`, `reason`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(blendable, reason)
      }
      self$`blendable` <- `blendable`
      self$`reason` <- `reason`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(blendable = self$`blendable`, reason = self$`reason`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blendable`)) {
          sprintf(
            '"blendable":
            %s
                  ',
            tolower(self$`blendable`)
          )
        },
        if (!is.null(self$`reason`)) {
          sprintf(
            '"reason":
            "%s"
                  ',
            self$`reason`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BlenderInfoRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BlenderInfoRetrieveResponseJson, validateParams = FALSE) {
      BlenderInfoRetrieveResponseObject <- jsonlite::fromJSON(BlenderInfoRetrieveResponseJson)
      self$`blendable` <- BlenderInfoRetrieveResponseObject$`blendable`
      self$`reason` <- BlenderInfoRetrieveResponseObject$`reason`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
