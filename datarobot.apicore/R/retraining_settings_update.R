# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RetrainingSettingsUpdate
#'
#' @description RetrainingSettingsUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialId  character [optional] ID of the credential used to refresh retraining dataset.
#'
#' @field datasetId  character [optional] ID of the retraining dataset.
#'
#' @field predictionEnvironmentId  character [optional] ID of the prediction environment to associate with the challengers created by retraining policies.
#'
#' @field retrainingUserId  character [optional] ID of the retraining user.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RetrainingSettingsUpdate <- R6::R6Class(
  "RetrainingSettingsUpdate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`credentialId` = NULL, `datasetId` = NULL, `predictionEnvironmentId` = NULL, `retrainingUserId` = NULL) {
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`predictionEnvironmentId`)) {
        stopifnot(is.character(`predictionEnvironmentId`), length(`predictionEnvironmentId`) == 1)
      }
      if (!is.null(`retrainingUserId`)) {
        stopifnot(is.character(`retrainingUserId`), length(`retrainingUserId`) == 1)
      }
    }
  ),
  public = list(
    `credentialId` = NULL,
    `datasetId` = NULL,
    `predictionEnvironmentId` = NULL,
    `retrainingUserId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialId ID of the credential used to refresh retraining dataset.
    #' @param datasetId ID of the retraining dataset.
    #' @param predictionEnvironmentId ID of the prediction environment to associate with the challengers created by retraining policies.
    #' @param retrainingUserId ID of the retraining user.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialId` = NULL, `datasetId` = NULL, `predictionEnvironmentId` = NULL, `retrainingUserId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(credentialId, datasetId, predictionEnvironmentId, retrainingUserId)
      }
      self$`credentialId` <- `credentialId`
      self$`datasetId` <- `datasetId`
      self$`predictionEnvironmentId` <- `predictionEnvironmentId`
      self$`retrainingUserId` <- `retrainingUserId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(credentialId = self$`credentialId`, datasetId = self$`datasetId`, predictionEnvironmentId = self$`predictionEnvironmentId`, retrainingUserId = self$`retrainingUserId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`predictionEnvironmentId`)) {
          sprintf(
            '"predictionEnvironmentId":
            "%s"
                  ',
            self$`predictionEnvironmentId`
          )
        },
        if (!is.null(self$`retrainingUserId`)) {
          sprintf(
            '"retrainingUserId":
            "%s"
                  ',
            self$`retrainingUserId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RetrainingSettingsUpdateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RetrainingSettingsUpdateJson, validateParams = FALSE) {
      RetrainingSettingsUpdateObject <- jsonlite::fromJSON(RetrainingSettingsUpdateJson)
      self$`credentialId` <- RetrainingSettingsUpdateObject$`credentialId`
      self$`datasetId` <- RetrainingSettingsUpdateObject$`datasetId`
      self$`predictionEnvironmentId` <- RetrainingSettingsUpdateObject$`predictionEnvironmentId`
      self$`retrainingUserId` <- RetrainingSettingsUpdateObject$`retrainingUserId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
