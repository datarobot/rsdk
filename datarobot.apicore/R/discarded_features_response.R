# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DiscardedFeaturesResponse
#'
#' @description DiscardedFeaturesResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer Discarded features count.
#'
#' @field features  list( character ) Discarded features.
#'
#' @field remainingRestoreLimit  integer The remaining available number of the features which can be restored in this project.
#'
#' @field totalRestoreLimit  integer The total limit indicating how many features can be restored in this project.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DiscardedFeaturesResponse <- R6::R6Class(
  "DiscardedFeaturesResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `features` = NULL, `remainingRestoreLimit` = NULL, `totalRestoreLimit` = NULL) {
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`), sapply(`features`, is.character))
      }
      if (!is.null(`remainingRestoreLimit`)) {
        stopifnot(is.numeric(`remainingRestoreLimit`), length(`remainingRestoreLimit`) == 1)
      }
      if (!is.null(`totalRestoreLimit`)) {
        stopifnot(is.numeric(`totalRestoreLimit`), length(`totalRestoreLimit`) == 1)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `features` = NULL,
    `remainingRestoreLimit` = NULL,
    `totalRestoreLimit` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count Discarded features count.
    #' @param features Discarded features.
    #' @param remainingRestoreLimit The remaining available number of the features which can be restored in this project.
    #' @param totalRestoreLimit The total limit indicating how many features can be restored in this project.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `features` = NULL, `remainingRestoreLimit` = NULL, `totalRestoreLimit` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`count`, `features`, `remainingRestoreLimit`, `totalRestoreLimit`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, features, remainingRestoreLimit, totalRestoreLimit)
      }
      self$`count` <- `count`
      self$`features` <- `features`
      self$`remainingRestoreLimit` <- `remainingRestoreLimit`
      self$`totalRestoreLimit` <- `totalRestoreLimit`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, features = self$`features`, remainingRestoreLimit = self$`remainingRestoreLimit`, totalRestoreLimit = self$`totalRestoreLimit`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
                  ',
            paste(unlist(lapply(self$`features`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`remainingRestoreLimit`)) {
          sprintf(
            '"remainingRestoreLimit":
            %d
                  ',
            self$`remainingRestoreLimit`
          )
        },
        if (!is.null(self$`totalRestoreLimit`)) {
          sprintf(
            '"totalRestoreLimit":
            %d
                  ',
            self$`totalRestoreLimit`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DiscardedFeaturesResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DiscardedFeaturesResponseJson, validateParams = FALSE) {
      DiscardedFeaturesResponseObject <- jsonlite::fromJSON(DiscardedFeaturesResponseJson)
      self$`count` <- DiscardedFeaturesResponseObject$`count`
      self$`features` <- ApiClient$new()$deserializeObj(DiscardedFeaturesResponseObject$`features`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`remainingRestoreLimit` <- DiscardedFeaturesResponseObject$`remainingRestoreLimit`
      self$`totalRestoreLimit` <- DiscardedFeaturesResponseObject$`totalRestoreLimit`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
