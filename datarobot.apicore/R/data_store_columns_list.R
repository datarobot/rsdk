# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataStoreColumnsList
#'
#' @description DataStoreColumnsList Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalog  character [optional] Name of specified database catalog.
#'
#' @field credentialId  character [optional] Id of credential mapping.
#'
#' @field password  character [optional] Password for the db connection.
#'
#' @field query  character [optional] Schema query.
#'
#' @field schema  character [optional] Schema name.
#'
#' @field table  character [optional] Table name.
#'
#' @field user  character [optional] Username for the db connection.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataStoreColumnsList <- R6::R6Class(
  "DataStoreColumnsList",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`catalog` = NULL, `credentialId` = NULL, `password` = NULL, `query` = NULL, `schema` = NULL, `table` = NULL, `user` = NULL) {
      if (!is.null(`catalog`)) {
        stopifnot(is.character(`catalog`), length(`catalog`) == 1)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`password`)) {
        stopifnot(is.character(`password`), length(`password`) == 1)
      }
      if (!is.null(`query`)) {
        stopifnot(is.character(`query`), length(`query`) == 1)
      }
      if (!is.null(`schema`)) {
        stopifnot(is.character(`schema`), length(`schema`) == 1)
      }
      if (!is.null(`table`)) {
        stopifnot(is.character(`table`), length(`table`) == 1)
      }
      if (!is.null(`user`)) {
        stopifnot(is.character(`user`), length(`user`) == 1)
      }
    }
  ),
  public = list(
    `catalog` = NULL,
    `credentialId` = NULL,
    `password` = NULL,
    `query` = NULL,
    `schema` = NULL,
    `table` = NULL,
    `user` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalog Name of specified database catalog.
    #' @param credentialId Id of credential mapping.
    #' @param password Password for the db connection.
    #' @param query Schema query.
    #' @param schema Schema name.
    #' @param table Table name.
    #' @param user Username for the db connection.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`catalog` = NULL, `credentialId` = NULL, `password` = NULL, `query` = NULL, `schema` = NULL, `table` = NULL, `user` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(catalog, credentialId, password, query, schema, table, user)
      }
      self$`catalog` <- `catalog`
      self$`credentialId` <- `credentialId`
      self$`password` <- `password`
      self$`query` <- `query`
      self$`schema` <- `schema`
      self$`table` <- `table`
      self$`user` <- `user`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(catalog = self$`catalog`, credentialId = self$`credentialId`, password = self$`password`, query = self$`query`, schema = self$`schema`, table = self$`table`, user = self$`user`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalog`)) {
          sprintf(
            '"catalog":
            "%s"
                  ',
            self$`catalog`
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`password`)) {
          sprintf(
            '"password":
            "%s"
                  ',
            self$`password`
          )
        },
        if (!is.null(self$`query`)) {
          sprintf(
            '"query":
            "%s"
                  ',
            self$`query`
          )
        },
        if (!is.null(self$`schema`)) {
          sprintf(
            '"schema":
            "%s"
                  ',
            self$`schema`
          )
        },
        if (!is.null(self$`table`)) {
          sprintf(
            '"table":
            "%s"
                  ',
            self$`table`
          )
        },
        if (!is.null(self$`user`)) {
          sprintf(
            '"user":
            "%s"
                  ',
            self$`user`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataStoreColumnsListJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataStoreColumnsListJson, validateParams = FALSE) {
      DataStoreColumnsListObject <- jsonlite::fromJSON(DataStoreColumnsListJson)
      self$`catalog` <- DataStoreColumnsListObject$`catalog`
      self$`credentialId` <- DataStoreColumnsListObject$`credentialId`
      self$`password` <- DataStoreColumnsListObject$`password`
      self$`query` <- DataStoreColumnsListObject$`query`
      self$`schema` <- DataStoreColumnsListObject$`schema`
      self$`table` <- DataStoreColumnsListObject$`table`
      self$`user` <- DataStoreColumnsListObject$`user`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
