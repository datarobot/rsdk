# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintsInputTypesResponse
#'
#' @description UserBlueprintsInputTypesResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field inputTypes  list( \link{UserBlueprintsInputType} ) A list of associated pairs of an input types and their human-readable names.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintsInputTypesResponse <- R6::R6Class(
  "UserBlueprintsInputTypesResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(inputTypes = NULL) {
      if (!is.null(`inputTypes`)) {
        stopifnot(is.vector(`inputTypes`))
      }
    }
  ),
  public = list(
    `inputTypes` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param inputTypes A list of associated pairs of an input types and their human-readable names.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`inputTypes` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`inputTypes`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(inputTypes)
      }
      sapply(`inputTypes`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(inputTypes = self$`inputTypes`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`inputTypes`)) {
          sprintf(
            '"inputTypes":
            [%s]
',
            paste(sapply(self$`inputTypes`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintsInputTypesResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintsInputTypesResponseJson, validateParams = FALSE) {
      UserBlueprintsInputTypesResponseObject <- jsonlite::fromJSON(UserBlueprintsInputTypesResponseJson)
      self$`inputTypes` <- ApiClient$new()$deserializeObj(UserBlueprintsInputTypesResponseObject$`inputTypes`, "array[UserBlueprintsInputType]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
