# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseValueTemplateCalculateResponse
#'
#' @description UseCaseValueTemplateCalculateResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field averageValuePerDecision  numeric Estimated average value per decision
#'
#' @field savedAnnually  numeric Estimated amount saved annually
#'
#' @field template  \link{UseCaseValueCalculateTemplate}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseValueTemplateCalculateResponse <- R6::R6Class(
  "UseCaseValueTemplateCalculateResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`averageValuePerDecision` = NULL, `savedAnnually` = NULL, `template` = NULL) {
      if (!is.null(`averageValuePerDecision`)) {
      }
      if (!is.null(`savedAnnually`)) {
      }
      if (!is.null(`template`)) {
        stopifnot(R6::is.R6(`template`))
      }
    }
  ),
  public = list(
    `averageValuePerDecision` = NULL,
    `savedAnnually` = NULL,
    `template` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param averageValuePerDecision Estimated average value per decision
    #' @param savedAnnually Estimated amount saved annually
    #' @param template
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`averageValuePerDecision` = NULL, `savedAnnually` = NULL, `template` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`averageValuePerDecision`, `savedAnnually`, `template`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(averageValuePerDecision, savedAnnually, template)
      }
      self$`averageValuePerDecision` <- `averageValuePerDecision`
      self$`savedAnnually` <- `savedAnnually`
      self$`template` <- `template`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(averageValuePerDecision = self$`averageValuePerDecision`, savedAnnually = self$`savedAnnually`, template = self$`template`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`averageValuePerDecision`)) {
          sprintf(
            '"averageValuePerDecision":
            %d
                  ',
            self$`averageValuePerDecision`
          )
        },
        if (!is.null(self$`savedAnnually`)) {
          sprintf(
            '"savedAnnually":
            %d
                  ',
            self$`savedAnnually`
          )
        },
        if (!is.null(self$`template`)) {
          sprintf(
            '"template":
            %s
      ',
            jsonlite::toJSON(self$`template`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseValueTemplateCalculateResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseValueTemplateCalculateResponseJson, validateParams = FALSE) {
      UseCaseValueTemplateCalculateResponseObject <- jsonlite::fromJSON(UseCaseValueTemplateCalculateResponseJson)
      self$`averageValuePerDecision` <- UseCaseValueTemplateCalculateResponseObject$`averageValuePerDecision`
      self$`savedAnnually` <- UseCaseValueTemplateCalculateResponseObject$`savedAnnually`
      self$`template` <- UseCaseValueCalculateTemplate$new()$fromJSON(jsonlite::toJSON(UseCaseValueTemplateCalculateResponseObject$template, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
