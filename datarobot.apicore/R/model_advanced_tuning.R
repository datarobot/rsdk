# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelAdvancedTuning
#'
#' @description ModelAdvancedTuning Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field tuningDescription  character [optional] Human-readable description of this advanced-tuning request.
#'
#' @field tuningParameters  list( \link{TuningParameter} ) Parameters to tune.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelAdvancedTuning <- R6::R6Class(
  "ModelAdvancedTuning",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(tuningDescription = NULL, tuningParameters = NULL) {
      if (!is.null(`tuningParameters`)) {
        stopifnot(is.vector(`tuningParameters`))
      }
      if (!is.null(`tuningDescription`)) {
        stopifnot(is.character(`tuningDescription`), length(`tuningDescription`) == 1)
      }
    }
  ),
  public = list(
    `tuningDescription` = NULL,
    `tuningParameters` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param tuningDescription Human-readable description of this advanced-tuning request.
    #' @param tuningParameters Parameters to tune.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`tuningParameters` = NULL, `tuningDescription` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`tuningParameters`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(tuningDescription, tuningParameters)
      }
      self$`tuningDescription` <- `tuningDescription`
      sapply(`tuningParameters`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(tuningDescription = self$`tuningDescription`, tuningParameters = self$`tuningParameters`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`tuningDescription`)) {
          sprintf(
            '"tuningDescription":
            "%s"
                  ',
            self$`tuningDescription`
          )
        },
        if (!is.null(self$`tuningParameters`)) {
          sprintf(
            '"tuningParameters":
            [%s]
',
            paste(sapply(self$`tuningParameters`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelAdvancedTuningJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelAdvancedTuningJson, validateParams = FALSE) {
      ModelAdvancedTuningObject <- jsonlite::fromJSON(ModelAdvancedTuningJson)
      self$`tuningDescription` <- ModelAdvancedTuningObject$`tuningDescription`
      self$`tuningParameters` <- ApiClient$new()$deserializeObj(ModelAdvancedTuningObject$`tuningParameters`, "array[TuningParameter]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
