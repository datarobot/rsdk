# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TrainDatetimeModel
#'
#' @description TrainDatetimeModel Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blueprintId  character The ID of a blueprint to use to generate the model. Allowed blueprints can be retrieved using :http:get:&#x60;/api/v2/projects/(projectId)/blueprints/&#x60; or taken from existing models.
#'
#' @field featurelistId  character [optional] If specified, the model will be trained using this featurelist. If not specified, the recommended featurelist for the specified blueprint will be used. If there is no recommended featurelist, the project&#39;s default will be used.
#'
#' @field monotonicDecreasingFeaturelistId  character [optional] The ID of the featurelist that defines the set of features with a monotonically decreasing relationship to the target. If &#x60;&#x60;null&#x60;&#x60;, no constraints will be enforced. If omitted, the project default is used. May only be specified for OTV projects.
#'
#' @field monotonicIncreasingFeaturelistId  character [optional] The ID of the featurelist that defines the set of features with a monotonically increasing relationship to the target. If &#x60;&#x60;null&#x60;&#x60;, no constraints will be enforced. If omitted, the project default is used. May only be specified for OTV projects.
#'
#' @field nClusters  integer [optional] The number of clusters to use in the specified unsupervised clustering model. Only valid in unsupervised clustering projects.
#'
#' @field samplingMethod  character [optional] Defines how training data is selected if subsampling is used (e.g., if &#x60;timeWindowSamplePct&#x60; is specified). Can be either &#x60;&#x60;random&#x60;&#x60; or &#x60;&#x60;latest&#x60;&#x60;. If omitted, defaults to &#x60;&#x60;latest&#x60;&#x60; if &#x60;trainingRowCount&#x60; is used and &#x60;&#x60;random&#x60;&#x60; for other cases (e.g., if &#x60;trainingDuration&#x60; or &#x60;useProjectSettings&#x60; is specified). May only be specified for OTV projects.
#'
#' @field sourceProjectId  character [optional] The project the blueprint comes from. Required only if the &#x60;blueprintId&#x60; comes from a different project.
#'
#' @field timeWindowSamplePct  integer [optional] An integer between 1 and 99 indicating the percentage of sampling within the time window. The points kept are determined by the value provided for the &#x60;samplingMethod&#x60; option. If specified, &#x60;trainingRowCount&#x60; may not be specified, and the specified model must either be a duration or selectedDateRange model, or one of &#x60;trainingDuration&#x60; or &#x60;trainingStartDate&#x60; and &#x60;trainingEndDate&#x60; must be specified.
#'
#' @field trainingDuration  character [optional] A duration string representing the training duration for the submitted model.
#'
#' @field trainingRowCount  integer [optional] The number of rows of data that should be used when training this model.
#'
#' @field useProjectSettings  character [optional] If &#x60;&#x60;True&#x60;&#x60;, the model will be trained using the previously-specified custom backtest training settings.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TrainDatetimeModel <- R6::R6Class(
  "TrainDatetimeModel",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(blueprintId = NULL, featurelistId = NULL, monotonicDecreasingFeaturelistId = NULL, monotonicIncreasingFeaturelistId = NULL, nClusters = NULL, samplingMethod = NULL, sourceProjectId = NULL, timeWindowSamplePct = NULL, trainingDuration = NULL, trainingRowCount = NULL, useProjectSettings = NULL) {
      if (!is.null(`blueprintId`)) {
        stopifnot(is.character(`blueprintId`), length(`blueprintId`) == 1)
      }
      if (!is.null(`featurelistId`)) {
        stopifnot(is.character(`featurelistId`), length(`featurelistId`) == 1)
      }
      if (!is.null(`monotonicDecreasingFeaturelistId`)) {
        stopifnot(is.character(`monotonicDecreasingFeaturelistId`), length(`monotonicDecreasingFeaturelistId`) == 1)
      }
      if (!is.null(`monotonicIncreasingFeaturelistId`)) {
        stopifnot(is.character(`monotonicIncreasingFeaturelistId`), length(`monotonicIncreasingFeaturelistId`) == 1)
      }
      if (!is.null(`nClusters`)) {
        stopifnot(is.numeric(`nClusters`), length(`nClusters`) == 1)
      }
      if (!is.null(`samplingMethod`)) {
        stopifnot(is.character(`samplingMethod`), length(`samplingMethod`) == 1)
      }
      if (!is.null(`sourceProjectId`)) {
        stopifnot(is.character(`sourceProjectId`), length(`sourceProjectId`) == 1)
      }
      if (!is.null(`timeWindowSamplePct`)) {
        stopifnot(is.numeric(`timeWindowSamplePct`), length(`timeWindowSamplePct`) == 1)
      }
      if (!is.null(`trainingDuration`)) {
        stopifnot(is.character(`trainingDuration`), length(`trainingDuration`) == 1)
      }
      if (!is.null(`trainingRowCount`)) {
        stopifnot(is.numeric(`trainingRowCount`), length(`trainingRowCount`) == 1)
      }
      if (!is.null(`useProjectSettings`)) {
        stopifnot(is.logical(`useProjectSettings`), length(`useProjectSettings`) == 1)
      }
    }
  ),
  public = list(
    `blueprintId` = NULL,
    `featurelistId` = NULL,
    `monotonicDecreasingFeaturelistId` = NULL,
    `monotonicIncreasingFeaturelistId` = NULL,
    `nClusters` = NULL,
    `samplingMethod` = NULL,
    `sourceProjectId` = NULL,
    `timeWindowSamplePct` = NULL,
    `trainingDuration` = NULL,
    `trainingRowCount` = NULL,
    `useProjectSettings` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blueprintId The ID of a blueprint to use to generate the model. Allowed blueprints can be retrieved using :http:get:&#x60;/api/v2/projects/(projectId)/blueprints/&#x60; or taken from existing models.
    #' @param featurelistId If specified, the model will be trained using this featurelist. If not specified, the recommended featurelist for the specified blueprint will be used. If there is no recommended featurelist, the project&#39;s default will be used.
    #' @param monotonicDecreasingFeaturelistId The ID of the featurelist that defines the set of features with a monotonically decreasing relationship to the target. If &#x60;&#x60;null&#x60;&#x60;, no constraints will be enforced. If omitted, the project default is used. May only be specified for OTV projects.
    #' @param monotonicIncreasingFeaturelistId The ID of the featurelist that defines the set of features with a monotonically increasing relationship to the target. If &#x60;&#x60;null&#x60;&#x60;, no constraints will be enforced. If omitted, the project default is used. May only be specified for OTV projects.
    #' @param nClusters The number of clusters to use in the specified unsupervised clustering model. Only valid in unsupervised clustering projects.
    #' @param samplingMethod Defines how training data is selected if subsampling is used (e.g., if &#x60;timeWindowSamplePct&#x60; is specified). Can be either &#x60;&#x60;random&#x60;&#x60; or &#x60;&#x60;latest&#x60;&#x60;. If omitted, defaults to &#x60;&#x60;latest&#x60;&#x60; if &#x60;trainingRowCount&#x60; is used and &#x60;&#x60;random&#x60;&#x60; for other cases (e.g., if &#x60;trainingDuration&#x60; or &#x60;useProjectSettings&#x60; is specified). May only be specified for OTV projects.
    #' @param sourceProjectId The project the blueprint comes from. Required only if the &#x60;blueprintId&#x60; comes from a different project.
    #' @param timeWindowSamplePct An integer between 1 and 99 indicating the percentage of sampling within the time window. The points kept are determined by the value provided for the &#x60;samplingMethod&#x60; option. If specified, &#x60;trainingRowCount&#x60; may not be specified, and the specified model must either be a duration or selectedDateRange model, or one of &#x60;trainingDuration&#x60; or &#x60;trainingStartDate&#x60; and &#x60;trainingEndDate&#x60; must be specified.
    #' @param trainingDuration A duration string representing the training duration for the submitted model.
    #' @param trainingRowCount The number of rows of data that should be used when training this model.
    #' @param useProjectSettings If &#x60;&#x60;True&#x60;&#x60;, the model will be trained using the previously-specified custom backtest training settings.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blueprintId` = NULL, `featurelistId` = NULL, `monotonicDecreasingFeaturelistId` = NULL, `monotonicIncreasingFeaturelistId` = NULL, `nClusters` = NULL, `samplingMethod` = NULL, `sourceProjectId` = NULL, `timeWindowSamplePct` = NULL, `trainingDuration` = NULL, `trainingRowCount` = NULL, `useProjectSettings` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`blueprintId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(blueprintId, featurelistId, monotonicDecreasingFeaturelistId, monotonicIncreasingFeaturelistId, nClusters, samplingMethod, sourceProjectId, timeWindowSamplePct, trainingDuration, trainingRowCount, useProjectSettings)
      }
      self$`blueprintId` <- `blueprintId`
      self$`featurelistId` <- `featurelistId`
      self$`monotonicDecreasingFeaturelistId` <- `monotonicDecreasingFeaturelistId`
      self$`monotonicIncreasingFeaturelistId` <- `monotonicIncreasingFeaturelistId`
      self$`nClusters` <- `nClusters`
      self$`samplingMethod` <- `samplingMethod`
      self$`sourceProjectId` <- `sourceProjectId`
      self$`timeWindowSamplePct` <- `timeWindowSamplePct`
      self$`trainingDuration` <- `trainingDuration`
      self$`trainingRowCount` <- `trainingRowCount`
      self$`useProjectSettings` <- `useProjectSettings`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(blueprintId = self$`blueprintId`, featurelistId = self$`featurelistId`, monotonicDecreasingFeaturelistId = self$`monotonicDecreasingFeaturelistId`, monotonicIncreasingFeaturelistId = self$`monotonicIncreasingFeaturelistId`, nClusters = self$`nClusters`, samplingMethod = self$`samplingMethod`, sourceProjectId = self$`sourceProjectId`, timeWindowSamplePct = self$`timeWindowSamplePct`, trainingDuration = self$`trainingDuration`, trainingRowCount = self$`trainingRowCount`, useProjectSettings = self$`useProjectSettings`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blueprintId`)) {
          sprintf(
            '"blueprintId":
            "%s"
                  ',
            self$`blueprintId`
          )
        },
        if (!is.null(self$`featurelistId`)) {
          sprintf(
            '"featurelistId":
            "%s"
                  ',
            self$`featurelistId`
          )
        },
        if (!is.null(self$`monotonicDecreasingFeaturelistId`)) {
          sprintf(
            '"monotonicDecreasingFeaturelistId":
            "%s"
                  ',
            self$`monotonicDecreasingFeaturelistId`
          )
        },
        if (!is.null(self$`monotonicIncreasingFeaturelistId`)) {
          sprintf(
            '"monotonicIncreasingFeaturelistId":
            "%s"
                  ',
            self$`monotonicIncreasingFeaturelistId`
          )
        },
        if (!is.null(self$`nClusters`)) {
          sprintf(
            '"nClusters":
            %d
                  ',
            self$`nClusters`
          )
        },
        if (!is.null(self$`samplingMethod`)) {
          sprintf(
            '"samplingMethod":
            "%s"
                  ',
            self$`samplingMethod`
          )
        },
        if (!is.null(self$`sourceProjectId`)) {
          sprintf(
            '"sourceProjectId":
            "%s"
                  ',
            self$`sourceProjectId`
          )
        },
        if (!is.null(self$`timeWindowSamplePct`)) {
          sprintf(
            '"timeWindowSamplePct":
            %d
                  ',
            self$`timeWindowSamplePct`
          )
        },
        if (!is.null(self$`trainingDuration`)) {
          sprintf(
            '"trainingDuration":
            "%s"
                  ',
            self$`trainingDuration`
          )
        },
        if (!is.null(self$`trainingRowCount`)) {
          sprintf(
            '"trainingRowCount":
            %d
                  ',
            self$`trainingRowCount`
          )
        },
        if (!is.null(self$`useProjectSettings`)) {
          sprintf(
            '"useProjectSettings":
            %s
                  ',
            tolower(self$`useProjectSettings`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TrainDatetimeModelJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TrainDatetimeModelJson, validateParams = FALSE) {
      TrainDatetimeModelObject <- jsonlite::fromJSON(TrainDatetimeModelJson)
      self$`blueprintId` <- TrainDatetimeModelObject$`blueprintId`
      self$`featurelistId` <- TrainDatetimeModelObject$`featurelistId`
      self$`monotonicDecreasingFeaturelistId` <- TrainDatetimeModelObject$`monotonicDecreasingFeaturelistId`
      self$`monotonicIncreasingFeaturelistId` <- TrainDatetimeModelObject$`monotonicIncreasingFeaturelistId`
      self$`nClusters` <- TrainDatetimeModelObject$`nClusters`
      self$`samplingMethod` <- TrainDatetimeModelObject$`samplingMethod`
      self$`sourceProjectId` <- TrainDatetimeModelObject$`sourceProjectId`
      self$`timeWindowSamplePct` <- TrainDatetimeModelObject$`timeWindowSamplePct`
      self$`trainingDuration` <- TrainDatetimeModelObject$`trainingDuration`
      self$`trainingRowCount` <- TrainDatetimeModelObject$`trainingRowCount`
      self$`useProjectSettings` <- TrainDatetimeModelObject$`useProjectSettings`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
