# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotificationEmailChannelVerification
#'
#' @description NotificationEmailChannelVerification Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field channelType  character The type of the new notification channel
#'
#' @field emailAddress  character The email address of the recipient
#'
#' @field name  character The name of the new notification channel
#'
#' @field orgId  character [optional] The id of organization that notification channel belongs to
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotificationEmailChannelVerification <- R6::R6Class(
  "NotificationEmailChannelVerification",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(channelType = NULL, emailAddress = NULL, name = NULL, orgId = NULL) {
      if (!is.null(`channelType`)) {
        stopifnot(is.character(`channelType`), length(`channelType`) == 1)
      }
      if (!is.null(`emailAddress`)) {
        stopifnot(is.character(`emailAddress`), length(`emailAddress`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
    }
  ),
  public = list(
    `channelType` = NULL,
    `emailAddress` = NULL,
    `name` = NULL,
    `orgId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param channelType The type of the new notification channel
    #' @param emailAddress The email address of the recipient
    #' @param name The name of the new notification channel
    #' @param orgId The id of organization that notification channel belongs to
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`channelType` = NULL, `emailAddress` = NULL, `name` = NULL, `orgId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`channelType`, `emailAddress`, `name`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(channelType, emailAddress, name, orgId)
      }
      self$`channelType` <- `channelType`
      self$`emailAddress` <- `emailAddress`
      self$`name` <- `name`
      self$`orgId` <- `orgId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(channelType = self$`channelType`, emailAddress = self$`emailAddress`, name = self$`name`, orgId = self$`orgId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`channelType`)) {
          sprintf(
            '"channelType":
            "%s"
                  ',
            self$`channelType`
          )
        },
        if (!is.null(self$`emailAddress`)) {
          sprintf(
            '"emailAddress":
            "%s"
                  ',
            self$`emailAddress`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotificationEmailChannelVerificationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotificationEmailChannelVerificationJson, validateParams = FALSE) {
      NotificationEmailChannelVerificationObject <- jsonlite::fromJSON(NotificationEmailChannelVerificationJson)
      self$`channelType` <- NotificationEmailChannelVerificationObject$`channelType`
      self$`emailAddress` <- NotificationEmailChannelVerificationObject$`emailAddress`
      self$`name` <- NotificationEmailChannelVerificationObject$`name`
      self$`orgId` <- NotificationEmailChannelVerificationObject$`orgId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
