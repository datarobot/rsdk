# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ActivationMapsRetrieveResponse
#'
#' @description ActivationMapsRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field activationMapHeight  integer The height of each activation map (the number of rows in each activationValues matrix).
#'
#' @field activationMapWidth  integer The width of each activation map (the number of items in each row of each activationValues matrix).
#'
#' @field activationMaps  list( \link{ActivationMap} ) List of activation map objects
#'
#' @field targetBins  list( \link{TargetBin} ) List of bin objects for regression or null
#'
#' @field targetValues  list( character ) List of target values for classification or null
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ActivationMapsRetrieveResponse <- R6::R6Class(
  "ActivationMapsRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`activationMapHeight` = NULL, `activationMapWidth` = NULL, `activationMaps` = NULL, `targetBins` = NULL, `targetValues` = NULL) {
      if (!is.null(`activationMapHeight`)) {
        stopifnot(is.numeric(`activationMapHeight`), length(`activationMapHeight`) == 1)
      }
      if (!is.null(`activationMapWidth`)) {
        stopifnot(is.numeric(`activationMapWidth`), length(`activationMapWidth`) == 1)
      }
      if (!is.null(`activationMaps`)) {
        stopifnot(is.vector(`activationMaps`))
      }
      if (!is.null(`targetBins`)) {
        stopifnot(is.vector(`targetBins`))
      }
      if (!is.null(`targetValues`)) {
        stopifnot(is.vector(`targetValues`))
      }
    }
  ),
  public = list(
    `activationMapHeight` = NULL,
    `activationMapWidth` = NULL,
    `activationMaps` = NULL,
    `targetBins` = NULL,
    `targetValues` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param activationMapHeight The height of each activation map (the number of rows in each activationValues matrix).
    #' @param activationMapWidth The width of each activation map (the number of items in each row of each activationValues matrix).
    #' @param activationMaps List of activation map objects
    #' @param targetBins List of bin objects for regression or null
    #' @param targetValues List of target values for classification or null
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`activationMapHeight` = NULL, `activationMapWidth` = NULL, `activationMaps` = NULL, `targetBins` = NULL, `targetValues` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`activationMapHeight`, `activationMapWidth`, `activationMaps`, `targetBins`, `targetValues`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(activationMapHeight, activationMapWidth, activationMaps, targetBins, targetValues)
      }
      self$`activationMapHeight` <- `activationMapHeight`
      self$`activationMapWidth` <- `activationMapWidth`
      sapply(`activationMaps`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`targetBins`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`targetValues`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(activationMapHeight = self$`activationMapHeight`, activationMapWidth = self$`activationMapWidth`, activationMaps = self$`activationMaps`, targetBins = self$`targetBins`, targetValues = self$`targetValues`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`activationMapHeight`)) {
          sprintf(
            '"activationMapHeight":
            %d
                  ',
            self$`activationMapHeight`
          )
        },
        if (!is.null(self$`activationMapWidth`)) {
          sprintf(
            '"activationMapWidth":
            %d
                  ',
            self$`activationMapWidth`
          )
        },
        if (!is.null(self$`activationMaps`)) {
          sprintf(
            '"activationMaps":
            [%s]
      ',
            paste(sapply(self$`activationMaps`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`targetBins`)) {
          sprintf(
            '"targetBins":
            [%s]
      ',
            paste(sapply(self$`targetBins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`targetValues`)) {
          sprintf(
            '"targetValues":
            [%s]
                  ',
            paste(unlist(lapply(self$`targetValues`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ActivationMapsRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ActivationMapsRetrieveResponseJson, validateParams = FALSE) {
      ActivationMapsRetrieveResponseObject <- jsonlite::fromJSON(ActivationMapsRetrieveResponseJson)
      self$`activationMapHeight` <- ActivationMapsRetrieveResponseObject$`activationMapHeight`
      self$`activationMapWidth` <- ActivationMapsRetrieveResponseObject$`activationMapWidth`
      self$`activationMaps` <- ApiClient$new()$deserializeObj(ActivationMapsRetrieveResponseObject$`activationMaps`, "array[ActivationMap]", loadNamespace("datarobot.apicore"))
      self$`targetBins` <- ApiClient$new()$deserializeObj(ActivationMapsRetrieveResponseObject$`targetBins`, "array[TargetBin]", loadNamespace("datarobot.apicore"))
      self$`targetValues` <- ApiClient$new()$deserializeObj(ActivationMapsRetrieveResponseObject$`targetValues`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
