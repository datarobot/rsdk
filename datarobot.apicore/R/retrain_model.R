# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title RetrainModel
#'
#' @description RetrainModel Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featurelistId  character [optional] If specified, the model will be trained using that featurelist, otherwise the model will be trained on the same feature list as before.
#'
#' @field modelId  character The model to be retrained
#'
#' @field monotonicDecreasingFeaturelistId  character [optional] The ID of the featurelist that defines the set of features with a monotonically decreasing relationship to the target. If null, no such constraints are enforced.
#'
#' @field monotonicIncreasingFeaturelistId  character [optional] The ID of the featurelist that defines the set of features with a monotonically increasing relationship to the target. If null, no such constraints are enforced.
#'
#' @field nClusters  integer [optional] The number of clusters to use in the specified unsupervised clustering model. Only valid in unsupervised clustering projects.
#'
#' @field samplePct  numeric [optional] The percentage of the dataset to use to use to train the model. The specified percentage should be between 0 and 100. If not specified, original model sample percent will be used.
#'
#' @field scoringType  character [optional] Validation is available for any partitioning. If the project uses cross validation, &#x60;crossValidation&#x60; may be used to indicate that all available training/validation combinations should be used.
#'
#' @field trainingRowCount  integer [optional] The number of rows to use to train the model. If not specified, original model training row count will be used.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RetrainModel <- R6::R6Class(
  "RetrainModel",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(featurelistId = NULL, modelId = NULL, monotonicDecreasingFeaturelistId = NULL, monotonicIncreasingFeaturelistId = NULL, nClusters = NULL, samplePct = NULL, scoringType = NULL, trainingRowCount = NULL) {
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`featurelistId`)) {
        stopifnot(is.character(`featurelistId`), length(`featurelistId`) == 1)
      }
      if (!is.null(`monotonicDecreasingFeaturelistId`)) {
        stopifnot(is.character(`monotonicDecreasingFeaturelistId`), length(`monotonicDecreasingFeaturelistId`) == 1)
      }
      if (!is.null(`monotonicIncreasingFeaturelistId`)) {
        stopifnot(is.character(`monotonicIncreasingFeaturelistId`), length(`monotonicIncreasingFeaturelistId`) == 1)
      }
      if (!is.null(`nClusters`)) {
        stopifnot(is.numeric(`nClusters`), length(`nClusters`) == 1)
      }
      if (!is.null(`samplePct`)) {
        stopifnot(is.numeric(`samplePct`), length(`samplePct`) == 1)
      }
      if (!is.null(`scoringType`)) {
        stopifnot(is.character(`scoringType`), length(`scoringType`) == 1)
      }
      if (!is.null(`trainingRowCount`)) {
        stopifnot(is.numeric(`trainingRowCount`), length(`trainingRowCount`) == 1)
      }
    }
  ),
  public = list(
    `featurelistId` = NULL,
    `modelId` = NULL,
    `monotonicDecreasingFeaturelistId` = NULL,
    `monotonicIncreasingFeaturelistId` = NULL,
    `nClusters` = NULL,
    `samplePct` = NULL,
    `scoringType` = NULL,
    `trainingRowCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featurelistId If specified, the model will be trained using that featurelist, otherwise the model will be trained on the same feature list as before.
    #' @param modelId The model to be retrained
    #' @param monotonicDecreasingFeaturelistId The ID of the featurelist that defines the set of features with a monotonically decreasing relationship to the target. If null, no such constraints are enforced.
    #' @param monotonicIncreasingFeaturelistId The ID of the featurelist that defines the set of features with a monotonically increasing relationship to the target. If null, no such constraints are enforced.
    #' @param nClusters The number of clusters to use in the specified unsupervised clustering model. Only valid in unsupervised clustering projects.
    #' @param samplePct The percentage of the dataset to use to use to train the model. The specified percentage should be between 0 and 100. If not specified, original model sample percent will be used.
    #' @param scoringType Validation is available for any partitioning. If the project uses cross validation, &#x60;crossValidation&#x60; may be used to indicate that all available training/validation combinations should be used.
    #' @param trainingRowCount The number of rows to use to train the model. If not specified, original model training row count will be used.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`modelId` = NULL, `featurelistId` = NULL, `monotonicDecreasingFeaturelistId` = NULL, `monotonicIncreasingFeaturelistId` = NULL, `nClusters` = NULL, `samplePct` = NULL, `scoringType` = NULL, `trainingRowCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`modelId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(featurelistId, modelId, monotonicDecreasingFeaturelistId, monotonicIncreasingFeaturelistId, nClusters, samplePct, scoringType, trainingRowCount)
      }
      self$`featurelistId` <- `featurelistId`
      self$`modelId` <- `modelId`
      self$`monotonicDecreasingFeaturelistId` <- `monotonicDecreasingFeaturelistId`
      self$`monotonicIncreasingFeaturelistId` <- `monotonicIncreasingFeaturelistId`
      self$`nClusters` <- `nClusters`
      self$`samplePct` <- `samplePct`
      self$`scoringType` <- `scoringType`
      self$`trainingRowCount` <- `trainingRowCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(featurelistId = self$`featurelistId`, modelId = self$`modelId`, monotonicDecreasingFeaturelistId = self$`monotonicDecreasingFeaturelistId`, monotonicIncreasingFeaturelistId = self$`monotonicIncreasingFeaturelistId`, nClusters = self$`nClusters`, samplePct = self$`samplePct`, scoringType = self$`scoringType`, trainingRowCount = self$`trainingRowCount`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featurelistId`)) {
          sprintf(
            '"featurelistId":
            "%s"
                  ',
            self$`featurelistId`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`monotonicDecreasingFeaturelistId`)) {
          sprintf(
            '"monotonicDecreasingFeaturelistId":
            "%s"
                  ',
            self$`monotonicDecreasingFeaturelistId`
          )
        },
        if (!is.null(self$`monotonicIncreasingFeaturelistId`)) {
          sprintf(
            '"monotonicIncreasingFeaturelistId":
            "%s"
                  ',
            self$`monotonicIncreasingFeaturelistId`
          )
        },
        if (!is.null(self$`nClusters`)) {
          sprintf(
            '"nClusters":
            %d
                  ',
            self$`nClusters`
          )
        },
        if (!is.null(self$`samplePct`)) {
          sprintf(
            '"samplePct":
            %d
                  ',
            self$`samplePct`
          )
        },
        if (!is.null(self$`scoringType`)) {
          sprintf(
            '"scoringType":
            "%s"
                  ',
            self$`scoringType`
          )
        },
        if (!is.null(self$`trainingRowCount`)) {
          sprintf(
            '"trainingRowCount":
            %d
                  ',
            self$`trainingRowCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param RetrainModelJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(RetrainModelJson, validateParams = FALSE) {
      RetrainModelObject <- jsonlite::fromJSON(RetrainModelJson)
      self$`featurelistId` <- RetrainModelObject$`featurelistId`
      self$`modelId` <- RetrainModelObject$`modelId`
      self$`monotonicDecreasingFeaturelistId` <- RetrainModelObject$`monotonicDecreasingFeaturelistId`
      self$`monotonicIncreasingFeaturelistId` <- RetrainModelObject$`monotonicIncreasingFeaturelistId`
      self$`nClusters` <- RetrainModelObject$`nClusters`
      self$`samplePct` <- RetrainModelObject$`samplePct`
      self$`scoringType` <- RetrainModelObject$`scoringType`
      self$`trainingRowCount` <- RetrainModelObject$`trainingRowCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
