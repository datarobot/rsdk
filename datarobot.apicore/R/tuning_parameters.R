# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TuningParameters
#'
#' @description TuningParameters Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field constraints  \link{Constraints}
#'
#' @field currentValue  \link{OneOfAnyTypearray} Value of this parameter used by the current model.
#'
#' @field defaultValue  \link{OneOfAnyTypearray} Default value of this parameter.
#'
#' @field parameterId  character Unique (per-blueprint) identifier of this parameter. This is the identifier used to specify which parameter to tune when make a new advanced tuning request.
#'
#' @field parameterName  character Name of the parameter.
#'
#' @field taskName  character Human-readable name of the task that this parameter belongs to.
#'
#' @field vertexId  character Id of the vertex this parameter belongs to.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TuningParameters <- R6::R6Class(
  "TuningParameters",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`constraints` = NULL, `currentValue` = NULL, `defaultValue` = NULL, `parameterId` = NULL, `parameterName` = NULL, `taskName` = NULL, `vertexId` = NULL) {
      if (!is.null(`constraints`)) {
        stopifnot(R6::is.R6(`constraints`))
      }
      if (!is.null(`currentValue`)) {
        .setMixedProperty(typeList = list("AnyType", "array"), propertyData = currentValue)
      }
      if (!is.null(`defaultValue`)) {
        .setMixedProperty(typeList = list("AnyType", "array"), propertyData = defaultValue)
      }
      if (!is.null(`parameterId`)) {
        stopifnot(is.character(`parameterId`), length(`parameterId`) == 1)
      }
      if (!is.null(`parameterName`)) {
        stopifnot(is.character(`parameterName`), length(`parameterName`) == 1)
      }
      if (!is.null(`taskName`)) {
        stopifnot(is.character(`taskName`), length(`taskName`) == 1)
      }
      if (!is.null(`vertexId`)) {
        stopifnot(is.character(`vertexId`), length(`vertexId`) == 1)
      }
    }
  ),
  public = list(
    `constraints` = NULL,
    `currentValue` = NULL,
    `defaultValue` = NULL,
    `parameterId` = NULL,
    `parameterName` = NULL,
    `taskName` = NULL,
    `vertexId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param constraints
    #' @param currentValue Value of this parameter used by the current model.
    #' @param defaultValue Default value of this parameter.
    #' @param parameterId Unique (per-blueprint) identifier of this parameter. This is the identifier used to specify which parameter to tune when make a new advanced tuning request.
    #' @param parameterName Name of the parameter.
    #' @param taskName Human-readable name of the task that this parameter belongs to.
    #' @param vertexId Id of the vertex this parameter belongs to.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`constraints` = NULL, `currentValue` = NULL, `defaultValue` = NULL, `parameterId` = NULL, `parameterName` = NULL, `taskName` = NULL, `vertexId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`constraints`, `currentValue`, `defaultValue`, `parameterId`, `parameterName`, `taskName`, `vertexId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(constraints, currentValue, defaultValue, parameterId, parameterName, taskName, vertexId)
      }
      self$`constraints` <- `constraints`
      self$`currentValue` <- .setMixedProperty(typeList = list("AnyType", "array"), propertyData = currentValue)
      self$`defaultValue` <- .setMixedProperty(typeList = list("AnyType", "array"), propertyData = defaultValue)
      self$`parameterId` <- `parameterId`
      self$`parameterName` <- `parameterName`
      self$`taskName` <- `taskName`
      self$`vertexId` <- `vertexId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(constraints = self$`constraints`, currentValue = self$`currentValue`, defaultValue = self$`defaultValue`, parameterId = self$`parameterId`, parameterName = self$`parameterName`, taskName = self$`taskName`, vertexId = self$`vertexId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`constraints`)) {
          sprintf(
            '"constraints":
            %s
      ',
            jsonlite::toJSON(self$`constraints`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`currentValue`)) {
          sprintf(
            '"currentValue":
            %s
      ',
          )
        },
        if (!is.null(self$`defaultValue`)) {
          sprintf(
            '"defaultValue":
            %s
      ',
          )
        },
        if (!is.null(self$`parameterId`)) {
          sprintf(
            '"parameterId":
            "%s"
                  ',
            self$`parameterId`
          )
        },
        if (!is.null(self$`parameterName`)) {
          sprintf(
            '"parameterName":
            "%s"
                  ',
            self$`parameterName`
          )
        },
        if (!is.null(self$`taskName`)) {
          sprintf(
            '"taskName":
            "%s"
                  ',
            self$`taskName`
          )
        },
        if (!is.null(self$`vertexId`)) {
          sprintf(
            '"vertexId":
            "%s"
                  ',
            self$`vertexId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TuningParametersJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TuningParametersJson, validateParams = FALSE) {
      TuningParametersObject <- jsonlite::fromJSON(TuningParametersJson)
      self$`constraints` <- Constraints$new()$fromJSON(jsonlite::toJSON(TuningParametersObject$constraints, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`currentValue` <- .setMixedProperty(typeList = list("AnyType", "array"), propertyData = TuningParametersObject$currentValue)
      self$`defaultValue` <- .setMixedProperty(typeList = list("AnyType", "array"), propertyData = TuningParametersObject$defaultValue)
      self$`parameterId` <- TuningParametersObject$`parameterId`
      self$`parameterName` <- TuningParametersObject$`parameterName`
      self$`taskName` <- TuningParametersObject$`taskName`
      self$`vertexId` <- TuningParametersObject$`vertexId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
