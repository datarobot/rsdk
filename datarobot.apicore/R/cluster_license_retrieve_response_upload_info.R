# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ClusterLicenseRetrieveResponseUploadInfo
#'
#' @description ClusterLicenseRetrieveResponseUploadInfo Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field uploadTimestamp  character the time when the current license was uploaded in UTC ISO format, ex. &#39;2020-12-21T23:59:59.000000Z&#39;
#'
#' @field uploaderUserId  character Id of the user who uploaded the current license
#'
#' @field uploaderUsername  character The username of the user who uploaded the current license
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ClusterLicenseRetrieveResponseUploadInfo <- R6::R6Class(
  "ClusterLicenseRetrieveResponseUploadInfo",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`uploadTimestamp` = NULL, `uploaderUserId` = NULL, `uploaderUsername` = NULL) {
      if (!is.null(`uploadTimestamp`)) {
        stopifnot(is.character(`uploadTimestamp`), length(`uploadTimestamp`) == 1)
      }
      if (!is.null(`uploaderUserId`)) {
        stopifnot(is.character(`uploaderUserId`), length(`uploaderUserId`) == 1)
      }
      if (!is.null(`uploaderUsername`)) {
        stopifnot(is.character(`uploaderUsername`), length(`uploaderUsername`) == 1)
      }
    }
  ),
  public = list(
    `uploadTimestamp` = NULL,
    `uploaderUserId` = NULL,
    `uploaderUsername` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param uploadTimestamp the time when the current license was uploaded in UTC ISO format, ex. &#39;2020-12-21T23:59:59.000000Z&#39;
    #' @param uploaderUserId Id of the user who uploaded the current license
    #' @param uploaderUsername The username of the user who uploaded the current license
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`uploadTimestamp` = NULL, `uploaderUserId` = NULL, `uploaderUsername` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`uploadTimestamp`, `uploaderUserId`, `uploaderUsername`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(uploadTimestamp, uploaderUserId, uploaderUsername)
      }
      self$`uploadTimestamp` <- `uploadTimestamp`
      self$`uploaderUserId` <- `uploaderUserId`
      self$`uploaderUsername` <- `uploaderUsername`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(uploadTimestamp = self$`uploadTimestamp`, uploaderUserId = self$`uploaderUserId`, uploaderUsername = self$`uploaderUsername`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`uploadTimestamp`)) {
          sprintf(
            '"uploadTimestamp":
            "%s"
                  ',
            self$`uploadTimestamp`
          )
        },
        if (!is.null(self$`uploaderUserId`)) {
          sprintf(
            '"uploaderUserId":
            "%s"
                  ',
            self$`uploaderUserId`
          )
        },
        if (!is.null(self$`uploaderUsername`)) {
          sprintf(
            '"uploaderUsername":
            "%s"
                  ',
            self$`uploaderUsername`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ClusterLicenseRetrieveResponseUploadInfoJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ClusterLicenseRetrieveResponseUploadInfoJson, validateParams = FALSE) {
      ClusterLicenseRetrieveResponseUploadInfoObject <- jsonlite::fromJSON(ClusterLicenseRetrieveResponseUploadInfoJson)
      self$`uploadTimestamp` <- ClusterLicenseRetrieveResponseUploadInfoObject$`uploadTimestamp`
      self$`uploaderUserId` <- ClusterLicenseRetrieveResponseUploadInfoObject$`uploaderUserId`
      self$`uploaderUsername` <- ClusterLicenseRetrieveResponseUploadInfoObject$`uploaderUsername`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
