# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApplicationCreate
#'
#' @description ApplicationCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field applicationTypeId  character [optional] The ID of the of application to be created.
#'
#' @field authenticationType  character [optional] Authentication type
#'
#' @field cloudProvider  character [optional] The optional cloud provider
#'
#' @field description  character [optional] The description of the application
#'
#' @field modelDeploymentId  character [optional] The ID of the model deployment. The deployed application will use this deployment to make predictions.
#'
#' @field name  character [optional] The name of the app
#'
#' @field purpose  character [optional] An optional field to describe the purpose of the application.
#'
#' @field sources  list( \link{ApplicationCreateSources} ) [optional] The sources for this application
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApplicationCreate <- R6::R6Class(
  "ApplicationCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`applicationTypeId` = NULL, `authenticationType` = NULL, `cloudProvider` = NULL, `description` = NULL, `modelDeploymentId` = NULL, `name` = NULL, `purpose` = NULL, `sources` = NULL) {
      if (!is.null(`applicationTypeId`)) {
        stopifnot(is.character(`applicationTypeId`), length(`applicationTypeId`) == 1)
      }
      if (!is.null(`authenticationType`)) {
        stopifnot(is.character(`authenticationType`), length(`authenticationType`) == 1)
      }
      if (!is.null(`cloudProvider`)) {
        stopifnot(is.character(`cloudProvider`), length(`cloudProvider`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`modelDeploymentId`)) {
        stopifnot(is.character(`modelDeploymentId`), length(`modelDeploymentId`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`purpose`)) {
        stopifnot(is.character(`purpose`), length(`purpose`) == 1)
      }
      if (!is.null(`sources`) && length(`sources`) > 0) {
        stopifnot(is.vector(`sources`), sapply(`sources`, R6::is.R6))
      }
    }
  ),
  public = list(
    `applicationTypeId` = NULL,
    `authenticationType` = NULL,
    `cloudProvider` = NULL,
    `description` = NULL,
    `modelDeploymentId` = NULL,
    `name` = NULL,
    `purpose` = NULL,
    `sources` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param applicationTypeId The ID of the of application to be created.
    #' @param authenticationType Authentication type
    #' @param cloudProvider The optional cloud provider
    #' @param description The description of the application
    #' @param modelDeploymentId The ID of the model deployment. The deployed application will use this deployment to make predictions.
    #' @param name The name of the app
    #' @param purpose An optional field to describe the purpose of the application.
    #' @param sources The sources for this application
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`applicationTypeId` = NULL, `authenticationType` = "invitedUsersOnly", `cloudProvider` = "drcloud", `description` = NULL, `modelDeploymentId` = NULL, `name` = NULL, `purpose` = NULL, `sources` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(applicationTypeId, authenticationType, cloudProvider, description, modelDeploymentId, name, purpose, sources)
      }
      self$`applicationTypeId` <- `applicationTypeId`
      self$`authenticationType` <- `authenticationType`
      self$`cloudProvider` <- `cloudProvider`
      self$`description` <- `description`
      self$`modelDeploymentId` <- `modelDeploymentId`
      self$`name` <- `name`
      self$`purpose` <- `purpose`
      self$`sources` <- `sources`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(applicationTypeId = self$`applicationTypeId`, authenticationType = self$`authenticationType`, cloudProvider = self$`cloudProvider`, description = self$`description`, modelDeploymentId = self$`modelDeploymentId`, name = self$`name`, purpose = self$`purpose`, sources = self$`sources`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`applicationTypeId`)) {
          sprintf(
            '"applicationTypeId":
            "%s"
                  ',
            self$`applicationTypeId`
          )
        },
        if (!is.null(self$`authenticationType`)) {
          sprintf(
            '"authenticationType":
            "%s"
                  ',
            self$`authenticationType`
          )
        },
        if (!is.null(self$`cloudProvider`)) {
          sprintf(
            '"cloudProvider":
            "%s"
                  ',
            self$`cloudProvider`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`modelDeploymentId`)) {
          sprintf(
            '"modelDeploymentId":
            "%s"
                  ',
            self$`modelDeploymentId`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`purpose`)) {
          sprintf(
            '"purpose":
            "%s"
                  ',
            self$`purpose`
          )
        },
        if (!is.null(self$`sources`)) {
          sprintf(
            '"sources":
            [%s]
      ',
            paste(sapply(self$`sources`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApplicationCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApplicationCreateJson, validateParams = FALSE) {
      ApplicationCreateObject <- jsonlite::fromJSON(ApplicationCreateJson)
      self$`applicationTypeId` <- ApplicationCreateObject$`applicationTypeId`
      self$`authenticationType` <- ApplicationCreateObject$`authenticationType`
      self$`cloudProvider` <- ApplicationCreateObject$`cloudProvider`
      self$`description` <- ApplicationCreateObject$`description`
      self$`modelDeploymentId` <- ApplicationCreateObject$`modelDeploymentId`
      self$`name` <- ApplicationCreateObject$`name`
      self$`purpose` <- ApplicationCreateObject$`purpose`
      self$`sources` <- ApiClient$new()$deserializeObj(ApplicationCreateObject$`sources`, "array[ApplicationCreateSources]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
