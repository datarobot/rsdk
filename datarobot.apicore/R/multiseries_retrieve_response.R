# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MultiseriesRetrieveResponse
#'
#' @description MultiseriesRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datetimePartitionColumn  character The datetime partition column name.
#'
#' @field detectedMultiseriesIdColumns  list( \link{MultiseriesIdColumnsRecord} ) A list of detected multiseries ID columns along with timeStep and timeUnit information. Note that if no eligible columns have been detected, this list will be empty.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MultiseriesRetrieveResponse <- R6::R6Class(
  "MultiseriesRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datetimePartitionColumn = NULL, detectedMultiseriesIdColumns = NULL) {
      if (!is.null(`datetimePartitionColumn`)) {
        stopifnot(is.character(`datetimePartitionColumn`), length(`datetimePartitionColumn`) == 1)
      }
      if (!is.null(`detectedMultiseriesIdColumns`)) {
        stopifnot(is.vector(`detectedMultiseriesIdColumns`))
      }
    }
  ),
  public = list(
    `datetimePartitionColumn` = NULL,
    `detectedMultiseriesIdColumns` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datetimePartitionColumn The datetime partition column name.
    #' @param detectedMultiseriesIdColumns A list of detected multiseries ID columns along with timeStep and timeUnit information. Note that if no eligible columns have been detected, this list will be empty.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datetimePartitionColumn` = NULL, `detectedMultiseriesIdColumns` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datetimePartitionColumn`, `detectedMultiseriesIdColumns`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datetimePartitionColumn, detectedMultiseriesIdColumns)
      }
      self$`datetimePartitionColumn` <- `datetimePartitionColumn`
      sapply(`detectedMultiseriesIdColumns`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datetimePartitionColumn = self$`datetimePartitionColumn`, detectedMultiseriesIdColumns = self$`detectedMultiseriesIdColumns`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datetimePartitionColumn`)) {
          sprintf(
            '"datetimePartitionColumn":
            "%s"
                  ',
            self$`datetimePartitionColumn`
          )
        },
        if (!is.null(self$`detectedMultiseriesIdColumns`)) {
          sprintf(
            '"detectedMultiseriesIdColumns":
            [%s]
',
            paste(sapply(self$`detectedMultiseriesIdColumns`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MultiseriesRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MultiseriesRetrieveResponseJson, validateParams = FALSE) {
      MultiseriesRetrieveResponseObject <- jsonlite::fromJSON(MultiseriesRetrieveResponseJson)
      self$`datetimePartitionColumn` <- MultiseriesRetrieveResponseObject$`datetimePartitionColumn`
      self$`detectedMultiseriesIdColumns` <- ApiClient$new()$deserializeObj(MultiseriesRetrieveResponseObject$`detectedMultiseriesIdColumns`, "array[MultiseriesIdColumnsRecord]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
