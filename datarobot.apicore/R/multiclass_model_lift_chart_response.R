# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MulticlassModelLiftChartResponse
#'
#' @description MulticlassModelLiftChartResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field classBins  list( \link{MulticlassLiftBinResponse} ) List of lift chart data for each target class.
#'
#' @field source  character Source of the data
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MulticlassModelLiftChartResponse <- R6::R6Class(
  "MulticlassModelLiftChartResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`classBins` = NULL, `source` = NULL) {
      if (!is.null(`classBins`)) {
        stopifnot(is.vector(`classBins`))
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
    }
  ),
  public = list(
    `classBins` = NULL,
    `source` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param classBins List of lift chart data for each target class.
    #' @param source Source of the data
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`classBins` = NULL, `source` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`classBins`, `source`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(classBins, source)
      }
      sapply(`classBins`, function(x) stopifnot(R6::is.R6(x)))
      self$`source` <- `source`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(classBins = self$`classBins`, source = self$`source`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`classBins`)) {
          sprintf(
            '"classBins":
            [%s]
      ',
            paste(sapply(self$`classBins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MulticlassModelLiftChartResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MulticlassModelLiftChartResponseJson, validateParams = FALSE) {
      MulticlassModelLiftChartResponseObject <- jsonlite::fromJSON(MulticlassModelLiftChartResponseJson)
      self$`classBins` <- ApiClient$new()$deserializeObj(MulticlassModelLiftChartResponseObject$`classBins`, "array[MulticlassLiftBinResponse]", loadNamespace("datarobot.apicore"))
      self$`source` <- MulticlassModelLiftChartResponseObject$`source`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
