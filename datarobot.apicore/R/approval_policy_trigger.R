# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApprovalPolicyTrigger
#'
#' @description ApprovalPolicyTrigger Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field entityType  character Type of entity to trigger on.
#'
#' @field filterGroups  list( \link{PolicyUserGroup} ) [optional] A list of user groups to apply Approval Policy for. If User &#39;A&#39; and User &#39;B&#39; are both members of the same organisation, and User &#39;A&#39; is a member of one of the groups listed in this field,but User &#39;B&#39; is not, then an approval workflow will be triggerred for User &#39;A&#39; on an action to the entity that matches policy condition, but not for User &#39;B&#39;. If &#x60;&#x60;null&#x60;&#x60;, approvals workflow will be triggered for all users.
#'
#' @field intendedAction  \link{ApprovalPolicyIntendedAction}
#'
#' @field labels  \link{ApprovalPolicyTriggerLabel} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApprovalPolicyTrigger <- R6::R6Class(
  "ApprovalPolicyTrigger",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`entityType` = NULL, `filterGroups` = NULL, `intendedAction` = NULL, `labels` = NULL) {
      if (!is.null(`entityType`)) {
        stopifnot(is.character(`entityType`), length(`entityType`) == 1)
      }
      if (!is.null(`intendedAction`)) {
        stopifnot(R6::is.R6(`intendedAction`))
      }
      if (!is.null(`filterGroups`) && length(`filterGroups`) > 0) {
        stopifnot(is.vector(`filterGroups`), sapply(`filterGroups`, R6::is.R6))
      }
      if (!is.null(`labels`)) {
        stopifnot(R6::is.R6(`labels`))
      }
    }
  ),
  public = list(
    `entityType` = NULL,
    `filterGroups` = NULL,
    `intendedAction` = NULL,
    `labels` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param entityType Type of entity to trigger on.
    #' @param filterGroups A list of user groups to apply Approval Policy for. If User &#39;A&#39; and User &#39;B&#39; are both members of the same organisation, and User &#39;A&#39; is a member of one of the groups listed in this field,but User &#39;B&#39; is not, then an approval workflow will be triggerred for User &#39;A&#39; on an action to the entity that matches policy condition, but not for User &#39;B&#39;. If &#x60;&#x60;null&#x60;&#x60;, approvals workflow will be triggered for all users.
    #' @param intendedAction
    #' @param labels
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`entityType` = NULL, `intendedAction` = NULL, `filterGroups` = NULL, `labels` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`entityType`, `intendedAction`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(entityType, filterGroups, intendedAction, labels)
      }
      self$`entityType` <- `entityType`
      self$`filterGroups` <- `filterGroups`
      self$`intendedAction` <- `intendedAction`
      self$`labels` <- `labels`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(entityType = self$`entityType`, filterGroups = self$`filterGroups`, intendedAction = self$`intendedAction`, labels = self$`labels`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`entityType`)) {
          sprintf(
            '"entityType":
            "%s"
                  ',
            self$`entityType`
          )
        },
        if (!is.null(self$`filterGroups`)) {
          sprintf(
            '"filterGroups":
            [%s]
      ',
            paste(sapply(self$`filterGroups`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`intendedAction`)) {
          sprintf(
            '"intendedAction":
            %s
      ',
            jsonlite::toJSON(self$`intendedAction`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`labels`)) {
          sprintf(
            '"labels":
            %s
      ',
            jsonlite::toJSON(self$`labels`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApprovalPolicyTriggerJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApprovalPolicyTriggerJson, validateParams = FALSE) {
      ApprovalPolicyTriggerObject <- jsonlite::fromJSON(ApprovalPolicyTriggerJson)
      self$`entityType` <- ApprovalPolicyTriggerObject$`entityType`
      self$`filterGroups` <- ApiClient$new()$deserializeObj(ApprovalPolicyTriggerObject$`filterGroups`, "array[PolicyUserGroup]", loadNamespace("datarobot.apicore"))
      self$`intendedAction` <- ApprovalPolicyIntendedAction$new()$fromJSON(jsonlite::toJSON(ApprovalPolicyTriggerObject$intendedAction, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`labels` <- ApprovalPolicyTriggerLabel$new()$fromJSON(jsonlite::toJSON(ApprovalPolicyTriggerObject$labels, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
