# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AzureOutput
#'
#' @description AzureOutput Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentialId  character [optional] Use the specified credential to access the url
#'
#' @field format  character [optional] Type of output file format
#'
#' @field partitionColumns  list( character ) [optional] For Parquet directory-scoring only. The column names of the intake data of which to partition the dataset. Columns are partitioned in the order they are given. At least one value is required if scoring to a directory (meaning the output url ends with a slash (\&quot;/\&quot;).
#'
#' @field type  character Type name for this output type
#'
#' @field url  character URL for the file or directory
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AzureOutput <- R6::R6Class(
  "AzureOutput",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`credentialId` = NULL, `format` = NULL, `partitionColumns` = NULL, `type` = NULL, `url` = NULL) {
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`format`)) {
        stopifnot(is.character(`format`), length(`format`) == 1)
      }
      if (!is.null(`partitionColumns`)) {
        stopifnot(is.vector(`partitionColumns`))
      }
    }
  ),
  public = list(
    `credentialId` = NULL,
    `format` = NULL,
    `partitionColumns` = NULL,
    `type` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentialId Use the specified credential to access the url
    #' @param format Type of output file format
    #' @param partitionColumns For Parquet directory-scoring only. The column names of the intake data of which to partition the dataset. Columns are partitioned in the order they are given. At least one value is required if scoring to a directory (meaning the output url ends with a slash (\&quot;/\&quot;).
    #' @param type Type name for this output type
    #' @param url URL for the file or directory
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`type` = NULL, `url` = NULL, `credentialId` = NULL, `format` = "csv", `partitionColumns` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`type`, `url`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(credentialId, format, partitionColumns, type, url)
      }
      self$`credentialId` <- `credentialId`
      self$`format` <- `format`
      sapply(`partitionColumns`, function(x) stopifnot(is.character(x)))
      self$`type` <- `type`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(credentialId = self$`credentialId`, format = self$`format`, partitionColumns = self$`partitionColumns`, type = self$`type`, url = self$`url`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`format`)) {
          sprintf(
            '"format":
            "%s"
                  ',
            self$`format`
          )
        },
        if (!is.null(self$`partitionColumns`)) {
          sprintf(
            '"partitionColumns":
            [%s]
                  ',
            paste(unlist(lapply(self$`partitionColumns`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AzureOutputJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AzureOutputJson, validateParams = FALSE) {
      AzureOutputObject <- jsonlite::fromJSON(AzureOutputJson)
      self$`credentialId` <- AzureOutputObject$`credentialId`
      self$`format` <- AzureOutputObject$`format`
      self$`partitionColumns` <- ApiClient$new()$deserializeObj(AzureOutputObject$`partitionColumns`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`type` <- AzureOutputObject$`type`
      self$`url` <- AzureOutputObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
