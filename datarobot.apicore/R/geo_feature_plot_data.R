# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title GeoFeaturePlotData
#'
#' @description GeoFeaturePlotData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field aggregation  character Type of geo aggregation.
#'
#' @field bbox  object Bounding box of feature map.
#'
#' @field features  list( \link{GeoFeaturePlotFeature} ) Location features over map
#'
#' @field summary  \link{GeoFeaturePlotSummary}
#'
#' @field type  character GeoJSON FeatureCollection.
#'
#' @field valueAggregation  character Type of feature aggregation.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GeoFeaturePlotData <- R6::R6Class(
  "GeoFeaturePlotData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`aggregation` = NULL, `bbox` = NULL, `features` = NULL, `summary` = NULL, `type` = NULL, `valueAggregation` = NULL) {
      if (!is.null(`aggregation`)) {
        stopifnot(is.character(`aggregation`), length(`aggregation`) == 1)
      }
      if (!is.null(`bbox`)) {
      }
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`))
      }
      if (!is.null(`summary`)) {
        stopifnot(R6::is.R6(`summary`))
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`valueAggregation`)) {
        stopifnot(is.character(`valueAggregation`), length(`valueAggregation`) == 1)
      }
    }
  ),
  public = list(
    `aggregation` = NULL,
    `bbox` = NULL,
    `features` = NULL,
    `summary` = NULL,
    `type` = NULL,
    `valueAggregation` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param aggregation Type of geo aggregation.
    #' @param bbox Bounding box of feature map.
    #' @param features Location features over map
    #' @param summary
    #' @param type GeoJSON FeatureCollection.
    #' @param valueAggregation Type of feature aggregation.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`aggregation` = NULL, `bbox` = NULL, `features` = NULL, `summary` = NULL, `type` = NULL, `valueAggregation` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`aggregation`, `bbox`, `features`, `summary`, `type`, `valueAggregation`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(aggregation, bbox, features, summary, type, valueAggregation)
      }
      self$`aggregation` <- `aggregation`
      self$`bbox` <- `bbox`
      sapply(`features`, function(x) stopifnot(R6::is.R6(x)))
      self$`summary` <- `summary`
      self$`type` <- `type`
      self$`valueAggregation` <- `valueAggregation`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(aggregation = self$`aggregation`, bbox = self$`bbox`, features = self$`features`, summary = self$`summary`, type = self$`type`, valueAggregation = self$`valueAggregation`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`aggregation`)) {
          sprintf(
            '"aggregation":
            "%s"
                  ',
            self$`aggregation`
          )
        },
        if (!is.null(self$`bbox`)) {
          sprintf(
            '"bbox":
            "%s"
                  ',
            self$`bbox`
          )
        },
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
      ',
            paste(sapply(self$`features`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`summary`)) {
          sprintf(
            '"summary":
            %s
      ',
            jsonlite::toJSON(self$`summary`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        },
        if (!is.null(self$`valueAggregation`)) {
          sprintf(
            '"valueAggregation":
            "%s"
                  ',
            self$`valueAggregation`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param GeoFeaturePlotDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(GeoFeaturePlotDataJson, validateParams = FALSE) {
      GeoFeaturePlotDataObject <- jsonlite::fromJSON(GeoFeaturePlotDataJson)
      self$`aggregation` <- GeoFeaturePlotDataObject$`aggregation`
      self$`bbox` <- GeoFeaturePlotDataObject$`bbox`
      self$`features` <- ApiClient$new()$deserializeObj(GeoFeaturePlotDataObject$`features`, "array[GeoFeaturePlotFeature]", loadNamespace("datarobot.apicore"))
      self$`summary` <- GeoFeaturePlotSummary$new()$fromJSON(jsonlite::toJSON(GeoFeaturePlotDataObject$summary, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`type` <- GeoFeaturePlotDataObject$`type`
      self$`valueAggregation` <- GeoFeaturePlotDataObject$`valueAggregation`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
