# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BigQueryIntake
#'
#' @description BigQueryIntake Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field bucket  character The name of gcs bucket for data export
#'
#' @field credentialId  character [optional] The ID of the GCP credentials
#'
#' @field dataset  character The name of the specified big query dataset to read input data from
#'
#' @field table  character The name of the specified big query table to read input data from
#'
#' @field type  character Type name for this intake type
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BigQueryIntake <- R6::R6Class(
  "BigQueryIntake",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(bucket = NULL, credentialId = NULL, dataset = NULL, table = NULL, type = NULL) {
      if (!is.null(`bucket`)) {
        stopifnot(is.character(`bucket`), length(`bucket`) == 1)
      }
      if (!is.null(`dataset`)) {
        stopifnot(is.character(`dataset`), length(`dataset`) == 1)
      }
      if (!is.null(`table`)) {
        stopifnot(is.character(`table`), length(`table`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
    }
  ),
  public = list(
    `bucket` = NULL,
    `credentialId` = NULL,
    `dataset` = NULL,
    `table` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param bucket The name of gcs bucket for data export
    #' @param credentialId The ID of the GCP credentials
    #' @param dataset The name of the specified big query dataset to read input data from
    #' @param table The name of the specified big query table to read input data from
    #' @param type Type name for this intake type
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`bucket` = NULL, `dataset` = NULL, `table` = NULL, `type` = NULL, `credentialId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`bucket`, `dataset`, `table`, `type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(bucket, credentialId, dataset, table, type)
      }
      self$`bucket` <- `bucket`
      self$`credentialId` <- `credentialId`
      self$`dataset` <- `dataset`
      self$`table` <- `table`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(bucket = self$`bucket`, credentialId = self$`credentialId`, dataset = self$`dataset`, table = self$`table`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`bucket`)) {
          sprintf(
            '"bucket":
            "%s"
                  ',
            self$`bucket`
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`dataset`)) {
          sprintf(
            '"dataset":
            "%s"
                  ',
            self$`dataset`
          )
        },
        if (!is.null(self$`table`)) {
          sprintf(
            '"table":
            "%s"
                  ',
            self$`table`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BigQueryIntakeJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BigQueryIntakeJson, validateParams = FALSE) {
      BigQueryIntakeObject <- jsonlite::fromJSON(BigQueryIntakeJson)
      self$`bucket` <- BigQueryIntakeObject$`bucket`
      self$`credentialId` <- BigQueryIntakeObject$`credentialId`
      self$`dataset` <- BigQueryIntakeObject$`dataset`
      self$`table` <- BigQueryIntakeObject$`table`
      self$`type` <- BigQueryIntakeObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
