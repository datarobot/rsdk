# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictedVsActualData
#'
#' @description PredictedVsActualData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actual  numeric Actual value. &#x60;null&#x60; for 0 rows bins and unsupervised time series models.
#'
#' @field bin  list( character ) [optional] For numeric features contains labels for left and right bin limits
#'
#' @field label  character Contains label for categorical features; For numeric features contains range or numeric value.
#'
#' @field predicted  numeric Predicted value. &#x60;null&#x60; for 0 rows bins.
#'
#' @field rowCount  integer Number of rows for the label and bin.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictedVsActualData <- R6::R6Class(
  "PredictedVsActualData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`actual` = NULL, `bin` = NULL, `label` = NULL, `predicted` = NULL, `rowCount` = NULL) {
      if (!is.null(`actual`)) {
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`predicted`)) {
      }
      if (!is.null(`rowCount`)) {
        stopifnot(is.numeric(`rowCount`), length(`rowCount`) == 1)
      }
      if (!is.null(`bin`)) {
        stopifnot(is.vector(`bin`))
      }
    }
  ),
  public = list(
    `actual` = NULL,
    `bin` = NULL,
    `label` = NULL,
    `predicted` = NULL,
    `rowCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actual Actual value. &#x60;null&#x60; for 0 rows bins and unsupervised time series models.
    #' @param bin For numeric features contains labels for left and right bin limits
    #' @param label Contains label for categorical features; For numeric features contains range or numeric value.
    #' @param predicted Predicted value. &#x60;null&#x60; for 0 rows bins.
    #' @param rowCount Number of rows for the label and bin.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actual` = NULL, `label` = NULL, `predicted` = NULL, `rowCount` = NULL, `bin` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`actual`, `label`, `predicted`, `rowCount`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(actual, bin, label, predicted, rowCount)
      }
      self$`actual` <- `actual`
      sapply(`bin`, function(x) stopifnot(is.character(x)))
      self$`label` <- `label`
      self$`predicted` <- `predicted`
      self$`rowCount` <- `rowCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(actual = self$`actual`, bin = self$`bin`, label = self$`label`, predicted = self$`predicted`, rowCount = self$`rowCount`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actual`)) {
          sprintf(
            '"actual":
            %d
                  ',
            self$`actual`
          )
        },
        if (!is.null(self$`bin`)) {
          sprintf(
            '"bin":
            [%s]
                  ',
            paste(unlist(lapply(self$`bin`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`predicted`)) {
          sprintf(
            '"predicted":
            %d
                  ',
            self$`predicted`
          )
        },
        if (!is.null(self$`rowCount`)) {
          sprintf(
            '"rowCount":
            %d
                  ',
            self$`rowCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictedVsActualDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictedVsActualDataJson, validateParams = FALSE) {
      PredictedVsActualDataObject <- jsonlite::fromJSON(PredictedVsActualDataJson)
      self$`actual` <- PredictedVsActualDataObject$`actual`
      self$`bin` <- ApiClient$new()$deserializeObj(PredictedVsActualDataObject$`bin`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`label` <- PredictedVsActualDataObject$`label`
      self$`predicted` <- PredictedVsActualDataObject$`predicted`
      self$`rowCount` <- PredictedVsActualDataObject$`rowCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
