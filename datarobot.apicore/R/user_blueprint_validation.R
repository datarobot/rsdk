# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintValidation
#'
#' @description UserBlueprintValidation Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blueprint  \link{OneOfarrayobject} The representation of a directed acyclic graph defining a pipeline of data through tasks and a final estimator.
#'
#' @field isInplaceEditor  character Whether the request is sent from the in place user BP editor.
#'
#' @field projectId  character [optional] String representation of ObjectId for the currently active project. The user blueprint is validated when this project is active. Necessary in the event of project specific tasks, such as column selection tasks.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintValidation <- R6::R6Class(
  "UserBlueprintValidation",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`blueprint` = NULL, `isInplaceEditor` = NULL, `projectId` = NULL) {
      if (!is.null(`blueprint`)) {
        .setPrimitiveProperty(typeList = list("array", "character"), propertyData = blueprint)
      }
      if (!is.null(`isInplaceEditor`)) {
        stopifnot(is.logical(`isInplaceEditor`), length(`isInplaceEditor`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `blueprint` = NULL,
    `isInplaceEditor` = NULL,
    `projectId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blueprint The representation of a directed acyclic graph defining a pipeline of data through tasks and a final estimator.
    #' @param isInplaceEditor Whether the request is sent from the in place user BP editor.
    #' @param projectId String representation of ObjectId for the currently active project. The user blueprint is validated when this project is active. Necessary in the event of project specific tasks, such as column selection tasks.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blueprint` = NULL, `isInplaceEditor` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`blueprint`, `isInplaceEditor`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(blueprint, isInplaceEditor, projectId)
      }
      self$`blueprint` <- .setPrimitiveProperty(typeList = list("array", "character"), propertyData = blueprint)
      self$`isInplaceEditor` <- `isInplaceEditor`
      self$`projectId` <- `projectId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(blueprint = self$`blueprint`, isInplaceEditor = self$`isInplaceEditor`, projectId = self$`projectId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blueprint`)) {
          sprintf(
            '"blueprint":
            %s
      ',
            self$`blueprint`
          )
        },
        if (!is.null(self$`isInplaceEditor`)) {
          sprintf(
            '"isInplaceEditor":
            %s
                  ',
            tolower(self$`isInplaceEditor`)
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintValidationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintValidationJson, validateParams = FALSE) {
      UserBlueprintValidationObject <- jsonlite::fromJSON(UserBlueprintValidationJson)
      self$`blueprint` <- .setPrimitiveProperty(typeList = list("array", "character"), propertyData = UserBlueprintValidationObject$blueprint)
      self$`isInplaceEditor` <- UserBlueprintValidationObject$`isInplaceEditor`
      self$`projectId` <- UserBlueprintValidationObject$`projectId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
