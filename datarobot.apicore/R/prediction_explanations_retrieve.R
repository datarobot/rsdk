# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanationsRetrieve
#'
#' @description PredictionExplanationsRetrieve Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field adjustmentMethod  character &#39;exposureNormalized&#39; (for regression projects with exposure) or &#39;N/A&#39; (for classification projects) The value of &#39;exposureNormalized&#39; indicates that prediction outputs are adjusted (or divided) by exposure. The value of &#39;N/A&#39; indicates that no adjustments are applied to the adjusted predictions and they are identical to the unadjusted predictions.
#'
#' @field count  integer How many rows of prediction explanations were returned.
#'
#' @field data  list( \link{PredictionExplanationsRow} ) Each is a PredictionExplanationsRow corresponding to one row of the prediction dataset.
#'
#' @field id  character The ID of this group of prediction explanations.
#'
#' @field next_  character URL pointing to the next page (if null, there is no next page).
#'
#' @field predictionExplanationsRecordLocation  character The URL of the PredictionExplanationsRecord associated with these prediction explanations.
#'
#' @field previous  character URL pointing to the previous page (if null, there is no previous page).
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanationsRetrieve <- R6::R6Class(
  "PredictionExplanationsRetrieve",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`adjustmentMethod` = NULL, `count` = NULL, `data` = NULL, `id` = NULL, `next_` = NULL, `predictionExplanationsRecordLocation` = NULL, `previous` = NULL) {
      if (!is.null(`adjustmentMethod`)) {
        stopifnot(is.character(`adjustmentMethod`), length(`adjustmentMethod`) == 1)
      }
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`data`)) {
        stopifnot(is.vector(`data`), sapply(`data`, R6::is.R6))
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`predictionExplanationsRecordLocation`)) {
        stopifnot(is.character(`predictionExplanationsRecordLocation`), length(`predictionExplanationsRecordLocation`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
    }
  ),
  public = list(
    `adjustmentMethod` = NULL,
    `count` = NULL,
    `data` = NULL,
    `id` = NULL,
    `next_` = NULL,
    `predictionExplanationsRecordLocation` = NULL,
    `previous` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param adjustmentMethod &#39;exposureNormalized&#39; (for regression projects with exposure) or &#39;N/A&#39; (for classification projects) The value of &#39;exposureNormalized&#39; indicates that prediction outputs are adjusted (or divided) by exposure. The value of &#39;N/A&#39; indicates that no adjustments are applied to the adjusted predictions and they are identical to the unadjusted predictions.
    #' @param count How many rows of prediction explanations were returned.
    #' @param data Each is a PredictionExplanationsRow corresponding to one row of the prediction dataset.
    #' @param id The ID of this group of prediction explanations.
    #' @param next_ URL pointing to the next page (if null, there is no next page).
    #' @param predictionExplanationsRecordLocation The URL of the PredictionExplanationsRecord associated with these prediction explanations.
    #' @param previous URL pointing to the previous page (if null, there is no previous page).
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`adjustmentMethod` = NULL, `count` = NULL, `data` = NULL, `id` = NULL, `next_` = NULL, `predictionExplanationsRecordLocation` = NULL, `previous` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`adjustmentMethod`, `count`, `data`, `id`, `next_`, `predictionExplanationsRecordLocation`, `previous`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(adjustmentMethod, count, data, id, next_, predictionExplanationsRecordLocation, previous)
      }
      self$`adjustmentMethod` <- `adjustmentMethod`
      self$`count` <- `count`
      self$`data` <- `data`
      self$`id` <- `id`
      self$`next_` <- `next_`
      self$`predictionExplanationsRecordLocation` <- `predictionExplanationsRecordLocation`
      self$`previous` <- `previous`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(adjustmentMethod = self$`adjustmentMethod`, count = self$`count`, data = self$`data`, id = self$`id`, next_ = self$`next_`, predictionExplanationsRecordLocation = self$`predictionExplanationsRecordLocation`, previous = self$`previous`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`adjustmentMethod`)) {
          sprintf(
            '"adjustmentMethod":
            "%s"
                  ',
            self$`adjustmentMethod`
          )
        },
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
      ',
            paste(sapply(self$`data`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`predictionExplanationsRecordLocation`)) {
          sprintf(
            '"predictionExplanationsRecordLocation":
            "%s"
                  ',
            self$`predictionExplanationsRecordLocation`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationsRetrieveJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationsRetrieveJson, validateParams = FALSE) {
      PredictionExplanationsRetrieveObject <- jsonlite::fromJSON(PredictionExplanationsRetrieveJson)
      self$`adjustmentMethod` <- PredictionExplanationsRetrieveObject$`adjustmentMethod`
      self$`count` <- PredictionExplanationsRetrieveObject$`count`
      self$`data` <- ApiClient$new()$deserializeObj(PredictionExplanationsRetrieveObject$`data`, "array[PredictionExplanationsRow]", loadNamespace("datarobot.apicore"))
      self$`id` <- PredictionExplanationsRetrieveObject$`id`
      self$`next_` <- PredictionExplanationsRetrieveObject$`next`
      self$`predictionExplanationsRecordLocation` <- PredictionExplanationsRetrieveObject$`predictionExplanationsRecordLocation`
      self$`previous` <- PredictionExplanationsRetrieveObject$`previous`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
