# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BlueprintChartRetrieveResponse
#'
#' @description BlueprintChartRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field edges  list( \link{array[character]} ) An array of chart edges - tuples of (start_id, end_id).
#'
#' @field nodes  list( \link{NodeDescription} ) An array of node descriptions.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BlueprintChartRetrieveResponse <- R6::R6Class(
  "BlueprintChartRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`edges` = NULL, `nodes` = NULL) {
      if (!is.null(`edges`)) {
        stopifnot(is.vector(`edges`))
      }
      if (!is.null(`nodes`)) {
        stopifnot(is.vector(`nodes`))
      }
    }
  ),
  public = list(
    `edges` = NULL,
    `nodes` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param edges An array of chart edges - tuples of (start_id, end_id).
    #' @param nodes An array of node descriptions.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`edges` = NULL, `nodes` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`edges`, `nodes`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(edges, nodes)
      }
      sapply(`edges`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`nodes`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(edges = self$`edges`, nodes = self$`nodes`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`edges`)) {
          sprintf(
            '"edges":
            [%s]
      ',
            paste(sapply(self$`edges`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`nodes`)) {
          sprintf(
            '"nodes":
            [%s]
      ',
            paste(sapply(self$`nodes`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BlueprintChartRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BlueprintChartRetrieveResponseJson, validateParams = FALSE) {
      BlueprintChartRetrieveResponseObject <- jsonlite::fromJSON(BlueprintChartRetrieveResponseJson)
      self$`edges` <- ApiClient$new()$deserializeObj(BlueprintChartRetrieveResponseObject$`edges`, "array[array[character]]", loadNamespace("datarobot.apicore"))
      self$`nodes` <- ApiClient$new()$deserializeObj(BlueprintChartRetrieveResponseObject$`nodes`, "array[NodeDescription]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
