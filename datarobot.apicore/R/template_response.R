# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TemplateResponse
#'
#' @description TemplateResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field creatorId  character The ID of the user who created the template
#'
#' @field creatorUsername  character The username of the user who created the template
#'
#' @field dateModified  character Last date/time of template modification.
#'
#' @field description  character An overview of the template
#'
#' @field id  character The ID of the template accessible by the user
#'
#' @field instructions  character Currently always returns &#x60;&#x60;null&#x60;&#x60;. Maintained as a placeholder for future functionality.
#'
#' @field labels  list( character ) User-added filtering labels for the template
#'
#' @field name  character The name of the template
#'
#' @field orgId  character The ID of the organization the user who created the template belongs to
#'
#' @field permissions  \link{TemplatePermissionsResponse}
#'
#' @field projectType  character Type of project template.
#'
#' @field sections  list( \link{OneOfSectionDataRobotSectionUserSectionCustomSectionTableOfContents} ) List of section objects representing the structure of the document. Each section can have sub-sections that have the same schema as the parent section, i.e., the structure is recursive. The number of nested sections allowed is 5. The total number of sections allowed is 500.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TemplateResponse <- R6::R6Class(
  "TemplateResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(creatorId = NULL, creatorUsername = NULL, dateModified = NULL, description = NULL, id = NULL, instructions = NULL, labels = NULL, name = NULL, orgId = NULL, permissions = NULL, projectType = NULL, sections = NULL) {
      if (!is.null(`creatorId`)) {
        stopifnot(is.character(`creatorId`), length(`creatorId`) == 1)
      }
      if (!is.null(`creatorUsername`)) {
        stopifnot(is.character(`creatorUsername`), length(`creatorUsername`) == 1)
      }
      if (!is.null(`dateModified`)) {
        stopifnot(inherits(`dateModified`, "POSIXt"))
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`instructions`)) {
        stopifnot(is.character(`instructions`), length(`instructions`) == 1)
      }
      if (!is.null(`labels`)) {
        stopifnot(is.vector(`labels`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
      if (!is.null(`permissions`)) {
        stopifnot(R6::is.R6(`permissions`))
      }
      if (!is.null(`projectType`)) {
        stopifnot(is.character(`projectType`), length(`projectType`) == 1)
      }
      if (!is.null(`sections`)) {
        .setComplexProperty(typeList = list(SectionDataRobot, SectionUser, SectionCustom, SectionTableOfContents), propertyData = sections)
      }
    }
  ),
  public = list(
    `creatorId` = NULL,
    `creatorUsername` = NULL,
    `dateModified` = NULL,
    `description` = NULL,
    `id` = NULL,
    `instructions` = NULL,
    `labels` = NULL,
    `name` = NULL,
    `orgId` = NULL,
    `permissions` = NULL,
    `projectType` = NULL,
    `sections` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param creatorId The ID of the user who created the template
    #' @param creatorUsername The username of the user who created the template
    #' @param dateModified Last date/time of template modification.
    #' @param description An overview of the template
    #' @param id The ID of the template accessible by the user
    #' @param instructions Currently always returns &#x60;&#x60;null&#x60;&#x60;. Maintained as a placeholder for future functionality.
    #' @param labels User-added filtering labels for the template
    #' @param name The name of the template
    #' @param orgId The ID of the organization the user who created the template belongs to
    #' @param permissions
    #' @param projectType Type of project template.
    #' @param sections List of section objects representing the structure of the document. Each section can have sub-sections that have the same schema as the parent section, i.e., the structure is recursive. The number of nested sections allowed is 5. The total number of sections allowed is 500.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`creatorId` = NULL, `creatorUsername` = NULL, `dateModified` = NULL, `description` = NULL, `id` = NULL, `instructions` = NULL, `labels` = NULL, `name` = NULL, `orgId` = NULL, `permissions` = NULL, `projectType` = NULL, `sections` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`creatorId`, `creatorUsername`, `dateModified`, `description`, `id`, `instructions`, `labels`, `name`, `orgId`, `permissions`, `projectType`, `sections`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(creatorId, creatorUsername, dateModified, description, id, instructions, labels, name, orgId, permissions, projectType, sections)
      }
      self$`creatorId` <- `creatorId`
      self$`creatorUsername` <- `creatorUsername`
      self$`dateModified` <- `dateModified`
      self$`description` <- `description`
      self$`id` <- `id`
      self$`instructions` <- `instructions`
      sapply(`labels`, function(x) stopifnot(is.character(x)))
      self$`name` <- `name`
      self$`orgId` <- `orgId`
      self$`permissions` <- `permissions`
      self$`projectType` <- `projectType`
      self$`sections` <- .setComplexProperty(typeList = list(SectionDataRobot, SectionUser, SectionCustom, SectionTableOfContents), propertyData = sections)
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(creatorId = self$`creatorId`, creatorUsername = self$`creatorUsername`, dateModified = self$`dateModified`, description = self$`description`, id = self$`id`, instructions = self$`instructions`, labels = self$`labels`, name = self$`name`, orgId = self$`orgId`, permissions = self$`permissions`, projectType = self$`projectType`, sections = self$`sections`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`creatorId`)) {
          sprintf(
            '"creatorId":
            "%s"
                  ',
            self$`creatorId`
          )
        },
        if (!is.null(self$`creatorUsername`)) {
          sprintf(
            '"creatorUsername":
            "%s"
                  ',
            self$`creatorUsername`
          )
        },
        if (!is.null(self$`dateModified`)) {
          sprintf(
            '"dateModified":
            "%s"
                  ',
            format(self$`dateModified`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`instructions`)) {
          sprintf(
            '"instructions":
            "%s"
                  ',
            self$`instructions`
          )
        },
        if (!is.null(self$`labels`)) {
          sprintf(
            '"labels":
              [%s]
            ',
            paste(unlist(lapply(self$`labels`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        },
        if (!is.null(self$`permissions`)) {
          sprintf(
            '"permissions":
          %s
        ',
            jsonlite::toJSON(self$`permissions`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`projectType`)) {
          sprintf(
            '"projectType":
            "%s"
                  ',
            self$`projectType`
          )
        },
        if (!is.null(self$`sections`)) {
          sprintf(
            '"sections":
            [%s]
',
            paste(sapply(self$`sections`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TemplateResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TemplateResponseJson, validateParams = FALSE) {
      TemplateResponseObject <- jsonlite::fromJSON(TemplateResponseJson)
      self$`creatorId` <- TemplateResponseObject$`creatorId`
      self$`creatorUsername` <- TemplateResponseObject$`creatorUsername`
      self$`dateModified` <- ParseRFC3339Timestamp(TemplateResponseObject$`dateModified`)
      self$`description` <- TemplateResponseObject$`description`
      self$`id` <- TemplateResponseObject$`id`
      self$`instructions` <- TemplateResponseObject$`instructions`
      self$`labels` <- ApiClient$new()$deserializeObj(TemplateResponseObject$`labels`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`name` <- TemplateResponseObject$`name`
      self$`orgId` <- TemplateResponseObject$`orgId`
      self$`permissions` <- TemplatePermissionsResponse$new()$fromJSON(jsonlite::toJSON(TemplateResponseObject$permissions, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`projectType` <- TemplateResponseObject$`projectType`
      self$`sections` <- ApiClient$new()$deserializeObj(TemplateResponseObject$`sections`, "array[OneOfSectionDataRobotSectionUserSectionCustomSectionTableOfContents]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
