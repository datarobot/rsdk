# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AverageModelMetricsField
#'
#' @description AverageModelMetricsField Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field metrics  list( \link{AverageModelMetricsValues} ) Average model metrics for the given thresholds.
#'
#' @field source  character Chart source.
#'
#' @field thresholds  list( numeric ) Threshold values for which model metrics are available.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AverageModelMetricsField <- R6::R6Class(
  "AverageModelMetricsField",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(metrics = NULL, source = NULL, thresholds = NULL) {
      if (!is.null(`metrics`)) {
        stopifnot(is.vector(`metrics`))
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
      if (!is.null(`thresholds`)) {
        stopifnot(is.vector(`thresholds`))
      }
    }
  ),
  public = list(
    `metrics` = NULL,
    `source` = NULL,
    `thresholds` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param metrics Average model metrics for the given thresholds.
    #' @param source Chart source.
    #' @param thresholds Threshold values for which model metrics are available.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`metrics` = NULL, `source` = NULL, `thresholds` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`metrics`, `source`, `thresholds`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(metrics, source, thresholds)
      }
      sapply(`metrics`, function(x) stopifnot(R6::is.R6(x)))
      self$`source` <- `source`
      sapply(`thresholds`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(metrics = self$`metrics`, source = self$`source`, thresholds = self$`thresholds`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`metrics`)) {
          sprintf(
            '"metrics":
            [%s]
',
            paste(sapply(self$`metrics`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        },
        if (!is.null(self$`thresholds`)) {
          sprintf(
            '"thresholds":
              [%s]
            ',
            paste(unlist(lapply(self$`thresholds`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AverageModelMetricsFieldJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AverageModelMetricsFieldJson, validateParams = FALSE) {
      AverageModelMetricsFieldObject <- jsonlite::fromJSON(AverageModelMetricsFieldJson)
      self$`metrics` <- ApiClient$new()$deserializeObj(AverageModelMetricsFieldObject$`metrics`, "array[AverageModelMetricsValues]", loadNamespace("datarobot.apicore"))
      self$`source` <- AverageModelMetricsFieldObject$`source`
      self$`thresholds` <- ApiClient$new()$deserializeObj(AverageModelMetricsFieldObject$`thresholds`, "array[numeric]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
