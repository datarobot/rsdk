# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintsBlueprintTask
#'
#' @description UserBlueprintsBlueprintTask Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field taskData  \link{OneOfUserBlueprintsBlueprintTaskData} The data defining the task / vertex in the blueprint.
#'
#' @field taskId  character The identifier of a task / vertex in the blueprint.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintsBlueprintTask <- R6::R6Class(
  "UserBlueprintsBlueprintTask",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`taskData` = NULL, `taskId` = NULL) {
      if (!is.null(`taskData`)) {
        .setComplexProperty(typeList = list(UserBlueprintsBlueprintTaskData), propertyData = taskData)
      }
      if (!is.null(`taskId`)) {
        stopifnot(is.character(`taskId`), length(`taskId`) == 1)
      }
    }
  ),
  public = list(
    `taskData` = NULL,
    `taskId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param taskData The data defining the task / vertex in the blueprint.
    #' @param taskId The identifier of a task / vertex in the blueprint.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`taskData` = NULL, `taskId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`taskData`, `taskId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(taskData, taskId)
      }
      self$`taskData` <- .setComplexProperty(typeList = list(UserBlueprintsBlueprintTaskData), propertyData = taskData)
      self$`taskId` <- `taskId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(taskData = self$`taskData`, taskId = self$`taskId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`taskData`)) {
          sprintf(
            '"taskData":
            %s
      ',
            jsonlite::toJSON(self$`taskData`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`taskId`)) {
          sprintf(
            '"taskId":
            "%s"
                  ',
            self$`taskId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintsBlueprintTaskJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintsBlueprintTaskJson, validateParams = FALSE) {
      UserBlueprintsBlueprintTaskObject <- jsonlite::fromJSON(UserBlueprintsBlueprintTaskJson)
      self$`taskData` <- .setComplexProperty(typeList = list(UserBlueprintsBlueprintTaskData), propertyData = UserBlueprintsBlueprintTaskObject$taskData)
      self$`taskId` <- UserBlueprintsBlueprintTaskObject$`taskId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
