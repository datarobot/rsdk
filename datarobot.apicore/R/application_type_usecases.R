# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApplicationTypeUsecases
#'
#' @description ApplicationTypeUsecases Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field applicationTypeId  character id of application type
#'
#' @field demoAvailable  character Is demo app creation available for this usecase
#'
#' @field description  character Description of this usecase
#'
#' @field id  character id of usecase
#'
#' @field sources  list( \link{ApplicationTypeUsecasesSource} ) List of resources linked to usecase
#'
#' @field title  character Title of usecase
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApplicationTypeUsecases <- R6::R6Class(
  "ApplicationTypeUsecases",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(applicationTypeId = NULL, demoAvailable = NULL, description = NULL, id = NULL, sources = NULL, title = NULL) {
      if (!is.null(`applicationTypeId`)) {
        stopifnot(is.character(`applicationTypeId`), length(`applicationTypeId`) == 1)
      }
      if (!is.null(`demoAvailable`)) {
        stopifnot(is.logical(`demoAvailable`), length(`demoAvailable`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`sources`)) {
        stopifnot(is.vector(`sources`))
      }
      if (!is.null(`title`)) {
        stopifnot(is.character(`title`), length(`title`) == 1)
      }
    }
  ),
  public = list(
    `applicationTypeId` = NULL,
    `demoAvailable` = NULL,
    `description` = NULL,
    `id` = NULL,
    `sources` = NULL,
    `title` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param applicationTypeId id of application type
    #' @param demoAvailable Is demo app creation available for this usecase
    #' @param description Description of this usecase
    #' @param id id of usecase
    #' @param sources List of resources linked to usecase
    #' @param title Title of usecase
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`applicationTypeId` = NULL, `demoAvailable` = NULL, `description` = NULL, `id` = NULL, `sources` = NULL, `title` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`applicationTypeId`, `demoAvailable`, `description`, `id`, `sources`, `title`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(applicationTypeId, demoAvailable, description, id, sources, title)
      }
      self$`applicationTypeId` <- `applicationTypeId`
      self$`demoAvailable` <- `demoAvailable`
      self$`description` <- `description`
      self$`id` <- `id`
      sapply(`sources`, function(x) stopifnot(R6::is.R6(x)))
      self$`title` <- `title`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(applicationTypeId = self$`applicationTypeId`, demoAvailable = self$`demoAvailable`, description = self$`description`, id = self$`id`, sources = self$`sources`, title = self$`title`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`applicationTypeId`)) {
          sprintf(
            '"applicationTypeId":
            "%s"
                  ',
            self$`applicationTypeId`
          )
        },
        if (!is.null(self$`demoAvailable`)) {
          sprintf(
            '"demoAvailable":
            %s
                  ',
            tolower(self$`demoAvailable`)
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`sources`)) {
          sprintf(
            '"sources":
            [%s]
',
            paste(sapply(self$`sources`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`title`)) {
          sprintf(
            '"title":
            "%s"
                  ',
            self$`title`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApplicationTypeUsecasesJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApplicationTypeUsecasesJson, validateParams = FALSE) {
      ApplicationTypeUsecasesObject <- jsonlite::fromJSON(ApplicationTypeUsecasesJson)
      self$`applicationTypeId` <- ApplicationTypeUsecasesObject$`applicationTypeId`
      self$`demoAvailable` <- ApplicationTypeUsecasesObject$`demoAvailable`
      self$`description` <- ApplicationTypeUsecasesObject$`description`
      self$`id` <- ApplicationTypeUsecasesObject$`id`
      self$`sources` <- ApiClient$new()$deserializeObj(ApplicationTypeUsecasesObject$`sources`, "array[ApplicationTypeUsecasesSource]", loadNamespace("datarobot.apicore"))
      self$`title` <- ApplicationTypeUsecasesObject$`title`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
