# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PrimeRulesetsListResponse
#'
#' @description PrimeRulesetsListResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field modelId  character the ID of the Prime model using this ruleset (if it exists) or null
#'
#' @field parentModelId  character ID of the model this ruleset approximates
#'
#' @field projectId  character the project this ruleset belongs to
#'
#' @field ruleCount  integer the number of rules used by this ruleset
#'
#' @field rulesetId  integer the ID of the ruleset
#'
#' @field score  numeric the validation score of the ruleset
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PrimeRulesetsListResponse <- R6::R6Class(
  "PrimeRulesetsListResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(modelId = NULL, parentModelId = NULL, projectId = NULL, ruleCount = NULL, rulesetId = NULL, score = NULL) {
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`parentModelId`)) {
        stopifnot(is.character(`parentModelId`), length(`parentModelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`ruleCount`)) {
        stopifnot(is.numeric(`ruleCount`), length(`ruleCount`) == 1)
      }
      if (!is.null(`rulesetId`)) {
        stopifnot(is.numeric(`rulesetId`), length(`rulesetId`) == 1)
      }
      if (!is.null(`score`)) {
      }
    }
  ),
  public = list(
    `modelId` = NULL,
    `parentModelId` = NULL,
    `projectId` = NULL,
    `ruleCount` = NULL,
    `rulesetId` = NULL,
    `score` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param modelId the ID of the Prime model using this ruleset (if it exists) or null
    #' @param parentModelId ID of the model this ruleset approximates
    #' @param projectId the project this ruleset belongs to
    #' @param ruleCount the number of rules used by this ruleset
    #' @param rulesetId the ID of the ruleset
    #' @param score the validation score of the ruleset
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`modelId` = NULL, `parentModelId` = NULL, `projectId` = NULL, `ruleCount` = NULL, `rulesetId` = NULL, `score` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`modelId`, `parentModelId`, `projectId`, `ruleCount`, `rulesetId`, `score`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(modelId, parentModelId, projectId, ruleCount, rulesetId, score)
      }
      self$`modelId` <- `modelId`
      self$`parentModelId` <- `parentModelId`
      self$`projectId` <- `projectId`
      self$`ruleCount` <- `ruleCount`
      self$`rulesetId` <- `rulesetId`
      self$`score` <- `score`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(modelId = self$`modelId`, parentModelId = self$`parentModelId`, projectId = self$`projectId`, ruleCount = self$`ruleCount`, rulesetId = self$`rulesetId`, score = self$`score`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`parentModelId`)) {
          sprintf(
            '"parentModelId":
            "%s"
                  ',
            self$`parentModelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`ruleCount`)) {
          sprintf(
            '"ruleCount":
            %d
                  ',
            self$`ruleCount`
          )
        },
        if (!is.null(self$`rulesetId`)) {
          sprintf(
            '"rulesetId":
            %d
                  ',
            self$`rulesetId`
          )
        },
        if (!is.null(self$`score`)) {
          sprintf(
            '"score":
            %d
                  ',
            self$`score`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PrimeRulesetsListResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PrimeRulesetsListResponseJson, validateParams = FALSE) {
      PrimeRulesetsListResponseObject <- jsonlite::fromJSON(PrimeRulesetsListResponseJson)
      self$`modelId` <- PrimeRulesetsListResponseObject$`modelId`
      self$`parentModelId` <- PrimeRulesetsListResponseObject$`parentModelId`
      self$`projectId` <- PrimeRulesetsListResponseObject$`projectId`
      self$`ruleCount` <- PrimeRulesetsListResponseObject$`ruleCount`
      self$`rulesetId` <- PrimeRulesetsListResponseObject$`rulesetId`
      self$`score` <- PrimeRulesetsListResponseObject$`score`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
