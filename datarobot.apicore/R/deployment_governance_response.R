# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentGovernanceResponse
#'
#' @description DeploymentGovernanceResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field approvalStatus  character Status to show whether the deployment was approved or not. It also shows up as a part of metadata within the prediction response.
#'
#' @field hasOpenedChangeRequests  character If there are change request related to this deployment with &#x60;PENDING&#x60; status.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentGovernanceResponse <- R6::R6Class(
  "DeploymentGovernanceResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`approvalStatus` = NULL, `hasOpenedChangeRequests` = NULL) {
      if (!is.null(`approvalStatus`)) {
        stopifnot(is.character(`approvalStatus`), length(`approvalStatus`) == 1)
      }
      if (!is.null(`hasOpenedChangeRequests`)) {
        stopifnot(is.logical(`hasOpenedChangeRequests`), length(`hasOpenedChangeRequests`) == 1)
      }
    }
  ),
  public = list(
    `approvalStatus` = NULL,
    `hasOpenedChangeRequests` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param approvalStatus Status to show whether the deployment was approved or not. It also shows up as a part of metadata within the prediction response.
    #' @param hasOpenedChangeRequests If there are change request related to this deployment with &#x60;PENDING&#x60; status.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`approvalStatus` = NULL, `hasOpenedChangeRequests` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`approvalStatus`, `hasOpenedChangeRequests`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(approvalStatus, hasOpenedChangeRequests)
      }
      self$`approvalStatus` <- `approvalStatus`
      self$`hasOpenedChangeRequests` <- `hasOpenedChangeRequests`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(approvalStatus = self$`approvalStatus`, hasOpenedChangeRequests = self$`hasOpenedChangeRequests`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`approvalStatus`)) {
          sprintf(
            '"approvalStatus":
            "%s"
                  ',
            self$`approvalStatus`
          )
        },
        if (!is.null(self$`hasOpenedChangeRequests`)) {
          sprintf(
            '"hasOpenedChangeRequests":
            %s
                  ',
            tolower(self$`hasOpenedChangeRequests`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentGovernanceResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentGovernanceResponseJson, validateParams = FALSE) {
      DeploymentGovernanceResponseObject <- jsonlite::fromJSON(DeploymentGovernanceResponseJson)
      self$`approvalStatus` <- DeploymentGovernanceResponseObject$`approvalStatus`
      self$`hasOpenedChangeRequests` <- DeploymentGovernanceResponseObject$`hasOpenedChangeRequests`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
