# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ChallengerCreate
#'
#' @description ChallengerCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field modelPackageId  character ID of the model package to add as a challenger.
#'
#' @field name  character Human-readable name for the challenger.
#'
#' @field predictionEnvironmentId  character [optional] ID of the Prediction Environment the challenger should use. If prediction environments are enabled, this is required
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ChallengerCreate <- R6::R6Class(
  "ChallengerCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(modelPackageId = NULL, name = NULL, predictionEnvironmentId = NULL) {
      if (!is.null(`modelPackageId`)) {
        stopifnot(is.character(`modelPackageId`), length(`modelPackageId`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`predictionEnvironmentId`)) {
        stopifnot(is.character(`predictionEnvironmentId`), length(`predictionEnvironmentId`) == 1)
      }
    }
  ),
  public = list(
    `modelPackageId` = NULL,
    `name` = NULL,
    `predictionEnvironmentId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param modelPackageId ID of the model package to add as a challenger.
    #' @param name Human-readable name for the challenger.
    #' @param predictionEnvironmentId ID of the Prediction Environment the challenger should use. If prediction environments are enabled, this is required
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`modelPackageId` = NULL, `name` = NULL, `predictionEnvironmentId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`modelPackageId`, `name`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(modelPackageId, name, predictionEnvironmentId)
      }
      self$`modelPackageId` <- `modelPackageId`
      self$`name` <- `name`
      self$`predictionEnvironmentId` <- `predictionEnvironmentId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(modelPackageId = self$`modelPackageId`, name = self$`name`, predictionEnvironmentId = self$`predictionEnvironmentId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`modelPackageId`)) {
          sprintf(
            '"modelPackageId":
            "%s"
                  ',
            self$`modelPackageId`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`predictionEnvironmentId`)) {
          sprintf(
            '"predictionEnvironmentId":
            "%s"
                  ',
            self$`predictionEnvironmentId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ChallengerCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ChallengerCreateJson, validateParams = FALSE) {
      ChallengerCreateObject <- jsonlite::fromJSON(ChallengerCreateJson)
      self$`modelPackageId` <- ChallengerCreateObject$`modelPackageId`
      self$`name` <- ChallengerCreateObject$`name`
      self$`predictionEnvironmentId` <- ChallengerCreateObject$`predictionEnvironmentId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
