# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PartitioningWarning
#'
#' @description PartitioningWarning Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field backtestIndex  integer Backtest index. Null if the warning does not correspond to a single backtest.
#'
#' @field partition  character Partition name.
#'
#' @field warnings  list( character ) A list of strings representing warnings for the specified partition
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PartitioningWarning <- R6::R6Class(
  "PartitioningWarning",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(backtestIndex = NULL, partition = NULL, warnings = NULL) {
      if (!is.null(`backtestIndex`)) {
        stopifnot(is.numeric(`backtestIndex`), length(`backtestIndex`) == 1)
      }
      if (!is.null(`partition`)) {
        stopifnot(is.character(`partition`), length(`partition`) == 1)
      }
      if (!is.null(`warnings`)) {
        stopifnot(is.vector(`warnings`))
      }
    }
  ),
  public = list(
    `backtestIndex` = NULL,
    `partition` = NULL,
    `warnings` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param backtestIndex Backtest index. Null if the warning does not correspond to a single backtest.
    #' @param partition Partition name.
    #' @param warnings A list of strings representing warnings for the specified partition
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`backtestIndex` = NULL, `partition` = NULL, `warnings` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`backtestIndex`, `partition`, `warnings`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(backtestIndex, partition, warnings)
      }
      self$`backtestIndex` <- `backtestIndex`
      self$`partition` <- `partition`
      sapply(`warnings`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(backtestIndex = self$`backtestIndex`, partition = self$`partition`, warnings = self$`warnings`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`backtestIndex`)) {
          sprintf(
            '"backtestIndex":
            %d
                  ',
            self$`backtestIndex`
          )
        },
        if (!is.null(self$`partition`)) {
          sprintf(
            '"partition":
            "%s"
                  ',
            self$`partition`
          )
        },
        if (!is.null(self$`warnings`)) {
          sprintf(
            '"warnings":
              [%s]
            ',
            paste(unlist(lapply(self$`warnings`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PartitioningWarningJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PartitioningWarningJson, validateParams = FALSE) {
      PartitioningWarningObject <- jsonlite::fromJSON(PartitioningWarningJson)
      self$`backtestIndex` <- PartitioningWarningObject$`backtestIndex`
      self$`partition` <- PartitioningWarningObject$`partition`
      self$`warnings` <- ApiClient$new()$deserializeObj(PartitioningWarningObject$`warnings`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
