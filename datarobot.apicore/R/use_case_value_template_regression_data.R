# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseValueTemplateRegressionData
#'
#' @description UseCaseValueTemplateRegressionData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accuracyImprovement  numeric Accuracy improvement
#'
#' @field decisionsCount  integer Estimated number of decisions per year
#'
#' @field targetValue  numeric Target value
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseValueTemplateRegressionData <- R6::R6Class(
  "UseCaseValueTemplateRegressionData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`accuracyImprovement` = NULL, `decisionsCount` = NULL, `targetValue` = NULL) {
      if (!is.null(`accuracyImprovement`)) {
      }
      if (!is.null(`decisionsCount`)) {
        stopifnot(is.numeric(`decisionsCount`), length(`decisionsCount`) == 1)
      }
      if (!is.null(`targetValue`)) {
      }
    }
  ),
  public = list(
    `accuracyImprovement` = NULL,
    `decisionsCount` = NULL,
    `targetValue` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accuracyImprovement Accuracy improvement
    #' @param decisionsCount Estimated number of decisions per year
    #' @param targetValue Target value
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`accuracyImprovement` = NULL, `decisionsCount` = NULL, `targetValue` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`accuracyImprovement`, `decisionsCount`, `targetValue`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(accuracyImprovement, decisionsCount, targetValue)
      }
      self$`accuracyImprovement` <- `accuracyImprovement`
      self$`decisionsCount` <- `decisionsCount`
      self$`targetValue` <- `targetValue`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(accuracyImprovement = self$`accuracyImprovement`, decisionsCount = self$`decisionsCount`, targetValue = self$`targetValue`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accuracyImprovement`)) {
          sprintf(
            '"accuracyImprovement":
            %d
                  ',
            self$`accuracyImprovement`
          )
        },
        if (!is.null(self$`decisionsCount`)) {
          sprintf(
            '"decisionsCount":
            %d
                  ',
            self$`decisionsCount`
          )
        },
        if (!is.null(self$`targetValue`)) {
          sprintf(
            '"targetValue":
            %d
                  ',
            self$`targetValue`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseValueTemplateRegressionDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseValueTemplateRegressionDataJson, validateParams = FALSE) {
      UseCaseValueTemplateRegressionDataObject <- jsonlite::fromJSON(UseCaseValueTemplateRegressionDataJson)
      self$`accuracyImprovement` <- UseCaseValueTemplateRegressionDataObject$`accuracyImprovement`
      self$`decisionsCount` <- UseCaseValueTemplateRegressionDataObject$`decisionsCount`
      self$`targetValue` <- UseCaseValueTemplateRegressionDataObject$`targetValue`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
