# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BpData
#'
#' @description BpData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field children  list( object ) [optional] A nested dictionary representation of the blueprint DAG.
#'
#' @field id  character The type of the node (e.g. \&quot;start\&quot;, \&quot;input\&quot;, \&quot;task\&quot;).
#'
#' @field inputs  list( character ) [optional] The inputs to the current node.
#'
#' @field output  \link{OneOfstringarray} [optional] Ids describing the destination of any outgoing edges.
#'
#' @field taskMap  object
#'
#' @field taskParameters  character A stringified JSON object describing the parameters and their values for a task.
#'
#' @field tasks  list( character ) The task defining the current node.
#'
#' @field type  character A unique id to represent the current node.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BpData <- R6::R6Class(
  "BpData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`children` = NULL, `id` = NULL, `inputs` = NULL, `output` = NULL, `taskMap` = NULL, `taskParameters` = NULL, `tasks` = NULL, `type` = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`taskMap`)) {
      }
      if (!is.null(`taskParameters`)) {
        stopifnot(is.character(`taskParameters`), length(`taskParameters`) == 1)
      }
      if (!is.null(`tasks`)) {
        stopifnot(is.vector(`tasks`), sapply(`tasks`, is.character))
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`children`) && length(`children`) > 0) {
        stopifnot(is.vector(`children`), sapply(`children`, is.character))
      }
      if (!is.null(`inputs`) && length(`inputs`) > 0) {
        stopifnot(is.vector(`inputs`), sapply(`inputs`, is.character))
      }
      if (!is.null(`output`)) {
        .setPrimitiveProperty(typeList = list("character", "array"), propertyData = output)
      }
    }
  ),
  public = list(
    `children` = NULL,
    `id` = NULL,
    `inputs` = NULL,
    `output` = NULL,
    `taskMap` = NULL,
    `taskParameters` = NULL,
    `tasks` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param children A nested dictionary representation of the blueprint DAG.
    #' @param id The type of the node (e.g. \&quot;start\&quot;, \&quot;input\&quot;, \&quot;task\&quot;).
    #' @param inputs The inputs to the current node.
    #' @param output Ids describing the destination of any outgoing edges.
    #' @param taskMap
    #' @param taskParameters A stringified JSON object describing the parameters and their values for a task.
    #' @param tasks The task defining the current node.
    #' @param type A unique id to represent the current node.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `taskMap` = NULL, `taskParameters` = NULL, `tasks` = NULL, `type` = NULL, `children` = NULL, `inputs` = NULL, `output` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`id`, `taskMap`, `taskParameters`, `tasks`, `type`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(children, id, inputs, output, taskMap, taskParameters, tasks, type)
      }
      self$`children` <- `children`
      self$`id` <- `id`
      self$`inputs` <- `inputs`
      self$`output` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = output)
      self$`taskMap` <- `taskMap`
      self$`taskParameters` <- `taskParameters`
      self$`tasks` <- `tasks`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(children = self$`children`, id = self$`id`, inputs = self$`inputs`, output = self$`output`, taskMap = self$`taskMap`, taskParameters = self$`taskParameters`, tasks = self$`tasks`, type = self$`type`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`children`)) {
          sprintf(
            '"children":
            [%s]
                  ',
            paste(unlist(lapply(self$`children`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`inputs`)) {
          sprintf(
            '"inputs":
            [%s]
                  ',
            paste(unlist(lapply(self$`inputs`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`output`)) {
          sprintf(
            '"output":
            %s
      ',
            self$`output`
          )
        },
        if (!is.null(self$`taskMap`)) {
          sprintf(
            '"taskMap":
            "%s"
                  ',
            self$`taskMap`
          )
        },
        if (!is.null(self$`taskParameters`)) {
          sprintf(
            '"taskParameters":
            "%s"
                  ',
            self$`taskParameters`
          )
        },
        if (!is.null(self$`tasks`)) {
          sprintf(
            '"tasks":
            [%s]
                  ',
            paste(unlist(lapply(self$`tasks`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BpDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BpDataJson, validateParams = FALSE) {
      BpDataObject <- jsonlite::fromJSON(BpDataJson)
      self$`children` <- ApiClient$new()$deserializeObj(BpDataObject$`children`, "array[object]", loadNamespace("datarobot.apicore"))
      self$`id` <- BpDataObject$`id`
      self$`inputs` <- ApiClient$new()$deserializeObj(BpDataObject$`inputs`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`output` <- .setPrimitiveProperty(typeList = list("character", "array"), propertyData = BpDataObject$output)
      self$`taskMap` <- BpDataObject$`taskMap`
      self$`taskParameters` <- BpDataObject$`taskParameters`
      self$`tasks` <- ApiClient$new()$deserializeObj(BpDataObject$`tasks`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`type` <- BpDataObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
