# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DemoApplication
#'
#' @description DemoApplication Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field applicationTypeId  character The ID of the application to be created
#'
#' @field usecaseId  character [optional] The ID of the use case to use for the demo application
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DemoApplication <- R6::R6Class(
  "DemoApplication",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(applicationTypeId = NULL, usecaseId = NULL) {
      if (!is.null(`applicationTypeId`)) {
        stopifnot(is.character(`applicationTypeId`), length(`applicationTypeId`) == 1)
      }
      if (!is.null(`usecaseId`)) {
        stopifnot(is.character(`usecaseId`), length(`usecaseId`) == 1)
      }
    }
  ),
  public = list(
    `applicationTypeId` = NULL,
    `usecaseId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param applicationTypeId The ID of the application to be created
    #' @param usecaseId The ID of the use case to use for the demo application
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`applicationTypeId` = NULL, `usecaseId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`applicationTypeId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(applicationTypeId, usecaseId)
      }
      self$`applicationTypeId` <- `applicationTypeId`
      self$`usecaseId` <- `usecaseId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(applicationTypeId = self$`applicationTypeId`, usecaseId = self$`usecaseId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`applicationTypeId`)) {
          sprintf(
            '"applicationTypeId":
            "%s"
                  ',
            self$`applicationTypeId`
          )
        },
        if (!is.null(self$`usecaseId`)) {
          sprintf(
            '"usecaseId":
            "%s"
                  ',
            self$`usecaseId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DemoApplicationJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DemoApplicationJson, validateParams = FALSE) {
      DemoApplicationObject <- jsonlite::fromJSON(DemoApplicationJson)
      self$`applicationTypeId` <- DemoApplicationObject$`applicationTypeId`
      self$`usecaseId` <- DemoApplicationObject$`usecaseId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
