# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AnalyzedFeature
#'
#' @description AnalyzedFeature Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field detailsHistogram  list( \link{HistogramDetails} ) Histogram details for the specified feature.
#'
#' @field disparityScore  numeric A number to describe disparity for the feature between the compared classes.
#'
#' @field featureImpact  numeric A feature importance value.
#'
#' @field name  character Name of the feature.
#'
#' @field status  character A status of the feature.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AnalyzedFeature <- R6::R6Class(
  "AnalyzedFeature",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`detailsHistogram` = NULL, `disparityScore` = NULL, `featureImpact` = NULL, `name` = NULL, `status` = NULL) {
      if (!is.null(`detailsHistogram`)) {
        stopifnot(is.vector(`detailsHistogram`))
      }
      if (!is.null(`disparityScore`)) {
      }
      if (!is.null(`featureImpact`)) {
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
    }
  ),
  public = list(
    `detailsHistogram` = NULL,
    `disparityScore` = NULL,
    `featureImpact` = NULL,
    `name` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param detailsHistogram Histogram details for the specified feature.
    #' @param disparityScore A number to describe disparity for the feature between the compared classes.
    #' @param featureImpact A feature importance value.
    #' @param name Name of the feature.
    #' @param status A status of the feature.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`detailsHistogram` = NULL, `disparityScore` = NULL, `featureImpact` = NULL, `name` = NULL, `status` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`detailsHistogram`, `disparityScore`, `featureImpact`, `name`, `status`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(detailsHistogram, disparityScore, featureImpact, name, status)
      }
      sapply(`detailsHistogram`, function(x) stopifnot(R6::is.R6(x)))
      self$`disparityScore` <- `disparityScore`
      self$`featureImpact` <- `featureImpact`
      self$`name` <- `name`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(detailsHistogram = self$`detailsHistogram`, disparityScore = self$`disparityScore`, featureImpact = self$`featureImpact`, name = self$`name`, status = self$`status`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`detailsHistogram`)) {
          sprintf(
            '"detailsHistogram":
            [%s]
      ',
            paste(sapply(self$`detailsHistogram`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`disparityScore`)) {
          sprintf(
            '"disparityScore":
            %d
                  ',
            self$`disparityScore`
          )
        },
        if (!is.null(self$`featureImpact`)) {
          sprintf(
            '"featureImpact":
            %d
                  ',
            self$`featureImpact`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AnalyzedFeatureJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AnalyzedFeatureJson, validateParams = FALSE) {
      AnalyzedFeatureObject <- jsonlite::fromJSON(AnalyzedFeatureJson)
      self$`detailsHistogram` <- ApiClient$new()$deserializeObj(AnalyzedFeatureObject$`detailsHistogram`, "array[HistogramDetails]", loadNamespace("datarobot.apicore"))
      self$`disparityScore` <- AnalyzedFeatureObject$`disparityScore`
      self$`featureImpact` <- AnalyzedFeatureObject$`featureImpact`
      self$`name` <- AnalyzedFeatureObject$`name`
      self$`status` <- AnalyzedFeatureObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
