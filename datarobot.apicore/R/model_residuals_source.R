# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelResidualsSource
#'
#' @description ModelResidualsSource Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field crossValidation  \link{ModelResidualsChartData} [optional]
#'
#' @field holdout  \link{ModelResidualsChartData} [optional]
#'
#' @field validation  \link{ModelResidualsChartData} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelResidualsSource <- R6::R6Class(
  "ModelResidualsSource",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`crossValidation` = NULL, `holdout` = NULL, `validation` = NULL) {
      if (!is.null(`crossValidation`)) {
        stopifnot(R6::is.R6(`crossValidation`))
      }
      if (!is.null(`holdout`)) {
        stopifnot(R6::is.R6(`holdout`))
      }
      if (!is.null(`validation`)) {
        stopifnot(R6::is.R6(`validation`))
      }
    }
  ),
  public = list(
    `crossValidation` = NULL,
    `holdout` = NULL,
    `validation` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param crossValidation
    #' @param holdout
    #' @param validation
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`crossValidation` = NULL, `holdout` = NULL, `validation` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(crossValidation, holdout, validation)
      }
      self$`crossValidation` <- `crossValidation`
      self$`holdout` <- `holdout`
      self$`validation` <- `validation`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(crossValidation = self$`crossValidation`, holdout = self$`holdout`, validation = self$`validation`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`crossValidation`)) {
          sprintf(
            '"crossValidation":
            %s
      ',
            jsonlite::toJSON(self$`crossValidation`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`holdout`)) {
          sprintf(
            '"holdout":
            %s
      ',
            jsonlite::toJSON(self$`holdout`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`validation`)) {
          sprintf(
            '"validation":
            %s
      ',
            jsonlite::toJSON(self$`validation`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelResidualsSourceJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelResidualsSourceJson, validateParams = FALSE) {
      ModelResidualsSourceObject <- jsonlite::fromJSON(ModelResidualsSourceJson)
      self$`crossValidation` <- ModelResidualsChartData$new()$fromJSON(jsonlite::toJSON(ModelResidualsSourceObject$crossValidation, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`holdout` <- ModelResidualsChartData$new()$fromJSON(jsonlite::toJSON(ModelResidualsSourceObject$holdout, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`validation` <- ModelResidualsChartData$new()$fromJSON(jsonlite::toJSON(ModelResidualsSourceObject$validation, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
