# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DataSource
#'
#' @description DataSource Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalog  character [optional] Catalog name of the data source.
#'
#' @field dataSourceId  character [optional] ID of the data source.
#'
#' @field dataStoreId  character ID of the data store.
#'
#' @field dataStoreName  character Name of the data store.
#'
#' @field dbtable  character Table name of the data source.
#'
#' @field schema  character Schema name of the data source.
#'
#' @field url  character URL of the data store.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DataSource <- R6::R6Class(
  "DataSource",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(catalog = NULL, dataSourceId = NULL, dataStoreId = NULL, dataStoreName = NULL, dbtable = NULL, schema = NULL, url = NULL) {
      if (!is.null(`dataStoreId`)) {
        stopifnot(is.character(`dataStoreId`), length(`dataStoreId`) == 1)
      }
      if (!is.null(`dataStoreName`)) {
        stopifnot(is.character(`dataStoreName`), length(`dataStoreName`) == 1)
      }
      if (!is.null(`dbtable`)) {
        stopifnot(is.character(`dbtable`), length(`dbtable`) == 1)
      }
      if (!is.null(`schema`)) {
        stopifnot(is.character(`schema`), length(`schema`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
      if (!is.null(`catalog`)) {
        stopifnot(is.character(`catalog`), length(`catalog`) == 1)
      }
      if (!is.null(`dataSourceId`)) {
        stopifnot(is.character(`dataSourceId`), length(`dataSourceId`) == 1)
      }
    }
  ),
  public = list(
    `catalog` = NULL,
    `dataSourceId` = NULL,
    `dataStoreId` = NULL,
    `dataStoreName` = NULL,
    `dbtable` = NULL,
    `schema` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalog Catalog name of the data source.
    #' @param dataSourceId ID of the data source.
    #' @param dataStoreId ID of the data store.
    #' @param dataStoreName Name of the data store.
    #' @param dbtable Table name of the data source.
    #' @param schema Schema name of the data source.
    #' @param url URL of the data store.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataStoreId` = NULL, `dataStoreName` = NULL, `dbtable` = NULL, `schema` = NULL, `url` = NULL, `catalog` = NULL, `dataSourceId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`dataStoreId`, `dataStoreName`, `dbtable`, `schema`, `url`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(catalog, dataSourceId, dataStoreId, dataStoreName, dbtable, schema, url)
      }
      self$`catalog` <- `catalog`
      self$`dataSourceId` <- `dataSourceId`
      self$`dataStoreId` <- `dataStoreId`
      self$`dataStoreName` <- `dataStoreName`
      self$`dbtable` <- `dbtable`
      self$`schema` <- `schema`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(catalog = self$`catalog`, dataSourceId = self$`dataSourceId`, dataStoreId = self$`dataStoreId`, dataStoreName = self$`dataStoreName`, dbtable = self$`dbtable`, schema = self$`schema`, url = self$`url`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalog`)) {
          sprintf(
            '"catalog":
            "%s"
                  ',
            self$`catalog`
          )
        },
        if (!is.null(self$`dataSourceId`)) {
          sprintf(
            '"dataSourceId":
            "%s"
                  ',
            self$`dataSourceId`
          )
        },
        if (!is.null(self$`dataStoreId`)) {
          sprintf(
            '"dataStoreId":
            "%s"
                  ',
            self$`dataStoreId`
          )
        },
        if (!is.null(self$`dataStoreName`)) {
          sprintf(
            '"dataStoreName":
            "%s"
                  ',
            self$`dataStoreName`
          )
        },
        if (!is.null(self$`dbtable`)) {
          sprintf(
            '"dbtable":
            "%s"
                  ',
            self$`dbtable`
          )
        },
        if (!is.null(self$`schema`)) {
          sprintf(
            '"schema":
            "%s"
                  ',
            self$`schema`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DataSourceJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DataSourceJson, validateParams = FALSE) {
      DataSourceObject <- jsonlite::fromJSON(DataSourceJson)
      self$`catalog` <- DataSourceObject$`catalog`
      self$`dataSourceId` <- DataSourceObject$`dataSourceId`
      self$`dataStoreId` <- DataSourceObject$`dataStoreId`
      self$`dataStoreName` <- DataSourceObject$`dataStoreName`
      self$`dbtable` <- DataSourceObject$`dbtable`
      self$`schema` <- DataSourceObject$`schema`
      self$`url` <- DataSourceObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
