# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureAssociationRetrieveFeatures
#'
#' @description FeatureAssociationRetrieveFeatures Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field alphabeticSortIndex  integer A number representing the alphabetical order of this feature compared to the other features in this dataset.
#'
#' @field clusterId  integer The ID of the feature cluster this feature belongs to.
#'
#' @field clusterName  character The name of feature cluster.
#'
#' @field clusterSortIndex  integer A number representing the ordering of the feature across all feature clusters. Features in the same cluster always have adjacent indices.
#'
#' @field feature  character The name of the feature.
#'
#' @field importanceSortIndex  integer A number ranking the importance of this feature compared to the other features in this dataset.
#'
#' @field strengthSortIndex  integer A number ranking the strength of this feature compared to the other features in this dataset.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureAssociationRetrieveFeatures <- R6::R6Class(
  "FeatureAssociationRetrieveFeatures",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`alphabeticSortIndex` = NULL, `clusterId` = NULL, `clusterName` = NULL, `clusterSortIndex` = NULL, `feature` = NULL, `importanceSortIndex` = NULL, `strengthSortIndex` = NULL) {
      if (!is.null(`alphabeticSortIndex`)) {
        stopifnot(is.numeric(`alphabeticSortIndex`), length(`alphabeticSortIndex`) == 1)
      }
      if (!is.null(`clusterId`)) {
        stopifnot(is.numeric(`clusterId`), length(`clusterId`) == 1)
      }
      if (!is.null(`clusterName`)) {
        stopifnot(is.character(`clusterName`), length(`clusterName`) == 1)
      }
      if (!is.null(`clusterSortIndex`)) {
        stopifnot(is.numeric(`clusterSortIndex`), length(`clusterSortIndex`) == 1)
      }
      if (!is.null(`feature`)) {
        stopifnot(is.character(`feature`), length(`feature`) == 1)
      }
      if (!is.null(`importanceSortIndex`)) {
        stopifnot(is.numeric(`importanceSortIndex`), length(`importanceSortIndex`) == 1)
      }
      if (!is.null(`strengthSortIndex`)) {
        stopifnot(is.numeric(`strengthSortIndex`), length(`strengthSortIndex`) == 1)
      }
    }
  ),
  public = list(
    `alphabeticSortIndex` = NULL,
    `clusterId` = NULL,
    `clusterName` = NULL,
    `clusterSortIndex` = NULL,
    `feature` = NULL,
    `importanceSortIndex` = NULL,
    `strengthSortIndex` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param alphabeticSortIndex A number representing the alphabetical order of this feature compared to the other features in this dataset.
    #' @param clusterId The ID of the feature cluster this feature belongs to.
    #' @param clusterName The name of feature cluster.
    #' @param clusterSortIndex A number representing the ordering of the feature across all feature clusters. Features in the same cluster always have adjacent indices.
    #' @param feature The name of the feature.
    #' @param importanceSortIndex A number ranking the importance of this feature compared to the other features in this dataset.
    #' @param strengthSortIndex A number ranking the strength of this feature compared to the other features in this dataset.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`alphabeticSortIndex` = NULL, `clusterId` = NULL, `clusterName` = NULL, `clusterSortIndex` = NULL, `feature` = NULL, `importanceSortIndex` = NULL, `strengthSortIndex` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`alphabeticSortIndex`, `clusterId`, `clusterName`, `clusterSortIndex`, `feature`, `importanceSortIndex`, `strengthSortIndex`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(alphabeticSortIndex, clusterId, clusterName, clusterSortIndex, feature, importanceSortIndex, strengthSortIndex)
      }
      self$`alphabeticSortIndex` <- `alphabeticSortIndex`
      self$`clusterId` <- `clusterId`
      self$`clusterName` <- `clusterName`
      self$`clusterSortIndex` <- `clusterSortIndex`
      self$`feature` <- `feature`
      self$`importanceSortIndex` <- `importanceSortIndex`
      self$`strengthSortIndex` <- `strengthSortIndex`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(alphabeticSortIndex = self$`alphabeticSortIndex`, clusterId = self$`clusterId`, clusterName = self$`clusterName`, clusterSortIndex = self$`clusterSortIndex`, feature = self$`feature`, importanceSortIndex = self$`importanceSortIndex`, strengthSortIndex = self$`strengthSortIndex`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`alphabeticSortIndex`)) {
          sprintf(
            '"alphabeticSortIndex":
            %d
                  ',
            self$`alphabeticSortIndex`
          )
        },
        if (!is.null(self$`clusterId`)) {
          sprintf(
            '"clusterId":
            %d
                  ',
            self$`clusterId`
          )
        },
        if (!is.null(self$`clusterName`)) {
          sprintf(
            '"clusterName":
            "%s"
                  ',
            self$`clusterName`
          )
        },
        if (!is.null(self$`clusterSortIndex`)) {
          sprintf(
            '"clusterSortIndex":
            %d
                  ',
            self$`clusterSortIndex`
          )
        },
        if (!is.null(self$`feature`)) {
          sprintf(
            '"feature":
            "%s"
                  ',
            self$`feature`
          )
        },
        if (!is.null(self$`importanceSortIndex`)) {
          sprintf(
            '"importanceSortIndex":
            %d
                  ',
            self$`importanceSortIndex`
          )
        },
        if (!is.null(self$`strengthSortIndex`)) {
          sprintf(
            '"strengthSortIndex":
            %d
                  ',
            self$`strengthSortIndex`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureAssociationRetrieveFeaturesJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureAssociationRetrieveFeaturesJson, validateParams = FALSE) {
      FeatureAssociationRetrieveFeaturesObject <- jsonlite::fromJSON(FeatureAssociationRetrieveFeaturesJson)
      self$`alphabeticSortIndex` <- FeatureAssociationRetrieveFeaturesObject$`alphabeticSortIndex`
      self$`clusterId` <- FeatureAssociationRetrieveFeaturesObject$`clusterId`
      self$`clusterName` <- FeatureAssociationRetrieveFeaturesObject$`clusterName`
      self$`clusterSortIndex` <- FeatureAssociationRetrieveFeaturesObject$`clusterSortIndex`
      self$`feature` <- FeatureAssociationRetrieveFeaturesObject$`feature`
      self$`importanceSortIndex` <- FeatureAssociationRetrieveFeaturesObject$`importanceSortIndex`
      self$`strengthSortIndex` <- FeatureAssociationRetrieveFeaturesObject$`strengthSortIndex`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
