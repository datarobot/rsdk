# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NotificationChannelCreate
#'
#' @description NotificationChannelCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field channelType  character The type of the new notification channel.
#'
#' @field contentType  character [optional] The content type of the messages of the new notification channel.
#'
#' @field customHeaders  list( \link{CustomerHeader} ) [optional] Custom headers and their values to be sent in the new notification channel.
#'
#' @field emailAddress  character [optional] The email address to be used in the new notification channel.
#'
#' @field languageCode  character [optional] The preferred language code.
#'
#' @field name  character The name of the new notification channel.
#'
#' @field orgId  character [optional] The id of organization that notification channel belongs to.
#'
#' @field payloadUrl  character [optional] The payload URL of the new notification channel.
#'
#' @field secretToken  character [optional] Secret token to be used for new notification channel.
#'
#' @field validateSsl  character [optional] Defines if  validate ssl or not in the notification channel.
#'
#' @field verificationCode  character [optional] Required if the channel type is Email.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NotificationChannelCreate <- R6::R6Class(
  "NotificationChannelCreate",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(channelType = NULL, contentType = NULL, customHeaders = NULL, emailAddress = NULL, languageCode = NULL, name = NULL, orgId = NULL, payloadUrl = NULL, secretToken = NULL, validateSsl = NULL, verificationCode = NULL) {
      if (!is.null(`channelType`)) {
        stopifnot(is.character(`channelType`), length(`channelType`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`contentType`)) {
        stopifnot(is.character(`contentType`), length(`contentType`) == 1)
      }
      if (!is.null(`customHeaders`)) {
        stopifnot(is.vector(`customHeaders`))
      }
      if (!is.null(`emailAddress`)) {
        stopifnot(is.character(`emailAddress`), length(`emailAddress`) == 1)
      }
      if (!is.null(`languageCode`)) {
        stopifnot(is.character(`languageCode`), length(`languageCode`) == 1)
      }
      if (!is.null(`orgId`)) {
        stopifnot(is.character(`orgId`), length(`orgId`) == 1)
      }
      if (!is.null(`payloadUrl`)) {
        stopifnot(is.character(`payloadUrl`), length(`payloadUrl`) == 1)
      }
      if (!is.null(`secretToken`)) {
        stopifnot(is.character(`secretToken`), length(`secretToken`) == 1)
      }
      if (!is.null(`validateSsl`)) {
        stopifnot(is.logical(`validateSsl`), length(`validateSsl`) == 1)
      }
      if (!is.null(`verificationCode`)) {
        stopifnot(is.character(`verificationCode`), length(`verificationCode`) == 1)
      }
    }
  ),
  public = list(
    `channelType` = NULL,
    `contentType` = NULL,
    `customHeaders` = NULL,
    `emailAddress` = NULL,
    `languageCode` = NULL,
    `name` = NULL,
    `orgId` = NULL,
    `payloadUrl` = NULL,
    `secretToken` = NULL,
    `validateSsl` = NULL,
    `verificationCode` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param channelType The type of the new notification channel.
    #' @param contentType The content type of the messages of the new notification channel.
    #' @param customHeaders Custom headers and their values to be sent in the new notification channel.
    #' @param emailAddress The email address to be used in the new notification channel.
    #' @param languageCode The preferred language code.
    #' @param name The name of the new notification channel.
    #' @param orgId The id of organization that notification channel belongs to.
    #' @param payloadUrl The payload URL of the new notification channel.
    #' @param secretToken Secret token to be used for new notification channel.
    #' @param validateSsl Defines if  validate ssl or not in the notification channel.
    #' @param verificationCode Required if the channel type is Email.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`channelType` = NULL, `name` = NULL, `contentType` = NULL, `customHeaders` = NULL, `emailAddress` = NULL, `languageCode` = NULL, `orgId` = NULL, `payloadUrl` = NULL, `secretToken` = NULL, `validateSsl` = NULL, `verificationCode` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`channelType`, `name`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(channelType, contentType, customHeaders, emailAddress, languageCode, name, orgId, payloadUrl, secretToken, validateSsl, verificationCode)
      }
      self$`channelType` <- `channelType`
      self$`contentType` <- `contentType`
      sapply(`customHeaders`, function(x) stopifnot(R6::is.R6(x)))
      self$`emailAddress` <- `emailAddress`
      self$`languageCode` <- `languageCode`
      self$`name` <- `name`
      self$`orgId` <- `orgId`
      self$`payloadUrl` <- `payloadUrl`
      self$`secretToken` <- `secretToken`
      self$`validateSsl` <- `validateSsl`
      self$`verificationCode` <- `verificationCode`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(channelType = self$`channelType`, contentType = self$`contentType`, customHeaders = self$`customHeaders`, emailAddress = self$`emailAddress`, languageCode = self$`languageCode`, name = self$`name`, orgId = self$`orgId`, payloadUrl = self$`payloadUrl`, secretToken = self$`secretToken`, validateSsl = self$`validateSsl`, verificationCode = self$`verificationCode`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`channelType`)) {
          sprintf(
            '"channelType":
            "%s"
                  ',
            self$`channelType`
          )
        },
        if (!is.null(self$`contentType`)) {
          sprintf(
            '"contentType":
            "%s"
                  ',
            self$`contentType`
          )
        },
        if (!is.null(self$`customHeaders`)) {
          sprintf(
            '"customHeaders":
            [%s]
',
            paste(sapply(self$`customHeaders`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`emailAddress`)) {
          sprintf(
            '"emailAddress":
            "%s"
                  ',
            self$`emailAddress`
          )
        },
        if (!is.null(self$`languageCode`)) {
          sprintf(
            '"languageCode":
            "%s"
                  ',
            self$`languageCode`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`orgId`)) {
          sprintf(
            '"orgId":
            "%s"
                  ',
            self$`orgId`
          )
        },
        if (!is.null(self$`payloadUrl`)) {
          sprintf(
            '"payloadUrl":
            "%s"
                  ',
            self$`payloadUrl`
          )
        },
        if (!is.null(self$`secretToken`)) {
          sprintf(
            '"secretToken":
            "%s"
                  ',
            self$`secretToken`
          )
        },
        if (!is.null(self$`validateSsl`)) {
          sprintf(
            '"validateSsl":
            %s
                  ',
            tolower(self$`validateSsl`)
          )
        },
        if (!is.null(self$`verificationCode`)) {
          sprintf(
            '"verificationCode":
            "%s"
                  ',
            self$`verificationCode`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param NotificationChannelCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(NotificationChannelCreateJson, validateParams = FALSE) {
      NotificationChannelCreateObject <- jsonlite::fromJSON(NotificationChannelCreateJson)
      self$`channelType` <- NotificationChannelCreateObject$`channelType`
      self$`contentType` <- NotificationChannelCreateObject$`contentType`
      self$`customHeaders` <- ApiClient$new()$deserializeObj(NotificationChannelCreateObject$`customHeaders`, "array[CustomerHeader]", loadNamespace("datarobot.apicore"))
      self$`emailAddress` <- NotificationChannelCreateObject$`emailAddress`
      self$`languageCode` <- NotificationChannelCreateObject$`languageCode`
      self$`name` <- NotificationChannelCreateObject$`name`
      self$`orgId` <- NotificationChannelCreateObject$`orgId`
      self$`payloadUrl` <- NotificationChannelCreateObject$`payloadUrl`
      self$`secretToken` <- NotificationChannelCreateObject$`secretToken`
      self$`validateSsl` <- NotificationChannelCreateObject$`validateSsl`
      self$`verificationCode` <- NotificationChannelCreateObject$`verificationCode`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
