# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelResponse
#'
#' @description ModelResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetName  character Name of dataset used to train challenger model
#'
#' @field description  character Description of the model.
#'
#' @field executionType  character Type of the current model.
#'
#' @field id  character ID of the current model.
#'
#' @field name  character Name of the model.
#'
#' @field projectId  character Project ID of the current model.
#'
#' @field projectName  character Project name of the current model.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelResponse <- R6::R6Class(
  "ModelResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datasetName = NULL, description = NULL, executionType = NULL, id = NULL, name = NULL, projectId = NULL, projectName = NULL) {
      if (!is.null(`datasetName`)) {
        stopifnot(is.character(`datasetName`), length(`datasetName`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`executionType`)) {
        stopifnot(is.character(`executionType`), length(`executionType`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`projectName`)) {
        stopifnot(is.character(`projectName`), length(`projectName`) == 1)
      }
    }
  ),
  public = list(
    `datasetName` = NULL,
    `description` = NULL,
    `executionType` = NULL,
    `id` = NULL,
    `name` = NULL,
    `projectId` = NULL,
    `projectName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetName Name of dataset used to train challenger model
    #' @param description Description of the model.
    #' @param executionType Type of the current model.
    #' @param id ID of the current model.
    #' @param name Name of the model.
    #' @param projectId Project ID of the current model.
    #' @param projectName Project name of the current model.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetName` = NULL, `description` = NULL, `executionType` = NULL, `id` = NULL, `name` = NULL, `projectId` = NULL, `projectName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasetName`, `description`, `executionType`, `id`, `name`, `projectId`, `projectName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datasetName, description, executionType, id, name, projectId, projectName)
      }
      self$`datasetName` <- `datasetName`
      self$`description` <- `description`
      self$`executionType` <- `executionType`
      self$`id` <- `id`
      self$`name` <- `name`
      self$`projectId` <- `projectId`
      self$`projectName` <- `projectName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datasetName = self$`datasetName`, description = self$`description`, executionType = self$`executionType`, id = self$`id`, name = self$`name`, projectId = self$`projectId`, projectName = self$`projectName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetName`)) {
          sprintf(
            '"datasetName":
            "%s"
                  ',
            self$`datasetName`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`executionType`)) {
          sprintf(
            '"executionType":
            "%s"
                  ',
            self$`executionType`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`projectName`)) {
          sprintf(
            '"projectName":
            "%s"
                  ',
            self$`projectName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelResponseJson, validateParams = FALSE) {
      ModelResponseObject <- jsonlite::fromJSON(ModelResponseJson)
      self$`datasetName` <- ModelResponseObject$`datasetName`
      self$`description` <- ModelResponseObject$`description`
      self$`executionType` <- ModelResponseObject$`executionType`
      self$`id` <- ModelResponseObject$`id`
      self$`name` <- ModelResponseObject$`name`
      self$`projectId` <- ModelResponseObject$`projectId`
      self$`projectName` <- ModelResponseObject$`projectName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
