# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureMetricsResponse
#'
#' @description FeatureMetricsResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field availableMetrics  list( character ) an array of strings representing the appropriate metrics.  If the feature cannot be selected as the target, then this array will be empty.
#'
#' @field featureName  character the name of the feature that was looked up
#'
#' @field metricDetails  list( \link{FeatureMetricDetailsResponse} ) the list of metricDetails objects.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureMetricsResponse <- R6::R6Class(
  "FeatureMetricsResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`availableMetrics` = NULL, `featureName` = NULL, `metricDetails` = NULL) {
      if (!is.null(`availableMetrics`)) {
        stopifnot(is.vector(`availableMetrics`))
      }
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`metricDetails`)) {
        stopifnot(is.vector(`metricDetails`))
      }
    }
  ),
  public = list(
    `availableMetrics` = NULL,
    `featureName` = NULL,
    `metricDetails` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param availableMetrics an array of strings representing the appropriate metrics.  If the feature cannot be selected as the target, then this array will be empty.
    #' @param featureName the name of the feature that was looked up
    #' @param metricDetails the list of metricDetails objects.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`availableMetrics` = NULL, `featureName` = NULL, `metricDetails` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`availableMetrics`, `featureName`, `metricDetails`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(availableMetrics, featureName, metricDetails)
      }
      sapply(`availableMetrics`, function(x) stopifnot(is.character(x)))
      self$`featureName` <- `featureName`
      sapply(`metricDetails`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(availableMetrics = self$`availableMetrics`, featureName = self$`featureName`, metricDetails = self$`metricDetails`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`availableMetrics`)) {
          sprintf(
            '"availableMetrics":
            [%s]
                  ',
            paste(unlist(lapply(self$`availableMetrics`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`metricDetails`)) {
          sprintf(
            '"metricDetails":
            [%s]
      ',
            paste(sapply(self$`metricDetails`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureMetricsResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureMetricsResponseJson, validateParams = FALSE) {
      FeatureMetricsResponseObject <- jsonlite::fromJSON(FeatureMetricsResponseJson)
      self$`availableMetrics` <- ApiClient$new()$deserializeObj(FeatureMetricsResponseObject$`availableMetrics`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`featureName` <- FeatureMetricsResponseObject$`featureName`
      self$`metricDetails` <- ApiClient$new()$deserializeObj(FeatureMetricsResponseObject$`metricDetails`, "array[FeatureMetricDetailsResponse]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
