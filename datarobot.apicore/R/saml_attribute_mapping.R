# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SamlAttributeMapping
#'
#' @description SamlAttributeMapping Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field displayName  character [optional] Display name.
#'
#' @field email  character [optional] Email.
#'
#' @field firstName  character [optional] First name.
#'
#' @field group  character [optional] Group.
#'
#' @field impersonationUser  character [optional] Impersonation user.
#'
#' @field lastName  character [optional] Last name.
#'
#' @field role  character [optional] Role.
#'
#' @field username  character [optional] Username.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SamlAttributeMapping <- R6::R6Class(
  "SamlAttributeMapping",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(displayName = NULL, email = NULL, firstName = NULL, group = NULL, impersonationUser = NULL, lastName = NULL, role = NULL, username = NULL) {
      if (!is.null(`displayName`)) {
        stopifnot(is.character(`displayName`), length(`displayName`) == 1)
      }
      if (!is.null(`email`)) {
        stopifnot(is.character(`email`), length(`email`) == 1)
      }
      if (!is.null(`firstName`)) {
        stopifnot(is.character(`firstName`), length(`firstName`) == 1)
      }
      if (!is.null(`group`)) {
        stopifnot(is.character(`group`), length(`group`) == 1)
      }
      if (!is.null(`impersonationUser`)) {
        stopifnot(is.character(`impersonationUser`), length(`impersonationUser`) == 1)
      }
      if (!is.null(`lastName`)) {
        stopifnot(is.character(`lastName`), length(`lastName`) == 1)
      }
      if (!is.null(`role`)) {
        stopifnot(is.character(`role`), length(`role`) == 1)
      }
      if (!is.null(`username`)) {
        stopifnot(is.character(`username`), length(`username`) == 1)
      }
    }
  ),
  public = list(
    `displayName` = NULL,
    `email` = NULL,
    `firstName` = NULL,
    `group` = NULL,
    `impersonationUser` = NULL,
    `lastName` = NULL,
    `role` = NULL,
    `username` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param displayName Display name.
    #' @param email Email.
    #' @param firstName First name.
    #' @param group Group.
    #' @param impersonationUser Impersonation user.
    #' @param lastName Last name.
    #' @param role Role.
    #' @param username Username.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`displayName` = NULL, `email` = NULL, `firstName` = NULL, `group` = NULL, `impersonationUser` = NULL, `lastName` = NULL, `role` = NULL, `username` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(displayName, email, firstName, group, impersonationUser, lastName, role, username)
      }
      self$`displayName` <- `displayName`
      self$`email` <- `email`
      self$`firstName` <- `firstName`
      self$`group` <- `group`
      self$`impersonationUser` <- `impersonationUser`
      self$`lastName` <- `lastName`
      self$`role` <- `role`
      self$`username` <- `username`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(displayName = self$`displayName`, email = self$`email`, firstName = self$`firstName`, group = self$`group`, impersonationUser = self$`impersonationUser`, lastName = self$`lastName`, role = self$`role`, username = self$`username`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`displayName`)) {
          sprintf(
            '"displayName":
            "%s"
                  ',
            self$`displayName`
          )
        },
        if (!is.null(self$`email`)) {
          sprintf(
            '"email":
            "%s"
                  ',
            self$`email`
          )
        },
        if (!is.null(self$`firstName`)) {
          sprintf(
            '"firstName":
            "%s"
                  ',
            self$`firstName`
          )
        },
        if (!is.null(self$`group`)) {
          sprintf(
            '"group":
            "%s"
                  ',
            self$`group`
          )
        },
        if (!is.null(self$`impersonationUser`)) {
          sprintf(
            '"impersonationUser":
            "%s"
                  ',
            self$`impersonationUser`
          )
        },
        if (!is.null(self$`lastName`)) {
          sprintf(
            '"lastName":
            "%s"
                  ',
            self$`lastName`
          )
        },
        if (!is.null(self$`role`)) {
          sprintf(
            '"role":
            "%s"
                  ',
            self$`role`
          )
        },
        if (!is.null(self$`username`)) {
          sprintf(
            '"username":
            "%s"
                  ',
            self$`username`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SamlAttributeMappingJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SamlAttributeMappingJson, validateParams = FALSE) {
      SamlAttributeMappingObject <- jsonlite::fromJSON(SamlAttributeMappingJson)
      self$`displayName` <- SamlAttributeMappingObject$`displayName`
      self$`email` <- SamlAttributeMappingObject$`email`
      self$`firstName` <- SamlAttributeMappingObject$`firstName`
      self$`group` <- SamlAttributeMappingObject$`group`
      self$`impersonationUser` <- SamlAttributeMappingObject$`impersonationUser`
      self$`lastName` <- SamlAttributeMappingObject$`lastName`
      self$`role` <- SamlAttributeMappingObject$`role`
      self$`username` <- SamlAttributeMappingObject$`username`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
