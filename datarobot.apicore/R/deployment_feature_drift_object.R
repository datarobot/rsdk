# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentFeatureDriftObject
#'
#' @description DeploymentFeatureDriftObject Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field baselineSampleSize  integer The sample size of the training data.
#'
#' @field driftScore  numeric The drift score for this feature.
#'
#' @field featureImpact  numeric The feature impact score for this feature.
#'
#' @field name  character The name of the feature.
#'
#' @field sampleSize  integer The number of predictions used to compute the drift score.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentFeatureDriftObject <- R6::R6Class(
  "DeploymentFeatureDriftObject",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(baselineSampleSize = NULL, driftScore = NULL, featureImpact = NULL, name = NULL, sampleSize = NULL) {
      if (!is.null(`baselineSampleSize`)) {
        stopifnot(is.numeric(`baselineSampleSize`), length(`baselineSampleSize`) == 1)
      }
      if (!is.null(`driftScore`)) {
      }
      if (!is.null(`featureImpact`)) {
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`sampleSize`)) {
        stopifnot(is.numeric(`sampleSize`), length(`sampleSize`) == 1)
      }
    }
  ),
  public = list(
    `baselineSampleSize` = NULL,
    `driftScore` = NULL,
    `featureImpact` = NULL,
    `name` = NULL,
    `sampleSize` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param baselineSampleSize The sample size of the training data.
    #' @param driftScore The drift score for this feature.
    #' @param featureImpact The feature impact score for this feature.
    #' @param name The name of the feature.
    #' @param sampleSize The number of predictions used to compute the drift score.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`baselineSampleSize` = NULL, `driftScore` = NULL, `featureImpact` = NULL, `name` = NULL, `sampleSize` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`baselineSampleSize`, `driftScore`, `featureImpact`, `name`, `sampleSize`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(baselineSampleSize, driftScore, featureImpact, name, sampleSize)
      }
      self$`baselineSampleSize` <- `baselineSampleSize`
      self$`driftScore` <- `driftScore`
      self$`featureImpact` <- `featureImpact`
      self$`name` <- `name`
      self$`sampleSize` <- `sampleSize`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(baselineSampleSize = self$`baselineSampleSize`, driftScore = self$`driftScore`, featureImpact = self$`featureImpact`, name = self$`name`, sampleSize = self$`sampleSize`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`baselineSampleSize`)) {
          sprintf(
            '"baselineSampleSize":
            %d
                  ',
            self$`baselineSampleSize`
          )
        },
        if (!is.null(self$`driftScore`)) {
          sprintf(
            '"driftScore":
            %d
                  ',
            self$`driftScore`
          )
        },
        if (!is.null(self$`featureImpact`)) {
          sprintf(
            '"featureImpact":
            %d
                  ',
            self$`featureImpact`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`sampleSize`)) {
          sprintf(
            '"sampleSize":
            %d
                  ',
            self$`sampleSize`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentFeatureDriftObjectJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentFeatureDriftObjectJson, validateParams = FALSE) {
      DeploymentFeatureDriftObjectObject <- jsonlite::fromJSON(DeploymentFeatureDriftObjectJson)
      self$`baselineSampleSize` <- DeploymentFeatureDriftObjectObject$`baselineSampleSize`
      self$`driftScore` <- DeploymentFeatureDriftObjectObject$`driftScore`
      self$`featureImpact` <- DeploymentFeatureDriftObjectObject$`featureImpact`
      self$`name` <- DeploymentFeatureDriftObjectObject$`name`
      self$`sampleSize` <- DeploymentFeatureDriftObjectObject$`sampleSize`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
