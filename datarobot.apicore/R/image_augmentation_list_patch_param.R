# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ImageAugmentationListPatchParam
#'
#' @description ImageAugmentationListPatchParam Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featureName  character [optional] The name of the image feature containing the data to be augmented
#'
#' @field inUse  character [optional] This is set to true when the Augmentation List has been used to train a model
#'
#' @field initialList  character [optional] Whether this list will be used during autopilot to perform image augmentation
#'
#' @field name  character [optional] The name of the image augmentation list
#'
#' @field numberOfNewImages  integer [optional] Number of new rows to add for each existing row
#'
#' @field projectId  character [optional] The project containing the image data to be augmented
#'
#' @field transformationProbability  numeric [optional] Probability that each enabled transformation will be applied to an image. This does not apply to Horizontal or Vertical Flip, which are set to 50% always.
#'
#' @field transformations  list( \link{Transformation} ) [optional] List of Transformations to possibly apply to each image
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ImageAugmentationListPatchParam <- R6::R6Class(
  "ImageAugmentationListPatchParam",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(featureName = NULL, inUse = NULL, initialList = NULL, name = NULL, numberOfNewImages = NULL, projectId = NULL, transformationProbability = NULL, transformations = NULL) {
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`inUse`)) {
        stopifnot(is.logical(`inUse`), length(`inUse`) == 1)
      }
      if (!is.null(`initialList`)) {
        stopifnot(is.logical(`initialList`), length(`initialList`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`numberOfNewImages`)) {
        stopifnot(is.numeric(`numberOfNewImages`), length(`numberOfNewImages`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`transformationProbability`)) {
        stopifnot(is.numeric(`transformationProbability`), length(`transformationProbability`) == 1)
      }
      if (!is.null(`transformations`)) {
        stopifnot(is.vector(`transformations`))
      }
    }
  ),
  public = list(
    `featureName` = NULL,
    `inUse` = NULL,
    `initialList` = NULL,
    `name` = NULL,
    `numberOfNewImages` = NULL,
    `projectId` = NULL,
    `transformationProbability` = NULL,
    `transformations` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featureName The name of the image feature containing the data to be augmented
    #' @param inUse This is set to true when the Augmentation List has been used to train a model
    #' @param initialList Whether this list will be used during autopilot to perform image augmentation
    #' @param name The name of the image augmentation list
    #' @param numberOfNewImages Number of new rows to add for each existing row
    #' @param projectId The project containing the image data to be augmented
    #' @param transformationProbability Probability that each enabled transformation will be applied to an image. This does not apply to Horizontal or Vertical Flip, which are set to 50% always.
    #' @param transformations List of Transformations to possibly apply to each image
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featureName` = NULL, `inUse` = NULL, `initialList` = NULL, `name` = NULL, `numberOfNewImages` = NULL, `projectId` = NULL, `transformationProbability` = NULL, `transformations` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(featureName, inUse, initialList, name, numberOfNewImages, projectId, transformationProbability, transformations)
      }
      self$`featureName` <- `featureName`
      self$`inUse` <- `inUse`
      self$`initialList` <- `initialList`
      self$`name` <- `name`
      self$`numberOfNewImages` <- `numberOfNewImages`
      self$`projectId` <- `projectId`
      self$`transformationProbability` <- `transformationProbability`
      sapply(`transformations`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(featureName = self$`featureName`, inUse = self$`inUse`, initialList = self$`initialList`, name = self$`name`, numberOfNewImages = self$`numberOfNewImages`, projectId = self$`projectId`, transformationProbability = self$`transformationProbability`, transformations = self$`transformations`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`inUse`)) {
          sprintf(
            '"inUse":
            %s
                  ',
            tolower(self$`inUse`)
          )
        },
        if (!is.null(self$`initialList`)) {
          sprintf(
            '"initialList":
            %s
                  ',
            tolower(self$`initialList`)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`numberOfNewImages`)) {
          sprintf(
            '"numberOfNewImages":
            %d
                  ',
            self$`numberOfNewImages`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`transformationProbability`)) {
          sprintf(
            '"transformationProbability":
            %d
                  ',
            self$`transformationProbability`
          )
        },
        if (!is.null(self$`transformations`)) {
          sprintf(
            '"transformations":
            [%s]
',
            paste(sapply(self$`transformations`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ImageAugmentationListPatchParamJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ImageAugmentationListPatchParamJson, validateParams = FALSE) {
      ImageAugmentationListPatchParamObject <- jsonlite::fromJSON(ImageAugmentationListPatchParamJson)
      self$`featureName` <- ImageAugmentationListPatchParamObject$`featureName`
      self$`inUse` <- ImageAugmentationListPatchParamObject$`inUse`
      self$`initialList` <- ImageAugmentationListPatchParamObject$`initialList`
      self$`name` <- ImageAugmentationListPatchParamObject$`name`
      self$`numberOfNewImages` <- ImageAugmentationListPatchParamObject$`numberOfNewImages`
      self$`projectId` <- ImageAugmentationListPatchParamObject$`projectId`
      self$`transformationProbability` <- ImageAugmentationListPatchParamObject$`transformationProbability`
      self$`transformations` <- ApiClient$new()$deserializeObj(ImageAugmentationListPatchParamObject$`transformations`, "array[Transformation]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
