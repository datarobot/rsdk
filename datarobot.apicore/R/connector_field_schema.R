# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ConnectorFieldSchema
#'
#' @description ConnectorFieldSchema Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field choices  list( character ) If non-empty, a list of all possible values for a parameter.
#'
#' @field default  character Default value of the connector parameter.
#'
#' @field description  character Description of the parameter.
#'
#' @field name  character Name of the connector parameter.
#'
#' @field required  character Whether or not the parameter is required for a connection.
#'
#' @field visibleByDefault  character Whether or not the parameter should be shown in the UI by default.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ConnectorFieldSchema <- R6::R6Class(
  "ConnectorFieldSchema",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(choices = NULL, default = NULL, description = NULL, name = NULL, required = NULL, visibleByDefault = NULL) {
      if (!is.null(`choices`)) {
        stopifnot(is.vector(`choices`))
      }
      if (!is.null(`default`)) {
        stopifnot(is.character(`default`), length(`default`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`required`)) {
        stopifnot(is.logical(`required`), length(`required`) == 1)
      }
      if (!is.null(`visibleByDefault`)) {
        stopifnot(is.logical(`visibleByDefault`), length(`visibleByDefault`) == 1)
      }
    }
  ),
  public = list(
    `choices` = NULL,
    `default` = NULL,
    `description` = NULL,
    `name` = NULL,
    `required` = NULL,
    `visibleByDefault` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param choices If non-empty, a list of all possible values for a parameter.
    #' @param default Default value of the connector parameter.
    #' @param description Description of the parameter.
    #' @param name Name of the connector parameter.
    #' @param required Whether or not the parameter is required for a connection.
    #' @param visibleByDefault Whether or not the parameter should be shown in the UI by default.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`choices` = NULL, `default` = NULL, `description` = NULL, `name` = NULL, `required` = NULL, `visibleByDefault` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`choices`, `default`, `description`, `name`, `required`, `visibleByDefault`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(choices, default, description, name, required, visibleByDefault)
      }
      sapply(`choices`, function(x) stopifnot(is.character(x)))
      self$`default` <- `default`
      self$`description` <- `description`
      self$`name` <- `name`
      self$`required` <- `required`
      self$`visibleByDefault` <- `visibleByDefault`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(choices = self$`choices`, default = self$`default`, description = self$`description`, name = self$`name`, required = self$`required`, visibleByDefault = self$`visibleByDefault`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`choices`)) {
          sprintf(
            '"choices":
              [%s]
            ',
            paste(unlist(lapply(self$`choices`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`default`)) {
          sprintf(
            '"default":
            "%s"
                  ',
            self$`default`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`required`)) {
          sprintf(
            '"required":
            %s
                  ',
            tolower(self$`required`)
          )
        },
        if (!is.null(self$`visibleByDefault`)) {
          sprintf(
            '"visibleByDefault":
            %s
                  ',
            tolower(self$`visibleByDefault`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ConnectorFieldSchemaJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ConnectorFieldSchemaJson, validateParams = FALSE) {
      ConnectorFieldSchemaObject <- jsonlite::fromJSON(ConnectorFieldSchemaJson)
      self$`choices` <- ApiClient$new()$deserializeObj(ConnectorFieldSchemaObject$`choices`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`default` <- ConnectorFieldSchemaObject$`default`
      self$`description` <- ConnectorFieldSchemaObject$`description`
      self$`name` <- ConnectorFieldSchemaObject$`name`
      self$`required` <- ConnectorFieldSchemaObject$`required`
      self$`visibleByDefault` <- ConnectorFieldSchemaObject$`visibleByDefault`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
