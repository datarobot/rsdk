# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TrainingHistoryEntry
#'
#' @description TrainingHistoryEntry Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field creationDate  character ISO-8601 timestamp of when the project the blueprint was trained on was created.
#'
#' @field lid  character The leaderboard ID the blueprint was trained on.
#'
#' @field pid  character The project ID the blueprint was trained on.
#'
#' @field projectModelsCount  integer Number of models in the project the blueprint was trained on.
#'
#' @field projectName  character The project name the blueprint was trained on.
#'
#' @field targetName  character The target name of the project the blueprint was trained on.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TrainingHistoryEntry <- R6::R6Class(
  "TrainingHistoryEntry",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(creationDate = NULL, lid = NULL, pid = NULL, projectModelsCount = NULL, projectName = NULL, targetName = NULL) {
      if (!is.null(`creationDate`)) {
        stopifnot(is.character(`creationDate`), length(`creationDate`) == 1)
      }
      if (!is.null(`lid`)) {
        stopifnot(is.character(`lid`), length(`lid`) == 1)
      }
      if (!is.null(`pid`)) {
        stopifnot(is.character(`pid`), length(`pid`) == 1)
      }
      if (!is.null(`projectModelsCount`)) {
        stopifnot(is.numeric(`projectModelsCount`), length(`projectModelsCount`) == 1)
      }
      if (!is.null(`projectName`)) {
        stopifnot(is.character(`projectName`), length(`projectName`) == 1)
      }
      if (!is.null(`targetName`)) {
        stopifnot(is.character(`targetName`), length(`targetName`) == 1)
      }
    }
  ),
  public = list(
    `creationDate` = NULL,
    `lid` = NULL,
    `pid` = NULL,
    `projectModelsCount` = NULL,
    `projectName` = NULL,
    `targetName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param creationDate ISO-8601 timestamp of when the project the blueprint was trained on was created.
    #' @param lid The leaderboard ID the blueprint was trained on.
    #' @param pid The project ID the blueprint was trained on.
    #' @param projectModelsCount Number of models in the project the blueprint was trained on.
    #' @param projectName The project name the blueprint was trained on.
    #' @param targetName The target name of the project the blueprint was trained on.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`creationDate` = NULL, `lid` = NULL, `pid` = NULL, `projectModelsCount` = NULL, `projectName` = NULL, `targetName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`creationDate`, `lid`, `pid`, `projectModelsCount`, `projectName`, `targetName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(creationDate, lid, pid, projectModelsCount, projectName, targetName)
      }
      self$`creationDate` <- `creationDate`
      self$`lid` <- `lid`
      self$`pid` <- `pid`
      self$`projectModelsCount` <- `projectModelsCount`
      self$`projectName` <- `projectName`
      self$`targetName` <- `targetName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(creationDate = self$`creationDate`, lid = self$`lid`, pid = self$`pid`, projectModelsCount = self$`projectModelsCount`, projectName = self$`projectName`, targetName = self$`targetName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`creationDate`)) {
          sprintf(
            '"creationDate":
            "%s"
                  ',
            self$`creationDate`
          )
        },
        if (!is.null(self$`lid`)) {
          sprintf(
            '"lid":
            "%s"
                  ',
            self$`lid`
          )
        },
        if (!is.null(self$`pid`)) {
          sprintf(
            '"pid":
            "%s"
                  ',
            self$`pid`
          )
        },
        if (!is.null(self$`projectModelsCount`)) {
          sprintf(
            '"projectModelsCount":
            %d
                  ',
            self$`projectModelsCount`
          )
        },
        if (!is.null(self$`projectName`)) {
          sprintf(
            '"projectName":
            "%s"
                  ',
            self$`projectName`
          )
        },
        if (!is.null(self$`targetName`)) {
          sprintf(
            '"targetName":
            "%s"
                  ',
            self$`targetName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TrainingHistoryEntryJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TrainingHistoryEntryJson, validateParams = FALSE) {
      TrainingHistoryEntryObject <- jsonlite::fromJSON(TrainingHistoryEntryJson)
      self$`creationDate` <- TrainingHistoryEntryObject$`creationDate`
      self$`lid` <- TrainingHistoryEntryObject$`lid`
      self$`pid` <- TrainingHistoryEntryObject$`pid`
      self$`projectModelsCount` <- TrainingHistoryEntryObject$`projectModelsCount`
      self$`projectName` <- TrainingHistoryEntryObject$`projectName`
      self$`targetName` <- TrainingHistoryEntryObject$`targetName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
