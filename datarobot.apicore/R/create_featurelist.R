# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CreateFeaturelist
#'
#' @description CreateFeaturelist Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field features  list( character ) List of features for new featurelist.
#'
#' @field name  character New featurelist name.
#'
#' @field skipDatetimePartitionColumn  character [optional] Whether featurelist should contain datetime partition column.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CreateFeaturelist <- R6::R6Class(
  "CreateFeaturelist",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`features` = NULL, `name` = NULL, `skipDatetimePartitionColumn` = NULL) {
      if (!is.null(`features`)) {
        stopifnot(is.vector(`features`), sapply(`features`, is.character))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`skipDatetimePartitionColumn`)) {
        stopifnot(is.logical(`skipDatetimePartitionColumn`), length(`skipDatetimePartitionColumn`) == 1)
      }
    }
  ),
  public = list(
    `features` = NULL,
    `name` = NULL,
    `skipDatetimePartitionColumn` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param features List of features for new featurelist.
    #' @param name New featurelist name.
    #' @param skipDatetimePartitionColumn Whether featurelist should contain datetime partition column.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`features` = NULL, `name` = NULL, `skipDatetimePartitionColumn` = FALSE, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`features`, `name`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(features, name, skipDatetimePartitionColumn)
      }
      self$`features` <- `features`
      self$`name` <- `name`
      self$`skipDatetimePartitionColumn` <- `skipDatetimePartitionColumn`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(features = self$`features`, name = self$`name`, skipDatetimePartitionColumn = self$`skipDatetimePartitionColumn`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`features`)) {
          sprintf(
            '"features":
            [%s]
                  ',
            paste(unlist(lapply(self$`features`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`skipDatetimePartitionColumn`)) {
          sprintf(
            '"skipDatetimePartitionColumn":
            %s
                  ',
            tolower(self$`skipDatetimePartitionColumn`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CreateFeaturelistJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CreateFeaturelistJson, validateParams = FALSE) {
      CreateFeaturelistObject <- jsonlite::fromJSON(CreateFeaturelistJson)
      self$`features` <- ApiClient$new()$deserializeObj(CreateFeaturelistObject$`features`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`name` <- CreateFeaturelistObject$`name`
      self$`skipDatetimePartitionColumn` <- CreateFeaturelistObject$`skipDatetimePartitionColumn`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
