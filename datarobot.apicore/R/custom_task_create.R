# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CustomTaskCreate
#'
#' @description CustomTaskCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field calibratePredictions  character [optional] Whether model predictions should be calibrated by DataRobot.Only applies to anomaly detection; we recommend this if you have not already included calibration in your model code.Calibration improves the probability estimates of a model, and modifies the predictions of non-probabilistic models to be interpretable as probabilities. This will facilitate comparison to DataRobot models, and give access to ROC curve insights on external data.
#'
#' @field description  character [optional] The user-friendly description of the task.
#'
#' @field language  character [optional] Programming language name in which task is written.
#'
#' @field maximumMemory  integer [optional] The maximum memory that might be allocated by the custom-model. If exceeded, the custom-model will be killed
#'
#' @field name  character The user-friendly name for the task.
#'
#' @field targetType  character The target type of the custom task
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CustomTaskCreate <- R6::R6Class(
  "CustomTaskCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`calibratePredictions` = NULL, `description` = NULL, `language` = NULL, `maximumMemory` = NULL, `name` = NULL, `targetType` = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`targetType`)) {
        stopifnot(is.character(`targetType`), length(`targetType`) == 1)
      }
      if (!is.null(`calibratePredictions`)) {
        stopifnot(is.logical(`calibratePredictions`), length(`calibratePredictions`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`language`)) {
        stopifnot(is.character(`language`), length(`language`) == 1)
      }
      if (!is.null(`maximumMemory`)) {
        stopifnot(is.numeric(`maximumMemory`), length(`maximumMemory`) == 1)
      }
    }
  ),
  public = list(
    `calibratePredictions` = NULL,
    `description` = NULL,
    `language` = NULL,
    `maximumMemory` = NULL,
    `name` = NULL,
    `targetType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param calibratePredictions Whether model predictions should be calibrated by DataRobot.Only applies to anomaly detection; we recommend this if you have not already included calibration in your model code.Calibration improves the probability estimates of a model, and modifies the predictions of non-probabilistic models to be interpretable as probabilities. This will facilitate comparison to DataRobot models, and give access to ROC curve insights on external data.
    #' @param description The user-friendly description of the task.
    #' @param language Programming language name in which task is written.
    #' @param maximumMemory The maximum memory that might be allocated by the custom-model. If exceeded, the custom-model will be killed
    #' @param name The user-friendly name for the task.
    #' @param targetType The target type of the custom task
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `targetType` = NULL, `calibratePredictions` = TRUE, `description` = NULL, `language` = NULL, `maximumMemory` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`name`, `targetType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(calibratePredictions, description, language, maximumMemory, name, targetType)
      }
      self$`calibratePredictions` <- `calibratePredictions`
      self$`description` <- `description`
      self$`language` <- `language`
      self$`maximumMemory` <- `maximumMemory`
      self$`name` <- `name`
      self$`targetType` <- `targetType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(calibratePredictions = self$`calibratePredictions`, description = self$`description`, language = self$`language`, maximumMemory = self$`maximumMemory`, name = self$`name`, targetType = self$`targetType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`calibratePredictions`)) {
          sprintf(
            '"calibratePredictions":
            %s
                  ',
            tolower(self$`calibratePredictions`)
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`language`)) {
          sprintf(
            '"language":
            "%s"
                  ',
            self$`language`
          )
        },
        if (!is.null(self$`maximumMemory`)) {
          sprintf(
            '"maximumMemory":
            %d
                  ',
            self$`maximumMemory`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`targetType`)) {
          sprintf(
            '"targetType":
            "%s"
                  ',
            self$`targetType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CustomTaskCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CustomTaskCreateJson, validateParams = FALSE) {
      CustomTaskCreateObject <- jsonlite::fromJSON(CustomTaskCreateJson)
      self$`calibratePredictions` <- CustomTaskCreateObject$`calibratePredictions`
      self$`description` <- CustomTaskCreateObject$`description`
      self$`language` <- CustomTaskCreateObject$`language`
      self$`maximumMemory` <- CustomTaskCreateObject$`maximumMemory`
      self$`name` <- CustomTaskCreateObject$`name`
      self$`targetType` <- CustomTaskCreateObject$`targetType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
