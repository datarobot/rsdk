# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserOperations
#'
#' @description UserOperations Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field canCancel  character Whether the user can cancel the Change Request.
#'
#' @field canComment  character Whether the user can create commenting review on the Change Request.
#'
#' @field canResolve  character Whether the user can resolve the Change Request.
#'
#' @field canReview  character Whether the user can review (approve or request updates) the Change Request.
#'
#' @field canUpdate  character Whether the user can update the Change Request.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserOperations <- R6::R6Class(
  "UserOperations",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(canCancel = NULL, canComment = NULL, canResolve = NULL, canReview = NULL, canUpdate = NULL) {
      if (!is.null(`canCancel`)) {
        stopifnot(is.logical(`canCancel`), length(`canCancel`) == 1)
      }
      if (!is.null(`canComment`)) {
        stopifnot(is.logical(`canComment`), length(`canComment`) == 1)
      }
      if (!is.null(`canResolve`)) {
        stopifnot(is.logical(`canResolve`), length(`canResolve`) == 1)
      }
      if (!is.null(`canReview`)) {
        stopifnot(is.logical(`canReview`), length(`canReview`) == 1)
      }
      if (!is.null(`canUpdate`)) {
        stopifnot(is.logical(`canUpdate`), length(`canUpdate`) == 1)
      }
    }
  ),
  public = list(
    `canCancel` = NULL,
    `canComment` = NULL,
    `canResolve` = NULL,
    `canReview` = NULL,
    `canUpdate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param canCancel Whether the user can cancel the Change Request.
    #' @param canComment Whether the user can create commenting review on the Change Request.
    #' @param canResolve Whether the user can resolve the Change Request.
    #' @param canReview Whether the user can review (approve or request updates) the Change Request.
    #' @param canUpdate Whether the user can update the Change Request.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`canCancel` = NULL, `canComment` = NULL, `canResolve` = NULL, `canReview` = NULL, `canUpdate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`canCancel`, `canComment`, `canResolve`, `canReview`, `canUpdate`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(canCancel, canComment, canResolve, canReview, canUpdate)
      }
      self$`canCancel` <- `canCancel`
      self$`canComment` <- `canComment`
      self$`canResolve` <- `canResolve`
      self$`canReview` <- `canReview`
      self$`canUpdate` <- `canUpdate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(canCancel = self$`canCancel`, canComment = self$`canComment`, canResolve = self$`canResolve`, canReview = self$`canReview`, canUpdate = self$`canUpdate`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`canCancel`)) {
          sprintf(
            '"canCancel":
            %s
                  ',
            tolower(self$`canCancel`)
          )
        },
        if (!is.null(self$`canComment`)) {
          sprintf(
            '"canComment":
            %s
                  ',
            tolower(self$`canComment`)
          )
        },
        if (!is.null(self$`canResolve`)) {
          sprintf(
            '"canResolve":
            %s
                  ',
            tolower(self$`canResolve`)
          )
        },
        if (!is.null(self$`canReview`)) {
          sprintf(
            '"canReview":
            %s
                  ',
            tolower(self$`canReview`)
          )
        },
        if (!is.null(self$`canUpdate`)) {
          sprintf(
            '"canUpdate":
            %s
                  ',
            tolower(self$`canUpdate`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserOperationsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserOperationsJson, validateParams = FALSE) {
      UserOperationsObject <- jsonlite::fromJSON(UserOperationsJson)
      self$`canCancel` <- UserOperationsObject$`canCancel`
      self$`canComment` <- UserOperationsObject$`canComment`
      self$`canResolve` <- UserOperationsObject$`canResolve`
      self$`canReview` <- UserOperationsObject$`canReview`
      self$`canUpdate` <- UserOperationsObject$`canUpdate`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
