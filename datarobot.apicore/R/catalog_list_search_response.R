# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CatalogListSearchResponse
#'
#' @description CatalogListSearchResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field cacheHit  character Indicates if the catalog item is returned from the cache.
#'
#' @field count  integer Number of catalog items returned on this page.
#'
#' @field data  list( \link{CatalogExtendedDetailsResponse} ) Detailed information for every found catalog item.
#'
#' @field next_  character Location of the next page.
#'
#' @field previous  character Location of the previous page.
#'
#' @field totalCount  integer [optional] Total number of catalog items.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CatalogListSearchResponse <- R6::R6Class(
  "CatalogListSearchResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`cacheHit` = NULL, `count` = NULL, `data` = NULL, `next_` = NULL, `previous` = NULL, `totalCount` = NULL) {
      if (!is.null(`cacheHit`)) {
        stopifnot(is.logical(`cacheHit`), length(`cacheHit`) == 1)
      }
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`data`)) {
        stopifnot(is.vector(`data`), sapply(`data`, R6::is.R6))
      }
      if (!is.null(`next_`)) {
        stopifnot(is.character(`next_`), length(`next_`) == 1)
      }
      if (!is.null(`previous`)) {
        stopifnot(is.character(`previous`), length(`previous`) == 1)
      }
      if (!is.null(`totalCount`)) {
        stopifnot(is.numeric(`totalCount`), length(`totalCount`) == 1)
      }
    }
  ),
  public = list(
    `cacheHit` = NULL,
    `count` = NULL,
    `data` = NULL,
    `next_` = NULL,
    `previous` = NULL,
    `totalCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param cacheHit Indicates if the catalog item is returned from the cache.
    #' @param count Number of catalog items returned on this page.
    #' @param data Detailed information for every found catalog item.
    #' @param next_ Location of the next page.
    #' @param previous Location of the previous page.
    #' @param totalCount Total number of catalog items.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`cacheHit` = NULL, `count` = NULL, `data` = NULL, `next_` = NULL, `previous` = NULL, `totalCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`cacheHit`, `count`, `data`, `next_`, `previous`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(cacheHit, count, data, next_, previous, totalCount)
      }
      self$`cacheHit` <- `cacheHit`
      self$`count` <- `count`
      self$`data` <- `data`
      self$`next_` <- `next_`
      self$`previous` <- `previous`
      self$`totalCount` <- `totalCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(cacheHit = self$`cacheHit`, count = self$`count`, data = self$`data`, next_ = self$`next_`, previous = self$`previous`, totalCount = self$`totalCount`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`cacheHit`)) {
          sprintf(
            '"cacheHit":
            %s
                  ',
            tolower(self$`cacheHit`)
          )
        },
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
      ',
            paste(sapply(self$`data`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`next_`)) {
          sprintf(
            '"next":
            "%s"
                  ',
            self$`next_`
          )
        },
        if (!is.null(self$`previous`)) {
          sprintf(
            '"previous":
            "%s"
                  ',
            self$`previous`
          )
        },
        if (!is.null(self$`totalCount`)) {
          sprintf(
            '"totalCount":
            %d
                  ',
            self$`totalCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CatalogListSearchResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CatalogListSearchResponseJson, validateParams = FALSE) {
      CatalogListSearchResponseObject <- jsonlite::fromJSON(CatalogListSearchResponseJson)
      self$`cacheHit` <- CatalogListSearchResponseObject$`cacheHit`
      self$`count` <- CatalogListSearchResponseObject$`count`
      self$`data` <- ApiClient$new()$deserializeObj(CatalogListSearchResponseObject$`data`, "array[CatalogExtendedDetailsResponse]", loadNamespace("datarobot.apicore"))
      self$`next_` <- CatalogListSearchResponseObject$`next`
      self$`previous` <- CatalogListSearchResponseObject$`previous`
      self$`totalCount` <- CatalogListSearchResponseObject$`totalCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
