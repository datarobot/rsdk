# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelPackageModelDescription
#'
#' @description ModelPackageModelDescription Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field buildEnvironmentType  character build environment type of the model
#'
#' @field description  character a description of the model
#'
#' @field location  character location of the model
#'
#' @field modelName  character [optional] model name
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelPackageModelDescription <- R6::R6Class(
  "ModelPackageModelDescription",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(buildEnvironmentType = NULL, description = NULL, location = NULL, modelName = NULL) {
      if (!is.null(`buildEnvironmentType`)) {
        stopifnot(is.character(`buildEnvironmentType`), length(`buildEnvironmentType`) == 1)
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`location`)) {
        stopifnot(is.character(`location`), length(`location`) == 1)
      }
      if (!is.null(`modelName`)) {
        stopifnot(is.character(`modelName`), length(`modelName`) == 1)
      }
    }
  ),
  public = list(
    `buildEnvironmentType` = NULL,
    `description` = NULL,
    `location` = NULL,
    `modelName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param buildEnvironmentType build environment type of the model
    #' @param description a description of the model
    #' @param location location of the model
    #' @param modelName model name
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`buildEnvironmentType` = NULL, `description` = NULL, `location` = NULL, `modelName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`buildEnvironmentType`, `description`, `location`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(buildEnvironmentType, description, location, modelName)
      }
      self$`buildEnvironmentType` <- `buildEnvironmentType`
      self$`description` <- `description`
      self$`location` <- `location`
      self$`modelName` <- `modelName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(buildEnvironmentType = self$`buildEnvironmentType`, description = self$`description`, location = self$`location`, modelName = self$`modelName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`buildEnvironmentType`)) {
          sprintf(
            '"buildEnvironmentType":
            "%s"
                  ',
            self$`buildEnvironmentType`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`location`)) {
          sprintf(
            '"location":
            "%s"
                  ',
            self$`location`
          )
        },
        if (!is.null(self$`modelName`)) {
          sprintf(
            '"modelName":
            "%s"
                  ',
            self$`modelName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelPackageModelDescriptionJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelPackageModelDescriptionJson, validateParams = FALSE) {
      ModelPackageModelDescriptionObject <- jsonlite::fromJSON(ModelPackageModelDescriptionJson)
      self$`buildEnvironmentType` <- ModelPackageModelDescriptionObject$`buildEnvironmentType`
      self$`description` <- ModelPackageModelDescriptionObject$`description`
      self$`location` <- ModelPackageModelDescriptionObject$`location`
      self$`modelName` <- ModelPackageModelDescriptionObject$`modelName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
