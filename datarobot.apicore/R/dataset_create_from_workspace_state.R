# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetCreateFromWorkspaceState
#'
#' @description DatasetCreateFromWorkspaceState Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field credentials  character [optional] JDBC credentials.
#'
#' @field datasetName  character [optional] Custom name for the created dataset.
#'
#' @field doSnapshot  character [optional] If true, create a snapshot dataset; if false, create a remote dataset. Creating snapshots from non-file sources requires an additional permission, &#x60;Enable Create Snapshot Data Source&#x60;.
#'
#' @field workspaceStateId  character ID of the workspace state to use as the source of data.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetCreateFromWorkspaceState <- R6::R6Class(
  "DatasetCreateFromWorkspaceState",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(credentials = NULL, datasetName = NULL, doSnapshot = NULL, workspaceStateId = NULL) {
      if (!is.null(`workspaceStateId`)) {
        stopifnot(is.character(`workspaceStateId`), length(`workspaceStateId`) == 1)
      }
      if (!is.null(`credentials`)) {
        stopifnot(is.character(`credentials`), length(`credentials`) == 1)
      }
      if (!is.null(`datasetName`)) {
        stopifnot(is.character(`datasetName`), length(`datasetName`) == 1)
      }
      if (!is.null(`doSnapshot`)) {
        stopifnot(is.logical(`doSnapshot`), length(`doSnapshot`) == 1)
      }
    }
  ),
  public = list(
    `credentials` = NULL,
    `datasetName` = NULL,
    `doSnapshot` = NULL,
    `workspaceStateId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param credentials JDBC credentials.
    #' @param datasetName Custom name for the created dataset.
    #' @param doSnapshot If true, create a snapshot dataset; if false, create a remote dataset. Creating snapshots from non-file sources requires an additional permission, &#x60;Enable Create Snapshot Data Source&#x60;.
    #' @param workspaceStateId ID of the workspace state to use as the source of data.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`workspaceStateId` = NULL, `credentials` = NULL, `datasetName` = NULL, `doSnapshot` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`workspaceStateId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(credentials, datasetName, doSnapshot, workspaceStateId)
      }
      self$`credentials` <- `credentials`
      self$`datasetName` <- `datasetName`
      self$`doSnapshot` <- `doSnapshot`
      self$`workspaceStateId` <- `workspaceStateId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(credentials = self$`credentials`, datasetName = self$`datasetName`, doSnapshot = self$`doSnapshot`, workspaceStateId = self$`workspaceStateId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`credentials`)) {
          sprintf(
            '"credentials":
            "%s"
                  ',
            self$`credentials`
          )
        },
        if (!is.null(self$`datasetName`)) {
          sprintf(
            '"datasetName":
            "%s"
                  ',
            self$`datasetName`
          )
        },
        if (!is.null(self$`doSnapshot`)) {
          sprintf(
            '"doSnapshot":
            %s
                  ',
            tolower(self$`doSnapshot`)
          )
        },
        if (!is.null(self$`workspaceStateId`)) {
          sprintf(
            '"workspaceStateId":
            "%s"
                  ',
            self$`workspaceStateId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetCreateFromWorkspaceStateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetCreateFromWorkspaceStateJson, validateParams = FALSE) {
      DatasetCreateFromWorkspaceStateObject <- jsonlite::fromJSON(DatasetCreateFromWorkspaceStateJson)
      self$`credentials` <- DatasetCreateFromWorkspaceStateObject$`credentials`
      self$`datasetName` <- DatasetCreateFromWorkspaceStateObject$`datasetName`
      self$`doSnapshot` <- DatasetCreateFromWorkspaceStateObject$`doSnapshot`
      self$`workspaceStateId` <- DatasetCreateFromWorkspaceStateObject$`workspaceStateId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
