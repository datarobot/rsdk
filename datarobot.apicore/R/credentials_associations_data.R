# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialsAssociationsData
#'
#' @description CredentialsAssociationsData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field isDefault  character [optional] Whether this credential association with the given object is default for given session user.
#'
#' @field link  character Link to get more details about associated object.
#'
#' @field objectId  character Associated object ID.
#'
#' @field objectType  character Associated object type.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsAssociationsData <- R6::R6Class(
  "CredentialsAssociationsData",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`isDefault` = NULL, `link` = NULL, `objectId` = NULL, `objectType` = NULL) {
      if (!is.null(`link`)) {
        stopifnot(is.character(`link`), length(`link`) == 1)
      }
      if (!is.null(`objectId`)) {
        stopifnot(is.character(`objectId`), length(`objectId`) == 1)
      }
      if (!is.null(`objectType`)) {
        stopifnot(is.character(`objectType`), length(`objectType`) == 1)
      }
      if (!is.null(`isDefault`)) {
        stopifnot(is.logical(`isDefault`), length(`isDefault`) == 1)
      }
    }
  ),
  public = list(
    `isDefault` = NULL,
    `link` = NULL,
    `objectId` = NULL,
    `objectType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param isDefault Whether this credential association with the given object is default for given session user.
    #' @param link Link to get more details about associated object.
    #' @param objectId Associated object ID.
    #' @param objectType Associated object type.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`link` = NULL, `objectId` = NULL, `objectType` = NULL, `isDefault` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`link`, `objectId`, `objectType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(isDefault, link, objectId, objectType)
      }
      self$`isDefault` <- `isDefault`
      self$`link` <- `link`
      self$`objectId` <- `objectId`
      self$`objectType` <- `objectType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(isDefault = self$`isDefault`, link = self$`link`, objectId = self$`objectId`, objectType = self$`objectType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`isDefault`)) {
          sprintf(
            '"isDefault":
            %s
                  ',
            tolower(self$`isDefault`)
          )
        },
        if (!is.null(self$`link`)) {
          sprintf(
            '"link":
            "%s"
                  ',
            self$`link`
          )
        },
        if (!is.null(self$`objectId`)) {
          sprintf(
            '"objectId":
            "%s"
                  ',
            self$`objectId`
          )
        },
        if (!is.null(self$`objectType`)) {
          sprintf(
            '"objectType":
            "%s"
                  ',
            self$`objectType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialsAssociationsDataJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialsAssociationsDataJson, validateParams = FALSE) {
      CredentialsAssociationsDataObject <- jsonlite::fromJSON(CredentialsAssociationsDataJson)
      self$`isDefault` <- CredentialsAssociationsDataObject$`isDefault`
      self$`link` <- CredentialsAssociationsDataObject$`link`
      self$`objectId` <- CredentialsAssociationsDataObject$`objectId`
      self$`objectType` <- CredentialsAssociationsDataObject$`objectType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
