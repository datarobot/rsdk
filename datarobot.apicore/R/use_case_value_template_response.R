# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseValueTemplateResponse
#'
#' @description UseCaseValueTemplateResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field description  character Description of Value Template
#'
#' @field schema  list( \link{UseCaseValueTemplateParameterWithSchema} ) Schema definition of all template parameters
#'
#' @field templateType  character The name of the use case value template to be retrieved
#'
#' @field title  character Title of Value Template
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseValueTemplateResponse <- R6::R6Class(
  "UseCaseValueTemplateResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`description` = NULL, `schema` = NULL, `templateType` = NULL, `title` = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`schema`)) {
        stopifnot(is.vector(`schema`), sapply(`schema`, R6::is.R6))
      }
      if (!is.null(`templateType`)) {
        stopifnot(is.character(`templateType`), length(`templateType`) == 1)
      }
      if (!is.null(`title`)) {
        stopifnot(is.character(`title`), length(`title`) == 1)
      }
    }
  ),
  public = list(
    `description` = NULL,
    `schema` = NULL,
    `templateType` = NULL,
    `title` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param description Description of Value Template
    #' @param schema Schema definition of all template parameters
    #' @param templateType The name of the use case value template to be retrieved
    #' @param title Title of Value Template
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `schema` = NULL, `templateType` = NULL, `title` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`description`, `schema`, `templateType`, `title`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(description, schema, templateType, title)
      }
      self$`description` <- `description`
      self$`schema` <- `schema`
      self$`templateType` <- `templateType`
      self$`title` <- `title`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(description = self$`description`, schema = self$`schema`, templateType = self$`templateType`, title = self$`title`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`schema`)) {
          sprintf(
            '"schema":
            [%s]
      ',
            paste(sapply(self$`schema`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`templateType`)) {
          sprintf(
            '"templateType":
            "%s"
                  ',
            self$`templateType`
          )
        },
        if (!is.null(self$`title`)) {
          sprintf(
            '"title":
            "%s"
                  ',
            self$`title`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseValueTemplateResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseValueTemplateResponseJson, validateParams = FALSE) {
      UseCaseValueTemplateResponseObject <- jsonlite::fromJSON(UseCaseValueTemplateResponseJson)
      self$`description` <- UseCaseValueTemplateResponseObject$`description`
      self$`schema` <- ApiClient$new()$deserializeObj(UseCaseValueTemplateResponseObject$`schema`, "array[UseCaseValueTemplateParameterWithSchema]", loadNamespace("datarobot.apicore"))
      self$`templateType` <- UseCaseValueTemplateResponseObject$`templateType`
      self$`title` <- UseCaseValueTemplateResponseObject$`title`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
