# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelConfusionChartMetadataRetrieveResponse
#'
#' @description ModelConfusionChartMetadataRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field classNames  list( character ) List of all class names in the full confusion matrix, sorted by the &#x60;orderBy&#x60; parameter
#'
#' @field relevantClassesPositions  list( \link{array[integer]} ) Matrix to highlight important cell blocks in the confusion chart.  Intended to represent a thumbnail view, where the relevantClassesPositions array has a 1 in thumbnail cells that are of interest, and 0 otherwise.  The dimensions of the implied thumbnail will not match those of the confusion matrix, e.g. a twenty-class confusion matrix may have a 2x2 thumbnail.
#'
#' @field source  character Source of the chart.
#'
#' @field totalMatrixSum  integer Sum of all values in the full confusion matrix (equal to the number of points considered)
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelConfusionChartMetadataRetrieveResponse <- R6::R6Class(
  "ModelConfusionChartMetadataRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`classNames` = NULL, `relevantClassesPositions` = NULL, `source` = NULL, `totalMatrixSum` = NULL) {
      if (!is.null(`classNames`)) {
        stopifnot(is.vector(`classNames`))
      }
      if (!is.null(`relevantClassesPositions`)) {
        stopifnot(is.vector(`relevantClassesPositions`))
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
      if (!is.null(`totalMatrixSum`)) {
        stopifnot(is.numeric(`totalMatrixSum`), length(`totalMatrixSum`) == 1)
      }
    }
  ),
  public = list(
    `classNames` = NULL,
    `relevantClassesPositions` = NULL,
    `source` = NULL,
    `totalMatrixSum` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param classNames List of all class names in the full confusion matrix, sorted by the &#x60;orderBy&#x60; parameter
    #' @param relevantClassesPositions Matrix to highlight important cell blocks in the confusion chart.  Intended to represent a thumbnail view, where the relevantClassesPositions array has a 1 in thumbnail cells that are of interest, and 0 otherwise.  The dimensions of the implied thumbnail will not match those of the confusion matrix, e.g. a twenty-class confusion matrix may have a 2x2 thumbnail.
    #' @param source Source of the chart.
    #' @param totalMatrixSum Sum of all values in the full confusion matrix (equal to the number of points considered)
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`classNames` = NULL, `relevantClassesPositions` = NULL, `source` = NULL, `totalMatrixSum` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`classNames`, `relevantClassesPositions`, `source`, `totalMatrixSum`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(classNames, relevantClassesPositions, source, totalMatrixSum)
      }
      sapply(`classNames`, function(x) stopifnot(is.character(x)))
      sapply(`relevantClassesPositions`, function(x) stopifnot(R6::is.R6(x)))
      self$`source` <- `source`
      self$`totalMatrixSum` <- `totalMatrixSum`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(classNames = self$`classNames`, relevantClassesPositions = self$`relevantClassesPositions`, source = self$`source`, totalMatrixSum = self$`totalMatrixSum`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`classNames`)) {
          sprintf(
            '"classNames":
            [%s]
                  ',
            paste(unlist(lapply(self$`classNames`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`relevantClassesPositions`)) {
          sprintf(
            '"relevantClassesPositions":
            [%s]
      ',
            paste(sapply(self$`relevantClassesPositions`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        },
        if (!is.null(self$`totalMatrixSum`)) {
          sprintf(
            '"totalMatrixSum":
            %d
                  ',
            self$`totalMatrixSum`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelConfusionChartMetadataRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelConfusionChartMetadataRetrieveResponseJson, validateParams = FALSE) {
      ModelConfusionChartMetadataRetrieveResponseObject <- jsonlite::fromJSON(ModelConfusionChartMetadataRetrieveResponseJson)
      self$`classNames` <- ApiClient$new()$deserializeObj(ModelConfusionChartMetadataRetrieveResponseObject$`classNames`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`relevantClassesPositions` <- ApiClient$new()$deserializeObj(ModelConfusionChartMetadataRetrieveResponseObject$`relevantClassesPositions`, "array[array[integer]]", loadNamespace("datarobot.apicore"))
      self$`source` <- ModelConfusionChartMetadataRetrieveResponseObject$`source`
      self$`totalMatrixSum` <- ModelConfusionChartMetadataRetrieveResponseObject$`totalMatrixSum`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
