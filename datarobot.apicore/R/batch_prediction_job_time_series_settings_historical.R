# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BatchPredictionJobTimeSeriesSettingsHistorical
#'
#' @description BatchPredictionJobTimeSeriesSettingsHistorical Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field predictionsEndDate  character [optional] Used for historical predictions in order to override date to which predictions should be calculated. By default value will be inferred automatically from the dataset.
#'
#' @field predictionsStartDate  character [optional] Used for historical predictions in order to override date from which predictions should be calculated. By default value will be inferred automatically from the dataset.
#'
#' @field relaxKnownInAdvanceFeaturesCheck  character [optional] If activated, missing values in the known in advance features are allowed in the forecast window at prediction time. If omitted or false, missing values are not allowed.
#'
#' @field type  character Historical mode enables bulk predictions which calculates predictions for all possible forecast points and forecast distances in the dataset within the predictionsStartDate/predictionsEndDate range.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BatchPredictionJobTimeSeriesSettingsHistorical <- R6::R6Class(
  "BatchPredictionJobTimeSeriesSettingsHistorical",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(predictionsEndDate = NULL, predictionsStartDate = NULL, relaxKnownInAdvanceFeaturesCheck = NULL, type = NULL) {
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`predictionsEndDate`)) {
        stopifnot(inherits(`predictionsEndDate`, "POSIXt"))
      }
      if (!is.null(`predictionsStartDate`)) {
        stopifnot(inherits(`predictionsStartDate`, "POSIXt"))
      }
      if (!is.null(`relaxKnownInAdvanceFeaturesCheck`)) {
        stopifnot(is.logical(`relaxKnownInAdvanceFeaturesCheck`), length(`relaxKnownInAdvanceFeaturesCheck`) == 1)
      }
    }
  ),
  public = list(
    `predictionsEndDate` = NULL,
    `predictionsStartDate` = NULL,
    `relaxKnownInAdvanceFeaturesCheck` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param predictionsEndDate Used for historical predictions in order to override date to which predictions should be calculated. By default value will be inferred automatically from the dataset.
    #' @param predictionsStartDate Used for historical predictions in order to override date from which predictions should be calculated. By default value will be inferred automatically from the dataset.
    #' @param relaxKnownInAdvanceFeaturesCheck If activated, missing values in the known in advance features are allowed in the forecast window at prediction time. If omitted or false, missing values are not allowed.
    #' @param type Historical mode enables bulk predictions which calculates predictions for all possible forecast points and forecast distances in the dataset within the predictionsStartDate/predictionsEndDate range.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`type` = NULL, `predictionsEndDate` = NULL, `predictionsStartDate` = NULL, `relaxKnownInAdvanceFeaturesCheck` = FALSE, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(predictionsEndDate, predictionsStartDate, relaxKnownInAdvanceFeaturesCheck, type)
      }
      self$`predictionsEndDate` <- `predictionsEndDate`
      self$`predictionsStartDate` <- `predictionsStartDate`
      self$`relaxKnownInAdvanceFeaturesCheck` <- `relaxKnownInAdvanceFeaturesCheck`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(predictionsEndDate = self$`predictionsEndDate`, predictionsStartDate = self$`predictionsStartDate`, relaxKnownInAdvanceFeaturesCheck = self$`relaxKnownInAdvanceFeaturesCheck`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`predictionsEndDate`)) {
          sprintf(
            '"predictionsEndDate":
            "%s"
                  ',
            format(self$`predictionsEndDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`predictionsStartDate`)) {
          sprintf(
            '"predictionsStartDate":
            "%s"
                  ',
            format(self$`predictionsStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`relaxKnownInAdvanceFeaturesCheck`)) {
          sprintf(
            '"relaxKnownInAdvanceFeaturesCheck":
            %s
                  ',
            tolower(self$`relaxKnownInAdvanceFeaturesCheck`)
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BatchPredictionJobTimeSeriesSettingsHistoricalJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BatchPredictionJobTimeSeriesSettingsHistoricalJson, validateParams = FALSE) {
      BatchPredictionJobTimeSeriesSettingsHistoricalObject <- jsonlite::fromJSON(BatchPredictionJobTimeSeriesSettingsHistoricalJson)
      self$`predictionsEndDate` <- ParseRFC3339Timestamp(BatchPredictionJobTimeSeriesSettingsHistoricalObject$`predictionsEndDate`)
      self$`predictionsStartDate` <- ParseRFC3339Timestamp(BatchPredictionJobTimeSeriesSettingsHistoricalObject$`predictionsStartDate`)
      self$`relaxKnownInAdvanceFeaturesCheck` <- BatchPredictionJobTimeSeriesSettingsHistoricalObject$`relaxKnownInAdvanceFeaturesCheck`
      self$`type` <- BatchPredictionJobTimeSeriesSettingsHistoricalObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
