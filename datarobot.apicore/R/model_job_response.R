# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelJobResponse
#'
#' @description ModelJobResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field blueprintId  character The blueprint used by the model - note that this is not an ObjectId.
#'
#' @field featurelistId  character The ID of the featurelist the model is using.
#'
#' @field id  character The job ID.
#'
#' @field isBlocked  character True if a job is waiting for its dependencies to be resolved first.
#'
#' @field modelCategory  character Indicates what kind of model this is. Will be &#x60;&#x60;combined&#x60;&#x60; for combined models.
#'
#' @field modelId  character The ID of the model
#'
#' @field modelType  character The type of model used by the job.
#'
#' @field processes  list( character ) List of processes the modeling job includes
#'
#' @field projectId  character The project the job belongs to.
#'
#' @field samplePct  numeric The percentage of the dataset the job is using.
#'
#' @field status  character The status of the job.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelJobResponse <- R6::R6Class(
  "ModelJobResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`blueprintId` = NULL, `featurelistId` = NULL, `id` = NULL, `isBlocked` = NULL, `modelCategory` = NULL, `modelId` = NULL, `modelType` = NULL, `processes` = NULL, `projectId` = NULL, `samplePct` = NULL, `status` = NULL) {
      if (!is.null(`blueprintId`)) {
        stopifnot(is.character(`blueprintId`), length(`blueprintId`) == 1)
      }
      if (!is.null(`featurelistId`)) {
        stopifnot(is.character(`featurelistId`), length(`featurelistId`) == 1)
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`isBlocked`)) {
        stopifnot(is.logical(`isBlocked`), length(`isBlocked`) == 1)
      }
      if (!is.null(`modelCategory`)) {
        stopifnot(is.character(`modelCategory`), length(`modelCategory`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`modelType`)) {
        stopifnot(is.character(`modelType`), length(`modelType`) == 1)
      }
      if (!is.null(`processes`)) {
        stopifnot(is.vector(`processes`), sapply(`processes`, is.character))
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`samplePct`)) {
      }
      if (!is.null(`status`)) {
        stopifnot(is.character(`status`), length(`status`) == 1)
      }
    }
  ),
  public = list(
    `blueprintId` = NULL,
    `featurelistId` = NULL,
    `id` = NULL,
    `isBlocked` = NULL,
    `modelCategory` = NULL,
    `modelId` = NULL,
    `modelType` = NULL,
    `processes` = NULL,
    `projectId` = NULL,
    `samplePct` = NULL,
    `status` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param blueprintId The blueprint used by the model - note that this is not an ObjectId.
    #' @param featurelistId The ID of the featurelist the model is using.
    #' @param id The job ID.
    #' @param isBlocked True if a job is waiting for its dependencies to be resolved first.
    #' @param modelCategory Indicates what kind of model this is. Will be &#x60;&#x60;combined&#x60;&#x60; for combined models.
    #' @param modelId The ID of the model
    #' @param modelType The type of model used by the job.
    #' @param processes List of processes the modeling job includes
    #' @param projectId The project the job belongs to.
    #' @param samplePct The percentage of the dataset the job is using.
    #' @param status The status of the job.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`blueprintId` = NULL, `featurelistId` = NULL, `id` = NULL, `isBlocked` = NULL, `modelCategory` = NULL, `modelId` = NULL, `modelType` = NULL, `processes` = NULL, `projectId` = NULL, `samplePct` = NULL, `status` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`blueprintId`, `featurelistId`, `id`, `isBlocked`, `modelCategory`, `modelId`, `modelType`, `processes`, `projectId`, `samplePct`, `status`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(blueprintId, featurelistId, id, isBlocked, modelCategory, modelId, modelType, processes, projectId, samplePct, status)
      }
      self$`blueprintId` <- `blueprintId`
      self$`featurelistId` <- `featurelistId`
      self$`id` <- `id`
      self$`isBlocked` <- `isBlocked`
      self$`modelCategory` <- `modelCategory`
      self$`modelId` <- `modelId`
      self$`modelType` <- `modelType`
      self$`processes` <- `processes`
      self$`projectId` <- `projectId`
      self$`samplePct` <- `samplePct`
      self$`status` <- `status`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(blueprintId = self$`blueprintId`, featurelistId = self$`featurelistId`, id = self$`id`, isBlocked = self$`isBlocked`, modelCategory = self$`modelCategory`, modelId = self$`modelId`, modelType = self$`modelType`, processes = self$`processes`, projectId = self$`projectId`, samplePct = self$`samplePct`, status = self$`status`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`blueprintId`)) {
          sprintf(
            '"blueprintId":
            "%s"
                  ',
            self$`blueprintId`
          )
        },
        if (!is.null(self$`featurelistId`)) {
          sprintf(
            '"featurelistId":
            "%s"
                  ',
            self$`featurelistId`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`isBlocked`)) {
          sprintf(
            '"isBlocked":
            %s
                  ',
            tolower(self$`isBlocked`)
          )
        },
        if (!is.null(self$`modelCategory`)) {
          sprintf(
            '"modelCategory":
            "%s"
                  ',
            self$`modelCategory`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`modelType`)) {
          sprintf(
            '"modelType":
            "%s"
                  ',
            self$`modelType`
          )
        },
        if (!is.null(self$`processes`)) {
          sprintf(
            '"processes":
            [%s]
                  ',
            paste(unlist(lapply(self$`processes`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`samplePct`)) {
          sprintf(
            '"samplePct":
            %d
                  ',
            self$`samplePct`
          )
        },
        if (!is.null(self$`status`)) {
          sprintf(
            '"status":
            "%s"
                  ',
            self$`status`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelJobResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelJobResponseJson, validateParams = FALSE) {
      ModelJobResponseObject <- jsonlite::fromJSON(ModelJobResponseJson)
      self$`blueprintId` <- ModelJobResponseObject$`blueprintId`
      self$`featurelistId` <- ModelJobResponseObject$`featurelistId`
      self$`id` <- ModelJobResponseObject$`id`
      self$`isBlocked` <- ModelJobResponseObject$`isBlocked`
      self$`modelCategory` <- ModelJobResponseObject$`modelCategory`
      self$`modelId` <- ModelJobResponseObject$`modelId`
      self$`modelType` <- ModelJobResponseObject$`modelType`
      self$`processes` <- ApiClient$new()$deserializeObj(ModelJobResponseObject$`processes`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`projectId` <- ModelJobResponseObject$`projectId`
      self$`samplePct` <- ModelJobResponseObject$`samplePct`
      self$`status` <- ModelJobResponseObject$`status`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
