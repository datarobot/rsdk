# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Select
#'
#' @description Select Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field supportsGridSearch  character When True, Grid Search is supported for this parameter.
#'
#' @field values  list( character ) List of valid values for this field.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Select <- R6::R6Class(
  "Select",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(supportsGridSearch = NULL, values = NULL) {
      if (!is.null(`supportsGridSearch`)) {
        stopifnot(is.logical(`supportsGridSearch`), length(`supportsGridSearch`) == 1)
      }
      if (!is.null(`values`)) {
        stopifnot(is.vector(`values`))
      }
    }
  ),
  public = list(
    `supportsGridSearch` = NULL,
    `values` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param supportsGridSearch When True, Grid Search is supported for this parameter.
    #' @param values List of valid values for this field.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`supportsGridSearch` = NULL, `values` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`supportsGridSearch`, `values`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(supportsGridSearch, values)
      }
      self$`supportsGridSearch` <- `supportsGridSearch`
      sapply(`values`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(supportsGridSearch = self$`supportsGridSearch`, values = self$`values`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`supportsGridSearch`)) {
          sprintf(
            '"supportsGridSearch":
            %s
                  ',
            tolower(self$`supportsGridSearch`)
          )
        },
        if (!is.null(self$`values`)) {
          sprintf(
            '"values":
              [%s]
            ',
            paste(unlist(lapply(self$`values`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param SelectJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(SelectJson, validateParams = FALSE) {
      SelectObject <- jsonlite::fromJSON(SelectJson)
      self$`supportsGridSearch` <- SelectObject$`supportsGridSearch`
      self$`values` <- ApiClient$new()$deserializeObj(SelectObject$`values`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
