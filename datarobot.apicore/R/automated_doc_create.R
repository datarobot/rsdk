# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AutomatedDocCreate
#'
#' @description AutomatedDocCreate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field documentType  character Type of the automated document you want to generate.
#'
#' @field documentTypeSpecificParameters  \link{OneOfAutomatedDocCreateDeploymentReport} [optional] Set parameters unique for a specific document type. Currently, only these document types can have document-specific parameters: [&#39;DEPLOYMENT_REPORT&#39;]
#'
#' @field entityId  character ID of the entity to generate the document for. It can be a model ID, a project ID.
#'
#' @field locale  character [optional] Localization of the document. Defaults to EN_US.
#'
#' @field outputFormat  character Format to generate the document in.
#'
#' @field templateId  character [optional] Template ID to use for the document outline. Defaults to standard Datarobot template.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AutomatedDocCreate <- R6::R6Class(
  "AutomatedDocCreate",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`documentType` = NULL, `documentTypeSpecificParameters` = NULL, `entityId` = NULL, `locale` = NULL, `outputFormat` = NULL, `templateId` = NULL) {
      if (!is.null(`documentType`)) {
        stopifnot(is.character(`documentType`), length(`documentType`) == 1)
      }
      if (!is.null(`entityId`)) {
        stopifnot(is.character(`entityId`), length(`entityId`) == 1)
      }
      if (!is.null(`outputFormat`)) {
        stopifnot(is.character(`outputFormat`), length(`outputFormat`) == 1)
      }
      if (!is.null(`documentTypeSpecificParameters`)) {
        .setComplexProperty(typeList = list(AutomatedDocCreateDeploymentReport), propertyData = documentTypeSpecificParameters)
      }
      if (!is.null(`locale`)) {
        stopifnot(is.character(`locale`), length(`locale`) == 1)
      }
      if (!is.null(`templateId`)) {
        stopifnot(is.character(`templateId`), length(`templateId`) == 1)
      }
    }
  ),
  public = list(
    `documentType` = NULL,
    `documentTypeSpecificParameters` = NULL,
    `entityId` = NULL,
    `locale` = NULL,
    `outputFormat` = NULL,
    `templateId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param documentType Type of the automated document you want to generate.
    #' @param documentTypeSpecificParameters Set parameters unique for a specific document type. Currently, only these document types can have document-specific parameters: [&#39;DEPLOYMENT_REPORT&#39;]
    #' @param entityId ID of the entity to generate the document for. It can be a model ID, a project ID.
    #' @param locale Localization of the document. Defaults to EN_US.
    #' @param outputFormat Format to generate the document in.
    #' @param templateId Template ID to use for the document outline. Defaults to standard Datarobot template.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`documentType` = NULL, `entityId` = NULL, `outputFormat` = NULL, `documentTypeSpecificParameters` = NULL, `locale` = NULL, `templateId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`documentType`, `entityId`, `outputFormat`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(documentType, documentTypeSpecificParameters, entityId, locale, outputFormat, templateId)
      }
      self$`documentType` <- `documentType`
      self$`documentTypeSpecificParameters` <- .setComplexProperty(typeList = list(AutomatedDocCreateDeploymentReport), propertyData = documentTypeSpecificParameters)
      self$`entityId` <- `entityId`
      self$`locale` <- `locale`
      self$`outputFormat` <- `outputFormat`
      self$`templateId` <- `templateId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(documentType = self$`documentType`, documentTypeSpecificParameters = self$`documentTypeSpecificParameters`, entityId = self$`entityId`, locale = self$`locale`, outputFormat = self$`outputFormat`, templateId = self$`templateId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`documentType`)) {
          sprintf(
            '"documentType":
            "%s"
                  ',
            self$`documentType`
          )
        },
        if (!is.null(self$`documentTypeSpecificParameters`)) {
          sprintf(
            '"documentTypeSpecificParameters":
            %s
      ',
            jsonlite::toJSON(self$`documentTypeSpecificParameters`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`entityId`)) {
          sprintf(
            '"entityId":
            "%s"
                  ',
            self$`entityId`
          )
        },
        if (!is.null(self$`locale`)) {
          sprintf(
            '"locale":
            "%s"
                  ',
            self$`locale`
          )
        },
        if (!is.null(self$`outputFormat`)) {
          sprintf(
            '"outputFormat":
            "%s"
                  ',
            self$`outputFormat`
          )
        },
        if (!is.null(self$`templateId`)) {
          sprintf(
            '"templateId":
            "%s"
                  ',
            self$`templateId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param AutomatedDocCreateJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(AutomatedDocCreateJson, validateParams = FALSE) {
      AutomatedDocCreateObject <- jsonlite::fromJSON(AutomatedDocCreateJson)
      self$`documentType` <- AutomatedDocCreateObject$`documentType`
      self$`documentTypeSpecificParameters` <- .setComplexProperty(typeList = list(AutomatedDocCreateDeploymentReport), propertyData = AutomatedDocCreateObject$documentTypeSpecificParameters)
      self$`entityId` <- AutomatedDocCreateObject$`entityId`
      self$`locale` <- AutomatedDocCreateObject$`locale`
      self$`outputFormat` <- AutomatedDocCreateObject$`outputFormat`
      self$`templateId` <- AutomatedDocCreateObject$`templateId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
