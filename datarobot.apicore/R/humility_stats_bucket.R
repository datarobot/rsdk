# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title HumilityStatsBucket
#'
#' @description HumilityStatsBucket Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field period  \link{TimeRange}
#'
#' @field values  list( \link{HumilityStatsRule} ) Rules response objects.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
HumilityStatsBucket <- R6::R6Class(
  "HumilityStatsBucket",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(period = NULL, values = NULL) {
      if (!is.null(`period`)) {
        stopifnot(R6::is.R6(`period`))
      }
      if (!is.null(`values`)) {
        stopifnot(is.vector(`values`))
      }
    }
  ),
  public = list(
    `period` = NULL,
    `values` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param period
    #' @param values Rules response objects.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`period` = NULL, `values` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`period`, `values`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(period, values)
      }
      self$`period` <- `period`
      sapply(`values`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(period = self$`period`, values = self$`values`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`period`)) {
          sprintf(
            '"period":
          %s
        ',
            jsonlite::toJSON(self$`period`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`values`)) {
          sprintf(
            '"values":
            [%s]
',
            paste(sapply(self$`values`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param HumilityStatsBucketJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(HumilityStatsBucketJson, validateParams = FALSE) {
      HumilityStatsBucketObject <- jsonlite::fromJSON(HumilityStatsBucketJson)
      self$`period` <- TimeRange$new()$fromJSON(jsonlite::toJSON(HumilityStatsBucketObject$period, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`values` <- ApiClient$new()$deserializeObj(HumilityStatsBucketObject$`values`, "array[HumilityStatsRule]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
