# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentCleanupStatsChange
#'
#' @description DeploymentCleanupStatsChange Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field dataType  character Type of stats to cleanup.
#'
#' @field end  character If specified, the stats will be cleaned up to this timestamp. If &#x60;&#x60;null&#x60;&#x60; all stats till the deployment end forecast date will be cleaned up. Defaults to &#x60;&#x60;null&#x60;&#x60;.
#'
#' @field modelId  character ID of the model to remove deployment stats for. If &#x60;&#x60;null&#x60;&#x60;, the stats will be cleaned up for all models in the mathcing period. Defaults to &#x60;&#x60;null&#x60;&#x60;.
#'
#' @field start  character If specified, the stats will be cleaned up from this timestamp. If &#x60;&#x60;null&#x60;&#x60; all stats from the deployment start forecast date will be cleaned up. Defaults to &#x60;&#x60;null&#x60;&#x60;.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentCleanupStatsChange <- R6::R6Class(
  "DeploymentCleanupStatsChange",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(dataType = NULL, end = NULL, modelId = NULL, start = NULL) {
      if (!is.null(`dataType`)) {
        stopifnot(is.character(`dataType`), length(`dataType`) == 1)
      }
      if (!is.null(`end`)) {
        stopifnot(inherits(`end`, "POSIXt"))
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`start`)) {
        stopifnot(inherits(`start`, "POSIXt"))
      }
    }
  ),
  public = list(
    `dataType` = NULL,
    `end` = NULL,
    `modelId` = NULL,
    `start` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param dataType Type of stats to cleanup.
    #' @param end If specified, the stats will be cleaned up to this timestamp. If &#x60;&#x60;null&#x60;&#x60; all stats till the deployment end forecast date will be cleaned up. Defaults to &#x60;&#x60;null&#x60;&#x60;.
    #' @param modelId ID of the model to remove deployment stats for. If &#x60;&#x60;null&#x60;&#x60;, the stats will be cleaned up for all models in the mathcing period. Defaults to &#x60;&#x60;null&#x60;&#x60;.
    #' @param start If specified, the stats will be cleaned up from this timestamp. If &#x60;&#x60;null&#x60;&#x60; all stats from the deployment start forecast date will be cleaned up. Defaults to &#x60;&#x60;null&#x60;&#x60;.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`dataType` = NULL, `end` = NULL, `modelId` = NULL, `start` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`dataType`, `end`, `modelId`, `start`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(dataType, end, modelId, start)
      }
      self$`dataType` <- `dataType`
      self$`end` <- `end`
      self$`modelId` <- `modelId`
      self$`start` <- `start`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(dataType = self$`dataType`, end = self$`end`, modelId = self$`modelId`, start = self$`start`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`dataType`)) {
          sprintf(
            '"dataType":
            "%s"
                  ',
            self$`dataType`
          )
        },
        if (!is.null(self$`end`)) {
          sprintf(
            '"end":
            "%s"
                  ',
            format(self$`end`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`start`)) {
          sprintf(
            '"start":
            "%s"
                  ',
            format(self$`start`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentCleanupStatsChangeJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentCleanupStatsChangeJson, validateParams = FALSE) {
      DeploymentCleanupStatsChangeObject <- jsonlite::fromJSON(DeploymentCleanupStatsChangeJson)
      self$`dataType` <- DeploymentCleanupStatsChangeObject$`dataType`
      self$`end` <- ParseRFC3339Timestamp(DeploymentCleanupStatsChangeObject$`end`)
      self$`modelId` <- DeploymentCleanupStatsChangeObject$`modelId`
      self$`start` <- ParseRFC3339Timestamp(DeploymentCleanupStatsChangeObject$`start`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
