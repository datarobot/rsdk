# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DatasetFeaturePlotDataResponse
#'
#' @description DatasetFeaturePlotDataResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  numeric Number of values in the bin.
#'
#' @field label  character Bin start for numerical/uncapped, or string value for categorical. The bin &#x60;&#x3D;&#x3D;Missing&#x3D;&#x3D;&#x60; is created for rows that did not have the feature.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DatasetFeaturePlotDataResponse <- R6::R6Class(
  "DatasetFeaturePlotDataResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `label` = NULL) {
      if (!is.null(`count`)) {
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `label` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count Number of values in the bin.
    #' @param label Bin start for numerical/uncapped, or string value for categorical. The bin &#x60;&#x3D;&#x3D;Missing&#x3D;&#x3D;&#x60; is created for rows that did not have the feature.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `label` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`count`, `label`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, label)
      }
      self$`count` <- `count`
      self$`label` <- `label`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, label = self$`label`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DatasetFeaturePlotDataResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DatasetFeaturePlotDataResponseJson, validateParams = FALSE) {
      DatasetFeaturePlotDataResponseObject <- jsonlite::fromJSON(DatasetFeaturePlotDataResponseJson)
      self$`count` <- DatasetFeaturePlotDataResponseObject$`count`
      self$`label` <- DatasetFeaturePlotDataResponseObject$`label`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
