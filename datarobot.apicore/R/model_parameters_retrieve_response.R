# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelParametersRetrieveResponse
#'
#' @description ModelParametersRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field derivedFeatures  list( \link{DerivedFeatures} ) An array of preprocessing information about derived features used in the model.
#'
#' @field parameters  list( \link{Parameters} ) An array of parameters that are related to the whole model.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelParametersRetrieveResponse <- R6::R6Class(
  "ModelParametersRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`derivedFeatures` = NULL, `parameters` = NULL) {
      if (!is.null(`derivedFeatures`)) {
        stopifnot(is.vector(`derivedFeatures`), sapply(`derivedFeatures`, R6::is.R6))
      }
      if (!is.null(`parameters`)) {
        stopifnot(is.vector(`parameters`), sapply(`parameters`, R6::is.R6))
      }
    }
  ),
  public = list(
    `derivedFeatures` = NULL,
    `parameters` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param derivedFeatures An array of preprocessing information about derived features used in the model.
    #' @param parameters An array of parameters that are related to the whole model.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`derivedFeatures` = NULL, `parameters` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`derivedFeatures`, `parameters`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(derivedFeatures, parameters)
      }
      self$`derivedFeatures` <- `derivedFeatures`
      self$`parameters` <- `parameters`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(derivedFeatures = self$`derivedFeatures`, parameters = self$`parameters`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`derivedFeatures`)) {
          sprintf(
            '"derivedFeatures":
            [%s]
      ',
            paste(sapply(self$`derivedFeatures`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`parameters`)) {
          sprintf(
            '"parameters":
            [%s]
      ',
            paste(sapply(self$`parameters`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelParametersRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelParametersRetrieveResponseJson, validateParams = FALSE) {
      ModelParametersRetrieveResponseObject <- jsonlite::fromJSON(ModelParametersRetrieveResponseJson)
      self$`derivedFeatures` <- ApiClient$new()$deserializeObj(ModelParametersRetrieveResponseObject$`derivedFeatures`, "array[DerivedFeatures]", loadNamespace("datarobot.apicore"))
      self$`parameters` <- ApiClient$new()$deserializeObj(ModelParametersRetrieveResponseObject$`parameters`, "array[Parameters]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
