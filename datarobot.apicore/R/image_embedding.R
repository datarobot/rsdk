# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ImageEmbedding
#'
#' @description ImageEmbedding Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualTargetValue  \link{OneOfstringintegerarray} Actual target value of the dataset row.
#'
#' @field imageId  character Id of the image. The actual image file can be retrieved with :http:get:&#x60;/api/v2/projects/(projectId)/images/(imageId)/file/&#x60;.
#'
#' @field positionX  numeric x coordinate of the image in the embedding space.
#'
#' @field positionY  numeric y coordinate of the image in the embedding space.
#'
#' @field prediction  \link{InsightsPredictionField}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ImageEmbedding <- R6::R6Class(
  "ImageEmbedding",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`actualTargetValue` = NULL, `imageId` = NULL, `positionX` = NULL, `positionY` = NULL, `prediction` = NULL) {
      if (!is.null(`actualTargetValue`)) {
        .setPrimitiveProperty(typeList = list("character", "numeric", "array"), propertyData = actualTargetValue)
      }
      if (!is.null(`imageId`)) {
        stopifnot(is.character(`imageId`), length(`imageId`) == 1)
      }
      if (!is.null(`positionX`)) {
      }
      if (!is.null(`positionY`)) {
      }
      if (!is.null(`prediction`)) {
        stopifnot(R6::is.R6(`prediction`))
      }
    }
  ),
  public = list(
    `actualTargetValue` = NULL,
    `imageId` = NULL,
    `positionX` = NULL,
    `positionY` = NULL,
    `prediction` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualTargetValue Actual target value of the dataset row.
    #' @param imageId Id of the image. The actual image file can be retrieved with :http:get:&#x60;/api/v2/projects/(projectId)/images/(imageId)/file/&#x60;.
    #' @param positionX x coordinate of the image in the embedding space.
    #' @param positionY y coordinate of the image in the embedding space.
    #' @param prediction
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actualTargetValue` = NULL, `imageId` = NULL, `positionX` = NULL, `positionY` = NULL, `prediction` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`actualTargetValue`, `imageId`, `positionX`, `positionY`, `prediction`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(actualTargetValue, imageId, positionX, positionY, prediction)
      }
      self$`actualTargetValue` <- .setPrimitiveProperty(typeList = list("character", "numeric", "array"), propertyData = actualTargetValue)
      self$`imageId` <- `imageId`
      self$`positionX` <- `positionX`
      self$`positionY` <- `positionY`
      self$`prediction` <- `prediction`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(actualTargetValue = self$`actualTargetValue`, imageId = self$`imageId`, positionX = self$`positionX`, positionY = self$`positionY`, prediction = self$`prediction`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualTargetValue`)) {
          sprintf(
            '"actualTargetValue":
            %s
      ',
            self$`actualTargetValue`
          )
        },
        if (!is.null(self$`imageId`)) {
          sprintf(
            '"imageId":
            "%s"
                  ',
            self$`imageId`
          )
        },
        if (!is.null(self$`positionX`)) {
          sprintf(
            '"positionX":
            %d
                  ',
            self$`positionX`
          )
        },
        if (!is.null(self$`positionY`)) {
          sprintf(
            '"positionY":
            %d
                  ',
            self$`positionY`
          )
        },
        if (!is.null(self$`prediction`)) {
          sprintf(
            '"prediction":
            %s
      ',
            jsonlite::toJSON(self$`prediction`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ImageEmbeddingJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ImageEmbeddingJson, validateParams = FALSE) {
      ImageEmbeddingObject <- jsonlite::fromJSON(ImageEmbeddingJson)
      self$`actualTargetValue` <- .setPrimitiveProperty(typeList = list("character", "numeric", "array"), propertyData = ImageEmbeddingObject$actualTargetValue)
      self$`imageId` <- ImageEmbeddingObject$`imageId`
      self$`positionX` <- ImageEmbeddingObject$`positionX`
      self$`positionY` <- ImageEmbeddingObject$`positionY`
      self$`prediction` <- InsightsPredictionField$new()$fromJSON(jsonlite::toJSON(ImageEmbeddingObject$prediction, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
