# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionResultResponse
#'
#' @description PredictionResultResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actual  \link{OneOfintegernumberstring} Actual value of the prediction
#'
#' @field associationId  character Association id of the prediction, which is used to associate a prediction with its actual value and calculate accuracy.
#'
#' @field forecastDistance  integer [optional] The number of time units this prediction is away from the forecastPoint. The unit of time is determined by the timeUnit of the datetime partition column. For time series models only.
#'
#' @field modelId  character ID of the model used to make these predictions
#'
#' @field predicted  \link{OneOfintegernumberstring} Predicted value of the prediction
#'
#' @field timestamp  character When the prediction was made.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionResultResponse <- R6::R6Class(
  "PredictionResultResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(actual = NULL, associationId = NULL, forecastDistance = NULL, modelId = NULL, predicted = NULL, timestamp = NULL) {
      if (!is.null(`actual`)) {
        .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = actual)
      }
      if (!is.null(`associationId`)) {
        stopifnot(is.character(`associationId`), length(`associationId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`predicted`)) {
        .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = predicted)
      }
      if (!is.null(`timestamp`)) {
        stopifnot(inherits(`timestamp`, "POSIXt"))
      }
      if (!is.null(`forecastDistance`)) {
        stopifnot(is.numeric(`forecastDistance`), length(`forecastDistance`) == 1)
      }
    }
  ),
  public = list(
    `actual` = NULL,
    `associationId` = NULL,
    `forecastDistance` = NULL,
    `modelId` = NULL,
    `predicted` = NULL,
    `timestamp` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actual Actual value of the prediction
    #' @param associationId Association id of the prediction, which is used to associate a prediction with its actual value and calculate accuracy.
    #' @param forecastDistance The number of time units this prediction is away from the forecastPoint. The unit of time is determined by the timeUnit of the datetime partition column. For time series models only.
    #' @param modelId ID of the model used to make these predictions
    #' @param predicted Predicted value of the prediction
    #' @param timestamp When the prediction was made.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actual` = NULL, `associationId` = NULL, `modelId` = NULL, `predicted` = NULL, `timestamp` = NULL, `forecastDistance` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`actual`, `associationId`, `modelId`, `predicted`, `timestamp`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(actual, associationId, forecastDistance, modelId, predicted, timestamp)
      }
      self$`actual` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = actual)
      self$`associationId` <- `associationId`
      self$`forecastDistance` <- `forecastDistance`
      self$`modelId` <- `modelId`
      self$`predicted` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = predicted)
      self$`timestamp` <- `timestamp`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(actual = self$`actual`, associationId = self$`associationId`, forecastDistance = self$`forecastDistance`, modelId = self$`modelId`, predicted = self$`predicted`, timestamp = self$`timestamp`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actual`)) {
          sprintf(
            '"actual":
          %s
        ',
            self$`actual`
          )
        },
        if (!is.null(self$`associationId`)) {
          sprintf(
            '"associationId":
            "%s"
                  ',
            self$`associationId`
          )
        },
        if (!is.null(self$`forecastDistance`)) {
          sprintf(
            '"forecastDistance":
            %d
                  ',
            self$`forecastDistance`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`predicted`)) {
          sprintf(
            '"predicted":
          %s
        ',
            self$`predicted`
          )
        },
        if (!is.null(self$`timestamp`)) {
          sprintf(
            '"timestamp":
            "%s"
                  ',
            format(self$`timestamp`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionResultResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionResultResponseJson, validateParams = FALSE) {
      PredictionResultResponseObject <- jsonlite::fromJSON(PredictionResultResponseJson)
      self$`actual` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = PredictionResultResponseObject$actual)
      self$`associationId` <- PredictionResultResponseObject$`associationId`
      self$`forecastDistance` <- PredictionResultResponseObject$`forecastDistance`
      self$`modelId` <- PredictionResultResponseObject$`modelId`
      self$`predicted` <- .setPrimitiveProperty(typeList = list("numeric", "character"), propertyData = PredictionResultResponseObject$predicted)
      self$`timestamp` <- ParseRFC3339Timestamp(PredictionResultResponseObject$`timestamp`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
