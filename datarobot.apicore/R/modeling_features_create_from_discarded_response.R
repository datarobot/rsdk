# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelingFeaturesCreateFromDiscardedResponse
#'
#' @description ModelingFeaturesCreateFromDiscardedResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field featuresToRestore  list( character ) Features to add back to the project.
#'
#' @field warnings  list( character ) Warnings about features which can not be restored.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelingFeaturesCreateFromDiscardedResponse <- R6::R6Class(
  "ModelingFeaturesCreateFromDiscardedResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(featuresToRestore = NULL, warnings = NULL) {
      if (!is.null(`featuresToRestore`)) {
        stopifnot(is.vector(`featuresToRestore`))
      }
      if (!is.null(`warnings`)) {
        stopifnot(is.vector(`warnings`))
      }
    }
  ),
  public = list(
    `featuresToRestore` = NULL,
    `warnings` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param featuresToRestore Features to add back to the project.
    #' @param warnings Warnings about features which can not be restored.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featuresToRestore` = NULL, `warnings` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`featuresToRestore`, `warnings`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(featuresToRestore, warnings)
      }
      sapply(`featuresToRestore`, function(x) stopifnot(is.character(x)))
      sapply(`warnings`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(featuresToRestore = self$`featuresToRestore`, warnings = self$`warnings`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`featuresToRestore`)) {
          sprintf(
            '"featuresToRestore":
              [%s]
            ',
            paste(unlist(lapply(self$`featuresToRestore`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`warnings`)) {
          sprintf(
            '"warnings":
              [%s]
            ',
            paste(unlist(lapply(self$`warnings`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelingFeaturesCreateFromDiscardedResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelingFeaturesCreateFromDiscardedResponseJson, validateParams = FALSE) {
      ModelingFeaturesCreateFromDiscardedResponseObject <- jsonlite::fromJSON(ModelingFeaturesCreateFromDiscardedResponseJson)
      self$`featuresToRestore` <- ApiClient$new()$deserializeObj(ModelingFeaturesCreateFromDiscardedResponseObject$`featuresToRestore`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`warnings` <- ApiClient$new()$deserializeObj(ModelingFeaturesCreateFromDiscardedResponseObject$`warnings`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
