# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ForecastVsActualPlotsForecastDistancesStatus
#'
#' @description ForecastVsActualPlotsForecastDistancesStatus Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field completed  list( integer ) [optional] An array of available forecast distances for the &#x60;completed&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
#'
#' @field errored  list( integer ) [optional] An array of available forecast distances for the &#x60;errored&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
#'
#' @field inProgress  list( integer ) [optional] An array of available forecast distances for the &#x60;inProgress&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
#'
#' @field insufficientData  list( integer ) [optional] An array of available forecast distances for the &#x60;insufficientData&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
#'
#' @field notCompleted  list( integer ) [optional] An array of available forecast distances for the &#x60;notCompleted&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ForecastVsActualPlotsForecastDistancesStatus <- R6::R6Class(
  "ForecastVsActualPlotsForecastDistancesStatus",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(completed = NULL, errored = NULL, inProgress = NULL, insufficientData = NULL, notCompleted = NULL) {
      if (!is.null(`completed`)) {
        stopifnot(is.vector(`completed`))
      }
      if (!is.null(`errored`)) {
        stopifnot(is.vector(`errored`))
      }
      if (!is.null(`inProgress`)) {
        stopifnot(is.vector(`inProgress`))
      }
      if (!is.null(`insufficientData`)) {
        stopifnot(is.vector(`insufficientData`))
      }
      if (!is.null(`notCompleted`)) {
        stopifnot(is.vector(`notCompleted`))
      }
    }
  ),
  public = list(
    `completed` = NULL,
    `errored` = NULL,
    `inProgress` = NULL,
    `insufficientData` = NULL,
    `notCompleted` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param completed An array of available forecast distances for the &#x60;completed&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
    #' @param errored An array of available forecast distances for the &#x60;errored&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
    #' @param inProgress An array of available forecast distances for the &#x60;inProgress&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
    #' @param insufficientData An array of available forecast distances for the &#x60;insufficientData&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
    #' @param notCompleted An array of available forecast distances for the &#x60;notCompleted&#x60; status. If there are no forecast distances for this status, it will not appear in the response.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`completed` = NULL, `errored` = NULL, `inProgress` = NULL, `insufficientData` = NULL, `notCompleted` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list()
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(completed, errored, inProgress, insufficientData, notCompleted)
      }
      sapply(`completed`, function(x) stopifnot(is.character(x)))
      sapply(`errored`, function(x) stopifnot(is.character(x)))
      sapply(`inProgress`, function(x) stopifnot(is.character(x)))
      sapply(`insufficientData`, function(x) stopifnot(is.character(x)))
      sapply(`notCompleted`, function(x) stopifnot(is.character(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(completed = self$`completed`, errored = self$`errored`, inProgress = self$`inProgress`, insufficientData = self$`insufficientData`, notCompleted = self$`notCompleted`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`completed`)) {
          sprintf(
            '"completed":
              [%s]
            ',
            paste(unlist(lapply(self$`completed`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`errored`)) {
          sprintf(
            '"errored":
              [%s]
            ',
            paste(unlist(lapply(self$`errored`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`inProgress`)) {
          sprintf(
            '"inProgress":
              [%s]
            ',
            paste(unlist(lapply(self$`inProgress`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`insufficientData`)) {
          sprintf(
            '"insufficientData":
              [%s]
            ',
            paste(unlist(lapply(self$`insufficientData`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`notCompleted`)) {
          sprintf(
            '"notCompleted":
              [%s]
            ',
            paste(unlist(lapply(self$`notCompleted`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ForecastVsActualPlotsForecastDistancesStatusJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ForecastVsActualPlotsForecastDistancesStatusJson, validateParams = FALSE) {
      ForecastVsActualPlotsForecastDistancesStatusObject <- jsonlite::fromJSON(ForecastVsActualPlotsForecastDistancesStatusJson)
      self$`completed` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsForecastDistancesStatusObject$`completed`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`errored` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsForecastDistancesStatusObject$`errored`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`inProgress` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsForecastDistancesStatusObject$`inProgress`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`insufficientData` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsForecastDistancesStatusObject$`insufficientData`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`notCompleted` <- ApiClient$new()$deserializeObj(ForecastVsActualPlotsForecastDistancesStatusObject$`notCompleted`, "array[integer]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
