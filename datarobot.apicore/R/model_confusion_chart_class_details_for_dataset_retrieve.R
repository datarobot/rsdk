# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelConfusionChartClassDetailsForDatasetRetrieve
#'
#' @description ModelConfusionChartClassDetailsForDatasetRetrieve Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualFrequency  list( \link{ActualFrequency} ) One vs All actual percentage and count in a format specified below sorted by percentage in decreasing order
#'
#' @field className  character Name of a class for which distribution frequency is requested
#'
#' @field datasetId  character The dataset to retrieve a Confusion chart from.
#'
#' @field modelId  character The model to retrieve a Confusion chart from.
#'
#' @field predictedFrequency  list( \link{PredictedFrequency} ) One vs All predicted percentage and count in a format specified below sorted by percentage in decreasing order
#'
#' @field projectId  character The project to retrieve a Confusion chart from.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelConfusionChartClassDetailsForDatasetRetrieve <- R6::R6Class(
  "ModelConfusionChartClassDetailsForDatasetRetrieve",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(actualFrequency = NULL, className = NULL, datasetId = NULL, modelId = NULL, predictedFrequency = NULL, projectId = NULL) {
      if (!is.null(`actualFrequency`)) {
        stopifnot(is.vector(`actualFrequency`))
      }
      if (!is.null(`className`)) {
        stopifnot(is.character(`className`), length(`className`) == 1)
      }
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`predictedFrequency`)) {
        stopifnot(is.vector(`predictedFrequency`))
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
    }
  ),
  public = list(
    `actualFrequency` = NULL,
    `className` = NULL,
    `datasetId` = NULL,
    `modelId` = NULL,
    `predictedFrequency` = NULL,
    `projectId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualFrequency One vs All actual percentage and count in a format specified below sorted by percentage in decreasing order
    #' @param className Name of a class for which distribution frequency is requested
    #' @param datasetId The dataset to retrieve a Confusion chart from.
    #' @param modelId The model to retrieve a Confusion chart from.
    #' @param predictedFrequency One vs All predicted percentage and count in a format specified below sorted by percentage in decreasing order
    #' @param projectId The project to retrieve a Confusion chart from.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`actualFrequency` = NULL, `className` = NULL, `datasetId` = NULL, `modelId` = NULL, `predictedFrequency` = NULL, `projectId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`actualFrequency`, `className`, `datasetId`, `modelId`, `predictedFrequency`, `projectId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(actualFrequency, className, datasetId, modelId, predictedFrequency, projectId)
      }
      sapply(`actualFrequency`, function(x) stopifnot(R6::is.R6(x)))
      self$`className` <- `className`
      self$`datasetId` <- `datasetId`
      self$`modelId` <- `modelId`
      sapply(`predictedFrequency`, function(x) stopifnot(R6::is.R6(x)))
      self$`projectId` <- `projectId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(actualFrequency = self$`actualFrequency`, className = self$`className`, datasetId = self$`datasetId`, modelId = self$`modelId`, predictedFrequency = self$`predictedFrequency`, projectId = self$`projectId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualFrequency`)) {
          sprintf(
            '"actualFrequency":
            [%s]
',
            paste(sapply(self$`actualFrequency`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`className`)) {
          sprintf(
            '"className":
            "%s"
                  ',
            self$`className`
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`predictedFrequency`)) {
          sprintf(
            '"predictedFrequency":
            [%s]
',
            paste(sapply(self$`predictedFrequency`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelConfusionChartClassDetailsForDatasetRetrieveJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelConfusionChartClassDetailsForDatasetRetrieveJson, validateParams = FALSE) {
      ModelConfusionChartClassDetailsForDatasetRetrieveObject <- jsonlite::fromJSON(ModelConfusionChartClassDetailsForDatasetRetrieveJson)
      self$`actualFrequency` <- ApiClient$new()$deserializeObj(ModelConfusionChartClassDetailsForDatasetRetrieveObject$`actualFrequency`, "array[ActualFrequency]", loadNamespace("datarobot.apicore"))
      self$`className` <- ModelConfusionChartClassDetailsForDatasetRetrieveObject$`className`
      self$`datasetId` <- ModelConfusionChartClassDetailsForDatasetRetrieveObject$`datasetId`
      self$`modelId` <- ModelConfusionChartClassDetailsForDatasetRetrieveObject$`modelId`
      self$`predictedFrequency` <- ApiClient$new()$deserializeObj(ModelConfusionChartClassDetailsForDatasetRetrieveObject$`predictedFrequency`, "array[PredictedFrequency]", loadNamespace("datarobot.apicore"))
      self$`projectId` <- ModelConfusionChartClassDetailsForDatasetRetrieveObject$`projectId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
