# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UseCaseAttachmentDeploymentOverview
#'
#' @description UseCaseAttachmentDeploymentOverview Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field accuracyHealth  \link{UseCaseAttachmentDeploymentHealth}
#'
#' @field modelHealth  \link{UseCaseAttachmentDeploymentHealth}
#'
#' @field name  character Name of the deployment
#'
#' @field predictionServer  character Name of prediction server
#'
#' @field predictionUsage  \link{UseCaseAttachmentDeploymentPredictionUsage}
#'
#' @field serviceHealth  \link{UseCaseAttachmentDeploymentHealth}
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UseCaseAttachmentDeploymentOverview <- R6::R6Class(
  "UseCaseAttachmentDeploymentOverview",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(accuracyHealth = NULL, modelHealth = NULL, name = NULL, predictionServer = NULL, predictionUsage = NULL, serviceHealth = NULL) {
      if (!is.null(`accuracyHealth`)) {
        stopifnot(R6::is.R6(`accuracyHealth`))
      }
      if (!is.null(`modelHealth`)) {
        stopifnot(R6::is.R6(`modelHealth`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`predictionServer`)) {
        stopifnot(is.character(`predictionServer`), length(`predictionServer`) == 1)
      }
      if (!is.null(`predictionUsage`)) {
        stopifnot(R6::is.R6(`predictionUsage`))
      }
      if (!is.null(`serviceHealth`)) {
        stopifnot(R6::is.R6(`serviceHealth`))
      }
    }
  ),
  public = list(
    `accuracyHealth` = NULL,
    `modelHealth` = NULL,
    `name` = NULL,
    `predictionServer` = NULL,
    `predictionUsage` = NULL,
    `serviceHealth` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param accuracyHealth
    #' @param modelHealth
    #' @param name Name of the deployment
    #' @param predictionServer Name of prediction server
    #' @param predictionUsage
    #' @param serviceHealth
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`accuracyHealth` = NULL, `modelHealth` = NULL, `name` = NULL, `predictionServer` = NULL, `predictionUsage` = NULL, `serviceHealth` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`accuracyHealth`, `modelHealth`, `name`, `predictionServer`, `predictionUsage`, `serviceHealth`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(accuracyHealth, modelHealth, name, predictionServer, predictionUsage, serviceHealth)
      }
      self$`accuracyHealth` <- `accuracyHealth`
      self$`modelHealth` <- `modelHealth`
      self$`name` <- `name`
      self$`predictionServer` <- `predictionServer`
      self$`predictionUsage` <- `predictionUsage`
      self$`serviceHealth` <- `serviceHealth`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(accuracyHealth = self$`accuracyHealth`, modelHealth = self$`modelHealth`, name = self$`name`, predictionServer = self$`predictionServer`, predictionUsage = self$`predictionUsage`, serviceHealth = self$`serviceHealth`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`accuracyHealth`)) {
          sprintf(
            '"accuracyHealth":
          %s
        ',
            jsonlite::toJSON(self$`accuracyHealth`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`modelHealth`)) {
          sprintf(
            '"modelHealth":
          %s
        ',
            jsonlite::toJSON(self$`modelHealth`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`predictionServer`)) {
          sprintf(
            '"predictionServer":
            "%s"
                  ',
            self$`predictionServer`
          )
        },
        if (!is.null(self$`predictionUsage`)) {
          sprintf(
            '"predictionUsage":
          %s
        ',
            jsonlite::toJSON(self$`predictionUsage`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`serviceHealth`)) {
          sprintf(
            '"serviceHealth":
          %s
        ',
            jsonlite::toJSON(self$`serviceHealth`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UseCaseAttachmentDeploymentOverviewJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UseCaseAttachmentDeploymentOverviewJson, validateParams = FALSE) {
      UseCaseAttachmentDeploymentOverviewObject <- jsonlite::fromJSON(UseCaseAttachmentDeploymentOverviewJson)
      self$`accuracyHealth` <- UseCaseAttachmentDeploymentHealth$new()$fromJSON(jsonlite::toJSON(UseCaseAttachmentDeploymentOverviewObject$accuracyHealth, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`modelHealth` <- UseCaseAttachmentDeploymentHealth$new()$fromJSON(jsonlite::toJSON(UseCaseAttachmentDeploymentOverviewObject$modelHealth, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`name` <- UseCaseAttachmentDeploymentOverviewObject$`name`
      self$`predictionServer` <- UseCaseAttachmentDeploymentOverviewObject$`predictionServer`
      self$`predictionUsage` <- UseCaseAttachmentDeploymentPredictionUsage$new()$fromJSON(jsonlite::toJSON(UseCaseAttachmentDeploymentOverviewObject$predictionUsage, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`serviceHealth` <- UseCaseAttachmentDeploymentHealth$new()$fromJSON(jsonlite::toJSON(UseCaseAttachmentDeploymentOverviewObject$serviceHealth, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
