# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PerClusterGeospatial
#'
#' @description PerClusterGeospatial Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field clusterName  character Cluster name.
#'
#' @field representativeLocations  list( \link{array[numeric]} ) A list of latitude and longitude location list
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PerClusterGeospatial <- R6::R6Class(
  "PerClusterGeospatial",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`clusterName` = NULL, `representativeLocations` = NULL) {
      if (!is.null(`clusterName`)) {
        stopifnot(is.character(`clusterName`), length(`clusterName`) == 1)
      }
      if (!is.null(`representativeLocations`)) {
        stopifnot(is.vector(`representativeLocations`))
      }
    }
  ),
  public = list(
    `clusterName` = NULL,
    `representativeLocations` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param clusterName Cluster name.
    #' @param representativeLocations A list of latitude and longitude location list
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`clusterName` = NULL, `representativeLocations` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`clusterName`, `representativeLocations`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(clusterName, representativeLocations)
      }
      self$`clusterName` <- `clusterName`
      sapply(`representativeLocations`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(clusterName = self$`clusterName`, representativeLocations = self$`representativeLocations`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`clusterName`)) {
          sprintf(
            '"clusterName":
            "%s"
                  ',
            self$`clusterName`
          )
        },
        if (!is.null(self$`representativeLocations`)) {
          sprintf(
            '"representativeLocations":
            [%s]
      ',
            paste(sapply(self$`representativeLocations`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PerClusterGeospatialJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PerClusterGeospatialJson, validateParams = FALSE) {
      PerClusterGeospatialObject <- jsonlite::fromJSON(PerClusterGeospatialJson)
      self$`clusterName` <- PerClusterGeospatialObject$`clusterName`
      self$`representativeLocations` <- ApiClient$new()$deserializeObj(PerClusterGeospatialObject$`representativeLocations`, "array[array[numeric]]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
