# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CreatePredictionFromDataset
#'
#' @description CreatePredictionFromDataset Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field actualValueColumn  character [optional] For time series projects only. Actual value column name, valid for the prediction files if the project is unsupervised and the dataset is considered as bulk predictions dataset. This value is optional.
#'
#' @field datasetId  character The dataset to compute predictions for - must have previously been uploaded.
#'
#' @field explanationAlgorithm  character [optional] If set to &#x60;shap&#x60;, the response will include prediction explanations based on the SHAP explainer (SHapley Additive exPlanations). Defaults to null (no prediction explanations).
#'
#' @field forecastPoint  character [optional] For time series projects only. The time in the dataset relative to which predictions are generated. This value is optional. If not specified the default value is the value in the row with the latest specified timestamp. Specifying this value for a project that is not a time series project will result in an error.
#'
#' @field includeFdwCounts  character [optional] For time series projects with partial history only. Indicates if feature derivation window counts &#x60;featureDerivationWindowCounts&#x60; will be part of the response.
#'
#' @field includePredictionIntervals  character [optional] Specifies whether prediction intervals should be calculated for this request. Defaults to True if &#x60;predictionIntervalsSize&#x60; is specified, otherwise defaults to False.
#'
#' @field maxExplanations  integer [optional] Specifies the maximum number of explanation values that should be returned for each row, ordered by absolute value, greatest to least. In the case of &#39;shap&#39;: If not set, explanations are returned for all features. If the number of features is greater than the &#39;maxExplanations&#39;, the sum of remaining values will also be returned as &#39;shapRemainingTotal&#39;. Defaults to null for datasets narrower than 100 columns, defaults to 100 for datasets wider than 100 columns. Cannot be set if &#39;explanationAlgorithm&#39; is omitted.
#'
#' @field modelId  character The model to make predictions on.
#'
#' @field predictionIntervalsSize  integer [optional] Represents the percentile to use for the size of the prediction intervals. Defaults to 80 if &#x60;includePredictionIntervals&#x60; is True.
#'
#' @field predictionThreshold  numeric [optional] Threshold used for binary classification in predictions. Accepts values from 0.0 to 1.0. If not specified, model default prediction threshold will be used.
#'
#' @field predictionsEndDate  character [optional] The end date for bulk predictions, exclusive. Used for time series projects only. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsStartDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
#'
#' @field predictionsStartDate  character [optional] The start date for bulk predictions. Used for time series projects only. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsEndDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CreatePredictionFromDataset <- R6::R6Class(
  "CreatePredictionFromDataset",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(actualValueColumn = NULL, datasetId = NULL, explanationAlgorithm = NULL, forecastPoint = NULL, includeFdwCounts = NULL, includePredictionIntervals = NULL, maxExplanations = NULL, modelId = NULL, predictionIntervalsSize = NULL, predictionThreshold = NULL, predictionsEndDate = NULL, predictionsStartDate = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`actualValueColumn`)) {
        stopifnot(is.character(`actualValueColumn`), length(`actualValueColumn`) == 1)
      }
      if (!is.null(`explanationAlgorithm`)) {
        stopifnot(is.character(`explanationAlgorithm`), length(`explanationAlgorithm`) == 1)
      }
      if (!is.null(`forecastPoint`)) {
        stopifnot(inherits(`forecastPoint`, "POSIXt"))
      }
      if (!is.null(`includeFdwCounts`)) {
        stopifnot(is.logical(`includeFdwCounts`), length(`includeFdwCounts`) == 1)
      }
      if (!is.null(`includePredictionIntervals`)) {
        stopifnot(is.logical(`includePredictionIntervals`), length(`includePredictionIntervals`) == 1)
      }
      if (!is.null(`maxExplanations`)) {
        stopifnot(is.numeric(`maxExplanations`), length(`maxExplanations`) == 1)
      }
      if (!is.null(`predictionIntervalsSize`)) {
        stopifnot(is.numeric(`predictionIntervalsSize`), length(`predictionIntervalsSize`) == 1)
      }
      if (!is.null(`predictionThreshold`)) {
        stopifnot(is.numeric(`predictionThreshold`), length(`predictionThreshold`) == 1)
      }
      if (!is.null(`predictionsEndDate`)) {
        stopifnot(inherits(`predictionsEndDate`, "POSIXt"))
      }
      if (!is.null(`predictionsStartDate`)) {
        stopifnot(inherits(`predictionsStartDate`, "POSIXt"))
      }
    }
  ),
  public = list(
    `actualValueColumn` = NULL,
    `datasetId` = NULL,
    `explanationAlgorithm` = NULL,
    `forecastPoint` = NULL,
    `includeFdwCounts` = NULL,
    `includePredictionIntervals` = NULL,
    `maxExplanations` = NULL,
    `modelId` = NULL,
    `predictionIntervalsSize` = NULL,
    `predictionThreshold` = NULL,
    `predictionsEndDate` = NULL,
    `predictionsStartDate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param actualValueColumn For time series projects only. Actual value column name, valid for the prediction files if the project is unsupervised and the dataset is considered as bulk predictions dataset. This value is optional.
    #' @param datasetId The dataset to compute predictions for - must have previously been uploaded.
    #' @param explanationAlgorithm If set to &#x60;shap&#x60;, the response will include prediction explanations based on the SHAP explainer (SHapley Additive exPlanations). Defaults to null (no prediction explanations).
    #' @param forecastPoint For time series projects only. The time in the dataset relative to which predictions are generated. This value is optional. If not specified the default value is the value in the row with the latest specified timestamp. Specifying this value for a project that is not a time series project will result in an error.
    #' @param includeFdwCounts For time series projects with partial history only. Indicates if feature derivation window counts &#x60;featureDerivationWindowCounts&#x60; will be part of the response.
    #' @param includePredictionIntervals Specifies whether prediction intervals should be calculated for this request. Defaults to True if &#x60;predictionIntervalsSize&#x60; is specified, otherwise defaults to False.
    #' @param maxExplanations Specifies the maximum number of explanation values that should be returned for each row, ordered by absolute value, greatest to least. In the case of &#39;shap&#39;: If not set, explanations are returned for all features. If the number of features is greater than the &#39;maxExplanations&#39;, the sum of remaining values will also be returned as &#39;shapRemainingTotal&#39;. Defaults to null for datasets narrower than 100 columns, defaults to 100 for datasets wider than 100 columns. Cannot be set if &#39;explanationAlgorithm&#39; is omitted.
    #' @param modelId The model to make predictions on.
    #' @param predictionIntervalsSize Represents the percentile to use for the size of the prediction intervals. Defaults to 80 if &#x60;includePredictionIntervals&#x60; is True.
    #' @param predictionThreshold Threshold used for binary classification in predictions. Accepts values from 0.0 to 1.0. If not specified, model default prediction threshold will be used.
    #' @param predictionsEndDate The end date for bulk predictions, exclusive. Used for time series projects only. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsStartDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
    #' @param predictionsStartDate The start date for bulk predictions. Used for time series projects only. Note that this parameter is used for generating historical predictions using the training data, not for future predictions. If not specified, the dataset is not considered as a bulk predictions dataset. This parameter should be provided in conjunction with a &#x60;&#x60;predictionsEndDate&#x60;&#x60;, and cannot be provided with the &#x60;&#x60;forecastPoint&#x60;&#x60; parameter.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `modelId` = NULL, `actualValueColumn` = NULL, `explanationAlgorithm` = NULL, `forecastPoint` = NULL, `includeFdwCounts` = FALSE, `includePredictionIntervals` = NULL, `maxExplanations` = NULL, `predictionIntervalsSize` = NULL, `predictionThreshold` = NULL, `predictionsEndDate` = NULL, `predictionsStartDate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasetId`, `modelId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(actualValueColumn, datasetId, explanationAlgorithm, forecastPoint, includeFdwCounts, includePredictionIntervals, maxExplanations, modelId, predictionIntervalsSize, predictionThreshold, predictionsEndDate, predictionsStartDate)
      }
      self$`actualValueColumn` <- `actualValueColumn`
      self$`datasetId` <- `datasetId`
      self$`explanationAlgorithm` <- `explanationAlgorithm`
      self$`forecastPoint` <- `forecastPoint`
      self$`includeFdwCounts` <- `includeFdwCounts`
      self$`includePredictionIntervals` <- `includePredictionIntervals`
      self$`maxExplanations` <- `maxExplanations`
      self$`modelId` <- `modelId`
      self$`predictionIntervalsSize` <- `predictionIntervalsSize`
      self$`predictionThreshold` <- `predictionThreshold`
      self$`predictionsEndDate` <- `predictionsEndDate`
      self$`predictionsStartDate` <- `predictionsStartDate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(actualValueColumn = self$`actualValueColumn`, datasetId = self$`datasetId`, explanationAlgorithm = self$`explanationAlgorithm`, forecastPoint = self$`forecastPoint`, includeFdwCounts = self$`includeFdwCounts`, includePredictionIntervals = self$`includePredictionIntervals`, maxExplanations = self$`maxExplanations`, modelId = self$`modelId`, predictionIntervalsSize = self$`predictionIntervalsSize`, predictionThreshold = self$`predictionThreshold`, predictionsEndDate = self$`predictionsEndDate`, predictionsStartDate = self$`predictionsStartDate`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`actualValueColumn`)) {
          sprintf(
            '"actualValueColumn":
            "%s"
                  ',
            self$`actualValueColumn`
          )
        },
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`explanationAlgorithm`)) {
          sprintf(
            '"explanationAlgorithm":
            "%s"
                  ',
            self$`explanationAlgorithm`
          )
        },
        if (!is.null(self$`forecastPoint`)) {
          sprintf(
            '"forecastPoint":
            "%s"
                  ',
            format(self$`forecastPoint`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`includeFdwCounts`)) {
          sprintf(
            '"includeFdwCounts":
            %s
                  ',
            tolower(self$`includeFdwCounts`)
          )
        },
        if (!is.null(self$`includePredictionIntervals`)) {
          sprintf(
            '"includePredictionIntervals":
            %s
                  ',
            tolower(self$`includePredictionIntervals`)
          )
        },
        if (!is.null(self$`maxExplanations`)) {
          sprintf(
            '"maxExplanations":
            %d
                  ',
            self$`maxExplanations`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`predictionIntervalsSize`)) {
          sprintf(
            '"predictionIntervalsSize":
            %d
                  ',
            self$`predictionIntervalsSize`
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        },
        if (!is.null(self$`predictionsEndDate`)) {
          sprintf(
            '"predictionsEndDate":
            "%s"
                  ',
            format(self$`predictionsEndDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`predictionsStartDate`)) {
          sprintf(
            '"predictionsStartDate":
            "%s"
                  ',
            format(self$`predictionsStartDate`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CreatePredictionFromDatasetJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CreatePredictionFromDatasetJson, validateParams = FALSE) {
      CreatePredictionFromDatasetObject <- jsonlite::fromJSON(CreatePredictionFromDatasetJson)
      self$`actualValueColumn` <- CreatePredictionFromDatasetObject$`actualValueColumn`
      self$`datasetId` <- CreatePredictionFromDatasetObject$`datasetId`
      self$`explanationAlgorithm` <- CreatePredictionFromDatasetObject$`explanationAlgorithm`
      self$`forecastPoint` <- ParseRFC3339Timestamp(CreatePredictionFromDatasetObject$`forecastPoint`)
      self$`includeFdwCounts` <- CreatePredictionFromDatasetObject$`includeFdwCounts`
      self$`includePredictionIntervals` <- CreatePredictionFromDatasetObject$`includePredictionIntervals`
      self$`maxExplanations` <- CreatePredictionFromDatasetObject$`maxExplanations`
      self$`modelId` <- CreatePredictionFromDatasetObject$`modelId`
      self$`predictionIntervalsSize` <- CreatePredictionFromDatasetObject$`predictionIntervalsSize`
      self$`predictionThreshold` <- CreatePredictionFromDatasetObject$`predictionThreshold`
      self$`predictionsEndDate` <- ParseRFC3339Timestamp(CreatePredictionFromDatasetObject$`predictionsEndDate`)
      self$`predictionsStartDate` <- ParseRFC3339Timestamp(CreatePredictionFromDatasetObject$`predictionsStartDate`)

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
