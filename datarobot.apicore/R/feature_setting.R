# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureSetting
#'
#' @description FeatureSetting Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field aPriori  character [optional] Renamed to &#x60;knownInAdvance&#x60;.
#'
#' @field doNotDerive  character [optional] For time series projects only. Sets whether the feature is do-not-derive, i.e., is excluded from feature derivation. If not specified, the feature uses the value from the &#x60;defaultToDoNotDerive&#x60; flag.
#'
#' @field featureName  character The name of the feature being specified.
#'
#' @field knownInAdvance  character [optional] For time series projects only. Sets whether the feature is known in advance, i.e., values for future dates are known at prediction time. If not specified, the feature uses the value from the &#x60;defaultToKnownInAdvance&#x60; flag.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureSetting <- R6::R6Class(
  "FeatureSetting",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(aPriori = NULL, doNotDerive = NULL, featureName = NULL, knownInAdvance = NULL) {
      if (!is.null(`featureName`)) {
        stopifnot(is.character(`featureName`), length(`featureName`) == 1)
      }
      if (!is.null(`aPriori`)) {
        stopifnot(is.logical(`aPriori`), length(`aPriori`) == 1)
      }
      if (!is.null(`doNotDerive`)) {
        stopifnot(is.logical(`doNotDerive`), length(`doNotDerive`) == 1)
      }
      if (!is.null(`knownInAdvance`)) {
        stopifnot(is.logical(`knownInAdvance`), length(`knownInAdvance`) == 1)
      }
    }
  ),
  public = list(
    `aPriori` = NULL,
    `doNotDerive` = NULL,
    `featureName` = NULL,
    `knownInAdvance` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param aPriori Renamed to &#x60;knownInAdvance&#x60;.
    #' @param doNotDerive For time series projects only. Sets whether the feature is do-not-derive, i.e., is excluded from feature derivation. If not specified, the feature uses the value from the &#x60;defaultToDoNotDerive&#x60; flag.
    #' @param featureName The name of the feature being specified.
    #' @param knownInAdvance For time series projects only. Sets whether the feature is known in advance, i.e., values for future dates are known at prediction time. If not specified, the feature uses the value from the &#x60;defaultToKnownInAdvance&#x60; flag.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`featureName` = NULL, `aPriori` = NULL, `doNotDerive` = NULL, `knownInAdvance` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`featureName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(aPriori, doNotDerive, featureName, knownInAdvance)
      }
      self$`aPriori` <- `aPriori`
      self$`doNotDerive` <- `doNotDerive`
      self$`featureName` <- `featureName`
      self$`knownInAdvance` <- `knownInAdvance`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(aPriori = self$`aPriori`, doNotDerive = self$`doNotDerive`, featureName = self$`featureName`, knownInAdvance = self$`knownInAdvance`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`aPriori`)) {
          sprintf(
            '"aPriori":
            %s
                  ',
            tolower(self$`aPriori`)
          )
        },
        if (!is.null(self$`doNotDerive`)) {
          sprintf(
            '"doNotDerive":
            %s
                  ',
            tolower(self$`doNotDerive`)
          )
        },
        if (!is.null(self$`featureName`)) {
          sprintf(
            '"featureName":
            "%s"
                  ',
            self$`featureName`
          )
        },
        if (!is.null(self$`knownInAdvance`)) {
          sprintf(
            '"knownInAdvance":
            %s
                  ',
            tolower(self$`knownInAdvance`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureSettingJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureSettingJson, validateParams = FALSE) {
      FeatureSettingObject <- jsonlite::fromJSON(FeatureSettingJson)
      self$`aPriori` <- FeatureSettingObject$`aPriori`
      self$`doNotDerive` <- FeatureSettingObject$`doNotDerive`
      self$`featureName` <- FeatureSettingObject$`featureName`
      self$`knownInAdvance` <- FeatureSettingObject$`knownInAdvance`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
