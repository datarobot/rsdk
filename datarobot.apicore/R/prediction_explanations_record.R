# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PredictionExplanationsRecord
#'
#' @description PredictionExplanationsRecord Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetId  character The dataset ID.
#'
#' @field finishTime  numeric Timestamp referencing when computation for these prediction explanations finished.
#'
#' @field id  character The PredictionExplanationsRecord ID.
#'
#' @field maxExplanations  integer The maximum number of codes generated per prediction.
#'
#' @field modelId  character The model ID.
#'
#' @field numColumns  integer The number of columns prediction explanations were computed for.
#'
#' @field predictionExplanationsLocation  character Where to retrieve the prediction explanations.
#'
#' @field predictionThreshold  numeric The threshold value used for binary classification prediction.
#'
#' @field projectId  character The project ID.
#'
#' @field thresholdHigh  numeric The prediction explanation high threshold. Predictions must be above this value (or below the thresholdLow value) to have PredictionExplanations computed.
#'
#' @field thresholdLow  numeric The prediction explanation low threshold. Predictions must be below this value (or above the thresholdHigh value) to have PredictionExplanations computed.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PredictionExplanationsRecord <- R6::R6Class(
  "PredictionExplanationsRecord",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`datasetId` = NULL, `finishTime` = NULL, `id` = NULL, `maxExplanations` = NULL, `modelId` = NULL, `numColumns` = NULL, `predictionExplanationsLocation` = NULL, `predictionThreshold` = NULL, `projectId` = NULL, `thresholdHigh` = NULL, `thresholdLow` = NULL) {
      if (!is.null(`datasetId`)) {
        stopifnot(is.character(`datasetId`), length(`datasetId`) == 1)
      }
      if (!is.null(`finishTime`)) {
      }
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`maxExplanations`)) {
        stopifnot(is.numeric(`maxExplanations`), length(`maxExplanations`) == 1)
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`numColumns`)) {
        stopifnot(is.numeric(`numColumns`), length(`numColumns`) == 1)
      }
      if (!is.null(`predictionExplanationsLocation`)) {
        stopifnot(is.character(`predictionExplanationsLocation`), length(`predictionExplanationsLocation`) == 1)
      }
      if (!is.null(`predictionThreshold`)) {
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`thresholdHigh`)) {
      }
      if (!is.null(`thresholdLow`)) {
      }
    }
  ),
  public = list(
    `datasetId` = NULL,
    `finishTime` = NULL,
    `id` = NULL,
    `maxExplanations` = NULL,
    `modelId` = NULL,
    `numColumns` = NULL,
    `predictionExplanationsLocation` = NULL,
    `predictionThreshold` = NULL,
    `projectId` = NULL,
    `thresholdHigh` = NULL,
    `thresholdLow` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetId The dataset ID.
    #' @param finishTime Timestamp referencing when computation for these prediction explanations finished.
    #' @param id The PredictionExplanationsRecord ID.
    #' @param maxExplanations The maximum number of codes generated per prediction.
    #' @param modelId The model ID.
    #' @param numColumns The number of columns prediction explanations were computed for.
    #' @param predictionExplanationsLocation Where to retrieve the prediction explanations.
    #' @param predictionThreshold The threshold value used for binary classification prediction.
    #' @param projectId The project ID.
    #' @param thresholdHigh The prediction explanation high threshold. Predictions must be above this value (or below the thresholdLow value) to have PredictionExplanations computed.
    #' @param thresholdLow The prediction explanation low threshold. Predictions must be below this value (or above the thresholdHigh value) to have PredictionExplanations computed.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `finishTime` = NULL, `id` = NULL, `maxExplanations` = NULL, `modelId` = NULL, `numColumns` = NULL, `predictionExplanationsLocation` = NULL, `predictionThreshold` = NULL, `projectId` = NULL, `thresholdHigh` = NULL, `thresholdLow` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`datasetId`, `finishTime`, `id`, `maxExplanations`, `modelId`, `numColumns`, `predictionExplanationsLocation`, `predictionThreshold`, `projectId`, `thresholdHigh`, `thresholdLow`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(datasetId, finishTime, id, maxExplanations, modelId, numColumns, predictionExplanationsLocation, predictionThreshold, projectId, thresholdHigh, thresholdLow)
      }
      self$`datasetId` <- `datasetId`
      self$`finishTime` <- `finishTime`
      self$`id` <- `id`
      self$`maxExplanations` <- `maxExplanations`
      self$`modelId` <- `modelId`
      self$`numColumns` <- `numColumns`
      self$`predictionExplanationsLocation` <- `predictionExplanationsLocation`
      self$`predictionThreshold` <- `predictionThreshold`
      self$`projectId` <- `projectId`
      self$`thresholdHigh` <- `thresholdHigh`
      self$`thresholdLow` <- `thresholdLow`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(datasetId = self$`datasetId`, finishTime = self$`finishTime`, id = self$`id`, maxExplanations = self$`maxExplanations`, modelId = self$`modelId`, numColumns = self$`numColumns`, predictionExplanationsLocation = self$`predictionExplanationsLocation`, predictionThreshold = self$`predictionThreshold`, projectId = self$`projectId`, thresholdHigh = self$`thresholdHigh`, thresholdLow = self$`thresholdLow`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
            "%s"
                  ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`finishTime`)) {
          sprintf(
            '"finishTime":
            %d
                  ',
            self$`finishTime`
          )
        },
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`maxExplanations`)) {
          sprintf(
            '"maxExplanations":
            %d
                  ',
            self$`maxExplanations`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`numColumns`)) {
          sprintf(
            '"numColumns":
            %d
                  ',
            self$`numColumns`
          )
        },
        if (!is.null(self$`predictionExplanationsLocation`)) {
          sprintf(
            '"predictionExplanationsLocation":
            "%s"
                  ',
            self$`predictionExplanationsLocation`
          )
        },
        if (!is.null(self$`predictionThreshold`)) {
          sprintf(
            '"predictionThreshold":
            %d
                  ',
            self$`predictionThreshold`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`thresholdHigh`)) {
          sprintf(
            '"thresholdHigh":
            %d
                  ',
            self$`thresholdHigh`
          )
        },
        if (!is.null(self$`thresholdLow`)) {
          sprintf(
            '"thresholdLow":
            %d
                  ',
            self$`thresholdLow`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PredictionExplanationsRecordJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PredictionExplanationsRecordJson, validateParams = FALSE) {
      PredictionExplanationsRecordObject <- jsonlite::fromJSON(PredictionExplanationsRecordJson)
      self$`datasetId` <- PredictionExplanationsRecordObject$`datasetId`
      self$`finishTime` <- PredictionExplanationsRecordObject$`finishTime`
      self$`id` <- PredictionExplanationsRecordObject$`id`
      self$`maxExplanations` <- PredictionExplanationsRecordObject$`maxExplanations`
      self$`modelId` <- PredictionExplanationsRecordObject$`modelId`
      self$`numColumns` <- PredictionExplanationsRecordObject$`numColumns`
      self$`predictionExplanationsLocation` <- PredictionExplanationsRecordObject$`predictionExplanationsLocation`
      self$`predictionThreshold` <- PredictionExplanationsRecordObject$`predictionThreshold`
      self$`projectId` <- PredictionExplanationsRecordObject$`projectId`
      self$`thresholdHigh` <- PredictionExplanationsRecordObject$`thresholdHigh`
      self$`thresholdLow` <- PredictionExplanationsRecordObject$`thresholdLow`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
