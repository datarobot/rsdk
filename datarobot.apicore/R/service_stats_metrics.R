# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ServiceStatsMetrics
#'
#' @description ServiceStatsMetrics Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field cacheHitRatio  numeric Number of cache hits.
#'
#' @field executionTime  numeric Request execution time at &#x60;executionTimeQuantile&#x60; (in milliseconds).
#'
#' @field medianLoad  numeric Median of the request rate (in requests per minute).
#'
#' @field numConsumers  integer Number of unique users performing requests.
#'
#' @field peakLoad  numeric Maximum of the request rate (in requests per minute).
#'
#' @field responseTime  numeric Request response time at &#x60;responseTimeQuantile&#x60; (in milliseconds).
#'
#' @field serverErrorRate  numeric Ratio of server errors to the total number of requests.
#'
#' @field slowRequests  integer Number of requests with response time greater than &#x60;slowRequestsThreshold&#x60;
#'
#' @field totalPredictions  integer Total number of prediction rows.
#'
#' @field totalRequests  integer Total number of prediction requests performed.
#'
#' @field userErrorRate  numeric Ratio of user errors to the total number of requests.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ServiceStatsMetrics <- R6::R6Class(
  "ServiceStatsMetrics",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(cacheHitRatio = NULL, executionTime = NULL, medianLoad = NULL, numConsumers = NULL, peakLoad = NULL, responseTime = NULL, serverErrorRate = NULL, slowRequests = NULL, totalPredictions = NULL, totalRequests = NULL, userErrorRate = NULL) {
      if (!is.null(`cacheHitRatio`)) {
      }
      if (!is.null(`executionTime`)) {
      }
      if (!is.null(`medianLoad`)) {
      }
      if (!is.null(`numConsumers`)) {
        stopifnot(is.numeric(`numConsumers`), length(`numConsumers`) == 1)
      }
      if (!is.null(`peakLoad`)) {
      }
      if (!is.null(`responseTime`)) {
      }
      if (!is.null(`serverErrorRate`)) {
      }
      if (!is.null(`slowRequests`)) {
        stopifnot(is.numeric(`slowRequests`), length(`slowRequests`) == 1)
      }
      if (!is.null(`totalPredictions`)) {
        stopifnot(is.numeric(`totalPredictions`), length(`totalPredictions`) == 1)
      }
      if (!is.null(`totalRequests`)) {
        stopifnot(is.numeric(`totalRequests`), length(`totalRequests`) == 1)
      }
      if (!is.null(`userErrorRate`)) {
      }
    }
  ),
  public = list(
    `cacheHitRatio` = NULL,
    `executionTime` = NULL,
    `medianLoad` = NULL,
    `numConsumers` = NULL,
    `peakLoad` = NULL,
    `responseTime` = NULL,
    `serverErrorRate` = NULL,
    `slowRequests` = NULL,
    `totalPredictions` = NULL,
    `totalRequests` = NULL,
    `userErrorRate` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param cacheHitRatio Number of cache hits.
    #' @param executionTime Request execution time at &#x60;executionTimeQuantile&#x60; (in milliseconds).
    #' @param medianLoad Median of the request rate (in requests per minute).
    #' @param numConsumers Number of unique users performing requests.
    #' @param peakLoad Maximum of the request rate (in requests per minute).
    #' @param responseTime Request response time at &#x60;responseTimeQuantile&#x60; (in milliseconds).
    #' @param serverErrorRate Ratio of server errors to the total number of requests.
    #' @param slowRequests Number of requests with response time greater than &#x60;slowRequestsThreshold&#x60;
    #' @param totalPredictions Total number of prediction rows.
    #' @param totalRequests Total number of prediction requests performed.
    #' @param userErrorRate Ratio of user errors to the total number of requests.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`cacheHitRatio` = NULL, `executionTime` = NULL, `medianLoad` = NULL, `numConsumers` = NULL, `peakLoad` = NULL, `responseTime` = NULL, `serverErrorRate` = NULL, `slowRequests` = NULL, `totalPredictions` = NULL, `totalRequests` = NULL, `userErrorRate` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`cacheHitRatio`, `executionTime`, `medianLoad`, `numConsumers`, `peakLoad`, `responseTime`, `serverErrorRate`, `slowRequests`, `totalPredictions`, `totalRequests`, `userErrorRate`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(cacheHitRatio, executionTime, medianLoad, numConsumers, peakLoad, responseTime, serverErrorRate, slowRequests, totalPredictions, totalRequests, userErrorRate)
      }
      self$`cacheHitRatio` <- `cacheHitRatio`
      self$`executionTime` <- `executionTime`
      self$`medianLoad` <- `medianLoad`
      self$`numConsumers` <- `numConsumers`
      self$`peakLoad` <- `peakLoad`
      self$`responseTime` <- `responseTime`
      self$`serverErrorRate` <- `serverErrorRate`
      self$`slowRequests` <- `slowRequests`
      self$`totalPredictions` <- `totalPredictions`
      self$`totalRequests` <- `totalRequests`
      self$`userErrorRate` <- `userErrorRate`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(cacheHitRatio = self$`cacheHitRatio`, executionTime = self$`executionTime`, medianLoad = self$`medianLoad`, numConsumers = self$`numConsumers`, peakLoad = self$`peakLoad`, responseTime = self$`responseTime`, serverErrorRate = self$`serverErrorRate`, slowRequests = self$`slowRequests`, totalPredictions = self$`totalPredictions`, totalRequests = self$`totalRequests`, userErrorRate = self$`userErrorRate`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`cacheHitRatio`)) {
          sprintf(
            '"cacheHitRatio":
            %d
                  ',
            self$`cacheHitRatio`
          )
        },
        if (!is.null(self$`executionTime`)) {
          sprintf(
            '"executionTime":
            %d
                  ',
            self$`executionTime`
          )
        },
        if (!is.null(self$`medianLoad`)) {
          sprintf(
            '"medianLoad":
            %d
                  ',
            self$`medianLoad`
          )
        },
        if (!is.null(self$`numConsumers`)) {
          sprintf(
            '"numConsumers":
            %d
                  ',
            self$`numConsumers`
          )
        },
        if (!is.null(self$`peakLoad`)) {
          sprintf(
            '"peakLoad":
            %d
                  ',
            self$`peakLoad`
          )
        },
        if (!is.null(self$`responseTime`)) {
          sprintf(
            '"responseTime":
            %d
                  ',
            self$`responseTime`
          )
        },
        if (!is.null(self$`serverErrorRate`)) {
          sprintf(
            '"serverErrorRate":
            %d
                  ',
            self$`serverErrorRate`
          )
        },
        if (!is.null(self$`slowRequests`)) {
          sprintf(
            '"slowRequests":
            %d
                  ',
            self$`slowRequests`
          )
        },
        if (!is.null(self$`totalPredictions`)) {
          sprintf(
            '"totalPredictions":
            %d
                  ',
            self$`totalPredictions`
          )
        },
        if (!is.null(self$`totalRequests`)) {
          sprintf(
            '"totalRequests":
            %d
                  ',
            self$`totalRequests`
          )
        },
        if (!is.null(self$`userErrorRate`)) {
          sprintf(
            '"userErrorRate":
            %d
                  ',
            self$`userErrorRate`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ServiceStatsMetricsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ServiceStatsMetricsJson, validateParams = FALSE) {
      ServiceStatsMetricsObject <- jsonlite::fromJSON(ServiceStatsMetricsJson)
      self$`cacheHitRatio` <- ServiceStatsMetricsObject$`cacheHitRatio`
      self$`executionTime` <- ServiceStatsMetricsObject$`executionTime`
      self$`medianLoad` <- ServiceStatsMetricsObject$`medianLoad`
      self$`numConsumers` <- ServiceStatsMetricsObject$`numConsumers`
      self$`peakLoad` <- ServiceStatsMetricsObject$`peakLoad`
      self$`responseTime` <- ServiceStatsMetricsObject$`responseTime`
      self$`serverErrorRate` <- ServiceStatsMetricsObject$`serverErrorRate`
      self$`slowRequests` <- ServiceStatsMetricsObject$`slowRequests`
      self$`totalPredictions` <- ServiceStatsMetricsObject$`totalPredictions`
      self$`totalRequests` <- ServiceStatsMetricsObject$`totalRequests`
      self$`userErrorRate` <- ServiceStatsMetricsObject$`userErrorRate`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
