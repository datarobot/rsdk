# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintSharedRolesResponse
#'
#' @description UserBlueprintSharedRolesResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field id  character The ID of the recipient organization, group or user.
#'
#' @field name  character The name of the recipient organization, group or user.
#'
#' @field role  character The role of the org/group/user on this dataset or \&quot;NO_ROLE\&quot; for removing access when used with route to modify access.
#'
#' @field shareRecipientType  character Describes the recipient type, either user, group, or organization.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintSharedRolesResponse <- R6::R6Class(
  "UserBlueprintSharedRolesResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`id` = NULL, `name` = NULL, `role` = NULL, `shareRecipientType` = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`role`)) {
        stopifnot(is.character(`role`), length(`role`) == 1)
      }
      if (!is.null(`shareRecipientType`)) {
        stopifnot(is.character(`shareRecipientType`), length(`shareRecipientType`) == 1)
      }
    }
  ),
  public = list(
    `id` = NULL,
    `name` = NULL,
    `role` = NULL,
    `shareRecipientType` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param id The ID of the recipient organization, group or user.
    #' @param name The name of the recipient organization, group or user.
    #' @param role The role of the org/group/user on this dataset or \&quot;NO_ROLE\&quot; for removing access when used with route to modify access.
    #' @param shareRecipientType Describes the recipient type, either user, group, or organization.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `name` = NULL, `role` = NULL, `shareRecipientType` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`id`, `name`, `role`, `shareRecipientType`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(id, name, role, shareRecipientType)
      }
      self$`id` <- `id`
      self$`name` <- `name`
      self$`role` <- `role`
      self$`shareRecipientType` <- `shareRecipientType`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(id = self$`id`, name = self$`name`, role = self$`role`, shareRecipientType = self$`shareRecipientType`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`role`)) {
          sprintf(
            '"role":
            "%s"
                  ',
            self$`role`
          )
        },
        if (!is.null(self$`shareRecipientType`)) {
          sprintf(
            '"shareRecipientType":
            "%s"
                  ',
            self$`shareRecipientType`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintSharedRolesResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintSharedRolesResponseJson, validateParams = FALSE) {
      UserBlueprintSharedRolesResponseObject <- jsonlite::fromJSON(UserBlueprintSharedRolesResponseJson)
      self$`id` <- UserBlueprintSharedRolesResponseObject$`id`
      self$`name` <- UserBlueprintSharedRolesResponseObject$`name`
      self$`role` <- UserBlueprintSharedRolesResponseObject$`role`
      self$`shareRecipientType` <- UserBlueprintSharedRolesResponseObject$`shareRecipientType`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
