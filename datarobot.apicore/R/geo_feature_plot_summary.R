# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title GeoFeaturePlotSummary
#'
#' @description GeoFeaturePlotSummary Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field maxCount  integer Max num of samples located within one geometry.
#'
#' @field minCount  integer Min num of samples located within one geometry.
#'
#' @field totalCount  integer Total num of samples across all geometry objects.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GeoFeaturePlotSummary <- R6::R6Class(
  "GeoFeaturePlotSummary",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`maxCount` = NULL, `minCount` = NULL, `totalCount` = NULL) {
      if (!is.null(`maxCount`)) {
        stopifnot(is.numeric(`maxCount`), length(`maxCount`) == 1)
      }
      if (!is.null(`minCount`)) {
        stopifnot(is.numeric(`minCount`), length(`minCount`) == 1)
      }
      if (!is.null(`totalCount`)) {
        stopifnot(is.numeric(`totalCount`), length(`totalCount`) == 1)
      }
    }
  ),
  public = list(
    `maxCount` = NULL,
    `minCount` = NULL,
    `totalCount` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param maxCount Max num of samples located within one geometry.
    #' @param minCount Min num of samples located within one geometry.
    #' @param totalCount Total num of samples across all geometry objects.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`maxCount` = NULL, `minCount` = NULL, `totalCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`maxCount`, `minCount`, `totalCount`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(maxCount, minCount, totalCount)
      }
      self$`maxCount` <- `maxCount`
      self$`minCount` <- `minCount`
      self$`totalCount` <- `totalCount`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(maxCount = self$`maxCount`, minCount = self$`minCount`, totalCount = self$`totalCount`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`maxCount`)) {
          sprintf(
            '"maxCount":
            %d
                  ',
            self$`maxCount`
          )
        },
        if (!is.null(self$`minCount`)) {
          sprintf(
            '"minCount":
            %d
                  ',
            self$`minCount`
          )
        },
        if (!is.null(self$`totalCount`)) {
          sprintf(
            '"totalCount":
            %d
                  ',
            self$`totalCount`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param GeoFeaturePlotSummaryJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(GeoFeaturePlotSummaryJson, validateParams = FALSE) {
      GeoFeaturePlotSummaryObject <- jsonlite::fromJSON(GeoFeaturePlotSummaryJson)
      self$`maxCount` <- GeoFeaturePlotSummaryObject$`maxCount`
      self$`minCount` <- GeoFeaturePlotSummaryObject$`minCount`
      self$`totalCount` <- GeoFeaturePlotSummaryObject$`totalCount`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
