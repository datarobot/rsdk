# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PPSImageMetadataResponse
#'
#' @description PPSImageMetadataResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field baseImageId  character Internal base image entity id for troubleshooting purposes
#'
#' @field created  character ISO formatted image upload date
#'
#' @field datarobotRuntimeImageTag  character [optional] For internal use only.
#'
#' @field dockerImageId  character A Docker image id (immutable, content-based) hash associated with the given image
#'
#' @field filename  character The name of the file when the download requested
#'
#' @field hash  character Hash of the image content, supposed to be used for verifying content after the download. The algorithm used for hashing is specified in &#x60;hashAlgorithm&#x60; field. Note that hash is calculated over compressed image data.
#'
#' @field hashAlgorithm  character An algorithm name used for calculating content &#x60;hash&#x60;
#'
#' @field imageSize  integer Size in bytes of the compressed PPS image data
#'
#' @field shortDockerImageId  character A 12-chars shortened version of the &#x60;dockerImageId&#x60; as shown in &#39;docker images&#39; command line command output
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PPSImageMetadataResponse <- R6::R6Class(
  "PPSImageMetadataResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`baseImageId` = NULL, `created` = NULL, `datarobotRuntimeImageTag` = NULL, `dockerImageId` = NULL, `filename` = NULL, `hash` = NULL, `hashAlgorithm` = NULL, `imageSize` = NULL, `shortDockerImageId` = NULL) {
      if (!is.null(`baseImageId`)) {
        stopifnot(is.character(`baseImageId`), length(`baseImageId`) == 1)
      }
      if (!is.null(`created`)) {
        stopifnot(inherits(`created`, "POSIXt"))
      }
      if (!is.null(`dockerImageId`)) {
        stopifnot(is.character(`dockerImageId`), length(`dockerImageId`) == 1)
      }
      if (!is.null(`filename`)) {
        stopifnot(is.character(`filename`), length(`filename`) == 1)
      }
      if (!is.null(`hash`)) {
        stopifnot(is.character(`hash`), length(`hash`) == 1)
      }
      if (!is.null(`hashAlgorithm`)) {
        stopifnot(is.character(`hashAlgorithm`), length(`hashAlgorithm`) == 1)
      }
      if (!is.null(`imageSize`)) {
        stopifnot(is.numeric(`imageSize`), length(`imageSize`) == 1)
      }
      if (!is.null(`shortDockerImageId`)) {
        stopifnot(is.character(`shortDockerImageId`), length(`shortDockerImageId`) == 1)
      }
      if (!is.null(`datarobotRuntimeImageTag`)) {
        stopifnot(is.character(`datarobotRuntimeImageTag`), length(`datarobotRuntimeImageTag`) == 1)
      }
    }
  ),
  public = list(
    `baseImageId` = NULL,
    `created` = NULL,
    `datarobotRuntimeImageTag` = NULL,
    `dockerImageId` = NULL,
    `filename` = NULL,
    `hash` = NULL,
    `hashAlgorithm` = NULL,
    `imageSize` = NULL,
    `shortDockerImageId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param baseImageId Internal base image entity id for troubleshooting purposes
    #' @param created ISO formatted image upload date
    #' @param datarobotRuntimeImageTag For internal use only.
    #' @param dockerImageId A Docker image id (immutable, content-based) hash associated with the given image
    #' @param filename The name of the file when the download requested
    #' @param hash Hash of the image content, supposed to be used for verifying content after the download. The algorithm used for hashing is specified in &#x60;hashAlgorithm&#x60; field. Note that hash is calculated over compressed image data.
    #' @param hashAlgorithm An algorithm name used for calculating content &#x60;hash&#x60;
    #' @param imageSize Size in bytes of the compressed PPS image data
    #' @param shortDockerImageId A 12-chars shortened version of the &#x60;dockerImageId&#x60; as shown in &#39;docker images&#39; command line command output
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`baseImageId` = NULL, `created` = NULL, `dockerImageId` = NULL, `filename` = NULL, `hash` = NULL, `hashAlgorithm` = NULL, `imageSize` = NULL, `shortDockerImageId` = NULL, `datarobotRuntimeImageTag` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`baseImageId`, `created`, `dockerImageId`, `filename`, `hash`, `hashAlgorithm`, `imageSize`, `shortDockerImageId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(baseImageId, created, datarobotRuntimeImageTag, dockerImageId, filename, hash, hashAlgorithm, imageSize, shortDockerImageId)
      }
      self$`baseImageId` <- `baseImageId`
      self$`created` <- `created`
      self$`datarobotRuntimeImageTag` <- `datarobotRuntimeImageTag`
      self$`dockerImageId` <- `dockerImageId`
      self$`filename` <- `filename`
      self$`hash` <- `hash`
      self$`hashAlgorithm` <- `hashAlgorithm`
      self$`imageSize` <- `imageSize`
      self$`shortDockerImageId` <- `shortDockerImageId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(baseImageId = self$`baseImageId`, created = self$`created`, datarobotRuntimeImageTag = self$`datarobotRuntimeImageTag`, dockerImageId = self$`dockerImageId`, filename = self$`filename`, hash = self$`hash`, hashAlgorithm = self$`hashAlgorithm`, imageSize = self$`imageSize`, shortDockerImageId = self$`shortDockerImageId`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`baseImageId`)) {
          sprintf(
            '"baseImageId":
            "%s"
                  ',
            self$`baseImageId`
          )
        },
        if (!is.null(self$`created`)) {
          sprintf(
            '"created":
            "%s"
                  ',
            format(self$`created`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`datarobotRuntimeImageTag`)) {
          sprintf(
            '"datarobotRuntimeImageTag":
            "%s"
                  ',
            self$`datarobotRuntimeImageTag`
          )
        },
        if (!is.null(self$`dockerImageId`)) {
          sprintf(
            '"dockerImageId":
            "%s"
                  ',
            self$`dockerImageId`
          )
        },
        if (!is.null(self$`filename`)) {
          sprintf(
            '"filename":
            "%s"
                  ',
            self$`filename`
          )
        },
        if (!is.null(self$`hash`)) {
          sprintf(
            '"hash":
            "%s"
                  ',
            self$`hash`
          )
        },
        if (!is.null(self$`hashAlgorithm`)) {
          sprintf(
            '"hashAlgorithm":
            "%s"
                  ',
            self$`hashAlgorithm`
          )
        },
        if (!is.null(self$`imageSize`)) {
          sprintf(
            '"imageSize":
            %d
                  ',
            self$`imageSize`
          )
        },
        if (!is.null(self$`shortDockerImageId`)) {
          sprintf(
            '"shortDockerImageId":
            "%s"
                  ',
            self$`shortDockerImageId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param PPSImageMetadataResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(PPSImageMetadataResponseJson, validateParams = FALSE) {
      PPSImageMetadataResponseObject <- jsonlite::fromJSON(PPSImageMetadataResponseJson)
      self$`baseImageId` <- PPSImageMetadataResponseObject$`baseImageId`
      self$`created` <- ParseRFC3339Timestamp(PPSImageMetadataResponseObject$`created`)
      self$`datarobotRuntimeImageTag` <- PPSImageMetadataResponseObject$`datarobotRuntimeImageTag`
      self$`dockerImageId` <- PPSImageMetadataResponseObject$`dockerImageId`
      self$`filename` <- PPSImageMetadataResponseObject$`filename`
      self$`hash` <- PPSImageMetadataResponseObject$`hash`
      self$`hashAlgorithm` <- PPSImageMetadataResponseObject$`hashAlgorithm`
      self$`imageSize` <- PPSImageMetadataResponseObject$`imageSize`
      self$`shortDockerImageId` <- PPSImageMetadataResponseObject$`shortDockerImageId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
