# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ChallengerResponse
#'
#' @description ChallengerResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field id  character ID of the challenger.
#'
#' @field model  \link{ModelResponse}
#'
#' @field modelPackage  \link{ModelPackageResponse}
#'
#' @field name  character Name of the challenger.
#'
#' @field predictionEnvironment  \link{PredictionEnvironmentResponse} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ChallengerResponse <- R6::R6Class(
  "ChallengerResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(id = NULL, model = NULL, modelPackage = NULL, name = NULL, predictionEnvironment = NULL) {
      if (!is.null(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
      }
      if (!is.null(`model`)) {
        stopifnot(R6::is.R6(`model`))
      }
      if (!is.null(`modelPackage`)) {
        stopifnot(R6::is.R6(`modelPackage`))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`predictionEnvironment`)) {
        stopifnot(R6::is.R6(`predictionEnvironment`))
      }
    }
  ),
  public = list(
    `id` = NULL,
    `model` = NULL,
    `modelPackage` = NULL,
    `name` = NULL,
    `predictionEnvironment` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param id ID of the challenger.
    #' @param model
    #' @param modelPackage
    #' @param name Name of the challenger.
    #' @param predictionEnvironment
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`id` = NULL, `model` = NULL, `modelPackage` = NULL, `name` = NULL, `predictionEnvironment` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`id`, `model`, `modelPackage`, `name`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(id, model, modelPackage, name, predictionEnvironment)
      }
      self$`id` <- `id`
      self$`model` <- `model`
      self$`modelPackage` <- `modelPackage`
      self$`name` <- `name`
      self$`predictionEnvironment` <- `predictionEnvironment`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(id = self$`id`, model = self$`model`, modelPackage = self$`modelPackage`, name = self$`name`, predictionEnvironment = self$`predictionEnvironment`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`id`)) {
          sprintf(
            '"id":
            "%s"
                  ',
            self$`id`
          )
        },
        if (!is.null(self$`model`)) {
          sprintf(
            '"model":
          %s
        ',
            jsonlite::toJSON(self$`model`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`modelPackage`)) {
          sprintf(
            '"modelPackage":
          %s
        ',
            jsonlite::toJSON(self$`modelPackage`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`predictionEnvironment`)) {
          sprintf(
            '"predictionEnvironment":
          %s
        ',
            jsonlite::toJSON(self$`predictionEnvironment`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ChallengerResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ChallengerResponseJson, validateParams = FALSE) {
      ChallengerResponseObject <- jsonlite::fromJSON(ChallengerResponseJson)
      self$`id` <- ChallengerResponseObject$`id`
      self$`model` <- ModelResponse$new()$fromJSON(jsonlite::toJSON(ChallengerResponseObject$model, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`modelPackage` <- ModelPackageResponse$new()$fromJSON(jsonlite::toJSON(ChallengerResponseObject$modelPackage, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`name` <- ChallengerResponseObject$`name`
      self$`predictionEnvironment` <- PredictionEnvironmentResponse$new()$fromJSON(jsonlite::toJSON(ChallengerResponseObject$predictionEnvironment, auto_unbox = TRUE, digits = NA, null = "null"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
