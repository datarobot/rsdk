# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title MulticlassFeatureImpactResponse
#'
#' @description MulticlassFeatureImpactResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field classFeatureImpacts  list( \link{MulticlassFeatureImpact} ) A list of feature importance scores for each class in multiclass project.
#'
#' @field ranRedundancyDetection  character Indicates whether redundant feature identification was run while calculating this feature impact. Currently always False, as redundant feature identification isn&#39;t supported for multiclass in DataRobot.
#'
#' @field shapBased  character Indicates whether feature impact was calculated using Shapley values. Currently always &#x60;False&#x60;, as SHAP isn&#39;t supported for multiclass in DataRobot.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MulticlassFeatureImpactResponse <- R6::R6Class(
  "MulticlassFeatureImpactResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(classFeatureImpacts = NULL, ranRedundancyDetection = NULL, shapBased = NULL) {
      if (!is.null(`classFeatureImpacts`)) {
        stopifnot(is.vector(`classFeatureImpacts`))
      }
      if (!is.null(`ranRedundancyDetection`)) {
        stopifnot(is.logical(`ranRedundancyDetection`), length(`ranRedundancyDetection`) == 1)
      }
      if (!is.null(`shapBased`)) {
        stopifnot(is.logical(`shapBased`), length(`shapBased`) == 1)
      }
    }
  ),
  public = list(
    `classFeatureImpacts` = NULL,
    `ranRedundancyDetection` = NULL,
    `shapBased` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param classFeatureImpacts A list of feature importance scores for each class in multiclass project.
    #' @param ranRedundancyDetection Indicates whether redundant feature identification was run while calculating this feature impact. Currently always False, as redundant feature identification isn&#39;t supported for multiclass in DataRobot.
    #' @param shapBased Indicates whether feature impact was calculated using Shapley values. Currently always &#x60;False&#x60;, as SHAP isn&#39;t supported for multiclass in DataRobot.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`classFeatureImpacts` = NULL, `ranRedundancyDetection` = NULL, `shapBased` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`classFeatureImpacts`, `ranRedundancyDetection`, `shapBased`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(classFeatureImpacts, ranRedundancyDetection, shapBased)
      }
      sapply(`classFeatureImpacts`, function(x) stopifnot(R6::is.R6(x)))
      self$`ranRedundancyDetection` <- `ranRedundancyDetection`
      self$`shapBased` <- `shapBased`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(classFeatureImpacts = self$`classFeatureImpacts`, ranRedundancyDetection = self$`ranRedundancyDetection`, shapBased = self$`shapBased`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`classFeatureImpacts`)) {
          sprintf(
            '"classFeatureImpacts":
            [%s]
',
            paste(sapply(self$`classFeatureImpacts`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`ranRedundancyDetection`)) {
          sprintf(
            '"ranRedundancyDetection":
            %s
                  ',
            tolower(self$`ranRedundancyDetection`)
          )
        },
        if (!is.null(self$`shapBased`)) {
          sprintf(
            '"shapBased":
            %s
                  ',
            tolower(self$`shapBased`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param MulticlassFeatureImpactResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(MulticlassFeatureImpactResponseJson, validateParams = FALSE) {
      MulticlassFeatureImpactResponseObject <- jsonlite::fromJSON(MulticlassFeatureImpactResponseJson)
      self$`classFeatureImpacts` <- ApiClient$new()$deserializeObj(MulticlassFeatureImpactResponseObject$`classFeatureImpacts`, "array[MulticlassFeatureImpact]", loadNamespace("datarobot.apicore"))
      self$`ranRedundancyDetection` <- MulticlassFeatureImpactResponseObject$`ranRedundancyDetection`
      self$`shapBased` <- MulticlassFeatureImpactResponseObject$`shapBased`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
