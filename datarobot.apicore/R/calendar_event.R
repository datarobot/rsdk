# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CalendarEvent
#'
#' @description CalendarEvent Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field date  character The date of the calendar event.
#'
#' @field name  character Name of the calendar event.
#'
#' @field seriesId  character The series ID for the event. If this event does not specify a series ID, then this will be &#x60;null&#x60;, indicating that the event applies to all series.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CalendarEvent <- R6::R6Class(
  "CalendarEvent",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(date = NULL, name = NULL, seriesId = NULL) {
      if (!is.null(`date`)) {
        stopifnot(inherits(`date`, "POSIXt"))
      }
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`seriesId`)) {
        stopifnot(is.character(`seriesId`), length(`seriesId`) == 1)
      }
    }
  ),
  public = list(
    `date` = NULL,
    `name` = NULL,
    `seriesId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param date The date of the calendar event.
    #' @param name Name of the calendar event.
    #' @param seriesId The series ID for the event. If this event does not specify a series ID, then this will be &#x60;null&#x60;, indicating that the event applies to all series.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`date` = NULL, `name` = NULL, `seriesId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`date`, `name`, `seriesId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(date, name, seriesId)
      }
      self$`date` <- `date`
      self$`name` <- `name`
      self$`seriesId` <- `seriesId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(date = self$`date`, name = self$`name`, seriesId = self$`seriesId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`date`)) {
          sprintf(
            '"date":
            "%s"
                  ',
            format(self$`date`, format = "%Y-%m-%dT%H:%M:%OSZ", tz = "UTC")
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        },
        if (!is.null(self$`seriesId`)) {
          sprintf(
            '"seriesId":
            "%s"
                  ',
            self$`seriesId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CalendarEventJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CalendarEventJson, validateParams = FALSE) {
      CalendarEventObject <- jsonlite::fromJSON(CalendarEventJson)
      self$`date` <- ParseRFC3339Timestamp(CalendarEventObject$`date`)
      self$`name` <- CalendarEventObject$`name`
      self$`seriesId` <- CalendarEventObject$`seriesId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
