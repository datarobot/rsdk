# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureDriftOverTimeRetrieveResponse
#'
#' @description FeatureDriftOverTimeRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field buckets  list( \link{FeatureDriftOverTimeBucket} ) A list of aggregated drift scores by feature over a given period.
#'
#' @field metric  character Name of requested metric.
#'
#' @field summaries  list( \link{FeatureDriftOverTimeBucket} ) A list of aggregated drift scores by feature over a given period.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureDriftOverTimeRetrieveResponse <- R6::R6Class(
  "FeatureDriftOverTimeRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`buckets` = NULL, `metric` = NULL, `summaries` = NULL) {
      if (!is.null(`buckets`)) {
        stopifnot(is.vector(`buckets`))
      }
      if (!is.null(`metric`)) {
        stopifnot(is.character(`metric`), length(`metric`) == 1)
      }
      if (!is.null(`summaries`)) {
        stopifnot(is.vector(`summaries`))
      }
    }
  ),
  public = list(
    `buckets` = NULL,
    `metric` = NULL,
    `summaries` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param buckets A list of aggregated drift scores by feature over a given period.
    #' @param metric Name of requested metric.
    #' @param summaries A list of aggregated drift scores by feature over a given period.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`buckets` = NULL, `metric` = NULL, `summaries` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`buckets`, `metric`, `summaries`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(buckets, metric, summaries)
      }
      sapply(`buckets`, function(x) stopifnot(R6::is.R6(x)))
      self$`metric` <- `metric`
      sapply(`summaries`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(buckets = self$`buckets`, metric = self$`metric`, summaries = self$`summaries`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`buckets`)) {
          sprintf(
            '"buckets":
            [%s]
      ',
            paste(sapply(self$`buckets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`metric`)) {
          sprintf(
            '"metric":
            "%s"
                  ',
            self$`metric`
          )
        },
        if (!is.null(self$`summaries`)) {
          sprintf(
            '"summaries":
            [%s]
      ',
            paste(sapply(self$`summaries`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureDriftOverTimeRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureDriftOverTimeRetrieveResponseJson, validateParams = FALSE) {
      FeatureDriftOverTimeRetrieveResponseObject <- jsonlite::fromJSON(FeatureDriftOverTimeRetrieveResponseJson)
      self$`buckets` <- ApiClient$new()$deserializeObj(FeatureDriftOverTimeRetrieveResponseObject$`buckets`, "array[FeatureDriftOverTimeBucket]", loadNamespace("datarobot.apicore"))
      self$`metric` <- FeatureDriftOverTimeRetrieveResponseObject$`metric`
      self$`summaries` <- ApiClient$new()$deserializeObj(FeatureDriftOverTimeRetrieveResponseObject$`summaries`, "array[FeatureDriftOverTimeBucket]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
