# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title UserBlueprintsBulkValidationResponseItem
#'
#' @description UserBlueprintsBulkValidationResponseItem Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field userBlueprintId  character The unique id associated with the user blueprint.
#'
#' @field vertexContext  list( \link{VertexContextItem} ) Info about, warnings about, and errors with a specific vertex in the blueprint.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
UserBlueprintsBulkValidationResponseItem <- R6::R6Class(
  "UserBlueprintsBulkValidationResponseItem",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(userBlueprintId = NULL, vertexContext = NULL) {
      if (!is.null(`userBlueprintId`)) {
        stopifnot(is.character(`userBlueprintId`), length(`userBlueprintId`) == 1)
      }
      if (!is.null(`vertexContext`)) {
        stopifnot(is.vector(`vertexContext`))
      }
    }
  ),
  public = list(
    `userBlueprintId` = NULL,
    `vertexContext` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param userBlueprintId The unique id associated with the user blueprint.
    #' @param vertexContext Info about, warnings about, and errors with a specific vertex in the blueprint.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`userBlueprintId` = NULL, `vertexContext` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`userBlueprintId`, `vertexContext`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(userBlueprintId, vertexContext)
      }
      self$`userBlueprintId` <- `userBlueprintId`
      sapply(`vertexContext`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(userBlueprintId = self$`userBlueprintId`, vertexContext = self$`vertexContext`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`userBlueprintId`)) {
          sprintf(
            '"userBlueprintId":
            "%s"
                  ',
            self$`userBlueprintId`
          )
        },
        if (!is.null(self$`vertexContext`)) {
          sprintf(
            '"vertexContext":
            [%s]
',
            paste(sapply(self$`vertexContext`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param UserBlueprintsBulkValidationResponseItemJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(UserBlueprintsBulkValidationResponseItemJson, validateParams = FALSE) {
      UserBlueprintsBulkValidationResponseItemObject <- jsonlite::fromJSON(UserBlueprintsBulkValidationResponseItemJson)
      self$`userBlueprintId` <- UserBlueprintsBulkValidationResponseItemObject$`userBlueprintId`
      self$`vertexContext` <- ApiClient$new()$deserializeObj(UserBlueprintsBulkValidationResponseItemObject$`vertexContext`, "array[VertexContextItem]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
