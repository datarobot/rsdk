# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ShapImpactRetrieveResponse
#'
#' @description ShapImpactRetrieveResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer The number of shapImpact object returned
#'
#' @field rowCount  integer [optional] The number of rows from dataset to use.
#'
#' @field shapImpacts  list( \link{ShapImpactsResponse} ) A list which contains shap impact scores for top 1000 features used by a model
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShapImpactRetrieveResponse <- R6::R6Class(
  "ShapImpactRetrieveResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `rowCount` = NULL, `shapImpacts` = NULL) {
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`shapImpacts`)) {
        stopifnot(is.vector(`shapImpacts`), sapply(`shapImpacts`, R6::is.R6))
      }
      if (!is.null(`rowCount`)) {
        stopifnot(is.numeric(`rowCount`), length(`rowCount`) == 1)
      }
    }
  ),
  public = list(
    `count` = NULL,
    `rowCount` = NULL,
    `shapImpacts` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count The number of shapImpact object returned
    #' @param rowCount The number of rows from dataset to use.
    #' @param shapImpacts A list which contains shap impact scores for top 1000 features used by a model
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `shapImpacts` = NULL, `rowCount` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`count`, `shapImpacts`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, rowCount, shapImpacts)
      }
      self$`count` <- `count`
      self$`rowCount` <- `rowCount`
      self$`shapImpacts` <- `shapImpacts`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, rowCount = self$`rowCount`, shapImpacts = self$`shapImpacts`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`rowCount`)) {
          sprintf(
            '"rowCount":
            %d
                  ',
            self$`rowCount`
          )
        },
        if (!is.null(self$`shapImpacts`)) {
          sprintf(
            '"shapImpacts":
            [%s]
      ',
            paste(sapply(self$`shapImpacts`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ShapImpactRetrieveResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ShapImpactRetrieveResponseJson, validateParams = FALSE) {
      ShapImpactRetrieveResponseObject <- jsonlite::fromJSON(ShapImpactRetrieveResponseJson)
      self$`count` <- ShapImpactRetrieveResponseObject$`count`
      self$`rowCount` <- ShapImpactRetrieveResponseObject$`rowCount`
      self$`shapImpacts` <- ApiClient$new()$deserializeObj(ShapImpactRetrieveResponseObject$`shapImpacts`, "array[ShapImpactsResponse]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
