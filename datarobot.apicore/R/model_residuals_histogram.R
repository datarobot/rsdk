# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelResidualsHistogram
#'
#' @description ModelResidualsHistogram Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field intervalEnd  numeric The interval end. For all but the last interval, the end value is exclusive
#'
#' @field intervalStart  numeric The interval start
#'
#' @field occurrences  integer The number of times the predicted value fits within the interval
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelResidualsHistogram <- R6::R6Class(
  "ModelResidualsHistogram",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(intervalEnd = NULL, intervalStart = NULL, occurrences = NULL) {
      if (!is.null(`intervalEnd`)) {
      }
      if (!is.null(`intervalStart`)) {
      }
      if (!is.null(`occurrences`)) {
        stopifnot(is.numeric(`occurrences`), length(`occurrences`) == 1)
      }
    }
  ),
  public = list(
    `intervalEnd` = NULL,
    `intervalStart` = NULL,
    `occurrences` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param intervalEnd The interval end. For all but the last interval, the end value is exclusive
    #' @param intervalStart The interval start
    #' @param occurrences The number of times the predicted value fits within the interval
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`intervalEnd` = NULL, `intervalStart` = NULL, `occurrences` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`intervalEnd`, `intervalStart`, `occurrences`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(intervalEnd, intervalStart, occurrences)
      }
      self$`intervalEnd` <- `intervalEnd`
      self$`intervalStart` <- `intervalStart`
      self$`occurrences` <- `occurrences`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(intervalEnd = self$`intervalEnd`, intervalStart = self$`intervalStart`, occurrences = self$`occurrences`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`intervalEnd`)) {
          sprintf(
            '"intervalEnd":
            %d
                  ',
            self$`intervalEnd`
          )
        },
        if (!is.null(self$`intervalStart`)) {
          sprintf(
            '"intervalStart":
            %d
                  ',
            self$`intervalStart`
          )
        },
        if (!is.null(self$`occurrences`)) {
          sprintf(
            '"occurrences":
            %d
                  ',
            self$`occurrences`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelResidualsHistogramJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelResidualsHistogramJson, validateParams = FALSE) {
      ModelResidualsHistogramObject <- jsonlite::fromJSON(ModelResidualsHistogramJson)
      self$`intervalEnd` <- ModelResidualsHistogramObject$`intervalEnd`
      self$`intervalStart` <- ModelResidualsHistogramObject$`intervalStart`
      self$`occurrences` <- ModelResidualsHistogramObject$`occurrences`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
