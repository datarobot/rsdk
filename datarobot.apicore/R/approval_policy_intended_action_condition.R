# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ApprovalPolicyIntendedActionCondition
#'
#' @description ApprovalPolicyIntendedActionCondition Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field condition  character Condition for the field content to trigger on.
#'
#' @field fieldName  character Name of the attribute of the entity to filter entities by. An example value is &#x60;&#x60;importance&#x60;&#x60; attribute for the deployment entity.
#'
#' @field values  list( character ) Array of field values to apply condition to trigger on. If &#x60;&#x60;null&#x60;&#x60; for &#x60;&#x60;equals&#x60;&#x60; condition, then any value for the field is accepted.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApprovalPolicyIntendedActionCondition <- R6::R6Class(
  "ApprovalPolicyIntendedActionCondition",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`condition` = NULL, `fieldName` = NULL, `values` = NULL) {
      if (!is.null(`condition`)) {
        stopifnot(is.character(`condition`), length(`condition`) == 1)
      }
      if (!is.null(`fieldName`)) {
        stopifnot(is.character(`fieldName`), length(`fieldName`) == 1)
      }
      if (!is.null(`values`)) {
        stopifnot(is.vector(`values`), sapply(`values`, is.character))
      }
    }
  ),
  public = list(
    `condition` = NULL,
    `fieldName` = NULL,
    `values` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param condition Condition for the field content to trigger on.
    #' @param fieldName Name of the attribute of the entity to filter entities by. An example value is &#x60;&#x60;importance&#x60;&#x60; attribute for the deployment entity.
    #' @param values Array of field values to apply condition to trigger on. If &#x60;&#x60;null&#x60;&#x60; for &#x60;&#x60;equals&#x60;&#x60; condition, then any value for the field is accepted.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`condition` = NULL, `fieldName` = NULL, `values` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`condition`, `fieldName`, `values`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(condition, fieldName, values)
      }
      self$`condition` <- `condition`
      self$`fieldName` <- `fieldName`
      self$`values` <- `values`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(condition = self$`condition`, fieldName = self$`fieldName`, values = self$`values`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`condition`)) {
          sprintf(
            '"condition":
            "%s"
                  ',
            self$`condition`
          )
        },
        if (!is.null(self$`fieldName`)) {
          sprintf(
            '"fieldName":
            "%s"
                  ',
            self$`fieldName`
          )
        },
        if (!is.null(self$`values`)) {
          sprintf(
            '"values":
            [%s]
                  ',
            paste(unlist(lapply(self$`values`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ApprovalPolicyIntendedActionConditionJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ApprovalPolicyIntendedActionConditionJson, validateParams = FALSE) {
      ApprovalPolicyIntendedActionConditionObject <- jsonlite::fromJSON(ApprovalPolicyIntendedActionConditionJson)
      self$`condition` <- ApprovalPolicyIntendedActionConditionObject$`condition`
      self$`fieldName` <- ApprovalPolicyIntendedActionConditionObject$`fieldName`
      self$`values` <- ApiClient$new()$deserializeObj(ApprovalPolicyIntendedActionConditionObject$`values`, "array[character]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
