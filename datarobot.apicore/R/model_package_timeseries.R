# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ModelPackageTimeseries
#'
#' @description ModelPackageTimeseries Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datetimeColumnFormat  character Date format for forecast date and forecast point column
#'
#' @field datetimeColumnName  character Name of the forecast date column
#'
#' @field effectiveFeatureDerivationWindowEnd  integer Same concept as &#x60;featureDerivationWindowEnd&#x60; which is chosen by the user and based on the initial sampled data from the eda sample. When the dataset goes through aim, the pipeline reads the full dataset and figures out the \&quot;real\&quot; FDW (i.e., the effective FDW). For most models, eFDW is approximately the same as the FDW.
#'
#' @field effectiveFeatureDerivationWindowStart  integer Same concept as &#x60;featureDerivationWindowStart&#x60; which is chosen by the user and based on the initial sampled data from the eda sample. When the dataset goes through aim, the pipeline reads the full dataset and figures out the \&quot;real\&quot; FDW (i.e., the effective FDW). For most models, eFDW is approximately the same as the FDW.
#'
#' @field featureDerivationWindowEnd  integer Negative number or zero defining how many time units of the forecast distances time unit into the past relative to the forecast point the feature derivation window should end.
#'
#' @field featureDerivationWindowStart  integer Negative number or zero defining how many time units of the forecast distances time unit into the past relative to the forecast point the feature derivation window should begin.
#'
#' @field forecastDistanceColumnName  character Name of the forecast distance column
#'
#' @field forecastDistances  list( integer ) List of integer forecast distances
#'
#' @field forecastDistancesTimeUnit  character The time unit of forecast distances
#'
#' @field forecastPointColumnName  character Name of the forecast point column
#'
#' @field isCrossSeries  character true if the model is cross-series.
#'
#' @field isNewSeriesSupport  character true if the model is optimized to support new series.
#'
#' @field isTraditionalTimeSeries  character true if the model is traditional time series.
#'
#' @field seriesColumnName  character Name of the series column in case of multi-series date
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ModelPackageTimeseries <- R6::R6Class(
  "ModelPackageTimeseries",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datetimeColumnFormat = NULL, datetimeColumnName = NULL, effectiveFeatureDerivationWindowEnd = NULL, effectiveFeatureDerivationWindowStart = NULL, featureDerivationWindowEnd = NULL, featureDerivationWindowStart = NULL, forecastDistanceColumnName = NULL, forecastDistances = NULL, forecastDistancesTimeUnit = NULL, forecastPointColumnName = NULL, isCrossSeries = NULL, isNewSeriesSupport = NULL, isTraditionalTimeSeries = NULL, seriesColumnName = NULL) {
      if (!is.null(`datetimeColumnFormat`)) {
        stopifnot(is.character(`datetimeColumnFormat`), length(`datetimeColumnFormat`) == 1)
      }
      if (!is.null(`datetimeColumnName`)) {
        stopifnot(is.character(`datetimeColumnName`), length(`datetimeColumnName`) == 1)
      }
      if (!is.null(`effectiveFeatureDerivationWindowEnd`)) {
        stopifnot(is.numeric(`effectiveFeatureDerivationWindowEnd`), length(`effectiveFeatureDerivationWindowEnd`) == 1)
      }
      if (!is.null(`effectiveFeatureDerivationWindowStart`)) {
        stopifnot(is.numeric(`effectiveFeatureDerivationWindowStart`), length(`effectiveFeatureDerivationWindowStart`) == 1)
      }
      if (!is.null(`featureDerivationWindowEnd`)) {
        stopifnot(is.numeric(`featureDerivationWindowEnd`), length(`featureDerivationWindowEnd`) == 1)
      }
      if (!is.null(`featureDerivationWindowStart`)) {
        stopifnot(is.numeric(`featureDerivationWindowStart`), length(`featureDerivationWindowStart`) == 1)
      }
      if (!is.null(`forecastDistanceColumnName`)) {
        stopifnot(is.character(`forecastDistanceColumnName`), length(`forecastDistanceColumnName`) == 1)
      }
      if (!is.null(`forecastDistances`)) {
        stopifnot(is.vector(`forecastDistances`))
      }
      if (!is.null(`forecastDistancesTimeUnit`)) {
        stopifnot(is.character(`forecastDistancesTimeUnit`), length(`forecastDistancesTimeUnit`) == 1)
      }
      if (!is.null(`forecastPointColumnName`)) {
        stopifnot(is.character(`forecastPointColumnName`), length(`forecastPointColumnName`) == 1)
      }
      if (!is.null(`isCrossSeries`)) {
        stopifnot(is.logical(`isCrossSeries`), length(`isCrossSeries`) == 1)
      }
      if (!is.null(`isNewSeriesSupport`)) {
        stopifnot(is.logical(`isNewSeriesSupport`), length(`isNewSeriesSupport`) == 1)
      }
      if (!is.null(`isTraditionalTimeSeries`)) {
        stopifnot(is.logical(`isTraditionalTimeSeries`), length(`isTraditionalTimeSeries`) == 1)
      }
      if (!is.null(`seriesColumnName`)) {
        stopifnot(is.character(`seriesColumnName`), length(`seriesColumnName`) == 1)
      }
    }
  ),
  public = list(
    `datetimeColumnFormat` = NULL,
    `datetimeColumnName` = NULL,
    `effectiveFeatureDerivationWindowEnd` = NULL,
    `effectiveFeatureDerivationWindowStart` = NULL,
    `featureDerivationWindowEnd` = NULL,
    `featureDerivationWindowStart` = NULL,
    `forecastDistanceColumnName` = NULL,
    `forecastDistances` = NULL,
    `forecastDistancesTimeUnit` = NULL,
    `forecastPointColumnName` = NULL,
    `isCrossSeries` = NULL,
    `isNewSeriesSupport` = NULL,
    `isTraditionalTimeSeries` = NULL,
    `seriesColumnName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datetimeColumnFormat Date format for forecast date and forecast point column
    #' @param datetimeColumnName Name of the forecast date column
    #' @param effectiveFeatureDerivationWindowEnd Same concept as &#x60;featureDerivationWindowEnd&#x60; which is chosen by the user and based on the initial sampled data from the eda sample. When the dataset goes through aim, the pipeline reads the full dataset and figures out the \&quot;real\&quot; FDW (i.e., the effective FDW). For most models, eFDW is approximately the same as the FDW.
    #' @param effectiveFeatureDerivationWindowStart Same concept as &#x60;featureDerivationWindowStart&#x60; which is chosen by the user and based on the initial sampled data from the eda sample. When the dataset goes through aim, the pipeline reads the full dataset and figures out the \&quot;real\&quot; FDW (i.e., the effective FDW). For most models, eFDW is approximately the same as the FDW.
    #' @param featureDerivationWindowEnd Negative number or zero defining how many time units of the forecast distances time unit into the past relative to the forecast point the feature derivation window should end.
    #' @param featureDerivationWindowStart Negative number or zero defining how many time units of the forecast distances time unit into the past relative to the forecast point the feature derivation window should begin.
    #' @param forecastDistanceColumnName Name of the forecast distance column
    #' @param forecastDistances List of integer forecast distances
    #' @param forecastDistancesTimeUnit The time unit of forecast distances
    #' @param forecastPointColumnName Name of the forecast point column
    #' @param isCrossSeries true if the model is cross-series.
    #' @param isNewSeriesSupport true if the model is optimized to support new series.
    #' @param isTraditionalTimeSeries true if the model is traditional time series.
    #' @param seriesColumnName Name of the series column in case of multi-series date
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datetimeColumnFormat` = NULL, `datetimeColumnName` = NULL, `effectiveFeatureDerivationWindowEnd` = NULL, `effectiveFeatureDerivationWindowStart` = NULL, `featureDerivationWindowEnd` = NULL, `featureDerivationWindowStart` = NULL, `forecastDistanceColumnName` = NULL, `forecastDistances` = NULL, `forecastDistancesTimeUnit` = NULL, `forecastPointColumnName` = NULL, `isCrossSeries` = NULL, `isNewSeriesSupport` = NULL, `isTraditionalTimeSeries` = NULL, `seriesColumnName` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datetimeColumnFormat`, `datetimeColumnName`, `effectiveFeatureDerivationWindowEnd`, `effectiveFeatureDerivationWindowStart`, `featureDerivationWindowEnd`, `featureDerivationWindowStart`, `forecastDistanceColumnName`, `forecastDistances`, `forecastDistancesTimeUnit`, `forecastPointColumnName`, `isCrossSeries`, `isNewSeriesSupport`, `isTraditionalTimeSeries`, `seriesColumnName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datetimeColumnFormat, datetimeColumnName, effectiveFeatureDerivationWindowEnd, effectiveFeatureDerivationWindowStart, featureDerivationWindowEnd, featureDerivationWindowStart, forecastDistanceColumnName, forecastDistances, forecastDistancesTimeUnit, forecastPointColumnName, isCrossSeries, isNewSeriesSupport, isTraditionalTimeSeries, seriesColumnName)
      }
      self$`datetimeColumnFormat` <- `datetimeColumnFormat`
      self$`datetimeColumnName` <- `datetimeColumnName`
      self$`effectiveFeatureDerivationWindowEnd` <- `effectiveFeatureDerivationWindowEnd`
      self$`effectiveFeatureDerivationWindowStart` <- `effectiveFeatureDerivationWindowStart`
      self$`featureDerivationWindowEnd` <- `featureDerivationWindowEnd`
      self$`featureDerivationWindowStart` <- `featureDerivationWindowStart`
      self$`forecastDistanceColumnName` <- `forecastDistanceColumnName`
      sapply(`forecastDistances`, function(x) stopifnot(is.character(x)))
      self$`forecastDistancesTimeUnit` <- `forecastDistancesTimeUnit`
      self$`forecastPointColumnName` <- `forecastPointColumnName`
      self$`isCrossSeries` <- `isCrossSeries`
      self$`isNewSeriesSupport` <- `isNewSeriesSupport`
      self$`isTraditionalTimeSeries` <- `isTraditionalTimeSeries`
      self$`seriesColumnName` <- `seriesColumnName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datetimeColumnFormat = self$`datetimeColumnFormat`, datetimeColumnName = self$`datetimeColumnName`, effectiveFeatureDerivationWindowEnd = self$`effectiveFeatureDerivationWindowEnd`, effectiveFeatureDerivationWindowStart = self$`effectiveFeatureDerivationWindowStart`, featureDerivationWindowEnd = self$`featureDerivationWindowEnd`, featureDerivationWindowStart = self$`featureDerivationWindowStart`, forecastDistanceColumnName = self$`forecastDistanceColumnName`, forecastDistances = self$`forecastDistances`, forecastDistancesTimeUnit = self$`forecastDistancesTimeUnit`, forecastPointColumnName = self$`forecastPointColumnName`, isCrossSeries = self$`isCrossSeries`, isNewSeriesSupport = self$`isNewSeriesSupport`, isTraditionalTimeSeries = self$`isTraditionalTimeSeries`, seriesColumnName = self$`seriesColumnName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datetimeColumnFormat`)) {
          sprintf(
            '"datetimeColumnFormat":
            "%s"
                  ',
            self$`datetimeColumnFormat`
          )
        },
        if (!is.null(self$`datetimeColumnName`)) {
          sprintf(
            '"datetimeColumnName":
            "%s"
                  ',
            self$`datetimeColumnName`
          )
        },
        if (!is.null(self$`effectiveFeatureDerivationWindowEnd`)) {
          sprintf(
            '"effectiveFeatureDerivationWindowEnd":
            %d
                  ',
            self$`effectiveFeatureDerivationWindowEnd`
          )
        },
        if (!is.null(self$`effectiveFeatureDerivationWindowStart`)) {
          sprintf(
            '"effectiveFeatureDerivationWindowStart":
            %d
                  ',
            self$`effectiveFeatureDerivationWindowStart`
          )
        },
        if (!is.null(self$`featureDerivationWindowEnd`)) {
          sprintf(
            '"featureDerivationWindowEnd":
            %d
                  ',
            self$`featureDerivationWindowEnd`
          )
        },
        if (!is.null(self$`featureDerivationWindowStart`)) {
          sprintf(
            '"featureDerivationWindowStart":
            %d
                  ',
            self$`featureDerivationWindowStart`
          )
        },
        if (!is.null(self$`forecastDistanceColumnName`)) {
          sprintf(
            '"forecastDistanceColumnName":
            "%s"
                  ',
            self$`forecastDistanceColumnName`
          )
        },
        if (!is.null(self$`forecastDistances`)) {
          sprintf(
            '"forecastDistances":
              [%s]
            ',
            paste(unlist(lapply(self$`forecastDistances`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`forecastDistancesTimeUnit`)) {
          sprintf(
            '"forecastDistancesTimeUnit":
            "%s"
                  ',
            self$`forecastDistancesTimeUnit`
          )
        },
        if (!is.null(self$`forecastPointColumnName`)) {
          sprintf(
            '"forecastPointColumnName":
            "%s"
                  ',
            self$`forecastPointColumnName`
          )
        },
        if (!is.null(self$`isCrossSeries`)) {
          sprintf(
            '"isCrossSeries":
            %s
                  ',
            tolower(self$`isCrossSeries`)
          )
        },
        if (!is.null(self$`isNewSeriesSupport`)) {
          sprintf(
            '"isNewSeriesSupport":
            %s
                  ',
            tolower(self$`isNewSeriesSupport`)
          )
        },
        if (!is.null(self$`isTraditionalTimeSeries`)) {
          sprintf(
            '"isTraditionalTimeSeries":
            %s
                  ',
            tolower(self$`isTraditionalTimeSeries`)
          )
        },
        if (!is.null(self$`seriesColumnName`)) {
          sprintf(
            '"seriesColumnName":
            "%s"
                  ',
            self$`seriesColumnName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ModelPackageTimeseriesJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ModelPackageTimeseriesJson, validateParams = FALSE) {
      ModelPackageTimeseriesObject <- jsonlite::fromJSON(ModelPackageTimeseriesJson)
      self$`datetimeColumnFormat` <- ModelPackageTimeseriesObject$`datetimeColumnFormat`
      self$`datetimeColumnName` <- ModelPackageTimeseriesObject$`datetimeColumnName`
      self$`effectiveFeatureDerivationWindowEnd` <- ModelPackageTimeseriesObject$`effectiveFeatureDerivationWindowEnd`
      self$`effectiveFeatureDerivationWindowStart` <- ModelPackageTimeseriesObject$`effectiveFeatureDerivationWindowStart`
      self$`featureDerivationWindowEnd` <- ModelPackageTimeseriesObject$`featureDerivationWindowEnd`
      self$`featureDerivationWindowStart` <- ModelPackageTimeseriesObject$`featureDerivationWindowStart`
      self$`forecastDistanceColumnName` <- ModelPackageTimeseriesObject$`forecastDistanceColumnName`
      self$`forecastDistances` <- ApiClient$new()$deserializeObj(ModelPackageTimeseriesObject$`forecastDistances`, "array[integer]", loadNamespace("datarobot.apicore"))
      self$`forecastDistancesTimeUnit` <- ModelPackageTimeseriesObject$`forecastDistancesTimeUnit`
      self$`forecastPointColumnName` <- ModelPackageTimeseriesObject$`forecastPointColumnName`
      self$`isCrossSeries` <- ModelPackageTimeseriesObject$`isCrossSeries`
      self$`isNewSeriesSupport` <- ModelPackageTimeseriesObject$`isNewSeriesSupport`
      self$`isTraditionalTimeSeries` <- ModelPackageTimeseriesObject$`isTraditionalTimeSeries`
      self$`seriesColumnName` <- ModelPackageTimeseriesObject$`seriesColumnName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
