# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BatchPredictionJobPredictionInstance
#'
#' @description BatchPredictionJobPredictionInstance Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field apiKey  character [optional] By default, prediction requests will use the API key of the user that created the job. This allows you to make requests on behalf of other users.
#'
#' @field datarobotKey  character [optional] If running a job against a prediction instance in the Managed AI Cloud, you must provide the organization level DataRobot-Key.
#'
#' @field hostName  character Override the default host name of the deployment with this.
#'
#' @field sslEnabled  character Use SSL (HTTPS) when communicating with the overriden prediction server.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BatchPredictionJobPredictionInstance <- R6::R6Class(
  "BatchPredictionJobPredictionInstance",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(apiKey = NULL, datarobotKey = NULL, hostName = NULL, sslEnabled = NULL) {
      if (!is.null(`hostName`)) {
        stopifnot(is.character(`hostName`), length(`hostName`) == 1)
      }
      if (!is.null(`sslEnabled`)) {
        stopifnot(is.logical(`sslEnabled`), length(`sslEnabled`) == 1)
      }
      if (!is.null(`apiKey`)) {
        stopifnot(is.character(`apiKey`), length(`apiKey`) == 1)
      }
      if (!is.null(`datarobotKey`)) {
        stopifnot(is.character(`datarobotKey`), length(`datarobotKey`) == 1)
      }
    }
  ),
  public = list(
    `apiKey` = NULL,
    `datarobotKey` = NULL,
    `hostName` = NULL,
    `sslEnabled` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param apiKey By default, prediction requests will use the API key of the user that created the job. This allows you to make requests on behalf of other users.
    #' @param datarobotKey If running a job against a prediction instance in the Managed AI Cloud, you must provide the organization level DataRobot-Key.
    #' @param hostName Override the default host name of the deployment with this.
    #' @param sslEnabled Use SSL (HTTPS) when communicating with the overriden prediction server.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`hostName` = NULL, `sslEnabled` = NULL, `apiKey` = NULL, `datarobotKey` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`hostName`, `sslEnabled`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(apiKey, datarobotKey, hostName, sslEnabled)
      }
      self$`apiKey` <- `apiKey`
      self$`datarobotKey` <- `datarobotKey`
      self$`hostName` <- `hostName`
      self$`sslEnabled` <- `sslEnabled`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(apiKey = self$`apiKey`, datarobotKey = self$`datarobotKey`, hostName = self$`hostName`, sslEnabled = self$`sslEnabled`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`apiKey`)) {
          sprintf(
            '"apiKey":
            "%s"
                  ',
            self$`apiKey`
          )
        },
        if (!is.null(self$`datarobotKey`)) {
          sprintf(
            '"datarobotKey":
            "%s"
                  ',
            self$`datarobotKey`
          )
        },
        if (!is.null(self$`hostName`)) {
          sprintf(
            '"hostName":
            "%s"
                  ',
            self$`hostName`
          )
        },
        if (!is.null(self$`sslEnabled`)) {
          sprintf(
            '"sslEnabled":
            %s
                  ',
            tolower(self$`sslEnabled`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param BatchPredictionJobPredictionInstanceJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(BatchPredictionJobPredictionInstanceJson, validateParams = FALSE) {
      BatchPredictionJobPredictionInstanceObject <- jsonlite::fromJSON(BatchPredictionJobPredictionInstanceJson)
      self$`apiKey` <- BatchPredictionJobPredictionInstanceObject$`apiKey`
      self$`datarobotKey` <- BatchPredictionJobPredictionInstanceObject$`datarobotKey`
      self$`hostName` <- BatchPredictionJobPredictionInstanceObject$`hostName`
      self$`sslEnabled` <- BatchPredictionJobPredictionInstanceObject$`sslEnabled`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
