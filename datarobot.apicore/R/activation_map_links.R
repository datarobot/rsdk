# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ActivationMapLinks
#'
#' @description ActivationMapLinks Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field downloadOriginalImage  character URL of the original image
#'
#' @field downloadOverlayImage  character URL of the original image overlaid by the activation heatmap
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ActivationMapLinks <- R6::R6Class(
  "ActivationMapLinks",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`downloadOriginalImage` = NULL, `downloadOverlayImage` = NULL) {
      if (!is.null(`downloadOriginalImage`)) {
        stopifnot(is.character(`downloadOriginalImage`), length(`downloadOriginalImage`) == 1)
      }
      if (!is.null(`downloadOverlayImage`)) {
        stopifnot(is.character(`downloadOverlayImage`), length(`downloadOverlayImage`) == 1)
      }
    }
  ),
  public = list(
    `downloadOriginalImage` = NULL,
    `downloadOverlayImage` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param downloadOriginalImage URL of the original image
    #' @param downloadOverlayImage URL of the original image overlaid by the activation heatmap
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`downloadOriginalImage` = NULL, `downloadOverlayImage` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`downloadOriginalImage`, `downloadOverlayImage`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(downloadOriginalImage, downloadOverlayImage)
      }
      self$`downloadOriginalImage` <- `downloadOriginalImage`
      self$`downloadOverlayImage` <- `downloadOverlayImage`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(downloadOriginalImage = self$`downloadOriginalImage`, downloadOverlayImage = self$`downloadOverlayImage`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`downloadOriginalImage`)) {
          sprintf(
            '"downloadOriginalImage":
            "%s"
                  ',
            self$`downloadOriginalImage`
          )
        },
        if (!is.null(self$`downloadOverlayImage`)) {
          sprintf(
            '"downloadOverlayImage":
            "%s"
                  ',
            self$`downloadOverlayImage`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param ActivationMapLinksJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(ActivationMapLinksJson, validateParams = FALSE) {
      ActivationMapLinksObject <- jsonlite::fromJSON(ActivationMapLinksJson)
      self$`downloadOriginalImage` <- ActivationMapLinksObject$`downloadOriginalImage`
      self$`downloadOverlayImage` <- ActivationMapLinksObject$`downloadOverlayImage`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
