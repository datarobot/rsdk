# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentCreateFromProjectRecommendedModel
#'
#' @description DeploymentCreateFromProjectRecommendedModel Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field defaultPredictionServerId  character [optional] ID of the default prediction server for deployment. Required for DataRobot Cloud, must be omitted for Enterprise installations.
#'
#' @field description  character A description for the deployment.
#'
#' @field importance  character [optional] Used only when &#x60;Model Deployment Approvals Workflow&#x60; is enabled. The parameter to show how important the deployment is. If &#x60;Model Deployment Approvals Workflow&#x60; is enabled and &#x60;&#x60;materiality&#x60;&#x60; is not specified defaults to &#x60;&#x60;MODERATE&#x60;&#x60;.
#'
#' @field label  character A human-readable name for the deployment.
#'
#' @field projectId  character ID of the project entry to deploy recommended model from.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentCreateFromProjectRecommendedModel <- R6::R6Class(
  "DeploymentCreateFromProjectRecommendedModel",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(defaultPredictionServerId = NULL, description = NULL, importance = NULL, label = NULL, projectId = NULL) {
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`defaultPredictionServerId`)) {
        stopifnot(is.character(`defaultPredictionServerId`), length(`defaultPredictionServerId`) == 1)
      }
      if (!is.null(`importance`)) {
        stopifnot(is.character(`importance`), length(`importance`) == 1)
      }
    }
  ),
  public = list(
    `defaultPredictionServerId` = NULL,
    `description` = NULL,
    `importance` = NULL,
    `label` = NULL,
    `projectId` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param defaultPredictionServerId ID of the default prediction server for deployment. Required for DataRobot Cloud, must be omitted for Enterprise installations.
    #' @param description A description for the deployment.
    #' @param importance Used only when &#x60;Model Deployment Approvals Workflow&#x60; is enabled. The parameter to show how important the deployment is. If &#x60;Model Deployment Approvals Workflow&#x60; is enabled and &#x60;&#x60;materiality&#x60;&#x60; is not specified defaults to &#x60;&#x60;MODERATE&#x60;&#x60;.
    #' @param label A human-readable name for the deployment.
    #' @param projectId ID of the project entry to deploy recommended model from.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`description` = NULL, `label` = NULL, `projectId` = NULL, `defaultPredictionServerId` = NULL, `importance` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`description`, `label`, `projectId`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(defaultPredictionServerId, description, importance, label, projectId)
      }
      self$`defaultPredictionServerId` <- `defaultPredictionServerId`
      self$`description` <- `description`
      self$`importance` <- `importance`
      self$`label` <- `label`
      self$`projectId` <- `projectId`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(defaultPredictionServerId = self$`defaultPredictionServerId`, description = self$`description`, importance = self$`importance`, label = self$`label`, projectId = self$`projectId`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`defaultPredictionServerId`)) {
          sprintf(
            '"defaultPredictionServerId":
            "%s"
                  ',
            self$`defaultPredictionServerId`
          )
        },
        if (!is.null(self$`description`)) {
          sprintf(
            '"description":
            "%s"
                  ',
            self$`description`
          )
        },
        if (!is.null(self$`importance`)) {
          sprintf(
            '"importance":
            "%s"
                  ',
            self$`importance`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentCreateFromProjectRecommendedModelJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentCreateFromProjectRecommendedModelJson, validateParams = FALSE) {
      DeploymentCreateFromProjectRecommendedModelObject <- jsonlite::fromJSON(DeploymentCreateFromProjectRecommendedModelJson)
      self$`defaultPredictionServerId` <- DeploymentCreateFromProjectRecommendedModelObject$`defaultPredictionServerId`
      self$`description` <- DeploymentCreateFromProjectRecommendedModelObject$`description`
      self$`importance` <- DeploymentCreateFromProjectRecommendedModelObject$`importance`
      self$`label` <- DeploymentCreateFromProjectRecommendedModelObject$`label`
      self$`projectId` <- DeploymentCreateFromProjectRecommendedModelObject$`projectId`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
