# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CredentialId
#'
#' @description CredentialId Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field catalogVersionId  character [optional] The ID of the latest version of the catalog entry.
#'
#' @field credentialId  character The ID of the set of credentials to use instead of user and password. Note that with this change, username and password will become optional.
#'
#' @field url  character [optional] The link to retrieve more detailed information about the entity that uses this catalog dataset.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialId <- R6::R6Class(
  "CredentialId",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`catalogVersionId` = NULL, `credentialId` = NULL, `url` = NULL) {
      if (!is.null(`credentialId`)) {
        stopifnot(is.character(`credentialId`), length(`credentialId`) == 1)
      }
      if (!is.null(`catalogVersionId`)) {
        stopifnot(is.character(`catalogVersionId`), length(`catalogVersionId`) == 1)
      }
      if (!is.null(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
      }
    }
  ),
  public = list(
    `catalogVersionId` = NULL,
    `credentialId` = NULL,
    `url` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param catalogVersionId The ID of the latest version of the catalog entry.
    #' @param credentialId The ID of the set of credentials to use instead of user and password. Note that with this change, username and password will become optional.
    #' @param url The link to retrieve more detailed information about the entity that uses this catalog dataset.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`credentialId` = NULL, `catalogVersionId` = NULL, `url` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`credentialId`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(catalogVersionId, credentialId, url)
      }
      self$`catalogVersionId` <- `catalogVersionId`
      self$`credentialId` <- `credentialId`
      self$`url` <- `url`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(catalogVersionId = self$`catalogVersionId`, credentialId = self$`credentialId`, url = self$`url`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`catalogVersionId`)) {
          sprintf(
            '"catalogVersionId":
            "%s"
                  ',
            self$`catalogVersionId`
          )
        },
        if (!is.null(self$`credentialId`)) {
          sprintf(
            '"credentialId":
            "%s"
                  ',
            self$`credentialId`
          )
        },
        if (!is.null(self$`url`)) {
          sprintf(
            '"url":
            "%s"
                  ',
            self$`url`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CredentialIdJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CredentialIdJson, validateParams = FALSE) {
      CredentialIdObject <- jsonlite::fromJSON(CredentialIdJson)
      self$`catalogVersionId` <- CredentialIdObject$`catalogVersionId`
      self$`credentialId` <- CredentialIdObject$`credentialId`
      self$`url` <- CredentialIdObject$`url`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
