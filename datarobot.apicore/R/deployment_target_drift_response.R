# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentTargetDriftResponse
#'
#' @description DeploymentTargetDriftResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field baselineSampleSize  integer sample size of the training data.
#'
#' @field driftScore  numeric drift score for the target.
#'
#' @field metric  character [optional] Metric used to calculate drift score.
#'
#' @field modelId  character id of the model for which data drift is being retrieved.
#'
#' @field period  \link{TimeRange}
#'
#' @field sampleSize  integer number of predictions used to compute the drift score.
#'
#' @field segmentAttribute  character [optional] The name of the segment on which segment analysis is being performed.
#'
#' @field segmentValue  character [optional] The value of the &#x60;segmentAttribute&#x60; to segment on.
#'
#' @field targetName  character name of the target feature.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentTargetDriftResponse <- R6::R6Class(
  "DeploymentTargetDriftResponse",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(baselineSampleSize = NULL, driftScore = NULL, metric = NULL, modelId = NULL, period = NULL, sampleSize = NULL, segmentAttribute = NULL, segmentValue = NULL, targetName = NULL) {
      if (!is.null(`baselineSampleSize`)) {
        stopifnot(is.numeric(`baselineSampleSize`), length(`baselineSampleSize`) == 1)
      }
      if (!is.null(`driftScore`)) {
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`period`)) {
        stopifnot(R6::is.R6(`period`))
      }
      if (!is.null(`sampleSize`)) {
        stopifnot(is.numeric(`sampleSize`), length(`sampleSize`) == 1)
      }
      if (!is.null(`targetName`)) {
        stopifnot(is.character(`targetName`), length(`targetName`) == 1)
      }
      if (!is.null(`metric`)) {
        stopifnot(is.character(`metric`), length(`metric`) == 1)
      }
      if (!is.null(`segmentAttribute`)) {
        stopifnot(is.character(`segmentAttribute`), length(`segmentAttribute`) == 1)
      }
      if (!is.null(`segmentValue`)) {
        stopifnot(is.character(`segmentValue`), length(`segmentValue`) == 1)
      }
    }
  ),
  public = list(
    `baselineSampleSize` = NULL,
    `driftScore` = NULL,
    `metric` = NULL,
    `modelId` = NULL,
    `period` = NULL,
    `sampleSize` = NULL,
    `segmentAttribute` = NULL,
    `segmentValue` = NULL,
    `targetName` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param baselineSampleSize sample size of the training data.
    #' @param driftScore drift score for the target.
    #' @param metric Metric used to calculate drift score.
    #' @param modelId id of the model for which data drift is being retrieved.
    #' @param period
    #' @param sampleSize number of predictions used to compute the drift score.
    #' @param segmentAttribute The name of the segment on which segment analysis is being performed.
    #' @param segmentValue The value of the &#x60;segmentAttribute&#x60; to segment on.
    #' @param targetName name of the target feature.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`baselineSampleSize` = NULL, `driftScore` = NULL, `modelId` = NULL, `period` = NULL, `sampleSize` = NULL, `targetName` = NULL, `metric` = NULL, `segmentAttribute` = NULL, `segmentValue` = "", validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`baselineSampleSize`, `driftScore`, `modelId`, `period`, `sampleSize`, `targetName`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(baselineSampleSize, driftScore, metric, modelId, period, sampleSize, segmentAttribute, segmentValue, targetName)
      }
      self$`baselineSampleSize` <- `baselineSampleSize`
      self$`driftScore` <- `driftScore`
      self$`metric` <- `metric`
      self$`modelId` <- `modelId`
      self$`period` <- `period`
      self$`sampleSize` <- `sampleSize`
      self$`segmentAttribute` <- `segmentAttribute`
      self$`segmentValue` <- `segmentValue`
      self$`targetName` <- `targetName`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(baselineSampleSize = self$`baselineSampleSize`, driftScore = self$`driftScore`, metric = self$`metric`, modelId = self$`modelId`, period = self$`period`, sampleSize = self$`sampleSize`, segmentAttribute = self$`segmentAttribute`, segmentValue = self$`segmentValue`, targetName = self$`targetName`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`baselineSampleSize`)) {
          sprintf(
            '"baselineSampleSize":
            %d
                  ',
            self$`baselineSampleSize`
          )
        },
        if (!is.null(self$`driftScore`)) {
          sprintf(
            '"driftScore":
            %d
                  ',
            self$`driftScore`
          )
        },
        if (!is.null(self$`metric`)) {
          sprintf(
            '"metric":
            "%s"
                  ',
            self$`metric`
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`period`)) {
          sprintf(
            '"period":
          %s
        ',
            jsonlite::toJSON(self$`period`$toJSON(), auto_unbox = TRUE, digits = NA)
          )
        },
        if (!is.null(self$`sampleSize`)) {
          sprintf(
            '"sampleSize":
            %d
                  ',
            self$`sampleSize`
          )
        },
        if (!is.null(self$`segmentAttribute`)) {
          sprintf(
            '"segmentAttribute":
            "%s"
                  ',
            self$`segmentAttribute`
          )
        },
        if (!is.null(self$`segmentValue`)) {
          sprintf(
            '"segmentValue":
            "%s"
                  ',
            self$`segmentValue`
          )
        },
        if (!is.null(self$`targetName`)) {
          sprintf(
            '"targetName":
            "%s"
                  ',
            self$`targetName`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentTargetDriftResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentTargetDriftResponseJson, validateParams = FALSE) {
      DeploymentTargetDriftResponseObject <- jsonlite::fromJSON(DeploymentTargetDriftResponseJson)
      self$`baselineSampleSize` <- DeploymentTargetDriftResponseObject$`baselineSampleSize`
      self$`driftScore` <- DeploymentTargetDriftResponseObject$`driftScore`
      self$`metric` <- DeploymentTargetDriftResponseObject$`metric`
      self$`modelId` <- DeploymentTargetDriftResponseObject$`modelId`
      self$`period` <- TimeRange$new()$fromJSON(jsonlite::toJSON(DeploymentTargetDriftResponseObject$period, auto_unbox = TRUE, digits = NA, null = "null"))
      self$`sampleSize` <- DeploymentTargetDriftResponseObject$`sampleSize`
      self$`segmentAttribute` <- DeploymentTargetDriftResponseObject$`segmentAttribute`
      self$`segmentValue` <- DeploymentTargetDriftResponseObject$`segmentValue`
      self$`targetName` <- DeploymentTargetDriftResponseObject$`targetName`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
