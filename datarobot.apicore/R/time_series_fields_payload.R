# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.27.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TimeSeriesFieldsPayload
#'
#' @description TimeSeriesFieldsPayload Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field forecastPoint  character [optional] A template of format for the expected day of prediction
#'
#' @field predictionEndDate  character [optional] A template of format for the end date of prediction
#'
#' @field predictionStartDate  character [optional] A template of format for the start date of prediction
#'
#' @field relaxKnownInAdvanceFeaturesCheck  character If &#x60;True&#x60;, missing values in the known in advance features are allowed in the forecast window at the prediction time. If omitted or &#x60;False&#x60;, missing values are not allowed.
#'
#' @field type  character One of forecast, historical
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TimeSeriesFieldsPayload <- R6::R6Class(
  "TimeSeriesFieldsPayload",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(forecastPoint = NULL, predictionEndDate = NULL, predictionStartDate = NULL, relaxKnownInAdvanceFeaturesCheck = NULL, type = NULL) {
      if (!is.null(`relaxKnownInAdvanceFeaturesCheck`)) {
        stopifnot(is.logical(`relaxKnownInAdvanceFeaturesCheck`), length(`relaxKnownInAdvanceFeaturesCheck`) == 1)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`forecastPoint`)) {
        stopifnot(is.character(`forecastPoint`), length(`forecastPoint`) == 1)
      }
      if (!is.null(`predictionEndDate`)) {
        stopifnot(is.character(`predictionEndDate`), length(`predictionEndDate`) == 1)
      }
      if (!is.null(`predictionStartDate`)) {
        stopifnot(is.character(`predictionStartDate`), length(`predictionStartDate`) == 1)
      }
    }
  ),
  public = list(
    `forecastPoint` = NULL,
    `predictionEndDate` = NULL,
    `predictionStartDate` = NULL,
    `relaxKnownInAdvanceFeaturesCheck` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param forecastPoint A template of format for the expected day of prediction
    #' @param predictionEndDate A template of format for the end date of prediction
    #' @param predictionStartDate A template of format for the start date of prediction
    #' @param relaxKnownInAdvanceFeaturesCheck If &#x60;True&#x60;, missing values in the known in advance features are allowed in the forecast window at the prediction time. If omitted or &#x60;False&#x60;, missing values are not allowed.
    #' @param type One of forecast, historical
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(
        `relaxKnownInAdvanceFeaturesCheck` = NULL, `type` = NULL, `forecastPoint` = NULL, `predictionEndDate` = NULL, `predictionStartDate` = NULL, validateParams = FALSE, ...
    ) {
      local.optional.var <- list(...)
      requiredProps <- list(`relaxKnownInAdvanceFeaturesCheck`, `type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(forecastPoint, predictionEndDate, predictionStartDate, relaxKnownInAdvanceFeaturesCheck, type)
      }
      self$`forecastPoint` <- `forecastPoint`
      self$`predictionEndDate` <- `predictionEndDate`
      self$`predictionStartDate` <- `predictionStartDate`
      self$`relaxKnownInAdvanceFeaturesCheck` <- `relaxKnownInAdvanceFeaturesCheck`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(forecastPoint = self$`forecastPoint`, predictionEndDate = self$`predictionEndDate`, predictionStartDate = self$`predictionStartDate`, relaxKnownInAdvanceFeaturesCheck = self$`relaxKnownInAdvanceFeaturesCheck`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`forecastPoint`)) {
        sprintf(
        '"forecastPoint":
          "%s"
                ',
        self$`forecastPoint`
        )},
        if (!is.null(self$`predictionEndDate`)) {
        sprintf(
        '"predictionEndDate":
          "%s"
                ',
        self$`predictionEndDate`
        )},
        if (!is.null(self$`predictionStartDate`)) {
        sprintf(
        '"predictionStartDate":
          "%s"
                ',
        self$`predictionStartDate`
        )},
        if (!is.null(self$`relaxKnownInAdvanceFeaturesCheck`)) {
        sprintf(
        '"relaxKnownInAdvanceFeaturesCheck":
          %s
                ',
        tolower(self$`relaxKnownInAdvanceFeaturesCheck`)
        )},
        if (!is.null(self$`type`)) {
        sprintf(
        '"type":
          "%s"
                ',
        self$`type`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TimeSeriesFieldsPayloadJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TimeSeriesFieldsPayloadJson, validateParams = FALSE) {
      TimeSeriesFieldsPayloadObject <- jsonlite::fromJSON(TimeSeriesFieldsPayloadJson)
      self$`forecastPoint` <- TimeSeriesFieldsPayloadObject$`forecastPoint`
      self$`predictionEndDate` <- TimeSeriesFieldsPayloadObject$`predictionEndDate`
      self$`predictionStartDate` <- TimeSeriesFieldsPayloadObject$`predictionStartDate`
      self$`relaxKnownInAdvanceFeaturesCheck` <- TimeSeriesFieldsPayloadObject$`relaxKnownInAdvanceFeaturesCheck`
      self$`type` <- TimeSeriesFieldsPayloadObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
