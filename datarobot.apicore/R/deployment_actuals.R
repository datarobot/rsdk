# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentActuals
#'
#' @description DeploymentActuals Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field data  list( \link{Actual} ) A list of &#x60;actual&#x60; objects that describes actual values. Minimum size of the list is 1 and maximum size is 10000 items. An &#x60;actual&#x60; object has the following schema.
#'
#' @field keepActualsWithoutPredictions  character [optional] Indicates if actual without predictions are kept.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentActuals <- R6::R6Class(
  "DeploymentActuals",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`data` = NULL, `keepActualsWithoutPredictions` = NULL) {
      if (!is.null(`data`)) {
        stopifnot(is.vector(`data`))
      }
      if (!is.null(`keepActualsWithoutPredictions`)) {
        stopifnot(is.logical(`keepActualsWithoutPredictions`), length(`keepActualsWithoutPredictions`) == 1)
      }
    }
  ),
  public = list(
    `data` = NULL,
    `keepActualsWithoutPredictions` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param data A list of &#x60;actual&#x60; objects that describes actual values. Minimum size of the list is 1 and maximum size is 10000 items. An &#x60;actual&#x60; object has the following schema.
    #' @param keepActualsWithoutPredictions Indicates if actual without predictions are kept.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`data` = NULL, `keepActualsWithoutPredictions` = TRUE, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`data`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(data, keepActualsWithoutPredictions)
      }
      sapply(`data`, function(x) stopifnot(R6::is.R6(x)))
      self$`keepActualsWithoutPredictions` <- `keepActualsWithoutPredictions`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(data = self$`data`, keepActualsWithoutPredictions = self$`keepActualsWithoutPredictions`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`data`)) {
          sprintf(
            '"data":
            [%s]
      ',
            paste(sapply(self$`data`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`keepActualsWithoutPredictions`)) {
          sprintf(
            '"keepActualsWithoutPredictions":
            %s
                  ',
            tolower(self$`keepActualsWithoutPredictions`)
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentActualsJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentActualsJson, validateParams = FALSE) {
      DeploymentActualsObject <- jsonlite::fromJSON(DeploymentActualsJson)
      self$`data` <- ApiClient$new()$deserializeObj(DeploymentActualsObject$`data`, "array[Actual]", loadNamespace("datarobot.apicore"))
      self$`keepActualsWithoutPredictions` <- DeploymentActualsObject$`keepActualsWithoutPredictions`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
