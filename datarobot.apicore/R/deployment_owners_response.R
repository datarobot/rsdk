# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title DeploymentOwnersResponse
#'
#' @description DeploymentOwnersResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  integer Total count of owners.
#'
#' @field preview  list( \link{DeploymentOwnerResponse} ) A list of owner objects.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DeploymentOwnersResponse <- R6::R6Class(
  "DeploymentOwnersResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `preview` = NULL) {
      if (!is.null(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
      }
      if (!is.null(`preview`)) {
        stopifnot(is.vector(`preview`))
      }
    }
  ),
  public = list(
    `count` = NULL,
    `preview` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count Total count of owners.
    #' @param preview A list of owner objects.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `preview` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`count`, `preview`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, preview)
      }
      self$`count` <- `count`
      sapply(`preview`, function(x) stopifnot(R6::is.R6(x)))
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, preview = self$`preview`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`preview`)) {
          sprintf(
            '"preview":
            [%s]
      ',
            paste(sapply(self$`preview`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param DeploymentOwnersResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(DeploymentOwnersResponseJson, validateParams = FALSE) {
      DeploymentOwnersResponseObject <- jsonlite::fromJSON(DeploymentOwnersResponseJson)
      self$`count` <- DeploymentOwnersResponseObject$`count`
      self$`preview` <- ApiClient$new()$deserializeObj(DeploymentOwnersResponseObject$`preview`, "array[DeploymentOwnerResponse]", loadNamespace("datarobot.apicore"))

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
