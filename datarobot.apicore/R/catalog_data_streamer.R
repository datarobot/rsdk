# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title CatalogDataStreamer
#'
#' @description CatalogDataStreamer Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field datasetId  \link{OneOfstringstring} Either the populated value of the field or [redacted] due to permission settings
#'
#' @field datasetVersionId  character [optional] The ID of the AI catalog dataset version
#'
#' @field type  character Type name for this intake type
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CatalogDataStreamer <- R6::R6Class(
  "CatalogDataStreamer",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(datasetId = NULL, datasetVersionId = NULL, type = NULL) {
      if (!is.null(`datasetId`)) {
        .setPrimitiveProperty(typeList = list("character"), propertyData = datasetId)
      }
      if (!is.null(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
      }
      if (!is.null(`datasetVersionId`)) {
        stopifnot(is.character(`datasetVersionId`), length(`datasetVersionId`) == 1)
      }
    }
  ),
  public = list(
    `datasetId` = NULL,
    `datasetVersionId` = NULL,
    `type` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param datasetId Either the populated value of the field or [redacted] due to permission settings
    #' @param datasetVersionId The ID of the AI catalog dataset version
    #' @param type Type name for this intake type
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`datasetId` = NULL, `type` = NULL, `datasetVersionId` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`datasetId`, `type`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(datasetId, datasetVersionId, type)
      }
      self$`datasetId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = datasetId)
      self$`datasetVersionId` <- `datasetVersionId`
      self$`type` <- `type`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(datasetId = self$`datasetId`, datasetVersionId = self$`datasetVersionId`, type = self$`type`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`datasetId`)) {
          sprintf(
            '"datasetId":
          %s
        ',
            self$`datasetId`
          )
        },
        if (!is.null(self$`datasetVersionId`)) {
          sprintf(
            '"datasetVersionId":
            "%s"
                  ',
            self$`datasetVersionId`
          )
        },
        if (!is.null(self$`type`)) {
          sprintf(
            '"type":
            "%s"
                  ',
            self$`type`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param CatalogDataStreamerJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(CatalogDataStreamerJson, validateParams = FALSE) {
      CatalogDataStreamerObject <- jsonlite::fromJSON(CatalogDataStreamerJson)
      self$`datasetId` <- .setPrimitiveProperty(typeList = list("character"), propertyData = CatalogDataStreamerObject$datasetId)
      self$`datasetVersionId` <- CatalogDataStreamerObject$`datasetVersionId`
      self$`type` <- CatalogDataStreamerObject$`type`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
