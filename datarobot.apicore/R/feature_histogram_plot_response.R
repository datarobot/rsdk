# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FeatureHistogramPlotResponse
#'
#' @description FeatureHistogramPlotResponse Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field count  numeric number of values in the bin (or weights if project is weighted)
#'
#' @field label  character bin start for numerical/uncapped, or string value for categorical. The bin &#x60;&#x3D;&#x3D;Missing&#x3D;&#x3D;&#x60; is created for rows that did not have the feature.
#'
#' @field target  numeric Average value of the target feature values for the bin. For regression projects, it will be null, if the feature was deemed as low informative or project target has not been selected yet or AIM processing has not finished yet. You can use :http:get:&#x60;/api/v2/projects/(projectId)/features/&#x60; endpoint to find more about low informative features. For binary classification, the same conditions apply as above, but the value should be treated as the ratio of total positives in the bin to bin&#39;s total size (&#x60;count&#x60;). For multiclass projects the value is always null.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FeatureHistogramPlotResponse <- R6::R6Class(
  "FeatureHistogramPlotResponse",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`count` = NULL, `label` = NULL, `target` = NULL) {
      if (!is.null(`count`)) {
      }
      if (!is.null(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
      }
      if (!is.null(`target`)) {
      }
    }
  ),
  public = list(
    `count` = NULL,
    `label` = NULL,
    `target` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param count number of values in the bin (or weights if project is weighted)
    #' @param label bin start for numerical/uncapped, or string value for categorical. The bin &#x60;&#x3D;&#x3D;Missing&#x3D;&#x3D;&#x60; is created for rows that did not have the feature.
    #' @param target Average value of the target feature values for the bin. For regression projects, it will be null, if the feature was deemed as low informative or project target has not been selected yet or AIM processing has not finished yet. You can use :http:get:&#x60;/api/v2/projects/(projectId)/features/&#x60; endpoint to find more about low informative features. For binary classification, the same conditions apply as above, but the value should be treated as the ratio of total positives in the bin to bin&#39;s total size (&#x60;count&#x60;). For multiclass projects the value is always null.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`count` = NULL, `label` = NULL, `target` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`count`, `label`, `target`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(count, label, target)
      }
      self$`count` <- `count`
      self$`label` <- `label`
      self$`target` <- `target`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(count = self$`count`, label = self$`label`, target = self$`target`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`count`)) {
          sprintf(
            '"count":
            %d
                  ',
            self$`count`
          )
        },
        if (!is.null(self$`label`)) {
          sprintf(
            '"label":
            "%s"
                  ',
            self$`label`
          )
        },
        if (!is.null(self$`target`)) {
          sprintf(
            '"target":
            %d
                  ',
            self$`target`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param FeatureHistogramPlotResponseJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(FeatureHistogramPlotResponseJson, validateParams = FALSE) {
      FeatureHistogramPlotResponseObject <- jsonlite::fromJSON(FeatureHistogramPlotResponseJson)
      self$`count` <- FeatureHistogramPlotResponseObject$`count`
      self$`label` <- FeatureHistogramPlotResponseObject$`label`
      self$`target` <- FeatureHistogramPlotResponseObject$`target`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
