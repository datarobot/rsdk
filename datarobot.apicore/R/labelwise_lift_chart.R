# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title LabelwiseLiftChart
#'
#' @description LabelwiseLiftChart Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field labelBins  list( \link{LabelwiseLiftChartItem} ) Lift charts for the given data source.
#'
#' @field labels  list( character ) All available target labels for this insight.
#'
#' @field modelId  character Model Id.
#'
#' @field projectId  character Project Id.
#'
#' @field source  character Data source of Lift charts.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
LabelwiseLiftChart <- R6::R6Class(
  "LabelwiseLiftChart",
  lock_objects = FALSE,
  private = list(
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(labelBins = NULL, labels = NULL, modelId = NULL, projectId = NULL, source = NULL) {
      if (!is.null(`labelBins`)) {
        stopifnot(is.vector(`labelBins`))
      }
      if (!is.null(`labels`)) {
        stopifnot(is.vector(`labels`))
      }
      if (!is.null(`modelId`)) {
        stopifnot(is.character(`modelId`), length(`modelId`) == 1)
      }
      if (!is.null(`projectId`)) {
        stopifnot(is.character(`projectId`), length(`projectId`) == 1)
      }
      if (!is.null(`source`)) {
        stopifnot(is.character(`source`), length(`source`) == 1)
      }
    }
  ),
  public = list(
    `labelBins` = NULL,
    `labels` = NULL,
    `modelId` = NULL,
    `projectId` = NULL,
    `source` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param labelBins Lift charts for the given data source.
    #' @param labels All available target labels for this insight.
    #' @param modelId Model Id.
    #' @param projectId Project Id.
    #' @param source Data source of Lift charts.
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`labelBins` = NULL, `labels` = NULL, `modelId` = NULL, `projectId` = NULL, `source` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      requiredProps <- list(`labelBins`, `labels`, `modelId`, `projectId`, `source`)
      if (validateParams) {
        lapply(requiredProps, missing)
        private$validateProps(labelBins, labels, modelId, projectId, source)
      }
      sapply(`labelBins`, function(x) stopifnot(R6::is.R6(x)))
      sapply(`labels`, function(x) stopifnot(is.character(x)))
      self$`modelId` <- `modelId`
      self$`projectId` <- `projectId`
      self$`source` <- `source`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    validate = function() {
      props <- list(labelBins = self$`labelBins`, labels = self$`labels`, modelId = self$`modelId`, projectId = self$`projectId`, source = self$`source`)
      do.call(private$validateProps, props)
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`labelBins`)) {
          sprintf(
            '"labelBins":
            [%s]
',
            paste(sapply(self$`labelBins`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox = TRUE, digits = NA)), collapse = ",")
          )
        },
        if (!is.null(self$`labels`)) {
          sprintf(
            '"labels":
              [%s]
            ',
            paste(unlist(lapply(self$`labels`, function(x) paste0('"', x, '"'))), collapse = ",")
          )
        },
        if (!is.null(self$`modelId`)) {
          sprintf(
            '"modelId":
            "%s"
                  ',
            self$`modelId`
          )
        },
        if (!is.null(self$`projectId`)) {
          sprintf(
            '"projectId":
            "%s"
                  ',
            self$`projectId`
          )
        },
        if (!is.null(self$`source`)) {
          sprintf(
            '"source":
            "%s"
                  ',
            self$`source`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param LabelwiseLiftChartJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(LabelwiseLiftChartJson, validateParams = FALSE) {
      LabelwiseLiftChartObject <- jsonlite::fromJSON(LabelwiseLiftChartJson)
      self$`labelBins` <- ApiClient$new()$deserializeObj(LabelwiseLiftChartObject$`labelBins`, "array[LabelwiseLiftChartItem]", loadNamespace("datarobot.apicore"))
      self$`labels` <- ApiClient$new()$deserializeObj(LabelwiseLiftChartObject$`labels`, "array[character]", loadNamespace("datarobot.apicore"))
      self$`modelId` <- LabelwiseLiftChartObject$`modelId`
      self$`projectId` <- LabelwiseLiftChartObject$`projectId`
      self$`source` <- LabelwiseLiftChartObject$`source`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
