# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TransformationParam
#'
#' @description TransformationParam Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field currentValue  \link{AnyOfintegernumber} [optional] Current transformation value
#'
#' @field maxValue  \link{AnyOfintegernumber} [optional] Max transformation value
#'
#' @field minValue  \link{AnyOfintegernumber} [optional] Min transformation value
#'
#' @field name  character Transformation param name
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TransformationParam <- R6::R6Class(
  "TransformationParam",
  lock_objects = FALSE,
  private = list(
    # @description The properties of this object that are required to be set.
    # @description A helper function to handle assist with type validation. This function will validate class parameters with definite
    # types assigned to them, as well as handling validation of parameters with anyOf and oneOf types listed. These types
    # can themselves be other R6 objects.
    validateProps = function(`currentValue` = NULL, `maxValue` = NULL, `minValue` = NULL, `name` = NULL) {
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
      }
      if (!is.null(`currentValue`)) {
        .setPrimitiveProperty(typeList = list("numeric"), propertyData = currentValue)
      }
      if (!is.null(`maxValue`)) {
        .setPrimitiveProperty(typeList = list("numeric"), propertyData = maxValue)
      }
      if (!is.null(`minValue`)) {
        .setPrimitiveProperty(typeList = list("numeric"), propertyData = minValue)
      }
    }
  ),
  public = list(
    `currentValue` = NULL,
    `maxValue` = NULL,
    `minValue` = NULL,
    `name` = NULL,
    #' @description A function used to initialize an instance of this class.
    #' @param currentValue Current transformation value
    #' @param maxValue Max transformation value
    #' @param minValue Min transformation value
    #' @param name Transformation param name
    #' @param validateParams An optional param for auto validating this object's parameters before initialization. Default FALSE.
    #' @param ... Any additional keyword arguments to be passed into this object for initialization.
    initialize = function(`name` = NULL, `currentValue` = NULL, `maxValue` = NULL, `minValue` = NULL, validateParams = FALSE, ...) {
      local.optional.var <- list(...)
      if (validateParams) {
        lapply(list(`name`), function(param) {
          stopifnot("Required param not set." = !is.null(param))
        })
        private$validateProps(currentValue, maxValue, minValue, name)
      }
      self$`currentValue` <- .setPrimitiveProperty(typeList = list("numeric"), propertyData = currentValue)
      self$`maxValue` <- .setPrimitiveProperty(typeList = list("numeric"), propertyData = maxValue)
      self$`minValue` <- .setPrimitiveProperty(typeList = list("numeric"), propertyData = minValue)
      self$`name` <- `name`
    },
    #' @description A helper function that provides public access to the private validateProps function. This allows users the ability
    #' to programmatically validate objects before sending them to DataRobot.
    #' checking this objects set properties.
    validate = function() {
      do.call(private$validateProps, list(currentValue = self$`currentValue`, maxValue = self$`maxValue`, minValue = self$`minValue`, name = self$`name`))
    },
    #' @description A helper function that serializes this object into a JSON encoded string.
    toJSON = function() {
      jsoncontent <- c(
        if (!is.null(self$`currentValue`)) {
          sprintf(
            '"currentValue":
            %s
      ',
            self$`currentValue`
          )
        },
        if (!is.null(self$`maxValue`)) {
          sprintf(
            '"maxValue":
            %s
      ',
            self$`maxValue`
          )
        },
        if (!is.null(self$`minValue`)) {
          sprintf(
            '"minValue":
            %s
      ',
            self$`minValue`
          )
        },
        if (!is.null(self$`name`)) {
          sprintf(
            '"name":
            "%s"
                  ',
            self$`name`
          )
        }
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste("{", jsoncontent, "}", sep = "")
    },
    #' @description A helper function that deserializes a JSON string into an instance of this class.
    #' @param TransformationParamJson A JSON encoded string representation of a class instance.
    #' @param validateParams An optional param for auto validating this object's parameters after deserialization. Default FALSE.
    fromJSON = function(TransformationParamJson, validateParams = FALSE) {
      TransformationParamObject <- jsonlite::fromJSON(TransformationParamJson)
      self$`currentValue` <- .setPrimitiveProperty(typeList = list("numeric"), propertyData = TransformationParamObject$currentValue)
      self$`maxValue` <- .setPrimitiveProperty(typeList = list("numeric"), propertyData = TransformationParamObject$maxValue)
      self$`minValue` <- .setPrimitiveProperty(typeList = list("numeric"), propertyData = TransformationParamObject$minValue)
      self$`name` <- TransformationParamObject$`name`

      if (validateParams) {
        self$validate()
      }

      return(self)
    }
  )
)
