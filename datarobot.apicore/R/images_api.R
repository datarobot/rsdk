# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Images operations
#' @description datarobot.apicore.Images
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#' @export
ImagesApi <- R6::R6Class(
  "ImagesApi",
  public = list(
    apiClient = NULL,

    #' @param apiClient A configurable `ApiClient` instance. If none provided, a new client with default configuration will be created.
    initialize = function(apiClient) {
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      } else {
        self$apiClient <- ApiClient$new()
      }
    },
    #' @description Retrieve a text file containing the images data quality log.
    #' Produces: NA
    #'
    #' @details Retrieve a text file containing the images data quality log.
    #' @details This method invokes `GET /datasets/{datasetId}/imagesDataQualityLog/file/` in the DataRobot Public API.
    #' @param datasetId character. The ID of the dataset
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** The response will contain a text file with the contents of the images data quality log.
    #' \itemize{
    #' \item **`ContentMinusDisposition`** &#x60;attachment;filename&#x3D;&lt;filename&gt;.txt&#x60; The suggested filename is dynamically generated
    #' \item **`ContentMinusType`** MIME type of the returned data
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' datasetId <- 'datasetId_example' # character | The ID of the dataset
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$DatasetsImagesDataQualityLogFileList(datasetId)
    #' }
    DatasetsImagesDataQualityLogFileList = function(datasetId, ...) {
      apiResponse <- private$DatasetsImagesDataQualityLogFileListWithHttpInfo(datasetId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve the images data quality log content and log length as JSON
    #' Produces: "application/json"
    #'
    #' @details Retrieve the images data quality log content and log length as JSON
    #' @details This method invokes `GET /datasets/{datasetId}/imagesDataQualityLog/` in the DataRobot Public API.
    #' @param datasetId character. The ID of the dataset
    #' @param offset integer. This many results will be skipped.
    #' @param limit integer. At most this many results are returned.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImagesDataQualityLogLinesResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Paginated list of lines of the image data quality log
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' datasetId <- 'datasetId_example' # character | The ID of the dataset
    #' offset <- 0 # integer | This many results will be skipped.
    #' limit <- 100 # integer | At most this many results are returned.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$DatasetsImagesDataQualityLogList(datasetId, offset=offset, limit=limit)
    #' }
    DatasetsImagesDataQualityLogList = function(datasetId, offset = 0, limit = 100, ...) {
      apiResponse <- private$DatasetsImagesDataQualityLogListWithHttpInfo(datasetId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Creates a new augmentation list based on the posted payload data.
    #' Produces: "application/json"
    #'
    #' @details Creates a new augmentation list based on the posted payload data.
    #' @details This method invokes `POST /imageAugmentationLists/` in the DataRobot Public API.
    #' @param imageAugmentationCreateList \link{ImageAugmentationCreateList}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageAugmentationListCreateResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`201`** Augmentation list id
    #' \itemize{
    #' }
    #' \item **`422`** Unable to create image augmentation list with the given input
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' imageAugmentationCreateList <- ImageAugmentationCreateList$new() # ImageAugmentationCreateList |
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationListsCreate(imageAugmentationCreateList=imageAugmentationCreateList)
    #' }
    ImageAugmentationListsCreate = function(imageAugmentationCreateList = NULL, ...) {
      apiResponse <- private$ImageAugmentationListsCreateWithHttpInfo(imageAugmentationCreateList, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Delete an existing augmentation lists by id
    #' Produces: NA
    #'
    #' @details Delete an existing augmentation lists by id
    #' @details This method invokes `DELETE /imageAugmentationLists/{augmentationId}/` in the DataRobot Public API.
    #' @param augmentationId character. The id of the augmentation list to fetch
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`** The successful response is empty
    #' \itemize{
    #' }
    #' \item **`400`** Cannot delete an augmentation list that is marked as in_use.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' augmentationId <- 'augmentationId_example' # character | The id of the augmentation list to fetch
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationListsDelete(augmentationId)
    #' }
    ImageAugmentationListsDelete = function(augmentationId, ...) {
      apiResponse <- private$ImageAugmentationListsDeleteWithHttpInfo(augmentationId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List of augmentation lists that match the specified query.
    #' Produces: "application/json"
    #'
    #' @details List of augmentation lists that match the specified query.
    #' @details This method invokes `GET /imageAugmentationLists/` in the DataRobot Public API.
    #' @param projectId character. Project ID to retrieve augmentation lists from
    #' @param offset integer. This many results will be skipped
    #' @param limit integer. At most this many results are returned. To specify no limit, use 0. The default may change without notice.
    #' @param featureName character. Name of the image feature that the augmentation list is associated with
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageAugmentationListsResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Image Augmentation Lists
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | Project ID to retrieve augmentation lists from
    #' offset <- 0 # integer | This many results will be skipped
    #' limit <- 100 # integer | At most this many results are returned. To specify no limit, use 0. The default may change without notice.
    #' featureName <- 'featureName_example' # character | Name of the image feature that the augmentation list is associated with
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationListsList(projectId, offset, limit, featureName=featureName)
    #' }
    ImageAugmentationListsList = function(projectId, offset, limit, featureName = NULL, ...) {
      apiResponse <- private$ImageAugmentationListsListWithHttpInfo(projectId, offset, limit, featureName, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Update and existing augmentation list, with passed in values.
    #' Produces: NA
    #'
    #' @details Update and existing augmentation list, with passed in values.
    #' @details This method invokes `PATCH /imageAugmentationLists/{augmentationId}/` in the DataRobot Public API.
    #' @param augmentationId character. The id of the augmentation list to fetch
    #' @param imageAugmentationListPatchParam \link{ImageAugmentationListPatchParam}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`204`** The successful response is empty
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' augmentationId <- 'augmentationId_example' # character | The id of the augmentation list to fetch
    #' imageAugmentationListPatchParam <- ImageAugmentationListPatchParam$new() # ImageAugmentationListPatchParam |
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationListsPatch(augmentationId, imageAugmentationListPatchParam=imageAugmentationListPatchParam)
    #' }
    ImageAugmentationListsPatch = function(augmentationId, imageAugmentationListPatchParam = NULL, ...) {
      apiResponse <- private$ImageAugmentationListsPatchWithHttpInfo(augmentationId, imageAugmentationListPatchParam, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Returns a single augmentation list with the specified id.
    #' Produces: "application/json"
    #'
    #' @details Returns a single augmentation list with the specified id.
    #' @details This method invokes `GET /imageAugmentationLists/{augmentationId}/` in the DataRobot Public API.
    #' @param augmentationId character. The id of the augmentation list to fetch
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageAugmentationRetrieveList}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Returns an augmentation list object
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' augmentationId <- 'augmentationId_example' # character | The id of the augmentation list to fetch
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationListsRetrieve(augmentationId)
    #' }
    ImageAugmentationListsRetrieve = function(augmentationId, ...) {
      apiResponse <- private$ImageAugmentationListsRetrieveWithHttpInfo(augmentationId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Requests the creation of sample augmentations based on the augmentation list passed in.
    #' Produces: "application/json"
    #'
    #' @details This endpoint will schedule a job to augment the specified images from a project&#39;s dataset and return a link to monitor the status of the job, as well as a link to retrieve the resulting augmented images.
    #' @details This method invokes `POST /imageAugmentationLists/{augmentationId}/samples/` in the DataRobot Public API.
    #' @param augmentationId character. The id of the augmentation list
    #' @param imageAugmentationListsCreateSamples \link{ImageAugmentationListsCreateSamples}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageAugmentationSamplesResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`202`** Image Augmentation Samples generation has been successfully requested
    #' \itemize{
    #' \item **`Location`** URL that can be polled to check the status of the operation.
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' augmentationId <- 'augmentationId_example' # character | The id of the augmentation list
    #' imageAugmentationListsCreateSamples <- ImageAugmentationListsCreateSamples$new() # ImageAugmentationListsCreateSamples |
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationListsSamplesCreate(augmentationId, imageAugmentationListsCreateSamples=imageAugmentationListsCreateSamples)
    #' }
    ImageAugmentationListsSamplesCreate = function(augmentationId, imageAugmentationListsCreateSamples = NULL, ...) {
      apiResponse <- private$ImageAugmentationListsSamplesCreateWithHttpInfo(augmentationId, imageAugmentationListsCreateSamples, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve latest Augmentation Samples generated for list
    #' Produces: "application/json"
    #'
    #' @details Retrieve latest Augmentation Samples generated for list
    #' @details This method invokes `GET /imageAugmentationLists/{augmentationId}/samples/` in the DataRobot Public API.
    #' @param augmentationId character. The id of the augmentation list
    #' @param offset integer. This many results will be skipped.
    #' @param limit integer. At most this many results are returned.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageAugmentationListsRetrieveSamplesResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`404`** Augmentation Samples not found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' augmentationId <- 'augmentationId_example' # character | The id of the augmentation list
    #' offset <- 0 # integer | This many results will be skipped.
    #' limit <- 100 # integer | At most this many results are returned.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationListsSamplesList(augmentationId, offset=offset, limit=limit)
    #' }
    ImageAugmentationListsSamplesList = function(augmentationId, offset = 0, limit = 100, ...) {
      apiResponse <- private$ImageAugmentationListsSamplesListWithHttpInfo(augmentationId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Augmentation list of all available transformations that are supported in the system.
    #' Produces: "application/json"
    #'
    #' @details Augmentation list of all available transformations that are supported in the system.
    #' @details This method invokes `GET /imageAugmentationOptions/{projectId}/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageAugmentationOptionsResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Augmentation list object
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationOptionsRetrieve(projectId)
    #' }
    ImageAugmentationOptionsRetrieve = function(projectId, ...) {
      apiResponse <- private$ImageAugmentationOptionsRetrieveWithHttpInfo(projectId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description (Deprecated in v2.28) Requests the creation of sample augmentations based on the augmentation list parameters passed in. This route is being deprecated please use &#x60;POST /imageAugmentationLists/&lt;augmentationId&gt;/samples&#x60; route instead.
    #' Produces: "application/json"
    #'
    #' @details (Deprecated in v2.28) This endpoint will schedule a job to augment the specified images from a project&#39;s dataset and return a link to monitor the status of the job, as well as a link to retrieve the resulting augmented images.
    #' @details This method invokes `POST /imageAugmentationSamples/` in the DataRobot Public API.
    #' @param imageAugmentationSamplesRequest \link{ImageAugmentationSamplesRequest}.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageAugmentationSamplesResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`202`** Image Augmentation Samples generation has been successfully requested
    #' \itemize{
    #' \item **`Location`** URL that can be polled to check the status of the operation.
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' imageAugmentationSamplesRequest <- ImageAugmentationSamplesRequest$new() # ImageAugmentationSamplesRequest |
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationSamplesCreate(imageAugmentationSamplesRequest=imageAugmentationSamplesRequest)
    #' }
    ImageAugmentationSamplesCreate = function(imageAugmentationSamplesRequest = NULL, ...) {
      apiResponse <- private$ImageAugmentationSamplesCreateWithHttpInfo(imageAugmentationSamplesRequest, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description (Deprecated in v2.28) Retrieve previously generated Augmentation Samples. This route is being deprecated please use &#x60;GET /imageAugmentationLists/&lt;augmentationId&gt;/samples&#x60; route instead.
    #' Produces: "application/json"
    #'
    #' @details (Deprecated in v2.28) Retrieve previously generated Augmentation Samples. This route is being deprecated please use GET /imageAugmentationLists/\\&lt;augmentationId\\&gt;/samples route instead.
    #' @details This method invokes `GET /imageAugmentationSamples/{samplesId}/` in the DataRobot Public API.
    #' @param samplesId character. Id of the augmentation sample
    #' @param offset integer. This many results will be skipped.
    #' @param limit integer. At most this many results are returned.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageAugmentationRetrieveSamplesResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`**
    #' \itemize{
    #' }
    #' \item **`404`** Augmentation Samples not found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' samplesId <- 'samplesId_example' # character | Id of the augmentation sample
    #' offset <- 0 # integer | This many results will be skipped.
    #' limit <- 100 # integer | At most this many results are returned.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ImageAugmentationSamplesList(samplesId, offset=offset, limit=limit)
    #' }
    ImageAugmentationSamplesList = function(samplesId, offset = 0, limit = 100, ...) {
      apiResponse <- private$ImageAugmentationSamplesListWithHttpInfo(samplesId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Get a list of duplicate images containing the number of occurrences of each image
    #' Produces: "application/json"
    #'
    #' @details Get a list of duplicate images containing the number of occurrences of each image
    #' @details This method invokes `GET /projects/{projectId}/duplicateImages/{column}/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param column character. Column parameter to filter the list of duplicate images returned
    #' @param offset integer. This many results will be skipped.
    #' @param limit integer. At most this many results are returned.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{DuplicateImageTableResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** List of image metadata
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #' column <- 'column_example' # character | Column parameter to filter the list of duplicate images returned
    #' offset <- 0 # integer | This many results will be skipped.
    #' limit <- 100 # integer | At most this many results are returned.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsDuplicateImagesList(projectId, column, offset=offset, limit=limit)
    #' }
    ProjectsDuplicateImagesList = function(projectId, column, offset = 0, limit = 100, ...) {
      apiResponse <- private$ProjectsDuplicateImagesListWithHttpInfo(projectId, column, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List all Image Activation Maps for the project.
    #' Produces: "application/json"
    #'
    #' @details List all Image Activation Maps for the project.
    #' @details This method invokes `GET /projects/{projectId}/imageActivationMaps/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param offset integer. The number of items to skip over.
    #' @param limit integer. The number of items to return.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ActivationMapsListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Image Activation Maps
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #' offset <- 0 # integer | The number of items to skip over.
    #' limit <- 100 # integer | The number of items to return.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImageActivationMapsList(projectId, offset=offset, limit=limit)
    #' }
    ProjectsImageActivationMapsList = function(projectId, offset = 0, limit = 100, ...) {
      apiResponse <- private$ProjectsImageActivationMapsListWithHttpInfo(projectId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List image bins and covers for every target value or range.
    #' Produces: "application/json"
    #'
    #' @details List image bins and covers for every target value or range.
    #' @details This method invokes `GET /projects/{projectId}/imageBins/` in the DataRobot Public API.
    #' @param featureName character. Name of the image feature
    #' @param projectId character. The project ID
    #' @param offset integer. This many results will be skipped.
    #' @param limit integer. At most this many results are returned.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageBinsListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Returns a list of image metadata
    #' \itemize{
    #' }
    #' \item **`422`** The request cannot be processed
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' featureName <- 'featureName_example' # character | Name of the image feature
    #' projectId <- 'projectId_example' # character | The project ID
    #' offset <- 0 # integer | This many results will be skipped.
    #' limit <- 100 # integer | At most this many results are returned.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImageBinsList(featureName, projectId, offset=offset, limit=limit)
    #' }
    ProjectsImageBinsList = function(featureName, projectId, offset = 0, limit = 100, ...) {
      apiResponse <- private$ProjectsImageBinsListWithHttpInfo(featureName, projectId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List all Image Embeddings for the project.
    #' Produces: "application/json"
    #'
    #' @details List all Image Embeddings for the project.
    #' @details This method invokes `GET /projects/{projectId}/imageEmbeddings/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param offset integer. The number of items to skip over.
    #' @param limit integer. The number of items to return.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{EmbeddingsListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Image Embeddings
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #' offset <- 0 # integer | The number of items to skip over.
    #' limit <- 100 # integer | The number of items to return.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImageEmbeddingsList(projectId, offset=offset, limit=limit)
    #' }
    ProjectsImageEmbeddingsList = function(projectId, offset = 0, limit = 100, ...) {
      apiResponse <- private$ProjectsImageEmbeddingsListWithHttpInfo(projectId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description List all metadata for images in the EDA sample
    #' Produces: "application/json"
    #'
    #' @details List all metadata for images in the EDA sample
    #' @details This method invokes `GET /projects/{projectId}/imageSamples/` in the DataRobot Public API.
    #' @param featureName character. Name of the image feature
    #' @param projectId character. The project ID
    #' @param offset integer. This many results will be skipped.
    #' @param limit integer. At most this many results are returned.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageMetadataListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Paginated list of image metadata
    #' \itemize{
    #' }
    #' \item **`422`** The request cannot be processed
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' featureName <- 'featureName_example' # character | Name of the image feature
    #' projectId <- 'projectId_example' # character | The project ID
    #' offset <- 0 # integer | This many results will be skipped.
    #' limit <- 100 # integer | At most this many results are returned.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImageSamplesList(featureName, projectId, offset=offset, limit=limit)
    #' }
    ProjectsImageSamplesList = function(featureName, projectId, offset = 0, limit = 100, ...) {
      apiResponse <- private$ProjectsImageSamplesListWithHttpInfo(featureName, projectId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve a text file containing the images data quality log
    #' Produces: NA
    #'
    #' @details Retrieve a text file containing the images data quality log
    #' @details This method invokes `GET /projects/{projectId}/imagesDataQualityLog/file/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** The response will contain a text file with the contents of the images data quality log.
    #' \itemize{
    #' \item **`ContentMinusDisposition`** &#x60;attachment;filename&#x3D;&lt;filename&gt;.txt&#x60; The suggested filename is dynamically generated
    #' \item **`ContentMinusType`** MIME type of the returned data
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImagesDataQualityLogFileList(projectId)
    #' }
    ProjectsImagesDataQualityLogFileList = function(projectId, ...) {
      apiResponse <- private$ProjectsImagesDataQualityLogFileListWithHttpInfo(projectId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve the images data quality log content and log length as JSON
    #' Produces: "application/json"
    #'
    #' @details Retrieve the images data quality log content and log length as JSON
    #' @details This method invokes `GET /projects/{projectId}/imagesDataQualityLog/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param offset integer. This many results will be skipped.
    #' @param limit integer. At most this many results are returned.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImagesDataQualityLogLinesResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Paginated list of lines of the image data quality log
    #' \itemize{
    #' }
    #' \item **`422`** Not a data quality enabled project
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #' offset <- 0 # integer | This many results will be skipped.
    #' limit <- 100 # integer | At most this many results are returned.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImagesDataQualityLogList(projectId, offset=offset, limit=limit)
    #' }
    ProjectsImagesDataQualityLogList = function(projectId, offset = 0, limit = 100, ...) {
      apiResponse <- private$ProjectsImagesDataQualityLogListWithHttpInfo(projectId, offset, limit, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Returns a file for a single image
    #' Produces: NA
    #'
    #' @details Returns a file for a single image
    #' @details This method invokes `GET /projects/{projectId}/images/{imageId}/file/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param imageId character. Id of the image
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** The response is an image file (not JSON) that can be saved or displayed.
    #' \itemize{
    #' }
    #' \item **`404`** Image not found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #' imageId <- 'imageId_example' # character | Id of the image
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImagesFileList(projectId, imageId)
    #' }
    ProjectsImagesFileList = function(projectId, imageId, ...) {
      apiResponse <- private$ProjectsImagesFileListWithHttpInfo(projectId, imageId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        if (httr::has_content(resp)) {
          httr::content(resp)
        }
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Returns a list of image metadata elements.
    #' Produces: "application/json"
    #'
    #' @details Returns a list of image metadata elements.
    #' @details This method invokes `GET /projects/{projectId}/images/` in the DataRobot Public API.
    #' @param offset integer. This many results will be skipped
    #' @param limit integer. At most this many results are returned
    #' @param projectId character. The project ID
    #' @param column character. Name of the column to query
    #' @param targetValue \link{AnyOfnumberstring}. For classification projects - when specified, only images corresponding to this target value will be returned. Mutually exclusive with targetBinStart/targetBinEnd.
    #' @param targetBinStart \link{AnyOfnumberstring}. For regression projects - when specified, only images corresponding to the target values above this will be returned. Mutually exclusive with targetValue. Must be specified with targetBinEnd.
    #' @param targetBinEnd \link{AnyOfnumberstring}. For regression projects - when specified, only images corresponding to the target values below this will be returned. Mutually exclusive with targetValue. Must be specified with targetBinStart.
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageMetadataListResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Returns a list of image metadata elements.
    #' \itemize{
    #' }
    #' \item **`422`** The request cannot be processed. Possible reasons include: - Cannot supply value for both TargetValue and TargetBin. - Must supply both TargetBinStart and TargetBinEnd. - TargetBin parameters are only valid for regression projects. - TargetValue parameter is only valid for classification projects.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' offset <- 0 # integer | This many results will be skipped
    #' limit <- 100 # integer | At most this many results are returned
    #' projectId <- 'projectId_example' # character | The project ID
    #' column <- 'column_example' # character | Name of the column to query
    #' targetValue <- 'targetValue_example' # AnyOfnumberstring | For classification projects - when specified, only images corresponding to this target value will be returned. Mutually exclusive with targetBinStart/targetBinEnd.
    #' targetBinStart <- 'targetBinStart_example' # AnyOfnumberstring | For regression projects - when specified, only images corresponding to the target values above this will be returned. Mutually exclusive with targetValue. Must be specified with targetBinEnd.
    #' targetBinEnd <- 'targetBinEnd_example' # AnyOfnumberstring | For regression projects - when specified, only images corresponding to the target values below this will be returned. Mutually exclusive with targetValue. Must be specified with targetBinStart.
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImagesList(offset, limit, projectId, column=column, targetValue=targetValue, targetBinStart=targetBinStart, targetBinEnd=targetBinEnd)
    #' }
    ProjectsImagesList = function(offset, limit, projectId, column = NULL, targetValue = NULL, targetBinStart = NULL, targetBinEnd = NULL, ...) {
      apiResponse <- private$ProjectsImagesListWithHttpInfo(offset, limit, projectId, column, targetValue, targetBinStart, targetBinEnd, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Returns a single image metadata.
    #' Produces: "application/json"
    #'
    #' @details Returns a single image metadata.
    #' @details This method invokes `GET /projects/{projectId}/images/{imageId}/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param imageId character. Id of the image
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageMetadataResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Metadata for a single image
    #' \itemize{
    #' }
    #' \item **`404`** Image not found
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #' imageId <- 'imageId_example' # character | Id of the image
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsImagesRetrieve(projectId, imageId)
    #' }
    ProjectsImagesRetrieve = function(projectId, imageId, ...) {
      apiResponse <- private$ProjectsImagesRetrieveWithHttpInfo(projectId, imageId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Request the computation of image activation maps for the specified model.
    #' Produces: "application/json"
    #'
    #' @details Request the computation of image activation maps for the specified model.
    #' @details This method invokes `POST /projects/{projectId}/models/{modelId}/imageActivationMaps/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param modelId character. The model ID
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ActivationMapsComputeResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`202`** Image activation map computation has been successfully requested
    #' \itemize{
    #' \item **`Location`** a url that can be polled to check the status of the job.
    #' }
    #' \item **`422`** Cannot compute image activation maps: if image activation maps were already computed for the model or there was another issue creating this job
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #' modelId <- 'modelId_example' # character | The model ID
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsModelsImageActivationMapsCreate(projectId, modelId)
    #' }
    ProjectsModelsImageActivationMapsCreate = function(projectId, modelId, ...) {
      apiResponse <- private$ProjectsModelsImageActivationMapsCreateWithHttpInfo(projectId, modelId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve Image Activation Maps for a feature of a model.
    #' Produces: "application/json"
    #'
    #' @details Retrieve Image Activation Maps for a feature of a model. Image Activation maps are a technique to get the discriminative image regions used by a CNN to identify a specific class in the image. In other words, an image activation map lets us see which regions in the image were relevant to this class. The higher the value in the activation map the greater the effect the region had on the prediction.
    #' @details This method invokes `GET /projects/{projectId}/models/{modelId}/imageActivationMaps/` in the DataRobot Public API.
    #' @param featureName character. Name of the feature to query
    #' @param projectId character. The project ID
    #' @param modelId character. The model ID
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ActivationMapsRetrieveResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Image Activation Maps
    #' \itemize{
    #' }
    #' \item **`422`** Unable to process request.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' featureName <- 'featureName_example' # character | Name of the feature to query
    #' projectId <- 'projectId_example' # character | The project ID
    #' modelId <- 'modelId_example' # character | The model ID
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsModelsImageActivationMapsList(featureName, projectId, modelId)
    #' }
    ProjectsModelsImageActivationMapsList = function(featureName, projectId, modelId, ...) {
      apiResponse <- private$ProjectsModelsImageActivationMapsListWithHttpInfo(featureName, projectId, modelId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Request the computation of image embeddings for the specified model.
    #' Produces: "application/json"
    #'
    #' @details Request the computation of image embeddings for the specified model.
    #' @details This method invokes `POST /projects/{projectId}/models/{modelId}/imageEmbeddings/` in the DataRobot Public API.
    #' @param projectId character. The project ID
    #' @param modelId character. The model ID
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{ImageEmbeddingsComputeResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`202`** Image embedding computation has been successfully requested
    #' \itemize{
    #' \item **`Location`** a url that can be polled to check the status of the job.
    #' }
    #' \item **`422`** Cannot compute image embeddings: if image embeddings were already computed for the model or there was another issue creating this job
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' projectId <- 'projectId_example' # character | The project ID
    #' modelId <- 'modelId_example' # character | The model ID
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsModelsImageEmbeddingsCreate(projectId, modelId)
    #' }
    ProjectsModelsImageEmbeddingsCreate = function(projectId, modelId, ...) {
      apiResponse <- private$ProjectsModelsImageEmbeddingsCreateWithHttpInfo(projectId, modelId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },
    #' @description Retrieve ImageEmbeddings for a feature of a model.
    #' Produces: "application/json"
    #'
    #' @details Retrieve ImageEmbeddings for a feature of a model.
    #' @details This method invokes `GET /projects/{projectId}/models/{modelId}/imageEmbeddings/` in the DataRobot Public API.
    #' @param featureName character. Name of the feature to query
    #' @param projectId character. The project ID
    #' @param modelId character. The model ID
    #' @param ... Optional. Additional named parameters to be passed downward.
    #' @return \link{EmbeddingsRetrieveResponse}
    #' @details Response status codes, messages, and headers:
    #' \itemize{
    #' \item **`200`** Image Embeddings
    #' \itemize{
    #' }
    #' \item **`422`** Unable to process request.
    #' \itemize{
    #' }
    #' }
    #' @examples
    #' \dontrun{
    #' library(datarobot.apicore)
    #' featureName <- 'featureName_example' # character | Name of the feature to query
    #' projectId <- 'projectId_example' # character | The project ID
    #' modelId <- 'modelId_example' # character | The model ID
    #'
    #' api.instance <- ImagesApi$new()
    #' result <- api.instance$ProjectsModelsImageEmbeddingsList(featureName, projectId, modelId)
    #' }
    ProjectsModelsImageEmbeddingsList = function(featureName, projectId, modelId, ...) {
      apiResponse <- private$ProjectsModelsImageEmbeddingsListWithHttpInfo(featureName, projectId, modelId, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) == 202) {
        # When the DataRobot Public API returns a 202, this means that
        # an asynchronous job or task has been started. The response will
        # not have a body, but will have a Location header pointing to an
        # endpoint for checking that job's status.
        apiResponse
      } else if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        .ReturnResponse(apiResponse$content)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    }
  ),
  private = list(
    # A helper function to invoke the API operation `DatasetsImagesDataQualityLogFileList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    DatasetsImagesDataQualityLogFileListWithHttpInfo = function(datasetId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`datasetId`)) {
        stop("Missing required parameter `datasetId`.")
      }

      body <- NULL
      urlPath <- "/datasets/{datasetId}/imagesDataQualityLog/file/"
      if (!missing(`datasetId`)) {
        urlPath <- gsub(paste0("\\{", "datasetId", "\\}"), URLencode(as.character(`datasetId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `DatasetsImagesDataQualityLogList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    DatasetsImagesDataQualityLogListWithHttpInfo = function(datasetId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`datasetId`)) {
        stop("Missing required parameter `datasetId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/datasets/{datasetId}/imagesDataQualityLog/"
      if (!missing(`datasetId`)) {
        urlPath <- gsub(paste0("\\{", "datasetId", "\\}"), URLencode(as.character(`datasetId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImagesDataQualityLogLinesResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationListsCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationListsCreateWithHttpInfo = function(imageAugmentationCreateList = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (!missing(`imageAugmentationCreateList`) && isa(imageAugmentationCreateList, c("ImageAugmentationCreateList", "R6"))) {
        body <- `imageAugmentationCreateList`$toJSON()
      } else {
        stop("ImageAugmentationListsCreateWithHttpInfo requires parameter imageAugmentationCreateList to be of type ImageAugmentationCreateList.")
      }

      urlPath <- "/imageAugmentationLists/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageAugmentationListCreateResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationListsDelete`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationListsDeleteWithHttpInfo = function(augmentationId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`augmentationId`)) {
        stop("Missing required parameter `augmentationId`.")
      }

      body <- NULL
      urlPath <- "/imageAugmentationLists/{augmentationId}/"
      if (!missing(`augmentationId`)) {
        urlPath <- gsub(paste0("\\{", "augmentationId", "\\}"), URLencode(as.character(`augmentationId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "DELETE",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationListsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationListsListWithHttpInfo = function(projectId, offset, limit, featureName = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      queryParams["projectId"] <- projectId

      queryParams["featureName"] <- featureName

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/imageAugmentationLists/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageAugmentationListsResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationListsPatch`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationListsPatchWithHttpInfo = function(augmentationId, imageAugmentationListPatchParam = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`augmentationId`)) {
        stop("Missing required parameter `augmentationId`.")
      }

      if (!missing(`imageAugmentationListPatchParam`) && isa(imageAugmentationListPatchParam, c("ImageAugmentationListPatchParam", "R6"))) {
        body <- `imageAugmentationListPatchParam`$toJSON()
      } else {
        stop("ImageAugmentationListsPatchWithHttpInfo requires parameter imageAugmentationListPatchParam to be of type ImageAugmentationListPatchParam.")
      }

      urlPath <- "/imageAugmentationLists/{augmentationId}/"
      if (!missing(`augmentationId`)) {
        urlPath <- gsub(paste0("\\{", "augmentationId", "\\}"), URLencode(as.character(`augmentationId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "PATCH",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationListsRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationListsRetrieveWithHttpInfo = function(augmentationId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`augmentationId`)) {
        stop("Missing required parameter `augmentationId`.")
      }

      body <- NULL
      urlPath <- "/imageAugmentationLists/{augmentationId}/"
      if (!missing(`augmentationId`)) {
        urlPath <- gsub(paste0("\\{", "augmentationId", "\\}"), URLencode(as.character(`augmentationId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageAugmentationRetrieveList", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationListsSamplesCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationListsSamplesCreateWithHttpInfo = function(augmentationId, imageAugmentationListsCreateSamples = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`augmentationId`)) {
        stop("Missing required parameter `augmentationId`.")
      }

      if (!missing(`imageAugmentationListsCreateSamples`) && isa(imageAugmentationListsCreateSamples, c("ImageAugmentationListsCreateSamples", "R6"))) {
        body <- `imageAugmentationListsCreateSamples`$toJSON()
      } else {
        stop("ImageAugmentationListsSamplesCreateWithHttpInfo requires parameter imageAugmentationListsCreateSamples to be of type ImageAugmentationListsCreateSamples.")
      }

      urlPath <- "/imageAugmentationLists/{augmentationId}/samples/"
      if (!missing(`augmentationId`)) {
        urlPath <- gsub(paste0("\\{", "augmentationId", "\\}"), URLencode(as.character(`augmentationId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageAugmentationSamplesResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationListsSamplesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationListsSamplesListWithHttpInfo = function(augmentationId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`augmentationId`)) {
        stop("Missing required parameter `augmentationId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/imageAugmentationLists/{augmentationId}/samples/"
      if (!missing(`augmentationId`)) {
        urlPath <- gsub(paste0("\\{", "augmentationId", "\\}"), URLencode(as.character(`augmentationId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageAugmentationListsRetrieveSamplesResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationOptionsRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationOptionsRetrieveWithHttpInfo = function(projectId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      body <- NULL
      urlPath <- "/imageAugmentationOptions/{projectId}/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageAugmentationOptionsResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationSamplesCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationSamplesCreateWithHttpInfo = function(imageAugmentationSamplesRequest = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (!missing(`imageAugmentationSamplesRequest`) && isa(imageAugmentationSamplesRequest, c("ImageAugmentationSamplesRequest", "R6"))) {
        body <- `imageAugmentationSamplesRequest`$toJSON()
      } else {
        stop("ImageAugmentationSamplesCreateWithHttpInfo requires parameter imageAugmentationSamplesRequest to be of type ImageAugmentationSamplesRequest.")
      }

      urlPath <- "/imageAugmentationSamples/"

      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageAugmentationSamplesResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ImageAugmentationSamplesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ImageAugmentationSamplesListWithHttpInfo = function(samplesId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`samplesId`)) {
        stop("Missing required parameter `samplesId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/imageAugmentationSamples/{samplesId}/"
      if (!missing(`samplesId`)) {
        urlPath <- gsub(paste0("\\{", "samplesId", "\\}"), URLencode(as.character(`samplesId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageAugmentationRetrieveSamplesResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsDuplicateImagesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsDuplicateImagesListWithHttpInfo = function(projectId, column, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      if (missing(`column`)) {
        stop("Missing required parameter `column`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/projects/{projectId}/duplicateImages/{column}/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }

      if (!missing(`column`)) {
        urlPath <- gsub(paste0("\\{", "column", "\\}"), URLencode(as.character(`column`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "DuplicateImageTableResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImageActivationMapsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImageActivationMapsListWithHttpInfo = function(projectId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/projects/{projectId}/imageActivationMaps/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ActivationMapsListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImageBinsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImageBinsListWithHttpInfo = function(featureName, projectId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`featureName`)) {
        stop("Missing required parameter `featureName`.")
      }

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["featureName"] <- featureName

      body <- NULL
      urlPath <- "/projects/{projectId}/imageBins/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageBinsListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImageEmbeddingsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImageEmbeddingsListWithHttpInfo = function(projectId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/projects/{projectId}/imageEmbeddings/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "EmbeddingsListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImageSamplesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImageSamplesListWithHttpInfo = function(featureName, projectId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`featureName`)) {
        stop("Missing required parameter `featureName`.")
      }

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      queryParams["featureName"] <- featureName

      body <- NULL
      urlPath <- "/projects/{projectId}/imageSamples/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageMetadataListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImagesDataQualityLogFileList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImagesDataQualityLogFileListWithHttpInfo = function(projectId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      body <- NULL
      urlPath <- "/projects/{projectId}/imagesDataQualityLog/file/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImagesDataQualityLogList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImagesDataQualityLogListWithHttpInfo = function(projectId, offset = 0, limit = 100, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/projects/{projectId}/imagesDataQualityLog/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImagesDataQualityLogLinesResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImagesFileList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImagesFileListWithHttpInfo = function(projectId, imageId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      if (missing(`imageId`)) {
        stop("Missing required parameter `imageId`.")
      }

      body <- NULL
      urlPath <- "/projects/{projectId}/images/{imageId}/file/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }

      if (!missing(`imageId`)) {
        urlPath <- gsub(paste0("\\{", "imageId", "\\}"), URLencode(as.character(`imageId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ApiResponse$new(NULL, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImagesList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImagesListWithHttpInfo = function(offset, limit, projectId, column = NULL, targetValue = NULL, targetBinStart = NULL, targetBinEnd = NULL, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      queryParams["column"] <- column

      if (!is.null(targetValue)) {
        queryParams["targetValue"] <- paste0(targetValue, collapse = ",")
      }

      if (!is.null(targetBinStart)) {
        queryParams["targetBinStart"] <- paste0(targetBinStart, collapse = ",")
      }

      if (!is.null(targetBinEnd)) {
        queryParams["targetBinEnd"] <- paste0(targetBinEnd, collapse = ",")
      }

      queryParams["offset"] <- offset

      queryParams["limit"] <- limit

      body <- NULL
      urlPath <- "/projects/{projectId}/images/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageMetadataListResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsImagesRetrieve`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsImagesRetrieveWithHttpInfo = function(projectId, imageId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      if (missing(`imageId`)) {
        stop("Missing required parameter `imageId`.")
      }

      body <- NULL
      urlPath <- "/projects/{projectId}/images/{imageId}/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }

      if (!missing(`imageId`)) {
        urlPath <- gsub(paste0("\\{", "imageId", "\\}"), URLencode(as.character(`imageId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageMetadataResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsModelsImageActivationMapsCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsModelsImageActivationMapsCreateWithHttpInfo = function(projectId, modelId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      if (missing(`modelId`)) {
        stop("Missing required parameter `modelId`.")
      }

      body <- NULL
      urlPath <- "/projects/{projectId}/models/{modelId}/imageActivationMaps/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }

      if (!missing(`modelId`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), URLencode(as.character(`modelId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ActivationMapsComputeResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsModelsImageActivationMapsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsModelsImageActivationMapsListWithHttpInfo = function(featureName, projectId, modelId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`featureName`)) {
        stop("Missing required parameter `featureName`.")
      }

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      if (missing(`modelId`)) {
        stop("Missing required parameter `modelId`.")
      }

      queryParams["featureName"] <- featureName

      body <- NULL
      urlPath <- "/projects/{projectId}/models/{modelId}/imageActivationMaps/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }

      if (!missing(`modelId`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), URLencode(as.character(`modelId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ActivationMapsRetrieveResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsModelsImageEmbeddingsCreate`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsModelsImageEmbeddingsCreateWithHttpInfo = function(projectId, modelId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      if (missing(`modelId`)) {
        stop("Missing required parameter `modelId`.")
      }

      body <- NULL
      urlPath <- "/projects/{projectId}/models/{modelId}/imageEmbeddings/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }

      if (!missing(`modelId`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), URLencode(as.character(`modelId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "POST",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "ImageEmbeddingsComputeResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    },
    # A helper function to invoke the API operation `ProjectsModelsImageEmbeddingsList`. This function is responsible for
    # validating request parameters, building the request, deserializing the response, and handling errors.
    ProjectsModelsImageEmbeddingsListWithHttpInfo = function(featureName, projectId, modelId, ...) {
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`featureName`)) {
        stop("Missing required parameter `featureName`.")
      }

      if (missing(`projectId`)) {
        stop("Missing required parameter `projectId`.")
      }

      if (missing(`modelId`)) {
        stop("Missing required parameter `modelId`.")
      }

      queryParams["featureName"] <- featureName

      body <- NULL
      urlPath <- "/projects/{projectId}/models/{modelId}/imageEmbeddings/"
      if (!missing(`projectId`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), URLencode(as.character(`projectId`), reserved = TRUE), urlPath)
      }

      if (!missing(`modelId`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), URLencode(as.character(`modelId`), reserved = TRUE), urlPath)
      }


      resp <- self$apiClient$CallApi(
        url = paste0(self$apiClient$basePath, urlPath),
        method = "GET",
        queryParams = queryParams,
        headerParams = headerParams,
        body = body,
        ...
      )

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- self$apiClient$deserialize(resp, "EmbeddingsRetrieveResponse", loadNamespace("datarobot.apicore"))
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new(paste("API client error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new(paste("API server error with ", httr::status_code(resp), " response status code. See $response for more detail."), resp)
      }
    }
  )
)
