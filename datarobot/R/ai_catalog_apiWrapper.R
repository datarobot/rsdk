# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech


# NOTE: This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
# Do not edit the file manually.

# API Wrapper methods for AiCatalog
# To use these methods without modification, DR endpoint and token
# should be set as environment variables. This will automatically
# happen when you call `datarobot::ConnectToDataRobot()`.



#' ListCatalogItems
#'
#' List all catalog items accessible by the user.
#'
#' List all catalog items accessible by the user.
#' @details This method invokes `GET /catalogItems/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CatalogItemsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListCatalogItems <- function(orderBy, type, datasourceType, ownerUsername, ownerUserId, tag, category, searchFor, offset = 0, accessType = "any", useCache = "false", limit = 0, initialCacheSize = 500, filterFailed = "false", ...) {
  return(datarobot.apicore::AiCatalogApi$new()$CatalogItemsList(offset = offset, orderBy = orderBy, type = type, datasourceType = datasourceType, accessType = accessType, ownerUsername = ownerUsername, useCache = useCache, ownerUserId = ownerUserId, limit = limit, initialCacheSize = initialCacheSize, tag = tag, category = category, searchFor = searchFor, filterFailed = filterFailed, ...))
}

#' PatchCatalogItems
#'
#' Update the name, description, or tags for the requested catalog item.
#'
#' Update the name, description, or tags for the requested catalog item.
#' @details This method invokes `PATCH /catalogItems/{catalogId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CatalogItemsPatch in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchCatalogItems <- function(catalogId, name, description, tags, ...) {
  updateCatalogMetadata <- datarobot.apicore::UpdateCatalogMetadata$new(name = name, description = description, tags = tags, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$CatalogItemsPatch(catalogId = catalogId, updateCatalogMetadata = updateCatalogMetadata, ...))
}

#' RetrieveCatalogItems
#'
#' Retrieves latest version information, by ID, for catalog items.
#'
#' Retrieves latest version information, by ID, for catalog items.
#' @details This method invokes `GET /catalogItems/{catalogId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CatalogItemsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveCatalogItems <- function(catalogId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$CatalogItemsRetrieve(catalogId = catalogId, ...))
}

#' CreateDataEngineQueryGenerator
#'
#' Create a data engine query generator
#'
#' Create a data engine query generator.
#' @details This method invokes `POST /dataEngineQueryGenerators/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DataEngineQueryGeneratorsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDataEngineQueryGenerator <- function(generatorType, dataset, generatorSettings, ...) {
  body <- list(
    generatorType = generatorType,
    datasets = list(dataset),
    generatorSettings = generatorSettings
  )
  routeString <- UrlJoin("dataEngineQueryGenerators")
  # start of generated addition
  # end of generated addition
  postResponse <- DataRobotPOST(routeString, body = body, addUrl = TRUE, returnRawResponse = TRUE, encode = "json")
  response <- WaitForAsyncReturn(GetRedirectFromResponse(postResponse),
    addUrl = FALSE
  )
  queryGenerator <- as.dataRobotQueryGenerator(response)
  return(queryGenerator)
}

#' RetrieveDataEngineQueryGenerators
#'
#' Retrieve a data engine query generator given ID.
#'
#' Retrieve a data engine query generator
#' @details This method invokes `GET /dataEngineQueryGenerators/{dataEngineQueryGeneratorId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DataEngineQueryGeneratorsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDataEngineQueryGenerators <- function(dataEngineQueryGeneratorId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DataEngineQueryGeneratorsRetrieve(dataEngineQueryGeneratorId = dataEngineQueryGeneratorId, ...))
}

#' CreateDataEngineWorkspaceStates
#'
#' Create Data Engine workspace state
#'
#' Create Data Engine workspace state in database.
#' @details This method invokes `POST /dataEngineWorkspaceStates/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DataEngineWorkspaceStatesCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDataEngineWorkspaceStates <- function(query, language, datasets, ...) {
  createWorkspaceState <- datarobot.apicore::CreateWorkspaceState$new(query = query, language = language, datasets = datasets, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DataEngineWorkspaceStatesCreate(createWorkspaceState = createWorkspaceState, ...))
}

#' CreateDatasetFromDataEngineQueryGenerator
#'
#' Create Data Engine workspace state from a query generator
#'
#' Create Data Engine workspace state in database from a query generator.
#' @details This method invokes `POST /dataEngineWorkspaceStates/fromDataEngineQueryGenerator/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DataEngineWorkspaceStatesFromDataEngineQueryGeneratorCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetFromDataEngineQueryGenerator <- function(generatorId, datasetId = NULL, datasetVersionId = NULL, ...) {
  body <- list(queryGeneratorId = generatorId)
  if (!is.null(datasetId)) {
    body$datasetId <- datasetId
  }
  if (!is.null(datasetVersionId)) {
    body$datasetVersionId <- datasetVersionId
  }
  routeString <- UrlJoin("dataEngineWorkspaceStates", "fromDataEngineQueryGenerator")
  # start of generated addition
  # end of generated addition
  postResponse <- DataRobotPOST(routeString, body = body)

  body <- list(workspaceStateId = postResponse$workspaceStateId)
  routeString <- UrlJoin("datasets", "fromDataEngineWorkspaceState")
  postResponse <- DataRobotPOST(routeString,
    body = body,
    addUrl = TRUE, returnRawResponse = TRUE
  )
  response <- WaitForAsyncReturn(GetRedirectFromResponse(postResponse),
    addUrl = FALSE
  )
  dataset <- as.dataRobotDataset(response)
  return(dataset)
}

#' RetrieveDataEngineWorkspaceStates
#'
#' Read Data Engine workspace state
#'
#' Read and return previously stored Data Engine workspace state.
#' @details This method invokes `GET /dataEngineWorkspaceStates/{workspaceStateId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DataEngineWorkspaceStatesRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDataEngineWorkspaceStates <- function(workspaceStateId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DataEngineWorkspaceStatesRetrieve(workspaceStateId = workspaceStateId, ...))
}

#' ListDatasetsAccessControl
#'
#' List dataset access
#'
#' List the users and their associated roles for the specified dataset.
#' @details This method invokes `GET /datasets/{datasetId}/accessControl/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsAccessControlList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsAccessControl <- function(datasetId, userId, username, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsAccessControlList(offset = offset, limit = limit, datasetId = datasetId, userId = userId, username = username, ...))
}

#' PatchManyDatasetsAccessControl
#'
#' Modify dataset access
#'
#' Grant access to the dataset at the specified role level, or remove access to the dataset.
#' @details This method invokes `PATCH /datasets/{datasetId}/accessControl/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsAccessControlPatchMany in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchManyDatasetsAccessControl <- function(data, datasetId, applyGrantToLinkedObjects = FALSE, ...) {
  datasetAccessSet <- datarobot.apicore::DatasetAccessSet$new(data = data, applyGrantToLinkedObjects = applyGrantToLinkedObjects, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsAccessControlPatchMany(datasetAccessSet = datasetAccessSet, datasetId = datasetId, ...))
}

#' ListDatasetsAllFeaturesDetails
#'
#' Get dataset features
#'
#' Return detailed information on all the features and transforms for this dataset.If the Dataset Item has attribute snapshot = True, all optional fields also appear
#' @details This method invokes `GET /datasets/{datasetId}/allFeaturesDetails/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsAllFeaturesDetailsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsAllFeaturesDetails <- function(datasetId, offset = 0, limit = 100, orderBy = "name", ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsAllFeaturesDetailsList(offset = offset, limit = limit, datasetId = datasetId, orderBy = orderBy, ...))
}

#' DeleteDatasets
#'
#' Delete dataset
#'
#' Marks the dataset with the given ID as deleted.
#' @details This method invokes `DELETE /datasets/{datasetId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsDelete in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteDatasets <- function(datasetId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsDelete(datasetId = datasetId, ...))
}

#' PatchManyDatasetsDeleted
#'
#' Recover deleted dataset
#'
#' Recover the dataset item with given datasetId from deleted.
#' @details This method invokes `PATCH /datasets/{datasetId}/deleted/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsDeletedPatchMany in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchManyDatasetsDeleted <- function(datasetId, body, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsDeletedPatchMany(datasetId = datasetId, body = body, ...))
}

#' RetrieveDatasetsFeatureHistograms
#'
#' Get dataset feature histogram
#'
#' Get histogram chart data for a specific feature in the specified dataset.
#' @details This method invokes `GET /datasets/{datasetId}/featureHistograms/{featureName}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeatureHistogramsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDatasetsFeatureHistograms <- function(featureName, datasetId, key, binLimit = 60, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeatureHistogramsRetrieve(binLimit = binLimit, featureName = featureName, datasetId = datasetId, key = key, ...))
}

#' CreateDatasetsFeatureTransforms
#'
#' Create dataset feature transform
#'
#' Create a new feature by changing the type of an existing one.
#' @details This method invokes `POST /datasets/{datasetId}/featureTransforms/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeatureTransformsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsFeatureTransforms <- function(variableType, parentName, name, datasetId, dateExtraction, replacement, ...) {
  featureTransform <- datarobot.apicore::FeatureTransform$new(variableType = variableType, parentName = parentName, name = name, dateExtraction = dateExtraction, replacement = replacement, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeatureTransformsCreate(featureTransform = featureTransform, datasetId = datasetId, ...))
}

#' ListDatasetsFeatureTransforms
#'
#' List dataset feature transforms
#'
#' Retrieves the transforms of the dataset with given ID.
#' @details This method invokes `GET /datasets/{datasetId}/featureTransforms/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeatureTransformsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsFeatureTransforms <- function(datasetId, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeatureTransformsList(offset = offset, limit = limit, datasetId = datasetId, ...))
}

#' RetrieveDatasetsFeatureTransforms
#'
#' Get dataset feature transform
#'
#' Retrieve the specified feature with descriptive information.
#' @details This method invokes `GET /datasets/{datasetId}/featureTransforms/{featureName}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeatureTransformsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDatasetsFeatureTransforms <- function(featureName, datasetId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeatureTransformsRetrieve(featureName = featureName, datasetId = datasetId, ...))
}

#' CreateDatasetsFeaturelists
#'
#' Create dataset featurelist
#'
#' Create featurelist for specified dataset.
#' @details This method invokes `POST /datasets/{datasetId}/featurelists/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeaturelistsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsFeaturelists <- function(features, name, description, datasetId, ...) {
  featureListCreate <- datarobot.apicore::FeatureListCreate$new(features = features, name = name, description = description, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeaturelistsCreate(featureListCreate = featureListCreate, datasetId = datasetId, ...))
}

#' DeleteDatasetsFeaturelists
#'
#' Delete dataset featurelist
#'
#' Deletes the indicated featurelist of the dataset with given ID.
#' @details This method invokes `DELETE /datasets/{datasetId}/featurelists/{featurelistId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeaturelistsDelete in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteDatasetsFeaturelists <- function(datasetId, featurelistId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeaturelistsDelete(datasetId = datasetId, featurelistId = featurelistId, ...))
}

#' ListDatasetsFeaturelists
#'
#' Retrieve dataset featurelists
#'
#' Retrieves the featurelists of the dataset with given ID and the latest dataset version.
#' @details This method invokes `GET /datasets/{datasetId}/featurelists/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeaturelistsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsFeaturelists <- function(datasetId, offset = 0, limit = 100, orderBy = "name", ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeaturelistsList(offset = offset, limit = limit, datasetId = datasetId, orderBy = orderBy, ...))
}

#' PatchDatasetsFeaturelists
#'
#' Update dataset featurelist
#'
#' Modifies the indicated featurelist of the dataset with given ID.
#' @details This method invokes `PATCH /datasets/{datasetId}/featurelists/{featurelistId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeaturelistsPatch in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchDatasetsFeaturelists <- function(name, datasetId, featurelistId, ...) {
  featureListModify <- datarobot.apicore::FeatureListModify$new(name = name, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeaturelistsPatch(featureListModify = featureListModify, datasetId = datasetId, featurelistId = featurelistId, ...))
}

#' RetrieveDatasetsFeaturelists
#'
#' Get dataset featurelist
#'
#' Retrieves the specified featurelist of the dataset with given ID and the latest dataset version.
#' @details This method invokes `GET /datasets/{datasetId}/featurelists/{featurelistId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFeaturelistsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDatasetsFeaturelists <- function(datasetId, featurelistId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFeaturelistsRetrieve(datasetId = datasetId, featurelistId = featurelistId, ...))
}

#' ListDatasetsFile
#'
#' Retrieve original dataset data
#'
#' Retrieve all the originally uploaded data, in CSV form.
#' @details This method invokes `GET /datasets/{datasetId}/file/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFileList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsFile <- function(datasetId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFileList(datasetId = datasetId, ...))
}

#' CreateDatasetsFromDataEngineWorkspaceState
#'
#' Create dataset from Data Engine workspace
#'
#' Create a dataset from a Data Engine workspace state.
#' @details This method invokes `POST /datasets/fromDataEngineWorkspaceState/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFromDataEngineWorkspaceStateCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsFromDataEngineWorkspaceState <- function(credentials, datasetName, workspaceStateId, doSnapshot, ...) {
  datasetCreateFromWorkspaceState <- datarobot.apicore::DatasetCreateFromWorkspaceState$new(credentials = credentials, datasetName = datasetName, workspaceStateId = workspaceStateId, doSnapshot = doSnapshot, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFromDataEngineWorkspaceStateCreate(datasetCreateFromWorkspaceState = datasetCreateFromWorkspaceState, ...))
}

#' CreateDatasetsFromDataSource
#'
#' Create dataset from data source
#'
#' Create a Dataset Item from a data source.
#' @details This method invokes `POST /datasets/fromDataSource/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFromDataSourceCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsFromDataSource <- function(password, dataSourceId, persistDataAfterIngestion, credentialId, credentialData, categories, sampleSize, user, doSnapshot = TRUE, useKerberos = FALSE, ...) {
  datasource <- datarobot.apicore::Datasource$new(password = password, dataSourceId = dataSourceId, persistDataAfterIngestion = persistDataAfterIngestion, credentialId = credentialId, credentialData = credentialData, categories = categories, sampleSize = sampleSize, user = user, doSnapshot = doSnapshot, useKerberos = useKerberos, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFromDataSourceCreate(datasource = datasource, ...))
}

#' CreateDatasetsFromFile
#'
#' Create dataset from file
#'
#' Create a dataset from a file.
#' @details This method invokes `POST /datasets/fromFile/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFromFileCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsFromFile <- function(file, categories, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFromFileCreate(file = file, categories = categories, ...))
}

#' CreateDatasetsFromHDFS
#'
#' Create dataset from HDFS URL
#'
#' Create a Dataset Item from an HDFS URL.
#' @details This method invokes `POST /datasets/fromHDFS/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFromHDFSCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsFromHDFS <- function(password, persistDataAfterIngestion, namenodeWebhdfsPort, categories, user, url, doSnapshot = TRUE, ...) {
  hdfs <- datarobot.apicore::Hdfs$new(password = password, persistDataAfterIngestion = persistDataAfterIngestion, namenodeWebhdfsPort = namenodeWebhdfsPort, categories = categories, user = user, url = url, doSnapshot = doSnapshot, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFromHDFSCreate(hdfs = hdfs, ...))
}

#' CreateDatasetsFromURL
#'
#' Create dataset from URL
#'
#' Create a Dataset Item from a URL.
#' @details This method invokes `POST /datasets/fromURL/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsFromURLCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsFromURL <- function(persistDataAfterIngestion, categories, sampleSize, url, doSnapshot = TRUE, ...) {
  url <- datarobot.apicore::Url$new(persistDataAfterIngestion = persistDataAfterIngestion, categories = categories, sampleSize = sampleSize, url = url, doSnapshot = doSnapshot, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsFromURLCreate(url = url, ...))
}

#' ListDatasets
#'
#' List datasets
#'
#' List all datasets accessible by the user.
#' @details This method invokes `GET /datasets/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasets <- function(orderBy, datasetVersionIds, category, offset = 0, limit = 100, filterFailed = "false", ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsList(offset = offset, limit = limit, orderBy = orderBy, datasetVersionIds = datasetVersionIds, category = category, filterFailed = filterFailed, ...))
}

#' PatchDatasets
#'
#' Modify dataset
#'
#' Modifies the specified dataset.
#' @details This method invokes `PATCH /datasets/{datasetId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsPatch in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchDatasets <- function(name, datasetId, categories, ...) {
  patchDataset <- datarobot.apicore::PatchDataset$new(name = name, categories = categories, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsPatch(patchDataset = patchDataset, datasetId = datasetId, ...))
}

#' PatchManyDatasets
#'
#' Execute bulk dataset action
#'
#' Execute the specified bulk action on multiple datasets.
#' @details This method invokes `PATCH /datasets/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsPatchMany in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchManyDatasets <- function(payload, datasetIds, ...) {
  bulkDatasetAction <- datarobot.apicore::BulkDatasetAction$new(payload = payload, datasetIds = datasetIds, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsPatchMany(bulkDatasetAction = bulkDatasetAction, ...))
}

#' ListDatasetsPermissions
#'
#' Describe dataset permissions
#'
#' Describe what permissions current user has for given dataset.
#' @details This method invokes `GET /datasets/{datasetId}/permissions/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsPermissionsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsPermissions <- function(datasetId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsPermissionsList(datasetId = datasetId, ...))
}

#' ListDatasetsProjects
#'
#' Get dataset projects
#'
#' Retrieves a dataset's projects by dataset ID.
#' @details This method invokes `GET /datasets/{datasetId}/projects/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsProjectsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsProjects <- function(datasetId, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsProjectsList(offset = offset, limit = limit, datasetId = datasetId, ...))
}

#' CreateDatasetsRefreshJobs
#'
#' Schedule dataset refresh
#'
#' Create a dataset refresh job that will automatically create dataset snapshots on a schedule.  Optionally if the limit of enabled jobs per user is reached the following metadata will be added to the default error response payload:  - **datasetsWithJob** (*array*) - The list of datasets IDs that have at   least one enabled job. - **errorType** (*string*) - (New in version v2.21) The type of error   that happened, possible values include (but are not limited to):   `Generic Limit Reached`, `Max Job Limit Reached for Dataset`, and   `Max Job Limit Reached for User`.
#' @details This method invokes `POST /datasets/{datasetId}/refreshJobs/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRefreshJobsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsRefreshJobs <- function(schedule, credentials, name, credentialId, datasetId, categories, scheduleReferenceDate, enabled = TRUE, useKerberos = FALSE, ...) {
  datasetRefreshJobCreate <- datarobot.apicore::DatasetRefreshJobCreate$new(schedule = schedule, credentials = credentials, name = name, credentialId = credentialId, categories = categories, scheduleReferenceDate = scheduleReferenceDate, enabled = enabled, useKerberos = useKerberos, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRefreshJobsCreate(datasetRefreshJobCreate = datasetRefreshJobCreate, datasetId = datasetId, ...))
}

#' DeleteDatasetsRefreshJobs
#'
#' Deletes an existing dataset refresh job
#'
#' Deletes an existing dataset refresh job.
#' @details This method invokes `DELETE /datasets/{datasetId}/refreshJobs/{jobId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRefreshJobsDelete in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteDatasetsRefreshJobs <- function(jobId, datasetId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRefreshJobsDelete(jobId = jobId, datasetId = datasetId, ...))
}

#' ListDatasetsRefreshJobsExecutionResults
#'
#' Results of dataset refresh job.
#'
#' Paginated list of execution results for refresh job with the given ID and dataset with the given ID, sorted from newest to oldest.
#' @details This method invokes `GET /datasets/{datasetId}/refreshJobs/{jobId}/executionResults/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRefreshJobsExecutionResultsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsRefreshJobsExecutionResults <- function(jobId, datasetId, offset = 0, limit = 20, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRefreshJobsExecutionResultsList(jobId = jobId, offset = offset, limit = limit, datasetId = datasetId, ...))
}

#' ListDatasetsRefreshJobs
#'
#' Information about scheduled jobs for given dataset.
#'
#' Paginated list of scheduled jobs descriptions for a specific dataset with given dataset ID, sorted by time of the last update.
#' @details This method invokes `GET /datasets/{datasetId}/refreshJobs/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRefreshJobsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsRefreshJobs <- function(datasetId, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRefreshJobsList(offset = offset, limit = limit, datasetId = datasetId, ...))
}

#' PatchDatasetsRefreshJobs
#'
#' Update a dataset refresh job
#'
#' Update a dataset refresh job.  Optionally if the limit of enabled jobs per user is reached the following metadata will be added to the default error response payload:  - **datasetsWithJob** (*array*) - The list of datasets IDs that have at   least one enabled job. - **errorType** (*string*) - (New in version v2.21) The type of error   that happened, possible values include (but are not limited to):   `Generic Limit Reached`, `Max Job Limit Reached for Dataset`, and   `Max Job Limit Reached for User`.
#' @details This method invokes `PATCH /datasets/{datasetId}/refreshJobs/{jobId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRefreshJobsPatch in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchDatasetsRefreshJobs <- function(schedule, jobId, credentials, name, credentialId, datasetId, categories, scheduleReferenceDate, enabled, useKerberos, ...) {
  datasetRefreshJobUpdate <- datarobot.apicore::DatasetRefreshJobUpdate$new(schedule = schedule, credentials = credentials, name = name, credentialId = credentialId, categories = categories, scheduleReferenceDate = scheduleReferenceDate, enabled = enabled, useKerberos = useKerberos, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRefreshJobsPatch(datasetRefreshJobUpdate = datasetRefreshJobUpdate, jobId = jobId, datasetId = datasetId, ...))
}

#' RetrieveDatasetsRefreshJobs
#'
#' Gets configuration of a user scheduled dataset refresh job by job ID
#'
#' Gets configuration of a user scheduled dataset refresh job by job ID.
#' @details This method invokes `GET /datasets/{datasetId}/refreshJobs/{jobId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRefreshJobsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDatasetsRefreshJobs <- function(jobId, datasetId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRefreshJobsRetrieve(jobId = jobId, datasetId = datasetId, ...))
}

#' CreateDatasetsRelationships
#'
#' Create dataset relationship.
#'
#' Create a dataset relationship.
#' @details This method invokes `POST /datasets/{datasetId}/relationships/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRelationshipsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsRelationships <- function(linkedFeatures, linkedDatasetId, datasetId, sourceFeatures, ...) {
  datasetRelationshipCreate <- datarobot.apicore::DatasetRelationshipCreate$new(linkedFeatures = linkedFeatures, linkedDatasetId = linkedDatasetId, sourceFeatures = sourceFeatures, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRelationshipsCreate(datasetRelationshipCreate = datasetRelationshipCreate, datasetId = datasetId, ...))
}

#' DeleteDatasetsRelationships
#'
#' Delete dataset relationship.
#'
#' Delete a dataset relationship.
#' @details This method invokes `DELETE /datasets/{datasetId}/relationships/{datasetRelationshipId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRelationshipsDelete in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteDatasetsRelationships <- function(datasetRelationshipId, datasetId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRelationshipsDelete(datasetRelationshipId = datasetRelationshipId, datasetId = datasetId, ...))
}

#' ListDatasetsRelationships
#'
#' List related datasets
#'
#' Retrieve a list of the dataset relationships for a specific dataset.
#' @details This method invokes `GET /datasets/{datasetId}/relationships/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRelationshipsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsRelationships <- function(linkedDatasetId, datasetId, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRelationshipsList(offset = offset, linkedDatasetId = linkedDatasetId, limit = limit, datasetId = datasetId, ...))
}

#' PatchDatasetsRelationships
#'
#' Update dataset relationship.
#'
#' Update a dataset relationship.
#' @details This method invokes `PATCH /datasets/{datasetId}/relationships/{datasetRelationshipId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRelationshipsPatch in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchDatasetsRelationships <- function(linkedFeatures, datasetRelationshipId, linkedDatasetId, datasetId, sourceFeatures, ...) {
  datasetRelationshipUpdate <- datarobot.apicore::DatasetRelationshipUpdate$new(linkedFeatures = linkedFeatures, linkedDatasetId = linkedDatasetId, sourceFeatures = sourceFeatures, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRelationshipsPatch(datasetRelationshipUpdate = datasetRelationshipUpdate, datasetRelationshipId = datasetRelationshipId, datasetId = datasetId, ...))
}

#' RetrieveDatasets
#'
#' Get dataset details
#'
#' Retrieves the details of the dataset with given ID.
#' @details This method invokes `GET /datasets/{datasetId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDatasets <- function(datasetId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsRetrieve(datasetId = datasetId, ...))
}

#' ListDatasetsSharedRoles
#'
#' List dataset shared roles
#'
#' Get a list of users, groups and organizations who have access to this dataset and their roles.
#' @details This method invokes `GET /datasets/{datasetId}/sharedRoles/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsSharedRolesList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsSharedRoles <- function(shareRecipientType, name, datasetId, id, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsSharedRolesList(shareRecipientType = shareRecipientType, offset = offset, name = name, limit = limit, datasetId = datasetId, id = id, ...))
}

#' PatchManyDatasetsSharedRoles
#'
#' Modify dataset shared roles
#'
#' Grant access, remove access or update roles for organizations, groups or users on this dataset. Up to 100 roles may be set per array in a single request.
#' @details This method invokes `PATCH /datasets/{datasetId}/sharedRoles/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsSharedRolesPatchMany in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchManyDatasetsSharedRoles <- function(roles, datasetId, operation, applyGrantToLinkedObjects = FALSE, ...) {
  datasetSharedRoles <- datarobot.apicore::DatasetSharedRoles$new(roles = roles, applyGrantToLinkedObjects = applyGrantToLinkedObjects, operation = operation, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsSharedRolesPatchMany(datasetSharedRoles = datasetSharedRoles, datasetId = datasetId, ...))
}

#' ListDatasetsVersionsAllFeaturesDetails
#'
#' Get dataset features
#'
#' Return detailed information on all the features and transforms for this dataset.If the Dataset Item has attribute snapshot = True, all optional fields also appear
#' @details This method invokes `GET /datasets/{datasetId}/versions/{datasetVersionId}/allFeaturesDetails/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsAllFeaturesDetailsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsVersionsAllFeaturesDetails <- function(datasetId, datasetVersionId, offset = 0, limit = 100, orderBy = "name", ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsAllFeaturesDetailsList(offset = offset, limit = limit, datasetId = datasetId, orderBy = orderBy, datasetVersionId = datasetVersionId, ...))
}

#' DeleteDatasetsVersions
#'
#' Delete dataset version
#'
#' Marks the dataset version with the given ID as deleted.
#' @details This method invokes `DELETE /datasets/{datasetId}/versions/{datasetVersionId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsDelete in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteDatasetsVersions <- function(datasetId, datasetVersionId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsDelete(datasetId = datasetId, datasetVersionId = datasetVersionId, ...))
}

#' PatchManyDatasetsVersionsDeleted
#'
#' Recover deleted dataset version
#'
#' Recover the dataset version item with given datasetId and datasetVersionId from deleted.
#' @details This method invokes `PATCH /datasets/{datasetId}/versions/{datasetVersionId}/deleted/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsDeletedPatchMany in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchManyDatasetsVersionsDeleted <- function(datasetId, body, datasetVersionId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsDeletedPatchMany(datasetId = datasetId, body = body, datasetVersionId = datasetVersionId, ...))
}

#' RetrieveDatasetsVersionsFeatureHistograms
#'
#' Get dataset feature histogram
#'
#' Get histogram chart data for a specific feature in the specified dataset.
#' @details This method invokes `GET /datasets/{datasetId}/versions/{datasetVersionId}/featureHistograms/{featureName}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFeatureHistogramsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDatasetsVersionsFeatureHistograms <- function(featureName, datasetId, key, datasetVersionId, binLimit = 60, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFeatureHistogramsRetrieve(binLimit = binLimit, featureName = featureName, datasetId = datasetId, key = key, datasetVersionId = datasetVersionId, ...))
}

#' ListDatasetsVersionsFeaturelists
#'
#' Retrieve dataset featurelists
#'
#' Retrieves the featurelists of the dataset with given ID and the latest dataset version.
#' @details This method invokes `GET /datasets/{datasetId}/versions/{datasetVersionId}/featurelists/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFeaturelistsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsVersionsFeaturelists <- function(datasetId, datasetVersionId, offset = 0, limit = 100, orderBy = "name", ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFeaturelistsList(offset = offset, limit = limit, datasetId = datasetId, orderBy = orderBy, datasetVersionId = datasetVersionId, ...))
}

#' RetrieveDatasetsVersionsFeaturelists
#'
#' Get dataset featurelist
#'
#' Retrieves the specified featurelist of the dataset with given ID and the latest dataset version.
#' @details This method invokes `GET /datasets/{datasetId}/versions/{datasetVersionId}/featurelists/{featurelistId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFeaturelistsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDatasetsVersionsFeaturelists <- function(datasetId, featurelistId, datasetVersionId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFeaturelistsRetrieve(datasetId = datasetId, featurelistId = featurelistId, datasetVersionId = datasetVersionId, ...))
}

#' ListDatasetsVersionsFile
#'
#' Retrieve original dataset data
#'
#' Retrieve all the originally uploaded data, in CSV form.
#' @details This method invokes `GET /datasets/{datasetId}/versions/{datasetVersionId}/file/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFileList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsVersionsFile <- function(datasetId, datasetVersionId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFileList(datasetId = datasetId, datasetVersionId = datasetVersionId, ...))
}

#' CreateDatasetsVersionsFromDataEngineWorkspaceState
#'
#' Create dataset version from Data Engine workspace
#'
#' Create a new dataset version for a specified dataset from a Data Engine workspace state. The new dataset version should have the same schema as the specified dataset.
#' @details This method invokes `POST /datasets/{datasetId}/versions/fromDataEngineWorkspaceState/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFromDataEngineWorkspaceStateCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsVersionsFromDataEngineWorkspaceState <- function(credentials, datasetName, datasetId, workspaceStateId, doSnapshot, ...) {
  datasetCreateFromWorkspaceState <- datarobot.apicore::DatasetCreateFromWorkspaceState$new(credentials = credentials, datasetName = datasetName, workspaceStateId = workspaceStateId, doSnapshot = doSnapshot, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFromDataEngineWorkspaceStateCreate(datasetCreateFromWorkspaceState = datasetCreateFromWorkspaceState, datasetId = datasetId, ...))
}

#' CreateDatasetsVersionsFromDataSource
#'
#' Create dataset version from Data Source
#'
#' Create a new version for the specified dataset from specified Data Source. The dataset must have been created from a compatible data source originally.
#' @details This method invokes `POST /datasets/{datasetId}/versions/fromDataSource/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFromDataSourceCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsVersionsFromDataSource <- function(password, dataSourceId, persistDataAfterIngestion, credentialId, datasetId, credentialData, categories, sampleSize, user, doSnapshot = TRUE, useKerberos = FALSE, ...) {
  datasource <- datarobot.apicore::Datasource$new(password = password, dataSourceId = dataSourceId, persistDataAfterIngestion = persistDataAfterIngestion, credentialId = credentialId, credentialData = credentialData, categories = categories, sampleSize = sampleSize, user = user, doSnapshot = doSnapshot, useKerberos = useKerberos, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFromDataSourceCreate(datasource = datasource, datasetId = datasetId, ...))
}

#' CreateDatasetsVersionsFromFile
#'
#' Create dataset version from file
#'
#' Create a new version for the specified dataset from a file.
#' @details This method invokes `POST /datasets/{datasetId}/versions/fromFile/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFromFileCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsVersionsFromFile <- function(file, datasetId, categories, ...) {
  datasetFromFile <- datarobot.apicore::DatasetFromFile$new(file = file, categories = categories, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFromFileCreate(datasetFromFile = datasetFromFile, datasetId = datasetId, ...))
}

#' CreateDatasetsVersionsFromHDFS
#'
#' Create dataset version from HDFS URL
#'
#' Create a new version for the specified dataset from a HDFS URL. The dataset must have been created from the same HDFS URL originally.
#' @details This method invokes `POST /datasets/{datasetId}/versions/fromHDFS/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFromHDFSCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsVersionsFromHDFS <- function(password, persistDataAfterIngestion, datasetId, namenodeWebhdfsPort, categories, user, url, doSnapshot = TRUE, ...) {
  hdfs <- datarobot.apicore::Hdfs$new(password = password, persistDataAfterIngestion = persistDataAfterIngestion, namenodeWebhdfsPort = namenodeWebhdfsPort, categories = categories, user = user, url = url, doSnapshot = doSnapshot, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFromHDFSCreate(hdfs = hdfs, datasetId = datasetId, ...))
}

#' CreateDatasetsVersionsFromLatestVersion
#'
#' Create dataset version from data source
#'
#' Create a new version of the specified dataset from the latest dataset version. This will reuse the same source of the data that was previously used. Not supported for datasets that were previously loaded from an uploaded file. If the dataset is currently a remote dataset, it will be converted to a snapshot dataset. NOTE: if the current version uses a Data Source, the user and password must be specified so the data can be accessed.
#' @details This method invokes `POST /datasets/{datasetId}/versions/fromLatestVersion/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFromLatestVersionCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsVersionsFromLatestVersion <- function(password, credentials, credentialId, datasetId, credentialData, categories, user, useLatestSuccess = FALSE, useKerberos = FALSE, ...) {
  fromLatest <- datarobot.apicore::FromLatest$new(password = password, credentials = credentials, credentialId = credentialId, useLatestSuccess = useLatestSuccess, credentialData = credentialData, categories = categories, user = user, useKerberos = useKerberos, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFromLatestVersionCreate(fromLatest = fromLatest, datasetId = datasetId, ...))
}

#' CreateDatasetsVersionsFromURL
#'
#' Create dataset version from URL
#'
#' Create a new version for the specified dataset from specified URL. The dataset must have been created from the same URL originally.
#' @details This method invokes `POST /datasets/{datasetId}/versions/fromURL/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFromURLCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsVersionsFromURL <- function(persistDataAfterIngestion, datasetId, categories, sampleSize, url, doSnapshot = TRUE, ...) {
  url <- datarobot.apicore::Url$new(persistDataAfterIngestion = persistDataAfterIngestion, categories = categories, sampleSize = sampleSize, url = url, doSnapshot = doSnapshot, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFromURLCreate(url = url, datasetId = datasetId, ...))
}

#' CreateDatasetsVersionsFromVersion
#'
#' Create dataset version from previous version
#'
#' Create a new version of the specified dataset from the specified dataset version. This will reuse the same source of the data that was previously used. Not supported for datasets that were previously loaded from an uploaded file. If the dataset is currently a remote dataset, it will be converted to a snapshot dataset. NOTE: If the specified version uses a Data Source, the user and password must be specified so the data can be accessed.
#' @details This method invokes `POST /datasets/{datasetId}/versions/{datasetVersionId}/fromVersion/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsFromVersionCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateDatasetsVersionsFromVersion <- function(password, credentials, credentialId, datasetId, credentialData, categories, user, datasetVersionId, useKerberos = FALSE, ...) {
  fromSpecific <- datarobot.apicore::FromSpecific$new(password = password, credentials = credentials, credentialId = credentialId, credentialData = credentialData, categories = categories, user = user, useKerberos = useKerberos, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsFromVersionCreate(fromSpecific = fromSpecific, datasetId = datasetId, datasetVersionId = datasetVersionId, ...))
}

#' ListDatasetsVersions
#'
#' List dataset versions
#'
#' List all versions associated with given datasetId and which match the specified query parameters.
#' @details This method invokes `GET /datasets/{datasetId}/versions/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsVersions <- function(orderBy, datasetId, category, offset = 0, limit = 100, filterFailed = "false", ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsList(offset = offset, limit = limit, orderBy = orderBy, datasetId = datasetId, category = category, filterFailed = filterFailed, ...))
}

#' ListDatasetsVersionsProjects
#'
#' Get dataset projects by version
#'
#' Retrieves a dataset's projects for the specified catalog dataset and dataset version id.
#' @details This method invokes `GET /datasets/{datasetId}/versions/{datasetVersionId}/projects/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsProjectsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListDatasetsVersionsProjects <- function(datasetId, datasetVersionId, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsProjectsList(offset = offset, limit = limit, datasetId = datasetId, datasetVersionId = datasetVersionId, ...))
}

#' RetrieveDatasetsVersions
#'
#' Get dataset details by version
#'
#' Retrieves the details of the dataset with given ID and version ID.
#' @details This method invokes `GET /datasets/{datasetId}/versions/{datasetVersionId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method DatasetsVersionsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveDatasetsVersions <- function(datasetId, datasetVersionId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$DatasetsVersionsRetrieve(datasetId = datasetId, datasetVersionId = datasetVersionId, ...))
}

#' CreateUserBlueprintsBulkValidations
#'
#' Validate many user blueprints.
#'
#' Validate many user blueprints, optionally using a specific project. Any non-existent or inaccessible user blueprints will be ignored.
#' @details This method invokes `POST /userBlueprintsBulkValidations/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsBulkValidationsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserBlueprintsBulkValidations <- function(userBlueprintIds, projectId, ...) {
  userBlueprintBulkValidationRequest <- datarobot.apicore::UserBlueprintBulkValidationRequest$new(userBlueprintIds = userBlueprintIds, projectId = projectId, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsBulkValidationsCreate(userBlueprintBulkValidationRequest = userBlueprintBulkValidationRequest, ...))
}

#' CreateUserBlueprints
#'
#' Create a user blueprint.
#'
#' Create a user blueprint
#' @details This method invokes `POST /userBlueprints/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserBlueprints <- function(blueprint, description, modelType, projectId, isInplaceEditor = FALSE, getDynamicLabels = FALSE, decompressedBlueprint = FALSE, saveToCatalog = TRUE, ...) {
  userBlueprintCreate <- datarobot.apicore::UserBlueprintCreate$new(blueprint = blueprint, isInplaceEditor = isInplaceEditor, getDynamicLabels = getDynamicLabels, decompressedBlueprint = decompressedBlueprint, description = description, modelType = modelType, projectId = projectId, saveToCatalog = saveToCatalog, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsCreate(userBlueprintCreate = userBlueprintCreate, ...))
}

#' DeleteUserBlueprints
#'
#' Delete a user blueprint.
#'
#' Delete a user blueprint, specified by the userBlueprintId.
#' @details This method invokes `DELETE /userBlueprints/{userBlueprintId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsDelete in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteUserBlueprints <- function(userBlueprintId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsDelete(userBlueprintId = userBlueprintId, ...))
}

#' DeleteManyUserBlueprints
#'
#' Delete user blueprints.
#'
#' Delete user blueprints, specified by userBlueprintIds.
#' @details This method invokes `DELETE /userBlueprints/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsDeleteMany in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteManyUserBlueprints <- function(userBlueprintIds, ...) {
  userBlueprintsBulkDelete <- datarobot.apicore::UserBlueprintsBulkDelete$new(userBlueprintIds = userBlueprintIds, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsDeleteMany(userBlueprintsBulkDelete = userBlueprintsBulkDelete, ...))
}

#' CreateUserBlueprintsFromBlueprintId
#'
#' Clone a blueprint from a project.
#'
#' Clone a blueprint from a project.
#' @details This method invokes `POST /userBlueprints/fromBlueprintId/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsFromBlueprintIdCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserBlueprintsFromBlueprintId <- function(description, modelType, projectId, blueprintId, isInplaceEditor = FALSE, getDynamicLabels = FALSE, decompressedBlueprint = FALSE, saveToCatalog = TRUE, ...) {
  userBlueprintCreateFromBlueprintId <- datarobot.apicore::UserBlueprintCreateFromBlueprintId$new(isInplaceEditor = isInplaceEditor, getDynamicLabels = getDynamicLabels, decompressedBlueprint = decompressedBlueprint, description = description, modelType = modelType, projectId = projectId, blueprintId = blueprintId, saveToCatalog = saveToCatalog, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsFromBlueprintIdCreate(userBlueprintCreateFromBlueprintId = userBlueprintCreateFromBlueprintId, ...))
}

#' CreateUserBlueprintsFromCustomTaskVersionId
#'
#' Create a user blueprint from a single custom task.
#'
#' Create a user blueprint from a single custom task.
#' @details This method invokes `POST /userBlueprints/fromCustomTaskVersionId/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsFromCustomTaskVersionIdCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserBlueprintsFromCustomTaskVersionId <- function(customTaskVersionId, description, decompressedBlueprint = FALSE, saveToCatalog = TRUE, ...) {
  userBlueprintCreateFromCustomTaskVersionIdPayload <- datarobot.apicore::UserBlueprintCreateFromCustomTaskVersionIdPayload$new(decompressedBlueprint = decompressedBlueprint, customTaskVersionId = customTaskVersionId, description = description, saveToCatalog = saveToCatalog, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsFromCustomTaskVersionIdCreate(userBlueprintCreateFromCustomTaskVersionIdPayload = userBlueprintCreateFromCustomTaskVersionIdPayload, ...))
}

#' CreateUserBlueprintsFromUserBlueprintId
#'
#' Clone a user blueprint.
#'
#' Clone a user blueprint.
#' @details This method invokes `POST /userBlueprints/fromUserBlueprintId/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsFromUserBlueprintIdCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserBlueprintsFromUserBlueprintId <- function(userBlueprintId, description, modelType, projectId, isInplaceEditor = FALSE, getDynamicLabels = FALSE, decompressedBlueprint = FALSE, saveToCatalog = TRUE, ...) {
  userBlueprintCreateFromUserBlueprintId <- datarobot.apicore::UserBlueprintCreateFromUserBlueprintId$new(userBlueprintId = userBlueprintId, isInplaceEditor = isInplaceEditor, getDynamicLabels = getDynamicLabels, decompressedBlueprint = decompressedBlueprint, description = description, modelType = modelType, projectId = projectId, saveToCatalog = saveToCatalog, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsFromUserBlueprintIdCreate(userBlueprintCreateFromUserBlueprintId = userBlueprintCreateFromUserBlueprintId, ...))
}

#' ListUserBlueprintsInputTypes
#'
#' Retrieve input types.
#'
#' Retrieve the input types which can be used with User Blueprints.
#' @details This method invokes `GET /userBlueprintsInputTypes/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsInputTypesList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserBlueprintsInputTypes <- function(...) {
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsInputTypesList(...))
}

#' ListUserBlueprints
#'
#' List user blueprints.
#'
#' Fetch a list of the user blueprints the current user has access to
#' @details This method invokes `GET /userBlueprints/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserBlueprints <- function(projectId, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsList(offset = offset, limit = limit, projectId = projectId, ...))
}

#' PatchUserBlueprints
#'
#' Update a user blueprint.
#'
#' Update a user blueprint
#' @details This method invokes `PATCH /userBlueprints/{userBlueprintId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsPatch in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchUserBlueprints <- function(blueprint, userBlueprintId, description, modelType, projectId, isInplaceEditor = FALSE, getDynamicLabels = FALSE, decompressedBlueprint = FALSE, saveToCatalog = TRUE, ...) {
  userBlueprintUpdate <- datarobot.apicore::UserBlueprintUpdate$new(blueprint = blueprint, isInplaceEditor = isInplaceEditor, getDynamicLabels = getDynamicLabels, decompressedBlueprint = decompressedBlueprint, description = description, modelType = modelType, projectId = projectId, saveToCatalog = saveToCatalog, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsPatch(userBlueprintUpdate = userBlueprintUpdate, userBlueprintId = userBlueprintId, ...))
}

#' CreateUserBlueprintsProjectBlueprints
#'
#' Add user blueprints to a project.
#'
#' Add a list of user blueprints, by id, to a specified (by id) project's repository.
#' @details This method invokes `POST /userBlueprintsProjectBlueprints/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsProjectBlueprintsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserBlueprintsProjectBlueprints <- function(userBlueprintIds, projectId, deleteAfter = FALSE, describeFailures = FALSE, ...) {
  userBlueprintAddToMenu <- datarobot.apicore::UserBlueprintAddToMenu$new(deleteAfter = deleteAfter, userBlueprintIds = userBlueprintIds, describeFailures = describeFailures, projectId = projectId, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsProjectBlueprintsCreate(userBlueprintAddToMenu = userBlueprintAddToMenu, ...))
}

#' RetrieveUserBlueprints
#'
#' Retrieve a user blueprint.
#'
#' Retrieve a user blueprint
#' @details This method invokes `GET /userBlueprints/{userBlueprintId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsRetrieve in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveUserBlueprints <- function(userBlueprintId, projectId, isInplaceEditor = FALSE, getDynamicLabels = FALSE, decompressedBlueprint = FALSE, editMode = FALSE, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsRetrieve(userBlueprintId = userBlueprintId, isInplaceEditor = isInplaceEditor, getDynamicLabels = getDynamicLabels, decompressedBlueprint = decompressedBlueprint, editMode = editMode, projectId = projectId, ...))
}

#' ListUserBlueprintsSharedRoles
#'
#' Get a list of users, groups and organizations that have an access to this user blueprint
#'
#' Get a list of users, groups and organizations that have an access to this user blueprint
#' @details This method invokes `GET /userBlueprints/{userBlueprintId}/sharedRoles/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsSharedRolesList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserBlueprintsSharedRoles <- function(userBlueprintId, shareRecipientType, name, id, offset = 0, limit = 10, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsSharedRolesList(userBlueprintId = userBlueprintId, shareRecipientType = shareRecipientType, offset = offset, name = name, limit = limit, id = id, ...))
}

#' PatchManyUserBlueprintsSharedRoles
#'
#' Share a user blueprint with a user, group, or organization
#'
#' Share a user blueprint with a user, group, or organization
#' @details This method invokes `PATCH /userBlueprints/{userBlueprintId}/sharedRoles/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsSharedRolesPatchMany in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchManyUserBlueprintsSharedRoles <- function(userBlueprintId, roles, operation, ...) {
  sharedRolesUpdate <- datarobot.apicore::SharedRolesUpdate$new(roles = roles, operation = operation, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsSharedRolesPatchMany(userBlueprintId = userBlueprintId, sharedRolesUpdate = sharedRolesUpdate, ...))
}

#' CreateUserBlueprintsTaskParameters
#'
#' Validate task parameters.
#'
#' Validate that each value assigned to specified task parameters are valid.
#' @details This method invokes `POST /userBlueprintsTaskParameters/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsTaskParametersCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserBlueprintsTaskParameters <- function(taskCode, outputMethod, taskParameters, projectId, ...) {
  userBlueprintTaskParameterValidation <- datarobot.apicore::UserBlueprintTaskParameterValidation$new(taskCode = taskCode, outputMethod = outputMethod, taskParameters = taskParameters, projectId = projectId, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsTaskParametersCreate(userBlueprintTaskParameterValidation = userBlueprintTaskParameterValidation, ...))
}

#' ListUserBlueprintsTasks
#'
#' Retrieve tasks for blueprint construction.
#'
#' Retrieve the available tasks, organized into categories, which can be used to create or modify User Blueprints.
#' @details This method invokes `GET /userBlueprintsTasks/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsTasksList in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserBlueprintsTasks <- function(userBlueprintId, blueprintId, projectId, ...) {
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsTasksList(userBlueprintId = userBlueprintId, blueprintId = blueprintId, projectId = projectId, ...))
}

#' CreateUserBlueprintsValidations
#'
#' Validate a user blueprint.
#'
#' Validate a user blueprint
#' @details This method invokes `POST /userBlueprintsValidations/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserBlueprintsValidationsCreate in [datarobot.apicore::AiCatalogApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserBlueprintsValidations <- function(blueprint, projectId, isInplaceEditor = FALSE, ...) {
  userBlueprintValidation <- datarobot.apicore::UserBlueprintValidation$new(blueprint = blueprint, isInplaceEditor = isInplaceEditor, projectId = projectId, validateParams = TRUE)
  return(datarobot.apicore::AiCatalogApi$new()$UserBlueprintsValidationsCreate(userBlueprintValidation = userBlueprintValidation, ...))
}
