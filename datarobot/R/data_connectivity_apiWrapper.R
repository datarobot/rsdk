# Copyright 2021 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.28.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech


# NOTE: This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
# Do not edit the file manually.

# API Wrapper methods for DataConnectivity
# To use these methods without modification, DR endpoint and token
# should be set as environment variables. This will automatically
# happen when you call `datarobot::ConnectToDataRobot()`.



#' CreateExternalDataDriverFile
#'
#' Upload JDBC driver from file.
#'
#' Upload JDBC driver from file. Only Java archive (.jar) files are supported.
#'
#' @seealso The method ExternalDataDriverFileCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
CreateExternalDataDriverFile <- function(file, ...) {
  driverUploadRequest <- datarobot.apicore::DriverUploadRequest$new(file = file, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataDriverFileCreate(driverUploadRequest = driverUploadRequest))
}

#' ListExternalDataDriversConfiguration
#'
#' Driver configuration details.
#'
#' Retrieve the driver configuration with the specified ID.
#'
#' @seealso The method ExternalDataDriversConfigurationList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListExternalDataDriversConfiguration <- function(driverId, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataDriversConfigurationList(driverId = driverId))
}

#' CreateExternalDataDrivers
#'
#' Create a new JDBC driver.
#'
#' Create a new JDBC driver. To create connections from fields, rather than supplying a JDBC URL, use 'configurationId'. When using 'configurationId', do not include 'canonicalName' or 'className' as they are part of the driver configuration. Specifying a 'version' is required as it is used in the construction of the canonicalName.
#'
#' @seealso The method ExternalDataDriversCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
CreateExternalDataDrivers <- function(baseNames, localJarUrls, className, configurationId, version, canonicalName, ...) {
  createDriverRequest <- datarobot.apicore::CreateDriverRequest$new(baseNames = baseNames, localJarUrls = localJarUrls, className = className, configurationId = configurationId, version = version, canonicalName = canonicalName, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataDriversCreate(createDriverRequest = createDriverRequest))
}

#' DeleteExternalDataDrivers
#'
#' Delete the driver.
#'
#' Delete the driver with given ID if it is not used by any data store or data source.
#'
#' @seealso The method ExternalDataDriversDelete in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
DeleteExternalDataDrivers <- function(driverId, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataDriversDelete(driverId = driverId))
}

#' ListDrivers
#'
#' List drivers
#'
#' Fetch all drivers a user has access to.
#'
#' @seealso The method ExternalDataDriversList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListDrivers <- function(...) {
  routeString <- UrlJoin("externalDataDrivers")
  # start of generated addition
  # end of generated addition
  drivers <- DataRobotGET(routeString)
  drivers <- GetServerDataInRows(drivers)
  as.dataRobotDrivers(drivers)
}

#' PatchExternalDataDrivers
#'
#' Update properties of an existing JDBC Driver.
#'
#' Update properties of an existing JDBC driver. To change the canonicalName and className, you must first remove the driver configuration, if it exists, as its properties will otherwise override name changes.
#'
#' @seealso The method ExternalDataDriversPatch in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
PatchExternalDataDrivers <- function(skipConfigVerification, baseNames, driverId, localJarUrls, className, configurationId, version, removeConfig, canonicalName, ...) {
  updateDriverRequest <- datarobot.apicore::UpdateDriverRequest$new(skipConfigVerification = skipConfigVerification, baseNames = baseNames, localJarUrls = localJarUrls, className = className, configurationId = configurationId, version = version, removeConfig = removeConfig, canonicalName = canonicalName, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataDriversPatch(updateDriverRequest = updateDriverRequest, driverId = driverId))
}

#' GetDriver
#'
#' Retrieve driver details.
#'
#' Retrieve driver details by ID.
#'
#' @seealso The method ExternalDataDriversRetrieve in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
GetDriver <- function(driverId, ...) {
  routeString <- UrlJoin("externalDataDrivers", driverId)
  # start of generated addition
  # end of generated addition
  driver <- DataRobotGET(routeString)
  as.dataRobotDriver(driver)
}

#' ListExternalDataSourcesAccessControl
#'
#' Get data source's access control list
#'
#' Get a list of users, groups and organizations who have access to this data source and their roles.
#'
#' @seealso The method ExternalDataSourcesAccessControlList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListExternalDataSourcesAccessControl <- function(dataSourceId, userId, username, offset = 0, limit = 0, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataSourcesAccessControlList(dataSourceId = dataSourceId, offset = offset, limit = limit, userId = userId, username = username))
}

#' PatchManyExternalDataSourcesAccessControl
#'
#' Update data source's access controls
#'
#' Set roles for users on this data source. Note that when granting access to a data source, access to the corresponding data store as a \"CONSUMER\" will also be granted.
#'
#' @seealso The method ExternalDataSourcesAccessControlPatchMany in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
PatchManyExternalDataSourcesAccessControl <- function(dataSourceId, data, ...) {
  sharingUpdateOrRemoveWithGrant <- datarobot.apicore::SharingUpdateOrRemoveWithGrant$new(data = data, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataSourcesAccessControlPatchMany(dataSourceId = dataSourceId, sharingUpdateOrRemoveWithGrant = sharingUpdateOrRemoveWithGrant))
}

#' CreateDataSource
#'
#' Create a data source.
#'
#' Create a fully configured source of data which could be used for datasets and projects creation.A `data source` specifies, via SQL query or selected table and schema data, which data to extract from the `data connection` (the location of data within a given endpoint) to use for modeling or predictions. A `data source` has one `data connection` and one `connector` but can have many `datasets`. To test the SQL query before creating the data source,use :http:post:`/api/v2/externalDataStores/(dataStoreId)/verifySQL/`.
#'
#' @seealso The method ExternalDataSourcesCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
CreateDataSource <- function(type, canonicalName, dataStoreId, query = NULL, table = NULL, schema = NULL, partitionColumn = NULL, fetchSize = NULL, ...) {
  body <- list(
    type = type,
    canonicalName = canonicalName,
    params = list(
      dataStoreId = dataStoreId,
      query = query,
      table = table,
      schema = schema,
      partitionColumn = partitionColumn,
      fetchSize = fetchSize
    )
  )
  body$params <- Filter(Negate(is.null), body$params)
  routeString <- UrlJoin("externalDataSources")
  # start of generated addition
  # end of generated addition
  response <- DataRobotPOST(routeString, body = body, encode = "json")
  as.dataRobotDataSource(response)
}

#' DeleteDataSource
#'
#' Delete the data source.
#'
#' Delete the data source with given ID if it is not used by any dataset.
#'
#' @seealso The method ExternalDataSourcesDelete in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
DeleteDataSource <- function(dataSourceId, ...) {
  if (is(dataSourceId, "dataRobotDataSource")) {
    dataSourceId <- dataSourceId$id
  }
  routeString <- UrlJoin("externalDataSources", dataSourceId)
  # start of generated addition
  # end of generated addition
  DataRobotDELETE(routeString)
  invisible(NULL)
}

#' ListDataSources
#'
#' List data sources.
#'
#' Return detailed list of available data sources.
#'
#' @seealso The method ExternalDataSourcesList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListDataSources <- function(type = "jdbc", ...) {
  routeString <- UrlJoin("externalDataSources")
  # start of generated addition
  query <- get0("query", mode = "list", ifnotfound = list())
  query$type <- type
  # end of generated addition
  dataSources <- DataRobotGET(routeString, query = query)
  dataSources <- GetServerDataInRows(dataSources)
  as.dataRobotDataSources(dataSources)
}

#' UpdateDataSource
#'
#' Update the data source.
#'
#' Update the data source with given ID. To test the SQL query before updating the data source,use :http:post:`/api/v2/externalDataStores/(dataStoreId)/verifySQL/`.
#'
#' @seealso The method ExternalDataSourcesPatch in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
UpdateDataSource <- function(dataSourceId, canonicalName = NULL, dataStoreId = NULL, query = NULL, table = NULL, schema = NULL, partitionColumn = NULL, fetchSize = NULL, ...) {
  if (is(dataSourceId, "dataRobotDataSource")) {
    dataSourceId <- dataSourceId$id
  }
  body <- list(
    canonicalName = canonicalName,
    params = list(
      dataStoreId = dataStoreId,
      query = query,
      table = table,
      schema = schema,
      partitionColumn = partitionColumn,
      fetchSize = fetchSize
    )
  )
  body <- Filter(Negate(is.null), body)
  body$params <- Filter(Negate(is.null), body$params)
  routeString <- UrlJoin("externalDataSources", dataSourceId)
  # start of generated addition
  # end of generated addition
  response <- DataRobotPATCH(routeString, body = body, encode = "json")
  as.dataRobotDataSource(response)
}

#' ListExternalDataSourcesPermissions
#'
#' Describe data source permissions.
#'
#' Describe what permissions current user has for given data source.
#'
#' @seealso The method ExternalDataSourcesPermissionsList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListExternalDataSourcesPermissions <- function(dataSourceId, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataSourcesPermissionsList(dataSourceId = dataSourceId))
}

#' GetDataSource
#'
#' Data source details.
#'
#' Return details of the existing data source with given ID, including SQL query or selected table and schema data, which fully describe which data to extract and from which location.
#'
#' @seealso The method ExternalDataSourcesRetrieve in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
GetDataSource <- function(dataSourceId, ...) {
  routeString <- UrlJoin("externalDataSources", dataSourceId)
  # start of generated addition
  # end of generated addition
  dataSource <- DataRobotGET(routeString)
  as.dataRobotDataSource(dataSource)
}

#' ListExternalDataSourcesSharedRoles
#'
#' Get data source's access control list
#'
#' Get a list of users, groups and organizations who have access to this data source and their roles.
#'
#' @seealso The method ExternalDataSourcesSharedRolesList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListExternalDataSourcesSharedRoles <- function(shareRecipientType, dataSourceId, name, id, offset = 0, limit = 10, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataSourcesSharedRolesList(shareRecipientType = shareRecipientType, dataSourceId = dataSourceId, offset = offset, name = name, limit = limit, id = id))
}

#' PatchManyExternalDataSourcesSharedRoles
#'
#' Modify data source shared roles.
#'
#' Grant access, remove access or update roles for organizations, groups or users on this data source. Up to 100 roles may be set per array in a single request.
#'
#' @seealso The method ExternalDataSourcesSharedRolesPatchMany in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
PatchManyExternalDataSourcesSharedRoles <- function(dataSourceId, roles, operation, ...) {
  sharedRolesUpdateWithGrant <- datarobot.apicore::SharedRolesUpdateWithGrant$new(roles = roles, operation = operation, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataSourcesSharedRolesPatchMany(dataSourceId = dataSourceId, sharedRolesUpdateWithGrant = sharedRolesUpdateWithGrant))
}

#' PatchManyExternalDataStoresAccessControl
#'
#' Update data store's controls
#'
#' Set roles for users on this data store.
#'
#' @seealso The method ExternalDataStoresAccessControlPatchMany in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
PatchManyExternalDataStoresAccessControl <- function(data, dataStoreId, ...) {
  sharingUpdateOrRemoveWithGrant <- datarobot.apicore::SharingUpdateOrRemoveWithGrant$new(data = data, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataStoresAccessControlPatchMany(sharingUpdateOrRemoveWithGrant = sharingUpdateOrRemoveWithGrant, dataStoreId = dataStoreId))
}

#' CreateExternalDataStoresColumns
#'
#' Retrieves a data store's data columns.
#'
#' Retrieves a data store's data columns.
#'
#' @seealso The method ExternalDataStoresColumnsCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
CreateExternalDataStoresColumns <- function(schema, password, dataStoreId, catalog, query, credentialId, user, table, ...) {
  dataStoreColumnsList <- datarobot.apicore::DataStoreColumnsList$new(schema = schema, password = password, catalog = catalog, query = query, credentialId = credentialId, user = user, table = table, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataStoresColumnsCreate(dataStoreColumnsList = dataStoreColumnsList, dataStoreId = dataStoreId))
}

#' CreateDataStore
#'
#' Create a data store.
#'
#' Create a data store which includes a name and a driver ID or a connector ID. The driver would be configured by a JDBC URL or by jdbc fields; The connector would be configured by connection fields.
#'
#' @seealso The method ExternalDataStoresCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
CreateDataStore <- function(type, canonicalName, driverId, jdbcUrl, ...) {
  body <- list(
    type = type,
    canonicalName = canonicalName,
    params = list(
      driverId = driverId,
      jdbcUrl = jdbcUrl
    )
  )
  routeString <- UrlJoin("externalDataStores")
  # start of generated addition
  # end of generated addition
  response <- DataRobotPOST(routeString, body = body, encode = "json")
  as.dataRobotDataStore(response)
}

#' ListExternalDataStoresCredentials
#'
#' List credentials associated with the specified data store.
#'
#' Returns a list of credentials associated with the specified data store.
#'
#' @seealso The method ExternalDataStoresCredentialsList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListExternalDataStoresCredentials <- function(types, dataStoreId, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataStoresCredentialsList(types = types, offset = offset, dataStoreId = dataStoreId, limit = limit))
}

#' DeleteDataStore
#'
#' Delete the data store.
#'
#' Delete the data store with given ID if it is not used by any data source.
#'
#' @seealso The method ExternalDataStoresDelete in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
DeleteDataStore <- function(dataStoreId, ...) {
  if (is(dataStoreId, "dataRobotDataStore")) {
    dataStoreId <- dataStoreId$id
  }
  routeString <- UrlJoin("externalDataStores", dataStoreId)
  # start of generated addition
  # end of generated addition
  DataRobotDELETE(routeString)
  invisible(NULL)
}

#' ListDataStores
#'
#' List data stores.
#'
#' Return list with details of the existing data stores available for the user.
#'
#' @seealso The method ExternalDataStoresList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListDataStores <- function(offset = 0, limit = 100, type = "jdbc", showHidden = "False", ...) {
  routeString <- UrlJoin("externalDataStores")
  # start of generated addition
  query <- get0("query", mode = "list", ifnotfound = list())
  query$offset <- offset
  query$limit <- limit
  query$type <- type
  query$showHidden <- showHidden
  # end of generated addition
  dataStores <- DataRobotGET(routeString, query = query)
  dataStores <- GetServerDataInRows(dataStores)
  return(dataStores)
}

#' UpdateDataStore
#'
#' Updates a data store configuration.
#'
#' Updates a data store configuration.
#'
#' @seealso The method ExternalDataStoresPatch in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
UpdateDataStore <- function(dataStoreId, canonicalName = NULL, driverId = NULL, jdbcUrl = NULL, ...) {
  if (is(dataStoreId, "dataRobotDataStore")) {
    dataStoreId <- dataStoreId$id
  }
  params <- list(driverId = driverId, jdbcUrl = jdbcUrl)
  params <- Filter(Negate(is.null), params)
  body <- list(canonicalName = canonicalName)
  body <- Filter(Negate(is.null), body)
  body$params <- params
  routeString <- UrlJoin("externalDataStores", dataStoreId)
  # start of generated addition
  # end of generated addition
  response <- DataRobotPATCH(routeString, body = body, encode = "json")
  as.dataRobotDataStore(response)
}

#' ListExternalDataStoresPermissions
#'
#' Describe data store permissions.
#'
#' Describe what permissions current user has for given data store.
#'
#' @seealso The method ExternalDataStoresPermissionsList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListExternalDataStoresPermissions <- function(dataStoreId, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataStoresPermissionsList(dataStoreId = dataStoreId))
}

#' GetDataStore
#'
#' Data store details.
#'
#' A configured connection to a database - it has a name and a specified driver. The driver may be specified by a JDBC URL or connection parameters if the driver was created with the parameter configuration.
#'
#' @seealso The method ExternalDataStoresRetrieve in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
GetDataStore <- function(dataStoreId, ...) {
  routeString <- UrlJoin("externalDataStores", dataStoreId)
  # start of generated addition
  # end of generated addition
  driver <- DataRobotGET(routeString)
  as.dataRobotDataStore(driver)
}

#' GetDataStoreSchemas
#'
#' Retrieves a data store's data schemas.
#'
#' Retrieves a data store's data schemas.
#'
#' @seealso The method ExternalDataStoresSchemasCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
GetDataStoreSchemas <- function(dataStoreId, username, password, ...) {
  if (is(dataStoreId, "dataRobotDataStore")) {
    dataStoreId <- dataStoreId$id
  }
  routeString <- UrlJoin("externalDataStores", dataStoreId, "schemas")
  body <- list(user = username, password = password)
  # start of generated addition
  # end of generated addition
  return(DataRobotPOST(routeString, body = body))
}

#' ListExternalDataStoresSharedRoles
#'
#' Get data store's access control list
#'
#' Get a list of users who have access to this data store and their roles on the data store.
#'
#' @seealso The method ExternalDataStoresSharedRolesList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListExternalDataStoresSharedRoles <- function(shareRecipientType, dataStoreId, name, id, offset = 0, limit = 10, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataStoresSharedRolesList(shareRecipientType = shareRecipientType, dataStoreId = dataStoreId, offset = offset, name = name, limit = limit, id = id))
}

#' PatchManyExternalDataStoresSharedRoles
#'
#' Modify data store shared roles.
#'
#' Grant access, remove access or update roles for organizations, groups or users on this data store. Up to 100 roles may be set per array in a single request.
#'
#' @seealso The method ExternalDataStoresSharedRolesPatchMany in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
PatchManyExternalDataStoresSharedRoles <- function(dataStoreId, roles, operation, ...) {
  sharedRolesUpdateWithGrant <- datarobot.apicore::SharedRolesUpdateWithGrant$new(roles = roles, operation = operation, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataStoresSharedRolesPatchMany(dataStoreId = dataStoreId, sharedRolesUpdateWithGrant = sharedRolesUpdateWithGrant))
}

#' GetDataStoreTables
#'
#' Retrieves a data store's database tables (including views).
#'
#' Retrieves a data store's database tables (including views).
#'
#' @seealso The method ExternalDataStoresTablesCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
GetDataStoreTables <- function(dataStoreId, username, password, schema = NULL, ...) {
  if (is(dataStoreId, "dataRobotDataStore")) {
    dataStoreId <- dataStoreId$id
  }
  routeString <- UrlJoin("externalDataStores", dataStoreId, "tables")
  body <- list(user = username, password = password)
  if (!is.null(schema)) {
    body$schema <- schema
  }
  # start of generated addition
  # end of generated addition
  return(DataRobotPOST(routeString, body = body))
}

#' TestDataStore
#'
#' Tests data store connection.
#'
#' Tests the ability to connect to a data store with specified authentication.
#'
#' @seealso The method ExternalDataStoresTestCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
TestDataStore <- function(dataStoreId, username, password, ...) {
  if (is(dataStoreId, "dataRobotDataStore")) {
    dataStoreId <- dataStoreId$id
  }
  body <- list(
    user = username,
    password = password
  )
  routeString <- UrlJoin("externalDataStores", dataStoreId, "test")
  # start of generated addition
  # end of generated addition
  response <- DataRobotPOST(routeString, body = body)
  if (identical(response$message, "Connection successful")) {
    TRUE
  } else {
    stop("Received ", response$message, " from server.")
  }
}

#' CreateExternalDataStoresVerifySQL
#'
#' Verifies a SQL query for a data store.
#'
#' Executes the SQL query on the data store, returning a small number of rows (max 999). Use this for quick query execution validation and exploring results, not for capturing an entire result set.
#'
#' @seealso The method ExternalDataStoresVerifySQLCreate in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
CreateExternalDataStoresVerifySQL <- function(password, maxRows, dataStoreId, query, credentialId, user, useKerberos = FALSE, ...) {
  dataStoreSqlVerify <- datarobot.apicore::DataStoreSQLVerify$new(password = password, maxRows = maxRows, query = query, credentialId = credentialId, user = user, useKerberos = useKerberos, validateParams = TRUE)
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDataStoresVerifySQLCreate(dataStoreSqlVerify = dataStoreSqlVerify, dataStoreId = dataStoreId))
}

#' ListExternalDriverConfigurations
#'
#' List available driver configurations.
#'
#' Retrieve matching driver configurations based on query.
#'
#' @seealso The method ExternalDriverConfigurationsList in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
ListExternalDriverConfigurations <- function(showHidden = "False", offset = 0, limit = 100, type = "jdbc", ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDriverConfigurationsList(showHidden = showHidden, offset = offset, limit = limit, type = type))
}

#' RetrieveExternalDriverConfigurations
#'
#' Driver configuration details.
#'
#' Retrieve the driver configuration with the specified ID.
#'
#' @seealso The method ExternalDriverConfigurationsRetrieve in [datarobot.apicore::DataConnectivityApi], which this function wraps.
#' @family DataConnectivity
#' @export
RetrieveExternalDriverConfigurations <- function(configurationId, ...) {
  return(datarobot.apicore::DataConnectivityApi$new()$ExternalDriverConfigurationsRetrieve(configurationId = configurationId))
}
