# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech


# NOTE: This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
# Do not edit the file manually.

# API Wrapper methods for Credentials
# To use these methods without modification, DR endpoint and token
# should be set as environment variables. This will automatically
# happen when you call `datarobot::ConnectToDataRobot()`.



#' ListForCredentialCredentialsAssociations
#'
#' List all objects associated with the specified credentials.
#'
#' List all objects associated with specified credentials for the current user.
#' @details This method invokes `GET /credentials/{credentialId}/associations/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CredentialsAssociationsListForCredential in [datarobot.apicore::CredentialsApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListForCredentialCredentialsAssociations <- function(types, credentialId, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::CredentialsApi$new()$CredentialsAssociationsListForCredential(types = types, offset = offset, limit = limit, credentialId = credentialId, ...))
}

#' PatchManyCredentialsAssociations
#'
#' Add objects associated with credentials
#'
#' Add objects associated with credentials
#' @details This method invokes `PATCH /credentials/{credentialId}/associations/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CredentialsAssociationsPatchMany in [datarobot.apicore::CredentialsApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchManyCredentialsAssociations <- function(credentialsToRemove, credentialId, credentialsToAdd, ...) {
  credentialsAssociationUpdate <- datarobot.apicore::CredentialsAssociationUpdate$new(credentialsToRemove = credentialsToRemove, credentialsToAdd = credentialsToAdd, validateParams = TRUE)
  return(datarobot.apicore::CredentialsApi$new()$CredentialsAssociationsPatchMany(credentialsAssociationUpdate = credentialsAssociationUpdate, credentialId = credentialId, ...))
}

#' CreateCredentials
#'
#' Store a new set of credentials which can be used for data sources creation.
#'
#' Store a new set of credentials.
#' @details This method invokes `POST /credentials/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CredentialsCreate in [datarobot.apicore::CredentialsApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateCredentials <- function(credentialsBody, ...) {
  return(datarobot.apicore::CredentialsApi$new()$CredentialsCreate(credentialsBody = credentialsBody, ...))
}

#' DeleteCredentials
#'
#' Delete the credentials set.
#'
#' Delete the credential set matching the specified ID if it is not used by any data connection or batch prediction job.
#' @details This method invokes `DELETE /credentials/{credentialId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CredentialsDelete in [datarobot.apicore::CredentialsApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteCredentials <- function(credentialId, ...) {
  return(datarobot.apicore::CredentialsApi$new()$CredentialsDelete(credentialId = credentialId, ...))
}

#' ListCredentials
#'
#' List credentials.
#'
#' List all sets of credentials available for given user.
#' @details This method invokes `GET /credentials/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CredentialsList in [datarobot.apicore::CredentialsApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListCredentials <- function(types, offset = 0, limit = 100, ...) {
  return(datarobot.apicore::CredentialsApi$new()$CredentialsList(types = types, offset = offset, limit = limit, ...))
}

#' PatchCredentials
#'
#' Update specified credentials
#'
#' Update specified credentials
#' @details This method invokes `PATCH /credentials/{credentialId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CredentialsPatch in [datarobot.apicore::CredentialsApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchCredentials <- function(gcpKey, clientId, oauthIssuerUrl, awsAccessKeyId, oauthIssuerType, tokenName, description, oauthScopes, token, password, name, credentialId, clientSecret, personalAccessToken, snowflakeAccountName, user, azureConnectionString, awsSessionToken, refreshToken, awsSecretAccessKey, ...) {
  credentialsUpdate <- datarobot.apicore::CredentialsUpdate$new(gcpKey = gcpKey, clientId = clientId, oauthIssuerUrl = oauthIssuerUrl, awsAccessKeyId = awsAccessKeyId, oauthIssuerType = oauthIssuerType, tokenName = tokenName, description = description, oauthScopes = oauthScopes, token = token, password = password, name = name, clientSecret = clientSecret, personalAccessToken = personalAccessToken, snowflakeAccountName = snowflakeAccountName, user = user, azureConnectionString = azureConnectionString, awsSessionToken = awsSessionToken, refreshToken = refreshToken, awsSecretAccessKey = awsSecretAccessKey, validateParams = TRUE)
  return(datarobot.apicore::CredentialsApi$new()$CredentialsPatch(credentialsUpdate = credentialsUpdate, credentialId = credentialId, ...))
}

#' RetrieveCredentials
#'
#' Retrieve the credentials set.
#'
#' Return the credential set matching the specified ID.
#' @details This method invokes `GET /credentials/{credentialId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method CredentialsRetrieve in [datarobot.apicore::CredentialsApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveCredentials <- function(credentialId, ...) {
  return(datarobot.apicore::CredentialsApi$new()$CredentialsRetrieve(credentialId = credentialId, ...))
}
