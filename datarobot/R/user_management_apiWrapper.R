# Copyright 2021-2022 DataRobot, Inc. and its affiliates.
#
# All rights reserved.
#
# DataRobot, Inc.
#
# This is proprietary source code of DataRobot, Inc. and its
# affiliates.

# Public API
#
# DataRobot's Public facing API
#
# The version of the OpenAPI document: 2.29.0
# Contact: api-maintainer@datarobot.com
# Generated by: https://openapi-generator.tech


# NOTE: This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
# Do not edit the file manually.

# API Wrapper methods for UserManagement
# To use these methods without modification, DR endpoint and token
# should be set as environment variables. This will automatically
# happen when you call `datarobot::ConnectToDataRobot()`.



#' CreateGroups
#'
#' Create user group
#'
#' Create a new user group.
#' @details This method invokes `POST /groups/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method GroupsCreate in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateGroups <- function(accessRoleId, name, description, orgId, email, ...) {
  userGroupCreate <- datarobot.apicore::UserGroupCreate$new(accessRoleId = accessRoleId, name = name, description = description, orgId = orgId, email = email, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$GroupsCreate(userGroupCreate = userGroupCreate, ...))
}

#' DeleteGroups
#'
#' Delete user group
#'
#' Delete the user group.
#' @details This method invokes `DELETE /groups/{groupId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method GroupsDelete in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteGroups <- function(groupId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$GroupsDelete(groupId = groupId, ...))
}

#' DeleteManyGroups
#'
#' Delete multiple user groups
#'
#' Delete the user groups.
#' @details This method invokes `DELETE /groups/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method GroupsDeleteMany in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteManyGroups <- function(groups, ...) {
  userGroupBulkDelete <- datarobot.apicore::UserGroupBulkDelete$new(groups = groups, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$GroupsDeleteMany(userGroupBulkDelete = userGroupBulkDelete, ...))
}

#' PatchGroups
#'
#' Update user group
#'
#' Update the user group.
#' @details This method invokes `PATCH /groups/{groupId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method GroupsPatch in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchGroups <- function(accessRoleId, maxAppCount, maxUploadSize, groupId, description, orgId, maxWorkers, maxEdaWorkers, maxUploadSizeCatalog, maxRam, name, maxCustomDeployments, email, accountPermissions, ...) {
  userGroupUpdate <- datarobot.apicore::UserGroupUpdate$new(accessRoleId = accessRoleId, maxAppCount = maxAppCount, maxUploadSize = maxUploadSize, description = description, orgId = orgId, maxWorkers = maxWorkers, maxEdaWorkers = maxEdaWorkers, maxUploadSizeCatalog = maxUploadSizeCatalog, maxRam = maxRam, name = name, maxCustomDeployments = maxCustomDeployments, email = email, accountPermissions = accountPermissions, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$GroupsPatch(userGroupUpdate = userGroupUpdate, groupId = groupId, ...))
}

#' RetrieveGroups
#'
#' Retrieve user group
#'
#' Retrieve the user group.
#' @details This method invokes `GET /groups/{groupId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method GroupsRetrieve in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveGroups <- function(groupId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$GroupsRetrieve(groupId = groupId, ...))
}

#' CreateGroupsUsers
#'
#' Add users to group
#'
#' Add users to the group.
#' @details This method invokes `POST /groups/{groupId}/users/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method GroupsUsersCreate in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateGroupsUsers <- function(groupId, users, ...) {
  modifyUsersInGroup <- datarobot.apicore::ModifyUsersInGroup$new(users = users, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$GroupsUsersCreate(groupId = groupId, modifyUsersInGroup = modifyUsersInGroup, ...))
}

#' DeleteManyGroupsUsers
#'
#' Remove users from group
#'
#' Remove users from the group.
#' @details This method invokes `DELETE /groups/{groupId}/users/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method GroupsUsersDeleteMany in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteManyGroupsUsers <- function(groupId, users, ...) {
  modifyUsersInGroup <- datarobot.apicore::ModifyUsersInGroup$new(users = users, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$GroupsUsersDeleteMany(groupId = groupId, modifyUsersInGroup = modifyUsersInGroup, ...))
}

#' ListGroupsUsers
#'
#' List users in group
#'
#' List users in the group.
#' @details This method invokes `GET /groups/{groupId}/users/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method GroupsUsersList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListGroupsUsers <- function(groupId, orderBy, isAdmin, isActive, offset = 0, limit = 50, namePart = "", ...) {
  return(datarobot.apicore::UserManagementApi$new()$GroupsUsersList(offset = offset, groupId = groupId, limit = limit, orderBy = orderBy, isAdmin = isAdmin, isActive = isActive, namePart = namePart, ...))
}

#' ListOrganizationsJobs
#'
#' List organization jobs
#'
#' List currently running jobs belonging to this organization.
#' @details This method invokes `GET /organizations/{organizationId}/jobs/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method OrganizationsJobsList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListOrganizationsJobs <- function(organizationId, offset = 0, limit = 0, ...) {
  return(datarobot.apicore::UserManagementApi$new()$OrganizationsJobsList(organizationId = organizationId, offset = offset, limit = limit, ...))
}

#' ListOrganizations
#'
#' List organizations
#'
#' List organizations available in the system.
#' @details This method invokes `GET /organizations/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method OrganizationsList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListOrganizations <- function(offset = 0, limit = 100, namePart = "", ...) {
  return(datarobot.apicore::UserManagementApi$new()$OrganizationsList(offset = offset, limit = limit, namePart = namePart, ...))
}

#' RetrieveOrganizations
#'
#' Retrieve organization
#'
#' Retrieve the organization details.
#' @details This method invokes `GET /organizations/{organizationId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method OrganizationsRetrieve in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveOrganizations <- function(organizationId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$OrganizationsRetrieve(organizationId = organizationId, ...))
}

#' CreateOrganizationsUsers
#'
#' Add user to an existing organization.
#'
#' Add user to an existing organization. A user can only be part of one organization at a time
#' @details This method invokes `POST /organizations/{organizationId}/users/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method OrganizationsUsersCreate in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateOrganizationsUsers <- function(organizationId, lastName, firstName, password, accessRoleId, language, accessRoleIds, username, orgAdmin = FALSE, requireClickthrough = FALSE, create = FALSE, ...) {
  organizationUser <- datarobot.apicore::OrganizationUser$new(lastName = lastName, firstName = firstName, password = password, accessRoleId = accessRoleId, orgAdmin = orgAdmin, requireClickthrough = requireClickthrough, create = create, language = language, accessRoleIds = accessRoleIds, username = username, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$OrganizationsUsersCreate(organizationId = organizationId, organizationUser = organizationUser, ...))
}

#' DeleteOrganizationsUsers
#'
#' Remove user from organization
#'
#' Remove the user from the organization.
#' @details This method invokes `DELETE /organizations/{organizationId}/users/{userId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method OrganizationsUsersDelete in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteOrganizationsUsers <- function(organizationId, userId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$OrganizationsUsersDelete(organizationId = organizationId, userId = userId, ...))
}

#' ListOrganizationsUsers
#'
#' List organization users
#'
#' List memberships in this organization.
#' @details This method invokes `GET /organizations/{organizationId}/users/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method OrganizationsUsersList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListOrganizationsUsers <- function(organizationId, offset = 0, limit = 0, ...) {
  return(datarobot.apicore::UserManagementApi$new()$OrganizationsUsersList(organizationId = organizationId, offset = offset, limit = limit, ...))
}

#' PatchOrganizationsUsers
#'
#' Patch organization's user
#'
#' Patch organization's user. Only system or the organization admin can perform this operation.
#' @details This method invokes `PATCH /organizations/{organizationId}/users/{userId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method OrganizationsUsersPatch in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchOrganizationsUsers <- function(organizationId, maxWorkers, accessRoleId, orgAdmin, accessRoleIds, userId, expirationDate, activated, ...) {
  organizationUserPatch <- datarobot.apicore::OrganizationUserPatch$new(maxWorkers = maxWorkers, accessRoleId = accessRoleId, orgAdmin = orgAdmin, accessRoleIds = accessRoleIds, expirationDate = expirationDate, activated = activated, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$OrganizationsUsersPatch(organizationId = organizationId, organizationUserPatch = organizationUserPatch, userId = userId, ...))
}

#' RetrieveOrganizationsUsers
#'
#' Retrieve user from organization
#'
#' Retrieve the user from this organization.
#' @details This method invokes `GET /organizations/{organizationId}/users/{userId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method OrganizationsUsersRetrieve in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveOrganizationsUsers <- function(organizationId, userId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$OrganizationsUsersRetrieve(organizationId = organizationId, userId = userId, ...))
}

#' CreateUserCleanupJobs
#'
#' Users permanent delete.
#'
#' Schedules a job which permanently delete the users represented by the provided users ids or organization id. Number of users to be deleted in one go is restricted by DELETED_USERS_BATCH_LIMIT system setting.
#' @details This method invokes `POST /userCleanupJobs/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupJobsCreate in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserCleanupJobs <- function(orphansOwner, userIds, orgId, ...) {
  usersPermadelete <- datarobot.apicore::UsersPermadelete$new(orphansOwner = orphansOwner, userIds = userIds, orgId = orgId, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupJobsCreate(usersPermadelete = usersPermadelete, ...))
}

#' DeleteUserCleanupJobs
#'
#' Cancel users perma-deletion.
#'
#' Cancel permanent deletion of the selected users.
#' @details This method invokes `DELETE /userCleanupJobs/{statusId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupJobsDelete in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteUserCleanupJobs <- function(statusId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupJobsDelete(statusId = statusId, ...))
}

#' RetrieveUserCleanupJobs
#'
#' Retrieve users perma-delete job status.
#'
#' Get async status of the users perma-delete job.
#' @details This method invokes `GET /userCleanupJobs/{statusId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupJobsRetrieve in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveUserCleanupJobs <- function(statusId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupJobsRetrieve(statusId = statusId, ...))
}

#' CreateUserCleanupPreviewJobs
#'
#' Users permanent delete preview.
#'
#' Schedules a job for building preview for users selected for permanent deletion. Number of users to be deleted in one go is restricted by DELETED_USERS_BATCH_LIMIT system setting.
#' @details This method invokes `POST /userCleanupPreviewJobs/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupPreviewJobsCreate in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUserCleanupPreviewJobs <- function(orphansOwner, userIds, orgId, ...) {
  usersPermadelete <- datarobot.apicore::UsersPermadelete$new(orphansOwner = orphansOwner, userIds = userIds, orgId = orgId, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupPreviewJobsCreate(usersPermadelete = usersPermadelete, ...))
}

#' DeleteUserCleanupPreviewJobs
#'
#' Cancel users perma-delete preview building.
#'
#' Cancel scheduled users permanent delete preview building.
#' @details This method invokes `DELETE /userCleanupPreviewJobs/{statusId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupPreviewJobsDelete in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteUserCleanupPreviewJobs <- function(statusId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupPreviewJobsDelete(statusId = statusId, ...))
}

#' RetrieveUserCleanupPreviewJobs
#'
#' Retrieve users perma-delete preview job status.
#'
#' Get async status of the users perma-delete preview building job.
#' @details This method invokes `GET /userCleanupPreviewJobs/{statusId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupPreviewJobsRetrieve in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
RetrieveUserCleanupPreviewJobs <- function(statusId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupPreviewJobsRetrieve(statusId = statusId, ...))
}

#' ListUserCleanupPreviewsContent
#'
#' Users permanent delete extended preview.
#'
#' Retrieve users permanent delete extended preview.
#' @details This method invokes `GET /userCleanupPreviews/{reportId}/content/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupPreviewsContentList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserCleanupPreviewsContent <- function(reportId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupPreviewsContentList(reportId = reportId, ...))
}

#' DeleteUserCleanupPreviews
#'
#' Delete users permanent delete report.
#'
#' Delete users permanent delete report.
#' @details This method invokes `DELETE /userCleanupPreviews/{reportId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupPreviewsDelete in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteUserCleanupPreviews <- function(reportId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupPreviewsDelete(reportId = reportId, ...))
}

#' ListUserCleanupPreviewsDeleteParams
#'
#' Users permanent delete report parameters.
#'
#' Retrieve users permanent delete report parameters.
#' @details This method invokes `GET /userCleanupPreviews/{reportId}/deleteParams/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupPreviewsDeleteParamsList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserCleanupPreviewsDeleteParams <- function(reportId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupPreviewsDeleteParamsList(reportId = reportId, ...))
}

#' ListUserCleanupPreviewsStatistics
#'
#' Users permanent delete preview statistics.
#'
#' Retrieve users permanent delete preview statistics.
#' @details This method invokes `GET /userCleanupPreviews/{reportId}/statistics/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupPreviewsStatisticsList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserCleanupPreviewsStatistics <- function(reportId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupPreviewsStatisticsList(reportId = reportId, ...))
}

#' ListUserCleanupSummariesContent
#'
#' Users permanent delete extended summary.
#'
#' Retrieve users permanent delete extended summary.
#' @details This method invokes `GET /userCleanupSummaries/{reportId}/content/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupSummariesContentList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserCleanupSummariesContent <- function(reportId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupSummariesContentList(reportId = reportId, ...))
}

#' DeleteUserCleanupSummaries
#'
#' Delete users permanent delete report.
#'
#' Delete users permanent delete report.
#' @details This method invokes `DELETE /userCleanupSummaries/{reportId}/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupSummariesDelete in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
DeleteUserCleanupSummaries <- function(reportId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupSummariesDelete(reportId = reportId, ...))
}

#' ListUserCleanupSummariesDeleteParams
#'
#' Users permanent delete report parameters.
#'
#' Retrieve users permanent delete report parameters.
#' @details This method invokes `GET /userCleanupSummaries/{reportId}/deleteParams/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupSummariesDeleteParamsList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserCleanupSummariesDeleteParams <- function(reportId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupSummariesDeleteParamsList(reportId = reportId, ...))
}

#' ListUserCleanupSummariesStatistics
#'
#' Users permanent delete summary statistics.
#'
#' Retrieve users permanent delete summary statistics.
#' @details This method invokes `GET /userCleanupSummaries/{reportId}/statistics/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UserCleanupSummariesStatisticsList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUserCleanupSummariesStatistics <- function(reportId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UserCleanupSummariesStatisticsList(reportId = reportId, ...))
}

#' CreateUsers
#'
#' Create a User and add them to an organisation.
#'
#' Create a User and add them to an organisation.
#' @details This method invokes `POST /users/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UsersCreate in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
CreateUsers <- function(lastName, country, maxUploadSize, externalId, maxIdleWorkers, language, maxGpuWorkers, organizationId, maxWorkers, firstName, password, maxUploadSizeCatalog, requireClickthroughAgreement, userType, defaultCatalogDatasetSampleSize, username, expirationDate, ...) {
  userCreate <- datarobot.apicore::UserCreate$new(lastName = lastName, country = country, maxUploadSize = maxUploadSize, externalId = externalId, maxIdleWorkers = maxIdleWorkers, language = language, maxGpuWorkers = maxGpuWorkers, organizationId = organizationId, maxWorkers = maxWorkers, firstName = firstName, password = password, maxUploadSizeCatalog = maxUploadSizeCatalog, requireClickthroughAgreement = requireClickthroughAgreement, userType = userType, defaultCatalogDatasetSampleSize = defaultCatalogDatasetSampleSize, username = username, expirationDate = expirationDate, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$UsersCreate(userCreate = userCreate, ...))
}

#' ListUsersLimits
#'
#' Get the rate limits and account limits for a user
#'
#' User limits are those limits on an account that can be updated per-user. Rate limits that can't be modified per-user are not returned here.
#' @details This method invokes `GET /users/{userId}/limits/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UsersLimitsList in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
ListUsersLimits <- function(userId, ...) {
  return(datarobot.apicore::UserManagementApi$new()$UsersLimitsList(userId = userId, ...))
}

#' PatchManyUsersLimits
#'
#' Update the rate limits and account limits for a user
#'
#' User limits are those limits on an account that can be updated per-user.Rate limits that can't be modified per-user are not returned here.
#' @details This method invokes `PATCH /users/{userId}/limits/` in the DataRobot Public API.
#'
#' @param ... Additional keyword arguments to be passed on to the `datarobot.apicore` and `httr` libraries.
#'
#' @seealso The method UsersLimitsPatchMany in [datarobot.apicore::UserManagementApi], which invokes the same underlying API endpoint.
#' @export
#' @md
PatchManyUsersLimits <- function(accountLimits, rateLimits, userId, ...) {
  userLimitsPatchBody <- datarobot.apicore::UserLimitsPatchBody$new(accountLimits = accountLimits, rateLimits = rateLimits, validateParams = TRUE)
  return(datarobot.apicore::UserManagementApi$new()$UsersLimitsPatchMany(userLimitsPatchBody = userLimitsPatchBody, userId = userId, ...))
}
